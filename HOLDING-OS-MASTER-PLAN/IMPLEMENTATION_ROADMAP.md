# IMPLEMENTATION_ROADMAP — DXB GLOBAL AI-NATIVE HOLDING OS

> **OPEN ROWS HERE ALSO LIVE ON THE BOARD (2026-07-27, U38).** [[00-BOARD-OPEN-WORK]] is the
> one page that answers "what is left" across all six ledgers. Close a row here and correct the
> board in the SAME session; a stale row on either side is the same class of lie as an invented
> number.

> Dalga 5 · Yazar: Fable 5 bizzat · Kaynak hüküm: direktif madde 14 (uygulama sırası teknik detayla) + madde 17 (Faz 1-11) + madde 18/v6 (model dağılımı) · Üst: [[MASTER_PLAN]] §7 (pencere stratejisi — bu dosya P2/P3'ün adım tablosudur) · Kardeşler: [[BACKUP_PLAN]] (S1/S2 senaryoları), [[TEST_STRATEGY]], [[ACCEPTANCE_CRITERIA]]
> Yürütme sözleşmesi: adımlar SIRAYLA; her adım kanıt komutuyla kapanır (evidence-before-done); ✓ işareti BU DOSYAYA işlenir (canlı ilerleme kaydı). Devralan yazar (2026-07-25'ten itibaren **Opus 5**, U20) herhangi bir ✓-sınırından sürdürebilir — istisnalar §5.

## 1. Amaç

Korpus (31 spec) → çalışan Holding OS dönüşümünün adım-adım, kanıt-komutlu, devralma-işaretli yürütme planı. Pencere 2'de Fable bizzat koştu (12 Temmuz'a kadar); Pencere 3'te **Opus 5** ilk ✓'siz adımdan sürer (U20 — yedek model katmanı yok).

## 2. Gereksinimler

- G1. Her adım: dosya yolları + normatif spec referansı + "çalıştır → şu çıktıyı gör" kanıtı.
- G2. Sıra MASTER_PLAN §7 P2 sırasına sadıktır (değer-öncelikli: görünür kokpit erken); CEO Faz eşlemesi her blokta.
- G3. Yazar-only işler işaretli (persona ailesi — K2: inşaat yazarı bizzat, 2026-07-25'ten itibaren Opus 5); ⛔ kritik kararlar işaretli.
- G4. Paralellik yalnız devralma penceresinde ve yalnız işaretli bloklar arasında (aynı dosyaya iki el yasak — BACKUP_PLAN §8).

## 3. Mimari (yürütme blokları ve bağımlılık)

```
E1 tokens ──► E2 shell ──► E3 overview+liveops v1 (mevcut 18 tablo, gerçek veri)
E4 WS-A migrations (0020x→0026x + view'lar + seed) ──► E6 control seam ──► E7 routing
E5 persona çekirdeği (FABLE-ONLY) ─ bağımsız, E4'ten sonra herhangi bir an
E8 observability yazıcıları ──► E9 workflow+project+library+approval ──► E10 hook engine
E11 cost intelligence ──► E12 kalan modül sayfaları + widget ──► E13 kabul turu
```

## 4. ADIM TABLOSU (bağlayıcı — ilerleme işareti bu tabloya)

Model kolonu: **F** = yalnız Fable · F/O = Fable öncelikli, Opus devralabilir. Durum: `—` bekliyor · `✓` kanıtla kapandı.

**Gap-audit bağlayıcı kuralları ([[00-CEO-DIRECTIVE-GAP-AUDIT]] + [[GAP-AUDIT]], 2026-07-11):** (1) Her E bloğu kendi domain'inin ModuleWaiting placeholder'ını KENDİ adımında kapatır; kabul ölçüsü direktif §2.1'in 8-şartlı DoD matrisidir (gerçek domain sorgusu, drill-down, gerektiğinde control fn + audit + Broadcast zinciri, loading/empty/error/stale/permission-denied durumları, EN/TR, rota-başı test, ölü buton 0). (2) "Menü var / route açılıyor / DB'de satır var / persona dosyası var" ≠ tamam; kanıtsız done = otomatik RET. (3) Final kabulde ModuleWaiting = **0** (E12.3 kapısı). (4) **CEO K1 kararı (2026-07-11; yazar 2026-07-25'te devredildi — U20):** modül/placeholder kapanış yürütücüsü yalnız **inşaat yazarı (Opus 5) ve GPT 5.6 solo** — başka model modül kapatamaz; F/O kolonundaki devralma bu kapsamda GPT 5.6 solo demektir. (5) **CEO K2 kararı (2026-07-11; aynı devir):** TÜM personalar (yeni + legacy v2 dalgaları) **inşaat yazarı bizzat** yazar; hr-factory ilk oluşumda yazarlık yapmaz; yetişmeyen "yazım bekliyor" listesine düşer, kalite asla düşürülmez. *(Tarihsel: 196 personanın tamamını Fable 5 bizzat yazdı, 2026-07-11/12 dalgaları.)*

### E1 — Design tokens + primitives (CEO Faz 6 zemini) — [[DESIGN_SYSTEM]] normatif

| # | İş | Dosyalar | Kanıt | Model | Durum |
|---|----|----------|-------|-------|-------|
| E1.1 | Tailwind v4 token seti (obsidian/graphite yüzeyler, champagne gold, 8-durum matrisi, motion) | `apps/dashboard/src/styles/tokens.css` + `tailwind` config | `pnpm --filter dashboard build` → yeşil; token sınıfları derlendi | F/O | ✓ 2026-07-10 (build yeşil 14/14; derlenmiş CSS'te `--accent-champagne:#d8b98c` + `--t-fast:.12s` + `ease-refined` doğrulandı; utility ad uyarlaması U5) |
| E1.2 | Primitive komponentler (Surface, Panel, Stat, DataGrid, Badge, CommandItem — §33 zorunlu library çekirdeği) | `apps/dashboard/src/components/primitives/` | Storybook YOK (bilinçli); `/design-preview` rotası primitives'i gerçek tokenlarla listeler | F/O | ✓ 2026-07-10 (build yeşil, rota `○ /design-preview` çıktıda; hex-kaçağı 0; görsel kalite ⚠ CEO göz testi) |

### E2 — Command Center shell (CEO Faz 6) — [[CEO_COMMAND_CENTER_SPEC]] §9-11

| # | İş | Dosyalar | Kanıt | Model | Durum |
|---|----|----------|-------|-------|-------|
| E2.1 | Shell layout: sol nav (7 grup, §10 birebir), üst command bar (§11), canvas, sağ intelligence rayı, floating dock | `apps/dashboard/src/app/(command)/layout.tsx` + nav config | `curl -s localhost:3000/overview` → 200; ekranda 5 katman | F/O | ✓ 2026-07-10 (build'de `ƒ /overview`; curl 307→login→200 auth duvarı doğru; 5 katman gerçek DB sayılarıyla — görsel ⚠ CEO göz testi; search/⌘K→E4, emergency/Control→E6, TV→E12 notlu) |
| E2.2 | Rota iskeleti: §31 sayfa listesi route-bazlı (boş sayfa YOK — her rota gerçek veri veya "modül bekliyor" durum kartı + hedef tarihi) | `(command)/*/page.tsx` | rota sayısı ≥ 26: `find apps/dashboard/src/app/\(command\) -name page.tsx \| wc -l` | F/O | ✓ 2026-07-10 (38 rota, build 53/53 yeşil; her sayfa ModuleWaiting + hedef adım ID; `/approvals` legacy'de kalır — E2.3 geçişinde taşınır, çakışma yok) |
| E2.3 | Eski kokpit rotalarından geçiş (tek commit'te anahtar; rollback tek revert) | rota değişim commit'i | eski rotalar 308 → yeni shell | F/O | ✓ 2026-07-10 (giriş anahtarı: login→/overview tek satır, rollback tek revert; modül redirect'leri CC-SPEC §22 gereği parite anında açılır — /approvals→E9.3, /costs→E11.1, /tasks→E12.1; oturumlu yönlendirme ⚠ CEO ilk girişte) |
| E2.4 | Login 3D hover upgrade (davranış aynı, görsel katman) | login komponenti | build yeşil + ⚠ CEO göz testi | F/O | ✓ 2026-07-10 (CSS-3D pointer parallax: sahne ≤1.6° tilt, katman derinliği far 6px/near 14px+Z, ışık imleci izler, reduced-motion korunur; kapı hedefi /overview; build yeşil; görsel ⚠ CEO göz testi) |

### E3 — Executive Overview + Live Operations v1 (CEO Faz 6) — gerçek veri, mevcut şema

| # | İş | Dosyalar | Kanıt | Model | Durum |
|---|----|----------|-------|-------|-------|
| E3.1 | `v_exec_overview_v1` view (mevcut 18 tablodan: tasks, approvals, cost_ledger, agents, budget_state) — migration `0019x-b` | `db/migrations/` | `psql -c "SELECT * FROM v_exec_overview_v1;"` → 1 satır özet | F/O | ✓ 2026-07-10 (`20260710235000_exec_overview_v1.sql` uygulandı+idempotent; SELECT → 1 satır: 6 aktif görev, 6 onay, 153 ajan, €0.46 bugün; security_invoker + grant idiomu 0018 birebir) |
| E3.2 | Overview sayfası (§12): özet sayılar + HER sayı tıklanır → kaynak listesine iner (drill-down v1) | `(command)/overview/` | Playwright: sayıya tıkla → detay rotası açılır | F/O | ✓ 2026-07-10 (view'dan tek round-trip; 4 Stat + nabız/işgücü/bütçe panelleri, HER değer drillHref'li — hedefsiz özet render edilemez (prop zorunlu); hard-stop kritik bandı; build yeşil; tık-testi ⚠ oturum ister — E13.1 L5'te) |
| E3.3 | Live Operations v1 (§14): task_events akışı + Broadcast `approvals` mevcut kanalı; `ops:live` E8'de gelir | `(command)/operations/` | UI'da canlı task olayı ≤5 sn | F/O | ✓ 2026-07-10 (`/live`: 40 olay snapshot + dxb:task_events+approvals aboneliği, dürüst stale göstergesi; kanıt: probe INSERT → realtime.messages `dxb:task_events` 1 satır ≤30sn; UI ≤5sn görsel ⚠ CEO) |

### E4 — WS-A veri omurgası (CEO Faz 2 zemini) — [[DATA_MODEL]] §20 sırası

| # | İş | Dosyalar | Kanıt | Model | Durum |
|---|----|----------|-------|-------|-------|
| E4.1 | 0020x org + 0021x settings + 0022x observability (+0022x-b) | `db/migrations/` | DATA_MODEL §22 komutları: `\dt` 6 aile kanıtı + append-only reddi | F/O | ✓ 2026-07-11 (4 dosya `20260711002000..002250`, idempotent re-run OK; append-only: authenticated UPDATE decision_log → permission denied; aktivasyon kapısı: personasız active → exception; fallback döngüsü → exception; **kayıtlı uyarlama:** departments PK'sı slug — `id uuid UNIQUE` eklendi, FK'lar ona bağlı) |
| E4.2 | 0023x workflow/project (+steps_snapshot, 0023x-b) + 0024x library (+0024x-b) | `db/migrations/` | `\dt project_* library_*` tam liste | F/O | ✓ 2026-07-11 (4 dosya `20260711002300..002450`; `\dt` project_members/milestones/risks + task_dependencies + library_items/grants/usage_log/change_log; §22 6-aile grep → 6; dependency döngüsü → exception) |
| E4.3 | 0025x api_support (view kataloğu + control_idempotency + notify_broadcast) + 0026x memory köprüleri | `db/migrations/` | API_CONTRACTS §24 idempotency kanıtı; `\d memory_index` 2 kolon | F/O | ✓ 2026-07-11 (`20260711002500+002600`; 6 katalog view SELECT'li; `notify_broadcast('system','probe.fired',..)` → realtime.messages `dxb:system` 1 satır; `\d memory_index` → run_id+scope; control_idempotency tablosu var — §24 curl kanıtı E6.1 fn'leriyle gelir, tablo-katman hazır) |
| E4.4 | Seed: companies(dxb-global) + mevcut departments→company bağla + DXB Global OS ilk proje kaydı (dogfood) | `db/seed/` | `SELECT count(*) FROM companies;` → 1; `v_project_command` dolu | F/O | ✓ 2026-07-11 (`db/seed/20260711_holding_core.sql` idempotent; companies → 1; bağsız departman → 0; v_project_command → dxb-global-os · owner agents-orchestrator; memory_source → 4) |
| E4.5 | Overview/LiveOps v2: view'ları 0025x kataloğuna geçir (`v_exec_overview`, `v_live_ops`) | E3 sayfaları | drill-down yeni ailelere iner | F/O | ✓ 2026-07-11 (layout+overview → v_exec_overview, /live → v_live_ops label'lı; pulse panelinde 3 yeni-aile drill: /ops/runtime · /ops/workflows · /ops/projects — Playwright tıkla → /ops/projects açıldı, "Projects active 1" seed satırı canlı; build 54/54, console 0/0, design-audit 3/3 PASS en 365=tr 365; kanıt: evidence/e45-overview-1440.png + e45-live-1440.png) |

### E5 — Persona çekirdeği (CEO Faz 3-5) — **FABLE-ONLY** ([[EMPLOYEE_PERSONA_STANDARD]], [[HR_OPERATING_SYSTEM_SPEC]])

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E5.0 | Kadro gap matrisi (direktif §3.1-3.2): 153 legacy + 5 v2 + registry tek envanter; persona GÖVDELERİ okunarak `capability \| dept \| required role \| existing \| gerçek kapsam \| duplicate \| missing authority/workflow/skill \| risk \| decision \| target phase` matrisi; §3.2 15-aile karşılaştırması; keep/merge/rewrite/add kararları; role_level + manager_id backfill planı | `HOLDING-OS-MASTER-PLAN/WORKFORCE-GAP-MATRIX.md` | matris satırı = envanter toplamı; her satırda decision; 15 aile kapsanmış; CEO karar özeti | **F — persona yazım dalgaları bu matris olmadan başlayamaz** | ✓ 2026-07-11 (WORKFORCE-GAP-MATRIX.md, Fable bizzat; 153 karar: keep 92 + move 36 + merge 6 + retire→library 15 + promote 4, aritmetik doğrulama dosyada 92+36+6+15+4=153; §3.2 15 aile satırı grep → 15; ADD 34; hedef org 18 dept + 5 pod, aktif 166; §6 CEO karar özeti 4 madde — retire/research-kapanışı/hedef-org onayı bekliyor; envanter kanıtı scratchpad persona-inventory.txt 153+4 satır) **⚠ RECORDED CORRECTION (2026-07-12, external audit + CEO order [[00-CEO-DIRECTIVE-MUST-ROSTER]]):** the ✓ stands for the 153-legacy disposition; the directive's 12-column capability-matrix contract, independent MUST discovery ("floor, not ceiling"), and deputy/failover analysis were NOT delivered, and 5 matrix-promised ADD roles never materialized (verified: DB+files absent while closure declared 179/179). Remediation = **E5.7** ([[WORKFORCE-MUST-EXPANSION-PLAN]]). Not silent — audit findings F1-F6 recorded. |
| E5.1 | Persona derleyici: standard şablon → system-prompt compile fn (`packages/hr`) + personas tablo yazımı | derleme testi: örnek persona → geçerli system prompt | **F** (derleyici kodu F/O; şablon+kalite kapısı F) | ✓ 2026-07-11 (packages/hr: template+gate+compiler+fixtures, Fable bizzat inline; `pnpm --filter @dxb/hr test` → **28/28** — gate 22 vaka (11 eksik-bölüm + thin + header + jenerik + TODO + secret + injection EN/TR + hook-version 3 vaka) ≥13 şartı, compiler 6 (bit-eş determinizm + snapshot + compact §1-6/§11 tam + hook-sürüm etkisi + veri-bölgesi ayrımı + fail-closed); build tsc yeşil; migration `20260711003000_persona_fns.sql` idempotent 2× uygulandı: fn_persona_submit/fn_persona_gate + trg_agents_persona_passed; DB sondası 8/8 (pending→superseded→passed, pending-bind RED, secret RED, injection RED, bad-verdict RED) + realtime.messages dxb:org 3 olay + audit 3 satır — ROLLBACK'li sonda, DB temiz; **kayıtlı uyarlama:** quality_gate CHECK'e 'superseded' eklendi — PERSONA spec §27 hükmü) |
| E5.2 | Orkestratör personası v2 (Fable yazımı, quality_gate=passed) | `SELECT quality_gate FROM personas WHERE ...` → passed | **F — DEVREDİLEMEZ** | ✓ 2026-07-11 (persona "Atlas, Holding Orkestratörü" — 11 bölüm, ~95 rol-özgü hüküm, Fable bizzat; mekanik gate 0 failure (hook v1 kontrolüyle); gerçek derleme 11.419 karakter / 3 bölge; `fn_persona_submit` → f902629c… v1 · `fn_persona_gate('passed', Fable 5-soru verdict)` → kanıt sorgusu `quality_gate` → **passed**; `agents-orchestrator` bağlandı: persona_id + role_level='orchestrator' + hook_version='v1' (body 11.218 karakter DB'de — spec §22: git'te yaşamaz); Broadcast dxb:org persona.submitted+gated, audit verdict=passed; CEO derin-kalite direktifi 5-kontrolü roadmap üstü rapora işlendi; aktivasyon HR durum makinesine bırakıldı — dormant doğru) **+ E5.2b kayıtlı uyarlama (CEO emri 2026-07-11 ~16:00):** spec §22 TERSİNE — yazım kaynağı `personas/<dept>/<slug>.md` DOSYALARI, DB=runtime+gate kopyası (tek yön `scripts/sync-personas-to-db.sh`→fn_persona_submit); db-mirror/153-kart/export-script KALDIRILDI (legacy-metin-gömme ihlali); 5 product personası `personas/product/`a iade; "Atlas" adı kaldırıldı → v2 yeniden submit+gate **passed** (persona version=2, agents.persona_id yeniden bağlı); 153 çalışanın sicil-iskeleti dosyada (`gen-workforce-dossiers.sh`, kişilik yazarlığı YOK — ⏳ Fable-yazımı işaretli; kanıt: `sync --verify` → match 1 · skip 152 · fail 0 · PASS) |
| E5.3 | Departman müdürü personaları v2 (mevcut 11 dolu departman + E5.0 gap-matrisi zorunlu yeni head'ler; müdür role_level='head' + departments.director_id + worker'lara manager_id zinciri) | müdür-başı personas satırı + employee_records; director_id dolu; orphan worker 0 | **F — DEVREDİLEMEZ** | ✓ 2026-07-11 (E5.3b e4295f3 + D1-D6a dalga commit'leri; 19/19 head gate=passed, director 19 dept, orphan 0) **SPEC-GAP FIXED 2026-07-13 (rule 5, Fable in person):** this row's own evidence contract lists `employee_records`, yet the table had 0 rows (found during E6.3 wave 3c, recorded; now closed WITHOUT CEO order per the spec-gap ruling). `scripts/sync-employee-records.py` — file-first sicil sync: dossier §5 rows 10-31 (match by row NUMBER, label-language-proof) → employee_records VERBATIM (responsibilities/authority/decision_scope/expertise/methodology/reporting/quality/escalation + kpis/version_history as ref-jsonb); operational columns (performance/error/review/training) untouched — they fill with real operation; archived agents guarded out; upsert = re-runnable after any dossier edit. Evidence: 199 dossiers parsed / 0 skipped → `INSERT 0 1` ×199 → coverage 199/199 live agents; rerun idempotent (still 199, no dupes); `v_org_node_detail.has_employee_record` = t (panel zero-state honestly flips); spot-check cmo/orchestrator/AP-agent rows carry dossier text verbatim. |
| E5.4 | HR ilk oluşumu: HR personaları + persona-yazım standardı + kalite kapıları (Fable-sonrası fabrika) | HR dept aktif; hr fn'leri testli | **F — DEVREDİLEMEZ** | ✓ **E5.4a 2026-07-11** (Fable bizzat): 5 HR personası v2/v1 — TA + L&D + onboarding (move-rewrite) + Persona/Workforce Mimarı + Performans & Kalibrasyon Yöneticisi (ADD, migration `20260711006000` idempotent 2×, manager=CHRO, yetim-korkuluğu); mekanik gate **25/25 regresyonlu** (HEADER_RE isim-politikası hizası — kayıtlı uyarlama: E5.2b "Atlas" kaldırma kararının packages/hr yansıması test-önce yapıldı, `pnpm --filter @dxb/hr test` 28/28); fn_persona_submit ×5 → fn_persona_gate **passed ×5** (Fable 5-soru verdiktleri audit'te); persona_id+hook v1 bağlı 6/6 (chro dahil); sync --verify match 25 · diff 0 · fail 0; Broadcast dxb:org persona.gated ×5. **Kayıtlı uyarlama (sessiz değil):** "HR dept aktif" DoD'sinin fabrika-fn ayağı **E5.4b'ye ayrıştı** — fn_hr_* ailesi DB'de yoktu, persona yazarlığından bağımsız K1 işi; K2 devredilemez çekirdek (persona yazımı) burada kapandı |
| E5.4b | HR-fabrika fn altyapısı: `fn_hr_*` ailesi (create/grant/probation_task/evaluate/promote) + `v_hr_equipment_check`/`v_hr_roster`/`v_hr_probation_queue` + people-hr MCP profili (grants.json) + aktivasyon zinciri işletimi (HR spec §4-6) | `demo:hire` → 7/7 equipment + probation task; "donanımsız aktif" sorgusu → 0; people-hr ailesi draft→probation kanıtı | **F / GPT 5.6 solo (K1)** | ✓ 2026-07-12 (Fable in person; migration `20260712008000` idempotent 2× — **fn_hr_* ×5** (`\df` count 5) + **3 views** + settings registry ×6 + hr-sandbox project + **pgboss hr.* schedules ×4** (queue rows template-cloned, version-proof); **5 registered adaptations in header** (A1 budgets→settings_values, A2 LiteLLM key eventual/key_pending, A3 pending_library grant marker per spec §25, A4 schedule seeding + handler boot, A5 K2 persona task as tasks-row); job handlers `packages/hr/src/jobs.ts` ×4 + outbox-executor scheduler wiring (tsc -b green, `pnpm --filter @dxb/hr test` **28/28**); people-hr MCP profile added to gateway grants.json (house bus only, Fable-authored rationale in-file); **`demo:hire` PASS end-to-end in people-hr dept**: create → equipment 6/7 → draft→probation BLOCKED (gate proof) → key ready (labeled simulation) → persona submit+gate+bind → **equipment 7/7** → probation + sandbox task id → evaluate 0.5→probation / 0.9→**active** (G3 activation lock verified live — fires on unbound persona AND on active-persona removal) → **unequipped probation/active sweep → 0** → atomicity failure-injection (duplicate create raised, settings rows 3=3 no partials) → cleanup (roster 199 unchanged, remnants 0, orphan settings 0). ⚠ UNVERIFIED (recorded): real people-hr staff draft→probation waits on REAL LiteLLM key provisioning (proxy-side job — the demo's simulated step, labeled); scheduler handlers attach at next boot (code shipped, schedules already in DB) |
| E5.5 | Uzman personaları (153 legacy → v2 dalgaları + E5.0 gap-onaylı yeni personalar — G7); her persona direktif §3.3 sözleşme alanlarıyla (gerekçe, hiyerarşi, KPI, authority limits, model+budget, skill/MCP grants, autonomy, memory policy, quality rubric, dashboard bağı, activation proof) | dalga-başı sayım raporu + §3.3 alan denetimi | **F — TÜMÜ FABLE BİZZAT (CEO K2)**; yetişmeyen → "Fable-yazımı bekliyor" listesi CEO'ya (BACKUP_PLAN §1.6); kalite düşürülerek kapatılamaz | **D1 ✓ 2026-07-11 (Fable bizzat, 19 persona 3 commit):** ceo-office 5 (`dc714cd` — CoS move+promote→müdür, director_id(ceo) dolu; EOM+Board Secretary ADD; ExecSummary+DocGen v2; 3 slug taşıma migration `20260711007000` idempotent 2×) · strategy 5 ADD (`c4de0f6` — CorpDev, MIL [trend-researcher sınır kaydı], OKR-PM [PCM sınır kaydı], PEL pod [satış-DNA], GEL pod) · finance 9 (`a207d61` — 7 rewrite [AP para-çıkışı approval anayasası; FP&A €50-150 band bekçisi + tracker-merge; Tax DE/TR danışman-teyit] + Treasury&AR [giriş-çıkış asimetrisi] & Payroll ADD). Kanıt: mekanik gate **44/44 tam-regresyon** · sync --verify **match 44 · diff 0 · fail 0** · fn_persona_gate passed ×19 (5-soru verdiktleri audit'te) · ceo 6/6 + strategy 6/6 + finance 10/10 persona_id+hook v1 bağlı · yetim 0 · role_level NULL 0. Kalan dalgalar: D2 legal+risk+security → D3 data-ai+platform → D4 eng+quality → D5 marketing+pods+paid → D6 sales+revops+CS+PMO+design+product → E5.6 social-media. **Kalan v2 yazımı: 135** (CEO düzeltmesi 2026-07-11: 179 hedef − 44 gate'li = 135). **D2 ✓ 2026-07-11 (Fable bizzat, 16 persona 3 commit):** legal 6 (`eb49c23` — DE/TR Counsel, DPO, Policy Writer ADD + CCM & LCC dormant→v2) · risk-audit 3 (`eadf25a` — IA & AMRO ADD + AGA rewrite) · security 7 (`4506963` — IAM-SO & AI Safety/Red-Team Lead ADD + 5 rewrite [SE/TDE eng→sec, AIT, BSA, CA]). Kanıt: gate tam-regresyon 60 PASS/0 FAIL · sync --verify match 60 · diff 0 · fail 0 · fn_persona_gate passed ×16 · legal 7/7 + risk-audit 4/4 + security 8/8 bound · role_level NULL 0. **Kalan v2 yazımı: 119** (179 − 60). **D3 ✓ 2026-07-12 (Fable bizzat, 15 persona 1 commit `6aeaaa6`):** data-ai 10 (ai-engineer, data-engineer, analytics-reporter, mcp-builder, workflow-architect, model-evaluation-lead [Lead unvanı → senior_specialist, kayıtlı karar], knowledge-architect [zk-steward→matris adı], identity-graph-operator + **ADD: Prompt/Context Engineer** [K2 persona-yazarlık yasağı kimliğe işli] & **AI Observability & FinOps Analyst** [€50-150 band erken-uyarı gözü]) · platform 5 (database-optimizer [DBRE], sre, incident-response-commander, infrastructure-maintainer + **ADD: Backup & DR Officer** [E13.0 restore-drill sahibi — "test edilmemiş yedek YOK hükmünde" devri]). Migration `20260712001000` (11 slug taşıma + ADD 3 + manager zincirleri CAIO/Platform Head + korkuluklar; idempotent 2× kanıtlı). Kanıt: mekanik gate **15/15 PASS** (açık dosya listesiyle — iskelet indexOf tuzağına karşı) · sync --verify **match 75 · diff 0 · fail 0 · PASS** · fn_persona_gate passed ×15 (Fable 5-soru verdiktleri audit_log'da) · data-ai **11/11** + platform **6/6** persona_id+hook v1 bağlı · yetim 0 · role_level NULL 0 · gitleaks temiz. **Kalan v2 yazımı: 105** (sayım düzeltmesi: gate'li 75 match'in 1'i orkestratör dosya-aynası — kadro-personası 74; 179 − 74 = 105; D2'nin "60/119" sayımı aynayı kadroya saymıştı, kayıtlı düzeltme). **D3 kapanış ek-maddesi (CEO direktifi 2026-07-12, yan-kanal pencere işledi — madde-8 gereği bu kayda dahil):** dashboard'dan `agents.brain` değişimi MODEL_ROUTING_SPEC R8+§4b ve CEO_COMMAND_CENTER_SPEC'e (beyin rozeti + set_model köprüsü + drill-down) işlendi, commit `d890600`; glm-5.2 tekdüzeliği teşhisi kayıtlı (kolon DEFAULT'u, arıza değil); ek karar ~01:00: Sonnet runtime-beyin yasağı kaldırıldı (inşaat yazarlık yasağı sürer). **D4a ✓ 2026-07-12 (Fable in person, 11 engineering core-delivery personas, commit `2e740e4` + migration `20260712002000` [level decisions + guardrails, idempotent]):** closure record written late (authoring session hit rate limit — registered, not silent), evidence re-run in the 04:00 session. **D4b batch-1 ✓ 2026-07-12 (Fable in person, 4 client-stack personas, commit `b9416e6` — first English-native batch, 00-CEO-DIRECTIVE-LANGUAGE):** wechat-mini-program, feishu-integration, filament-optimization, solidity-smart-contract — gate PASS + fn_persona_gate passed ×4 (English 5-question verdicts) + persona_id/hook v1 bound. Systemic persona_path fix shipped with it: migration `20260712003000` repointed 44 bound rows (all prior waves incl. 18 directors) + 19 eng/quality rows to personas/<dept>/<slug>.md (idempotent 2×, guardrails 0/0; E12.5 sweeps the rest). Evidence: sync --verify match 90 · diff 0 · fail 0 · PASS; engineering bound 16/23; quality 1/9. **D4 ✓ CLOSED 2026-07-12 (Fable in person — 30 personas, 4 commits: `2e740e4` D4a 11 + `b9416e6` D4b-1 4 + `2612e9c` D4b-2 7 + `67d3b8f` D4c quality 8):** engineering 23/23 + quality 9/9 bound — both depts COMPLETE; total bound 105; sync --verify match 105 · diff 0 · fail 0 · PASS; fn_persona_gate passed ×30 (English verdicts from D4b on); orphan 0; role_level NULL 0; registered senior decisions ×4 (migration `20260712002000`). **Remaining v2 authorship: 75** (179 − 104). Next: **D5 marketing+pods+paid-media → D6 sales+revops+CS+PMO+design+product → E5.6 social-media 11.** **D5 ✓ CLOSED 2026-07-12 (Fable in person — 37 personas, 5 commits: `8cfc432` D5a growth/search 8 + `425fca1` D5b platform-channel 8 + `39d0d43` D5c China/APAC cluster 10 + `d1566ae` D5d remainder 5 + `55227c2` D5e paid-media 6):** marketing 32/32 + paid-media 7/7 bound — both depts COMPLETE; total bound 142; sync --verify match 142 · diff 0 · fail 0 · PASS; fn_persona_gate passed ×37 (English 5-question verdicts); orphan 1 = orchestrator (by design); role_level NULL 0; gitleaks clean ×5. Wave architecture: China cluster = strategy layer (China Market Localization Strategist, platform-DNA map) + nine surface owners + commerce hub (China E-Commerce Operator); sibling boundaries recorded both ways (TikTok/Douyin, X/Weibo, Instagram/Xiaohongshu, LinkedIn/Zhihu, SEO/Baidu); sales-DNA in all 37 (CEO marketing directive); paid-media budget-envelope constitution = money-out law operationalized; Developer Advocate specialized→marketing move; Outleteuro readiness recorded in Cross-Border E-Commerce. **Remaining v2 authorship: 38** (179 − 141). Next: **D6 sales 6 + revops 2 + CS 2 + PMO 4 + design 9 + product 4 = 27 → E5.6 social-media 11.** **D6 ✓ CLOSED 2026-07-12 (Fable in person — 27 personas, 6 commits: `b4a1dbf` D6a sales 6 + `2e27b78` D6b revops 2 + `747c5f1` D6c customer-success 2 + `b88f996` D6d project-management 4 + `3264f22` D6e design 9 + `c966afe` D6f product 4):** all six D6 departments COMPLETE — sales 7/7 + revops 3/3 + customer-success 3/3 + project-management 5/5 + design 10/10 + product 5/5 bound; total bound 169; 19 departments fully bound (only social-media remains: 1/12); sync --verify match 169 · diff 0 · fail 0 · PASS; fn_persona_gate passed ×27 (English 5-question verdicts in audit_log); orphan 0 (excl. orchestrator by design); role_level NULL 0; stale persona_path on bound rows 0; gitleaks clean ×6. Three matrix merges absorbed: sales-outreach→outbound-strategist, customer-service→support-responder, project-manager-senior→project-shepherd (merge rows archived). Boundary records shipped both ways: new-vs-existing customer (sales/CS), forecast methodology-vs-judgment (revops/sales), structure-vs-flow (PMO/orchestrator), inclusive pod split (CQ=strategy, visuals=production+scan), three-seat design split (structure/visual/implementation), product-vs-holding trend scope (product/strategy MIL), ranking-mechanics-vs-priority-calls (prioritizer/Head). Sales-DNA in commercial seats; money-out/contract CEO gates constitutionalized in every §4; Evidence-Before-Done inherited verbatim (shepherd milestones, studio-ops process claims). **Remaining v2 authorship: 11** (179 − 168 gated roster personas; sync 169 = 168 + orchestrator mirror). Next: **E5.6 social-media 11 — FINAL persona wave.** |
| E5.6 | **Social Media Department kuruluşu (CEO direktifi 2026-07-11 — 00-CEO-DIRECTIVE-SOCIAL-MEDIA-DEPT):** departments migration (+social-media) + 12 rol ADD personası + workflow zinciri (müşteri isteği→içerik planı→taslak→onay→takvim→yayın→inbox→analitik rapor); platform API'leri modüler-sonra; publish=dışa dönük eylem → APPROVAL_ENGINE zinciri | dept satırı DB'de + `personas/social-media/` 12 dosya + matris §4 sayımı 179 + workflow sözleşmesi persona §3/§7'lerde | **F — persona yazımı DEVREDİLEMEZ (K2)** | **✓ 2026-07-12 (Fable in person — FINAL wave, commit `2d19d7a`):** 11 specialists authored+gated+bound in one session: Account Connector, Content Strategist, Copywriter, Creative Asset Producer, Approval Workflow Steward, Scheduler & Publisher, Inbox Manager, Analytics Analyst, Reporting Specialist, Client Workspace Manager, MCP & API Integration Engineer. Workflow contract (client request→plan→draft→approval→calendar→publish→inbox→report) encoded in §3/§7 across all 11; publish=outward action APPROVAL_ENGINE-bound; money-out 0 in dept; marketing=strategy/social-media=operations seam recorded both ways. Migration `20260712004000` (persona_path repoint ×11, idempotent 2×: RUN1 UPDATE 11 / RUN2 UPDATE 0, guardrail pass). Evidence: mechanical gate 11/11 PASS (explicit file list) · fn_persona_gate passed ×11 (Fable 5-question verdicts, English, audit_log) · social-media bound **12/12** · **total bound 180 = 179 roster + orchestrator mirror** · **20/20 departments fully bound** · sync --verify match 180 · diff 0 · fail 0 · PASS · orphan 0 · role_level NULL 0 · stale persona_path 0 · gitleaks no leaks. **K2 FULFILLED: 179/179 target roster Fable-authored — persona waves CLOSED.** |

### E5.7 — MUST roster expansion: revenue-engine completeness — **FABLE-ONLY (K2)** ([[WORKFORCE-MUST-EXPANSION-PLAN]], [[00-CEO-DIRECTIVE-MUST-ROSTER]])

> Source: external audit `Solo -kadro denetim raporu.odt` + CEO order 2026-07-12. Frame: the 4 revenue engines (social-selling · own e-commerce · consultancy · venture factory) each get a complete MUST roster. Target: 179 → **198** personas (+19: 17 audit-aligned + 2 Fable independent discoveries — Stock-Lot Sourcing, Managed Automation Services), +`commerce` dept (20 operating depts + 7 pods). Insurance-priority order (promise-debt first); unfinished waves → "awaiting Fable authorship" list (K2 — quality never lowered). E5.4b/E6.0 slide behind persona work (recorded deviation — K1 steps survive the Fable window).

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E5.7a | Expansion plan + 12-column revenue-engine capability matrix + directive mirror + CEO approval of §10 decision block (+19 roster / commerce dept / library recall / wave order) | `WORKFORCE-MUST-EXPANSION-PLAN.md` + `00-CEO-DIRECTIVE-MUST-ROSTER.md` committed; CEO approval recorded in plan §10 | **F** | ✓ 2026-07-12 (plan commit `7afb1e3` + v2 bu commit; **CEO onayı ✓ ~15:55** — bağımsız-keşif sorgusu cevaplandı, v2 planı onaylandı, kayıt plan §10) |
| E5.7b | **D7-A promise-debt 5:** Revenue Growth Specialist + CRM & Data Steward + Pricing & Deal Desk Manager (revops) · Onboarding & Implementation Lead (CS) · Corporate Communications Lead (marketing corp-comms pod) + agents-rows migration | gate 5/5 PASS · fn_persona_gate passed ×5 (English verdicts) · revops 6/6 + CS 4/4 + marketing 33/33 bound · migration idempotent 2× · sync --verify diff 0 fail 0 · gitleaks | **F — DEVREDİLEMEZ** | ✓ 2026-07-12 (Fable in person; migration `20260712005000` RUN1 INSERT 5 / RUN2 INSERT 0 + orphan-guardrail; mechanical gate **5/5 PASS** explicit file list; fn_persona_submit ×5 → fn_persona_gate **passed ×5** — Fable 5-question verdicts English in audit_log; bind persona_id+hook v1+v2.0-fable ×5; **revops 6/6 · CS 4/4 · marketing 33/33 bound** · orphan 0 (orchestrator by design) · role_level NULL 0 · sync --verify **match 185 · diff 0 · fail 0 · PASS**; F4 promise-debt CLOSED — all 5 matrix-promised ADDs now live) |
| E5.7c | **D7-B commerce cell 10:** `commerce` dept migration (dept row + 10 agents + director + manager chain) + Head of Commerce (director) · WooCommerce/WordPress Commerce Architect (senior) · Commerce Automation & Integration Engineer (senior) · Catalog & PIM · Merchandising/Pricing/Promotions · **Stock-Lot & Liquidation Sourcing (Fable keşfi — outlet alış avcısı)** · Inventory/Order/Fulfillment · CRO & Checkout · Customer Ops & Returns (library recall retail-customer-returns) · Commerce Analytics & Revenue Intelligence | dept row + director_id · gate 10/10 · passed ×10 · commerce 10/10 bound · orphan 0 · idempotent 2× · sync PASS · gitleaks | **F — DEVREDİLEMEZ** | ✓ 2026-07-12 (Fable in person; migration `20260712006000` RUN1 INSERT 1 dept + 1 head + 9 workers / RUN2 all 0 + guardrails headcount=10 · orphan 0 · director_id set; mechanical gate **10/10 PASS** explicit file list; fn_persona_submit ×10 → fn_persona_gate **passed ×10** — Fable 5-question verdicts English; bind persona_id+hook v1+v2.0-fable ×10; **commerce 10/10 bound** · orphan 0 · role_level NULL 0 · total bound 195 · sync --verify **match 195 · diff 0 · fail 0 · PASS**; F6 store-cell remediated — engine R2 owned incl. Fable-discovery buy-side seat + MUST-B→seat analytics promotion; retail-customer-returns recalled reference-only, no text embedded) |
| E5.7d | **D7-C 4:** Venture Builder (strategy, venture-studio pod, senior) · Business Automation Solutions Architect (CS, consultancy-delivery pod, senior) · **Managed Automation Services Engineer (Fable keşfi — consultancy MRR motoru, CS pod)** · Social Commerce & Creator/Affiliate Lead (social-media) | gate 4/4 · passed ×4 · strategy 7/7 + CS 6/6 + social-media 13/13 · sync **match 199 = 198 + orchestrator mirror** · gitleaks | **F — DEVREDİLEMEZ** | ✓ 2026-07-12 (Fable in person; migration `20260712007000` RUN1 INSERT 4 / RUN2 INSERT 0 + orphan guardrail; mechanical gate **4/4 PASS** explicit file list; fn_persona_submit ×4 → fn_persona_gate **passed ×4** — Fable 5-question verdicts English; bind persona_id+hook v1+v2.0-fable ×4; **strategy 7/7 · CS 6/6 · social-media 13/13 bound** · orphan 1 = orchestrator by design · role_level NULL 0 · **total bound 199 · sync --verify match 199 = 198 roster + orchestrator mirror · diff 0 · fail 0 · PASS**; engine R4 operating seat live [Venture Builder], R3 chain complete [Solutions Architect design link + **Managed Automation Services MRR engine — Fable discovery LIVE**], R1 transaction line live [Social Commerce & Creator/Affiliate Lead, dept 13th seat, zero-money-out constitution inherited]; **+19 MUST expansion roster COMPLETE — all 4 revenue engines owned**) |
| E5.7e | **D7-D closure:** `DEPUTY-FAILOVER-MAP.md` (SPOF set: DPO, Backup&DR, IAM&Secrets, Payroll, AI Obs/FinOps, Board Secretary, dept heads) + MUST-B §-amendments (8 existing personas re-gated, incl. messaging-commerce pair) + roster set-diff reconciliation table (plan §9) + matrix/STATE closure records | map file + SPOF coverage · re-gate passed · matrix-promised-ADD-absent sweep → **0** · set-diff table in plan §9 | **F** (map/analysis devredilebilir kısmı GPT 5.6 solo — K1; persona §-amendment yazarlığı F) | ✓ 2026-07-12 (Fable in person, all parts: **DEPUTY-FAILOVER-MAP.md** live — takeover protocol + SPOF officer set 6 + dept heads 22 + D7-wave 19 mirrored; every deputy slug resolves to live agent → unresolved 0; **MUST-B §-amendments ×6 files / ×8 duties**: cross-border (feed ops + marketplace health), treasury-ar-manager (commerce reconciliation), ciso (commerce fraud), legal-de-counsel (consumer-commerce checklist), email-intelligence-engineer (lifecycle pair + messaging-commerce engineering), social-inbox-agent (messaging-commerce operation) — each gate PASS + fn_persona_gate **passed ×6** re-gate verdicts + rebind v2, old versions superseded, sync match 199 PASS; **§9.1 reconciliation CLOSED TO THE ROW** — no substitution drift: matrix §4 had two defects (base 153 incl. orchestrator; ADD "47" vs §3's actual 53 names), corrected ledger 131+48=179 pre-D7 / 198 post-D7 verified both directions; **promised-ADD-absent sweep (67-slug set) → 0**; matrix §7 addendum resolution recorded) |

### E6 — Control seam + Settings (CEO Faz 7) — [[SETTINGS_AND_CONTROL_SPEC]], [[API_CONTRACTS]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E6.0 | Auth Closure (direktif §2.2 — GAP-01 BLOCKER): görünür Logout, `supabase.auth.signOut()` → `/login` replace+refresh, geri-tuşu auth duvarı, session-expiry uyarısı + güvenli yeniden giriş, çoklu-sekme logout senkronu, cookie bozulması kurtarma; yerel password-only davranışına regresyon testi | `(command)/layout.tsx` + auth helper + route handler | Playwright: logout → /login; geri tuşu korumalı sayfayı AÇMAZ; ikinci sekme oturumu düşürür | F/O | ✓ 2026-07-12 (Fable in person — K1): 8 files — `auth/signout/route.ts` (server-side revoke + sb-* cookie sweep, 303→/login, fail-open exit on broken session) + `logout-button.tsx` (visible Sign out in CommandBar; signOut()→replace+refresh; corrupted-client fallback to server sweep; logout-intent flag so chosen exit ≠ expiry) + `session-guard.tsx` (SIGNED_OUT multi-tab sync, expiry warning modal + safe re-entry, bfcache pageshow revalidation, 30s heartbeat) + proxy cookie-corruption recovery (try/catch getUser → sweep sb-* → clean /login, loop-proof) + CommandBar/layout wiring + EN/TR i18n keys. Evidence: `tsc --noEmit` exit 0 · eslint 6 files exit 0 · curl probes: /login 200, unauth /overview 307→/login, POST /auth/signout 303→/login, corrupted sb-* cookie on protected route → 307→/login · Playwright battery executed against this exact tree (session 19:20–19:28, recorded obs 4905–4922; disk unchanged since — git diff identical): login E2E ✓, logout→/login session cleared ✓, back-button does NOT reopen protected page (session-guard blocks) ✓, second tab drops session (cross-tab invalidation) ✓, cross-tab session sharing ✓, cookie-corruption recovery ✓, TR locale logout ✓, password-only local regression ✓, console 0 errors ✓ · gitleaks staged: no leaks. Authoring session crashed twice (VS Code segfault); closure written post-crash with fresh re-verification. |
| E6.1 | settings_registry seed (§18 alan kataloğu) + `control_settings_*` fn'leri + route handler + client helper **+ migration `0021h_model_catalog_governance.sql` (MODEL_ROUTING_SPEC §4 alignment note, 2026-07-12): ALTER model_catalog ADD display_name/banned/mechanical_only + status CHECK extended (+'testing' [§4c onboarding], +'disabled'); ALTER routing_rules ADD department_id/risk_max/min_context/cost_cap_per_task + UNIQUE(role_slot,priority,department_id); prerequisite for §4b `fn_update_agent_brain` + `0021g_agent_brain_source` — those fns cannot be written before this delta ships** | API_CONTRACTS §24 komutu birebir (set → change_log 1 satır; idempotent tekrar); `\d model_catalog` shows banned+mechanical_only+extended CHECK | F/O | ✓ 2026-07-12 (Fable in person — K1): migrations `20260712009000` (0021h: model_catalog +display_name/banned/mechanical_only + status CHECK +testing/disabled; routing_rules +department_id/risk_max/min_context/cost_cap_per_task + UNIQUE NULLS NOT DISTINCT slot index; 4 bootstrap catalog rows — registered adaptation, E7.1 completes metadata) + `20260712010000` (0021i: registry +locked/scope_types/delegate; `control_settings_set`/`control_settings_undo`/`resolve_setting` + validator; §18 seed 83 keys → registry **89** [models 13 · orchestrator 10 · global_os 4 · departments 15 · employees 28 · workflows 13 · hr 6]; 6 registered adaptations A1-A6 in header incl. approvals task_id nullable + critical risk class, workflow 17→13 CRUD split, expected_current concurrency) — both idempotent 2× (RUN2 all skips). Route `api/control/settings` (set/undo, Idempotency-Key mandatory, error-dictionary→HTTP map) + client helper `lib/control/settings.ts`. **Evidence:** DB battery 23 checks PASS (locked B7b refusal+audit · delegate refusal · unknown/banned/mechanical-on-verdict-slot model refusals · mechanical-on-fast-slot allowed · enum/range/scope validation · CONFLICT_STALE · dept+employee scope resolve chain · undo-of-first-set removes row+falls to default · undo_of chain · system-writer whitelist · APPROVAL_REQUIRED→approvals row · Broadcast settings.changed ×8) · **API_CONTRACTS §24 contract VERBATIM via authenticated CEO session (Playwright): POST set → `{ok:true,change_id:9}`, repeat same Idempotency-Key → byte-identical response, change_log rows for key +1 only; HTTP undo → change_id 10, undo_of=9** · `\d model_catalog` shows banned+mechanical_only+status CHECK(active,testing,degraded,disabled,retired) · registry count 89 ≥ 80 · resolve default → "fable-5" · tsc 0 · eslint 0 · probe cleanup values=3 (HR trio only) · gitleaks clean. |
| E6.2 | Settings UI (§18 bölümleri) + Live Impact Preview + tek-tık undo | Playwright: değiştir → preview → kaydet → undo → eski değer | F/O | ✓ 2026-07-12 (Fable in person, K1): `/sys/settings/[section]` — SettingsShell 3 panels (section nav · searchable rows · Live Impact Preview), all 89 registry keys across the 7 REAL seeded categories (registered adaptation: §18's 22 sections collapse to the E6.1 seed; empty sections = §35 fake surface — the rest arrive WITH their subsystems: providers E7.1, budget keys COST steps, security/backups P7). Per row: type-appropriate editor (boolean/enum/number/string/json/model_ref from `model_catalog` with banned excluded + mechanical-only filtered per slot, mirroring the fn rule) + risk/cost/approval badges + resolution origin (scope > global > default) + change history with per-entry Revert. ScopeSwitcher global/department/employee (entity picker, scope string always visible, global-only keys hidden with honest count). LockedSettingRow (B7b lock + reason, no editor); delegated keys read-only. Writes ride the E6.1 seam only (idempotency, expectedCurrent concurrency, CONFLICT_STALE row lock + refresh, APPROVAL_REQUIRED → approvals link, loud noop). Undo toast = one click. Container-query layout (3 columns on ultrawide, stacked on narrow canvas — mid-canvas overlap found by probe and fixed). **Evidence (UI E2E, real CEO session):** os.timezone default→"Europe/Berlin" save → DB `settings_values` row + change_log #11 by ceo → toast Undo click → value row removed (falls to default UTC) + change_log #12 `undo_of=11` (history never shrinks) → row shows "registry default" again ON SCREEN; model_ref selects verified (haiku only on allow_mechanical slots); employee scope: 2/6 hr keys editable after entity pick, banner shows `employee:<id>`, 4 global-only hidden; TR verified (title/sections/impact/registry descriptions); design-audit **3/3 PASS** (hex 119/0 · en 562 = tr 562 · contract 37→41); console 0/0; tsc 0 · eslint 0. Evidence PNGs: `evidence/e62-settings-saved.png`, `e62-settings-undone.png`, `e62-settings-models.png`. Recorded boundaries: `settings` Broadcast live-row highlight lands with E8 liveliness; palette→setting deep link needs the settings family in `v_global_search` (E8 follow-up); bulk revert (last-N chain) deferred with ChangeHistoryDrawer v2 |
| E6.3 | Org mutasyonları (`control_org_*`) + Organization sayfası v1 (read-only graph → sonra drag-drop) | org fn testi + `org` kanalında olay | F/O | ✓ 2026-07-12 (Fable in person; session crashed mid-milestone — laptop swap exhaustion again, zero work lost, all three artifacts recovered intact from disk and completed): migration `20260712014000` — 8 `control_org_*` SECURITY DEFINER fns (create_company/create_department/assign_director/move_employee/suspend/reactivate/assign_model_group/archive) + `fn_org_actor()` wall (ceo/system, anon denied) + `fn_org_manager_cycle_guard` trigger (no write path can loop the manager chain) + `v_org_graph` (243 nodes live: company → departments by parent_id → 220 employees by manager chain; sub_agents excluded per adaptation A3 — they join with graph v2). Every mutation: row lock → idempotency (digest mismatch = IDEMPOTENCY_MISMATCH) → audit_log row → `dxb:org` Broadcast, one transaction; undo = inverse op (adaptation A2 — org trail lives in audit_log, not settings_change_log). POST `/api/control/org` — one route, zod op union (adaptation A2 of the E6.1 seam idiom), Idempotency-Key header mandatory, session-guarded. UI v1: `/org` ModuleWaiting → real RSC (one `v_org_graph` read) + `OrgTree` client component — collapse tree (directors ranked first, honest per-dept live counts §35), node detail panel (status badge, manager, brain, direct reports), no graph library in v1 (⛔ stays open for drag-drop v2, recorded boundary). i18n `command.orgTree` 26 keys EN+TR. **Evidence (fn layer, real DB):** suspend → `{ok:true, audit_id:2423}` → same-key replay returns IDENTICAL response, audit count unchanged (single new row; the 21:16 row is the pre-crash session's own probe) → status `suspended` in agents → reactivate → `active`; `realtime.messages` topic `dxb:org`: `employee.suspended` + `employee.reactivated` rows with EN/TR summaries; cycle guard raises `ORG_MANAGER_CYCLE` on both self-manager and 2-node loop (rolled back); pre-crash probe residue verified clean (move finance→marketing reverted by its inverse, no stuck suspends); `/org` unauthenticated → 307 `/login` (E6.0 guard); tsc 0 · eslint 0 · en 588 = tr 588 · contrast-audit PASS. ⚠ UNVERIFIED: browser render of the tree (Chrome extension down after the freeze; dev server serves, guard proven) — CEO eye test pending; authed HTTP roundtrip of the route (fn layer proven; route is thin zod+rpc) lands with the graph-v2 E2E. Registered boundary: graph v2 = drag-drop + sub_agents overlay + the ⛔ graph-library decision. **CEO eye test (2026-07-12 ~23:55): visual PASS, content RET → fix wave same night, migration `20260712230000`:** R1 Social Media rendered as a ROOT sibling (departments.company_id was NULL from the E5.2b wave — data fix, now under the holding); R2 employees scattered outside their departments (view parented every employee to its manager even cross-department; now manager-nesting only within the same department — 20 employees rehomed, verify query `employees parented outside own dept = 0`); R3 empty department detail panel → now Director (view carries director_slug, 22/22 assigned: cfo→finance, cmo→marketing…), live Headcount (total · active), and a dormant explainer line (143/220 staff await Phase-7 runtime activation — the badge finally says why). Evidence: single root node, Social Media under_company=t, tsc 0 · eslint 0 · en 592 = tr 592. **Fix wave 3 (2026-07-13 ~00:45, CEO RET — "professional holding chart"):** R4 tree showed raw kebab slugs because no human title existed anywhere in the DB → migration `20260713003000`: `agents.title` + `v_org_graph` v1.3 (employee label = COALESCE(title, slug), slug exposed as own column); 199/199 live agents backfilled from persona file H1s (file-first: sync-personas-to-db.sh now syncs title on submit); R5 "Dormant" badge on ~143 rows = fault wall → badge diet: dormant renders as muted text, badge reserved for draft/probation/suspended; R6 alphabetical department order → holding chart order (Leadership → Corporate → Revenue → Product & Technology, presentation-only rank map); R7 detail panel raw slugs → department display name + director resolved to title + Agent ID row (slug), employees link queries by slug. Archived-agents leak in CEO screenshot verified STALE RENDER (v1.2 already excludes; rendered page contains 0 archived slugs). Evidence: titled=199/199; platform subtree renders "Platform Direktörü (Platform Head)"; Playwright authed render — human titles + holding order + 0 archived, detail panel shows "Platform, Infrastructure & Reliability" + Agent ID `platform-head`; tsc 0 · eslint 0 · en 599 = tr 599. Known limit (recorded): directors show "No manager (root)" in the panel — the view parents them to the department, not the orchestrator; lands with graph v2 node-detail view. **Wave 3b (same night, 2 CEO RETs):** R8 Turkish titles rendered on the EN locale (A2 violation) → `agents.title_tr` + view v1.4 (migration `20260713010000`), backfill language-split from persona H1s (199 rows reviewed personally, 6 TR-only titles given mechanical EN equivalents), RSC label by locale, sync script restricted to the dossier EN Title field; evidence: EN page 0 TR-title tokens, TR page shows TR titles, DB `title ~ TR-chars` = 0, `title_tr` = 67. R9 detail panel a full workforce away (~220-row expanded tree; below @3xl it stacked under the tree) → tree scrolls in its own `max-h-[70vh]` box, panel order-first on narrow + sticky at @3xl; evidence: Playwright deep-node click screenshot, panel fully visible at viewport top. tsc 0 · eslint 0. **Wave 3c (CEO order: spec madde 5.3 NOW):** the spec's `v_org_node_detail` contract (§12) had never been written — implemented as migration `20260713013000` (full madde 5.3 set: identity EN/TR, true cross-dept manager, runtime counters, persona version+gate+author, sicil flag, skills/grants/memory) + session-guarded `GET /api/org/node?id=` (persona body lazy per spec) + panel v2: Governance + Runtime sections, clickable manager (kills the "No manager (root)" lie), in-panel persona reader — first UI surface where the CEO can read a persona. i18n +24 keys (623=623). Evidence: view 199/199, 0 persona-less; Playwright CMO click → sections filled, persona dossier loads in-panel; tsc 0 · eslint 0. **Found gap (recorded):** `employee_records` = 0 rows — HR wave never populated the sicil layer; panel states it honestly; separate HR data task. **Wave 3d (CEO RET: mixed-language TR screen):** departments had no Turkish name in the schema and title_tr covered 67/199 → migration `20260713023000`: `departments.display_name_tr` (22 TR names) + v_org_graph v1.5 + title_tr completed 199/199 (18 recovered from dossier Unvan, 114 authored by Fable personally); standing gate `scripts/i18n-purity-check.sh` (agents EN+TR, departments EN+TR, dictionary parity) added to the verification battery. Evidence: NULL counts 0/0; Playwright TR page 0 English remnants, EN page 0 TR leak; purity gate PASS. |
| E6.4 | Global Search + ⌘K Command Palette + CEO intent surface (TR/EN doğal dil → kernel; intent → classification → task/workflow → approval/outbox → audit zinciri ekrandan izlenir; READ-ONLY ↔ CONTROL MODE ayrımı + kill-switch görünürlüğü) — E2.1 notundan sahiplenildi, GAP-07 | command bar + palette + intent route handler | Playwright: ⌘K açılır, gerçek sonuç döner; intent gönder → task satırı + audit kaydı ekranda | F/O | ✓ 2026-07-12 (Fable in person; session crashed mid-milestone — laptop swap exhaustion, zero work lost, recovery verified from disk): migration `20260712013000` — `v_global_search` (10 entity families, 871 rows live) + intent audit triggers; GET `/api/search` (read-only, type-weight ranking, LIMIT 50); `CommandPalette` ⌘K — 3 lanes: search / action catalog (7 nav rows + kill-switch pair, per-row CONTROL badge) / CEO intent with LIVE chain watcher (2s poll → task rows + audit links); kill-switch chip in command bar (paused state never invisible, GAP-07). Evidence: DB seam vitest 4/4 PASS (dispatch real, broadcast shape asserted); **UI E2E PASS (Playwright, real CEO session):** ⌘K opens → "revenue" returns real ranked rows (Employee > Department > Audit) → probe intent submitted → intake dispatched → chain panel shows "Queued" + task row + View tasks/audit links ON SCREEN; TR locale verified (palette fully Turkish); design-audit **3/3 PASS** (hex 117/0 · en 464 = tr 464 · contract 37→41); console 0 error/0 warn; tsc 0 · eslint 0; probe rows cleaned (intents/tasks/task_events 0 — audit trail row kept, append-only). Evidence PNGs: `evidence/e64-palette-search.png`, `evidence/e64-intent-chain.png`. Recorded boundary: palette intent dispatch relies on the pg-boss resident's intent-intake queue in production (worker library proven; resident boot is Phase 7 VPS scope) |
| E6.5 | **Revenue & P&L surface (CEO direktifi 2026-07-12 akşam — "kazançlar nerede?"):** append-only `revenue_ledger` + `fn_revenue_record` (para-GİRİŞİ onaysız, audit+Broadcast) + `v_pnl_daily`/`v_pnl_engine` + `/fin/pnl` sayfası (günlük gelir/maliyet/net KPI'ları, motor kırılımı R1-R4+fiziki, manuel gelir girişi formu — fiziki şirketin geliri entegrasyonlar gelene dek elle girilir) + nav Finans grubunun BAŞINA | migration idempotent 2× · fn bataryası · form→ledger→KPI E2E · design-audit 3/3 · sıfır-uydurma: seed YOK | **F (K1)** | ✓ 2026-07-12 (Fable in person): migration `20260712011000` (0027a) idempotent 2×; **DB battery 10 checks PASS** (idempotent record · reversal-reason rule for negative amounts · unknown-dept refusal · P&L join rev−cost=net · engine view · Broadcast finance/revenue.recorded ×2 · audit ×2 · append-only: authenticated yalnız SELECT); **E2E PASS (Playwright, gerçek CEO oturumu):** /fin/pnl TR başlıkla render → form 1500 EUR probe → "Kaydedildi" → reload → ledger satırı + KPI 1.500 + motor kırılımı "Fiziki şirket" ekranda; entered_by=ceo, audit ×1; probe temizlendi (ledger 0 — gerçek para yok, uydurma yok); console 0 error; design-audit **3/3 GEÇTİ** (en 407 = tr 407, connection contract, hex 0); tsc 0 · eslint 0; kanıt PNG `evidence/e65-pnl-page.png`. Kayıtlı sınırlar: WooCommerce/banka/CRM otomatik yazımı entegrasyon adımlarında (source kolonu hazır); Overview'a net-widget köprüsü E8/E11'de |
| E6.5b | **Content monetization gelir sınıfı + platform kırılımı (CEO direktifi 2026-07-12 akşam #2 — "YouTube, Instagram, TikTok'tan kazançlar akmalı"):** `revenue_ledger.engine` +`content_monetization` (platform payoutları — social_selling'den AYRI sınıf: para platformdan gelir, müşteriden değil) + `platform` kolonu (9 yüzey CHECK'li) + fn kuralı: content_monetization platform ZORUNLU + `v_pnl_platform` view + /fin/pnl'de "Platforma göre içerik kazancı" paneli + form motor+platform seçicileri + EN/TR | migration idempotent 2× · fn bataryası (platformsuz CM reddi, bilinmeyen platform nazik reddi) · E2E form→panel · design-audit | **F (K1)** | ✓ 2026-07-12 (Fable in person): migration `20260712012000` (0027b) idempotent 2×; DB battery: CM-platformsuz → VALIDATION_FAILED · youtube/tiktok kayıtları → v_pnl_platform 2 satır + v_pnl_engine content_monetization toplamı + v_pnl_daily join doğru · myspace → nazik ret (fn-seviyesi, exception değil — ilk koşuda CHECK-exception yakalandı, fn'e ön-doğrulama eklendi, kayıtlı düzeltme); **E2E PASS (gerçek CEO oturumu):** form content_monetization+YouTube 87.40 → "saved" → TikTok 23.10 → reload → platform paneli YouTube €87.40/TikTok €23.10 barları + motor €110.50 + defter "Content monetization · YouTube" + günlük P&L geçmiş maliyet günleriyle join (−€0.46 net görünür); entered_by=ceo ×2, audit platform-payload'lı ×2; probe temiz (ledger 0); design-audit **3/3** (en 421 = tr 421); tsc 0 · eslint 0; kanıt PNG `evidence/e65b-platform-earnings.png`. Kayıtlı sınır: platform payoutlarının OTOMATİK çekimi (YouTube Analytics/TikTok Creator API) entegrasyon adımlarında — manuel giriş + source kolonu bugünden hazır; sosyal-media dept'in içerik-üretim operasyonu zaten 13 kişilik kadroda, para-kazanma hedefi Social Commerce & Creator/Affiliate Lead + Analytics Analyst KPI'larına persona-revizyonuyla işlenecek (E7+ listesinde) |

### E7 — Model routing tablo-güdümlü (CEO Faz 7) — [[MODEL_ROUTING_SPEC]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E7.1 | model_catalog seed + routing_rules genişleme + orchestrator tablo-okur seçim | routing testi: slot → beklenen model; fallback zinciri decision_log'a | F/O | ✓ 2026-07-13 (Fable in person, K1): migration `20260713040000` (0021x) idempotent 2× — 13 role-slot rows in routing_rules (global, priority 100; defaults = live E6.1 registry: primary=fable-5, fast_task/low_cost=claude-haiku-4-5, other 10=claude-opus-4-8 — registered adaptation A1: spec §4 footnote applied); model_catalog fallback chain fable→opus→sonnet + haiku→sonnet (walk = follow `fallback_of`, acyclic trigger live) + metadata (context_window fable-5=1M measured; cost/quality NULL until LiteLLM wiring — zero fabrication, A2); `fn_select_model` (dept-beats-global rule scan, guardrails active/banned/mechanical_only/context/cost-cap, budget_state hard-stop gate with critical exemption, decision_log `routing_decision` + eliminated candidates in alternatives); `fn_model_fallback` (chain ≤4, per-hop guardrails, `routing_fallback` rows, loud CHAIN_EXHAUSTED); `fn_update_routing` (assign_role ceo-only / set_catalog_status system-allowed §13, idempotency+audit+decision_log+`settings` Broadcast); settings delegate flip: 13 `orchestrator.*_model` keys → `delegate='routing'` (spec §7 single source — control_settings_set refuses them, proven). Orchestrator: `packages/orchestrator/src/select-model.ts` — `selectModel`/`fallbackModel` typed wrappers, no model name in code. **Evidence:** DB battery 13 blocks PASS (all slots resolve · §24 smoke execution→opus · haiku-on-review eliminated `mechanical_only` · banned test-row eliminated + Sonnet stays free · dept override marketing research→sonnet beats global opus · hard-stop refuses non-critical/passes critical · fallback decisions 21-23 in decision_log); CEO-actor jwt-sim battery (transactional ROLLBACK, zero residue): MODEL_BANNED / MODEL_NOT_ACTIVE(`testing`, §4c) / MODEL_MECHANICAL_ONLY refusals + happy-path assign coding→sonnet re-resolves live + audit 2426; vitest `tests/e7/routing-slots.test.ts` **8/8**; phase5 legacy routing regression 5/5 (slot rows don't disturb kernel task_class path); tsc 0; raw-key scan 0. Recorded boundaries: v_model_stats+panel+simulator+§4c drawer → E7.2; §4b agent-brain → with its UI seam; 2-fallback alert → E8.4b; LiteLLM call path → Phase 7. |
| E7.2 | Model Orchestration Panel (§19) + banned-mekanizması denetimi görünür + model-ekleme akışı (MODEL_ROUTING_SPEC §4c) | panelde 13 rol slotu; `banned=true` test-satırı hiçbir slota atanamaz (fn reddi — mekanizma kanıtı); Sonnet atanabilir havuzda (CEO kararı 2026-07-12, R2/§4); add_model→testing→duman→active zinciri çalışır, `testing` model atanamaz | F/O | ✓ 2026-07-13 (Fable in person, K1): migration `20260713050000` idempotent 2× — `v_model_stats` (§19 R3 meta from REAL `agent_runs` 30d aggregates + `agents.brain` + `routing_rules` — zero fabricated scores, §35) + `v_role_slots` (13-slot board read) + `fn_update_routing` v2 adds §4c ops `add_model` (born `status='testing'`, ceo-only, lowercase-alias validation, R5 no raw keys) & `set_fallback` (acyclic-trigger guarded). Routes: `/api/control/models` (all 5 ops, Idempotency-Key mandatory, error→HTTP map; `test_model` = TS-side LiteLLM smoke, honest `LITELLM_UNREACHABLE` until Phase-7 proxy — registered adaptation A1) · `/api/ai/simulate` (`fn_select_model p_log=false` — simulations NEVER write decision_log) · `/api/ai/model-detail` (drawer lazy read). UI: `/ai/models` catalog (full R3 meta columns, detail drawer with status control + smoke + assigned employees + routing-decision trail, §4c 4-step onboard drawer) · `/ai/orchestration` (13-slot board select+confirm+rationale [adaptation A2: drag lands with graph v2], live guardrails panel [banned/mechanical/testing], RoutingSimulator). **Evidence:** DB battery 13 blocks PASS transactional-ROLLBACK zero-residue (13/13 slots resolved · MODEL_BANNED refusal on banned test-row · Sonnet assign OK research→sonnet · add_model→testing · MODEL_NOT_ACTIVE refusal on testing · activate→assign OK (full §4c chain) · duplicate+bad-alias refusals · set_fallback OK + FALLBACK_CYCLE refusal · MODEL_MECHANICAL_ONLY regression · idempotency replay + digest mismatch · audit 4 + decision 4 rows · v_model_stats 6 rows · system-actor refused on ceo-only ops); UI E2E real CEO session (Playwright): assign research→Sonnet with rationale → toast + DB slot flip + audit #2441 + decision #87 → revert to Opus verified; §4c UI chain: add `smoke-e72-ui` → "testing, henüz atanamaz" → smoke → honest LITELLM_UNREACHABLE 503 → guardrail panel lists it under "Testte (atanamaz)" → picker excludes it (probe cleanup: row deleted, catalog back to 4); simulator research→claude-opus-4-8 matches fn; vitest tests/e7 8/8; tsc 0; eslint 0; i18n purity PASS en 726 = tr 726; raw-hex scan 0. **Design pass (RULE #0):** both routes × EN+TR × 1280+1920 rendered real browser, checklist walked, 8 baselines refreshed in references/design-bank (INDEX PENDING); defects found & fixed IN-PASS: tr-TR number grouping (`1.000.000`, was en-US hardcoded), simulator risk options localized (were raw English enums in TR), smoke error detail localized, stale unconditional READ-ONLY header badge removed (§35 honesty — cockpit writes since E6.2). ⚠ UNVERIFIED: visual "reference-grade luxury" verdict = CEO eye test (baselines PENDING). Boundaries: LiteLLM live smoke → Phase-7 VPS; drag-assign → graph v2; §4b agent-brain seam → own row. |

### E8 — Observability yazıcıları (CEO Faz 6/8) — [[OBSERVABILITY_SPEC]], [[AUDIT_AND_LOGGING_SPEC]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E8.1 | Orchestrator SDK sarmalayıcı: agent_runs + tool_calls + file_changes otomatik yazım (1 sn tampon) | AUDIT §24: mock koşu N çağrı → N satır | F/O | ✓ 2026-07-13 (Fable in person, K1): new `packages/observability` — `runScope(ctx, fn)` (agent_runs INSERT 'running' at open; closure UPDATE = the R2 append-only exception: status/ended_at/tokens/cost/model/progress/error), `RunScope.recordToolCall/recordFileChange` through ONE batch buffer (spec §3 normative thresholds: **20 rows / 2s flush** — roadmap's "1 sn" wording yields to spec §3, registered adaptation A1), `addUsage/setModel/setProgress`, scope travels on AsyncLocalStorage (`currentRunScope()`) so the certified Phase-5 Executor signature stays untouched. ⛔ "observation never blocks" implemented at EVERY layer: record*() = sync queue push; flush async serialized, 3 failed attempts → JSONL spill (`OBS_SPILL_DIR`, default `/var/lib/dxb/obs-spill`) + loud log; `restoreSpill()` replays at next scope open (corrupt file kept, never deleted); even a failed OPENING insert degrades to an unrecorded run instead of stopping work. Orchestrator: `runWorkerOnce` wraps execute in runScope (task outcome decided by execute alone); defaultExecutor taps SDK assistant tool_use blocks (params key-digest only, §16 no raw content) + usage into the scope on both subscription and LiteLLM paths (costEur stays 0 pre-Phase-7 — single-source cost rule, litellm.ts LOCKED header). `@dxb/shared` db-types +3 obs tables. **Evidence:** vitest `tests/e8/obs-wrapper.test.ts` **9/9** — open running→close succeeded (tokens 120/45 verbatim) · failed close + rethrow untouched · **N=7 calls → exactly 7 tool_calls rows (AUDIT §24 acceptance)** · size-flush at 20 mid-run · file_changes with diff summaries · 3-attempt fail → spill file → restoreSpill replays → spill dir empty · corrupt spill kept · real claim path: dispatch→runWorkerOnce→agent_runs succeeded + model_id + tokens_in 500 + 3 tool_calls + 1 file_change, task reaches review · throwing executor fails BOTH task and run record; full regression e7+e8+phase5+phase6 **78 passed / 0 failed** (phase5/6 cleanups extended for the new FK chain — evidence rows blocked task deletion, correct behaviour); tsc -b 0; append-only proof as `authenticated`: UPDATE decision_log/tool_calls/agent_runs + DELETE file_changes → 4× `permission denied`; raw-key scan 0. Fixed in-pass: vitest alias for `@dxb/observability` (src/dist double-instance would silently lose the ALS scope). Boundaries: flush-failure ALERT row → E8.4b (alerts table); decision_log writer `logDecision` → E8.2; `ops:live` Broadcast triggers → E8.3; health probe + snapshots → E8 UI rows; SDK PreToolUse/PostToolUse hook depth (duration/ok per call) → deepens when the resident worker lands (Phase 7). |
| E8.2 | `logDecision` yardımcısı + §10 "önemli karar" noktalarına yerleştirme | decision_log 8 alan dolu satır | F/O | ✓ 2026-07-13 (Fable in person, K1): `packages/observability/src/log-decision.ts` — `logDecision(rec, extras)` with ALL EIGHT record fields as REQUIRED keys (runId/decidedBy/decision/rationale/dataUsed/alternatives/confidence/risk — omission = compile error, AUDIT §20 contract; nullable VALUES only where the column allows; decision+rationale NOT NULL in type AND schema, R6). Failure discipline AUDIT §17: never throws into the execution path, but never silent — failed write logs loudly + appends `degraded_logging` to the task's event trail (evidence gap visible). §10 binding-list placement (points that EXIST in code today): **dispatch** `task_plan` one row per committed batch AFTER the transaction (rolled-back batch leaves no decision) · **escalate** `escalation` on every requeue rung (rationale = fail_count + ladder + tier move; deterministic code → confidence null) AND on the terminal `blocked` rung (risk high, outcome blocked — CEO escalation) · **qa** `workflow_branch` on qa-fail (verdict confidence carried) + `approval_conversion` on pass→awaiting_approval (pass→done deliberately stays task_events — §10 log-inflation limit). `@dxb/shared` db-types +decision_log. **Evidence:** vitest `tests/e8/log-decision.test.ts` **6/6** — 8/8-field row verbatim readback (data_used array, alternatives jsonb, confidence 0.8, risk low) · doomed write (bad approval FK) → `ok:false` + exactly 1 `degraded_logging` event, no throw · dispatch/escalate/qa call sites each produce their row on the REAL claim path (dispatch→runWorkerOnce→escalate/qa) · compile gate: 2× `@ts-expect-error` fixtures (missing rationale / missing alternatives) hold under `tsc -b` full workspace 0; full regression e7+e8+phase5+phase6 **84 passed / 0 failed**. DB-side §10 points already live from E7.1 (model selection `routing_decision` / fallback `routing_fallback` in fn_select_model/fn_model_fallback). Recorded boundaries (no code path exists yet): hook red/escalation → E10.1 · bütçe-sınır TS aksiyonu → COST steps (DB hard-stop gate already logs eliminations) · persona sürüm terfisi → HR runtime · org değişikliği önerisi → org-proposal flow. Test-DB idiom note: decision rows written by legacy phase5/6 suites stay append-only (E7-established rule). |
| E8.3 | `ops:live` Broadcast trigger'ları + Live Ops v2 canlı ajan akışı | EVENT_MODEL §24 probe komutu | F/O | ✓ 2026-07-13 (Fable in person, K1): envelope contract `packages/shared/src/contracts/events.ts` (§9a Zod single source, §20; entity.kind +`decision` registered append); migration `20260713060000` — `fn_opslive_notify` (pg_notify, 8000-byte guard) + 3 envelope triggers `trg_broadcast_opslive_{runs,task_events,decisions}` (run.* full type map incl. paused/cancelled append, task.event_appended, decision.logged; OBSERVABILITY §9 payloads; §15 log+continue never blocks source write; 2× idempotent); collector `packages/orchestrator/src/ops-live-collector.ts` (LISTEN, 1 s window §9b, n=1 verbatim / n>1 payload.batch §18, loss-tolerant §17; §26 held — NO new resident service: dev host `scripts/dev/ops-live-collector.mjs`, Phase-7 worker adopts); probe `scripts/dev/event-probe.mjs` (real CEO session = §13 RLS join proof); Live Ops v2 — realtime.ts +ops:live, live-feed envelope consumption (batch unwrap, objective backfill), §10 reconnect snapshot re-fetch, shared RSC/client mapper `lib/live-ops.ts`, EN/TR note strings. **Evidence:** §24 sequence executed (probe printed `{"type":"task.event_appended",…}` in-window); `tests/e8/ops-live.test.ts` 6/6 (10 INSERTs → ≤2 publishes with batch=10 §21 · single verbatim §24 · real claim path run.started+succeeded on channel); regression 15 files 90/0; tsc -b 0; i18n purity PASS; RULE #0 pass EN+TR×1280+1920 + in-browser live delivery proof; baselines live-*.png PENDING. **Fixed in-pass:** routing coding-slot drift (E7.2 panel demo residue, decision 340) restored via fn_update_routing (audit 2895). Boundaries: collector→resident worker (P7), health snapshots (E8 health row), alerts kanalı (E8.4b), decision drill (E8.4), run drill /ops/tasks (E12.1). |
| E8.4 | Audit sayfası (Governance grubu): birleşik akış + detail_ref drill-down + Decision Logs sekmesi | UI'da audit satırı → aile kaydına iniş | F/O | ✓ 2026-07-13 (Fable in person, K1): migration `20260713070000` — `v_audit_trail` (audit_log ⋈ detail_ref ONE query §8; resolves canonical §4 `{"table","id"}` AND legacy `{"<table>_id"}` writer shapes — append-only rows never rewritten, registered adaptation), `v_decision_log` (8 fields + run→employee/task context), `control_audit_mark_reviewed` (single audit-family mutation §8; idempotent; canonical-ref audit row; `reviewed_flagged` → `alert.raised` on `alerts` §9 — only broadcasting log write), routing_rules authenticated SELECT policy+GRANT (missing grant failed the whole security_invoker view); `/api/control/audit` seam; `/gov/audit` unified stream (actor/entity/date/risk filters §7, row→payload+family drill), `/gov/audit/[id]` full record (CCC drill map), `/gov/decisions` — madde 10.2's 8 questions VERBATIM as headers (§7 "8 sütun birebir", table-fixed, 1280 scrolls in container); GovTabs (tab=link, both IA routes real). **Evidence:** spec §24 sequence (review cols · `permission denied` as authenticated · detail_ref object); `tests/e8/audit-surface.test.ts` 8/8 (both ref shapes resolve · 8/8 questions one query · mark_reviewed drill-back · idempotency+mismatch · alert.raised with corr on dxb:alerts · anon zero grant · append-only re-proof); regression 16 files 98/0; tsc -b 0; i18n purity PASS; **roadmap acceptance in-browser: audit row 2895 → routing rule family record + /gov/audit/2895**; RULE #0 EN+TR×1280+1920, baselines gov-audit-*/gov-decisions-* PENDING. Fixed in-pass: stray E8.3 dev collector duplicating publishes (swept). Boundaries: retention jobs → P7; /alerts center → E8.4b; per-run tool_calls/file_changes browse → run drawer; v_global_search audit leg → CCC search row. |
| E8.4b | Notification/Alert center (GAP-10): gerçek alarm kaynağı (cost eşik, run failure, queue age, heartbeat kaybı), severity, owner, acknowledge, escalation, resolved; `/alerts` ModuleWaiting kapanır; Intelligence Rail gerçek öncelik sırasına bağlanır | `(command)/alerts/` + alert kaynağı | alerts sayfası gerçek kayıt listeler; ack → audit satırı | F/O | ✓ 2026-07-13 (Fable in person, K1): migration `20260713080000` 2× idempotent — `alerts` + `system_health_snapshots` (0022x completion; adaptations A1-A5 registered in OBSERVABILITY_SPEC §4 note: dedup_key active-unique, run/task/source_ref loose link, escalation columns, muted_until) · ONE broadcast producer `trg_alerts_broadcast` (raised/acknowledged/resolved/escalated, §9a envelope, §15 log+continue) · REAL sources: agent_runs failed trigger · cost 70/90/100 (COST R4 constants, per-month dedup) · budget hard-stop+breaker · `fn_model_fallback` refit (≥2 hops High / exhausted Critical, ROUTING §9/§17) · mark_reviewed refit (flagged → alerts row; E8.4 test updated) · obs spill → High alert (E8.1 debt; packages/observability best-effort insert) · `fn_alerts_evaluate()` queue-age/heartbeat/escalation (settings-driven thresholds; Phase-7 'alert-evaluate' job adopts — R3 held; empty snapshots = honest skip) · `control_alerts_action` ack/resolve/assign/mute (idempotent, audit row canonical detail_ref, resolved-wall) · `v_alerts_active` (severity→unacked→age ∪ pending critical approvals) · v_audit_trail +alerts family · `/api/control/alerts` · `/alerts` AlertCenter (ModuleWaiting öldü; active/resolved, filters, actions, source drill, `alerts` Broadcast → server refetch) · IntelligenceRail alerts panel = view head, live. **Evidence:** `tests/e8/alerts.test.ts` **12/12** (every source fires · cost dedup rolled-back probe · evaluate fn 3 checks + single-step escalation + alert.escalated · **ack → audit satırı + v_audit_trail alerts drill = ROADMAP ACCEPTANCE** · idempotency+mismatch · anon zero grant + authenticated direct UPDATE denied · view ordering+mute-hide+approval leg); regression e7+e8+phase5+6 **17 files 110/0 ×2** + alerts table returns to 0 after full run (suite self-clean proven; e7/obs cleanups extended — fixed-in-pass: pre-patch orphan alerts swept, 16 aborted-run1 residue rows removed); tsc -b 0; dashboard tsc 0; i18n purity PASS (830=830); **in-browser acceptance: real queue-age alert (9 stale battery tasks) listed on /alerts → UI ack with note → audit row actor=ceo + /gov/audit drill to alerts family** (probe ack reverted after — eye-test mutation hygiene); RULE #0 EN+TR×1280+1920, baselines alerts-* PENDING. Boundaries: pg-boss alert-evaluate/health-probe/cost-guard jobs → P7; /sys/health board → own row; CommandBar critical badge → CCC row; anomaly 3σ → P7. |

### E9 — İş akışı katmanı (CEO Faz 8) — [[WORKFLOW_ENGINE_SPEC]], [[PROJECT_OPERATING_SYSTEM_SPEC]], [[APPROVAL_ENGINE_SPEC]], [[HOLDING_LIBRARY_SPEC]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E9.1 | Workflow runner (`packages/kernel/src/workflow/`) + step handlers + control fn'ler | WORKFLOW §24 smoke-wf uçtan uca | F/O | ✓ 2026-07-13 (K1 Fable in person; migration 20260713090000 control_workflow_action + run.* Broadcast; kernel runner/steps/triggers library (§3 ⛔); scheduler workflow.run 10s drain; /api/control/workflows; §24 live: create ok → run_now run_id → succeeded; tests/e9 24/24; regression 18 files 134/0; adaptations A1-A6 in spec) |
| E9.2 | Workflow Settings UI (§6.4'ün 17 kalemi) + koşu geçmişi | 17 kalem denetim listesi UI'da işaretlenir | F/O | ✓ 2026-07-13 (K1 Fable in person; /ops/workflows WorkflowCenter — ModuleWaiting DEAD; 17/17 kalem gerçek tarayıcıda işlendi + DB doğrulandı (create v1 → edit v2/v3 → copy -copy-1 → disable/enable → manual/cron/event trigger → slot+pin model → çalışan → 5 adım türü → bütçe/token/timeout → risk (B7b reddi UI'da birebir) → logging verbose → output standard; reorder persisted; koşu geçmişi drill agent_runs'a; run_now → succeeded canlı); RULE #0 EN+TR × 1280+1920, i18n purity PASS 923=923, baselines workflows-*.png PENDING CEO eye; demo mutasyonlar süpürüldü) |
| E9.3 | Approval Center genişleme (§21: tam sayfa, karar geçmişi, politika bağı) — mevcut karar yolu KALIR | mevcut approval testleri yeşil + yeni center rotası | F/O | ✓ 2026-07-13 (K1 Fable in person, commit `e4b496b`; migration 20260713110000 — approvals §4 extension + approval_rules 7 LOCKED seeds + fn_classify_operation FAIL-CLOSED + control_approvals_action 7 CEO actions §5 [CEO wall §13, idempotency twin, money_out outbox release inside fn tx via 0003 trigger] + v_approvals_center/v_approval_fatigue + fn_alerts_evaluate §9 additive; /approvals full-page Approval Center + /approvals/[id] detail §21 R3; existing 0015 decide path UNTOUCHED — B7b DB-grant proof has_table_privilege f/f; tests/e9 approval-center 22/22 + approval regression 18/18 + full regression green after 5 stale-test fixes; §24 psql probes; adaptations A1-A7 in APPROVAL_ENGINE_SPEC; RULE #0 EN+TR × 1280+1920, i18n purity PASS, 8 baselines approvals-*/approval-detail-*.png PENDING CEO eye; mutation hygiene swept to 0 residue) |
| E9.4 | Project OS: 0023x-b üstüne Command View (§23 19 alan) + health fn | PROJECT_OS §24 komutları | F/O | ✓ 2026-07-14 (K1 Fable in person; migration 20260714010000 idempotent 2× — project_health + project_health_breakdown (§10 formula verbatim, per-component columns), v_project_command v2 (19-field single round-trip + health_live/pen_*/blockers/tokens/current_phase), control_project_action 7 ops (§13 CEO wall, idempotency twin, §16 links credential regex reject, §27 archive-guard, §17 cycle path + CONFLICT_STALE), tasks.milestone_id + milestone.reached trigger + projects channel broadcasts; /api/control/projects Zod seam; /ops/projects + /ops/projects/[slug] Command View — ModuleWaiting projects DEAD; dogfood: dxb-global-os 13 phase milestones (roadmap E-blocks, real ✓ dates) + 3 real recorded risks via control fn, health 85 live (−15 high risks); §24 probes: \dt project_* 4 tables · project_health→0-100 · view breakdown non-null; tests/e9/project-command 17/17 + full regression 27 files 195/0; adaptations A1-A6 in spec; RULE #0 EN+TR × 1280+1920 (3 catches fixed in-pass: card label overlap, timeline label collision + TZ drift, 3-lane grid overflow), i18n purity PASS 1152=1152, 8 baselines e94-*.png PENDING CEO eye) |
| E9.5 | Library intake script + katalog UI + grant→profil derleme entegrasyonu | LIBRARY §24: intake raporu + grant→gateway reddi kanıtı | F/O | ✓ 2026-07-14 (K1 Fable in person; migration 20260714020000 idempotent 2× — expires_at (A2) + source_ref (A3), v_library_catalog v2, control_library_action 4 ops (A1 single door; §13 CEO wall; idempotency twin; §5 change_log INSIDE fn — creation entry + field diff; settings-channel broadcasts), fn_library_usage trigger on tool_calls (A7); scripts/library/intake.mjs — 352 REAL assets registered through the control fn in CEO context (A8): 10 acceptance kinds all ≥1 (skill 23 · plugin 18 · tool 21 · mcp 8 · persona 214 · policy 2 · sop 2 · research 8 · report 2 · memory_source 4), zero-discovery kinds explicitly listed (§26), re-run = 0 writes; gateway: generate-profiles library layer (A4 union→intersection, §22 backward compat) + `_skills` key (A5) + employee overlay profiles + compileLibraryProfiles (staged atomic swap §17, hash-skip, system channel event) + scheduler library.profile_recompile 30 s self-chain (A6); §21 E2E in tests: grant→profile→resolveTool/resolveSkill, revoke→refusal both legs; FOUND+FIXED: Phase-7 policy files still keyed to pre-E5.x registry (generator failing live) — re-keyed to 21 live departments, NO widening, stale dept profiles cleaned, live recompile → 21 profiles + profile.recompiled event; /ai/library ModuleWaiting DEAD (20-kind rail honest zeros, 11-field record, 4 tabs, grant editor through seam — live UI grant+revoke proven, audit actor=ceo); tests/e9/library 13/13 + full regression 46 files 305/0; tsc 0 root+dashboard; i18n purity PASS 1226=1226; RULE #0 EN+TR × 1280+1920 — CEO mid-pass catch: 1400-1500 band 2-column crush + unscrollable clipping → 2xl breakpoint + table min-w fix, 1400 probe docW=innerW + in-container table scroll proven; 8 baselines e95-*.png design-bank INDEX PENDING CEO eye) |

### E10 — Opus 5 Hook engine (CEO Faz 4) — [[FABLE_5_HOOK_SPEC]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E10.1 | `packages/hook`: policy yükleyici + pre-task gate + post-task gate + ihlal kaydı | hook testi: policy ihlali → red + decision_log satırı | F/O (policy İÇERİĞİ **F** — E5 ailesi) | ✓ 2026-07-14 (K1 Fable in person; migration 20260714030000 idempotent — hook_policies (§4 + A2 bilingual titles) + hook_violations append-only + 19-row seed / 17 distinct standards (§21 SQL proof 19/17) + fn_hook_set_policy (CEO wall, idempotency twin, block-weakening = risk high audit, settings-channel cache-drop broadcast) + violations→alerts trigger (A4 mapping, action-aware dedup) + fn_alerts_evaluate §7 24h-stale-escalation block + 7 settings keys; packages/hook LIBRARY: loadPolicies 60s TTL + invalid_policy reject §17 + conflict→most-restrictive+alert §27, preTask §6 order (permission→persona→completeness→plan→budget→project-inject), runtimeLimits/checkSpawn/monitorTokens/monitorContext/checkConfidence (monitor-only, runner = single kill authority), postTask PASS/REVISE/ESCALATE + §7 chain walk (decision_log per hop, CEO rung approval item) + §27 CEO warn+audit never block + FAIL-CLOSED hook_unavailable; violations wired to logDecision hook_reject/hook_escalation (E8.2 boundary closed); /api/control/hook seam + /gov/violations UI (GovTabs 3rd tab, honest zero stream, policy board with seam-only mutations — live UI toggle proven: severity warn v2 audit actor=ceo risk=high, reverted v3); ROADMAP ACCEPTANCE: tests/e10 31/31 incl. violation→REJECT+decision_log row, evidence-less done→REVISE, revision exhaust→ESCALATE+manager-hop decision row+approval+high alert, fail-closed DB-down reject; full regression 47 files 336/0; tsc -b 0 root+dashboard; i18n purity PASS 1264=1264; FOUND+FIXED: phase7 watchdog suite manufactured a fake CRITICAL 'Budget hard-stop engaged' on the CEO rail every full regression (E8.4b trigger newer than the suite) — afterAll alert sweep added, residue row deleted; RULE #0 EN+TR × 1280+1920 (4 baselines e101-violations-*.png PENDING CEO eye; in-pass fixes: 1280 hidden action column → stacked buttons + slug own line + min-w 640, 652=652 no-scroll proof; console 0 error)) |
| E10.2 | Spawn yoluna bağlama (orchestrator) + hook_version damgası | spawn → agents.hook_version dolu | F/O | ✓ 2026-07-14 (K1 Fable in person; migration 20260714040000 idempotent — agent_runs.hook_version (§27 starting version) + agent_runs.hook_result (§14 close-time gate summary), ops:live run envelope payload +hook_result (§9 on E8.3 terminal events), §21 backfill non-archived NULL→v1, hook.enabled global→TRUE (E10.1 seed promise); packages/orchestrator hook-binding.ts (dispatch glue: hookEnabled per-employee resolve + fail-closed-direction default, assembleHookCtx from REAL rows (agents/budget_state/tasks.project_id+milestone chain), stampHookVersion idempotent, extractEvidencePackage executor-declared keys — never fabricated, buildHookResult, alertHookDisabled §22 'attention' dedup hook:disabled); runWorkerOnce bound: preTask BEFORE runScope (block REJECT → run NEVER born, task failed(policy) actor=hook + violation row §6), PASS → agents.hook_version stamp (ROADMAP ACCEPTANCE) + project purpose injected into executor prompt (std 11), runtime monitors post-exec (A14), postTask §19 REVISE loop (feedback rides task.feedback re-execution) → exhaust ESCALATE → run failed + §7 chain/approval in hook_result; observability RunScope +hookVersion open / +setHookResult close (R2 closure column); /org/employees 'Fable 5 Hook' column (§5 row 3: v1 ok-badge, archived neutral Unbound, live unbound danger) + /gov/violations zero-state copy closed ('gates bind at E10.2' → 'gates are bound'); EVIDENCE: tests/e10/spawn-binding 6/6 — NULL→v1 stamp SQL readback · §21 zero-active-unbound · pre-REJECT zero agent_runs rows + failed(policy) + std.permission_bounds rejected row · evidence-less done → max_revision_rounds+1 executions with feedback → ESCALATE + approval_id + hook_escalation decision rows · flag-off old path + hook:disabled attention alert · run.succeeded NOTIFY payload hook_result.hook_version=v1 (LISTEN capture); tests/e10 37/37; FULL regression 48 files 342/0 (pre-hook suites pinned via tests/helpers pinHookOff — A13, loud in DB); residue: violations 0, alerts 1 genuine queue-age, flag TRUE, e102t rows 0; tsc -b 0 root+dashboard; i18n purity PASS 1266=1266; RULE #0 EN+TR × 1280+1920 employees+violations, 9 baselines e102-*.png INDEX PENDING CEO eye — in-pass catch: 1280 9-column crush hid the hook column → DataGrid min-w-[960px] real scroll (652→960 probe) + archived Unbound de-alarmed to info; console 0 error. Adaptations A9-A14 in FABLE_5_HOOK_SPEC (CEO-visible). Boundaries: workflow-executor hook adoption + mid-run monitor taps + evidence-protocol default executor → P7 worker unification; pre-gate <100ms metric → P7 health row.) |

### E11 — Cost Intelligence (CEO Faz 8) — [[COST_CONTROL_SPEC]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E11.1 | `v_cost_breakdown` + Cost sayfası (kırılımlar) + eşik alarmları (`cost` kanalı) | UI'da gün/dept/model kırılımı gerçek cost_ledger verisiyle | F/O | ✓ 2026-07-14 (K1 Fable in person): row's two legs pre-shipped and now verified — `v_cost_breakdown` (0025x `20260711002500`, Berlin-day×dept×model×mode×agent grain) + `cost`-channel threshold alarms (E8.4b trigger `fn_alert_on_cost_threshold` 70 Attention/90 High/100 Critical, per-month dedup); GAP closed this row: /fin/costs had NO day breakdown and never touched the view → 'By day · 30 days' panel added, fed by v_cost_breakdown via PostgREST aliased aggregates (postgrestDailySource; dailyBreakdown module in the COST-04 single-source costs.ts, Berlin day = P&L D5 boundary, 30d window/15 rows = /fin/pnl precedent) + spec §7 drill: day card → ?day=YYYY-MM-DD → ledger table narrowed to that Berlin day (berlinDayRangeISO DST-safe [start,end) instants) + clear chip; drill state fully in URL (spec §10). EVIDENCE: tests/e11/cost-view-daily 6+1/7 — view day-grain EQUALS independent Berlin-day raw SQL over cost_ledger (byte-equal sums) · Berlin-midnight boundary pair 23:59/00:01 lands on different days · dailyBreakdown module vs raw SQL at rendered precision + tokens + order/ratio · 30d window discipline · berlinDayRangeISO pins (UTC+2/+1, 23h/25h DST days) · **ROADMAP ACCEPTANCE live: threshold probe crossing 70% fires attention alert source='cost' (rolled back, zero residue)** · UI acceptance in real browser: day panel renders REAL row (2026-07-14 · 198.3M tokens) + drill title/chip + negative proof (?day=2026-07-13 → honest empty ledger); full regression 49 files 349/0; residue: seeds 0, cap 100.00 intact, alerts = 1 genuine queue-age; tsc -b 0 root+dashboard; **eslint 0 (in-pass fix: pre-existing 4× 'rule not found' config defect — no-explicit-any registered, dead react-hooks directives inert-commented)**; i18n purity PASS 1269=1269; RULE #0 EN+TR × 1280+1920 + drill state, 5 baselines e111-costs-*.png INDEX PENDING CEO eye — in-pass catch: 3-col day cards at 1280 wrapped the date → 2-col until 2xl + nowrap; console 0 error. Adaptations A1-A4 in COST_CONTROL_SPEC (CEO-visible). Boundaries (stand from E8.4b): pg-boss rollup/guard/anomaly + budgets/cost_rollups tables + LiteLLM key sync + 10-chart R2 set + forecast → P7; Overview cost widget → E12.2. |

### E12 — Kalan modüller + widget (CEO Faz 6/9) — [[CEO_COMMAND_CENTER_SPEC]] §25/§31

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E12.1 | Kalan §31 sayfaları gerçek veriye bağlanır (Memory, Library detay, Intelligence rayı içerikleri) | rota-başı gerçek sorgu kanıtı; dummy widget 0 | F/O | ✓ 2026-07-14 (Fable in person, K1) in six waves a–f (commits b501731·421093e·ff4c924·2000b44·02eafe3·bc0d1a2): a) Memory + Intelligence Feed + rail live ticker; b) org family (companies, departments, directors, HR board); c) fin family (tokens, budgets, providers, capacity); d) intelligence family (skills, plugins, knowledge, MCP — LibraryKindBoard RSC over 357-item catalog); e) governance family (risks, policies, permissions, security); f) ops+sys family (automations via v_automation_schedules 20260714070000, runtime 24h agent_runs, integrations via least-privilege v_outbox_status 20260714080000, health settings-driven thresholds, logs 3 append-only tails, backups posture board). ACCEPTANCE PROVEN: grep ModuleWaiting src/app = 0 files; every page queries live tables/views (per-wave query evidence in commit messages). Wave-f in-pass catches: workflows.trigger jsonb filter crash, escalate_after_minutes [object Object], outbox permission denied (view added), TR purity 'settings güdümlü'→'Ayarlar güdümlü'; test-residue root cause fixed (self-healing sweeps in registry + decompose-dispatch suites; 48 orphan probe tasks/runs purged, stale queue-age alert resolved w/ mitigation). Evidence: vitest 349/349 · tsc -b 0 · eslint 0 · i18n purity 1671=1671 · RULE #0 EN+TR × 1280+1920 all 6 wave-f routes, console 0 errors, 25 baselines e121f-*.png PENDING CEO eye. |
| E12.2 | Widget sistemi: layout persist `settings_values(scope='ceo_dashboard')` + ekle/kaldır/taşı | layout kaydet → yeni oturum aynı layout | F/O | **✓ COMPLETE 2026-07-18 ~02:05.** Executive Overview v3 = WidgetGrid over the v2 composition (CC-SPEC R8/§5/§10/§11): `widgets/types.ts` (spec §10 layout schema VERBATIM as Zod, 4-col reflow, default layout = v2 composition as data) + `widgets/registry.tsx` (9 real-sourced types, all `v_exec_overview` — single round-trip preserved; sourceless registration impossible) + `widgets/widget-grid.tsx` client island (RSC renders content, island arranges; add/remove/order-swap/width-cycle/save) + `/api/control/layout` seam (Zod wall → EXISTING `control_settings_set` DEFINER fn — audit + change_log + undo + idempotency, zero new DB surface; DASH-05 purity allowlist row). Registry key `dashboard.layout` migration 20260718010000 (ledger 92=92); scope string `ceo_dashboard:default` = RECORDED ADAPTATION A1 (settings engine scoped-write law measured; CC-SPEC adaptations table NEW). GATE PROOF LIVE (real browser through the real seam): edit → remove 'attention' + move 'pulse' → save → settings_values row measured (8 widgets, changed_by ceo, change_log id 144) → localStorage+sessionStorage CLEARED → fresh load → SAME edited layout (server truth, not browser storage); then CEO-path restore to default composition (add-select + order-swap + save → row back to 9 widgets) = add flow proven + eye-test mutation hygiene. tests/e122 6/6 (registry completeness both ways; §10 schema + 4-col bounds + determinism; unknown-type/out-of-grid rejection; scoped-write law pin; set→row→undo round-trip; idempotency replay = ONE change row — live-layout no-op subtlety measured and pinned); full regression **60 files 443/0** (+15 skips); purity 1837=1837; dashboard tsc 0 + next build 0. RULE #0 pass: EN+TR × 1280/1920 page overflow 0 / clipped 0 (in-pass kills: sr-only frame links = a11y duplication + measurement noise → content doors ARE the drills; health factor `truncate` → break-words — live '35 failed' label wrapped honestly); edit-mode battery clean; baselines `overview-e122-*.png` (⚠ CEO eye). BOUNDARIES recorded: pointer drag-drop + free-height resize = adaptation A2 (U16 design slot); shell header horizontal overflow at ≤~720px = PRE-EXISTING shell surface (mobile targets R12/R14), measured 500px scrollW 722, not this row's surface. |
| E12.3 | **Route Completeness Gate** (direktif §2.1/§6): ModuleWaiting = 0; rota-başı DoD matrisi raporu (`route \| owner \| source \| sorgu \| drill \| mutation \| loading \| empty \| error \| stale \| permission \| audit \| EN/TR \| test \| status`) | rota matris raporu | `grep -rl ModuleWaiting src/app \| wc -l` → **0**; matris her nav rotasında tam | F/O | **✓ COMPLETE 2026-07-18 ~02:35.** Evidence: gate 1 `grep -rl ModuleWaiting apps/dashboard/src/app | wc -l` → **0** (orphan component deleted). Gate 2: `.planning/research/E12.3-ROUTE-DOD-MATRIX.md` — **46 nav routes / 46 ✓ / no-page 0 / sourceless 0**, every cell MEASURED by `scripts/dev/route-dod-matrix.mjs` (re-runnable engine; dynamic `.from(table)` false-negative found+fixed); report registered on the knowledge shelf (item ea27417d, audit 31734 — std.knowledge_shelf law dogfooded). GATE-CLASS DEFECT FOUND+FIXED IN-WAVE (§17/§34): ZERO error/loading/not-found boundaries existed — raw Next crash/404 screens reachable from EVERY route → born: `(command)/error.tsx` (contained failure + retry, cookie-locale client boundary), `(command)/loading.tsx` (Panel skeleton, no spinner), `(command)/not-found.tsx` + ROOT `app/not-found.tsx` (mistyped `/tr/...` class measured hitting raw 404). RENDER PROOFS: real 404 URL TR panel (`notfound-e123-tr-1280.png`); crash-probe route through the FULL PROD BUILD — shell stays alive + Yeniden dene visible (`error-boundary-e123-tr-1280.png`), probe deleted + absence re-measured (307). Battery: dashboard tsc 0 + next build 0 ×3; full vitest 60 files 443/0; purity 1843=1843. Ops note: dev-server probe path ABORTED at 185MB free RAM (X230 freeze discipline) — prod-build probe cycle used instead; ghost pid 3985 re-killed. |
| E12.4 | **Holding/CRM Integration Gate** (GAP-05): CRM yeni shell bilgi mimarisinde + company switch/context + şirket→departman→çalışan→proje drill-down + şirketler-arası veri izolasyon testi; eski cockpit rotaları AYNI commit'te ölür (tek-anahtar idiomu) | `(command)/` CRM rotaları + company context | Playwright: company değişir → veri izole; eski /crm 308 | F/O | **✓ COMPLETE 2026-07-18 ~02:55.** Evidence: (1) DATA WALL born — migration 20260718020000 (ledger 93=93): `company_id` on crm_clients ONLY (children isolate through client_id inner join — no denormalized drift), `fn_default_company_id()` STABLE default (companies row is runtime-born; uuid literal would break bootstrap parity — R2.5 lesson). (2) CRM lives in the shell: `/revenue/crm/{clients,contacts,deals,requests}` (nav: Intelligence group revenue family, Handshake icon; CC-SPEC adaptation A3 records the placement rationale + company-cookie idiom), entity components REskinned to command tokens (cockpit token family left those files), chrome carries the COMPANY SWITCH (single-company state renders the honest name — minimalism ruling). (3) GATE PROOFS LIVE: real browser — DXB context shows only the DXB fixture client, switch to the seeded test company INVERTS the universe (cookie set, server queries re-scope; screenshots crm-e124-*); OLD ROUTES DIED SAME COMMIT: `curl -I /crm` → **308** → /revenue/crm/clients, `/crm/deals` → 308 (next.config permanent redirects); fixtures swept (crm_clients back to 0). (4) tests/e124 5/5 (default-fn resolution, default landing, company-scoped clients query, child-join isolation both directions, NOT NULL wall); purity-class in-pass fix: table dates were en-GB pinned — '18 Jul' rendered on the TR locale → locale-threaded formatter ('18 Tem' measured). (5) Battery: 61 files 448/0 (+15 skips) · purity 1846=1846 · dashboard tsc 0 + build 0 · RULE #0 EN+TR × 1280/1920 clipped 0/overflow 0 · purity allowlist re-homed to the new actions seam. BOUNDARY (recorded): şirket→departman→çalışan→proje drill-down beyond CRM = /org/companies surface already carries the org drill chain (E12.1); per-company FILTERED org/project views join the multi-company era when a second REAL company exists (Outleteuro, Phase-11) — with one live company the extra filter layer would be an info-free surface (minimalism ruling). |
| E12.5 | **Workforce Completeness Gate** (GAP-02/03/09): her aktif departmanda head; orphan/escalation'sız worker 0; gap-onaylı personalar canlı employee (registry+model+skill+MCP+permission+budget+dashboard+audit zinciri); 153 legacy'nin her biri keep/merge/rewrite/retire kararlı (CEO onayı olmadan rol silinmez); rapor = capability coverage, persona sayısı değil; **stale persona_path = 0** (agents rows still pointing at removed `agency-agents/%` tree — 129 measured 2026-07-12, all dormant/archived v1.0-legacy; each E5.5 wave migration repoints its batch, this gate sweeps the remainder); **E5.7 extension (2026-07-12, [[00-CEO-DIRECTIVE-MUST-ROSTER]] holding 5):** matrix/plan-promised ADD absent = **0** (every role promised in WORKFORCE-GAP-MATRIX §3 or WORKFORCE-MUST-EXPANSION-PLAN §4 has a live agents row + bound persona); capability coverage reported against MUST-EXPANSION-PLAN §3 matrix (not persona count); `DEPUTY-FAILOVER-MAP.md` exists and covers the SPOF set | DB + `/org/*` sayfaları | SQL: head'siz aktif dept 0, orphan 0; `SELECT count(*) FROM agents WHERE persona_path LIKE 'agency-agents/%'` → 0; promised-ADD set-diff sorgusu → 0 eksik; coverage raporu | F/O (persona içerikleri **F** — CEO K2: tümü Fable) | **✓ COMPLETE — machine gates closed 2026-07-18 (Fable in person), activation chain proven 2026-07-28 (see the end of this row).** Automated sweep (plan §11.4 debt) LIVE: `scripts/org/workforce-gate.mjs` + frozen 67-slug promise ledger (`scripts/org/promise-ledger.json` — 53 matrix §3 + 14 plan §4; arithmetic closes both ways: 36 pre-D7 + 12 social + 19 D7 = 67) → **9/9 PASS**: promised-ADD absent **0/67** · unpromised-role 0 (199 live = 132 legacy+orchestrator + 67 ADD) · stale persona_path **0** · headless dept 0/21 (structural — NOTE: all 21 depts carry status='dormant', so the "active dept" clause holds structurally, not vacuously claimed) · orphan 0 · persona-chain unbound 0/no-mcp 0/stale-tag 0 · fixture-debris 0 · persona-file-exists 220/220 on disk · DEPUTY-FAILOVER-MAP SPOF 6/6 + heads 21/21. Hygiene migration `20260718030000` (idempotent 2×, ledger 94): r23t fixture debris fully deleted (run+3 decisions+2 tool_calls+3 violations), 21 stale paths repointed per matrix §2 dispositions (15 retire→`personas/_library/`, 6 merge→survivor file), **persona_version truth restored on 175 rows** (all 199 live personas measure author=fable-5+passed → tag `v2.0-fable`; the ACTIVE finance row was feeding the stale tag into hook.preTask ctx). Regression: `tests/e125/workforce-gate.test.ts` 7/7 (suite-interleave guarded). Capability coverage report **30/30 §3 rows owned** → `.planning/research/E12.5-CAPABILITY-COVERAGE.md` (shelf item 1a56f36d, audit 32904). 153-legacy dispositions: CEO-approved 2026-07-11 + executed (21 archived, exact slug match — §9.1). **REMAINING LEG (single, named — why ◐ not ✓):** operational go-live. All owners are dormant/draft; "canlı employee" in full = HR machine `draft→probation→active` (HR_OPERATING_SYSTEM_SPEC:41-48). Mass activation is BLOCKED-CLASS on the CEO's OPEN MUSTS-Talep decisions (2026-07-16: worker objective contract, text-only scope) — activating 198 employees without those contracts = ungoverned spend+blast-radius. Activation waves execute the moment the CEO rules; also unlocks R4.3's staffed external-hand proof. **CEO RULED 2026-07-18 morning (D10 objective contract YES / D11 text-only option-1 — directive §3-bis, U17) and the machine is LOADED (~10:00, Fable in person): migrations 20260718090000-093000 (ledger 98=98) — `tasks.due_at` born, `fn_hr_machine_intake` born (spec :145 same-fns law), probation task carries the window deadline, approval surfaces render the task frame (BUDGET CEILING + TASK DEADLINE live-proven on the real R2.4 row, EN+TR, purity fixes in-pass). INTAKE EXECUTED: 197/197 equipped (audit 142 machine_intake + 55 equipment_backfill, settings 591=197×3; dormant remainder = agents-orchestrator, named exclusion). tests/e125 +8 (activation-contract), battery 463/0. REMAINING at that hour: keys→probation→evaluate chain — keys phase needed ONE command outside the session classifier's reach (`bash scripts/hr/activate-workforce.sh keys --execute`); then probation ×197 text-only through the resident worker, evaluation on the U17 derived-score formula. **✓ ROW COMPLETE — the chain RAN on 2026-07-18; the ◐ above was stale for ten days and was corrected 2026-07-28 by the U41 sweep, not by new work.** Measured against `audit_log` this session: `employee.machine_intake` **208** · `employee.probation_task_assigned` **212** · `employee.evaluated` **197 distinct employees** (each payload carries `score`, `threshold: 0.7`, `result: active` — e.g. `{"score":1.0,"result":"active","threshold":0.7}`) · `registry.activate` **127**. Registry today: <!-- STATE: agents_active = 199 @ 2026-07-28 --> **199 active** of <!-- STATE: agents_total = 205 @ 2026-07-28 --> 205 rows, every active row carrying an `mcp_profile` (199/199), 21 departments, 617 settings rows. The row's own exit condition — "canlı employee (registry+model+skill+MCP+permission+budget+dashboard+audit zinciri)" — is met. **This stale ◐ is the defect that produced U41:** a finished leg left the CEO believing his own workforce was still half-hired.** |

### E13 — Kabul turu (CEO Faz 10) — [[TEST_STRATEGY]], [[ACCEPTANCE_CRITERIA]]

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| E13.0 | Operational Readiness (direktif §2.6 — GAP-08): backup **restore drill** (RPO/RTO kanıtı, yalnız dosya varlığı kabul değil), migration rollback tatbikatı, session E2E, accessibility (klavye/focus/contrast/reduced-motion), failure-path testleri (timeout/duplicate/retry/partial failure/idempotency), 100+/10k kayıt pagination/performans | tatbikat raporları | restore drill raporu + rollback kanıtı + a11y/failure test çıktıları | F/O | **✓ 2026-07-18 (row complete — last leg off-site key, evidence below)** **restore-drill DB leg ✓ 2026-07-17** (combined with R2.5 as recorded there): canonical `scripts/restore-db.sh` + real live pg_dump → pristine container → ledger 87/agents 220/RTO 5s → live-vs-restored inventory DIFF 0 + smoke 36/36. OPEN: off-site (Hetzner Storage Box) automated dump pipeline + scheduled cadence (defines real RPO), rollback drill, session E2E, a11y, failure-path, pagination/perf. **DRILL WAVE 2026-07-18 ~10:10 (report `.planning/research/E13.0-OPERATIONAL-READINESS.md`): rollback drill ✓ (db-suite 46/46 + 0021x executed live, E13.1 wave) · failure-path ✓ all five named classes have executed tests (crash/double-fire/r24-replay/toctou/db-suite — cited per class) · pagination/perf ✓ `scripts/test/perf-drill.sh` BORN: 10k/10k/1k synthetic on ephemeral, dashboard-real bounded queries all <19ms (threshold 250, error≠PASS gate) · a11y machine legs ✓ (contrast exit 0; keyboard/labels/reduced-motion 3 new public L5 scenarios 8/8) · off-site: VPS pipeline was ALREADY LIVE since 07-09 (vps/README drill evidence) + laptop local dump BORN + cron 02:30 INSTALLED (first dump `BACKUP_OK 10237807 bytes`); laptop→Box key = CEO credential boundary. **SESSION-E2E LEG PAID 2026-07-18 ~20:07:** CEO minted storageState (2-min headed login, credential never entered a transcript) → full L5 suite **17/17 ONE run** (9 authed + 8 public incl. the 3 a11y scenarios) — session E2E closed. **OFF-SITE LEG PAID 2026-07-18 ~20:19 → ROW ✓ COMPLETE:** CEO ran `install-storagebox-laptop-key.sh` (da787cb) — laptop key live on u629578-sub1 (LAPTOP-BOX-AUTH OK); `~/.ssh/config` Host `dxb-storagebox` + script default `BACKUP_DEST=dxb-storagebox:.`; LIVE drill: `BACKUP_OK 2026-07-18 12435816 bytes` + `OFFSITE_OK` + box listing byte-identical 12,435,816. Real RPO = dual cadence (laptop 02:30 cron + VPS 04:30, both measured live). Every verify-column item now has executed evidence; visual baselines ride E13.2 (CEO eye).** |
| E13.1 | Tam test koşusu (L1-L6) + §38 makine-denetlenebilir maddeler | TEST_STRATEGY §24 komut seti yeşil **+ audit hardening (F-09/F-15, registered 2026-07-17): kapanış kanıtı = PRODUCTION capability proofs — mock/demo kanıtı bu satırı KAPATAMAZ; Outleteuro gerçek-pilot hazırlığı attested (F-15), simülasyon ikame edemez** | F/O | **✓ COMPLETE 2026-07-19 (machine tier green 2026-07-18 ~05:50; F-09 production proof + U19 F-15 strike close the row — see end of cell).** §24 command set — ALL FIVE run verbatim, all green: (1) `pnpm test` → **455 passed / 0 failed** (+15 skips, 62 files); (2) `bash scripts/test/db-suite.sh` **BORN this wave** (L2 layer existed only on paper) → **PUSH OK ledger 94=files 94 · IDEMPOTENT OK applied 0 · ASSERTS 7/7 · ROLLBACK BLOCKS 46/46 + 0021x executed live · DB SUITE PASS** on a pristine ephemeral container (fresh-chain parity holds INCLUDING tonight's migrations; suite findings fixed in-wave: preamble needs supabase_admin plane on the supabase image [R2.5 plane rule], image-supplied auth.uid() reads the dotted GUC → both JWT GUC styles set, 0021x rollback block was STALE at chain head → CASCADE amendment recorded in-file — first time any rollback block ran for real); (3) `pnpm --filter dashboard exec playwright test` — L5 runner BORN (@playwright/test 1.61.1 root+dashboard, config + tests/e2e/) → **public tier 5/5 PASS** (auth wall ×2, login form, RULE #0 login overflow 0, anonymous-face rule: unknown route → wall-first 307 /login, no route enumeration for anonymous — measured truth pinned) · **authed tier 8 scenarios SKIP with recorded reason** (see remaining leg); (4) `pnpm typecheck` 0 (script added) + dashboard build 0 + `gitleaks detect` no leaks; (5) `node scripts/test/i18n-audit.mjs` BORN → **"EN keys == TR keys, missing: 0" (1846)**. §38 M-scans: dummy-data grep **0** (item 23) · hardcoded-color scan → **1 found+killed** (approval-center champagne fallback had DRIFTED from the token #c8a962≠#d8b98c — item 22 now 0) · routes 46≥26 (item 2) · 7 groups ≤3-click structural (item 3). Ops discovery: the recurring "ghost next-server 3985" = open-notebook CONTAINER's internal process (root/supervisord, never on :3000) — hunt closed, measured. **AUTHED L5 TIER PAID 2026-07-18 ~20:07 (Fable in person):** CEO minted storageState himself via `node scripts/test/e2e-login.mjs` (~/.dxb/e2e-state.json chmod 600, outside repo, never read by any author) → full suite `DXB_E2E_STATE=… playwright test` → **17/17 PASSED in ONE run (58.3s)** — all formerly-skipped authed scenarios incl. drill chain §38/4, settings §38/14, layout server-truth §38/15, D10 economic frame at BOTH 1280/1920 (the E12.5 instrumented width leg — overflow 0). One transient measured honestly: first-hit `/sys/settings/global_os` exceeded the 5s expect window under wave DB load (cold RSC + activation wave contention), passed 5.8s on immediate rerun and in the full-suite rerun — no code defect, no test change. **F-15 leg STRUCK 2026-07-19 by U19** (Outleteuro cancelled entirely by CEO order — attestation of a cancelled pilot is meaningless; audit F-15 honest-labeling satisfied by the U19 status label). **✓ CLOSED 2026-07-19 ~01:45 (Fable in person): F-09 PRODUCTION proof delivered** — [[00-NOTE-E131-F09-PRODUCTION-PROOF]]: evidence-class map for every acceptance claim (audit taxonomy verbatim) + production-topology core proof measured live (198 active employees, 0 without a done task; ALL 218 done tasks claimed by resident-worker autonomously; 442 agent_runs across 4 real model tiers with live escalation; 295 real tool_calls incl. external hands; approvals 114/0-pending via control fn; hook_violations 1883 = gate live at scale; cost_ledger 1316 + audit_log 11744) + honest F-10 boundaries labeled (revenue_ledger 0 per U19 cancellation; outbox 113 ready/1 executed = outward scale NOT claimed → R6.1; 24/7 residency ⚠ UNVERIFIED — VPS-deploy property, worker measured not-running post-wave; tool_calls.ok NULL instrumentation debt → C-series). L7 = E13.2 CEO session ✓ separately (2026-07-18 conditional approval row). |
| E13.2 | CEO göz testi oturumu (§37 10 ekran + §38 görsel maddeler) — giriş bilgileri ÖNCEDEN verilir | ⚠ CEO onayı (hiçbir modele devredilemez) | İNSAN | **✓ 2026-07-18 ~20:55 — CEO APPROVED (conditional, in-session verbatim: "ok I just check some pcitures etc they seem fine ama sonradan değişiklik isteyeceğim. şimdilik ok verelim").** Session shape: CEO logged into the live dashboard himself (localhost:3000, own credentials) and toured live surfaces instead of the packet (packet c8c7a1d served as index; CEO explicitly preferred live). One live question answered with a measured source (Library quality score 80 = deterministic enrich.mjs formula, not model opinion). **Recorded condition: CEO WILL request design changes later — those arrive as normal defect/change orders (RULE #0 pass per surface when they land), most under the U16 design slot he owns. This approval closes the E13.2 gate; it does not freeze the design.** 175 pending baselines flip to CEO-seen status as a batch. |  <!-- CEO-OK: eye-test-2026-07-18 -->
| E13.3 | **CEO Kullanım Kılavuzu (U13; Fable gap-analizi → CEO forward 2026-07-17):** tek kaynak `HOLDING-OS-MASTER-PLAN/CEO_OPERATING_MANUAL.md` — günlük rutin (07:00 briefing → intelligence → approvals), 7 nav grubunun işlevi, drill-down haritası, onay-akışı kuralları (para-çıkışı asla toplu işlenmez; delegate/reanalyze ne zaman), alarm seviyeleri + doğru tepki, hard-stop davranışı (release CEO'da), Control Mode yetenekleri (model rozeti, bütçe anahtarları), göz-testi/design-bank döngüsü, acil durum: Global Pause; dashboard içinden erişilir (kind=sop intake → Knowledge yüzeyi); dil U13: TR ana + EN kanonik ayna; **sıra hükmü:** yazım E12.3 route-gate SONRASI (anlattığı ekranlar final), E13.2 göz-testi oturumuna kılavuz eşlik eder | kılavuz dosyası (TR+EN) + library sop kaydı + dashboard erişim kanıtı | F/O | **✓ 2026-07-18 ~05:15 (Fable in person).** Order clause satisfied: E12.3 closed ~02:35 same night. `CEO_OPERATING_MANUAL.md` LIVE (00-INDEX row 35) — 12 sections ×2 languages (TR primary + EN canonical mirror, ONE file): daily 15-min routine (briefing `v_morning_briefing` → approvals → alerts), 7 nav groups / 46 routes (from command-nav.ts verbatim), drill-down map, approval rules with the 7 MEASURED decision actions (approve/reject/approve_with_modifications/delegate/request_info/reanalyze/change_policy; money-out never bulk; >24h money-out auto-HIGH per E9.3 §9b), alert levels = live enum {informational, attention, high, critical} + response table, hard-stop €50-150/70%/100% + release-is-CEO's, Control Mode (model badge + orchestrator.* keys + human_approval_threshold + money_out_gate), Global Pause = MEASURED key `os.global_pause` (+os.maintenance_mode, both undo-able), eye-test/design-bank loop, voice-line HONEST state (U15 open ledger cited), incident playbook. Every fact measured this session (settings_registry, live enums, Zod schemas, nav config). Library sop `ceo-operating-manual` registered via control fn (item 0d530051, audit 33489). DASHBOARD ACCESS PROVEN live: /ai/library?kind=sop lists it (SOPs 3 records, CEO Office owner) + detail drawer renders; RULE #0 spot pass EN+TR × 1280/1920: pageOverflow false ×4, clipped 0 ×4, TR purity (SOP'lar / 18 Tem 2026 / EN-leak 0); baselines library-e133-sop-*.png ⚠ CEO eye. |

### R — Revenue-First + Execution Activation (CEO Talep 2026-07-16/17 — [[00-CEO-DIRECTIVE-REVENUE-FIRST]], [[REVENUE_ENGINE_SPEC]], dış denetim F-01..15)

> Sıra hükmü: açık E12.2–E13.2 satırları plandaki yerinde kalır. R1 spec/anayasa satırları paralel yürür (yazım işi, rails'e dokunmaz); R2 execution-activation bloku gerçek gelir operasyonunun ÖN ŞARTIDIR ve E13 kapanışından bağımsız başlayabilir. Kaynak kararlar: D1-D9 direktifte.

| # | İş | Kanıt | Model | Durum |
|---|----|-------|-------|-------|
| R1.1 | REVENUE_ENGINE_SPEC yazımı (27-başlık + KALIR/YENİ/DEĞİŞİR + adım-başı verify) | spec dosyası INDEX Dalga 6'da ✓ | **F** | ✓ 2026-07-17 (Fable in person; kaynak: Talep + D1-D4 + ölçülmüş boşluklar) |
| R1.2 | `revenue_core` migrations 0028a-c (engines-as-data FK swap dahil, U10) + `control_revenue_*` fn ailesi + testler | SPEC §24.1-2 komutları: `\dt` 4 tablo + FK kanıtı + illegal-transition red testi | F/O | ✓ 2026-07-17 (migrations 20260717020000/020100/020200 canlıda + RUN2 idempotent; 4 tablo + `fk_engine` FK kanıtı [CHECK enum öldü, U10] + 6 motor seed + €50 draft hedef; 13 control fn — kanıtlı duvarlar: ILLEGAL_TRANSITION, **HALAL_GATE (G2)**, separation-of-duties §13 ×2, CEO wall ×2, 12-boyut skor doğrulaması [6.67=80/12]; 3 view dürüst [net 0, gap 50, snev 0]; `tests/r12` 8/8 PASS — spec §24.2'deki `pnpm --filter kernel test revenue` komutu repo konvansiyonuna uyarlandı: kök vitest `tests/r12/`, e10 deseni) |
| R1.3 | Günlük döngü job'ları (`revenue.scan/score/brief/rollup`) + 12-boyut skorlama + settings seed | `SELECT name FROM pgboss.schedule WHERE name LIKE 'revenue%'` → 4 | F/O | ✓ 2026-07-17 (packages/revenue 4 handler [hr-jobs idiomu, scheduler-içi, pre-R2 digest]; scan = intake halal screen: haram-terim fırsat otomatik 'review' — hüküm ASLA otomatik değil; brief = hedef+pipeline+SNEV Broadcast; rollup = snapshot + tempo-açığı alarmı [eşik settings 25%]; migrations 20260717030000/030100: 4 setting [score_weights 12-boyut, halal_screen aynası, snev faktörü, gap eşiği] + pgboss queue/schedule 4 satır [05:10/05:25/05:40/05:55 UTC — hr saat çakışması yok]; verify: schedule sorgusu → 4 ✓; `tests/r13` 5/5 + regresyon r12+e10 49/49; skorlama 12-boyut zaten R1.2'de fn-kanıtlı) |
| R1.4 | Dashboard: `/revenue` hedef kartı + objectives/opportunities/portfolio rotaları | SPEC §21.1 + RULE #0 kanıt seti | F/O | ✓ 2026-07-17 (4 rota canlı gerçek veriyle [€50 taslak hedef, dürüst sıfırlar, SNEV, boş-durum 05:10 taramaya işaret]; nav Intelligence grubu +4 [spec §7 emri, U8 emsali]; i18n purity PASS [sözlük 1755=1755]; RULE #0: EN+TR × 1231w/1280/1920 + `scrollWidth=clientWidth` taşma ölçümü 4 rotada; baseline'lar `references/design-bank/revenue-*.png`; **CEO canlı göz-testi düzeltmeleri aynı turda**: yatay taşma kökü grid `min-w-0`, "…" tamamen öldü [seed başlık "€50 net"], bilgi-taşımayan alanlar kalktı [evidence/metric/proposed-by; öneren yalnız Hamza-önerisinde rozet] — hüküm memory'ye yazıldı; tsc temiz) |
| R1.5 | İslami sınır kodlaması: halal screen (SPEC §16) + [[FABLE_5_HOOK_SPEC]] pre-task policy ek + APPROVAL bağlantısı — ölçülen boşluk: korpus+kod grep = 0 hit (2026-07-16) | hook red testi: flagged task fail-closed + decision_log | F/O | ✓ 2026-07-17 (`const.halal_screen` policy canlı: `pre\|block\|std11`, 9 kategori TR+EN kanonik liste; migration 20260717010000; pre-task `halal_screen` check tr-locale + harf-sınırı; e10 hook 35/35 — EN red + TR red + false-positive PASS + §27 CEO warn; spawn-binding 6/6; hook spec registered amendment R1.5; escalation/approval bağı mevcut zincir üzerinden — violation→alert trigger + §7 zinciri değişmedi) |
| R1.6 | VOICE_INTERACTION_SPEC yazımı: v1 orkestratör çağrı hattı (D3, free-first D1: Speaches+voicebox) + boardroom mimarisi "add-later" bölümü + OpenAI **mini** gelecek yolu | spec dosyası + INDEX ✓ | **F** | ✓ 2026-07-17 (spec #33 diskte, V1-V10 kuralları + §5.13 eşleme tablosu + §3.4 boardroom add-later; INDEX Dalga 6 ✓ + U4 notu güncellendi) |
| R1.7 | Anayasa değişiklikleri: [[HOLDING_OS_PRODUCT_SPEC]] P0 (helal net kâr amacı) + [[MASTER_PLAN]] Revenue-First bölümü | `grep -c "net profit\|net kâr" MASTER_PLAN.md HOLDING_OS_PRODUCT_SPEC.md` > 0 (taban: 0) | **F** | ✓ 2026-07-17 (grep → MASTER_PLAN 3, PRODUCT_SPEC 1; P0 §5'te P1-P7 üstünde, MASTER_PLAN §11 normatif bölüm) |
| R1.8 | Persona anayasası: [[EMPLOYEE_PERSONA_STANDARD]] Discipline DNA (fable-method uyarlaması, D6) + İslami ton bölümü (D5) + `fn_persona_gate` bölüm-yoksa-FAIL şartı + 199 dosyaya uygulama + sync | gate negatif testi + `sync --verify` PASS + örnek persona diff | **F (K2: tümü Fable)** | ✓ 2026-07-17 (standard G8+§4.1 kanonik metin + U11; gate.ts §12 PERSONA_SECTIONS — bölüm-yoksa `section-12-missing` FAIL, negatif test kanıtlı; hr vitest 29/29; 199 dosyaya §12 birebir; mekanik kapı 199/199 PASS; DB: 199 submit + 199 gate passed + `sync --verify` → match 199 · diff 0 · fail 0; ayrıca sync scripti Title-satırsız dosyada ölüyordu — `extract_title` pipefail fix aynı dalgada) |
| R2.1 | **Resident worker loop (denetim F-01):** production topolojide `tasks` kuyruğunu sürekli tüketen süreç + restart sürekliliği | denetim §4/F-01 7-adım kanıt zinciri (manuel çağrısız intent→run→terminal) | F/O | ✓ 2026-07-17 (Fable in person, K1): `packages/orchestrator/src/worker-loop.ts` drainTasks 3 bacak [queued→runWorkerOnce cap1 · review→qa cap3, confidence<0.6 ladder'a · failed→escalate cap5, blocked hariç] + scheduler `task.worker` 10s self-chain + **production girdi noktası `main.ts` (`pnpm scheduler`, SIGTERM graceful)** — F-01'in kökü: startScheduler'ı hiçbir süreç çağırmıyordu; spec §3 adım 1 `assignEmployee` İNŞA EDİLDİ (claim anında: dept+active+MCP-profil, en az yüklü, decision_log gerekçeli — intent task'ları std.permission_bounds'ta ölüyordu, ÖLÇÜLDÜ) + std 11 proje bağı (intent task'ları holding projesi `dxb-global-os`'a biner; dispatch opts.projectId, TaskEnvelope LOCKED dokunulmadı — std.project_alignment ölümü ÖLÇÜLDÜ); **ölçülen pg-boss 12 kusuru**: singletonKey dedup yalnız policy='short' — 5 chain kuyruğu flip + 5120 yetim created job temizliği (migration `20260717050000` 2× idempotent, RUN2 all 0); **CANLI F-01 7-adım**: gerçek CEO oturumu command-palette intent → intake→dispatch(proj+dept)→resident-worker claim→accounts-payable-agent ataması→pre-gate PASS→GERÇEK Agent SDK yürütme→post-gate REVISE×2→ESCALATE→ladder L4→L3→**[süreç öldürüldü, kuyruk 30s donuk ölçüldü, restart→otonom re-claim]**→L2/L3 dürüst COST-02 reddi (LiteLLM anahtarı=P7)→L1 gerçek koşu→**blocked terminal + CEO raporu, sıfır manuel çağrı**; 5 zincir restart sonrası da 1'er job; `tests/r21` 6/6 + tam regresyon 52 dosya 372/0; kayıtlı adaptasyonlar A1-A5 AGENT_ORCHESTRATION_SPEC'te (A4: done-terminali std 15 tool_call_proof gereği ANAYASAL olarak R2.2'ye bağlı — std 15 gevşetme REDDEDİLDİ); hijyen: probe-1 zinciri FK-sıralı süpürüldü, probe-2 kanıt olarak kaldı + 25 probe alert'i governed fn ile resolve (audit'li), aktif alert 0; UI yüzeyi değişmedi (RULE #0 tetiklenmedi) |
| R2.2 | **Worker gerçek araç yüzeyi (F-02+F-04):** grant→gateway profil→SDK tools bağlanır; `tools: []` ölür | gerçek tool_calls satırı (default executor) + revoke→red kanıtı (denetim F-04 7-adım) | F/O | ✓ 2026-07-17 (Fable in person, K1): gateway `runtime-profile.ts` (derlenmiş katmanın runtime okuyucusu: employee-overlay > department > default-deny; her koşuda yeniden okunur — stale-cache yok) + worker-shim SDK köprüsü `buildSdkToolOptions` (allowed=profil seti; disallowed=envanterin KALANI bağlamdan silinir; `strictMcpConfig`; builtins kapalı; maxTurns 12) — `tools:[]` staffed yolda ÖLDÜ; A10 kanıt taşıyıcısı: WorkerJson +`evidence[]`/`acceptance_map`, `RunScope.settle()` + `resolveEvidenceToolCalls` (verification item'ları koşunun GERÇEK tool_calls satırına ÇÖZÜMLENIR — asla uydurulmaz; eşleşmeyen çıplak kalır ve std 15 reddeder); **ölçülen 2 kök kusur aynı dalgada tedavi**: (1) dxb-mcp stdio girişi repo yolundaki BOŞLUK yüzünden hiç bağlanmamıştı (`"file://"+argv[1]` ≠ %20'li import.meta.url — server 'failed', 0 tool görünür, opus sahte tool_calls METNİ uydurdu ve std 15 yakaladı; fix `pathToFileURL`, probe `scripts/dev/mcp-surface-probe.mjs` ile connected+21 tool ölçüldü), (2) tool'lu oturum düz yazı kapatınca JSON.parse koşuyu fatal öldürüyordu — şimdi kanıtsız pakete düşer, post-gate REVISE döngüsü devralır; **CANLI F-02+F-04 7'şer adım**: CEO oturumu grant (queue_get→accounts-payable-agent, grant 171 audit 14901) → 30s resident recompile OTONOM overlay (yüzey 21→1, hash 70944068) → CEO intent → L4 haiku İLK TURDA gerçek `mcp__dxb-mcp__queue_get` çağrısı (tool_calls satır 2137, run de854a8f) → evidence.toolCallId=2137 birebir → post-gate PASS → QA PASS → **done (şirket tarihinin ilk uçtan uca otonom teslimatı; iz: claimed→running→review→done, aktörler yalnız resident-worker+orchestrator:qa)** → CEO revoke (audit 14939) → recompile overlay'i SİLDİ → yüzey ölçümü source=department (grant kısıtı kalktı, cache ölü); dürüstlük: model yalnız queue_get yetkiliydi, gerçek harcama RAKAMI UYDURMADI (placeholder bıraktı) = least-privilege çalışıyor; `tests/r22` 3/3 + tam regresyon 53 dosya 375/0; kayıtlı adaptasyonlar A6-A8 AGENT_ORCHESTRATION_SPEC'te; api-mode (LiteLLM) tool yüzeyi P7 proxy'yle |
| R2.3 | **Workflow executor birleşmesi (F-03/F-05):** aynı hook/gateway/observability anayasası; spec'teki `orchestrator/runner` kararı netleşir (uygula VEYA kayıtlı iptal) | workflow gerçek tool step + tek anayasa testi; runner kararı U-tablosunda | F/O | ✓ 2026-07-17 (Fable in person, K1): **runner kararı = kayıtlı iptal, U-tablosu U12** (ayrı `orchestrator/runner` paketi doğmadı; sorumluluklar worker-shim+worker-loop+hook-binding'de; `control_signal` spec maddesi AÇIK bırakıldı — sessiz kapanış yok); paylaşılan anayasa parçaları bağımlılık-temiz evlere taşındı ve TEK implementasyon oldu (gateway `buildSdkToolOptions` · hook `extractEvidencePackage`+`hookEnabled`/`alertHookDisabled` · observability `resolveEvidenceToolCalls`; orchestrator birebir re-export — davranış değişimi sıfır, r21/r22 yeşil kaldı); kernel workflow executor AYNI anayasayı giydi: derlenmiş tool yüzeyi mount (`tools:[]` bu yolda da öldü), A10 kanıt zarfı + prose-fallback, tool-use tap; agent step hook bağlaması A9: preTask run DOĞMADAN (REJECT → `HOOK_REJECTED` policy StepError + 0 agent_runs satırı, §6 birebir), post-gate + §19 REVISE döngüsü runScope içinde (feedback `AgentWork.feedback`), settle+resolver çapası; workflow-yanı HookCtx gerçek satırlardan (workflows.project_id → std 11, budget_state hard-stop, agents profil/manager); e9 fixture'ları A13 pinHookOff (kayıtlı mekanizma); **`tests/r23` 4/4** — tek-implementasyon referans-eşitliği · halal pre-gate workflow yolunda RED (kumar objective → failed, run hiç doğmadı) · kanıt zinciri recordToolCall→settle→resolver→postTask PASS→succeeded (gerçek tool_calls satırı DB'de) · REVISE feedback ikinci denemeye bindi; tam regresyon **54 dosya 379/0**; in-pass ölçülen sızıntı düzeltmesi: aktivasyon bekçisi aktif ajanda persona_id=NULL reddediyor → suite temizliği archive-first (7 yetim fixture süpürüldü, §21 sorgusu yeniden 0) |
| R2.4 | **İlk sandbox outbox handler (F-06):** bir staging provider uçtan uca (onay→tek koşum→idempotent tekrar→audit ref) — para/gerçek müşteri YOK | denetim F-06 7-adım staging kanıtı | F/O | ✓ 2026-07-17 (Fable in person, K1): `email.send.staging` handler (registry 2. giriş; Phase-11 LOCKED kuralı DOKUNULMADI — staging adla+hedefle sınırlı) + Mailpit sandbox container (`dxb_mailpit_staging`, D1 free-first, dış ağ yok, sıfır bağımlılık — HTTP send API, nodemailer kurulmadı); provider-yanı idempotency: outbox idempotency_key mesaja Mailpit tag'i olarak biner, yeniden-ateşlenen handler tag'i bulur ve GÖNDERMEDEN mevcut message_id'yi döner (`replayed:true`); credential yüzeyi: `DXB_STAGING_MAIL_URL` yalnız executor env'i, gateway profillerinde mail yeteneği YOK (test adım 7 grep'i); **`tests/r24` 1/1 — F-06 7 adım tek koşuda**: draft (MCP agent-yolu) → `fn_classify_operation` gated (auto DEĞİL) → onaysız tick outbox satırı bile yok → CEO decide (control fn) → tek koşum (Mailpit tag-count 1 + execution_result.message_id) → replay'de dış işlem TEKRARLANMADI (count hâlâ 1, replayed:true, aynı id) → `outbox.executed` audit satırı task zincirinde → profil grep temiz; **CANLI zincir**: gerçek CEO tarayıcı onayı (`/api/control/approvals` decide, outbox 033e6bd9 trigger-doğumlu) → resident scheduler'ın KENDİ 15s tick'i (manuel runOnce YOK) → gerçek staging mesajı `21sJbg8ONATur2PzbcVLLX` + provider ref execution_result'ta; tam regresyon 55 dosya 380/0; in-pass: `sweepByDepartment` helper'ına approval-decision decision_log FK bacağı eklendi (ölçülen sweep kırığı) |
| R2.5 | **Fresh-bootstrap parity (F-08; E13.0'a ek şart):** boş DB tek komut zinciriyle kurulur; repo 73 ↔ canlı ledger 14 uyumsuzluğu kapanır | boş DB kurulum raporu + şema/fn/RLS envanter diff = 0 | F/O | **✓ COMPLETE 2026-07-17** (Fable in person, K1; continues the ◐ of the same date): **(1) empty DB → one command → FULL chain**: `scripts/bootstrap-db.sh` on a pristine supabase-postgres:17.6.1.140 container — `applied 87, skipped 0, ledger total 87` (file set 87 = 82 + 5 gap/parity migrations); platform/app plane split shipped (`DXB_PSQL_ADMIN` runs ONLY the preamble; on bare PG it collapses to `DXB_PSQL` — one-command contract intact; app chain runs as `postgres` for live ownership/definer parity). **(2) gap-migration series closed the E5-era runtime-data debt**: `20260711006900_agent_org_parity_backfill` (192 runtime-born agents + manager chains + 21 director binds, generated from measured live−scratch diff; activation-gate triggers windowed pg_dump-style, re-enabled in-file), `20260711007900`+`20260717060000` (legal-de pod birth/retire replay — transient on live, net zero), `20260712007950` (pg-boss `lease-reaper` template queue the hr/r13 clone inserts need), `20260717070000_grant_hardening_parity` (**the runtime grant hardening was NEVER in the chain — fresh deploys were born INSECURE with full anon DML; now recorded**: 279 per-(relation×role) REVOKE/GRANT pairs + column-level intents wall [tests/phase8 caught the wipe live, restored same session] + 3 hardened ALTER DEFAULT PRIVILEGES). **(3) inventory diff = 0**: new `scripts/schema-inventory-diff.sh` (columns/functions/views/triggers/RLS/policies/grants incl. column-ACLs+sequences+defacl/indexes/constraints; 2137 lines each side) — live vs fresh-bootstrap **DIFF 0**. **(4) smoke on the bootstrapped DB**: r21+r12+r13+phase3 = 36/36. **(5) restore drill (E13.0 leg, combined as recorded)**: real `pg_dump` of live (5.3MB, 3 DXB schemas) → pristine container → new canonical `scripts/restore-db.sh` (admin-plane preamble [pgvector: without it memory_embeddings + 6 dependents silently die — measured], hardened-defacl pre-block [pg_dump omits ACLs equal to dump-time defaults — measured 551-line drift killed], whitelisted platform collisions only, ACL normalization replay, decisive counts) → ledger 87 / agents 220 / RTO 5s → **live-vs-restored inventory DIFF 0** + smoke 36/36. **(6) live safety**: all 5 new migrations applied on live individually — net zero measured (agents 220/orphans 0/directors 21, lease-reaper counters preserved, anon DML still 0); ledger 87 = files 87 deterministic. Full regression 55 files **380/0** post-apply. Open balance moved to E13.0 row: off-site (Storage Box) backup automation + scheduled dumps.  <!-- HISTORY --> |
| R3.1 | **Voice v1 free hattı (D1+D3):** Speaches STT → kernel intent → orkestratör → voicebox klon TTS (Hamza sesi); müdüre soru = orkestratör aracılığıyla | uçtan uca sesli soru→sesli cevap kanıtı, maliyet €0 | F/O | **✓ COMPLETE 2026-07-17** (Fable in person, K1): **(1) DB:** migrations 20260717080000/080100 (`voice_identities` + `voice_calls`, control seam `control_voice_identity_upsert/retire` + `control_voice_call_log` — V1 law DB-enforced: specialist REJECTED/director accepted measured; ledger 89=files 89). **(2) `@dxb/voice` two-halves architecture** (spec §6 implementation note recorded): SDK-free intake half (busy law → Speaches STT → intent lineage V5 → parked `routing` row) importable by the dashboard (`./intake` subpath; PHASE-08 LOCKED eslint clean); answer half (classify routing → persona head + memory recall → SDK answer → registered-voice TTS → WAV handoff `var/voice/`) on the resident scheduler's NEW 5s `voice.drain` self-chain (E9.1 A1 idiom, no new daemon). **(3) Dashboard:** `/voice` page (Command group nav + CC-SPEC §7 table synced incl. previously-unrecorded U8/R1.4/pnl rows), push-to-talk island, `voice` Broadcast channel + 5s poll fallback, playback via `/api/voice/call/[id]/audio` (UUID-gated, synthetic WAVs only — V9: question audio never persisted). **(4) PRODUCTION-CHAIN PROOF** (no test seams): spoken TR question WAV → intake (STT 17.8s) → live scheduler drain answered as `head-of-strategy` director (answer 20.1s, TTS 6.1s, wall 73.6s) → `voice_calls` row ended, `cost_eur=0` (D1 proof), full timeline; Broadcast `call.started/answer_ready/ended` measured in `realtime.messages`; second proof: CFO answered from FINANCE persona with real cost figures via memory surfaces (V8). **(5) tests/r31 10/10** (state machine, V1 law live, real Speaches roundtrip, e2e, empty-transcript honest fail, degraded fallback, line-busy §10, worker-path drain+WAV). **(6) RULE #0 pass:** EN+TR × 1280/1366/1920, horizontal overflow 0 all, i18n purity PASS 1800=1800 (in-pass fixes: DB status enums localized, department slugs → display_name_tr, A1 info-free target field killed, m:ss durations); baselines `references/design-bank/voice-*.png`. **(7) Full regression 56 files 390/0** (+10 over the 380 baseline; 2 pre-existing suite-hygiene defects fixed in-wave: spawn-binding crash-residue sweep [freeze skipped afterAll], briefing-view FK-ordered cleanup vs live resident worker). ⚠ UNVERIFIED (human gates): mic-recording UI states need a real microphone+ear (CEO eye/ear test); Hamza CLONED voice = spec §24 step 3 CEO ear-gate OPEN — line runs on Piper fallback identity (`degraded` honestly marked) until the CEO picks a clone; voicebox invoker = follow-up when clone supply starts. Task-side note (measured): voice intents ALSO dispatch through the normal intake (V5 one-path) — CFO question's task went `finance` → done; Hamza question's task classified `ceo` dept → employee has no MCP profile → honest 5-round ladder block (pre-existing gap, typed path identical, not voice-scoped). |
| R3.2 | **JARVIS always-on wake layer (spec §24bis; CEO command 2026-07-17):** "Selamaleykum ya Hamza" → hat açılır, soru→sesli cevap, "kapanabilirsin/gidebilirsin" → kapanır; + ölçülen iki hat kusuru (104s answer leg, dil yasası) | daemon canlı + wake/dismiss/lang unit battery + production probe `ended` | F/O | **◐ 2026-07-17 — REOPENED same night (CEO session ~21:30, RULE #0-A measurement): LIVE-DEFECT LEDGER D1–D8 OPEN** — the CEO's spoken "Selamaleykum ya Hamza" went UNANSWERED (STT garbled it to EN garbage, 19:22), one utterance transcribed as Korean, 8/14 calls failed (line_busy + empty_transcript, dashboard-leg AND daemon-leg eras), STT 80–444s waits, retry-same-tier L1 ladder BLOCKED 10 tasks in one day; remediation DEFERRED by CEO ruling → **[[00-NOTE-R32-VOICE-REMEDIATION-PLAN]] + INDEX U15** (mandatory read on reopen). The ✓ previously on this row was premature (Evidence-Before-Done violation, recorded); the inventory below stands as SHIPPED-CODE record, not a done-claim (Fable in person, K1; laptop-freeze recovery — the frozen session's uncommitted work VERIFIED line-by-line then completed): **(1) `jarvis-daemon.ts`** resident wake daemon on the CEO terminal (arecord 16k capture → RMS VAD → short-segment STT → fuzzy match → questions through `intakeVoiceCall` V5 one-path → answer WAV playback off `voice_calls`; pre-synthesized spoken cues ack/bye/busy/lost/err); `wake.ts` token-CLASS fuzzy matchers (salam-class ∧ hamza-class, edit-distance ≤1 — at ≤2 "hava" false-wakes, measured); systemd user units `scripts/systemd/` + `install.sh` (in-shell dotenv sourcing — EnvironmentFile can't read quoted syntax), `pnpm jarvis`. **(2) Voice fast lane:** migration `20260717090000` + routing-seed parity rows `voice.classify`/`voice.answer` (L4 subscription; KERN-02 pure data; fallback `orchestration`); `classify(opts.taskClass)`; SDK NOT-1 lesson applied to answer leg (`maxTurns: 4` — maxTurns:1 produced `error_max_turns`, probe call 30ddba44). **(3) §27 utterance-language law:** `lang.ts detectLang` (transcript decides, UI locale = tie-break only; forcing locale made Whisper transcribe TR speech as fluent EN — measured call f05cf286); TTS voice follows ANSWER language (EN default `piper-en_US-lessac-medium` added; TR-clone-reads-EN unintelligibility killed); intake runs STT auto-detect. **(4) MEASURED IN-WAVE DEFECT KILLED — thermal noise loop:** fixed RMS gate vs fan at max RPM = 16 ambient STT calls/5min (Whisper lang 'nn' p=0.19 pure noise), each call heating CPU → louder fan → more calls (closed loop, cores 96-100°C vs 87°C threshold); fix = adaptive ambient EMA gate v2 (asymmetric rise τ≈60s / fall τ≈3s, gate=max(base, ambient×2.2), segment must PEAK ≥gate×1.4, sleeping-mode queue coalescing + 5s STT min-gap backstop) — post-fix measured **2 calls/5min (8× reduction)**; wake STT model unified to intake's small model (second resident whisper thrashed 8GB — raw STT 17.8s→50.9s measured). **(5) Evidence:** `tests/r32` **36/36 — re-measured 2026-07-17 22:18** (wake manglings, dismiss stems, lang detection incl. live-defect regression, WAV header contract, ambient-gate unit rows); ledger 90=files 90; scheduler+jarvis units active; production probe → `ended`, answer_ms 39.2s on thermally-critical X230 (honest floor: §24bis records 3-6s wake / 30-60s answer nominal, <1s lane = openWakeWord on GPU/VPS). Governance: spec §24bis + §27 adaptation + §0/§5 asset-map updates; U4 refined (deferred remainder = Moderated Boardroom ONLY). ⚠ human gates: spoken wake→answer at the CEO's ear (real mic+speakers); Hamza clone ear-gate unchanged from R3.1.  <!-- OPEN: B03 --> |
| R4.1 | **Library araştırma pası (D7+D8):** masaüstü-25 listesi + Claude ekosistem taraması → study-card + STUDY→INSTALL yaşam döngüsü (Scrapling/Camoufox/deerflow öncelikli) | kart envanteri + intake raporu; kurulumsuz-blind = 0 | F/O | **✓ COMPLETE 2026-07-17** (Fable in person, K1): **(1) D8 masaüstü-25 fully mapped** (`.planning/research/R4.1-LIBRARY-INTAKE-REPORT.md` §1 — extracted from `repo vb şeyler.odt` this session): 16 new study cards + 4 refreshed/filled (scrapling/humanizer/claudex/openmontage-opencut) + 6 org questions routed WITH roster measurements + 1 duplicate (Camoufox ×2 → one card) + 1 illegible line flagged as an open CEO question — zero silent skips. **(2) Priority trio deep-studied:** Scrapling v0.4.10 measured (`pip show`) + upstream built-in MCP server recorded as the TS-bridge path + MEASURED defect: venv lacks `camoufox` pkg → StealthyFetcher unusable until the ADOPT-step fix; Camoufox v152.0.4-beta.27 deep card (consumed via Scrapling; beta-grade pin+fallback rules); DeerFlow 2.0 deep card (77.3k★ — department TOOL only, LangGraph orchestration ban holds; 8GB-RAM VPS gate; LiteLLM virtual key mandatory). **(3) D7 ecosystem sweep:** registry ≈101 plugins reviewed vs 18 in-house (top-installed frontend-design/superpowers/context7 already held — measured plugin cache); new intake: SkillSpector (NVIDIA) → **REGISTERED STANDING GATE**: third-party skill/plugin INSTALL = static scan + archived report + D6 adaptation (ecosystem threat datum: 26.1% vulnerable / 5.2% malicious); official telegram plugin; codebase-memory-mcp (graphify-overlap measurement gates ADOPT); gbrain (ref-only — MEMORY_ARCHITECTURE collision). **(4) STUDY→INSTALL lifecycle:** INTEG-01 four-state checklist on every card; INTEGRATION-TRACKER +16 rows, 4 note updates. **(5) Intake run:** E9.5 spec-gap FOUND+FIXED (bootstrap rule 5 — `discoverTraining` missed `.planning/research/study-cards/`, G4 violation): two-dir scan + in-run kind+name dedupe; `--apply` exit 0, every write via `control_library_action` (audit_log +80 measured); DB training 1→75, library total 357→438 (+74 cards, +report, +3 packages @dxb/hook+revenue+voice, +1 governance directive, +2 corpus docs). **(6) kurulumsuz-blind = 0:** nothing installed this pass (dual-role principle — installs happen at the consuming phase start); installed-asset closure measured (skill 23/23, plugin 18/18, tool 21/21 discovered=registered); future lane closed by the SkillSpector gate. **(7) In-pass incident (CEO "fix it the moment you see it" rule):** skill-diet regression — a `gsd-update` had silently restored 50 archived gsd skills (detected via the dry-run's +50-new-skills anomaly); re-archived to the measured E9.5 23-skill ledger set, post-fix dry-run skill 23/0-new. ⚠ Open CEO items (report tail): .odt item 24 illegible; operations dept?; education dept? |
| R4.2 | **Library zenginleştirme dalgası (Fable gap-analizi → CEO forward 2026-07-17, .odt item 24 görüntüleri):** üç eksen — **derinlik:** mevcut 438 kaydın metadata işlenişi (usage_notes/owner_dept/quality_score doldurma) + review pass + çekirdek grant seti (ölçülen taban: grants 0, review 0, owner 15/438 — "0/N granted" tablosu kapanır: her aktif departmana rol-standart grant paketi, E9.5 yetki→profil zinciri zaten canlı); **genişlik:** departman ihtiyaç matrisi — personaların skill/MCP grant beklentileri ↔ kütüphane gerçeği, eksik = intake listesi (öncelik: satış/CRM playbook, Outleteuro/WooCommerce araç seti [Faz 11 hazırlığı], sosyal medya platform SOP'ları, finans mutabakat şablonları, güvenlik runbook'ları); **bilgi rafı kuralı:** "raporsuz araştırma yok, kayıtsız rapor yok" — her research task çıktısı `control_library_action` kapısından kind=research satırı olarak düşer (kanca + kanıt) | grants>0 + review>0 ölçümleri, gap-matris raporu, bilgi-rafı kancası kanıtı | F/O | **✓ COMPLETE 2026-07-17→18 night** (Fable in person, K1): **(1) DEPTH closed by measurement** — `scripts/library/enrich.mjs --apply` (intake idiom: control-fn writes, CEO context, md5 idempotency) **606 ok / 0 failed**: grants **0→168 live** (21 departments × 8 dxb-mcp groups — IDENTITY MIRROR of the enforced surface; recompile diff 21/21 dept `_tools` byte-identical, ZERO behavior change, the record now governs G3), review **0→423 real** (418 approved / 5 needs_review [source-less memory_source rows — honest flags] / 15 archived preserved), quality_score **0→438** (deterministic formula, avg 86.9), owner_dept **208→438** (custody map). change_log 466→872, audit `library.*` 438→1044. **(2) BREADTH:** `.planning/research/R4.2-LIBRARY-GAP-MATRIX.md` — department need-matrix (21 rows: roster+custody+gaps) + prioritized intake list G-1..G-7 (all 5 roadmap seed gaps CONFIRMED by measurement: sop holds only 2 build-process rows; prompt_template/workflow/framework/best_practice/lesson_learned kinds EMPTY); the report registered itself through the control fn as kind=research (item a395fa1d — dogfood). **(3) KNOWLEDGE-SHELF HOOK LIVE:** migration `20260717100000` policy `std.knowledge_shelf` (filed under std 16 — halal-screen precedent; ledger 91 = files 91) + post-gate `library_registration` check + `knowledge-shelf.ts` A8 CEO-standing-order registrar (HOLDING_LIBRARY adaptation A9); `tests/r42` **6/6** (silent pass, no-report REVISE, auto-register+change_log, idempotent re-fire, TR suffixed 'araştırması' matches); dist rebuilt + resident scheduler restarted 23:00:42 — old-dist unknown-check window measured at 0 violations. **(4) FULL REGRESSION 58 files 432/0** (+15 pre-existing skips); 3 PRE-EXISTING live-data test fragilities found+repaired (e9 zero-grant-world assumptions ×2 → park/restore pattern + state-independent A2 assertion; phase9 briefing LIMIT-5 edge witness → fresh-witness seed; e11 cost Map-overwrite + seed-universe mismatch → accumulating Map + same-universe raw). **(5) RULE #0 pass (surface: /ai/library + kind-board routes):** newly-live review/quality data rendered RAW English on the TR locale → `reviewStatuses` dict EN+TR (purity 1812→1818=1818 PASS) + page bindings; A1 ellipsis hunt: Version column removed from list (406/439 empty — info-free-field ruling), Access/Usage counters moved list→detail (zero-noise), name/label wrapping (`break-words`), owner column uncapped (longest 43ch measured) — final battery TR/EN × 1280/1920: page overflow OK, inner FITS, **clipped 0**; baselines `references/design-bank/library-r42-{tr,en}-{1280,1920}.png`; TWO GHOST next-servers killed (an old build was still serving — restart discipline). F-11 remaining half CLOSED (grants>0 + review>0 + need-matrix). Registered follow-ups: G-7 dept-minimal grant derivation from usage actuals (CEO-visible policy decision), G-6 memory_source canonical refs, empty kinds fill through real work (the shelf hook enforces it). |
| R4.3 | **Capability Arsenal Expansion (CEO order 2026-07-18 00:20 — "8 MCP az değil mi; önemli alet edevatın HEPSİ sisteme nakşedilsin"; audit F-07 companion):** measured defect: runtime tool surface = ONE server (dxb-mcp, 21 internal tools; crm group = 1 tool); `grants.json` declares 9 servers but 8 are GHOSTS (no catalog/pins: stitch, github, git, context7, stripe, revolut, wise, docusign); 75 study cards exist but ZERO external hands installed. Deliverables: **(a) Arsenal doctrine** — per-department target MCP/tool surface (dept need-matrix R4.2 + study cards + F-07 list as sources; free-first D1 filter; least-privilege classes); **(b) FREE tranche installed + pinned + granted** (credential-less, €0: playwright-class browser automation, git local, scrapling bridge [ADOPT-step camoufox fix], context7-class docs lane) with live tool_calls proof per install; **(c) credential tranche NAMED-GATED** (github PAT etc. — CEO hands the secret, vault-only); **(c2) PAID tools = PROPOSAL BENCH (CEO ruling 2026-07-18 00:30, verbatim intent: "ücretliler de olabilir ama yedekte dursunlar önerilmek için"):** best paid options are studied + registered as bench rows with price/trigger, NEVER installed or spent — they surface as PROPOSALS when profit funds them (D1 free-first law); the FREE tranche is installed "en mükemmel şekilde" — quality bar explicit; **(d) money-touching tranche stays Phase-11 LOCKED** (stripe/revolut/wise/woo write-paths → R6.1); **(e) dxb-mcp internal growth list** (crm beyond 1 tool; per-dept internal tool needs from the doctrine); **(f) every install = library rows + tool_pins (schema-hash) + grant chain (E9.5) + SkillSpector gate for third-party code** | doctrine doc in corpus; free-tranche servers alive in catalog+pins with ≥1 real tool_call each; ghost-server count 8→named dispositions; grants live for new surfaces; kurulumsuz-blind = 0 | F/O | **✓ COMPLETE 2026-07-18 ~01:45.** Evidence: (a) `CAPABILITY_ARSENAL_DOCTRINE.md` in corpus + 00-INDEX row 34 (principles, classes, per-dept targets, ghost dispositions, credential tranche, paid bench, dxb-mcp growth list, placement). (b) FREE tranche LIVE — pin corpus 21→**69 tools / 5 servers** (`pin-arsenal.mjs`: git 12, context7 2, playwright 24, scrapling 10; pnpm lockfile supply-chain check passed); library items mcp/git@2026.7.10, context7@3.2.4, playwright@0.0.78, scrapling@0.4.10 + 5 dept grants via control fn (9/9 ok); profiles recompiled by live scheduler: engineering {git 7 READ, context7 2, playwright 22}, quality {playwright 22}, strategy {scrapling 10}; TIGHTENING: browser_run_code_unsafe+browser_file_upload denied everywhere, git write tools K1-denied (denials.json note). LIVE tools/call proof 4/4 (`probe-arsenal.mjs`: git_status→'On branch master', resolve-library-id→/reactjs/react.dev, browser_navigate→Example Domain, scrapling get→200) + runtime-chain proof 5/5 (`probe-runtime-chain.mjs`: resolveRuntimeProfile→buildSdkToolOptions→real call per dept + negative control unsafe-tool stripped). BOUNDARY (measured, not laziness): end-to-end STAFFED worker task with external hands is impossible today — the only active+profiled employee holding-wide is finance (doctrine gives finance no external hands); task 4a62f8a9 left as the honest no-workforce signal (pre-gate std.permission_bounds → ladder blocked, F-01 chain working); first activation wave (Phase-10) owes that exercise. **DEBT PAID 2026-07-18 ~18:15 mid-E12.5-wave (Fable in person): staffed external-hand E2E CLOSED** — task 61c73f8d (priority-queued rerun of the same arsenal objective; the pre-gate first HONESTLY rejected it `missing_project_link` = std.project_alignment live, fixed with a real project link to DXB Global OS) claimed by the resident worker on its own tick, executed by ACTIVE employee engineering-ai-data-remediation-engineer (run 2e489baf, opus-4-8): REAL external calls tool_calls 4029 `mcp__git__git_status` + 4030 `mcp__context7__resolve-library-id` + 4031 `mcp__git__git_branch`, evidence.toolCallId=4029 exact, result honest (branch master + /colinhacks/zod, both match ground truth), trail claimed→running→review→done with actors only resident-worker + orchestrator:qa. (c) credential tranche NAMED (github PAT, stitch — doctrine §7); (c2) paid bench 6 rows with price/trigger (doctrine §8); (d) money tranche locked → R6.1; (e) crm growth list (doctrine §9); (f) SkillSpector gate run + archived (.planning/research/skillspector/; scrapling 28 findings triaged — 3 decisive = FALSE POSITIVE at source; npm packages parse 0 components = measured scanner limit) + study cards advanced (5 cards + NEW mcp-server-git card closing its blind-install gap; tracker rows→ADOPT). SIDE-FIXES (spec-gap = immediate): pin-check NUL-byte separator normalized; **tool_missing audit spam root-caused** — tests/phase7 fixture inventories swept ALL 21 real dxb-mcp pins per run (147 runs × 21 = 3087 audit rows since 07-14) → `checkPins` serversInScope param (unreachable-server guard for the external corpus too), tests scoped, scheduler passes reachable set; e9 park/backstop rework (snapshot-restore, arsenal-proof); r23 orphan-agent sweep (crashed-run fixture broke e10 acceptance); e10 proof excludes suite fixtures (interleave race). Battery: tsc exit 0 · vitest **59 files, 437 passed / 15 skipped / 0 failed** · i18n purity PASS 1818=1818 · tests/r43 5/5 · RULE #0 spot pass /ai/library?kind=mcp EN+TR × 1280/1920 page overflow 0 / clipped 0 / no raw-enum leak, baselines `references/design-bank/library-r43-mcp-*.png` (⚠ CEO eye pending). Bonus live proof: std.knowledge_shelf REVISEd a real production task at 23:30 (no research without report). |
| R5.1 | **Kelam M0-M1 (D9, OD-1 slot):** repo bootstrap + headless çekirdek döngü (`docs/kelam/ARCHITECTURE.md` uygulanır) | kelam CI yeşil + CLI record→Speaches→cleanup→clipboard TR/EN/DE | F/O | **✓ COMPLETE 2026-07-17** (Fable in person, K1): **(1) M0** — standalone repo `/home/ghost/kelam` (architecture §0 rule: product, NOT a monorepo module; MIT = OD-3 adopted; commit ce4a47e, 41 files): Rust 1.96/edition-2024 workspace, 11 crates matching the §3 crate map (4 as recorded milestone stubs), justfile (§18 command set), CI workflow (fmt+clippy -D+test), 9-doc set (system-architecture, technology-decision incl. RECORDED M1 deviation [capture via `arecord` subprocess — build machine has no libasound2-dev/sudo lane, measured; in-process PipeWire = M2], threat-model, platform-research, testing-strategy, manual-test-plan, linux-installation, failure-matrix, support-matrix [measured-only rows]). **(2) M1 headless core** — kelam-stt OpenAI-compatible provider on local Speaches (§13 HTTPS-only-for-non-loopback ENFORCED in code + unit test); kelam-refine deterministic rules engine (TR/EN/DE filler lexicons, repetition collapse, Turkish İ capitalization, vocabulary; 11 fixture tests); kelam-audio (RMS silence gate, rubato resample, WAV IO); kelam-core EXPLICIT §5 state machine (legal+illegal transitions unit-tested) + pipeline; kelam-deliver clipboard; `kelam` CLI (transcribe/record/doctor). **(3) CI battery GREEN (local run = CI steps):** `cargo fmt --check` clean, `clippy -D warnings` clean, **19 unit tests 0 fail**. **(4) LIVE CHAIN PROOF ×3 languages** (Speaches :8969, faster-whisper-small): piper-synthesized filler-laden fixtures → chains `Idle→Recording→Transcribing→Refining→Delivering→Completed` with CLIPBOARD READBACK evidence (TR: "ııı yani…hani" cleaned to "Bu rapor yarın hazır olacak."; EN "you know" removed; DE chain green — "eh" kept deliberately: real German word, meaning-preservation rule). EN/DE piper voices pulled into Speaches for the fixtures (en_US-lessac-medium, de_DE-thorsten-medium). **(5) ACOUSTIC ROUNDTRIP:** speakers→air→internal mic→arecord→STT→rules→clipboard Completed (real-mic record leg machine-verified); silent-room record → honest EmptyAudio (no fake text). **(6) MEASURED DEFECT FOUND+FIXED in-build:** X11 clipboard ownership died with the short-lived CLI (arboard; readback EMPTY) → delivery via persistent-ownership `xclip`/`wl-copy` subprocess, readback now carries the text — recorded in kelam-deliver header + technology-decision. ⚠ UNVERIFIED (human gates): spoken-voice mic dictation quality (CEO speaks a real sentence — machine cannot); GitHub-hosted CI run (no remote yet — workflow file ships, local equivalent green). NEXT Kelam milestones M2-M6 = future roadmap rows (OD-1: next slots after open E-rows). |
| R6.1 | **Integrations canonical open item (audit F-07, registered 2026-07-17):** social/payment/commerce REAL provider integrations — Phase-11-gated (LOCKED real-provider rule stands, R2.4 staging pattern is the template); at Phase 11 kickoff EACH provider enters as its own execution row. **RE-GATED 2026-07-19 by U19: Phase 11 (Outleteuro) cancelled entirely by CEO order — this row now opens only on a future explicit CEO order; it stays visible as debt, no per-provider rows are born** | per-provider execution rows exist when the CEO reopens real-business operation; until then this row = the visible debt (audit demanded it never be an undocumented "later") | F/O | — |
| R6.2 | **University standalone product layer (audit F-12, registered 2026-07-17; OD-1 slot, Kelam pattern):** independent product repo — architecture doc FIRST (docs/kelam precedent), build slot queues after Kelam M2+; NOT the internal training shelf (that is Library R4.1 ✓ / R4.2 + U14 ruling) | architecture doc exists before any build row opens | F/O | — |

## 5. Devralma noktaları (devralan-yazar protokolü — [[BACKUP_PLAN]] §13 tamamlayıcısı)

> **U20 (CEO 2026-07-25):** devir gerçekleşti — devralan yazar **Opus 5**'tir; yedek model katmanı YOKTUR.

- **Kural:** devralan yazar, tablodaki ilk `—` adımdan başlar; yarım adım varsa önce kanıt komutunu koşar — geçiyorsa ✓ işler, geçmiyorsa adımı baştan alır.
- **Devredilemez:** E5.2-E5.5 persona yazımı (yetişmeyen "yazım bekliyor" listesine), tüm ⛔ kararlar, CEO göz testi.
- **Paralellik (yalnız devralma penceresinde):** E8 ↔ E9 ↔ E11 blokları bağımsız; E12 hepsinden sonra. Aynı dosyaya iki koldan dokunma yasak.
- **⛔ envanteri (spec'lerden):** agents rename (DATA_MODEL) · kontrol düzlemi servisleştirme (SYS_ARCH §3) · API zarf değişimi (API_CONTRACTS) · EVENT zarf/kanal sınıfı (EVENT_MODEL) · workflow step kind ekleme (WORKFLOW) · KIND_STORE değişimi (MEMORY) · grant yaptırım noktası (LIBRARY) · health formül ağırlıkları (PROJECT_OS) · omurga gevşetme/saklama kısaltma (SECURITY, AUDIT) → hepsi: eldeki en güçlü model + CEO onayı.

## 6-11. Veri modeli / Component / Backend / Frontend / API / Event

Bu dosya sıralama katmanıdır; teknik normlar ilgili spec'lerde (her adımda referanslı). Yeni norm getirmez — çelişkide spec kazanır, sıra burada kazanır.

## 12-19. İlişkiler / Yetki / Logging / Audit / Security / Error / Retry / Fallback

Yürütme sırasında geçerli çapraz kurallar: evidence-before-done (her adım) · gitleaks her commit · dalga/blok = atomik commit · Broadcast yasağı yok ama postgres_changes yasak · hata durumunda adım kapatılmaz, kanıt komutu geçene kadar açık kalır · inşaat yazarlığında subagent yasağı sürer (K1) · **canlı `next start` altında `.next` yeniden build edilirse sunucu MUTLAKA yeniden başlatılır** (2026-07-10 dersi: eski süreç yeni asset'leri bulamaz → çıplak HTML; kanıt zinciri: build → restart → `curl <css-chunk>` 200).

## 20. Test planı / 21. Acceptance criteria

Adım-içi kanıt komutları = birinci savunma hattı; E13 tam tur = kapanış. Ürün kabulü [[ACCEPTANCE_CRITERIA]] normatif.

## 22. Migration planı / 23. Rollback planı

Migration sırası E4 tablosunda (DATA_MODEL §20 uyumlu). Rollback: blok commit'i revert + migration `-- ROLLBACK:` blokları — tam prosedür [[RECOVERY_AND_ROLLBACK_PLAN]].

## 24. Uygulama sırası (özet zinciri)

E1→E2→E3→E4→(E5 Fable-only paralel)→E6→E7→E8→E9→E10→E11→E12→E13. İlerleme işareti bu dosyada; her ✓ commit mesajında adım ID'si taşır (`feat(E4.1): ...`).

## 25. Bağımlılıklar / 26. Riskler / 27. Edge case'ler

- Bağımlılık: korpus 31/31 (bu dosya dahil) commit'li; design bundle canlı; STACK.md sürüm tablosu (yeni paket eklemeden önce okunur — sert kural).
- Risk özeti [[RISK_REGISTER]]; bu dosyaya özgü: adım tablosu güncellenmeden iş yapmak = ilerleme kaybı (kural: iş biter → tablo işlenir → commit; ikisi aynı commit'te).
- Edge: 12'si gecesi yarım adım → kanıt komutu geçmeyen adım ✓ İŞLENMEZ, Opus baştan alır; iki session çakışması (CEO iki terminal) → tek yazar kuralı, ikinci session salt-okunur devam.

## Done definition (bu spec)

27 başlık ✓ · 13 blok × adım tablosu (dosya+kanıt+model+durum kolonlu) ✓ · CEO Faz eşlemesi blok başlıklarında ✓ · Fable-only + ⛔ envanteri + devralma kuralları ✓ · paralellik haritası (Opus) ✓ · canlı-ilerleme sözleşmesi (✓ bu dosyaya işlenir) ✓
