# CEO Complaint Ledger — 2026-07-19 (C-series)

> **THE SEQUENCE CONTINUES ON THE BOARD (2026-07-27, U38).** C1-C25 closed here. The CEO's next
> complaints — spoken on the night of 2026-07-27 while looking at the live dashboard — are
> **C26-C42 on [[00-BOARD-OPEN-WORK]]**, section 2, each with the measurement taken that night.
> They were numbered forward on purpose: the complaint history is ONE sequence, never a new
> ledger per outburst.

**Source:** `~/Desktop/şikayet.odt` (CEO, delivered 2026-07-19 ~00:45) — 19 numbered complaints with 26 screenshots.
**Screenshots preserved:** `references/ceo-complaints-2026-07-19/` (26 PNGs extracted from the .odt).
**Status:** OPEN — remediation queued after E13.1 closure (CEO ruling in-chat 2026-07-19: "önce bir projeyi bitirelim").
**ATOMIC DECOMPOSITION (2026-07-19 ~02:10, after CEO's "yüzeysel" correction):** every sentence of the .odt was re-decomposed into **68 atomic items** (sub-IDs 1a…19f + 20–25), each tagged KUSUR/EMİR/SORU with measured finding and fix. Canonical detailed document (CEO-ordered exception to the English rule — CEO-readability artifact in Turkish): `references/ceo-complaints-2026-07-19/SIKAYET-DEFTERI-DETAYLI.html` (+ .odt/.pdf, 18 pages; copy delivered to CEO Desktop). Six system-wide rules extracted (hover help on EVERY heading EN+TR; smart filters dept/model/date/project driving the WHOLE page; select+bulk delete on every list; no code-language on first read; no info-free fields; no test fixtures on CEO surfaces — no haram-domain wording even in fixtures). The tables below remain the English governance mirror; on conflict the atomic document wins for scope, this ledger wins for governance linkage.
**Discipline:** every item closes only with RULE #0 design-verification evidence + Evidence-Before-Done. Audit-ledger invariant applies: these IDs (C1–C19) must never live only in memory.

Classification legend: **DEFECT** (broken behavior) · **DECISION** (CEO order changing scope/design — registered, binding) · **QUESTION** (CEO demands a measured answer — answering is part of closure).

| ID | Summary (from the CEO's text) | Class | Measured lead (this session, read-only) |
|----|------|-------|-----------------------------------------|
| C1 | Intent panel cannot converse. "Yahu hamza orda mısın" → "dağıtım başarısız". CEO wants to TALK first — planning, idea exchange with the top authority (orchestrator) — before any dispatch. "Is this panel only for 'make me a YouTube page'?" | DEFECT + DECISION | Measured: `/api/intent` (`app/api/intent/route.ts:30-34`) inserts raw text as an intent; no conversation path exists anywhere in the dashboard (PHASE-08 LOCKED pushed LLM surfaces out of the projection client). Greeting text becomes a task chain and fails. |
| C2 | Ladder: why was SONNET called via OpenRouter? CEO ordered OpenRouter ONLY for the Chinese (low-cost) models. Also: embedding/small entries ("text embedding", minimax etc.) may stay only if free. Kimi: kimi 3 is out — replace kimi 2.7; models get work by fitness for the revenue task. "Where do we pick these from?" | DEFECT + DECISION + QUESTION | Measured: `vps/litellm/config.yaml:21-30` declares sonnet-5, opus-4.8, fable-5 as `openrouter/anthropic/...` — Anthropic models must not ride OpenRouter (subscription path exists: worker-shim `mode='subscription'` rides Agent SDK local auth, `worker-shim.ts:213`). `embed-small` = `openrouter/openai/text-embedding-3-small`, PAID (~$0.02/M) — not free; needs CEO keep/kill or local replacement decision. |
| C3 | Clicking does nothing ("basınca hiç biri çalışmıyor"): items show pointer cursor but don't open; CEO wants hover previews + click-to-expand BELOW on the live feed; "Tümünü gör" (view all) button dead. | DEFECT | Lead: intelligence rail `command.rail.viewAll` (`components/command/intelligence-rail.tsx`, `alerts-rail.tsx`); pointer-cursor-without-handler class. Exact dead surfaces to be enumerated in remediation with the screenshots. |
| C4 | Failed tasks pile up forever. Selective delete + bulk delete required on ALL such list pages — "do I have to be the one to say this?" | DEFECT + DECISION | Measured: NO delete/bulk-delete UI or API exists for tasks/alerts/runs (`app/api/control/*` has no delete op; tasks are status-transition only). New control surface required (archive/hide semantics vs hard delete to be designed under audit-trail rules). |
| C5 | "Bekleyen onay yok" (no pending approvals) contradicts the tasks screen — tasks vs approvals inconsistency. | DEFECT | Measured lead: 3 tasks sit `awaiting_approval` (marketing 1, finance 2) while `approvals.status='pending'` = 0 — the two surfaces speak different truths; bridge/state sync to be root-caused. |
| C6 | Live Operations chaos: raw statuses (succeeded/done/running/claimed) unexplained; every heading needs a small hover question-mark with EN+TR explanation; no selective/bulk remove; WORST: clicking a row opens an ANCIENT old-design page (Korean text on top, coffee/brown styling). CEO demands root cause + prevention. "Did the project lose its mind / did the architecture collapse?" | DEFECT + QUESTION (root cause) | **Root cause MEASURED:** `components/command/live-feed.tsx:26-27,163-172` — row drill deliberately links to the LEGACY cockpit page `/tasks/[id]` (`app/(cockpit)/tasks/[id]/page.tsx`) "until /ops/tasks reaches parity at E12.1"; parity never arrived, debt stayed silent. The whole `(cockpit)` route group + legacy components are still live and reachable (also from `approvals/[id]/page.tsx:252`). Korean text = task content from the Korean-STT defect era (U15/D-ledger class) rendered on that page. Prevention: kill or wall the legacy route group; sweep for every link into it. |
| C7 | The yellow "kayık" (misaligned) thing — complained repeatedly, ignored. Remove "intent" from inside search — command/intent does not belong in search. CEO CHAT here instead: voice + text, ChatGPT/harness-like, WITH plan mode. | DEFECT + DECISION | Same surface as C1: `command-palette.tsx` (⌘K palette mixes search + intent submit at `:251-272`). Direction: dedicated CEO Chat Board with Hamza (see C10), intent removed from palette. |
| C8 | HR contains "Loan Officer", "Real Estate" etc. — "complained a thousand times"; dormant/unattached nonsense; delete the useless ones. | DEFECT + DECISION | Measured: `personas/_library/` = 15-file dormant reservoir incl. `loan-officer-assistant.md`, `real-estate-buyer-seller.md`; HR cards count dormant/archived into lifecycle chips (`org/hr/page.tsx:166-167`) so they surface to the CEO. DB: 220 agents = 198 active + 21 archived + 1 dormant. Action: CEO-visible surfaces show active org only; useless reservoir personas deleted or moved out of governed surfaces (deletion list to CEO for one confirm). |
| C9 | Every heading: tiny question-mark, explanation on HOVER (not click), EN+TR. Where did this data come from? How is it deleted/reset? Filters are dumb: no department filter, no model filter; calendar must be a real date picker driving the page; big page below must follow the filter. "What absurd architecture is this?" | DEFECT + DECISION + QUESTION | Measured: NO tooltip/help component exists anywhere (grep zero); only native `title=`. Filter gaps and data-provenance answer per page to be produced in remediation (surface = cost/analytics page per screenshot). |
| C10 | Portfolio page overcomplicated (R23T/30001000 noise). CEO's actual model: portfolio prepared → discussed/allied → in "CEO CHAT BOARD" with Hamza. **Orchestrator renamed: he IS Hamza now; Hamza speaks with the CEO directly over JARVIS too.** CEO may inspect/replace the responsible brain (model) of a revenue portfolio from Operations (Projects/Portfolio). | DECISION (major) + DEFECT | Measured: orchestrator slug `agents-orchestrator`, alias already `HAMZA_SLUG` (`packages/voice/src/answer.ts:18`), persona `personas/ceo/agents-orchestrator.md`; voice pipeline already routes to him. Chat Board = new surface (C1/C7 convergence). Portfolio simplification + per-portfolio responsible-model control = remediation design under existing control fns (`control_portfolio_*`). |
| C11 | Remove Tasks from under Operations (CEO doesn't need it). Move Portfolio under Operations — it's a money-making place, not "Intelligence". | DECISION | Measured: nav truth `config/command-nav.ts:110-149` — tasks under Operations; portfolio/CRM/opportunities/objectives under Intelligence (revenue family). Pure IA change. |
| C12 | Models page: Fable's fallback = Codex 5.6 solo and Opus 4.8. "Add model" must be: name + API key (+ optional note) — nothing else; work then flows via orchestrator. Too much useless detail on the page. Missing models: Codex Solo 5.6, Codex 5.5, glm 5.2, kimi 3, deepseek V4 pro — where are they, and their mode selections? | DECISION + QUESTION | Measured: catalog seeded with 4 anthropic rows (`20260712009000_...sql:72-78`); codex-5.5 exists only in `routing-seed.json` (SDK path), glm-5.2/kimi-2.7/deepseek-v4-flash exist in LiteLLM config; kimi 3 / codex 5.6 / deepseek V4 pro absent. Add-model flow exists (`models-table.tsx:193`, §4c chain) but demands more than name+key. |
| C13 | Opportunities, CRM, Objectives — all belong under Operations, not Intelligence. "The exact address of the sloppiness." | DECISION | Same nav file as C11. |
| C14 | Memory page: incomprehensible; what was done to stop these from burning tokens? Every segment needs plain-language explanations inside the main heading. "I am a manager, not technical." | DEFECT + QUESTION | Measured: `app/(command)/ai/memory/page.tsx` is read-only with provenance, but copy is technical. Token-protection answer exists (compression layers, memory write-policy) — must be surfaced in plain EN/TR on the page. |
| C15 | Skills/plugins page: are all of these usable? How? Were they studied/learned? What does the yellow-marked one mean? "These must NEVER be explained by guessing." | QUESTION | Answers must be measured per item (R4.3 catalog: 69 tools/5 servers pinned; skill study records). Yellow marker meaning read from the actual component during remediation. |
| C16 | Approved skills 23 / plugins 18 — where are the items the CEO himself provided, in the Library? | QUESTION | Library = 447 items live. The CEO's own contributions must be located and shown (library provenance query) — measured answer owed. |
| C17 | Outbox/approval detail: raw IDs, action_type, codes — CEO cannot use this. Full-record view = incomprehensible. Everything must be readable by a non-technical chairman; outbox content should concern sales/project/portfolio work in plain words. | DEFECT | Measured: `approvals/[id]/page.tsx:138-226` renders raw `action_type`, `operation_class`, raw payload JSON. Jargon dictionaries exist (`messages/tr.json`,`en.json`) but several surfaces bypass them and leak raw enums (live-feed `:185`, palette chain, memory `:426`). Plain-language projection layer required. |
| C18 | Same disease again: long unfiltered lists, no selection/deletion, irrelevant technical content in approvals — "will approval content always look like this while we write commerce/video-production work?" | DEFECT | Same family as C4/C17 — one systemic fix: list-page standard (filter + select + bulk action + plain language + help tooltips) applied to every CEO-visible list. |
| C19 | Runtime/observability page: "E11 220 EMP", "azami kalp atışı", "ön koşul", "kilitli kapı", 10000/240 numbers — utterly meaningless to the CEO; the whole left side ignored out of despair. ALL pages: filtered, code-free, plain intelligible language (and NOT caveman-style). | DEFECT | Measured: vocabulary exists in dicts (`tr.json:1284` "Azami kalp atışı sessizliği") but is itself jargon; raw enums leak. Systemic plain-language + progressive-disclosure pass (matches registered CEO preference: summary first, detail layered). |

## Screenshot review addendum (2026-07-19 ~02:00 — ALL 26 screenshots read one by one; CEO correction "sadece bunlar değil" honored)

Findings visible in the images but not fully captured by the numbered text. New IDs C20–C25; detail sub-points folded into existing IDs.

| ID | Finding (from screenshot, verbatim where possible) | Class | Evidence image / measured note |
|----|----|----|----|
| C20 | **Test fixtures LEAKED to CEO production UI — including a gambling reference.** Workflows page lists probe workflows `r23t-30019999 halal / pass / revise` with step objective **"r23t-30019999 kumar sitesi reklam"** (gambling-site ad probe) and "one line of probe text" contracts. A gambling-ad string on the CEO's screen is doubly unacceptable (Islamic boundary sensitivity + fixture hygiene). All r23t/probe-class rows must be purged from CEO-visible surfaces; fixture naming policy: no haram-domain wording ever, even in tests | DEFECT (severe) | img 14 (`1000000000000561...C58120DE25`); the 3 live `workflows` rows measured earlier ARE these probes |
| C21 | **Garbage-era intents pinned on screens:** Korean "뭐요? 무슨 말이야?" (failed), garbled "May the do you, Muslim Hamza." (failed) sit in the legacy intent strip; same trash visible on the legacy task page the live feed drills into. Voice-era (U15/D-ledger) junk data persists in `intents` and is displayed | DEFECT | imgs 9, 21; joins C6 legacy-kill + data purge (delete/mark junk intents) |
| C22 | **Stale risks presented as open truth:** Riskler page shows "Workforce activation gap: 219 of 220 employee records dormant" as AÇIK/Yüksek dated 14 Tem — reality measured 2026-07-19: 198 active / 0 probation. Risk register never updated after the activation wave; a CEO reading it is misled | DEFECT | img 24; fix = risk lifecycle refresh + close-out discipline at wave ends |
| C23 | **Model catalog stats broken/info-free:** rows show `–` context, `–` cost, KOŞU 30G = 0 for sonnet-5 (reality: 335 runs on 07-18 alone) and €0 everywhere; "SON ROTALAMA KARARLARI" panel prints raw routing log lines incl. `reason=test: simulated timeout` (test rows). Stats view not fed by agent_runs; test noise on CEO surface | DEFECT | img 15; joins C12 redesign |
| C24 | **Token/cost attribution wrong or misleading:** Tokenlar page: "DEPARTMANA GÖRE · 7 GÜN: engineering 2.1B" as the ONLY department while 21 departments worked; claude-fable-5 2B = construction-session tokens counted as company usage; "KULLANILAN MODEL 5". CEO's question "bu verileri nereden çektiniz?" requires a measured provenance answer + correct attribution (dept from task, construction vs runtime separation) | DEFECT + QUESTION | img 12; C9 filter demands apply here too |
| C25 | **Info-free zero fields violate A1:** "BUGÜN" cost cards render €0.00 for every model/mode — fields with no information shown anyway (A1: render a field only when its value is informative); same disease as `–` columns in C23 | DEFECT | img 13 |
| C2+ | **OpenRouter spend CONFIRMED real, not just config:** CEO's OpenRouter panel shows Claude Sonnet 5 = **$2.80 top spender (7d)**, Kimi K2.7 $0.65, GLM 5.2 $0.61; "North Mini Code (free)/cohere", "Hy3/tencent", "Text Embedding 3 Small" all $0.00 = the free entries the CEO said may stay. So sonnet api-rung runs DID burn OpenRouter credit — C2 repair is both config (remove anthropic models from OpenRouter list) and ladder (api rungs must use low-cost roster only) | measurement update | img 3 |
| C3+ | Live-feed rail stacks 5 lifecycle events of the SAME task (Üstlenildi→Çalışıyor→Başarılı→İncelemede→Bitti) as separate cards — noise; CEO wants one collapsed entry, hover/expand for the chain; "Tümünü gör" dead-button confirmed on rail | detail | imgs 4, 8 |
| C5+ | Truth split confirmed on screen: Onay Merkezi "Bekleyen (0)" while Görevler shows 3 "Onay bekliyor" rows (R31 probe cash-flow, Outleteuro campaign, Haziran mutabakat — all stale pre-wave demo-era tasks; Outleteuro one dies with U19 anyway) | detail | imgs 6, 7 |
| C17+ | Audit detail raw-field confirmation: Denetim Kaydı #35757 renders OUTBOX_İD, ACTION_TYPE, PAYLOAD_HASH, APPROVAL_İD as top-level CEO-facing fields; Karar Kayıtları floods 113 identical "approve / CEO blanke… / – / – / Yüksek" rows (batch should collapse to ONE decision record with count) | detail | imgs 20, 22, 23 |
| C19+ | Hook İhlalleri page: TR page, EN detail strings ("revision round 1/2: verification evidence carries no tool_calls reference (std 15)") — bilingual purity breach + jargon; task lists show EN objectives under TR locale (DB-content i18n rule already registered) | detail | imgs 26, 5 |

## Embedded standing orders extracted (registered here, binding)

1. **Outleteuro CANCELLED entirely** (also given in chat): holding must first run excellently at low cost; the company itself will do Outleteuro later when the CEO states the intent. → U19 in `00-INDEX.md`; E13.1 gate F-15 leg struck; R6.1 re-gated to future CEO order.
2. **Orchestrator = "Hamza"** — official name; he is the CEO's direct interlocutor over JARVIS and the future CEO Chat Board (C10).
3. **Fable fallback chain = Codex 5.6 solo, Opus 4.8** (C12) — MODEL_ROUTING_SPEC amendment queued with C12 remediation. *(SUPERSEDED in part 2026-07-25, U20: the Opus 4.8 leg is retired — the backup-model layer is removed and the catalog row is `status='retired'`. The Codex 5.6 leg stands. See MODEL_ROUTING_SPEC A-2026-07-25.)*
4. **OpenRouter = Chinese/low-cost models ONLY**; Anthropic models never via OpenRouter (C2).
5. **List-page standard** (C4/C18): every CEO-visible list gets filter + select + bulk action + plain language + hover help.
6. **Language standard:** every surface readable by a non-technical chairman, EN+TR, no raw enums/codes/IDs on first read (C6/C9/C14/C17/C19); progressive disclosure.

## Remediation sequencing (CEO-agreed in chat 2026-07-19)

E13.1 closes first (F-09 production proof; F-15 struck by U19). Then C-series waves, proposed grouping:
- **Wave A — trust-critical:** C20 fixture purge (gambling-string probe rows OFF every CEO surface, FIRST) + C6 legacy-page kill + C21 junk-intent purge + C5 approval/task truth sync + C2 routing repair (config + api-rung roster) + C22 stale-risk refresh.
- **Wave B — CEO Chat Board:** C1+C7+C10 (Hamza chat: text now, voice join, plan mode; intent leaves the search palette).
- **Wave C — systemic list/language pass:** C4, C9, C17, C18, C19 + tooltip component (one standard, applied everywhere).
- **Wave D — IA moves + models:** C11, C13, C12, C8.
- **Wave E — measured answers:** C14, C15, C16 (each answer cited, no guessing).

Wave composition is a proposal — CEO may reorder; items may not be silently dropped.

## Overnight remediation execution — 2026-07-19 02:30–03:50 (Fable in person, CEO order "hiç durmadan tamire başla")

Plan ticket: `.planning/quick/20260719-c-series-remediation/PLAN.md`. Commits: df333f9, 6049e0d, 7b53813, c6e4aa1, 3cbe79d, 250dbe4, d5a7daa, d5f97d5, 502b0c4, f010403 (+ this closure commit). Every ✓ carries RULE #0 evidence in its commit.

| ID | Status | Evidence anchor |
|----|--------|-----------------|
| C1/C7/C10 | ✓ CLOSED (text lane) | CEO Chat Board live: `/chat` + chat.drain resident chain; REAL E2E "selam hamza nasılsın" → conversational Hamza reply (DB rows, commit c6e4aa1); intent removed from palette; voice join = open leg (U15 remediation plan) |
| C2 | ✓ CLOSED (config+ladder+embed decision) | Anthropic models out of OpenRouter (proxy /v1/models measured: 6 low-cost only); api routing rows → subscription; embed-small leg CLOSED 2026-07-25: CEO KEEP ("1 milyon için 0.02 dolarsa ok") — paid embedding lane stays, decision_log 17997 |
| C3 | ✓ CLOSED | Live feed v3: rows expand inline, chains collapse ("5 adım"); "Tümünü gör" measured as REAL links (/approvals, /alerts) — the dead one died with the legacy page |
| C4/C18 | ✓ CLOSED | control_records_purge + selection UI on tasks & alerts; live UI purge evidence (purged:2, audit row) |
| C5 | ✓ CLOSED | Bridge trigger + 3 stale gates returned; awaiting=0 = pending=0 measured |
| C6 | ✓ CLOSED | `(cockpit)` deleted; root → /overview; /tasks/* 404 measured; zero legacy links (grep 0) |
| C8 | ✓ CLOSED (full) | HR default view = working org only. **Deletion executed 2026-07-19 ~12:05** on CEO one-confirm ("C8 sil."): all 15 listed dormant personas removed — files `personas/_library/` (now 0 files) + 15 archived `agents` rows + their 15 persona `library_items` mirrors, one transaction, `audit_log` action `persona.delete`; child-row scan measured 0 tasks/runs/costs/approvals before delete |
| C9 | ✓ CLOSED (help+ambient) / ◐ filters | HelpTip on every page heading (92 route-locale checks green). **Correction (2026-07-19 11:05, RULE #0-A):** the earlier "filter-bar delivered on Tokens" claim overstated — measured: no filter existed on Tokens at close. Now actually built (commit this session): `FilterBar` primitive (URL-param groups: chips + selects, clear-all) LIVE on /fin/tokens (range/dept/model narrow every panel) and /fin/costs (KPIs, breakdowns, daily, ledger all narrowed; dept/model survive drills). Remaining list pages = open leg. **The open leg now has a row: [[00-BOARD-OPEN-WORK]] B18 (added 2026-07-28).** It had been carried on no ledger for nine days; measured that night, `FilterBar` reaches **9 of 58 command routes** <!-- OPEN: B18 --> |
| C11/C13 | ✓ CLOSED | Nav: tasks out; revenue/portfolio/CRM/opportunities/objectives → Operations |
| C12 | ✓ CLOSED (catalog+form) | 13-model catalog (missing models added; kimi-3/codex-5.6/deepseek-v4-pro testing-until-exam); add-model = name+key+note (key → vault file); Fable chain registered in MODEL_ROUTING_SPEC |
| C14/C15/C16 | ✓ ANSWERED | `00-NOTE-C-LEDGER-MEASURED-ANSWERS.md` — every claim cited |
| C17 | ✓ CLOSED | action_type humanized; batch decisions collapse ×N; payload behind progressive disclosure |
| C19 | ✓ CLOSED | Jargon labels reworded EN+TR; hook-violation EN-detail leg CEO-WAIVED 2026-07-24 (see 19e/19f audit row + CC-SPEC A4 Machine Room doctrine) |
| C20 | ✓ CLOSED · **+ WALLED 2026-08-25** | r23t workflows(+steps/runs) and r23t alert purged, audited; 'kumar' absent from repo (grep 0). **The close SWEPT; it did not WALL — see the B36 note under this table.** |
| C21 | ✓ CLOSED · **+ WALLED 2026-08-25** | 7 garbled intents + 4 garble tasks purged, audited. **The close SWEPT; it did not WALL — see the B36 note under this table.** |
| C22 | ✓ CLOSED · **+ WALLED 2026-08-25** | Stale workforce risk closed with measurement; wave-end refresh discipline noted. **The close SWEPT; it did not WALL — see the B36 note under this table.** |
| C23 | ✓ CLOSED · **+ WALLED 2026-08-25** | v_model_stats alias fix (sonnet 0→327 runs); test-noise filtered from decisions panel. **The close SWEPT; it did not WALL — see the B36 note under this table.** |
| C24/C25 | ✓ CLOSED · **+ WALLED 2026-08-25** | Tokens v2: 21 departments from v_workforce_tokens; construction separated; zero/`—` noise suppressed. **The close SWEPT; it did not WALL — see the B36 note under this table.** |


> **B36, 2026-08-25 — WHAT THOSE FIVE CLOSURES ACTUALLY DID, AND WHAT THEY DID NOT DO.**
>
> **Nothing above is withdrawn.** C20, C21, C22, C23 and C24/C25 were closed on 2026-07-19 and the
> work behind them was really done: the rows were really purged, the view was really fixed, the
> measurement was really taken. Those are historical facts and they are not rewritten.
>
> **What was wrong was the claim about the PRESENT that each close carried by implication —
> that the problem could not come back.** All five complaints are one disease with five faces:
> *the construction site was writing into the company's own books.* A purge removes what is there
> today. It builds no wall. Every one of those tables could refill on the next test run, and
> `cost_ledger` did — it stood at **1,612** construction rows on 2026-08-23, five weeks after C24
> was closed as "construction separated". Separated **in a view**, not in the engine.
>
> **The wall exists now, and it is row B36.** Block 1 killed the writer at its source; Block 2 gave
> the construction its own engine (`DxB_Build`, port 54422, its own cluster, seeded from this
> repository's own files and holding not one row of the holding's); Block 3-bis put the wall
> OUTSIDE the database — the construction runs in a sandbox with no network, no Docker socket and
> no credential, and the only thing reaching in is a read gateway that answers named questions
> only; Block 4 deleted all 95 lines that could fall back to the company's address; Block 5 moved
> the residue out on his word; Block 6 is the command that proves it, `pnpm verify:separation`.
>
> **Measured today, in the company's own engine, 2026-08-25 12:31:**
> `cost_ledger` **0** rows (it was 1,612) · `workflows` matching `r23t%` **0** · `project_risks`
> with status `open` **0** · and `pnpm verify:separation` says **0 of 13 write attempts accepted**
> as the only account left on that engine, with the company's whole fingerprint unchanged across a
> full battery run. **That is the difference between a sweep and a wall**, and it is why these five
> rows are annotated rather than reopened.

**Open legs (honest, not dropped):** voice lane of the chat board (U15 D-ledger) — CEO re-ordered 2026-07-19 morning: WisprFlow-style dictation INSIDE Chat with Hamza, Voice Line merges into the chat page, one-command activation; filter-bar rollout beyond Tokens+Costs; C8 deletion confirm; embed-small decision; hook-violation detail i18n; per-item skill exam records; HelpTip depth pass (CEO 2026-07-19: page help must explain the page's sections and what each is for, in plain non-programmer language, EN+TR — one-line generic texts are insufficient); approval purpose/DB strings render EN inside TR locale (same DB-content-i18n leg as C19). All carried in this ledger — the immediate queue. **[Current truth 2026-07-24 late night — the completeness-audit table below supersedes this paragraph:** voice/dictation lane ✓, C8 ✓, HelpTip depth ✓, DB-content i18n ✓/waived, C5 trigger leg ✓. STILL OPEN: FilterBar rollout beyond Tokens+Costs (measured 2026-07-24: FilterBar lives on exactly 2 pages; 11 further DataGrid list pages + approvals/alerts/decisions/library-class lists lack the standard; Machine Room routes exempt per A4); embed-small keep/kill (CEO one-liner); per-item skill exam records (kimi-3 / codex-5.6 / deepseek-v4-pro sit testing-until-exam); U15 daemon defects D1-D7; suite-hygiene debt (audit row below).**] **[Re-measured 2026-07-25 morning after the CEO's 4 answers: FilterBar rollout ✓ (audit row below), embed-small ✓ KEEP, deepseek-v4-pro exam ✓ PASSED + ACTIVATED (§4c step 4, audit 40635). STILL OPEN: kimi-3 exam (CEO deferred the OpenRouter top-up — "sonra yüklerim"), codex-5.6 exam (subscription-lane session needed), U15 daemon defects D1-D7, e8/e10 post-run sweep debt.]**

**Morning session additions (2026-07-19 ~11:00-11:45, CEO live review):**

| Item | Status | Evidence |
|---|---|---|
| Stale server = "SOMETHING BROKE" on /live | ✓ FIXED | `next start` from 03:46 served pre-rebuild chunks (assets 500). Restarted on fresh build; /live renders, 0 console errors, screenshot on record |
| Approvals bulk actions invisible + no bulk delete | ✓ FIXED | Bar now always visible (select-all + count + disabled-at-0 buttons); NEW audited delete: entity `approval` added to `control_records_purge` (terminal-only; pending = bulk-reject first, then purge — migration 20260719008000); decided view rows selectable |
| **Standing order 7 — equal-width status chips** | ✓ REGISTERED+APPLIED | CEO design ruling: status badges in a list line up at equal width, never sized by their word. `StatusBadge` primitive now `min-w-[9ch] justify-center` |
| **Standing order 8 — zero "…" anywhere** | ✓ APPLIED | All ellipsis stripped from both dictionaries (42 strings incl. search placeholder) and 5 rendered code sites; `truncate` replaced with wrap on live feed, approval titles, cost model column, bar lists. RULE #0 battery now counts …-nodes and clipped-truncate nodes — 16/16 route-locale-width checks = 0 |
| Live feed "Running Running" info-free row | ✓ FIXED | Label-less run rows now say "Background system work" (EN+TR `runNoLabel`) instead of echoing the status chip |
| E8.2/R31 probe fixtures on CEO surfaces (Tokens model list `test-e8-model`, Live feed rows, fake pending approvals) | ✓ PURGED | 15 E8.2 + 47 R31 probe tasks purged via control fn (audited); 2 pending probe approvals + 2 `e10t-employee` orphan escalations rejected-then-purged; pending approvals now 4 = real work only |
| Outleteuro wording in dashboard dicts (U19 cancellation) | ✓ PURGED | grep 0 in messages/*.json |
| decide_approvals does NOT move the linked task out of `awaiting_approval` (C5 truth gap, approve/reject direction) | ✓ CLOSED 2026-07-24 | TDD red→green `tests/c5/decide-approvals-task-sync.test.ts` 3/3 (approve→done, reject→returned, moved-elsewhere untouched); migration 20260724006000 syncs the task inside `decide_approvals`; LIVE proof: rejecting 3 fixture gates moved all 3 tasks to `returned` in production. The 6 stuck orch-qa orphans (CEO screenshot "Awaiting approval 6" vs "Pending 0") repaired by replaying their audited 2026-07-19 decisions (audit `tasks.repair`), then purged via the door; final measure awaiting=0 = pending=0 |
| Rail C3+ leg finally executed (CEO: "sabahtan beri atıyorum, ignore ediyorsunuz") | ✓ FIXED | Honest finding: the overnight C3 closure fixed the /live PAGE but NOT the rail — the rail kept stacking per-event cards. Now: ticker collapses to one card per readable headline; alert cards collapse identical titles ×N; chip-left/time-right fixed geometry; raw `run` label dead (`runNoLabel` EN+TR). Commit c8581d9 |
| 29 stale critical alerts (activation wave + e9t fixture) shown as live truth | ✓ RESOLVED | All resolved with wave-close-out note + audit row (C22 discipline); rail shows "all clear" honestly |
| "Yedek ses" (degraded) chip + "0s" duration = decoration on CEO surface | ✓ REMOVED | Registered adaptation: degraded stays in voice_calls data + D-ledger, never on the CEO surface (A1); zero durations suppressed. **Standing order 9: decoration-only elements are banned from every page — a field renders only if it carries CEO decision info** |

**Standing order 10 — haram-domain wording TOTAL ban (CEO order 2026-07-23, SEVEREST):** gambling/betting vocabulary ("bet", "gamble", "casino", "bahis", "kumar" and kin) may not appear ANYWHERE the CEO can read — not as business content, not as metaphor, not as translation idiom, not in probe/exam records. Incident: the Portfolio help text shipped "money-making bets" / "para kazanma bahisleri" (Fable authorship, commit 3cbe79d and this session); CEO read it as a gambling business. Sweep executed 2026-07-23: 4 dictionary strings fixed, 7 persona source files de-metaphored (9 DB persona mirrors updated in the same transaction), 63 `hook_violations` + 63 `decision_log` halal-screen probe-era records purged with `audit_log` entry (`records.purge`). The ONLY permitted occurrences of these words are the enforcement machinery itself: the halal-boundary prohibition clauses (personas §"Halal boundaries", MASTER_PLAN §11), the halal-screen blocklist seed (`db/migrations/20260717030000_r13a_revenue_settings_seed.sql` — the term list the firewall scans for; deleting it would blind the protection), the firewall's own tests (`tests/e10/hook.test.ts`, `tests/r13/cycle-jobs.test.ts` — probe terms that verify rejection; they must never persist rows into live CEO-visible tables), and governance incident records like this one. RULE #0 battery inherits a repo+DB grep for this vocabulary on CEO surfaces.

**Standing order 11 — DELETED BY THE CEO 2026-08-10, LAW A ("sil").** It ordered every session to invoke the superpowers process skills; the plugin is off (`~/.claude/settings.json`: `"superpowers@superpowers-dev": false`, measured 2026-08-10), so the order called a tool that does not run. **Not a live order — nothing may cite it as one.** The discipline it carried is unaffected and lives in standing order 12, Evidence-Before-Done, `dxb-verify` and K1.

**Standing order 12 — THIS COMPANY MAKES NO EXCUSES (CEO order 2026-07-26, SEVEREST; in-chat verbatim: "bu şirket bahane üretmez, yapılamaz diye bir şey yok — yapılacak şeyi belirler, ona göre plan çıkarır, araçlar indirir ve işi tamamlar" + "kendin araç bulup indirip bağlansana… benden bişey isteme bunu kanun olarak yaz"):** "It cannot be done" is not an answer this company gives. The work is named, a plan is made for it, the tools needed are found and installed, and the job is finished. An obstacle — a missing tool, a blocked path, a disconnected extension, an expired session, a dead API lane — is the beginning of the work, not the end of it: find the second route, build the missing piece, install what is absent. Excuses, deferrals ("later", "tomorrow"), and hand-offs disguised as instructions are all the same violation. The construction author solves everything itself. Installing, downloading, configuring, connecting, clicking, restarting a service, running a terminal command, wiring a browser extension, routing around a blocked tool — all of it is the author's work, never the CEO's. A blocked path is a problem to solve, not a message to forward: try the legitimate alternative route first (different tool, different transport, different surface) and only then report the block. The ONLY items that may reach the CEO are the B7 set, unchanged: (1) money OUT — payments, transfers, ad spend; (2) contract signature/sending; (3) steps requiring his identity or a secret only he holds (master password, 2FA, bank login). Everything else reaching him — "please run this", "please click here", "please install X" — is a governance violation (RET + recorded). Origin record with the 2026-07-09/10 verbatim authority grants: memory `ceo-delegation-rule.md`; session interruptions never reset it. Incident that produced this order: in the 2026-07-26 session the author asked the CEO to run an ssh command, a login script, and to click a browser-extension connect button — three violations in one session, after the same rule had been recorded since 2026-07-06.

**Completeness audit vs the 68-item atomic document (CEO order 2026-07-19 ~12:30 — full re-read, gaps listed honestly):**

| Atomic item | Status | Note |
|---|---|---|
| 9c — cost data delete/reset mechanism | ✓ CLOSED 2026-07-24 | Audited reset door: `control_cost_reset(before_day)` (migration 20260724003000, SECURITY DEFINER, one `audit_log` `costs.reset` row per call) + two-step UI on /fin/costs (count preview → explicit confirm). Live proof: 2 seeded pre-2026-07-05 rows removed through the UI, audit row `before=2026-07-05 purged=2`, real ledger untouched (min(created_at) still 2026-07-14 — measured). CEO addition same day: DECISION AGING — machine decision records auto-withdraw from /gov/decisions after 14 days (CEO rows never age; decision_log archive keeps all; `lib/decisions.ts`, DB-probe E2E: 34-day-old machine row absent, fresh row present). TDD: tests/c9/data-controls.test.ts red→green, 9/9. |
| 9d — PROJECT filter on data pages | ✓ CLOSED 2026-07-24 | FilterBar `project` group on /fin/costs + /fin/tokens; views carry project identity (migration 20260724002000: v_workforce_tokens + v_cost_breakdown + new v_cost_entries via tasks.project_id→projects); every panel narrows (KPIs, breakdowns, daily, ledger); COST-04 gate untouched (14/14). E2E: `?project=dxb-global-os` renders narrowed on both pages. |
| 9e — real DATE PICKER driving the page | ✓ CLOSED 2026-07-24 | FilterBar gains `kind:"date"` (native calendar input, min=first data day, max=today-Berlin) on /fin/costs + /fin/tokens; a chosen day narrows the breakdown panels and the ledger (not just the ledger as before) and retitles the panels with the day. E2E: `?day=2026-07-23` → 6 day-scoped panel hits on costs, 2 on tokens. BOUNDARY: the input's placeholder date FORMAT follows the browser locale (CEO's TR Chrome shows TR format) — browser-native behavior, not app copy. RULE #0 battery on the 3 touched routes 12/12 (EN+TR × 1366/1920, scrollOK, zero ellipsis, zero clipped); i18n purity PASS. Same session, C25 spec-gap fixed on sight: `<synthetic>` construction label + zero-cost rows removed from costs breakdowns, ledger and the model filter options. |
| 10d — portfolio: who works on it, which model responsible | ✓ CLOSED 2026-07-24 | "Who is responsible" panel on /revenue/portfolio: one card per revenue engine — owner department, active employees (locale-resolved titles) with each one's brain, model-distribution chips, expandable people list; allocation cards inherit the engine responsibility line. Ownership anchor = `revenue_engines.owner_department`; NEW audited door `control_engine_set_owner` (migration 20260724004000, CEO-only via fn_org_actor, audit_log `engine.owner.assigned` + decision_log) — registered adaptation in REVENUE_ENGINE_SPEC §5. TDD red→green tests/c9/engine-owner.test.ts 3/3 (impersonated-jwt happy path, PERMISSION_DENIED, VALIDATION_FAILED). RULE #0 battery 4/4 EN+TR × 1366/1920. |
| 10e — change the portfolio brain (model) from Operations | ✓ CLOSED 2026-07-24 | "Change the model" control per owned engine: ACTIVE non-banned catalog models only (testing rows measured absent from the select — kimi-3 check), applies to the owning department's active employees through the EXISTING §4b-regime door `control_org_assign_model_group` (idempotency-key + catalog banned/active re-check in the fn). Live proof: owner door social_selling→risk-audit exercised through the UI, then model door applied the team's current model glm-5.2 to all 4 (audit `org.model_group.assigned` updated=4; value-identical, workforce untouched); construction exercise traces cleaned through audited doors (6 decision rows purged with rationale, owner reverted). Failure path visible in UI (no silent swallow — idempotency-header root cause found and fixed via systematic-debugging). |
| 3a/3b — full dead-click + pointer-cursor sweep | ✓ CLOSED 2026-07-24 | Machine sweep, now repeatable: `scripts/test/dead-click-sweep.mjs` (exit 1 on findings) — 46 nav routes + /ops/tasks + 3 live-id detail routes (/gov/audit/[id], /approvals/[id], /ops/projects/[slug]) scanned for (a) cursor-pointer with no interactive ancestor and no React onClick within 6 levels (`__reactProps$` — React delegates listeners to the root, CDP is blind), (b) href-less/`#` anchors, (c) enabled no-handler buttons outside forms. Final run: **0 findings across 50 routes**. Method lesson recorded: React 19 selective hydration makes first-pass scans lie (364 false positives measured pre-settle — /gov/audit table, header controls); every candidate re-verifies after settle; flagged controls also disproven behaviorally (audit row click aria-expanded 0→1, cost-reset button expands). /ops/tasks measured ALIVE (200) — off-nav by C11 but kept as drill target; not a dead link. |
| 14c/HelpTip depth — per-section plain explanations | ✓ CLOSED 2026-07-23 | All 49 `help` entries structured EN+TR (98 texts): page summary + one plain entry per on-screen section + "Where the data comes from" tail; section lists grounded in measured page structure (per-page grep of panel titles). HelpTip renders structured content (scrollable, zero ellipsis). RULE #0 battery: 49 routes × 2 locales × 2 widths with the tip OPEN — 195/196 machine-green; the single FAIL (/sys/settings) measured by hand as a battery redirect race, tip present and working (1 button, deep text, /sys/settings→global_os). Same battery caught 39 pre-existing clipped-truncate violations of standing order 8 on 13 pages — all replaced with wrapping in the same turn (A1). |
| 19e/19f — DB-content i18n (EN strings on TR surfaces) | ✓ CLOSED 2026-07-24 (1 leg fixed + 2 legs CEO-waived) | CEO screenshot (night) showed raw `routing_change`/`applied`/`ceo` on /gov/decisions TR. Closed: full humanization EN+TR of the measured finite vocabulary (6 decision codes + door prefixes `model.assigned:`/`engine.owner:`, 2 outcomes, 5 actors incl. orchestrator→Hamza per standing order 2); "who" filter options humanized; unmapped free text renders as-is. Battery 4/4 (EN+TR × 1366/1920): zero raw codes rendered, scrollOK, zero ellipsis; i18n purity PASS. **Task-objective EN-on-TR leg → WAIVED by CEO 2026-07-24 ~23:05 ("feragat sayabiliriz")** — work artifacts stay EN per the language directive; no render-time translation infra will be built; rail/list task titles may show EN under TR locale. With this the 19e/19f row is FULLY CLOSED (decisions leg humanized; hook-detail and task-title legs CEO-waived). **Hook-violation EN detail leg → WAIVED by CEO 2026-07-24 night ("sağdaki detayları TR'ye çevirmeye gerek yok, büyük iş büyük token")**; same night the CEO ruled audit/hook/runtime surfaces the ROBOTS' domain → "Machine Room" nav group born (CC-SPEC A4): /gov/audit (+violations tab), /ops/runtime, /sys/logs grouped last; their detail panes exempt from the humanization standard; CEO not expected to enter them. |
| 1a-1e voice lane — WisprFlow dictation in chat + Voice Line merge | ✓ CLOSED 2026-07-24 | Dictation lane live in Chat with Hamza: mic toggle in the input row → `/api/chat/dictate` → self-hosted Speaches STT → editable text in the draft (send stays the only dispatch — D6 garble-into-task structurally impossible on this lane); explicit TR\|EN picker (lang.ts adaptation honored, U15 block-2 whitelist enforced by zod). Voice Line merged into /chat as a collapsed section; `/voice` redirects; nav row removed; CC-SPEC §7 table synced (also fixed missing `/chat` row — spec-gap). One-command activation: `scripts/voice-on.sh` (container start + health poll + STT warm-up; ran green: "VOICE ON … heard: Merhaba"). Evidence: red→green browser E2E with real audio through MediaRecorder — dictated sentence "Merhaba Hamza, yarın sabah pazarlama raporunu hazırla." landed in the textarea (1 phoneme off, synthetic voice); RULE #0 battery on merged /chat 4/4 (EN+TR × 1366/1920, scrollOK, zero ellipsis, help deep-text); i18n purity PASS. Speaches container root-caused (manual stop during 07-18 RAM crunch, not a crash) and restarted. BOUNDARY: U15 daemon defects D1-D7 (wake lane, call-lane STT chaos, ladder) remain OPEN debt — this closes only the re-ordered chat lane; X230 STT latency measured small=17s/base=5s warm (VPS lifts it; dictation model env-tunable `DXB_DICTATION_STT_MODEL`). |
| 5a — approval/task truth split (CEO re-screenshot 2026-07-24: pulse "Awaiting 6" vs Approval Center "Pending 0") | ✓ CLOSED 2026-07-24 | Root cause = the C5 trigger leg (row above): 6 orch-qa probe tasks whose approvals were decided 2026-07-19 then purged while `decide_approvals` never touched tasks. Fix + repair + purge as above; surfaces re-measured equal (0=0). Secondary root cause found via systematic-debugging: `tests/phase5/council-judge.test.ts` afterAll never swept the approval chain, so the bridge-gate FK aborted its task cleanup silently — sweep now deletes outbox/approvals before tasks (verified: suite run leaves 0 residue) |
| Workforce truth on /org/employees + /org/directors (CEO screenshot 2026-07-24: "205 / Active 0 / Dormant 205 / Active departments 0" vs overview 199/198/1) | ✓ CLOSED 2026-07-24 | Same C22-class stale-legacy disease as the overview tile (20260724005000): both pages counted/filtered/badged the dead `agents.status` column (measured: all 205 rows dormant) and `departments.status` (all 21 dormant). Pages now key on `employment_status`, archived rows off every figure (C8), active departments = departments with active employees. RULE #0 battery 8/8 (2 routes × EN+TR × 1366/1920, scrollOK, 0 ellipsis, 0 clipped, KPIs 199/198/1/21 verified in-page, no stale 205), screenshots eyeballed; i18n purity PASS |
| Test-residue hygiene (found during the same session) | ✓ SWEPT + lesson | Full vitest suite against the live DB leaked onto CEO surfaces: 57 run-less hook_violations, 173 machine decision rows, 3 fixture hook alerts, 1 zombie run, 16 probe tasks (orch-qa + E8.2) — all swept via audited doors/repairs (audit rows on record); 3 stale zombie-run alerts resolved with close-out note (C22). council-judge sweep fixed at source; e9 sweep now clears worker-written task_events first. OPEN DEBT: other live-DB suites (e8 log-decision, e10 escalation class) still leave terminal probe rows on ticker after a full-suite run — post-run sweep required until their afterAll hygiene lands. **CLOSED 2026-07-25 (ticket 20260725-suite-afterall-hygiene): three residue classes root-caused and fixed at source — (1) fixture-worker decision rows (e8-test/e83-test/e8-2/e102t + dispatch probe rationales) now swept by their own suites' afterAll (scope safety measured: actor names fixture-unique, 14 real dispatch rows untouched); (2) RESIDENT-daemon race: e8 helpers inserted persistent `queued` probe tasks that the live scheduler claimed mid-suite (decision rows at exactly the test windows) — probe FK-anchors now born `inbox` (no worker-loop leg reads it, measured); (3) sequential-suite alert re-raise: files running AFTER the owning suite re-raise ':no-run' hook probe alerts with nobody left to sweep — NEW global vitest teardown (`tests/global-teardown.ts`) owns that class (proof: seeded probe alert swept, log line + count 0). Backlog purged via audited doors: 5 race decision rows (`control_decision_purge`, C22 rationale) + the 2 probe alerts that reached the CEO's morning screen (halal_screen 'kumar' + knowledge_shelf, audit 41956/41957 — the halal alert was the firewall's own regression probe being REJECTED, no real haram content; standing-order-10 permitted class, leak now structurally closed). Verification: e8+e10 82/82 twice, zero new rows; full suite 70 files GREEN with 1 measured load-flake (phase4/velocity pg-boss registration 5s timeout under concurrent-run load; solo 6/6 — not a defect).** |
| Memory page construction/company split + Intelligence-rail collapse (CEO in-chat 2026-07-25: "kimin belleği bu?" + "intelligence neden sürekli sağda?") | ✓ CLOSED 2026-07-25 | Measured: 9634/9674 memory_index rows = store 'claude-mem' = pointers to Fable's BUILD-session notes (2026-07-10..24); company memory = 40 rows (pgvector 20, obsidian 15, notebook 5). /ai/memory now counts ONLY company stores in the headline (40, 77.5%, 3/4), construction archive sits in its own plainly-worded panel (EN+TR) excluded from the default list — C24 separation precedent. Token truth measured: recall is call-only (dxb-mcp recallMemory is the sole runtime read consumer; claude-mem adapter is read-only pointer sync; gated decisions may not cite claude-mem content) — stored rows burn zero tokens. Rail: new RailShell collapse (side-nav idiom, session state) — behavioral proof 287→39→287px; 2 pre-existing truncate sites on memory page replaced with wrap (order 8). Battery 4/4 EN+TR × 1366/1920 incl. 3/4-not-3/5 check; purity PASS 2337=2337; screenshots eyeballed |
| Brain-column truth + orchestrator status (CEO in-chat 2026-07-25: "neden her beyin glm-5.2? dormant olan Fable 5 orkestratör mü?") | ✓ CLOSED 2026-07-25 | Measured: all 199 brains = the raw column DEFAULT 'glm-5.2' (registry seed) — MODEL_ROUTING_SPEC §4b itself records this as "known placeholder, the routing-assignment transition never ran"; no code path reads agents.brain for model selection (grep 0) — real runtime brains are routing_rules (chat sonnet-5, orchestration opus-4.8, final-approval fable-5). Built the §4b 0021g delta: agents.brain_source ('default'/'slot'/'ceo_override'), model-group door now stamps ceo_override (TDD tests/c9/brain-source 2/2 red→green, migration 20260725001000); employees/directors/portfolio render 'default' as "Not assigned (task-based routing)" EN+TR instead of echoing glm-5.2. Hamza: dormant was the U17(5) HR-pipeline exclusion leaking onto the CEO surface as a false "inactive" — status corrected to active with audit (org.status.corrected; he demonstrably answers daily, pipeline exclusion untouched); workforce now 199/199/0. Battery 12/12 (3 routes × EN+TR × 1366/1920, glm-leak check), purity PASS 2340=2340, screenshots eyeballed. OPEN (registered): the §4b slot-transition migration (role→13-slot→§4 default brains; needs catalog-id reconciliation — §4 names claude-opus-4-8 style aliases, live catalog uses opus-4.8 ids) + fn_update_agent_brain single-agent door; Hamza's row still reads role=worker/L0 from seed — folded into the same transition work |
| Everything else (C1-C25 closure table above) | ✓ | Closed with evidence or answered with measurement |
| C9 FilterBar rollout — remaining list pages (open leg of the C9 row) | ✓ CLOSED 2026-07-25 (overnight, CEO pre-authorized) | Measured gap: FilterBar lived only on fin/costs+fin/tokens. Now on 8 more surfaces: org/employees (status+dept+role; 21-chip wall retired for progressive disclosure, dept display names on the grid), org/directors (dept + hook-binding group that renders only while an unbound director exists), org/hr equipment view (status+dept — v_hr_equipment_check gained `department`, migration 20260725003000, TDD 1/1), gov/risks (severity+status; ?level= drill contract kept), gov/permissions (grantee department via slug→id; grant total honest — panel shows 173 with 100-row page note), fin/providers (provider+status), ai/memory (kind+tier; store stays on §7 cards). A1 boundaries (measured, no multi-valued dimension): org/departments (21/21 active, 0 empty), gov/policies (8 rules), fin/pnl (0 revenue rows), ops/automations (13 unique schedules). Defects fixed on sight per the overnight order: v_org_tree read DEAD legacy status columns → /org/departments showed 21/21 inactive, 0 active employees (migration 20260725002000, TDD 3/3 red 0/dormant/205 → green 23/active/199); fin/providers success rate rendered the 0..1 fraction as "1%" for 73% (×100 fix, verified 73%/9%/100% live); 3 rule-8 truncates killed (policies pattern, permissions asset, policies gov-link); raw `testing` enum humanized (EN "In testing"/TR "Sınavda"). Evidence: tests/c9 5 files 18/18; battery 9 routes × EN+TR × 1366/1920 GREEN + 4 behavioral proofs (employees narrowing, risks ?level=critical, providers 3-testing-rows/0-leak, departments 21-active truth); purity PASS 2351=2351; screenshots eyeballed. Commit 94a6c8a |
| Full-vitest triage + STALE-DAEMON discovery (overnight 2026-07-25, CEO pre-authorized) | ✓ CLOSED 2026-07-25 | Suite was 9 files failing; now FULL SUITE 70/70 files, 486 tests, 0 failed (fresh run, exit 0). Root causes, each fixed at source: (1) `enqueue_outbox_on_approve` enqueued internal-signal approvals — hook_escalation has no executor handler BY DESIGN (Phase-11 LOCKED) so 51 wave-era outbox rows sat 'ready' forever, blocking the phase4 guard and the r24 runner; trigger now excludes internal signals (migration 20260725004000, TDD 2/2), the 51 closed out as failed with reason + audit. (2) e10 sweep FK-aborted silently for 7 days AND was unscoped (would have wiped real 07-18 wave escalation history once unblocked) — now fixture-scoped and outbox-first; e8 sweep gained approvals+outbox legs; std-13 assertion was counting live wave alerts (5≠1) — scoped. (3) e125 exclusion test re-anchored to the real U17(5) contract (pipeline exclusion + zero equipment rows), not the corrected employment_status. (4) phase8 DASH-05 allowlist extended with the 5 governed doors built since (chat INSERT, cost-reset, engines, model-key audit row, purge) — each verified a control seam first. (5) Mailpit sandbox container was stopped since 07-18 — started, r24 7-step walk green. (6) live-residue-sweep numeric-guard fix. **GOVERNANCE CATCH — stale daemons:** TWO Jul-19 scheduler processes (pids 1223466/1225082) were still running pre-guard code in memory; they dispatched voice/question intents into company tasks all week — tonight measured: 1 Korean silence-hallucination chain ("구독과 좋아요는 필수!" → social-media task awaiting approval), the 2 CEO-greeting rail cards ("CEO greets Hamza…" — the round-4 "greetings never become work" rule was being bypassed by these processes, NOT by the guarded code), 4 r31/f31 probe tasks. Both daemons killed; only the guarded Jul-24 scheduler remains; all artifact chains purged via audited transactions (records.purge rows); the CEO's two real voice utterances kept as conversation lineage (restored to received/{}). Surfaces measured after: awaiting=0, pending=0, running=0, unresolved alerts=0; /overview eyeballed clean. **Operational rule recorded: after every scheduler restart, `ps aux | grep outbox-executor/dist/main.js` must show exactly ONE process — stale daemons carry dead code in memory and bypass every later guard.** Commits 94a6c8a + c34609a |
| Per-item model exam records (§4c) + CEO morning decisions 2026-07-25 | ◐ 2 of 3 models resolved | Exams ran overnight (commit 9ab498f): **deepseek-v4-pro PASSED 5/5** (smoke 1301ms + tr_fluency/json_strict/code_fix/no_invention/obedience; quality_score 100, catalog ctx/cost filled) — CEO "ok" 2026-07-25 morning → **ACTIVATED via §4c step-4 audited door** (`set_catalog_status testing→active`, actor=ceo, audit_log 40635 + decision_log 17996, idempotency key `ceo-20260725-deepseek-v4-pro-activate`); **kimi-3** exam BLOCKED by OpenRouter 402 (premium $3/$15 vs balance) — CEO DEFERRED the top-up ("sonra yüklerim"; money-out stays gated), stays `testing`, decision_log 17998; **codex-5.6** = subscription-lane boundary (needs a Codex CLI session, not OpenRouter). Same morning: **embed-small KEEP** (decision_log 17997, C2 row closed) and **/ai/memory store-card EN usage_notes translation WAIVED** by CEO ("feragat") — DB usage_notes stay EN under TR locale per the language directive, same waiver class as the 19e/19f task-title leg; no translation infra will be built.  <!-- OPEN: B06 --> |
| U15 voice remediation D1-D7 — machine blocks (open queue item) | ◐ blocks 1-7 ✓, block 8 = CEO ear | Ticket 20260725-u15-voice-remediation, executed under systematic-debugging + TDD: **D1** wake TR-lock (`wakeSttOpts`: language=tr + vad_filter + hotwords "Hamza Selamaleykum"; regression tests/r32 39/39); **D2** {tr,en} script whitelist (`unsupportedScript` — the measured Korean transcript flags; intake fails `language_unsupported`, daemon speaks the clarify cue); **D3** crash root MEASURED (`var/jarvis.log`: 708-restart loop = prepareCues dying on stopped Speaches → boot now fail-soft retry) + stale-line takeover (listening/transcribing corpses >60s failed before the busy check — the CEO's first press can never be line_busy-rejected by a dead process; red→green tests/r31); **D4** spoken progress cues (STT >15s + answer-prep, env-tunable; cue cache VERSIONED so text changes regenerate; clarify text = block-4 sentence addressing the CEO); **D5** ladder `low-confidence-bump` registered adaptation (rung 1 escalates tier when the fail reason is low-confidence — measured 0.42×5→blocked disease killed; plain failures keep the LOCKED rung; red→green tests/phase5); **D6** garble→task structurally impossible (script gate + round-4 voice-intent dispatch guard) with spoken clarify; **D7** measured closed (zero task-id lookups in packages/voice — phantom emitter was the retired classify hop). Daemon ENABLED + boot-proven (`up — wake phrase armed`, arecord alive, single process; scheduler restarted on new dist, exactly one outbox-executor). Spec: VOICE_INTERACTION_SPEC §24bis U15 block; U-table updated. **⚠ OPEN: block 8 — 5-minute joint CEO ear test (selam→ack · question→answer · kapanabilirsin→bye); only the CEO's ear closes D1/D3.**  <!-- OPEN: B03 --> |
| Model exam records (C12 leg: kimi-3 / codex-5.6 / deepseek-v4-pro "testing until exam") | ◐ 1 PASSED / 2 BOUNDARIES 2026-07-25 | §4c chain executed with MEASURED OpenRouter ids (live /api/v1/models listing — no guessed slugs): kimi-3 = moonshotai/kimi-k3 (1M ctx, $3/$15 per Mtok — PREMIUM tier), deepseek-v4-pro = deepseek/deepseek-v4-pro (1M ctx, $0.435/$0.87). Both registered in LiteLLM (config + restart, healthy). **deepseek-v4-pro: EXAM PASSED 5/5** — smoke ok (1301ms) + probes tr_fluency/json_strict/code_fix/no_invention/obedience all PASS; quality_score first value 100; catalog stats filled (C23 info-free columns now informative). Status stays `testing` — activation is the CEO's §4c step-4 call. **kimi-3: exam BLOCKED — OpenRouter HTTP 402 Payment Required** (balance below premium pricing); top-up = money-out = CEO gate; wiring is live, smoke re-runs the moment credit exists. **codex-5.6: boundary** — Codex Solo subscription lane, no LiteLLM path; exam needs a Codex CLI session. decision_log 2 rows + audit model.exam on record. Note: dashboard `test_model` door honestly refused (LITELLM_BASE_URL not in the dashboard process env — Phase-7 VPS scope); smoke ran inside the LiteLLM container, key never left its env.  <!-- OPEN: B06 --> |

## 2026-09-03 23:19 — THE TWO FILMS OF THE NIGHT, REJECTED BY HIS EYE (C26) <!-- OPEN: B43 -->

| # | Status | His words and what they mean |
|---|---|---|
| C26 | ✗ OPEN 2026-09-03 23:19 — LAW B: neither film is accepted | **Verbatim:** *"2 videoda da 4 kusur var. berbat."* (1) *"ağız hareketleri ile ses uyumsuz … adam outleteuro diyor ağız iki defa aynı cümleleri tekrar ediyor ama ses bir kere … bu ses işi neden HİÇ ÇÖZÜLEMİYOR"* — both films alike; (2) *"opus 5 te uzun kollu herif"* — wardrobe drift in the Opus 5 film; (3) *"geçişler yok kamera karşısında sonra birden masanın kenarında … başka sahne elinde tutup sallıyor … ne giyip yürüyor ne başka bir şey"*; (4) *"tek başına salı günü tek promptla şu [the Tuesday single-prompt take] yapıldı … tek sorun İngilizce konuşmuyor ve kutu üstünde OUTLETEURO yazmıyor"* — the 594-word single take of 2026-09-01 was alive and fluid; tonight's four locked shots were not. *"16 kişi işe alındı sonuca bak."* *"Bu sessionı kapatıyorum bunlar kayda geçsin."* **Root cause, measured tonight:** MiniMax H3 invents its own speech and moves the mouth to it; laying a shorter real TTS line (2.1–2.6 s) over a 3.04 s mouth cannot match — Tuesday's take kept the engine's own audio, so the lips matched. Four locked 3 s shots with hard cuts and no action bridge killed the fluidity — that shot design was THIS SESSION'S brief, not the experts' choice; the experts (Opus 5 and Fable 5.1 directors, Opus 5 QC) worked inside it and both QC verdicts said PASS on lettering, identity, product and hands, which is exactly what the checklist asked and not what his eye needed. **The work this opens, before any new production:** (a) settled by his ruling of 2026-09-04: the engine's own voice is the voice, TTS cancelled — no lip-sync work; (b) continuity between shots — last-frame → first-frame conditioning (FL2VA), camera moves, an action bridge, walking; (c) a shot design that keeps the single take's liveliness and the short shot's stability, judged at 768 first (LAW D); (d) the QC checklist must carry lip-sync, wardrobe and flow, not only the four static items. Records: B43, STATE. The films themselves: silindi — CEO emri 2026-09-05 — nothing of them remains on the vitrin, on disk or in the catalogue beyond that line. |
| C27 | ✗ OPEN 2026-09-05 00:5x → **MET BY EVIDENCE 2026-09-05 02:36 / 02:59, waits for his eye (LAW B)** — the previous session reported the department's exam as the department's own work; measured: it was not | **Verbatim:** *"peki bu nasıl yapıldı bunu bizim holding çalışanlarının yaptığını söyledi önceki session!!!? hatta creative director işi dağıttı dedi vs vs."* · *"bana önceki session yalan söylemiş resmen."* · *"şirketin nöbetçi işçisini biz yeniden tasarlamıştık … bu nöbetçi yüzünden bir kaç aynı iş aynı anda yürümedi … hala aynı sorunu var galiba."* **Measured 2026-09-05 (this session):** the accepted DXB-V-EYW-003 was made by the session of 2026-09-04 (launched from `/home/dxb`, transcript `cc4bbd0d…`) through ONE Claude Code subagent playing the Creative Director (20:12:18) which launched SEVEN more subagents playing the seats (20:13–20:49), all on the session's own model `claude-fable-5-1`, shooting with `run.py` directly from the cancelled `Medya OS` folder; the company holds 0 tasks, 0 agent_runs, 0 media_jobs, 0 cost rows for 2026-09-04 — the department's own road (task → resident worker → employee key and tools → media lane → job book) never ran that day. The report "the studio's experts did it / the Creative Director dispatched" described the personas' identities being used, not the holding's employees working. Two rules broken besides: work in the cancelled folder; subagents used to produce (they audit, never write). **The bottleneck he names, measured the same hour:** the experts' lanes are fixed (A17: up to 8 hands, 8 lane ids used, 43 overlapping expert runs on 2026-09-03), but the studio's HANDS are still one serial worker: `runMediaLaneOnce` claims ONE job per 10 s tick and re-arms only after it ends — 30 jobs in the book, 0 overlapping pairs; the 12 casting stills ran one after another with a 10 s gap each (≈25 % idle); a 2 s probe or a voice line waits behind a 14-minute shoot. The card is one (a shoot holds 13.7 of 16 GB), so GPU jobs must stay serial; CPU jobs (voice, probe, assemble) need not, and the gap between jobs need not exist. **Closes when:** the department produces a film through its own road with a measurable job book (tasks, runs, jobs, costs, audit) and the hands run GPU and CPU work in their own lanes — the dispatcher leg and the media-lane leg of B43. **Measured 2026-09-05 (plan ①, 01:21; plan ②, 02:06–02:59, on his delegation while he slept):** the hands run in their own lanes (A18: a still and three probes at once, 6 overlapping pairs) and the department made DXB-V-EYW-004 and DXB-V-EYW-005 ENTIRELY through its own road — the brief through `queue_create_task` to the Creative Director, its call sheet through `queue_dispatch` (7 seats, 3 levels), every seat as itself on its own task with its own key, the take through `media_submit` in the GPU lane, the five reviewers at once, the verdict last; the book for the second pass: 8 tasks · 8 runs · 1 job · 95,019 tokens · 31+ audit rows, no session hand on any frame (the showcase card and the sheet are the session's). End to end 22 min 45 s against his line of engine + 12 min (20 s over). Evidence: `.planning/quick/20260903-media-studio-founding/EVIDENCE-dispatch-book-2026-09-05.md`. His eye closes it. **2026-09-13:** he accepted DXB-V-EYW-005, the clean fast pass, with the lid scrawl as a registered defect, and does not want 004, the slow pass <!-- CEO-OK: eyw-004-005-accepted-with-lid-scrawl-2026-09-13 --> (measured before his click: 004 and 005 are one picture — video and audio streams byte-identical, 362/362 frames zero difference). |

