# HOLDING-OS-MASTER-PLAN — KORPUS HARİTASI VE CANLI DURUM

> **Kaynak hüküm:** CEO BEKLENTİLER direktifi (2026-07-10, tam metin: [[00-CEO-DIRECTIVE-BEKLENTILER]]).
> **Bağlayıcı sözleşme:** `~/.claude/plans/sana-s-yl-orm-konu-al-m-nce-agile-pebble.md` (CEO hizalaması 7 madde + yasak listesi).
> **Yazar:** korpusun her satırı bizzat, inline yazılır; yazarlık devri yok (model routing **v9**). Güncel yazar: **Opus 5** (CEO 2026-07-25, U20). *Tarihsel: korpusun 31 spec'ini 2026-07-10'da Fable 5 bizzat yazdı.*
> **Süreç:** Plan-first. Korpus %100 → Fable execution (12 Temmuz son geceye kadar) → **Opus 5 devraldı (2026-07-25, U20 — yedek model katmanı yok)**.
> **Port standardı:** 3000 (direktifteki 3100 düzeltildi — kayıtlı uyarlama #U1).

## Session açılış protokolü (her yeni session)

1. Bağlayıcı sözleşmeyi oku (yol yukarıda) → 2. governance + routing v6 memory → 3. `.planning/STATE.md` + bu INDEX'in durum tablosu → 4. CEO direktifi → 5. Kaldığın dosyadan YAZMAYA DEVAM. Soru yok, izin yok, subagent yok, Sonnet yok.

## Derinlik standardı (her spec dosyası — §14, 27 başlık)

Amaç · Gereksinimler · Mimari · Veri modeli · Component yapısı · Backend yapısı · Frontend yapısı · API'ler · Event yapısı · State yönetimi · Database tabloları · İlişkiler · Yetkilendirme · Logging · Audit · Security · Error handling · Retry · Fallback · Test planı · Acceptance criteria · Migration planı · Rollback planı · Uygulama sırası · Bağımlılıklar · Riskler · Edge case'ler · Done definition

Ek zorunluluklar (sözleşme Adım 1): mevcut-varlık eşlemesi (KALIR/YENİ/DEĞİŞİR) · adım-başı "çalıştır → şu çıktıyı gör" doğrulama komutu · Opus-devralma netliği · ⛔ kritik kararlarda "eldeki en güçlü model + CEO onayı" protokolü.

## DURUM TABLOSU

Durum değerleri: `—` başlanmadı · `YAZILIYOR` · `✓` tamam (derinlik şablonu tam + doğrulama komutlu + done-definition var).

### İntake (Adım 0 — tamam)

| # | Dosya | Durum | Not |
|---|-------|-------|-----|
| i1 | 00-CEO-DIRECTIVE-BEKLENTILER.md | ✓ | Sanitized (şifre 0 eşleşme); orijinal gitignore'da |
| i2 | 00-INDEX.md | ✓ | Bu dosya — dalga başı/sonu güncellenir |
| i3 | 00-CEO-DIRECTIVE-MUST-ROSTER.md | ✓ | 2026-07-12: external solo audit + CEO order — MUST discovery binding |
| i4 | WORKFORCE-MUST-EXPANSION-PLAN.md | ✓ | E5.7: 12-col revenue-engine matrix, +19 MUST roster → 198, commerce dept — **EXECUTED 2026-07-12, sync 199, engines 4/4 owned** |
| i5 | DEPUTY-FAILOVER-MAP.md | ✓ | E5.7e (audit F3): SPOF set + dept heads + D7-wave deputies; takeover protocol; E12.5 gate artifact |
| i6 | 00-CEO-DIRECTIVE-REVENUE-FIRST.md | ✓ | 2026-07-17: Talep (MUSTS) + CEO kararları D1-D9 + dış denetim (Codex F-01..15) adaptasyonu — Dalga 6 + R-serisi kaynağı |

### Dalga 1 — Temel

| # | Dosya | Direktif kaynağı | Durum |
|---|-------|------------------|-------|
| 1 | MASTER_PLAN.md | §14 + madde 17 (faz sırası) | ✓ |
| 2 | SYSTEM_ARCHITECTURE.md | §15 listesi + madde 1 | ✓ |
| 3 | DATA_MODEL.md | §14 (veri modeli/tablolar/ilişkiler) | ✓ |
| 4 | BACKUP_PLAN.md | §16 birebir + Opus devir | ✓ |

### Dalga 2 — Kontrol düzlemi

| # | Dosya | Direktif kaynağı | Durum |
|---|-------|------------------|-------|
| 5 | CEO_COMMAND_CENTER_SPEC.md | §§1-39 + madde 1-5 | ✓ |
| 6 | DESIGN_SYSTEM.md | §32 | ✓ |
| 7 | SETTINGS_AND_CONTROL_SPEC.md | §18 + madde 6.1-6.4 | ✓ |
| 8 | MODEL_ROUTING_SPEC.md | §19 + madde 6.1 | ✓ |
| 9 | OBSERVABILITY_SPEC.md | madde 10.1-10.6 | ✓ |
| 10 | APPROVAL_ENGINE_SPEC.md | §21 + madde 11 + B7b (para-çıkışı kapısı KALIR) | ✓ |
| 11 | COST_CONTROL_SPEC.md | §20 + madde 10.5 | ✓ |

### Dalga 3 — Organizasyon + ajanlar

| # | Dosya | Direktif kaynağı | Durum |
|---|-------|------------------|-------|
| 12 | ORGANIZATION_ENGINE_SPEC.md | §15(org) + madde 5.3 | ✓ |
| 13 | AGENT_ORCHESTRATION_SPEC.md | madde 5.2 + 18 | ✓ |
| 14 | FABLE_5_HOOK_SPEC.md | madde 7 (Intelligence & Discipline Hook) | ✓ |
| 15 | EMPLOYEE_PERSONA_STANDARD.md | madde 8 (persona + sicil; CEO emri: tümü Fable yazar) | ✓ |
| 16 | HR_OPERATING_SYSTEM_SPEC.md | madde 9 | ✓ |
| 17 | PERMISSION_MODEL.md | §15 listesi | ✓ |
| 18 | HOLDING_OS_PRODUCT_SPEC.md | madde 1 (doğru tanım) | ✓ |

### Dalga 4 — Platform kontratları

| # | Dosya | Direktif kaynağı | Durum |
|---|-------|------------------|-------|
| 19 | API_CONTRACTS.md | §15 | ✓ |
| 20 | EVENT_MODEL.md | §15 | ✓ |
| 21 | WORKFLOW_ENGINE_SPEC.md | madde 6.4 | ✓ |
| 22 | MEMORY_ARCHITECTURE.md | §15 | ✓ |
| 23 | HOLDING_LIBRARY_SPEC.md | madde 13 | ✓ |
| 24 | PROJECT_OPERATING_SYSTEM_SPEC.md | madde 12 + §23 | ✓ |
| 25 | SECURITY_MODEL.md | madde 4 (ertelenmiş-sertleştirme sicili; yeni bürokrasi YOK) | ✓ |
| 26 | AUDIT_AND_LOGGING_SPEC.md | madde 10.2-10.4 | ✓ |

Dalga 4 kapanış notu: spec'lerin "kayıtlı ek"leri (yeni kolon/tablo/fn envanterleri) DATA_MODEL §20 tablosuna, API_CONTRACTS 8b envanterine ve EVENT_MODEL §9b kataloğuna aynı dalga içinde işlendi — devirli satır açık kalmadı.

### Dalga 5 — Teslimat

| # | Dosya | Direktif kaynağı | Durum |
|---|-------|------------------|-------|
| 27 | IMPLEMENTATION_ROADMAP.md | madde 14 (uygulama sırası + Opus devralma noktaları) | ✓ |
| 28 | TEST_STRATEGY.md | §15 | ✓ |
| 29 | ACCEPTANCE_CRITERIA.md | §38 + modül-başı | ✓ |
| 30 | RISK_REGISTER.md | §15 | ✓ |
| 31 | RECOVERY_AND_ROLLBACK_PLAN.md | §15 + §16 | ✓ |

### Dalga 6 — Revenue-First genişlemesi (CEO Talep 2026-07-16/17)

| # | Dosya | Direktif kaynağı | Durum |
|---|-------|------------------|-------|
| 32 | REVENUE_ENGINE_SPEC.md | [[00-CEO-DIRECTIVE-REVENUE-FIRST]] (Talep §§2-4, 7; D1-D4) | ✓ 2026-07-17 |
| 33 | VOICE_INTERACTION_SPEC.md | Talep §5 + D3 (v1 çağrı hattı; boardroom "add-later") | ✓ 2026-07-17 |
| 34 | CAPABILITY_ARSENAL_DOCTRINE.md | CEO emri 2026-07-18 00:20 ("alet edevatın HEPSİ nakşedilsin") + 00:30 paid-bench hükmü; denetim F-07 kapanışı — roadmap R4.3 | ✓ 2026-07-18 |
| 35 | CEO_OPERATING_MANUAL.md | U13 (Fable gap-analizi → CEO forward 2026-07-17) — roadmap E13.3; TR ana + EN kanonik ayna (U13 dil istisnası); library sop `ceo-operating-manual` (0d530051); E13.2 göz-testi oturumuna eşlik eder | ✓ 2026-07-18 |

**KORPUS 31/31 ✓ (2026-07-10 ~23:00).** Sözleşme Adım 2 yürürlükte: execution ayrı onay İSTEMEZ — sıradaki iş IMPLEMENTATION_ROADMAP E1.1'den başlar (Fable bizzat, 12 Temmuz son geceye kadar; sonra Opus ilk ✓'siz adımdan). Canlı ilerleme işareti artık IMPLEMENTATION_ROADMAP adım tablosundadır.

## Kayıtlı uyarlamalar (sessiz sapma yasak — master-plan-fidelity)

| # | Uyarlama | Gerekçe | CEO görünürlüğü |
|---|----------|---------|-----------------|
| U1 | Port 3100 → 3000 | Sistem 3000'de canlı; direktifteki 3100 yazım kaynaklı | Bu tablo + sözleşme Adım 0 |
| U2 | Dalga sırası: 31 dosya 5 dalgada, dalga=atomik commit | 12 Temmuz kesinti riski — kesinti kayıpsız devam için | Sözleşme Adım 1 (CEO onaylı plan) |
| U3 | Kadro kapsamı G7: `agency-agents/` legacy 153 + Fable'ın olmazsa-olmaz ekleri; persona = canlı çalışan | CEO sözlü ek hükmü 2026-07-10 ~21:35 (spec'lere dışarıdan işlendi, Fable devraldı) | EMPLOYEE_PERSONA_STANDARD G7 + HR §27 + bu tablo |
| U4 | JARVIS voice (eski Faz 9, 09-03..05) "sistem sonrası" dilimine ertelendi — CEO Faz 1-11 listesinde yer almıyor. **Refined 2026-07-17:** spec artık VAR ([[VOICE_INTERACTION_SPEC]] #33); v1 ücretsiz çağrı hattı = roadmap R3.1; Moderated boardroom D3 gereği "add-later" (spec §3.4, execution satırı YOK). **Refined 2026-07-17 (2):** always-on wake daemon SHIPPED as R3.2 (spec §24bis — "Selamaleykum ya Hamza" line live on the CEO terminal via systemd user units); U4 deferred remainder = Moderated Boardroom ONLY | BEKLENTİLER pivotu; sonra CEO Talep §5 + D1/D3 kararları | MASTER_PLAN §6 + VOICE_INTERACTION_SPEC + bu tablo |
| U5 | Tip ölçeği utility ADLARI: kontrat "display"→`text-display-lg`, "body"→`text-body-md`; radius utility'leri semantik (`rounded-input/panel/modal`) | Legacy cockpit `@theme` anahtarlarıyla (--text-display, --text-body, --radius-sm/md/lg) çakışma — legacy izolasyonu (DESIGN_SYSTEM §22); px DEĞERLERİ kontrat birebir | tokens.css başlık yorumu + bu tablo |
| U6 | Görsel yön güncellemesi: IRON MAN / JARVIS HUD hissi (holo-glow serbest, champagne kimlik + obsidian zemin KALIR; §35 neon-çizgi yasağı gevşedi) + login↔shell tema birleşmesi (E2.4-b) | CEO sözlü RET + yeni referans 2026-07-11 ~00:40 ("Iron Man'deki gibi istiyorum, bunu beğenmedim") | phase8-design-brief memory A3 + bu tablo + DESIGN_SYSTEM uygulaması sıradaki design pası |
| U8 | CC-SPEC §7 rota tablosuna +1 sayfa: Intelligence grubuna `/ai/mcp` (MCP Servers) eklendi; TR/EN dil anahtarı command bar'a kondu (A2 gereği); approvals boş-durumu Command diline geçti ("Back to cockpit" kalıntısı öldü) | CEO göz-testi emirleri 2026-07-11 ~02:55 ("MCP'ler nerede?", "TR/EN dil butonu nerede?", "rengi eskiden kalma") | Bu tablo + commit + canlı UI |
| U9 | Revenue-First dalgası (D1-D9): free-first büyüme, ilk hedef €50, voice v1 = orkestratör çağrı hattı (boardroom ertelendi), İslami ton persona direktifi, fable-method DNA onayı, Claude ekosistem + masaüstü-25 library taraması | CEO Talep 2026-07-16 + karar turu 2026-07-17 | [[00-CEO-DIRECTIVE-REVENUE-FIRST]] + bu tablo + R-serisi roadmap |
| U10 | `revenue_ledger.engine` donmuş CHECK enum → `revenue_engines` tablosuna FK (motor=veri; yeni motor migration istemez) | Talep §7.2 "engines are a floor, not a ceiling"; keşif mimarisi şartı | REVENUE_ENGINE_SPEC §0/§11 + migration 0028a |
| U11 | Persona şablonu 11 → 12 bölüm: `## 12. Discipline DNA & Islamic conduct` anayasal bölümü (tek tip kanonik metin — rol-özgü-derinlik kuralının kayıtlı TEK istisnası) her personaya eklendi; mekanik kapı bölüm-yoksa-FAIL | CEO D5 (İslami ton) + D6 (fable-method DNA) + Talep §5.12 "constitutional, not optional" — madde 8'in 11-bölüm şekline CEO'nun YENİ hükmü ekleme yaptı | EMPLOYEE_PERSONA_STANDARD G8 + §4.1 + roadmap R1.8 |
| U12 | AGENT_ORCHESTRATION §5 "SDK runner (`packages/orchestrator/runner`) YENİ" satırı İPTAL (denetim F-05 kararı): runner ayrı paket olarak DOĞMADI — spec'in runner sorumlulukları üç mevcut parçada yaşıyor: worker-shim (oturum+kayıt), worker-loop (residency, R2.1), hook-binding (kapılar). Workflow executor R2.3'te AYNI anayasaya bağlandı (paylaşılan parçalar tek implementasyon: gateway `buildSdkToolOptions` + hook `extractEvidencePackage`/flag + observability `resolveEvidenceToolCalls`; kernel agent step preTask/postTask + REVISE döngüsü + kanıt çapası). Kontrol sinyalleri (`agent_runs.control_signal` pause/cancel) hâlâ açık — spec §4 hükmü duruyor, ayrı satır | Denetim F-05 "spec'teki runner ile gerçek kod eşleşmiyor — karar kaydı yok"; iki spawn yolunun kapı/kayıt/araç anayasası R2.2-R2.3'te birleşti | AGENT_ORCHESTRATION_SPEC A9 tablosu + roadmap R2.3 + tests/r23 (tek-implementasyon referans-eşitlik testi) |
| U13 | **CEO Operating Manual** roadmap'e kayıtlı satır olarak girdi (E13.3): tek kaynak `CEO_OPERATING_MANUAL.md`, dashboard'dan erişilir (kind=sop intake), yazım E12.3 route-gate SONRASI, E13.2 göz-testi oturumuna eşlik eder. **Dil istisnası (language-directive registered exception):** kılavuz TR ANA metin + EN kanonik ayna — gerekçe: belgenin tek okuru CEO (TR); İngilizce-artifact kuralının amacı (yazar-devamlılığı) EN aynayla korunur | Fable 5'in kendi gap-analizi (CEO'nun masaüstü .odt'sine gömülü görüntüler, item 24) + CEO forward/onayı 2026-07-17 ("eksiklerin giderilmesini istiyorum") | Roadmap E13.3 + bu tablo + [[00-CEO-DIRECTIVE-LANGUAGE]] istisna kaydı |
| U14 | **Eğitim departmanı KURULMAZ (şimdilik)** — masaüstü item 10 kararı CEO tarafından Fable'a delege edildi (2026-07-17), hüküm: ayrı ORG birimi pre-revenue overhead; işlev ZATEN karşılanıyor (people-hr `Corporate Training Designer` rolü + Holding Library `training` rafı — R4.1'de 1→75 kayıt). Yeniden-değerlendirme tetiği: ilk kâr (O1 €50) + eğitim-içerik hacmi departman eşiğine ulaşınca. Operations departmanı sorusu da kapandı: CEO kararı "hayır — ops-unvanlı uzman roller yeterli" (mevcut: executive-operations-manager + studio-operations) | CEO delegasyonu + kararı 2026-07-17 ("senin zekana bırakıyorum" / "hayır, ops unvanlı uzman roller varsa tamamdır") | Bu tablo + R4.1-LIBRARY-INTAKE-REPORT org bölümü |
| U7 | Dashboard Kurtarma D-bloku E4'ün ÖNÜNE alındı; R-kapısı süreci eklendi (22 referanslık pano → CEO seçimi) ve CEO **reçete C — DXB Hibrit**'i seçti (B-malzemesi: şampanya+obsidyen lüks zemin; A-derinliği: JARVIS holo-glow yalnız canlı veri/seçimde; mobil R12/R14 + voice R21 hedef yüzeyler). Bağlantı kontratı: ikon/veri/drill eşlemesi `command-nav.ts`+`module-live.ts` tek kaynak | CEO canlı emri 2026-07-11 ~01:00 ("önce internetten örnek sunun" + "her şey bağlantılı olacak" + 01:56 "c-hibrit seçeneği aynen uygula") | `references/design-direction/` (pano + seçim) + bu tablo + STATE.md + commit'ler 25ae9f6, 3ac7444 |
| U15 | **UPDATE 2026-07-25: machine blocks 1-7 EXECUTED (ticket 20260725-u15-voice-remediation) — wake TR-lock, {tr,en} script gate, crash-root fix (fail-soft boot) + stale-line takeover, progress/clarify cues, ladder low-confidence-bump adaptation, D7 measured closed; daemon re-enabled + boot-proven. ONLY OPEN LEG: block 8 joint CEO ear test.** Original: R3.2 voice line REOPENED as ◐ — live-defect ledger D1–D8 OPEN, remediation DEFERRED ("at the end we MUST solve it"): wake greeting garbled to EN garbage (the CEO's selam went unanswered, 19:22), Korean mistranscription, 8/14 calls failed (line_busy + empty_transcript, two eras: dashboard leg + daemon leg), STT 80–444s, retry-same-tier L1 ladder BLOCKED 10 tasks in one day (only finance answers), garble→task leak, phantom task d1bd0c59. Full ledger + ordered remediation blocks + joint-CEO-test protocol: [[00-NOTE-R32-VOICE-REMEDIATION-PLAN]] — **MANDATORY read when voice work reopens or any session reaches E13.x** | CEO ruling 2026-07-17 ~22:05 in-session: "voice can wait now — write it somewhere visible; at the end we must solve it; future sessions must see this plan" | This table + the 00-NOTE + roadmap R3.2 ◐ + STATE.md  <!-- OPEN: B03 --> |
| U17 | **E12.5 activation machinery adaptations (CEO rulings D10/D11, 2026-07-18):** (1) `fn_hr_machine_intake` BORN — HR spec §4 had NO machine entry for pre-machine `dormant` stock (fn_hr_create_employee fails on existing slugs); the new fn writes the same three equipment rows as create-steps 4-6 verbatim and moves `dormant→draft`, honoring spec :145 ("scripted but through the same fns; no off-ledger path" — a mass-UPDATE migration would have violated it). (2) `tasks.due_at` added (D10 fourth field; 7-day default SLA, probation window from `hr.probation_max_days`). (3) Probation score = DERIVED formula `succeeded/total agent_runs` of the probation task (daily_kpi precedent, jobs.ts) — manual scores forbidden at scale (F-09 class). (4) Probation task EXECUTION path measured legal without code change: task is born pre-staffed (`agent_id` set) so worker `assignEmployee` early-returns, and neither claim nor hook checks `employment_status` (worker-shim:409 gate only staffs agent-LESS tasks). (5) Named roster exclusion: `agents-orchestrator` (infrastructure identity, fails e1 by design — never an activation target). (6) Per-employee LiteLLM keys generated INSIDE the proxy container (its own env; alias-only in DB, key material never leaves the proxy) | CEO in-session answers 2026-07-18 ("karar A Evet, karar B 1.seçenek") to the two OPEN MUSTS questions | [[00-CEO-DIRECTIVE-REVENUE-FIRST]] §3-bis + migration 20260718090000 + scripts/hr/activate-workforce.sh + this table |
| U18 | **E12.5 go-live infrastructure repairs (2026-07-18, mid-activation — found+fixed):** (1) activate-workforce keys phase had `docker exec` WITHOUT `-i` — the heredoc stdin never reached the container python, so the first live run (CEO's terminal) failed 197/197 with empty `http=`; fixed + a diagnostic except-branch added (a keygen failure now names its exception). (2) **Department LiteLLM key layer BORN**: worker api-path model calls read `DXB_LITELLM_KEY_<DEPT>` (litellm.ts COST-02) but only `dxb-os` existed — every first-rung api run failed and the ladder always fell through to the subscription path. 21 `dxb-<dept>` virtual keys provisioned via `scripts/litellm/provision-dept-keys.sh` (CEO ran it; €25/30d each from the `department.budget_eur` registry default; key material lands in gitignored `.env.daemon` only, never printed). (3) **std 15 × D11 structural collision resolved**: a toolless run (api path, or empty compiled profile) cannot write `tool_calls` rows, yet the global `tool_call_proof` policy demanded that anchor — post-gate now waives ONLY the anchor when the executor MEASURED the surface absent (`WorkerOutput.toolSurfaceMounted=false` → `HookCtx.toollessRun`); the verification-kind-evidence requirement itself stays; an undeclared flag keeps std 15 fully strict (custom/test executors unaffected). (4) **U17(3) score formula refined**: the post-gate IS the examiner — a probation task reaches 'done' only through a post-gate PASS run, so the derived score is 1.0 on done (runs/oks ratio rides the evaluation note for audit only); the original succeeded/total-runs ratio measured ROUTING (ladder depth), not the employee, and handed 0.2 unfair FAILs after infra retries; manual scores stay forbidden (F-09). (5) **L3 ladder repair (2026-07-18 ~15:00, next-session candidate honored)**: worker-shim defaultExecutor resolves the model as the highest-priority enabled routing_rules row for the task's TIER (task_class not consulted; tie-break updated_at DESC) — L3's winner was code.bulk/kimi-2.7-code, measured 4/52 succeeded runs on the live probation class while 177/180 queued tasks were L3; migration 20260718120000 raises the L3 sonnet-5 row to priority 20 (subscription mode — L3 lane leaves per-token API spend), kimi row stays enabled at 10 as the next rung; class-aware resolution = post-wave decision if ever needed | E12.5 activation blocked mid-wave: every api-rung probation run ESCALATEd on std 15/std 4, polluting the U17 derived scores with infra retries (runs=5 ok=1 → 0.2 unfair FAIL); then the L3 lane fed 92%-fail kimi runs into the ladder | packages/hook/src/post-task.ts + packages/orchestrator/src/worker-shim.ts + scripts/litellm/provision-dept-keys.sh + supabase/migrations/20260718120000_e125_l3_ladder_repair.sql + this table |
| U16 | **Small design work DEFERRED by the same ruling**: CEO observed symmetry defects he dislikes (surface list to be captured at the U15 joint walkthrough); open item for the next design slot. RULE #0 discipline unchanged for any surface that changes meanwhile | Same CEO ruling 2026-07-17 ("ufak tefek design işleri beklesin") | This table + [[00-NOTE-R32-VOICE-REMEDIATION-PLAN]] companion section |
| U19 | **Outleteuro pilot CANCELLED entirely (CEO order 2026-07-19, given twice: in-chat + şikayet.odt item 2 "Outleteuro projesini şuan kaldırın tamamiyle")**: the holding must first run excellently at low cost; when that is proven, the company itself executes Outleteuro on a future CEO intent — it leaves the project scope NOW. Effects: (1) E13.1 closure gate loses its F-15 leg (Outleteuro pilot-readiness attestation struck — attestation of a cancelled pilot is meaningless); F-09 production-proof leg UNCHANGED. (2) R6.1 (real provider integrations) was Phase-11-gated; with Phase 11 cancelled it re-gates to "opens only on a future CEO order" — the row stays visible as debt, no per-provider rows are born. (3) MASTER_PLAN Phase-11 text stays as historical record; this U-row is the governing disposition. (4) Audit F-15 disposition (REVENUE-FIRST directive table) is superseded by this cancellation: final product status label = **"Holding OS foundation complete; real business pilot cancelled by CEO — future intent reopens it"** (the audit's honest-labeling requirement is satisfied by this exact label). **Companion: CEO complaint ledger C1–C19 registered same night** — [[00-NOTE-CEO-COMPLAINT-LEDGER-2026-07-19]] (19 defect/decision/question items + 6 standing orders incl. orchestrator="Hamza", OpenRouter=low-cost-models-only, list-page standard, plain-language standard); remediation waves start after E13.1 closes | CEO orders 2026-07-19 ~00:30–00:50 (chat + şikayet.odt) | This table + [[00-NOTE-CEO-COMPLAINT-LEDGER-2026-07-19]] + IMPLEMENTATION_ROADMAP E13.1/R6.1 rows |
| U20 | **İnşaat yazarlığı Fable 5 → OPUS 5 (CEO emri 2026-07-25) + yedek model katmanı KALDIRILDI.** (1) İleriye dönük her yetki/kural cümlesi artık Opus 5'i işaret eder: plan yazımı, repo'ya giren her satır (K1), tüm personalar (K2), modül kapanışları, commit öncesi diff okuma + final verdict. `model-routing-hierarchy` **v9**; memory dosyası `fable-5-construction-governance.md` → `opus-5-construction-governance.md` (repo aynası `.planning/governance/` senkron). (2) **Yedek katman yok:** v6'nın "Fable bizzat → en kötü Opus 4.8" zinciri ve v5 bütçe-fallback modu İPTAL; tek beyin Opus 5, hata/timeout'ta sessiz alt-model düşüşü yerine `blocked` raporu CEO'ya. Katalogda `claude-opus-4-8` → `status='retired'`; A-2026-07-19 madde 2'nin Opus-4.8 bacağı SÜPERSEDE (Codex 5.6 bacağı dokunulmadı). (3) **Tarihsel kayıt dokunulmaz** (CEO kararı): persona `Created by: fable-5`, canlı `persona_version='v2.0-fable'`, uygulanmış migration'lar, kanıt notları AYNEN kalır — inşaatı 2026-07-06→07-25 arası gerçekten Fable 5 yaptı; değiştirmek §35 sıfır-uydurma ihlali olurdu. (4) **İç teknik ID'ler korunur** (CEO kararı): `model_catalog.id='fable-5'`, `orchestrator.fable_review_required`, `fable-final` basamağı, `FABLE_5_HOOK_SPEC.md` dosya adı, persona §11 başlığı (kapı bu başlıkla eşleşir) — yeniden adlandırma canlı FK zincirini, ayar undo zincirini ve 196-persona kapısını kırardı, CEO'ya görünür faydası sıfır. **CEO'ya GÖRÜNEN her etiket Opus 5'tir** (`model_catalog.display_name` + i18n değerleri). (5) Bilinen kozmetik sınır: persona dosyalarında `## 11. Fable 5 hook binding` başlığı ve `Created by: fable-5` satırı duruyor — kasıtlı (madde 3+4), dashboard bu başlığı birebir render etmiyor | CEO emri 2026-07-25 ("projenin heryerinde Fable 5 geçen yerler Opus 5, 4.8 geçen yerler 5 olsun") + aynı turda alınan 3 kapsam kararı (geçmiş korunur / görünen isim değişir, ID kalır / yedek katman kalkar) | Bu tablo + [[MODEL_ROUTING_SPEC]] A-2026-07-25 + MASTER_PLAN §2 model zinciri + `.planning/governance/model-routing-hierarchy.md` v9 + `.planning/quick/20260725-fable-to-opus5-rename/` |
| U21 | **QUALITY TIER LAW — runtime model roster restructured (CEO order 2026-07-26).** Normative text: [[MODEL_ROUTING_SPEC]] §4d. (1) **The law:** judgment, taste, structure and every output a human (CEO or customer) sees is produced by Opus 5; a lower tier's output is never a finished product, only the higher tier's input. The dividing line is NOT cost and NOT code-vs-text — it is "does this work carry taste, structure or a human-visible result". (2) **Sonnet leaves every critical seat:** chat.answer, voice.answer, research.synthesis, dept-head.planning, content.outbound, memory.promote and code.standard all move to L1 Opus 5. Site/store build, design and video direction are L1 **permanently** — a cheaper coder model arriving later does not reclaim them (CEO: "site mağaza kurulumunu sen yapacaksın tasarım vs … videolarda alt yazı başka model ama videoların tasarımı vs sen"). Sonnet keeps L2 (mechanical backend code), L3 (gathering/drafting) and L4 (classify/extract/transcribe, the local model's reserved seat). (3) **Eight models dismissed** (`status='retired'` + `banned=true`, never deleted — U20 precedent): haiku 4.5, DeepSeek V4 Flash, Kimi 2.7 Code, Kimi 3, GLM 5.2, Qwen 3.6 Flash, MiniMax M3, Codex 5.5. Second, independent reason: every one was an OpenRouter `api` row and that balance is exhausted, so none could execute. DeepSeek V4 Pro survives as `mechanical_only` (bulk reads only, may never produce a verdict). (4) **§4b routing-assignment pass RUN:** all 205 agent brains left the `glm-5.2` placeholder (`brain_source='default'` → `'slot'`); 22 orchestrator/director rows = Opus 5, 183 = Sonnet. The dashboard no longer shows a fired model as an employee's brain. (5) **New invariant — tier homogeneity:** `worker-shim` resolves a model by `model_tier` alone, so two models on one tier is now a defect, not a preference. (6) **Voice `effort` un-hardcoded:** `packages/voice/src/answer.ts` read a constant `"low"` and silently overrode the routing row — the one lane a CEO dashboard change could not reach. (7) **GPT/Codex lane NOT opened by the API key** (measured): the key authenticates (HTTP 200, 117 models incl. `gpt-5.6-sol/terra/luna`) but every completion returns `429 insufficient_quota` — OpenAI API billing is separate from the ChatGPT subscription. The working subscription path is the Codex CLI (logged in, answered live as `gpt-5.6-sol`); wiring it is separate work. (8) **Critical gate redefined — [[MODEL_ROUTING_SPEC]] §4e** (CEO order 2026-07-26, repeated and closed with "bunu unutma sakın"): Solo 5.6 + GPT 5.5 serve as CHALLENGERS of Opus 5's work, not as co-authors. Opus 5 writes → challengers try to refute → Opus 5 revises and signs, objections and their disposition recorded. Fires only on money/reputation/direction-binding L1 classes via the existing `needs_council` flag. Transport = Codex CLI subscription mode (verified live), not the API key. The v1 `CNCL-01` shape (three cheap producers + comparison-only judge) is DEAD — all three producers are dismissed models and the design kept the strongest model out of authorship. (9) Eye-test caught in the same turn and fixed: five CEO surfaces (employees, directors, org-tree drawer, portfolio brain chips + model picker) rendered the raw catalog id, so the CEO read "fable-5" on his own workforce page after U20 moved every visible label to `display_name` — new shared resolver `apps/dashboard/src/lib/model-names.ts`; employee slugs also stopped breaking mid-word. Test debt paid in the same turn: four tests asserted the pre-U21 roster as truth (`codex-5.5`, `claude-haiku-4-5`, live `brain_source='default'` counts, `code.standard`=L3) and were rewritten to self-fixture or read live rows | CEO orders 2026-07-26 in chat ("ana hedef KALİTE!!! her işte!!! zeka!!!" · "sonnet şuanlık kritik işlerde asla olmamalı … sadece angarya işlerinde" · "site mağaza kurulumunu sen yapacaksın tasarım vs") | Bu tablo + [[MODEL_ROUTING_SPEC]] §4d + `supabase/migrations/20260726001000_u21_quality_tier_law.sql` + `packages/kernel/policy/routing-seed.json` |
| U22 | **THE BRAIN IS A FLOOR — `agents.brain` becomes load-bearing (CEO decision 2026-07-26).** Normative text: [[MODEL_ROUTING_SPEC]] §4f. Trigger: the CEO read his workforce page after U21 and asked *"183 sonnet nedir?"*. Measured answer: the column decided NOTHING — `fn_select_model` never read `agents.brain`, no TypeScript router read it, and `worker-shim` resolved by `model_tier` alone; the §4b agent-brain contract specified 2026-07-12 had never been built. Offered A (make the column honest — "task-based") vs B (make it real), the CEO chose **B**. (1) **The rule:** the brain may RAISE a task's tier and may NEVER lower it — a director's routine gathering runs on Opus 5; a Sonnet-brained specialist writing outbound content still runs on Opus 5 because §4d puts that class at L1 and the class always wins. (2) **Ordering lives in one place:** new `model_catalog.tier_floor` (L1 best … L4) + `fn_tier_rank` (unknown → 99) + `fn_effective_tier(task_tier, agent_id)`. `quality_score` was rejected as the source: it is NULL on both Opus 5 and Sonnet. (3) **Fail-open by design:** unassigned task, `brain_source='default'` placeholder, unrated/retired/banned model, or a raised tier with no enabled row — all leave the task at its own tier. A floor is an upgrade path, never a new failure mode. (4) **§4b step 0 built:** a `ceo_override` brain wins the slot lane outright after the same guardrails, logged as `selected_ceo_override`; a `slot` brain does NOT hijack that lane (derived ≠ override); a banned or mechanical-only override falls through to the rule scan with the refusal written into `considered`. (5) Executor seam extracted as `resolveExecutionRoute()` so the wiring is provable without paying for a live model call. (6) The employees page header now carries the meaning in both locales — the CEO should not have to read a spec to know what a column means | CEO chat 2026-07-26, option B chosen after a measured A/B | Bu tablo + [[MODEL_ROUTING_SPEC]] §4f + `db/migrations/20260726002000_brain_floor.sql` + `tests/c9/brain-floor.test.ts` (14 cases) + `.planning/quick/20260726-brain-floor/` |
| U23 | **THE CRITICAL GATE IS BUILT — challengers now actually run (2026-07-26).** §4e stopped being a written intention: `packages/orchestrator/src/critical-gate.ts` drives Solo 5.6 + GPT 5.5 over the **Codex CLI subscription lane** (`--ephemeral -s read-only --skip-git-repo-check --output-schema`, temp cwd — a challenger reasons about text and may never touch this repo). Two measured traps recorded: stdin must be closed or the CLI blocks forever on "Reading additional input from stdin…", and the final message must be read from `-o <file>` because stdout carries CLI chatter. **Runtime seam:** `runWorkerOnce`, immediately after the QA hook returns PASS — the hook asks *does this meet its contract*, the gate asks *is it WRONG* — with objections returning as exactly ONE revision round through the existing `feedback` channel, so the author revises and signs. Switch = the routing row's `needs_council` flag (CEO-configurable as data). **Failure policy:** challengers never throw; a dead panel logs `unavailable` and the work proceeds, because a gate that can halt the company on a subscription hiccup is a worse failure than a missed review; off-schema verdicts are refused rather than believed. **LIVE PROOF:** a deliberately flawed recommendation (HunyuanVideo 13B at bf16, "~14 GB VRAM, fits the 16 GB card", "open weights so commercial use is fine", "€0/month") went through the real panel — both challengers returned `flawed` with **7 sourced objections**: the VRAM figure is wrong (13B at bf16 ≈ 26 GB of weights alone; Tencent's README specifies 45-60 GB), open weights ≠ commercial rights (Stable Video Diffusion gates commercial use on revenue), the "best motion quality" claim rests on a table where it was the only open model, and the €0 figure never prices the remedy if the model does not fit. `decision_log` id 20251 | CEO order 2026-07-26, given twice and closed with "bunu unutma sakın" | Bu tablo + [[MODEL_ROUTING_SPEC]] §4e + `packages/orchestrator/src/critical-gate.ts` + `tests/c9/critical-gate.test.ts` (8 cases) |
| U24 | **HAMZA'S TWO LEGS BUILT (CEO directive 2026-07-25, shipped 2026-07-26).** Normative text: [[VOICE_INTERACTION_SPEC]] §24ter. The CEO asked for two conversations — money/plans/projects, and the daily "how are we doing" report — and guessed they needed "farklı beyinler". **Resolved as different BEHAVIOUR on the same brain:** §4d puts anything the CEO reads on Opus 5, so both legs are L1; splitting them onto different models would have put the report he reads every morning below the ceiling. `chat.strategy` (effort `max`, takes a position) and `chat.brief` (effort `medium`, and handed a LIVE snapshot). **The brief leg may not remember figures — it is given them:** `buildBriefSnapshot()` reads `v_exec_overview`, the revenue ledger, opportunity count and open alerts live, renders them in the CEO's language, and the prompt permits no other numbers; a failed query drops its line rather than inviting an estimate (RULE #0-A applied to Hamza himself). **Routing is one-directional:** anything smelling of money or planning goes UP — `classifyLeg()` defaults to strategy and only routes to brief on an explicit report/status marker, in both languages, because the CEO switches mid-sentence. **Standing framing rule now carried in the prompt:** empty tables are NOT a defect — the CEO deliberately has not started the money leg, and he has corrected that framing twice. Fallback chain leg → `chat.answer` → `voice.answer` → `orchestration`, every rung L1, so a fallback cannot quietly downgrade the CEO's conversation | CEO directive 2026-07-25 ("2 ayak var…") | Bu tablo + [[VOICE_INTERACTION_SPEC]] §24ter + `db/migrations/20260726003000_chat_legs.sql` + `packages/orchestrator/src/chat-legs.ts` + `tests/c9/chat-legs.test.ts` (13 cases) |
| U25 | **THE MONTHLY BRAKE IS REAL (2026-07-26, W1.6).** Normative text: [[COST_CONTROL_SPEC]] §4bis. **Measured, after correcting my own first reading twice:** alerting was already COMPLETE — a `cost_ledger` trigger raises 70/90/100 rows (its 100% text says *"Non-critical work hard-stops"*) and `trg_alert_budget_stop` raises `budget-hard-stop` the moment the flag flips. What was MISSING was the writer: `budget_state.hard_stopped` had exactly one, the hand-run CLI kill-switch. **So the system told the CEO that work stops and then did not stop it** — the same class of defect as an invented number. `packages/outbox-executor/src/monthly-cap.ts` supplies only that writer and raises NO alert of its own (flipping the flag makes the existing trigger speak; a second voice for one fact is noise). On the existing 5-minute breaker tick: at 100% it writes `hard_stopped=true` FIRST, then best-effort blocks every non-critical LiteLLM key, then one `audit_log` row; the critical class (approvals, outbox, health, backup) keeps running. Spend = `cost_ledger` + LiteLLM spend logs month-to-date, and if the proxy is unreachable the ledger alone still governs — a monitoring outage may not silently disable the brake, and proxy-only spend never fires the ledger trigger at all. Two windows share the tick under `Promise.allSettled` so neither can mask the other. Release stays MANUAL — a month rolling over does not undo a stop the company earned. **Recorded incident the same hour:** the first version of the test wrapped its cases in a rolled-back transaction but called `getDb()` in its helpers, so every write went to the LIVE database on another connection — it tripped the real brake, left €105 of fake spend, and the trigger raised three real alerts off it. Cleaned within the minute (probe rows purged, flag reset, four alerts resolved with an explaining note) and recorded honestly as `audit_log` action `budget.hard_stop.reverted`; the trip itself was left in the log because history is not rewritten. `checkMonthlyCap(db)` is injectable BECAUSE of this | Roadmap W1.6; CEO order 2026-07-26 to finish the factory | Bu tablo + [[COST_CONTROL_SPEC]] §4bis + `packages/outbox-executor/src/monthly-cap.ts` + `tests/c9/monthly-cap.test.ts` (6 cases) |
| U26 | **CONVERSATIONS ON THE CHAT BOARD (W1.5, 2026-07-26).** Normative text: [[VOICE_INTERACTION_SPEC]] §24quater. The CEO named the gap himself — chat had no "new chat" concept. Measured: no session column, one flat 200-row board, and Hamza's context window was the newest 20 messages whatever they were about, so a question about video production carried the tail of a conversation about the budget. Now: `chat_sessions` + `chat_messages.session_id`; titles are the CEO's own opening line (never model-generated — a title is navigation, not a decision worth a model call) and the system authors NO title, because DB text is an i18n surface; `fn_chat_session_for_new_message` resolves the thread server-side and starts a new one after a 12-hour silence, so a client can never orphan a message. **The context window is scoped to the thread in BOTH lanes** — that is the half that protects answer quality; the navigation strip is only how the CEO steers it. **The voice mirror now joins a real conversation:** every mirrored spoken turn previously landed with no thread — 8 such orphans existed and were adopted. History preserved: the old board became one legacy thread timestamped from its own rows. **Two defects caught by the RULE #0 pass in the same turn and fixed before commit:** the new table shipped without RLS/grants so the dashboard saw nothing and the board rendered empty, and the adopted legacy thread carried the English literal "Earlier conversation" onto the Turkish board | Roadmap W1.5; CEO complaint about the missing "new chat" | Bu tablo + [[VOICE_INTERACTION_SPEC]] §24quater + migrations `20260726004000/004100/004200` + `tests/c9/chat-sessions.test.ts` (6 cases) |

| U27 | **THE CHAT BOARD COULD NOT ACCEPT A MESSAGE — CEO-reported, repaired 2026-07-26.** Normative text: [[VOICE_INTERACTION_SPEC]] §24quater-bis. The CEO found W1.5 broken in use (*"yeni konuşma basıyorum çalışmıyor"* · *"yeni konuşma segmesi yalancı"*), with three empty threads titled "selam" as the wreckage of three lost attempts. **Four faults:** (1) the 2026-07-19 migration granted INSERT **per column** and W1.5 added `session_id` without widening it, so the browser's role lost the write — RLS was fine, the table grant was the wall, and a column-only grant is invisible in `role_table_grants`; (2) the write was two statements, so a failed message kept the thread it had just minted; (3) `ChatBoard` was never re-mounted on a soft navigation, so "new conversation" opened with the old thread still on screen; (4) `if (res.ok)` had no else, so a 500 produced complete silence. **Fixed as shape, not as grant:** `fn_chat_post_message` writes thread+message in ONE statement and is now the ONLY door (direct INSERT revoked, dead policy dropped), so the next column added here cannot break the CEO's chat; the board is keyed by conversation and its Broadcast handler scoped to it; a failed send prints its reason and keeps his text. **The other half of the failure was operational:** the resident scheduler had run since 2026-07-25 17:16 while W1.5 shipped at 03:56, so Hamza's answers were written by the PRE-SESSION drain still in memory (`session_id` NULL). `dxb-scheduler` + `dxb-jarvis` restarted; the invariant moved into the schema (`session_id NOT NULL`) because a test cannot stop a stale process from writing. **Standing rule added: shipping runtime code is not shipping until the resident units are restarted in the same turn, and the restart is part of the evidence.** Design in the same turn (CEO: *"eski konuşmalar nerede… design'ı da düzeltin"*): the chip wall became a conversation column with its own scroll (name · size · last activity), the board keeps the remaining height so the composer is no longer below the fold, and an untitled thread borrows the CEO's own opening line via `v_chat_threads` — his 100-message history is named "selam hamza nasılsın" instead of "Başlıksız konuşma" | CEO live report 2026-07-26 05:40-06:10 | Bu tablo + [[VOICE_INTERACTION_SPEC]] §24quater-bis + migrations `20260726006000/007000/008000` + `tests/c9/chat-sessions.test.ts` (10 cases, 4 new, all as the `authenticated` role) |

| U28 | **THE DISCOVERY ENGINE IS LIVE — the holding's outside hands finally reached the world (W2.2, 2026-07-26).** Normative text: [[REVENUE_ENGINE_SPEC]] §7ter. Measured before: `opportunities` empty, `revenue.scan` an intake screen over an empty table, and the research tools installed for this exact job **never called once** (`tool_calls`: 240 dxb-mcp, 2 git, 1 context7, **0 scrapling**). No new design was needed — §5 already routed research through the department task queue and R2 activated on 2026-07-18; nothing had been connected to the seam. Built: `commissionScoutingRun` (ONE staffed Strategy research task, bounded by an open-run lock, a pipeline floor, a candidate cap and an off switch) + `harvestScoutingRuns` (findings become `opportunities` through the audited door, carrying the run, the URLs and the tools) + `revenue_scout_runs` ledger, both halves wired into the daily `revenue.scan`. Refusals are recorded (`no_evidence`, `unknown_engine`) so "found nothing" and "made something up" never look alike. **FOUR DEFECTS THE HOLDING'S OWN GATES CAUGHT, all mine:** (1) no project link — the pre-task hook refused it 5× and was right; the standing project `revenue-discovery` now ships as data. (2) The brief enumerated prohibited categories and the halal screen fail-closed the task on its own instructions (the CEO's "bahis" class); the boundary is now a rule, never a word list, with a mechanical test. (3) JSON-inside-JSON: four live runs fetched real pages then failed on shape; repaired by a line-block format that cannot collide with the worker envelope. (4) **The knowledge-shelf rule was structurally unsatisfiable** — the gate demands `ref` on file evidence while `WorkerEvidence` carried no `ref` field, so Zod stripped it before the gate could see it; that is the "knowledge_shelf deterministic loop" recorded open on 2026-07-24, root-caused and fixed, after which the next run passed on its FIRST attempt. A second catch the same hour: an intermediate brief lost the word "research" and the shelf gate silently stopped firing — dodging a rule by vocabulary is worse than failing it, so the contract declares itself and a test holds it. **LIVE PROOF:** task `ff956901`, one run, `scrapling.get ×2 + bulk_get ×3`, report filed on the shelf, **5 opportunities registered**, each €0 capital, `discovered`, `halal_verdict='pending'`, each citing 3 distinct fetched URLs | Roadmap W2.2; CEO standing order to build the factory | Bu tablo + [[REVENUE_ENGINE_SPEC]] §7ter + migrations `20260726009000/009100` + `packages/revenue/src/discovery.ts` + `tests/c9/discovery.test.ts` (12 cases) |
| U29 | **A TASK NOW HAS A HEADLINE — the live feed stopped reciting instructions (CEO-reported, 2026-07-26 11:10).** Normative text: [[CEO_COMMAND_CENTER_SPEC]] §9ter. The CEO sent a screenshot of Canlı Operasyonlar and the rail: *"canlı türkçe değil hala ingilizce"*. **Two defects behind one symptom.** (a) 009300 gave `tasks` a Turkish leg but only the RAIL was taught to read it — the Live Operations page, the Intelligence tail, the approval dock, the task grid, the project page, the approval detail and the decision log all still rendered the artifact leg. (b) The ugly half: **the feed's line WAS the task objective**, and a scout brief is ~1700 characters of instruction, so one feed row rendered a page of English text. Translating it would have been the wrong fix — a brief is not a label, and the language directive rightly keeps artifacts English. **The shape now matches the CEO's own mental model:** a task carries a SHORT HEADLINE in both languages (`tasks.label` / `label_tr`, the 30-minute-old `objective_tr` renamed because `label`/`objective_tr` was a name that lied about its job) and separately the long instruction it was given; the fallback chain (label → first LINE of the objective, never a mid-word cut, never an ellipsis) lives in the views so page, rail, tail and briefing cannot answer differently. Writers set both legs at creation — the scout because it knows the work, the intent intake because the CEO's own sentence needs no translation. **Two more cuts found and killed in the same pass:** `v_morning_briefing` built its two human lists with `left(objective, 80)` — a mid-word cut the Intelligence page then rendered inside `truncate`, producing exactly the visible "…" banned on 2026-07-18 — and the project page truncated task lines the same way | CEO screenshot 2026-07-26 11:10 | Bu tablo + [[CEO_COMMAND_CENTER_SPEC]] §9ter + migrations `20260726010000/010100/010200` + `tests/c9/feed-headlines.test.ts` (8 cases) |
| U30 | **CONSTRUCTION AUTHORSHIP: Fable 5 RE-AUTHORIZED alongside Opus 5 (CEO order 2026-07-26, "bu benim en son nihai kararım").** Amends U20's forward-pointing rules: every authority/rule sentence that reads "Opus 5" now reads **"the session's authorized author — Opus 5 OR Fable 5"**; whichever model drives the session is that session's sole author, gatekeeper and commit approver. (1) This is NOT a fallback chain — U20's no-silent-downgrade rule stands: one session = one author; on error/timeout the work surfaces as a `blocked` report to the CEO, never a quiet model switch. (2) K1 unchanged: every repo line is the session author's, written personally and inline; subagent authorship (including an Opus 5 / Fable 5-modelled subagent) remains a violation. (3) Historical records untouched and internal technical IDs preserved (U20 clauses 3-4 precedent). (4) Governance mirrors updated in the same turn: `model-routing-hierarchy` **v11** + `opus-5-construction-governance` (both in memory and `.planning/governance/`) | CEO order 2026-07-26 in chat ("şuan emrimdir. Fable 5 te aynı şekilde bu projeden sorumlu ve yetkilidir") | Bu tablo + `.planning/governance/model-routing-hierarchy.md` v11 + `.planning/governance/opus-5-construction-governance.md` |
| U31 | **THE CALL LINE SHARES THE COLUMN — /chat voice-open collision repaired (CEO-reported, 2026-07-26 ~12:00).** Normative text: [[VOICE_INTERACTION_SPEC]] §24quater-ter. The CEO's screenshot: opening Ses Hattı interleaved voice and chat. Measured root cause: the fixed-height chat column (U27 shape) received ~900px of `shrink-0` disclosure content, so the only flexible row — the conversation grid — was crushed to 42px and its thread list, board and composer spilled across the voice panel (rect-measured: 3 overlap pairs at 1366 AND 1920; screen size never rescued it). **Fix as shape:** the open panel is a BOUNDED region scrolling inside itself, bound = viewport remainder `clamp(12rem, 100vh − 38rem, 34rem)` on the content div (a bare `45vh` first cut passed 1366/1920 and was REFUTED by the 1280×800 windowed leg — the CEO's real window class — before commit), plus a 12rem grid floor so future sibling growth pushes the page tall instead of crushing the grid. The bound sits on the content div, NOT on `<details>` flex — Chromium slots the content, a flex chain through `<details>` does not reach it (measured: details-level flex left the panel overflowing the shell by 518px). Mechanical gate added: `tests/e2e/authed.spec.ts` U31 case — 3 viewports × open state, thread-column height floor, zero rect overlap, zero horizontal AND vertical spill; written red-first against the broken build. Same turn: the dead D10 anchor (the R2.4 approval row no longer exists in the live DB — taken by the 07-24 residue sweeps) re-pinned to shape-based selection; full authed suite 18/18. **Battery law recorded in the spec: a fixed-height surface's design pass MUST include the open state of every disclosure on it** | CEO screenshot 2026-07-26 ~12:00 ("ses hattını açtığımda chat ile birbirine giriyor… hizasız design görüntüleri") | Bu tablo + [[VOICE_INTERACTION_SPEC]] §24quater-ter + `apps/dashboard/src/app/(command)/chat/page.tsx` + `tests/e2e/authed.spec.ts` (U31 case) |
| U32 | **THE TWO WRITTEN GATES FIRE — G3 + G4 stopped being spec-only text (W2.3, 2026-07-26).** Normative text: [[REVENUE_ENGINE_SPEC]] §7quater. **Measured before writing a line:** `capital_required_eur` was compared NOWHERE and `evidence_refs` was validated NOWHERE — both rules binding since 2026-07-17, neither in the machine. (1) `fn_revenue_capital_limit()` puts the governing ceiling in ONE place (most recent `active` objective's `capital_limit_eur`, else 0 — the §7bis safe end), and it is the SAME selection `discovery.ts` uses for the scout brief, so the ask and the gate cannot drift into two truths. (2) **G4 refuses, never auto-rejects:** an over-limit opportunity is refused at `control_opportunity_score` with `CAPITAL_LIMIT` and stays at `discovered`, because capital raises are CEO-only and a gate that deletes his future options has exceeded its job; the refusal writes `audit_log` `revenue.opportunity.capital_filtered` (required vs limit) so a filtered candidate never looks like a candidate nobody found. (3) The check sits at BOTH forward doors — `control_opportunity_advance` re-checks on `shortlisted|piloting|scaling`, or a row scored under a higher limit would walk into `piloting` after the CEO lowers it; retreat to `retired` stays free. (4) Registration deliberately stays open: discovery finds, it never rules (§7ter). (5) **G3 at `control_objective_create`:** a `proposed` objective must cite at least one opportunity that EXISTS — empty, malformed or unknown-id citations are refused `EVIDENCE_GATE` with the offending id named; `draft` untouched (the CEO's own door creates drafts). **Evidence:** `tests/c9/revenue-gates.test.ts` 9 cases red-first then green · migration `20260726012000` · live rolled-back door proof on the real DB (€900 vs €0 refusal, ghost-citation refusal, backed proposal accepted) · full suite 82 files / 606 passed · `tsc -b` clean. **Same turn, unrelated defect fixed:** `tests/phase4/velocity.test.ts` scheduler case failed the full run on vitest's 5s DEFAULT while passing in 2.0s alone — a stopwatch verdict, not a behaviour verdict; the one case that boots a real pg-boss process now carries a 30s timeout, assertions unchanged | Factory completion roadmap row W2.3 ("the two written gates actually fire"), executed under CEO order 2026-07-26 16:00 | Bu tablo + [[REVENUE_ENGINE_SPEC]] §7quater + `db/migrations/20260726012000_w23_revenue_gates.sql` + `tests/c9/revenue-gates.test.ts` + `.planning/quick/20260726-w23-revenue-gates/PLAN.md` |
| U33 | **AN APPROVED ALLOCATION NOW PRODUCES REAL WORK (W2.4, 2026-07-26).** Normative text: [[REVENUE_ENGINE_SPEC]] §7quinquies. **Measured before writing:** all six engines had `owner_department = NULL` and `control_portfolio_allocate` wrote its row and stopped — the CEO's approval produced a ledger entry and silence. (1) **Engine owners ship as DATA** (ecommerce+physical→commerce · social_selling→sales · content_monetization→social-media · consultancy→customer-success · venture→strategy); the owner door is CEO-only, so the OS cannot self-assign — the CEO reassigns from Operations. (2) **The seam is inside the allocation door:** one transaction produces the `projects` row (slug `rev-<engine>-<opportunity>`, §12 pointer in `links` — `projects` has no `meta` column and never had one, registered adaptation) and ONE staffed kickoff task in the owning department (director seat, tier L1 per U21, headline EN+TR per U29, brief carrying objective + capital ceiling + approval rule). (3) **NO_OWNER refuses** rather than orphaning work; one project per opportunity, re-commitment re-activates instead of duplicating. (4) **STOP NOW STOPS:** `control_portfolio_stop` pauses the project and `claim_next_task` gained the rule it never had — a paused|done|archived project does not dispatch. Measured: the dispatcher read only `tasks`, so pausing a project was a label with no consequence FOR THE WHOLE COMPANY. **Surface debt found and closed the same turn:** the card HEADING had no Turkish leg (TR board read "HR Sandbox") → `projects.name_tr` (migration 20260726012500, view + BOTH consumers, i18n purity gains a projects leg); and `xl:grid-cols-3` sized by VIEWPORT while the 1366 content area is ~790px, collapsing each card's text column to one word per line → container-sized columns (22rem floor). **Process lesson recorded:** the first RULE #0 run reported PASS while measuring the LOGIN page (state cookie domain `localhost`, battery navigated `127.0.0.1`) and a second run screenshotted an unstyled page (piping `pnpm build` into `head` killed the build with SIGPIPE, leaving a half-written `.next`); the battery now asserts the landed pathname and a non-empty heading set. **EVIDENCE:** `tests/c9/allocation-seam.test.ts` 6 cases red-first then green · live rolled-back door proof (project `rev-consultancy-feed2756`, Head of Customer Success, TR headline) · full suite 83 files / 612 passed · tsc clean · i18n purity PASS · authed E2E 20/20 incl. the new projects case · RULE #0 pass EN+TR × 1366/1920 with screenshots read by eye | Factory roadmap row W2.4, executed under CEO order 2026-07-26 ("devam edelim aynı titizlikle" + "bozuk bir şey fark ederseniz o an düzeltin") | Bu tablo + [[REVENUE_ENGINE_SPEC]] §7quinquies + `db/migrations/20260726013000_w24_allocation_work_seam.sql` + `20260726012500_project_name_tr.sql` + `tests/c9/allocation-seam.test.ts` + `tests/e2e/authed.spec.ts` |
| U34 | **THE HOLDING OPENS ITS OWN WORK (W2.5, 2026-07-26).** Normative text: [[AGENT_ORCHESTRATION_SPEC]] A10-A13. **Measured before writing:** all three task-creating paths began at a human (`dispatch.ts:54` ← CEO intent · `dxb-mcp/groups/queue.ts:41` ← a session a human started · `revenue/discovery.ts:276` ← `trigger='ceo'`), so the W2.4 pilot plan was written, filed, and executed by nobody; `tasks WHERE status IN (queued,claimed,running)` = 0. (1) **A fourth path exists: machine output → machine work.** `control_work_generate(plan_task_id, steps)` turns a DONE plan's steps into staffed tasks in the departments the plan named — only inside a project the CEO already approved. It never creates a project, objective, opportunity or allocation: those are DECISIONS and stay with the CEO (§13). (2) **Exactly-once is structural:** `generated_work` UNIQUE (plan_task_id, step_index), skipped steps recorded with their reason, and a `step_index=-1` marker for a plan that named no step, so the seam never loops on a dead plan. (3) **The CEO owns the switch and the ceiling:** `orchestration.autogen.enabled` (default true — execution inside an approved project is not a new decision) and `orchestration.autogen.max_steps` (default 3); a paused project refuses generation as it already refuses dispatch. (4) **Two defects found in the FIRST live harvest and fixed the same turn:** the generated rows carried 120-char labels cut mid-word (house standard 41-45) → the plan contract now writes a short headline (`title`) beside the full instruction (`do:`), contract extracted into `fn_plan_step_contract()`; and a compliance step naming `riba` was minted only to be fail-closed five times by the constitutional halal screen until the ladder BLOCKED it (task a7038a28, zero `agent_runs` rows — every attempt refused before a run was born) → the door now runs the SAME screen at birth (`fn_halal_screen_hit`, reading `hook_policies.const.halal_screen`) and refuses with the flagged term. **The screen itself was NOT weakened** — §11 is immutable and CEO-only; the OS refuses its own work instead. **Surface debt closed the same turn:** `/ops/tasks` measured 770px of table inside a 748px container at 1366 TR, cutting the "GÜNCELLENDİ" header and its timestamps mid-word — the column that paid for it was information-free (213/216 rows read `resident-worker`), so it is gone and the headline column took the space (127→197px). **EVIDENCE:** `tests/c9/work-generation.test.ts` 14 cases red-first then green · **LIVE WINDOW 16:45:43Z: the scheduler harvested plan f724848a with no human in the loop and opened 3 staffed tasks (risk-audit, finance, strategy); 2 completed autonomously, the 3rd is the halal refusal above** · full suite 84 files / 626 passed · tsc clean · DB suite 74/74 · i18n purity PASS · authed E2E 21/21 incl. the new clipped-grid gate · RULE #0 pass EN+TR × 1366/1920 on /ops/tasks, /live, /ops/projects with screenshots read by eye | Factory roadmap row W2.5, executed under CEO order 2026-07-26 ("devam et çok hassas ol… mutlaka hata bulduğun an düzelt") | Bu tablo + [[AGENT_ORCHESTRATION_SPEC]] A10-A13 + `db/migrations/20260726014000_w25_autonomous_work.sql` + `20260726014500_w25_step_titles.sql` + `20260726015000_w25_halal_birth_refusal.sql` + `packages/orchestrator/src/work-generation.ts` + `tests/c9/work-generation.test.ts` |

| U35 | **THE CAPITAL REFUSAL IS VISIBLE WHERE THE CEO LOOKS (W2.3b, 2026-07-26).** Normative text: [[REVENUE_ENGINE_SPEC]] §7quater. **Measured before writing:** the G4 refusal existed ONLY in `audit_log` — `/revenue/opportunities` rendered a capital-blocked candidate exactly like any other, so "stopped by money" and "stopped by merit" were indistinguishable, and the CEO is the only person who can raise the ceiling. (1) **The board states the ceiling:** active limit + the objective it comes from + how many candidates wait on it, in the CEO's language. When no objective is active the line says so ("aktif hedef yok, yalnız sıfır-sermaye iş ilerleyebilir") instead of showing a bare €0.00. (2) **The blocked card marks itself:** warn border, `Sermaye kilidi` / `Capital-blocked` badge, and one plain line — "€900.00 gerekiyor, tavan €0.00 — değerinden değil, paradan durdu. Tavanı yükseltmek sizin kararınız." (3) **ONE source, ONE row:** `v_revenue_capital_ceiling` (migration 20260726016000) returns the number from `fn_revenue_capital_limit()` and the objective beside it, so the board can never attribute a ceiling to the wrong objective. First implementation read the limit through `supabase.rpc()` and the DASH-05 purity gate caught it (`tests/phase8/live-projection.test.ts`) — the view is the fix, so no allowlist entry and no weakened gate. (4) The comparison rule lives in one pure function (`isCapitalBlocked`, `>` not `>=`, live states only) shared by the board and its tests. **EVIDENCE:** `tests/c9/revenue-capital-surface.test.ts` 6 cases · full vitest 85 files / 632 passed · tsc clean · DB suite 75/75 · i18n purity PASS (12 new keys, parity 2388=2388) · authed E2E 22/22 incl. the new bilingual ceiling gate · RULE #0 EN+TR × 1366/1920 with a temporary €900 probe row rendering the blocked state, screenshots read by eye, probe deleted and residue measured 0 | CEO order 2026-07-26 16:45 ("bunu sıraya al hemen"), factory roadmap row W2.3b | Bu tablo + [[REVENUE_ENGINE_SPEC]] §7quater + `db/migrations/20260726016000_w23b_capital_ceiling_view.sql` + `apps/dashboard/src/lib/revenue-capital.ts` + `tests/c9/revenue-capital-surface.test.ts` |
| U36 | **THE AUDIT TWIN — the builder stops approving his own work (CEO ruling 2026-07-26).** Normative text: [[00-CEO-DIRECTIVE-AUDIT-TWIN]]; amends Standing Order 11 (`.claude/CLAUDE.md` Amendment A1). **Measured before the ruling:** the runtime already forbids self-approval — `critical-gate.ts:4` "Opus 5 WRITES the answer. Challengers try to REFUTE it", challengers `gpt-5.6-sol` + `gpt-5.5`, line 125 "another model owns authorship" — while Standing Order 11 excluded subagents from construction ENTIRELY, so every "done" in this repo rested on machine gates plus the author's own word. The machine gates catch code defects (632 tests, tsc, DB suite, E2E, i18n purity, RULE #0); they do not catch CLAIM defects, and every defect that reached the CEO's eye belongs to that class (ellipsis truncation, English cards on the Turkish board, dead chat, U28 record-vs-reality, a RULE #0 pass that photographed the login page). **(1) K1 UNTOUCHED:** subagent authorship stays forbidden — every repo line inline by the session author. **(2) Two REQUIRED uses:** adversarial audit (given claim + where to measure, told to REFUTE, never the author's conclusion) and read-only breadth sweeps returning a table. **(3) Read-only BY TOOL, not by promise:** `codex exec -s read-only` or a Claude agent limited to Read/Grep/Glob; measuring commands yes, writing commands never, test suites never (fixtures seed the live DB — 3,087 false `tool_missing` rows precedent), and the author measures `audit_log`/`tasks`/`agent_runs` row counts before and after, a difference invalidating the audit. **(4) Three triggers only:** the CEO acceptance session (cross-model, Codex lane — a Claude auditing a Claude shares its blind spots), a row whose closing evidence has a leg the machine cannot check, and a CEO-caught defect (sweep the CLASS repo-wide, not the instance). Never per-run, never per-commit. **(5) A finding is EVIDENCE, never a verdict:** the author signs every ✓; auditor and author disagreeing with neither able to prove it drops the claim to `⚠ UNVERIFIED` instead of leaving it ✓; the audit runs AFTER the full battery and anything machine-catchable it finds is recorded as the AUTHOR's defect. **(6) Pilot metrics written BEFORE the first run** (≥24/27 criteria attacked with a command · ≥80% of findings command-backed · ≤25% false alarms · write-check identical · cost recorded) with an explicit STOP RULE: high false-alarm rate or nothing the batteries missed = the twin is withdrawn. **Cost:** `cost_ledger` measures 3.59bn tokens over 30 days at €0.0000 — construction and runtime both ride subscription lanes, so the metered €50-150 OS budget is untouched; ~10 audits × ~1M tokens to project completion is an ESTIMATE the pilot will replace with a measurement. | CEO ruling 2026-07-26: option B approved ("B onayla, W5.1 denetimini bu kurallarla koş… nakşet en mükemmel şekilde") after the CEO reviewed a second agent's independent opinion and its five sharpenings, four adopted verbatim and one corrected (mechanical read-only instead of prompt-promised) | Bu tablo + [[00-CEO-DIRECTIVE-AUDIT-TWIN]] + `.claude/CLAUDE.md` Amendment A1 + [[ACCEPTANCE_CRITERIA]] (tier C = never delegable) |

| U37 | **HAMZA OPENS THE CONVERSATION — the proactive morning briefing (W2.6, 2026-07-27).** Normative text: [[VOICE_INTERACTION_SPEC]] §24quinquies + [[CEO_COMMAND_CENTER_SPEC]] A6. **Measured before writing a line:** `scheduler.ts` carried 14 scheduled jobs and NOT ONE wrote to `chat_messages` — every row on the CEO's board since 2026-07-19 answered something he typed first, so the holding had never once spoken to its CEO unprompted; and the operating manual's "07:00 itibarıyla hazır" was true only passively (a live view renders at every hour and addresses nobody). **(1) A thread, not a bulletin:** the briefing is delivered as a real conversation (`chat_sessions` + one `role='hamza'`, `source='briefing'` message), so the CEO answers it where he reads it and the 12-hour idle rule (§24quater) keeps his reply in the same thread. **(2) ONE view, no model:** Phase 9 LOCKED the briefing to a single SQL source ("brifing tek SQL görünümden; ajan brifing yazmaz", `09-02-PLAN.md:14`); `v_ceo_briefing` measures (same 19:00 Europe/Berlin window as `v_morning_briefing`, plus self-opened work, approvals, alerts, 24h+month cost against the cap, the revenue line and its capital ceiling) and `renderBriefing()` only formats. Three consequences, all deliberate: zero tokens, zero hallucination, and **a dead subscription lane cannot silence the CEO's morning**. **(3) Both language legs stored** (`chat_messages.content_tr`, `chat_sessions.title_tr`, `v_chat_threads.title_tr`): a system-authored message is an i18n surface — U26 satisfied that rule by having the system author nothing, which only holds while the OS is silent. Conversation turns stay single-leg on purpose. **(4) The CEO's switch and ceiling** (`briefing.proactive.enabled`, `briefing.proactive.max_per_day`, U34 idiom) with **both refusals audited**, so "switched off" and "broken" can never look the same; exactly-once is structural (`ceo_briefings UNIQUE(briefing_date, slot)` + unique-violation caught as `ALREADY_DELIVERED`). **(5) 07:00 means 07:00 where the CEO stands:** the first non-UTC schedule in this company (`tz: Europe/Berlin`; the other 14 rows are UTC). **(6) Framing law in the renderer:** zero revenue / zero work are stated as facts, and the single interpretive line ("the money leg is not open yet — your decision, not a fault") appears only while there is no active objective and no lifetime revenue, so it removes itself the day he opens it. **(7) A report is not a proposal:** the dispatch button is not drawn on a briefing. **DEFECT FOUND BY THE LIVE RUN AND FIXED AT ITS SOURCE:** the first real delivery filed and titled itself **"26 Temmuz" at 01:30 on the 27th** — a `date` column crosses the driver as a JS Date at LOCAL midnight and `toISOString()` rolls the calendar back for every hour of the CEO's morning that is still yesterday in UTC. The view now hands the day over as TEXT in his timezone, the probe rows were purged with an audit row (`chat.briefing.purged`, residue measured 0) and a test pins the class. **SECOND DEFECT, caught by the full battery and recorded as mine:** the door cases booked the REAL day, so once the live briefing existed every one of them answered `ALREADY_DELIVERED` — a suite that is green only until the feature runs in production is not a gate. They now book a day the company will never live through (live-DB tests are state-independent, the R4.2 lesson, paid for twice). **Two content sharpenings the perfection gate produced before shipping:** the failed line NAMES what broke and its owning department in the CEO's language (two tasks can share a headline — a reworded retry beside the attempt it replaced — and the same name under finished and failed reads like a contradiction), and the alert count says how many are critical. **A THIRD DEFECT THE BRIEFING EXPOSED — not its own, but a week old:** the first thing that landed in Hamza's new thread was four voice-probe turns from `tests/r31`. Answering a call MIRRORS both turns onto the chat board (U15 D13), the r31 suite deleted its `voice_calls` and `intents` and never knew about the mirror, and the 12-hour idle rule attaches new turns to the NEWEST thread — which is now the briefing. Measured across the whole board: **192 probe voice turns had been living inside the CEO's own conversations since 2026-07-19** (one thread was 98 messages of which 81 were test noise). Swept, and the sweeper added in the same turn at BOTH levels — the r31 file cleans its own mirror and `tests/global-teardown.ts` owns it as a fifth class. The pattern is ILIKE on the STEM, not the sentence: the intake normalises the probe before the mirror sees it, so one question reaches the board as "R31 probe:", "F31 Probe", "Pre-31 Probe" and "ve otuz bir probe … görevin nedir" — a case-sensitive first pattern swept 178 and left 15 behind. **Boundary recorded, not dropped:** the SPOKEN briefing waits for wave W4 (voice repair); it will read this same view into this same thread. **EVIDENCE:** `tests/c9/proactive-briefing.test.ts` 17 cases red-first then green · **LIVE: the resident scheduler delivered it with no human in the loop** (ledger row 2026-07-27/morning, thread `27 Temmuz sabah brifingi`, zero CEO messages in it, newest thread on the board) · full vitest 86 files / 649 passed / 0 failed · `tsc -b` clean · DB suite PASS · i18n purity PASS (new briefings leg) · authed E2E 24/24 incl. the new W2.6 locale gate (a mid-session run failed 7 cases and the cause was measured, not guessed: the storageState COOKIE expired at 01:53 while its own JWT is valid until 2026-08-02 — the cookie's expiry was realigned to the token it carries, so the battery stops dying an hour after every re-mint) · RULE #0 EN+TR × 1366/1920 on `/chat`, screenshots read by eye, `scrollWidth === clientWidth`, zero visible "…" | Factory roadmap row W2.6, executed under the CEO's standing order to finish the factory ("en mükemmel şekilde devam et… SIFIR HATA") | Bu tablo + [[VOICE_INTERACTION_SPEC]] §24quinquies + `db/migrations/20260727001000_w26_proactive_briefing.sql` + `packages/orchestrator/src/morning-briefing.ts` + `packages/outbox-executor/src/scheduler.ts` + `tests/c9/proactive-briefing.test.ts` |
| U38 | **THE OPEN WORK BOARD — one page answers "what is left" (CEO acceptance 2026-07-27, "açık işler tahtası teklifini kabul ettim").** Normative text: [[00-BOARD-OPEN-WORK]]. **Measured before proposing it:** open work was living in SIX ledgers at once — the roadmap, this U-table, the factory queue, the complaint ledger, [[ACCEPTANCE_CRITERIA]] and [[00-NOTE-R32-VOICE-REMEDIATION-PLAN]] — with no file that joins them, so neither the CEO nor a fresh session could see the remainder in one look. That scatter is the measured root cause of the CEO's 2026-07-27 complaint that the project "advances in a mess and specs stay half-finished": work was finishing while its row stayed open, and work was open while its row read closed. **(1) The board indexes, it does not replace:** each row points at the spec or ledger that owns it; detail stays where it lives. **(2) Its four laws:** one row per open thing · nothing closes without evidence (command → output, or the CEO's own eye/ear where the machine cannot check) · no work starts outside the board · when a row closes, the owning ledger is corrected in the SAME session. **(3) Historical order** — oldest first, because the CEO's ruling of the same night is that half-finished older work outranks new work. **(4) It carries the CEO's new complaint sequence C26-C42** (page-blind chat · weak page-to-page linking · opportunities with no detail view · flat skills page · ~80 external sources never integrated · a catalogue with zero recorded use · every surface to be re-upgraded · timestamps with no date · long text that should collapse · construction debris in the risk register · code words spoken to the CEO · a microphone that must be pressed · the missing "company doctor" · the never-built loop system · a throwaway example frozen into the design personas · and the standing order that competitor systems get matched or beaten), continuing C1-C25 so the complaint history stays ONE sequence. **(5) It also records the CEO's decisions of that night** so they are never re-asked: cloud speech-to-text REJECTED (the workstation arrives Friday; local models get chosen by measurement, never reputation), the board ACCEPTED, and the order of work set to oldest-first. **Boundary, stated not hidden:** the board's rows are an index of open work — closing them is the work that follows, and each closure lands in its owning spec as a registered adaptation, never in a new spec | CEO acceptance 2026-07-27 in session, after the measured six-ledger finding was put to him | This table + [[00-BOARD-OPEN-WORK]] + `.planning/STATE.md` |  <!-- CEO-OK: board-2026-07-27 -->
| U39 | **HAMZA GETS HIS NAME, HIS CHARACTER — AND, FOR THE FIRST TIME, HIS PERSONA (CEO ruling 2026-07-27).** Normative text: `personas/ceo/agents-orchestrator.md` §13 + [[EMPLOYEE_PERSONA_STANDARD]] G4-bis + [[00-BOARD-OPEN-WORK]] C43-C46. **The CEO's order:** the orchestrator he will entrust his company to is named **Hamza**, after **Hamza ibn Abd al-Muttalib (raḍiyallāhu ʿanhu)**, and must carry his character — resolute, hardworking and never lazy, fearless, the strongest of Mecca, a protector, a problem-solver who does whatever it takes in the most detailed way to beat the rivals, who plans and finds the best weapon when he does not hold one, honest and gallant, loving Allah and His Messenger, the most intelligent of believers. Written as **§13, a constitutional section for this role**, with every trait expressed as an OPERATING rule (fearless = bad news reaches the CEO first and this role's own failure goes at the TOP of the report; strongest = it never hands the CEO a problem it could have solved; if he lacks the weapon = a missing capability is a procurement task, never an excuse). **§13 is explicitly NOT inherited** — §12 is uniform across all 199 personas, §13 belongs to the name and the trust behind it. **THEN THE ORDER EXPOSED FOUR DEFECTS, ALL FIXED IN THE SAME TURN, and the first one is the mechanical cause of the CEO's complaint that "Hamza does not know the holding, he is confused":** (1) **the persona was authored and never delivered** — both live answer lanes each kept a private `personaHead()` that read the **first 60 lines of the persona FILE**, and a persona file opens with the 33-row SİCİL dossier (48 lines here), so the identity reaching the live Hamza was the dossier table, §1 and one sentence of §2 — **2 of 13 sections**; his method, decision rules, escalation limits, reporting standard and §12 never arrived. One shared loader (`packages/voice/src/persona.ts`, `loadPersonaBody`, used by BOTH lanes — the duplicate is gone) now delivers the body from the same `# PERSONA — ` boundary the sync script, the gate and the compiler already use; measured `[1,2]` → `[1..13]`, 18,168 chars, and `tests/r31/persona-delivery.test.ts` pins the class with the tail assertion a truncating loader cannot pass. (2) **The database refused to record the truth about authorship** — `personas_author_check` allowed only `fable-5|hr-factory`, so an Opus 5 session could file a persona only under Fable 5's name (U20/U30 never reached the schema); migration `20260728001000` extends it, and the CEO re-confirmed the rule in-session: *"Fable 5 ve Opus 5 yazar olabilir bu projede CEO talimatıdır, kayda geçilsin"*. Historical rows are NOT relabelled — guessing which were mislabelled would replace one wrong record with another. (3) **The sync script hardcoded `'fable-5'` and called a database refusal a success** (`submit: 1 · fail: 0 · EXIT=0` on a rejected insert); the author is now a required, whitelisted `DXB_PERSONA_AUTHOR` with **no default**, and an empty returned persona id is a FAIL (`submit: 0 · fail: 1 · EXIT=1`). (4) **§13 would have been silently dropped from the compiled prompt** — the compiler emits only listed sections, and adding §13 as REQUIRED would have failed the other 198 personas at the gate; `SectionSpec.optional` resolves both. **EVIDENCE:** persona v6 filed `author='opus-5'`, mechanical gate `ok: true / 0 failures`, deep verdict signed by the session author, `quality_gate='passed'` · full vitest **87 files / 653 passed / 0 failed** · `tsc -b` clean · i18n purity PASS (dictionary parity en 2390 = tr 2390) · migration applied twice, idempotent · residents `dxb-scheduler` + `dxb-jarvis` restarted AFTER the build (01:20:28) because the chat lane only loads new code on restart — U27 standing rule | CEO ruling 2026-07-27 in session, with the character described by the CEO verbatim | This table + `personas/ceo/agents-orchestrator.md` §13 + [[EMPLOYEE_PERSONA_STANDARD]] G4-bis + `db/migrations/20260728001000_persona_author_u30.sql` + `packages/voice/src/persona.ts` + `tests/r31/persona-delivery.test.ts` |
| U40 | **C42 STAGE 1 — SIXTEEN RIVAL SOURCES READ TO THE FRAME, AND A CRASH-PROOF WAY TO DO IT (2026-07-28).** Normative text: [[00-BOARD-OPEN-WORK]] Section 3 + `.planning/research/rival-intel/00-SYNTHESIS.md`. **The CEO's order, verbatim:** *"ÖLÜMCÜL UYARI EN BÜYÜK HATA: ÖZET İNCELEME YAPILMAYACAK MİLİMETRİK DEĞERLENDİRME HER KAREDEKİ OLGU … ŞU SKİLLERİ ŞU PLUGİNLERİ ŞU TOOLLARI İNDİRİP KURUP YAPARIM YAPILIR PROJESİ OLMALI"*, then in-session: *"HEPSİNİ İZLE TEKTEK EN İNCE EN KUCUK DETAYLARI YAZ. HEPİSININ SONRA DA BİRLEŞTİRİCİ BİR PLAN ÇIKAR!"*, and — because two of his own sessions had died with their work unfiled on 2026-07-27 and his editor was killed three times by the memory reaper at 01:49-01:53 on 2026-07-28 — *"BİR SONRAKİ OPUS 5 TE DEVAM ETSİN KALDIĞIN YERDEN … CONTEXT ŞİŞERSE KAPANIRSA SİSTEM FALAN"*. **(1) THE WORK STOPS LIVING IN THE CONVERSATION.** `.planning/research/rival-intel/00-LEDGER.md` is the queue: one row per source, a status vocabulary (`pending → claimed → fetched → watched → reported`), and five laws — claim BEFORE working (a crash then leaves a claimed row and the next session redoes exactly that one source, never the fifteen already finished) · one commit per source · `reported` is a claim that must survive a test · no summarising · ledger parity with the board and the tracker in the SAME session. `scripts/rival-intel/next.sh` is the single resume command: it prints the stage, the progress, a stale-claim warning if a session died mid-row, the next row, and the exact command to continue with. `scripts/rival-intel/fetch.sh` is mechanical only — claim, download, sha256, transcribe **through the holding's own Speaches container (V9: no audio leaves this machine)**, extract frames at 1 fps plus every scene cut — and makes no judgement, because the reading is the session author's work (K1). **(2) A STALE ✓ IS MECHANICALLY IMPOSSIBLE HERE.** `tests/c42/rival-intel-ledger.test.ts` (7 cases) fails the suite if a `reported` row has no report, if a report is missing any of its six required sections, if a reel report's section 2 carries fewer than 8 timestamped rows (a summary, not a record), if a report names neither a sha256 nor a commit, if the NEXT pointer points at a finished row, or if a report on disk belongs to no row. **The gate caught five of my own defects across the programme and each is recorded rather than quietly fixed:** two ledger rows carrying a literal `|` inside an uploader name silently shifted every column after it · the orphan regex matched the ledger's own `00-` files · twelve reports carried no sha256 and would have passed only on an incidental hex match · the frame-row regex rejected a time RANGE in the time column · and the required-section list demanded the literal words "frame-by-frame", which would have forced a repository report to lie about what it read (sources 08 and 16 honestly call their section "file-by-file record") — loosened by principle, with the anti-summary rule enforced twice instead. **A SIXTH DEFECT THE GATE COULD NOT CATCH, FOUND BY EYE:** the first contact-sheet sweep matched frames by a `t0` prefix, so source 02 silently lost frames `t100`-`t143` — 44 unread frames holding the entire skills section the CEO had asked for — and it surfaced only because one sheet rendered three-quarters black. The sweep now lives in `scripts/rival-intel/sheets.sh`, sorts numerically, includes the scene cuts, and **prints the count it swept** so a silent omission cannot repeat. **(3) WHAT THE READING FOUND.** 16/16 reported: 13 reels, 1 PDF (the Drive link was not a video), 2 repositories. Behind them are 6-7 distinct systems — sources 01/03/16 are one story (the demo, its teardown, and the Stanford open-source stack it ends at), rows 11-14 are one account, and rows 05 and 11-14 are the same clipping operation from opposite ends. **THE DIAGNOSIS — AND THE AUTHOR'S DEFECT THAT PRODUCED IT.** The first synthesis read *"DXB is bigger, safer and better governed than every system in these sixteen sources"*. **The CEO rejected it the moment he read it, and he was right; the corrected verdict is recorded here rather than quietly overwritten.** The error is exactly what RULE #0-A exists to prevent: I counted what was countable — 199 personas, 60 tables, a rich objectives schema — and presented the count as a judgement of quality. A system is judged by what it produces, and this one produces nothing: `realized_revenue_eur` **0** since the project began, `library_items` **434 rows** against `library_usage_log` **0**, and 199 personas the CEO has never heard speak. **Capability that has never run is inventory, not capability.** Second half of the error: I graded the rivals from 15-100 second promotional reels while the CEO knows several of these systems personally — *"BEN O SİSTEMLERİ VİDEODAN DEĞİL HEPSİNİ TANIYORUM, ARKADAŞLARIM, VE MİLYONLARCA DOLAR KAZANIYOR VE HEPSİ GERÇEK, HEPSİ CANLI"* — so direct knowledge of a running system outranks a frame-by-frame reading of an advertisement for it, and every "we are ahead" cell must be read as *ahead on that one countable dimension*, never as *ahead as a system*. **CORRECTED VERDICT: every one of the sixteen RUNS and EARNS; DXB does neither. On the only measure that decides — does it work and does it make money — DXB loses to all sixteen.** What DXB holds is more unfinished structure and stricter rules, and rules are not results. The consequences, in order: it does not earn · it loses 44% of the CEO's speech · it shows him nothing · it answers in 29-35s against roughly 1.5s. **The programme's purpose is therefore not to prove DXB is good — it is to SWITCH IT ON.** Source 03 dissolves the mystery of source 01's admired "judgement": it is a connected data source with the right breakdown (a Meta Ads MCP returning ROAS per creative), not reasoning. **(4) SIX WAVES, EVERY PROJECT INSIDE AN EXISTING SPEC** (board Section 3.2): feedback and speed · the living command centre · a briefing that decides instead of reports · connectors · run durability · revenue. **No new spec was opened** — PLAN.md ≠ a plan (CEO ruling 2026-07-13). **(5) REFUSALS RECORDED WITH REASONS, NOT SILENTLY OMITTED:** Coolify (already banned by our stack rules; its one-click Redis reintroduces a removed dependency) · Trigger.dev (a second job runtime — **its idea, resume-from-step, is adopted; its infrastructure is not**) · adopting Paperclip or OpenJarvis wholesale · the trading desk of source 10 (market speculation touches constitutional Islamic boundaries — **we take the constellation, not the desk**) · Kokoro TTS (recorded as the first fallback to measure, not a gap) · the $97 six-agent kit · "duplicate a repo and let Claude Code edit it" (K1) · every comment-for-DM funnel. **(6) THE DESIGN LAW THE WAVES INHERIT**, in the CEO's own words — *"HER PANELIN CANLI OLDUĞU YAŞAYAN BİR HOLDİNG"* — made operational: every panel reads from a query or shows an honest empty state; zero is a real answer; the refusals are visible; outward actions look different from reads; no "…" truncation; RULE #0 per surface. **EVIDENCE:** 16/16 rows `reported`, `NEXT: done` · `npx vitest run tests/c42/rival-intel-ledger.test.ts` **7/7** · `scripts/rival-intel/sheets.sh` swept 148/84/35/129/66/82/21/75/98/52/52/61 frames for rows 02-14 · every DXB comparison cell measured this session by command or query (wake daemon `active` since 01:44:09 and `muted` since 08:58:54 · 9 voice calls, 4 `empty_transcript` · `objectives` 2 rows realising €0 · no mailbox connected) · 6 commits, gitleaks clean on each. **BOUNDARY, STATED NOT HIDDEN: nothing in the six waves is built yet.** Stage 1 delivered the target definition; the building starts on the CEO's word | CEO standing order C42 (2026-07-27), restated and widened 2026-07-28 in `HAMZA VE KOMPLE SISTEM SIKAYETİ.odt`; work order chosen by the CEO himself (*"Rakip analizi önce"*) | Bu tablo + [[00-BOARD-OPEN-WORK]] Section 3 + `.planning/research/rival-intel/` (16 reports + `00-LEDGER.md` + `00-SYNTHESIS.md`) + `scripts/rival-intel/{next,fetch,sheets}.sh` + `tests/c42/rival-intel-ledger.test.ts` |
| U41 | **THE CORPUS MAY NO LONGER DISAGREE WITH THE SYSTEM — laws 1 and 5 of the open work board become machine-enforced (2026-07-28).** Normative text: [[00-BOARD-OPEN-WORK]] "Laws 1 and 5 are enforced by machine". **Trigger — the CEO's own question, and his own catch.** He pointed at `.planning/research/` and asked: *"buradaki şeyler bizim sistemimizde yapılmış mı? eğer yapılmadıysa bizim eksikler tahtasında mevcut mu? sorun şu ki her gelen model bir şeyleri atlıyor mutlaka."* Mid-sweep he added the warning that saved the answer from being wrong: *"tahtada yoktur ama belki sistemde yapılmıştır dashboardta yapılmıştır bilmiorm"* — and it caught a real error in the making: the off-site backup was about to be reported as a live gap when **he himself had closed it** on 07-18 20:19 (`OFFSITE_OK`, 12,435,816 bytes). **MEASURED, two directions of drift.** (a) Promised and tracked NOWHERE — board 0 / roadmap 0 / U-table 0 — **six rows born**: B14 video-learn never reached a screen (`tools/video-learn` live since 07-09, `apps/dashboard/src/app` holds not one file mentioning video) · B15 the autonomy dial (column exists, `/org/directors` prints `L0`, all 205 agents at 0, nothing can change it) · B16 the B6 hard gate we set and walked past (`LITERATURE.md:98`, W2-W5 never ran) · B17 a standing CEO report never delivered (`REVENUE-OPPORTUNITIES.md:117`, last updated 07-09) · B18 filters reach 9 of 58 command routes (complaint C9's own ◐ leg, carried on no ledger for nine days) · B19 a security TODO whose trigger nobody watches (gitleaks action pinned to a moving tag) · B20 the roadmap's status token is not machine-readable, which is why every session's completion figure drifts. (b) Recorded state contradicted by the database — **six corrections**: E12.5-WORKFORCE-BASELINE said the holding had ONE employee (it has 199) and listed four finished items as remaining · E13.0-OPERATIONAL-READINESS carried two ◐ legs both paid the same evening · E12.5-CAPABILITY-COVERAGE called activation BLOCKED on decisions the CEO had already made · roadmap row E12.5 sat ◐ for ten days after `audit_log` recorded `employee.evaluated` for **197 distinct employees** · `.planning/STATE.md` counted three ✓ rows as unfinished · and STATE.md still carried the rival-analysis verdict the CEO had already rejected. **THE RULE.** Every durable statement is now STATE (true NOW → `<!-- STATE: id = value @ date -->`, re-measured against the live company database every battery run) or EVENT (happened ONCE → frozen, must carry its date). The defect that started this was a STATE claim written in EVENT clothing. Any line declaring open work carries `<!-- OPEN: <row> -->` or `<!-- HISTORY -->`; an unmarked declaration fails the battery. In a LEDGER the status token is the declaration, so there the tripwire fires on `◐` alone. The board is exempt — it IS the register. **Read-only by construction:** the gate refuses any claim query that is not a bare SELECT, and it reads the COMPANY database because the test clone (C47) cannot prove what is true in the company; the newest `audit_log` row after the full run was 09:04, twelve hours before the session's first execution. **Sweep result:** 172 corpus files, 8 STATE claims, 9 OPEN bindings, 39 HISTORY exemptions, 49 trigger lines all accounted for, 60 board rows parsed. **Boundaries, stated:** the tripwire is a word list, not an oracle — its value is that the sweep tuned it to zero, so any future ring is real; study-card version numbers describe the outside world and cannot be measured by SQL, so they carry dated HISTORY instead of a false promise of freshness; and the gate proves the marked claims, not that every claim got marked. | CEO order 2026-07-28; the honour rule failed in two directions on one evening and he named the pattern himself | Bu tablo + [[00-BOARD-OPEN-WORK]] laws section + rows B14-B20 + `scripts/governance/{ledger-truth.mjs,claims.json}` + `tests/governance/ledger-truth.test.ts` + [[00-CEO-DIRECTIVE-DESIGN-VERIFICATION]] machine gates + `package.json` `verify:ledger` |

| U42 | **THE CONTEXT ARCHITECTURE — one rule, one owner, and nothing loaded before it is needed (CEO order 2026-07-30).** Normative text: `.claude/CLAUDE.md` + the eight doors in `.claude/skills/dxb-*`. **The CEO brought the material himself** (`DXB_Claude_5_Context_Engineering_Package/`) and said what that meant: *"Ben bir CEO'yum, siz ise mühendissiniz — bunların mimarisini zaten sizin yapmanız gerekirdi. Yine ben uğraştım."* Recorded as the author's defect, not as a favour. **MEASURED BEFORE ANY CHANGE:** every session read ~27 pages of always-on text before the CEO's first word, plus a further page re-injected on EVERY prompt, and the ordered reading list he had given came to ~147 pages more; the phrase *"they are leads to verify, never answers"* and its siblings lived in **43 files**, evidence-before-done in **44**; `.planning/STATE.md` said 95 % complete at the top and 66 % in the middle, named a focus stopped on 10 July, and carried a **47,336-character single line**; the knowledge-graph rule pointed at a graph **368 commits** old; the always-on file mandated a workflow whose skills are 48-archived, named a desktop note as "master source of truth" beside the corpus, and still carried template stubs saying this project had no skills and no architecture. **BUILT:** (1) the always-on core cut to one page and given the thing it never had — an **authority order**, whose absence is why two documents could contradict each other with nothing to settle it; (2) **eight doors** (`dxb-start · dxb-close-row · dxb-verify · dxb-surface · dxb-ceo-report · dxb-rival-intel · dxb-persona · dxb-hamza-context`) holding the procedures that used to be always-on; (3) STATE.md reduced to a photograph, its history frozen in `.planning/STATE-ARCHIVE.md`; (4) two new machine checks inside `verify:ledger` — **one rule, one owner** (`rules.json`) and **no unregistered claim of the CEO's approval** (`ceo-approvals.json`); (5) the session-start hook stripped of the six rules it duplicated, leaving only the live position it alone can read. **HIS TWO NEW LAWS, now in the core: LAW A** — a live order DELETES what contradicts it (*"şuanki emrimle eski bulunan şeyler çelişiyorsa hepsini sil"*); **LAW B** — finished ≠ approved (*"iş tamamlanınca bitti anlamına gelmez, ben bakmam lazım"*), with his own example: the board said the rival analysis was done and approved and **he had never approved it**. That claim is deleted (board Section 3 correction) and eleven surviving approval claims are now bound to a register carrying his words — three of them honestly recorded as having **no captured verbatim**, so he can dispute them. **THE SAME DISEASE FOUND IN THE PRODUCT AND FIXED:** the two live answer lanes each wrote their own copy of Hamza's standing instructions and had **already drifted** — voice carried *"HE IS TALKING TO YOU, Hamza"* and chat did not, so the same person answered with a different self-understanding depending on which door the CEO used; his slug was declared in **three** places. One definition now (`packages/voice/src/prompt-core.ts`), and **STANDING ORDER 14 reaches the runtime for the first time** — Hamza is now under the same language law as the session author, plus an honesty rule that forbids a recalled or estimated number. **TWO OF MY OWN DEFECTS CAUGHT WHILE BUILDING IT:** RULE #0-A had **no owning file** — its full text lived only inside the always-on context, so compressing it would have *lost* the rule rather than moved it ([[00-CEO-DIRECTIVE-MEASURE-NEVER-GUESS]] born); and the first draft of the closing door **restated the board's own five laws**, the exact violation the gate exists to prevent. **BOUNDARY, STATED:** the duplication registry guards twelve rules, not every rule in the corpus — an unregistered rule is unguarded, never blessed; and this work bought DISCIPLINE, not capability: no new surface, no chart, no connector, no revenue | CEO order 2026-07-30 in session ("onaylıyorum, hamza da dahil"), scope narrowed by him the same hour ("şuan sadece odak noktamız bu directive paketi o kadar ve bu context engineering işi") | Bu tablo + `.claude/CLAUDE.md` + `.claude/skills/dxb-*` + [[00-CEO-DIRECTIVE-MEASURE-NEVER-GUESS]] + [[00-CEO-DIRECTIVE-REANALYSIS-AND-DESIGN]] + `scripts/governance/{rules,ceo-approvals}.json` + `packages/voice/src/prompt-core.ts` + `tests/b21/agent-context.test.ts` + board rows B21-B23 |

## Doğrulama komutları (dalga kapanışında koşulur)

```bash
ls HOLDING-OS-MASTER-PLAN/ | wc -l          # hedef: 33 (31 spec + INDEX + direktif)
grep -rn "DxB-Kokpit" HOLDING-OS-MASTER-PLAN/ .planning/ | wc -l   # hedef: 0
git log --oneline -5                          # dalga-başı atomik commit görünür

# AUDIT-LEDGER INVARIANT (registered 2026-07-17 ~22:40 — lesson: 8 findings lived only in
# session memory for a day; memory is a lead, NEVER a ledger [RULE #0-A]. Every external-audit
# finding ID must hit a GOVERNED anchor: roadmap row, U-row, or directive disposition table.)
for f in F-01 F-02 F-03 F-04 F-05 F-06 F-07 F-08 F-09 F-10 F-11 F-12 F-13 F-14 F-15; do
  grep -ql "$f" HOLDING-OS-MASTER-PLAN/IMPLEMENTATION_ROADMAP.md HOLDING-OS-MASTER-PLAN/00-CEO-DIRECTIVE-REVENUE-FIRST.md \
    || echo "ORPHANED AUDIT FINDING: $f"; done   # hedef: sıfır satır çıktı
```
