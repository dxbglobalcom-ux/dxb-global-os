{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "claims",
  "description": "The one file the model writes. Every claim points at ledger rows that already exist; the gate checks the referent, not the shape.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id","text","cites"],
    "properties": {
      "id": {"type": "string"},
      "text": {"type": "string"},
      "cites": {"type": "array", "items": {"type": "string", "pattern": "^L[0-9]{4}$"}, "minItems": 1},
      "type": {"enum": ["fact","user-report","interpretation","recommendation"]},
      "load_bearing": {"type": "boolean", "description": "the 3-5 sentences the answer rests on; these get a contradiction search"},
      "confidence": {"enum": ["PROVEN","LIKELY","UNPROVEN"]},
      "about_the_source": {"type": "boolean", "description": "the claim is ABOUT the cited page (e.g. 'this is a vendor page'), so vendor-only citation is legitimate"},
      "caveat": {"type": ["string","null"]}
    }
  }
}
