# TWO LANES, ONE OF THEM NAMELESS

`crowd.jsonl` names the sub-question it answers (S2); `rival.jsonl` names none. The ruler runs
`fleet/merge.py` over a copy of this folder and requires that the nameless verdict is DROPPED
and COUNTED. Measured 2026-09-20: the requirement had lived only in the hunters' briefing —
a sentence, which a tired lane ignores. The mechanism is the merge.
