# GAPS — what this run did not reach

Copy this into `runs/<id>/GAPS.md`. The gate demands it whenever the budget is spent or a
page defeated every door in the reading chain.

**Stopping early is legal. Stopping early in silence is not.** This file is not an apology;
it is the boundary of what the CEO can lean on.

## Channels that failed or were never opened

| channel | what happened | what it would have added |
|---|---|---|
| e.g. `weibo` | `FAIL`, 162 bytes | the Chinese consumer-side view |
| e.g. `linux-do` | `AUTH_REQUIRED` — needs a browser login only the CEO can give | the Chinese developer view |

## Pages that stayed shut after every door

| page | liveness | doors tried |
|---|---|---|
| e.g. `https://…` | `blocked` | media → pdf → scrapling → stealth → opencli → tavily → firecrawl → exa → playwright → jina → curl |

## Questions left open

- the sub-question nobody answered, and what would answer it
- the contradiction that is still standing
- the number that could not be measured, and why

## What I would do with one more hour

One or two lines. This is what tells the CEO whether the gap is worth paying for.
