# THE CUPBOARD — what this holding holds, and how each drawer opens
Doors (procedures): `.claude/CLAUDE.md` §4 → `Skill("dxb-<door>")`.
Plugins → `Skill("<plugin>:<skill>")`, no restart (all ON, his ruling 2026-08-27):
- caveman: terse output, cavecrew subagents (investigator/builder/reviewer); index `caveman:caveman-help`
- taste-skill · ui-ux-pro-max · frontend-design · impeccable: UI design — Phase-8 work only, behind the R-gate
- obsidian: vault/markdown/canvas · claude-md-management: CLAUDE.md audit · andrej-karpathy-skills: coding rules
- skill-creator: the repo copy opens as `Skill("skill-creator")`; the plugin twin is `skill-creator:skill-creator`
- claude-mem: cross-session memory (`claude-mem:mem-search`) · codex: OFF, the council calls the binary
MCP servers (tools arrive as names; `ToolSearch("select:<tool>")` loads one):
- Browser: `mcp__playwright__*` (.mcp.json, headless) and `mcp__claude-in-chrome__*` (his browser, eye tests) are the two in use; `mcp__plugin_playwright_*` is a dead twin
- scrapling: fetch/scrape a page · dxb-mcp: the house bus (queue, media, memory, approval…) · context7: library docs
Outside world: door `dxb-research` — `fleet/fleet.sh`, 7 hunters, 37 channels, counts the crowd.
Screen/mouse/keyboard: global skill `operator` — `operator shot` after every action, look before typing.
