import { ModuleWaiting } from "@/components/command/module-waiting";

// /live — real module lands at roadmap step E3.3 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="live" step="E3.3" />;
}
