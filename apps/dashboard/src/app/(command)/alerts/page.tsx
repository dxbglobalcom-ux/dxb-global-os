import { ModuleWaiting } from "@/components/command/module-waiting";

// /alerts — real module lands at roadmap step E8.3 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="alerts" step="E8.3" />;
}
