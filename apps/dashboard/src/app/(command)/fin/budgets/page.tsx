import { ModuleWaiting } from "@/components/command/module-waiting";

// /fin/budgets — real module lands at roadmap step E11.1 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="budgets" step="E11.1" />;
}
