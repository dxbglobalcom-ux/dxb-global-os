import { ModuleWaiting } from "@/components/command/module-waiting";

// /fin/costs — real module lands at roadmap step E11.1 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="costs" step="E11.1" />;
}
