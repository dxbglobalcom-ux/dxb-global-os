import { ModuleWaiting } from "@/components/command/module-waiting";

// /fin/capacity — real module lands at roadmap step E12.1 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="capacity" step="E12.1" />;
}
