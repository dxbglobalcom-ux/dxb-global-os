import { ModuleWaiting } from "@/components/command/module-waiting";

// /fin/tokens — real module lands at roadmap step E11.1 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="tokens" step="E11.1" />;
}
