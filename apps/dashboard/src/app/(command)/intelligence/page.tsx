import { ModuleWaiting } from "@/components/command/module-waiting";

// /intelligence — real module lands at roadmap step E12.1 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="intel" step="E12.1" />;
}
