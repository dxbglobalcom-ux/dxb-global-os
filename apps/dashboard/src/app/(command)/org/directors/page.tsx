import { ModuleWaiting } from "@/components/command/module-waiting";

// /org/directors — real module lands at roadmap step E12.1 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="directors" step="E12.1" />;
}
