import { ModuleWaiting } from "@/components/command/module-waiting";

// /org — real module lands at roadmap step E6.3 (E2.2 skeleton rule).
export default function Page() {
  return <ModuleWaiting pageKey="org" step="E6.3" />;
}
