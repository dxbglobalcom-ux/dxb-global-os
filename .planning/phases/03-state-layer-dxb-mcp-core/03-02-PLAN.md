---
phase: 03-state-layer-dxb-mcp-core
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - supabase/config.toml
  - db/migrations/0001_operational_core.sql
  - db/migrations/0002_registry.sql
  - db/migrations/0003_approvals_outbox.sql
  - db/migrations/0004_cost_audit.sql
  - db/migrations/0005_memory_index.sql
  - db/migrations/0006_crm.sql
  - db/seed/import-personas.ts
  - .env.example
autonomous: true
requirements: [QUEUE-01, QUEUE-02, REG-01, REG-02]
must_haves:
  truths:
    - "`supabase db reset` applies all six migrations to a fresh database and exits 0"
    - "claim_next_task() and reap_expired_leases() exist and are callable (QUEUE-01/02 primitives)"
    - "Persona seed imports every CLASSIFIED legacy persona (measured 159 across 13 dirs; integrations/examples/scripts excluded, non-persona files skip-logged); DB counts equal classifier counts; no file aborts the import"
    - "approvals trigger pair enforces one-way decisions and auto-births the outbox row on approve"
    - "task_events / audit_log / cost_ledger reject UPDATE and DELETE (append-only proven, not assumed)"
  artifacts:
    - "six migration files byte-faithful to master-plan PHASE-03 LOCKED SQL"
    - "db/seed/import-personas.ts (idempotent, frontmatter-tolerant)"
    - "supabase/config.toml (local stack config)"
  key_links:
    - "master-plan PHASE-03 §3 SQL <-> migration files (verbatim; any divergence is ⛔ FABLE-ONLY)"
    - "agency-agents/ corpus <-> agents rows (persona_path column, body stays on disk — REG-02 lazy activation)"
    - "0002 agents table <-> 0001 tasks.agent_id FK (added in 0002 by design)"
---

<objective>
Stand up the local Supabase stack and land the complete LOCKED operational schema as six migrations plus the persona seed — the data model every later package depends on.

Purpose: Phase-3 gate criterion 1 (schema applies cleanly to a fresh DB) and the DB half of criteria 3-4.
Output: running local stack, `supabase db reset` green over 0001-0006, 159-persona legacy registry seeded dormant (classifier-based, evidence-counted).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-03.md   (LOCKED SQL — copy verbatim, do not restyle)
@.planning/research/study-cards/supabase-cli.md
@.planning/research/study-cards/supabase.md
</context>

<environment_constraints>
- ThinkPad X230, 8GB RAM, ~1.9GB available measured 2026-07-07. The full default Supabase local stack (~10 containers incl. Studio, Storage, Realtime, edge-runtime, analytics) will NOT fit comfortably.
- MANDATE: start with excluded services. Phase 3 needs ONLY Postgres (+ the CLI's required core). Use `supabase start -x studio,storage-api,imgproxy,inbucket,realtime,edge-runtime,logflare,vector` (adjust flag names to the installed CLI's `supabase start --help` output — service names vary slightly by CLI version; the intent is: db + api gateway stay, heavy extras stay down).
- psql is NOT installed on the host. All psql verifications run inside the db container: `docker exec <supabase-db-container> psql -U postgres -d postgres -c "..."` (container name from `docker ps`, matches `supabase_db_*`).
- Docker 28.3.0 present and running (verified 2026-07-07).
</environment_constraints>

<tasks>

<task type="auto">
  <name>Task 1: supabase init + minimal local stack up</name>
  <files>supabase/config.toml, .env.example</files>
  <read_first>
    - .planning/research/study-cards/supabase-cli.md — init/start/reset lifecycle, ports (API 54321, DB 54322)
  </read_first>
  <action>
    Run `pnpm exec supabase init` at repo root (creates supabase/config.toml; accept defaults, project_id `dxb-global-os`).
    Point migrations at the repo's canonical folder: in config.toml, no change needed if we symlink or configure — Supabase CLI reads `supabase/migrations/` by convention. DECISION (recorded): keep `db/migrations/` as the authoring location per master-plan file spec, and make `supabase/migrations` a relative symlink to `../db/migrations` so both the master-plan tree and the CLI convention hold. Verify the symlink survives git (`git add` records the symlink).
    Start the stack minimal per <environment_constraints>. Confirm `pnpm exec supabase status` prints the API URL (http://127.0.0.1:54321) and DB URL (54322).
    Write `.env.example` with `DXB_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres` (local-dev default credentials — public knowledge, NOT a secret; real environments override via vault pattern). Do NOT write a real `.env` with anything sensitive.
    Record actual RAM headroom after start (`free -m`) in the summary.
  </action>
  <verify>
    <automated>pnpm exec supabase status | grep -E 'API URL|54321' && docker ps --format '{{.Names}}' | grep -q 'supabase_db' && echo "stack OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase status` shows API URL http://127.0.0.1:54321 and a DB connection on 54322
    - `docker ps` shows the db container running; excluded heavy services are NOT running
    - `free -m` shows the machine still has headroom (>300MB available) — recorded in summary
    - supabase/config.toml committed; `supabase/migrations` resolves to db/migrations content
  </acceptance_criteria>
  <done>Local Supabase runs within X230 RAM budget; migration path wired to db/migrations.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 0001 + 0002 + persona seed</name>
  <files>db/migrations/0001_operational_core.sql, db/migrations/0002_registry.sql, db/seed/import-personas.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-03.md §3 — 0001 and 0002 SQL blocks (LOCKED — copy byte-faithful; timestamped filename prefixes per Supabase CLI convention are allowed: `<timestamp>_0001_operational_core.sql` style if the CLI requires ordering prefixes; content stays verbatim)
  </read_first>
  <action>
    Create 0001_operational_core.sql and 0002_registry.sql exactly as specified (tasks, task_events, claim_next_task, reap_expired_leases, indexes, REVOKE, RLS ENABLE; departments, agents, FK back onto tasks). If the Supabase CLI enforces `YYYYMMDDHHMMSS_name.sql` naming, use `<ts>_operational_core.sql` etc. while keeping the master-plan's logical order and content unchanged — record the mapping in the summary.
    Write db/seed/import-personas.ts (runs with `pnpm exec tsx` if tsx present, else `node --experimental-strip-types` on Node 22 — pick what works, record it):
    - Scans `agency-agents/` top-level dirs; HARD-EXCLUDES non-department dirs: `integrations` (185 files = the same agents converted for other tools — NOT personas), `examples`, `scripts`, and any dotdir. Dir name → department slug: lowercase, spaces→`-` (e.g. "Creative Department"→`creative-department`, "social media departman"→`social-media-departman`). A department row is upserted (dormant) only when its dir yields ≥1 classified persona.
    - CLASSIFIER RULE (master-plan "Persona v2 Programı" section): a file is a persona IFF it starts with `---` frontmatter containing a `name:` field (README* always skipped, case-insensitive). Non-matching files are SKIPPED and appended to a `seed-skipped.log` audit output (path + reason) — strategy/ currently yields 0 personas this way (doc-only dir).
    - Classified personas insert as agents dormant, `persona_version='v1.0-legacy'`, persona_path relative to repo root. A persona-dir file with PARTIAL/broken frontmatter (has `---` + name but missing slug/role) falls back to role='worker', slug=kebab-case filename, `persona_version='v1.0-unparsed'` — import never aborts on a bad file.
    - Persona BODY never enters the DB (REG-02 lazy activation). Legacy corpus stays READ-ONLY; Fable v2 rewrites land later in `personas/<dept>/` per the staged program (Phase 5 first batch, Phase 10 waves).
    - Idempotent: ON CONFLICT (slug) DO NOTHING (re-run adds 0).
    Run `pnpm exec supabase db reset` (applies 0001+0002 fresh), then run the seed. Query counts inside the db container.
    EXPECTATION vs EVIDENCE: corpus measured 2026-07-07 → **159 classified personas across 13 persona-bearing department dirs** ("367" was a corrected myth — CEO directive 2026-07-07). The seed prints its own classifier counts; DB counts must equal them AND match 159/13. On ANY mismatch do NOT force numbers: report actual counts + seed-skipped.log diff in the summary and STOP for Fable review (⛔ count reconciliation is a phase-gate fact, not an executor judgment call).
  </action>
  <verify>
    <automated>pnpm exec supabase db reset && docker exec $(docker ps --format '{{.Names}}' | grep supabase_db) psql -U postgres -d postgres -tAc "SELECT count(*) FROM agents" && docker exec $(docker ps --format '{{.Names}}' | grep supabase_db) psql -U postgres -d postgres -tAc "\df claim_next_task" | grep -q claim_next_task && echo "0001+0002+seed OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` exit 0 with 0001+0002 applied
    - `\df claim_next_task` and `\df reap_expired_leases` list both functions
    - Seed run prints classifier counts + seed-skipped.log; agents count equals the classifier count AND the measured 159 (evidence decides, mismatch halts for Fable)
    - Second seed run inserts 0 rows (idempotency proven)
    - `psql -c "UPDATE task_events SET event='x' WHERE false"` as non-superuser role check documented OR REVOKE verified via `\dp task_events` showing no UPDATE/DELETE for PUBLIC
  </acceptance_criteria>
  <done>Operational core + registry schema live; 159-persona legacy registry seeded dormant (v1.0-legacy), idempotent, un-abortable, skip-audited.</done>
</task>

<task type="auto">
  <name>Task 3: Migrations 0003-0006 + trigger negative tests + full reset</name>
  <files>db/migrations/0003_approvals_outbox.sql, db/migrations/0004_cost_audit.sql, db/migrations/0005_memory_index.sql, db/migrations/0006_crm.sql</files>
  <read_first>
    - .planning/master-plan/PHASE-03.md §3 — 0003/0004/0005/0006 SQL blocks (LOCKED; 0006 note: RLS ENABLE on all four CRM tables must be written out explicitly)
  </read_first>
  <action>
    Create the four remaining migrations verbatim (approvals+outbox+guard/enqueue triggers; cost_ledger+audit_log append-only; memory_index; 4 CRM tables with explicit `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` per table).
    `pnpm exec supabase db reset` — all six from scratch, exit 0.
    Negative tests inside the db container (each MUST behave as specified):
    1. `INSERT approvals (draft)` → `UPDATE ... SET status='pending'` OK → `UPDATE ... SET status='draft'` back → EXPECT EXCEPTION ('pending→draft yasak' path: pending may only go approved/rejected).
    2. `UPDATE ... SET status='approved'` on the pending row → EXPECT outbox row born with idempotency_key = action_type||':'||id.
    3. `UPDATE ... SET status='rejected'` on an already-approved row → EXPECT EXCEPTION ('karar değiştirilemez').
    4. `UPDATE audit_log`/`DELETE FROM task_events` as a role without grants → blocked; verify grants via `\dp` output showing REVOKE took effect.
    Count tables: `\dt` lists ≥13 tables (tasks, task_events, departments, agents, approvals, outbox, cost_ledger, audit_log, memory_index, crm_clients, crm_contacts, crm_requests, crm_deals).
  </action>
  <verify>
    <automated>pnpm exec supabase db reset && docker exec $(docker ps --format '{{.Names}}' | grep supabase_db) psql -U postgres -d postgres -tAc "SELECT count(*) FROM information_schema.tables WHERE table_schema='public'" && echo "full schema OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` exit 0 over all six migrations (fresh DB)
    - public schema table count ≥ 13
    - Negative tests 1-3 raise the exact trigger exceptions; test 2 births exactly one outbox row
    - RLS enabled on every new table (`SELECT relname FROM pg_class WHERE relrowsecurity` covers all 13)
  </acceptance_criteria>
  <done>Full LOCKED schema applies clean from zero; approval state machine provably one-way; append-only provably enforced.</done>
</task>

</tasks>

<artifacts_produced>
- supabase/config.toml + minimal-service local stack runbook (in summary)
- db/migrations/0001-0006 (LOCKED SQL, byte-faithful)
- db/seed/import-personas.ts (idempotent, exclusion-aware, frontmatter-tolerant)
- .env.example with local DXB_DATABASE_URL
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| master-plan LOCKED SQL → migration files | Schema authority transfer; divergence = governance violation |
| agency-agents/*.md → agents rows | Untrusted file content parsed by the seed script |
| local dev credentials → repo | .env.example carries only public local-dev defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Tampering | schema drift vs LOCKED spec | high | mitigate | Byte-faithful copy; any change is ⛔ FABLE-ONLY; summary records diff-vs-masterplan check |
| T-3-05 | Tampering | approval state machine bypass | high | mitigate | Trigger negative tests 1-3 executed, exceptions observed (not assumed) |
| T-3-06 | Repudiation | audit/event mutability | high | mitigate | REVOKE UPDATE/DELETE verified via \dp evidence; RLS enabled everywhere |
| T-3-07 | Injection | persona frontmatter → SQL | medium | mitigate | Seed uses parameterized inserts only; no string-concatenated SQL |
| T-3-08 | Info disclosure | credentials in repo | medium | mitigate | Only public local-dev defaults in .env.example; real env stays in gitignored .env (Phase-1 vault pattern) |
</threat_model>

<verification>
- `pnpm exec supabase db reset` exit 0 (all six, fresh).
- claim/reaper functions listed; agents=corpus count; departments seeded; idempotent re-run.
- Trigger exceptions raised verbatim; outbox auto-birth observed; ≥13 tables; RLS everywhere.
</verification>

<success_criteria>
Phase-3 gate criterion 1 fully met (schema applies clean to fresh DB) and the registry half of criterion 4 (159 dormant v1.0-legacy personas, classifier-evidenced) is live — with append-only and one-way-decision guarantees demonstrated, not asserted.
</success_criteria>

<output>
Create `.planning/phases/03-state-layer-dxb-mcp-core/03-02-SUMMARY.md` when done (paste reset exit line, counts, and the three trigger-exception messages as evidence).
</output>
