---
phase: 03-state-layer-dxb-mcp-core
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - tests/phase3/crash.test.ts
  - tests/phase3/crash-worker.ts
  - scripts/phase3-lifecycle-battery.mjs
  - .planning/research/INTEGRATION-TRACKER.md
  - .planning/phases/03-state-layer-dxb-mcp-core/03-VERIFICATION.md
autonomous: true
requirements: [QUEUE-02]
must_haves:
  truths:
    - "A claimed task whose owner dies by kill -9 is re-claimable after lease expiry via reap_expired_leases() — zero state loss, 'reaped' event recorded (QUEUE-02)"
    - "10/10 consecutive full lifecycles pass via the battery script (exit 0) — phase exit gate"
    - "INTEGRATION-TRACKER EMBED column flipped for the Phase-3 toolset rows actually embedded in shipped code; validator exit 0"
    - "03-VERIFICATION.md records every gate criterion with its executed command and decisive output line"
  artifacts:
    - "tests/phase3/crash.test.ts + crash-worker.ts (real kill -9, not simulated)"
    - "scripts/phase3-lifecycle-battery.mjs (10-run loop, exit code = gate)"
    - "03-VERIFICATION.md (evidence table for all 5 gate criteria)"
  key_links:
    - "crash test <-> claim_next_task lease + reap_expired_leases (0001 functions under real process death)"
    - "battery script <-> gate criterion 'a task moves through the complete lifecycle' at 10/10 reliability"
    - "VERIFICATION.md <-> Fable phase-closure verdict (⛔ FABLE-ONLY reads it before declaring the phase complete)"
---

<objective>
Prove durability and close the phase: real-process crash test, ten-of-ten lifecycle battery, tracker EMBED truth-up, and the evidence file the Fable closure verdict will read.

Purpose: gate criterion 3 (crash durability) + the phase-exit reliability bar + honest tracker state.
Output: crash test green, battery exit 0, tracker validated, 03-VERIFICATION.md complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-03.md   (steps 11-12 + gate list)
@.planning/phases/03-state-layer-dxb-mcp-core/03-02-PLAN.md
@.planning/phases/03-state-layer-dxb-mcp-core/03-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crash test — kill -9 the claim owner, reap, re-claim</name>
  <files>tests/phase3/crash.test.ts, tests/phase3/crash-worker.ts</files>
  <read_first>
    - db/migrations/0001_operational_core.sql — claim_next_task(p_lease_seconds) + reap_expired_leases semantics
  </read_first>
  <action>
    crash-worker.ts: standalone script — connects via @dxb/shared getDb(), claims the next task for its department with a SHORT lease (e.g. 2s), prints the claimed task id to stdout, then sleeps forever (never completes, never releases).
    crash.test.ts (vitest):
    1. Seed one queued task (department 'crash-test').
    2. Spawn crash-worker.ts as a REAL child process (node child_process.spawn); read the claimed id from its stdout; assert task status 'claimed'.
    3. `process.kill(child.pid, 'SIGKILL')` — hard death, no cleanup path.
    4. Wait past lease expiry; call `SELECT reap_expired_leases()` → returns ≥ 1.
    5. Assert: task status back to 'queued', claimed_by/lease NULL, task_events has a 'reaped' row with was_claimed_by payload.
    6. Claim again with a new worker id → succeeds; SAME task id — zero state loss.
    Timing discipline: lease 2s, single wait with margin (e.g. 3s) — no polling loops that could flake; document the timing choice in the test header.
  </action>
  <verify>
    <automated>pnpm vitest run tests/phase3/crash.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Child provably dies via SIGKILL (exit signal asserted), not graceful shutdown
    - reap returns ≥1; task re-claimed by a different worker; 'reaped' event present with was_claimed_by
    - Test green twice in a row (`vitest run` executed twice) — flake check
  </acceptance_criteria>
  <done>Gate criterion 3 demonstrated with a real process death, not a simulation.</done>
</task>

<task type="auto">
  <name>Task 2: 10/10 lifecycle battery + tracker EMBED truth-up</name>
  <files>scripts/phase3-lifecycle-battery.mjs, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - tests/phase3/lifecycle.test.ts — the canonical chain to repeat
    - .planning/research/INTEGRATION-TRACKER.md — EMBED column semantics (embedded = shipped code imports/uses it)
  </read_first>
  <action>
    scripts/phase3-lifecycle-battery.mjs: standalone ESM (node, no vitest) — loops 10 iterations; each: create (unique objective) → claim → running → review → awaiting_approval → done via the SAME tool-call path the tests use (in-process server + InMemoryTransport client, imported from dxb-mcp build output or via tsx); one returned-path run appended as iteration 10's variant (review→returned→queued→re-claim→done). Prints `iteration N OK` per run, `10/10 PASS` and exit 0 only if ALL pass; any failure → nonzero exit with the failing iteration's error.
    Run it. Gate: exit 0.
    Tracker: flip EMBED for rows whose packages are now imported by shipped code (mcp-sdk, zod, kysely, pg, supabase-cli, supabase-js — flip ONLY what `grep -r "from '"` proves is imported; supabase-js stays un-embedded if nothing imports it yet — tracker must state reality, not intention). Re-run `node scripts/check-integration-tracker.mjs`.
  </action>
  <verify>
    <automated>node scripts/phase3-lifecycle-battery.mjs && node scripts/check-integration-tracker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Battery prints `10/10 PASS`, exit 0
    - EMBED flipped only for import-proven packages (grep evidence in summary); validator exit 0
  </acceptance_criteria>
  <done>Phase exit reliability bar met; tracker reflects shipped reality.</done>
</task>

<task type="auto">
  <name>Task 3: 03-VERIFICATION.md — gate evidence table</name>
  <files>.planning/phases/03-state-layer-dxb-mcp-core/03-VERIFICATION.md</files>
  <read_first>
    - .planning/master-plan/PHASE-03.md §1 — the five gate criteria verbatim
  </read_first>
  <action>
    Re-run each gate criterion's decisive command NOW (fresh, this task, not copied from earlier logs) and record command + decisive output line per criterion:
    1. `pnpm exec supabase db reset` → exit 0
    2. lifecycle via MCP tools → battery `10/10 PASS` line + returned-path evidence
    3. crash test → vitest pass line
    4. agents count + dormant status + registry_create_department test line
    5. tools/list ≥ 17 across 8 groups + stub error line
    Structure: frontmatter `status: passed|failed` (truthful — if ANY criterion fails, status: failed and the phase does NOT close), one section per criterion: command, output, PASS/FAIL. Close with a `## Fable closure input` note: this file is evidence FOR the ⛔ FABLE-ONLY phase verdict — it does not itself declare the phase complete.
  </action>
  <verify>
    <automated>test -f ".planning/phases/03-state-layer-dxb-mcp-core/03-VERIFICATION.md" && grep -q 'status:' ".planning/phases/03-state-layer-dxb-mcp-core/03-VERIFICATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - All five criteria have freshly-executed command + output evidence
    - status field truthful; no criterion marked PASS without its output line present
  </acceptance_criteria>
  <done>Closure evidence complete; phase verdict unblocked (verdict itself stays ⛔ FABLE-ONLY).</done>
</task>

</tasks>

<artifacts_produced>
- tests/phase3/crash.test.ts + crash-worker.ts (SIGKILL durability proof)
- scripts/phase3-lifecycle-battery.mjs (10/10 gate, exit-code driven)
- Updated tracker (EMBED truth) + 03-VERIFICATION.md (5-criteria evidence)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker process death → task state | The exact failure mode QUEUE-02 exists for |
| test evidence → phase closure | Verdict quality depends on evidence being freshly executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-17 | DoS | orphaned leases starve the queue | high | mitigate | reap_expired_leases proven under real SIGKILL; reaper scheduling itself lands with pg-boss in Phase 4 (documented gap, not silent) |
| T-3-18 | Repudiation | stale/copied evidence in VERIFICATION | medium | mitigate | Task 3 mandates fresh re-execution; every PASS requires its output line inline |
</threat_model>

<verification>
- Crash test green ×2; battery `10/10 PASS` exit 0; validator exit 0; VERIFICATION.md complete and truthful.
</verification>

<success_criteria>
Gate criterion 3 proven, reliability battery passed, tracker truthful, and the evidence file the Fable phase verdict requires exists. Master-plan steps 11-12 closed — phase ready for ⛔ FABLE-ONLY closure.
</success_criteria>

<output>
Create `.planning/phases/03-state-layer-dxb-mcp-core/03-05-SUMMARY.md` when done (paste crash-test pass, `10/10 PASS`, validator lines as evidence).
</output>
