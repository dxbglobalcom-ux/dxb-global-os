---
phase: 03-state-layer-dxb-mcp-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/study-cards/kysely.md
  - .planning/research/study-cards/pg.md
  - .planning/research/INTEGRATION-TRACKER.md
  - .planning/phases/03-state-layer-dxb-mcp-core/03-01-legitimacy-approval.md
  - pnpm-workspace.yaml
  - package.json
  - packages/shared/package.json
  - packages/dxb-mcp/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [INTEG-01, MCP-01]
must_haves:
  truths:
    - "kysely and pg have FULL study cards on disk BEFORE their install (study-before-install, no exceptions)"
    - "CEO has approved the Phase-3 install list via a blocking human checkpoint; the decision is recorded on disk"
    - "All Phase-3 packages resolve at their CLAUDE.md-pinned versions; pg-boss is NOT installed (deferred to Phase 4 per master-plan)"
    - "INTEGRATION-TRACKER INSTALL column flipped only for actually-installed rows; tracker-integrity validator exits 0"
  artifacts:
    - ".planning/research/study-cards/kysely.md and pg.md (full cards, STUDY checked)"
    - "03-01-legitimacy-approval.md (CEO decision record)"
    - "updated pnpm-lock.yaml with Phase-3 toolset"
  key_links:
    - "study cards <-> tracker rows (validator rule 3 card coverage)"
    - "CEO approval record <-> pnpm install run (install only after approved)"
    - "catalog pins <-> CLAUDE.md version table (no drift)"
---

<objective>
Open Phase 3's toolset gate the same way 02-02/02-04 did: study cards first, human supply-chain approval second, install third — then flip tracker INSTALL cells and prove the workspace still builds green.

Purpose: INTEG-01 discipline — no blind install, ever. This plan is the only Phase-3 plan that touches the npm supply chain; every later plan consumes what lands here.
Output: 2 new study cards, CEO approval record, installed+pinned toolset, updated tracker, green build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-03.md         (LOCKED decisions + step 1)
@.planning/research/INTEGRATION-TRACKER.md
@.planning/research/study-cards/supabase-cli.md
@.planning/research/study-cards/supabase-js.md
@.planning/research/study-cards/mcp-sdk.md
@.planning/research/study-cards/zod.md
@.planning/phases/02-foundation-integration-program/02-02-SUMMARY.md  (human-gate precedent)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author kysely + pg study cards and tracker rows</name>
  <files>.planning/research/study-cards/kysely.md, .planning/research/study-cards/pg.md, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - .planning/research/study-cards/zod.md — card template shape (pinned version, exact install command un-run, legitimacy verdict, pitfalls, lifecycle checklist)
    - .planning/master-plan/PHASE-03.md §5 — kysely decision rationale (SQL-first migrations own the schema; drizzle's TS-first model conflicts). Revision of this choice is ⛔ FABLE-ONLY.
  </read_first>
  <action>
    Write FULL study cards (same template as the nine 02-04 cards) for:
    1. `kysely` — typed SQL query builder chosen over drizzle (decision recorded in master-plan PHASE-03 §5). Card covers: PostgresDialect over `pg` Pool, session-mode 5432 requirement shared with pg-boss pitfall, hand-written `DB` interface pattern (schema authority = SQL migrations, so NO codegen dependency), transaction API used by queue transitions.
    2. `pg` (node-postgres) — the driver under kysely's PostgresDialect. Card covers: Pool vs Client, session-mode direct connection (NOT PgBouncer transaction pooling — same constraint as pg-boss in CLAUDE.md), parameterized queries as the injection barrier, `@types/pg` dev-types companion.
    Both cards: pinned version fetched live from npm registry at authoring time, exact `pnpm add` command recorded but NOT run, legitimacy verdict with registry evidence (created date, publisher, weekly downloads, exact-spelling check), STUDY checked.
    Add two `lib`-trigger rows to INTEGRATION-TRACKER.md main table (Status: STUDY done, INSTALL pending) pointing at the new cards. Do NOT touch any EXCLUDED row.
    Run `node scripts/check-integration-tracker.mjs` — must exit 0 with 53 non-excluded rows / 53 cards.
  </action>
  <verify>
    <automated>test -f .planning/research/study-cards/kysely.md && test -f .planning/research/study-cards/pg.md && node scripts/check-integration-tracker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both cards exist with legitimacy verdict + pinned version + un-run install command
    - Tracker gains exactly 2 non-excluded rows; validator exit 0
    - `grep -c 'EXCLUDED' .planning/research/INTEGRATION-TRACKER.md` unchanged from before the edit
  </acceptance_criteria>
  <done>kysely + pg carded and tracked BEFORE any install command runs.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: CEO supply-chain legitimacy checkpoint — Phase-3 install list</name>
  <files>.planning/phases/03-state-layer-dxb-mcp-core/03-01-legitimacy-approval.md</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md — record format precedent
  </read_first>
  <action>
    Fetch live npm registry evidence inline (no subagent — governance v4) for EVERY package on the install list:
    `supabase` (CLI, dev), `@supabase/supabase-js`, `@modelcontextprotocol/sdk`, `zod`, `kysely`, `pg`, `@types/pg` (dev).
    For each: package age/created date, repository, publisher/maintainers, weekly downloads, character-for-character spelling check against the catalog entry about to be written. Flag anything anomalous.
    Explicitly present the TWO build-script decisions the install will need:
    - `supabase` npm package runs a postinstall that downloads the CLI binary from GitHub releases — install cannot work without allowing it. Proposal: `allowBuilds: supabase: true` (explicit, recorded).
    - any other package requesting build scripts defaults to DENY (esbuild precedent from 02-03).
    Present the full list + evidence + allowBuilds proposal to the CEO via blocking checkpoint. Record the decision (approved/rejected, per-package if split) in 03-01-legitimacy-approval.md.
    If rejected or no decision: STOP. Task 3 must not run.
  </action>
  <verify>
    <automated>grep -q 'Decision.*approved' ".planning/phases/03-state-layer-dxb-mcp-core/03-01-legitimacy-approval.md"</automated>
  </verify>
  <acceptance_criteria>
    - Approval record exists on disk with per-package registry evidence and the explicit allowBuilds decision
    - Checkpoint was blocking — no install command ran before the recorded approval
  </acceptance_criteria>
  <done>Supply-chain trust decision is auditable on disk; install gate open (or plan halted on rejection).</done>
</task>

<task type="auto">
  <name>Task 3: Install pinned toolset, flip tracker INSTALL cells, prove green</name>
  <files>pnpm-workspace.yaml, package.json, packages/shared/package.json, packages/dxb-mcp/package.json, pnpm-lock.yaml, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - CLAUDE.md "Version Compatibility" table — pins: @modelcontextprotocol/sdk 1.29.0, zod 4.x, supabase-js 2.110.x
    - .planning/phases/02-foundation-integration-program/02-03-SUMMARY.md — Corepack/allowBuilds mechanics on this machine
  </read_first>
  <action>
    Extend the pnpm catalog with pinned entries: `@modelcontextprotocol/sdk` (1.29.x), `zod` (^4), `@supabase/supabase-js` (^2.110), `kysely` (card-pinned), `pg` (card-pinned), `@types/pg` (dev), `supabase` (dev, CLI).
    Wire dependencies where they are USED (no root dumping): packages/shared ← zod, kysely, pg, @types/pg, @supabase/supabase-js; packages/dxb-mcp ← @modelcontextprotocol/sdk, zod (+ workspace @dxb/shared already present). `supabase` CLI → root devDependencies.
    Record the approved build-script decision in pnpm-workspace.yaml `allowBuilds`: `supabase: true` (per Task 2 record); every other new package absent from allowBuilds (deny-by-default stays).
    Run `pnpm install`. If a NEW package other than `supabase` demands a build script, STOP and report — that requires a fresh CEO decision, not an inline allow.
    pg-boss: do NOT install. Confirm its tracker row still says Phase-4 install.
    Flip INSTALL column to done for: supabase (CLI), supabase-js, mcp-sdk, zod, kysely, pg rows. Re-run validator.
    Prove the workspace is still green: `pnpm build && pnpm test`.
  </action>
  <verify>
    <automated>pnpm -r ls @modelcontextprotocol/sdk zod @supabase/supabase-js kysely pg | grep -E '@modelcontextprotocol/sdk|zod|supabase-js|kysely|pg ' && pnpm build && pnpm test && node scripts/check-integration-tracker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r ls` shows all six runtime/dev packages at catalog-pinned versions
    - `! grep -q 'pg-boss' pnpm-lock.yaml` exits 0 (pg-boss NOT in the tree)
    - `supabase --version` (via `pnpm exec supabase --version`) prints a version — CLI binary landed
    - `pnpm build` and `pnpm test` exit 0
    - validator exit 0; INSTALL flipped only on installed rows
  </acceptance_criteria>
  <done>Phase-3 toolset installed at pinned versions under an auditable human gate; tracker truthful; workspace green.</done>
</task>

</tasks>

<artifacts_produced>
- Study cards: kysely.md, pg.md (full, pre-install)
- 03-01-legitimacy-approval.md — CEO decision record incl. allowBuilds: supabase
- Updated catalog + per-package deps + pnpm-lock.yaml
- INTEGRATION-TRACKER.md: +2 rows, INSTALL cells flipped, validator green
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party code enters the build machine |
| postinstall scripts → local machine | `supabase` postinstall downloads and executes a binary fetch |
| approval record → install action | CEO sign-off authorizes the supply-chain expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | npm packages (typosquat/malicious) | high | mitigate | Blocking CEO checkpoint with live registry evidence; character-exact spelling check; catalog version pins; lockfile committed |
| T-3-02 | Elevation | postinstall build scripts | high | mitigate | pnpm deny-by-default; single explicit `allowBuilds: supabase: true` recorded in the approval; any other build-script request halts the plan |
| T-3-03 | Tampering | version drift vs studied cards | medium | mitigate | Card-pinned versions written into the catalog; `pnpm -r ls` acceptance check against pins |
</threat_model>

<verification>
- Cards + tracker rows exist BEFORE install (git history order proves it — separate commits).
- Approval record on disk precedes the lockfile change.
- `pnpm build && pnpm test` green after install; validator exit 0; pg-boss absent.
</verification>

<success_criteria>
Phase-3 toolset is studied, human-approved, installed at pinned versions, and tracked — with the workspace still compiling and testing clean. Master-plan step 1 closed.
</success_criteria>

<output>
Create `.planning/phases/03-state-layer-dxb-mcp-core/03-01-SUMMARY.md` when done (paste decisive `pnpm -r ls`, validator, and build/test output lines as evidence).
</output>
