---
phase: 03-state-layer-dxb-mcp-core
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/shared/src/envelope.ts
  - packages/shared/src/db.ts
  - packages/shared/src/db-types.ts
  - packages/shared/src/index.ts
  - tests/phase3/envelope.test.ts
autonomous: true
requirements: [QUEUE-01]
must_haves:
  truths:
    - "TaskEnvelope Zod schema exists byte-faithful to master-plan PHASE-03 (LOCKED) and rejects objective < 20 chars"
    - "One shared Kysely client over pg (direct 5432 session mode) is the ONLY db access path exported by @dxb/shared"
    - "`pnpm -r exec tsc --build` stays green with the new shared surface"
  artifacts:
    - "packages/shared/src/envelope.ts (LOCKED, verbatim)"
    - "packages/shared/src/db.ts + db-types.ts (hand-written DB interface matching the SQL migrations)"
    - "tests/phase3/envelope.test.ts (unit, no DB needed)"
  key_links:
    - "envelope.ts <-> queue.create_task input (dxb-mcp consumes this exact schema in 03-04)"
    - "db-types.ts <-> db/migrations DDL (columns match 1:1; SQL is the authority)"
    - "db.ts env var <-> .env.example DXB_DATABASE_URL (03-02)"
---

<objective>
Give every package one typed contract layer: the LOCKED TaskEnvelope schema plus a single Kysely-over-pg client with hand-written table types mirroring the migrations.

Purpose: dxb-mcp (03-04) must consume validation and DB access from @dxb/shared — never roll its own. This plan is that seam.
Output: envelope.ts (verbatim), db.ts + db-types.ts, green build, envelope unit tests passing without a running DB.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-03.md   (envelope.ts LOCKED block + db.ts spec line)
@.planning/research/study-cards/zod.md
@.planning/research/study-cards/kysely.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: envelope.ts (LOCKED) + unit tests</name>
  <files>packages/shared/src/envelope.ts, packages/shared/src/index.ts, tests/phase3/envelope.test.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-03.md §3 "envelope.ts (birebir — LOCKED)" — copy the TypeScript verbatim
  </read_first>
  <action>
    Create packages/shared/src/envelope.ts exactly as the master-plan block (imports z from "zod"; TaskEnvelope object + inferred type). No stylistic edits — LOCKED means verbatim.
    Re-export from packages/shared/src/index.ts (keep the existing PACKAGE export intact — 02-01 scaffold contract).
    Write tests/phase3/envelope.test.ts (vitest, pure unit — no DB):
    - objective shorter than 20 chars → parse FAILS (the master-plan's named acceptance test)
    - output_contract shorter than 10 chars → FAILS
    - model_tier outside L1-L4 → FAILS; priority 10 → FAILS (max 9)
    - minimal valid input → parses with defaults: approval_class 'none', budget {200000, 1.0}, priority 0, parent_task_id null
    - parent_task_id must be uuid-or-null (bad string fails)
  </action>
  <verify>
    <automated>pnpm vitest run tests/phase3/envelope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - envelope.ts diff vs master-plan block is content-identical (whitespace-insensitive check acceptable)
    - All envelope unit tests pass; the objective<20 rejection test exists and passes
    - `pnpm build` exit 0
  </acceptance_criteria>
  <done>LOCKED envelope contract live and provably enforcing its floors/defaults.</done>
</task>

<task type="auto">
  <name>Task 2: db-types.ts + db.ts single client</name>
  <files>packages/shared/src/db-types.ts, packages/shared/src/db.ts, packages/shared/src/index.ts</files>
  <read_first>
    - db/migrations/0001-0006 (as landed by 03-02; if 03-02 not yet executed, the master-plan §3 SQL is the same authority) — column names/types for the DB interface
    - .planning/research/study-cards/kysely.md — PostgresDialect + Pool pattern, session-mode note
  </read_first>
  <action>
    Write db-types.ts: a Kysely `DB` interface with one interface per table (tasks, task_events, departments, agents, approvals, outbox, cost_ledger, audit_log, memory_index, crm_clients, crm_contacts, crm_requests, crm_deals). Hand-written from the SQL — the migrations are the authority; use Kysely's `Generated<...>` for DB-defaulted columns (ids, created_at, updated_at, status defaults) and `ColumnType` where insert/select types differ (timestamptz → Date select, string|Date insert). jsonb columns type as `unknown` at this layer (Zod refines at the MCP boundary).
    Write db.ts: lazy singleton `getDb(): Kysely<DB>` over `new PostgresDialect({ pool: new pg.Pool({ connectionString: process.env.DXB_DATABASE_URL }) })`. Direct 5432/54322 session mode — NEVER a transaction-pooler URL (pg-boss/kysely shared constraint, CLAUDE.md). Throw a clear error if DXB_DATABASE_URL is unset. Export also `closeDb()` for tests. No other file may construct a pg Pool — single client rule (master-plan file spec).
    Re-export getDb/closeDb/DB types from index.ts.
    Compile proof only in this task (`pnpm build`); live connection is exercised by 03-04/03-05 tests against the running stack. If the 03-02 stack happens to be up, run an optional `SELECT 1` smoke and record it — do not fail the task if the stack is down (wave-2 parallel ordering).
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - db-types.ts covers all 13 tables with column sets matching the migrations 1:1
    - db.ts exports exactly one client construction path; grep proves no other `new pg.Pool(` in packages/
    - Missing DXB_DATABASE_URL → thrown error with actionable message (unit-testable without DB)
    - `pnpm build` and `pnpm test` exit 0
  </acceptance_criteria>
  <done>@dxb/shared exposes the one-and-only typed DB path plus the LOCKED envelope; workspace green.</done>
</task>

</tasks>

<artifacts_produced>
- packages/shared/src/envelope.ts (LOCKED verbatim) + envelope unit tests
- packages/shared/src/db-types.ts (13-table DB interface, SQL-authoritative)
- packages/shared/src/db.ts (lazy singleton Kysely client, env-driven, session mode)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task payload → envelope.parse | Untrusted agent input meets the Zod floor |
| env → connection string | Credentials enter only via environment (vault pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-09 | Tampering | envelope contract drift | high | mitigate | Verbatim LOCKED copy + unit tests pin floors/defaults/enums |
| T-3-10 | Info disclosure | connection string in code | medium | mitigate | env-only (DXB_DATABASE_URL); no literal DSN anywhere; error message never echoes the URL |
| T-3-11 | Injection | ad-hoc SQL escapes the typed layer | medium | mitigate | Single-client rule + Kysely parameterized builders; grep gate for stray pg.Pool constructions |
</threat_model>

<verification>
- envelope tests green incl. objective<20 rejection; build green workspace-wide.
- Single-Pool grep gate holds; env error path tested.
</verification>

<success_criteria>
Shared contract layer done: the exact LOCKED envelope + one typed DB client, consumed (not reimplemented) by dxb-mcp in 03-04. Master-plan step 7 closed.
</success_criteria>

<output>
Create `.planning/phases/03-state-layer-dxb-mcp-core/03-03-SUMMARY.md` when done (paste envelope test pass lines + build exit as evidence).
</output>
