---
phase: 02-foundation-integration-program
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/INTEGRATION-TRACKER.md
  - .planning/research/study-cards/supabase.md
  - .planning/research/study-cards/supabase-js.md
  - .planning/research/study-cards/supabase-cli.md
  - .planning/research/study-cards/mcp-sdk.md
  - .planning/research/study-cards/pg-boss.md
  - .planning/research/study-cards/zod.md
  - .planning/research/study-cards/claude-agent-sdk.md
  - .planning/research/study-cards/playwright-mcp.md
  - .planning/research/study-cards/context7.md
autonomous: true
requirements: [INTEG-01, INTEG-02]
must_haves:
  truths:
    - "Every master-plan §8B item has exactly one tracking row with a STUDY→INSTALL→ADOPT→EMBED (or EXCLUDED) status and a named study-card destination"
    - "The four excluded items are recorded with reasons and an explicit CEO-sign-off re-admission requirement"
    - "A full study card exists for every tool in the Phase 3 toolset BEFORE any Phase 3 install — demonstrating the install-at-phase-start rule"
  artifacts:
    - ".planning/research/INTEGRATION-TRACKER.md (single tracker: seed rows + Excluded table + Re-admission Log + Status legend)"
    - "Nine full Phase-3-toolset study cards under .planning/research/study-cards/"
  key_links:
    - "§8B source inventory <-> one tracker row per item (no item skipped)"
    - "each non-EXCLUDED row's Study Card column <-> an actual study-cards/<slug>.md file (validated in Plan 02-05)"
    - "EXCLUDED status <-> Re-admission Log CEO sign-off requirement (INTEG-02)"
---

<objective>
Create the single source-of-truth integration tracker and the Phase-3-toolset study cards. The tracker gives every doc-mandated §8B tool an auditable STUDY→INSTALL→ADOPT→EMBED row (INTEG-01) and records the excluded items with re-admission gates (INTEG-02). The nine full study cards prove success criterion 3: the Phase 3 toolset is studied before anything is installed.

Purpose: Guarantees no doc-mandated tool is skipped or installed blind, and demonstrates the "tools studied at the START of the phase that uses them" rule with a concrete, complete Phase-3 example.
Output: `INTEGRATION-TRACKER.md` + 9 full study cards. Remaining-row stubs and the machine-checkable validator are Plan 02-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-foundation-integration-program/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build INTEGRATION-TRACKER.md with all §8B rows, excluded items, and re-admission log</name>
  <files>.planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Integration Tracking Program (INTEG-01 / INTEG-02)": the full seed-data table (~45 §8B rows with Item/Category/Status/Target Phase/Owner/Trigger/Notes), the "Excluded items (INTEG-02)" table (4 items + reasons + re-admission requirement), the Status enum, and the phase-mapping caveat
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Standard Stack — Supporting" table (the six Phase-3+ locked-stack runtime libs that Phase 3 will `pnpm add`: claude-agent-sdk, @modelcontextprotocol/sdk, pg-boss, @supabase/supabase-js, supabase CLI, zod)
    - .planning/ROADMAP.md — the locked P0–P7 → Phase 1–11 renumbering (map any §8B "P#" tag to the new number)
    - docs/source-architecture-notes-sanitized.md — the §8B MCP classification (A. Core / B. Department / C. Custom DXB MCPs) to sanity-check nothing in the doc's tool list is missing from the seed set
  </read_first>
  <action>
    Create `.planning/research/INTEGRATION-TRACKER.md` with these sections:
      1. Header + a Status legend: the valid Status tokens are the five bare words STUDY, INSTALL, ADOPT, EMBED, EXCLUDED (one word, no parentheses — a row advances only left-to-right; EXCLUDED is terminal unless a Re-admission Log entry supersedes it). State the column schema: `Item | Category | Status | Target Phase | Owner (dept/tier) | Trigger | Study Card | Notes`.
      2. The main tracking table. Transcribe ALL ~45 §8B seed rows from 02-RESEARCH exactly, PLUS one row each for the six Phase-3 locked-stack runtime libs above (Status STUDY, Target Phase 3). For every row: put ONLY a bare Status token in the Status column; move any qualifier the research wrote in parentheses (retroactive / conditional / verified active) into the Notes column. Fill the `Study Card` column for every non-EXCLUDED row with a relative path `study-cards/<slug>.md` (kebab-case slug of the item). Map each §8B "P#" tag to the new Phase number per the ROADMAP renumbering; mark rows the research flagged `[ASSUMED]` so the CEO can confirm them at that phase's discuss step.
      3. A `## Excluded Items (INTEG-02)` table: kickbacks.ai, automaton, llm-council (as dependency), and ToS-gray systems — each with the reason and "explicit CEO sign-off entry in the Re-admission Log required" (transcribe from 02-RESEARCH "Excluded items").
      4. A `## Re-admission Log` section with an explanatory line and NO entries yet (the machine rule: an EXCLUDED row may only change status if a dated CEO-sign-off entry appears here).
    Do NOT author study cards in this task (Task 2 + Plan 02-05 handle those). Do NOT invent tools not in §8B or the locked stack.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/research/INTEGRATION-TRACKER.md','utf8');const rows=t.split('\n').filter(l=>/^\|/.test(l)&&!/^\|\s*-+/.test(l)&&!/Item\s*\|\s*Category/.test(l));const bad=rows.filter(r=>{const c=r.split('|').map(s=>s.trim());const st=c[3];return st&&!/^(STUDY|INSTALL|ADOPT|EMBED|EXCLUDED)$/.test(st)&&/^(STUDY|INSTALL|ADOPT|EMBED|EXCLUDED|Status)/i.test(st)===false?false:(st&&!/^(STUDY|INSTALL|ADOPT|EMBED|EXCLUDED)$/.test(st)&&['STUDY','INSTALL','ADOPT','EMBED','EXCLUDED'].every(x=>x!==st)&&st!=='Status'&&st!=='')});if(!t.includes('## Re-admission Log')){console.error('missing Re-admission Log');process.exit(1)}for(const item of ['kickbacks.ai','automaton','llm-council','ToS']){if(!t.includes(item)){console.error('excluded item missing: '+item);process.exit(1)}}console.log('tracker structural OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/INTEGRATION-TRACKER.md` exits 0
    - `grep -q '## Re-admission Log' .planning/research/INTEGRATION-TRACKER.md` exits 0
    - `grep -q '## Excluded Items' .planning/research/INTEGRATION-TRACKER.md` exits 0
    - `for i in kickbacks.ai automaton llm-council ToS; do grep -q "$i" .planning/research/INTEGRATION-TRACKER.md || exit 1; done` exits 0 (all four excluded items present)
    - `awk -F'|' 'NF>3 && $1 ~ /^[[:space:]]*$/' .planning/research/INTEGRATION-TRACKER.md | grep -Ec 'STUDY|INSTALL|ADOPT|EMBED|EXCLUDED'` returns ≥ 49 (≥45 §8B rows + 6 Phase-3 libs; header/separator excluded by the leading-pipe empty-first-cell filter)
    - `grep -Eic 'STUDY \(|INSTALL \(|ADOPT \(|EMBED \(|EXCLUDED \(' .planning/research/INTEGRATION-TRACKER.md` returns 0 (Status column holds bare tokens only; qualifiers live in Notes)
  </acceptance_criteria>
  <done>The tracker holds one row per §8B item plus the six Phase-3 locked-stack libs, all Status values are bare enum tokens, every non-EXCLUDED row names a study-card path, the four excluded items carry reasons, and an empty Re-admission Log gate is present.</done>
</task>

<task type="auto">
  <name>Task 2: Author the nine full Phase-3-toolset study cards</name>
  <files>.planning/research/study-cards/supabase.md, .planning/research/study-cards/supabase-js.md, .planning/research/study-cards/supabase-cli.md, .planning/research/study-cards/mcp-sdk.md, .planning/research/study-cards/pg-boss.md, .planning/research/study-cards/zod.md, .planning/research/study-cards/claude-agent-sdk.md, .planning/research/study-cards/playwright-mcp.md, .planning/research/study-cards/context7.md</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Standard Stack — Supporting" (pinned versions + purpose for each Phase-3 lib), "Common Pitfalls" (Pitfall 1 local-Supabase-RAM, Pitfall 2 pg-boss session-mode connection, Pitfall 4 Agent SDK API drift / use query()+Task* not TodoWrite), "Package Legitimacy Audit" (verdicts), "State of the Art" (deprecated Agent SDK APIs)
    - ./.claude/CLAUDE.md — Version Compatibility table (Node/Zod/Postgres constraints per package)
  </read_first>
  <action>
    Create the `study-cards/` subdirectory and author nine FULL study cards (not stubs) for the Phase 3 toolset — the concrete set Phase 3 will install: `supabase` (self-hosted platform), `supabase-js` (@supabase/supabase-js client), `supabase-cli`, `mcp-sdk` (@modelcontextprotocol/sdk), `pg-boss`, `zod`, `claude-agent-sdk` (@anthropic-ai/claude-agent-sdk — foundational, study card owed before Phase 3 per success criterion 3), `playwright-mcp`, `context7`.
    Use one consistent study-card template with these headed fields: Tool, Slug, Category, Status, Target Phase, Owner (dept/tier), Trigger Type, Source (registry + GitHub URL), Pinned Version, Purpose, Official Docs URL, Key API / Usage Notes, Known Pitfalls (cite the relevant 02-RESEARCH pitfall), Install Command (the exact `pnpm add`/CLI command Phase 3 will run — recorded, not run now), Legitimacy Verdict, and a `STUDY / INSTALL / ADOPT / EMBED` checklist with STUDY checked and the rest unchecked.
    Populate real content from the read_first sources: e.g. pg-boss must note the session-mode (direct 5432) connection requirement (Pitfall 2); claude-agent-sdk must note use `query()` + `AsyncIterable<SDKUserMessage>` / `options.resume` and the Task* tool family, and that MCP servers connect non-blocking by default (Pitfall 4); supabase must note the local-stack RAM constraint → schema-only local dev (Pitfall 1). Do NOT run any install command in this task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const cards=['supabase','supabase-js','supabase-cli','mcp-sdk','pg-boss','zod','claude-agent-sdk','playwright-mcp','context7'];for(const c of cards){const p='.planning/research/study-cards/'+c+'.md';if(!fs.existsSync(p)){console.error('MISSING '+p);process.exit(1)}const s=fs.readFileSync(p,'utf8');for(const f of ['Pinned Version','Install Command','Legitimacy Verdict','STUDY']){if(!s.includes(f)){console.error(p+' missing field: '+f);process.exit(1)}}}console.log('9 Phase-3 study cards OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `for c in supabase supabase-js supabase-cli mcp-sdk pg-boss zod claude-agent-sdk playwright-mcp context7; do test -f .planning/research/study-cards/$c.md || exit 1; done` exits 0
    - `grep -lq 'session-mode\|session mode\|direct.*5432\|port 5432' .planning/research/study-cards/pg-boss.md` exits 0 (Pitfall 2 captured)
    - `grep -Eq 'query\(\)|options.resume|Task(Create|List)' .planning/research/study-cards/claude-agent-sdk.md` exits 0 (Pitfall 4 / current API captured)
    - `grep -Eq 'schema-only|schema only|RAM|259' .planning/research/study-cards/supabase.md` exits 0 (Pitfall 1 captured)
    - `for c in supabase-js mcp-sdk pg-boss zod; do grep -q 'pnpm add' .planning/research/study-cards/$c.md || exit 1; done` exits 0 (each records its future install command, un-run)
  </acceptance_criteria>
  <done>Nine full study cards exist for the Phase 3 toolset, each carrying pinned version, install command, legitimacy verdict, the relevant research pitfall, and a STUDY→EMBED checklist — success criterion 3 demonstrated before any install.</done>
</task>

</tasks>

<artifacts_produced>
- `.planning/research/INTEGRATION-TRACKER.md` — single tracker: ≥49 rows (≥45 §8B + 6 Phase-3 libs), `## Excluded Items` table (kickbacks.ai, automaton, llm-council, ToS-gray), `## Re-admission Log` gate, Status legend (STUDY|INSTALL|ADOPT|EMBED|EXCLUDED)
- `.planning/research/study-cards/` subdirectory
- Nine full study cards: `supabase.md`, `supabase-js.md`, `supabase-cli.md`, `mcp-sdk.md`, `pg-boss.md`, `zod.md`, `claude-agent-sdk.md`, `playwright-mcp.md`, `context7.md`
- Study-card slug convention (`study-cards/<slug>.md`) referenced by every non-EXCLUDED tracker row and validated in Plan 02-05
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §8B source doc → tracker rows | Fidelity boundary: the tracker must not drop or invent tools |
| CEO exclusion decisions → tracker EXCLUDED rows | Process-integrity boundary: excluded items must stay excluded absent sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Tampering / Elevation of Privilege | INTEGRATION-TRACKER.md EXCLUDED rows | medium | mitigate | Excluded items recorded with reasons + a `## Re-admission Log` CEO-sign-off gate; machine-enforced by the Plan 02-05 validator |
| T-2-05 | Tampering | §8B → tracker fidelity | low | mitigate | Seed transcribed row-for-row from 02-RESEARCH; acceptance floor of ≥49 rows and cross-check against the sanitized §8B MCP classification |
</threat_model>

<verification>
- Tracker has ≥49 rows, all bare Status tokens, Re-admission Log + all four excluded items present (Task 1).
- Nine Phase-3 study cards exist with the mandatory fields and captured pitfalls (Task 2).
</verification>

<success_criteria>
Every §8B item (and each Phase-3 locked-stack lib) has a tracking row with a valid status and a study-card destination; excluded items carry re-admission gates; and the full Phase-3 toolset is studied — on disk — before any Phase 3 install. Covers INTEG-01 (tracker + Phase-3 study cards) and INTEG-02 (excluded items + re-admission gate).
</success_criteria>

<output>
Create `.planning/phases/02-foundation-integration-program/02-04-SUMMARY.md` when done.
</output>
