---
phase: 02-foundation-integration-program
plan: 05
type: execute
wave: 2
depends_on: [02-04]
files_modified:
  - .planning/research/study-cards/superpowers.md
  - .planning/research/study-cards/gsd.md
  - .planning/research/study-cards/gstack.md
  - .planning/research/study-cards/ruflo.md
  - .planning/research/study-cards/claude-mem.md
  - .planning/research/study-cards/caveman.md
  - .planning/research/study-cards/headroom.md
  - .planning/research/study-cards/codex-plugin-cc.md
  - .planning/research/study-cards/graphify.md
  - .planning/research/study-cards/moneyprinterturbo.md
  - .planning/research/study-cards/voicebox.md
  - scripts/gen-study-card-stubs.mjs
  - scripts/check-integration-tracker.mjs
autonomous: true
requirements: [INTEG-01, INTEG-02]
must_haves:
  truths:
    - "Every non-EXCLUDED tracker row has a study-card file on disk (full for already-installed tools, stub for not-yet-studied tools)"
    - "A machine-checkable validator proves tracker integrity: valid status tokens, one study card per non-EXCLUDED row, and the EXCLUDED re-admission rule"
    - "Tools installed before this program existed have retroactive study cards (no silent 'assumed done' gaps)"
  artifacts:
    - "Retroactive study cards for the already-installed tools"
    - "scripts/gen-study-card-stubs.mjs (fills remaining non-EXCLUDED rows with template stubs)"
    - "scripts/check-integration-tracker.mjs (the INTEG-01/INTEG-02 compliance check)"
  key_links:
    - "tracker Study Card column <-> study-cards/<slug>.md existence (validator asserts one-to-one)"
    - "EXCLUDED status without a Re-admission Log entry <-> validator pass (validator asserts the rule)"
    - "already-installed tools <-> retroactive cards (Pitfall 5: no carve-out)"
---

<objective>
Complete the study-card set so every non-EXCLUDED tracker row has a card, backfill retroactive cards for the tools installed before this tracking program existed (Pitfall 5), and ship the machine-checkable validator that makes INTEG-01 and INTEG-02 auditable on every commit.

Purpose: Success criterion 2 says "the checklist holds each row until EMBED is done" with no carve-out for already-installed tools; INTEG-02's re-admission rule must be machine-enforced, not just documented.
Output: Retroactive full cards + a stub generator that covers the remaining rows + `scripts/check-integration-tracker.mjs` running green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/INTEGRATION-TRACKER.md
@.planning/phases/02-foundation-integration-program/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retroactive cards for already-installed tools + stub generator for remaining rows</name>
  <files>.planning/research/study-cards/superpowers.md, .planning/research/study-cards/gsd.md, .planning/research/study-cards/gstack.md, .planning/research/study-cards/ruflo.md, .planning/research/study-cards/claude-mem.md, .planning/research/study-cards/caveman.md, .planning/research/study-cards/headroom.md, .planning/research/study-cards/codex-plugin-cc.md, .planning/research/study-cards/graphify.md, .planning/research/study-cards/moneyprinterturbo.md, .planning/research/study-cards/voicebox.md, scripts/gen-study-card-stubs.mjs</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Common Pitfalls" Pitfall 5 (the ten already-installed tools that must get retroactive cards, no carve-out), the seed-data table (Owner/Trigger/Target Phase/Notes per already-installed tool)
    - .planning/research/INTEGRATION-TRACKER.md — the `Study Card` column values (the exact slug filenames to generate) and the Status/Notes for each row
    - .planning/research/study-cards/supabase.md — reuse the SAME study-card template established in Plan 02-04 (identical headed fields)
  </read_first>
  <action>
    Author retroactive FULL study cards for the already-installed tools called out in Pitfall 5: `superpowers`, `gsd` (gsd-core suite), `gstack`, `ruflo`, `claude-mem`, `caveman`, `headroom`, `codex-plugin-cc`, `graphify` (INSTALL/active), `moneyprinterturbo`, `voicebox`. Use the same template as Plan 02-04's cards; mark STUDY as backfilled in the checklist and set the Status field to match the tracker row (STUDY or INSTALL). Note in each that the tool was installed before this tracking program existed and the card is a retroactive backfill.
    Write `scripts/gen-study-card-stubs.mjs`: a standalone Node ESM script (no npm deps — reads/writes with `node:fs`). It parses `.planning/research/INTEGRATION-TRACKER.md`, and for every non-EXCLUDED row whose `Study Card` path does NOT yet exist, writes a STUB card from the shared template (headed fields present, values marked `TBD — filled at Phase <N> study step`, STUDY unchecked). It MUST skip any study-card file that already exists (never overwrite the full Plan 02-04 cards or the retroactive cards above).
    Run `node scripts/gen-study-card-stubs.mjs` to fill all remaining rows. After it runs, every non-EXCLUDED tracker row must have a matching study-card file.
  </action>
  <verify>
    <automated>node scripts/gen-study-card-stubs.mjs && node -e "const fs=require('fs');const retro=['superpowers','gsd','gstack','ruflo','claude-mem','caveman','headroom','codex-plugin-cc','graphify','moneyprinterturbo','voicebox'];for(const r of retro){if(!fs.existsSync('.planning/research/study-cards/'+r+'.md')){console.error('missing retroactive card: '+r);process.exit(1)}}console.log('retroactive + stubs OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `for c in superpowers gsd gstack ruflo claude-mem caveman headroom codex-plugin-cc graphify moneyprinterturbo voicebox; do test -f .planning/research/study-cards/$c.md || exit 1; done` exits 0 (all retroactive cards present)
    - `test -f scripts/gen-study-card-stubs.mjs && node --check scripts/gen-study-card-stubs.mjs` exits 0 (script parses)
    - `node scripts/gen-study-card-stubs.mjs` exits 0 and is idempotent (a second run makes no changes: `node scripts/gen-study-card-stubs.mjs && git diff --quiet .planning/research/study-cards/` exits 0)
    - `grep -lq 'retroactive\|backfill' .planning/research/study-cards/superpowers.md` exits 0 (Pitfall 5 discipline recorded)
  </acceptance_criteria>
  <done>Every already-installed tool has a retroactive card, the stub generator idempotently fills all remaining non-EXCLUDED rows, and no full card was overwritten.</done>
</task>

<task type="auto">
  <name>Task 2: Tracker-integrity validator (INTEG-01 + INTEG-02 machine check)</name>
  <files>scripts/check-integration-tracker.mjs</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Validation Architecture" Wave 0 Gaps (script spec), "Integration Tracking Program" (the machine-checkable rule for INTEG-02: an EXCLUDED row may only change status if a dated CEO-sign-off entry exists in `## Re-admission Log`)
    - .planning/research/INTEGRATION-TRACKER.md — the table shape the parser must read (column order, Status legend, Excluded Items table, Re-admission Log)
  </read_first>
  <action>
    Write `scripts/check-integration-tracker.mjs`: a standalone Node ESM script (no npm deps) that reads `.planning/research/INTEGRATION-TRACKER.md` and the `study-cards/` directory. For the row-count, Status-enum, and card-coverage checks (rules 1–3) parse the MAIN tracking table only — everything above the `## Excluded Items` heading — splitting each table row on the pipe and reading the Status from its fixed column index (the field after Item and Category); separately read the `## Excluded Items` register and the `## Re-admission Log` section for the completeness and re-admission checks (rules 4–5). Assert:
      1. Row floor: the main table has at least 49 NON-EXCLUDED data rows (Status in STUDY/INSTALL/ADOPT/EMBED) AND at least 4 EXCLUDED rows — i.e. ≥53 data rows total (skip the header and `---` separator rows by filtering rows whose first cell is empty).
      2. Status enum: every main-table data row's Status cell is exactly one of STUDY, INSTALL, ADOPT, EMBED, EXCLUDED (no parentheses / qualifiers) — fail listing any offenders.
      3. Study-card coverage: for every non-EXCLUDED row, the file named in its `Study Card` column exists under `.planning/research/`; EXCLUDED rows (Study Card dash) are EXEMPT; fail listing any missing card.
      4. Excluded completeness: the four excluded items (kickbacks.ai, automaton, llm-council, ToS-gray) are present in the `## Excluded Items` register.
      5. Re-admission rule (INTEG-02): if any item that appears in the Excluded Items register has a main-table Status other than EXCLUDED, there MUST be a matching dated entry in `## Re-admission Log` naming CEO approval; otherwise fail.
    On success print a one-line summary (`tracker OK: <N> data rows (<K> non-excluded, <E> excluded), <M> study cards`) and exit 0; on any failure print the specific offending rows/files and exit 1. Document the direct invocation `node scripts/check-integration-tracker.mjs` in the script's header comment; do NOT add or modify any `package.json` in this task.
    Run the validator; it must exit 0 against the tracker + full/stub cards produced by Plan 02-04 and Task 1.
  </action>
  <verify>
    <automated>node --check scripts/check-integration-tracker.mjs && node scripts/check-integration-tracker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-integration-tracker.mjs && node --check scripts/check-integration-tracker.mjs` exits 0
    - `node scripts/check-integration-tracker.mjs` exits 0 and prints a `tracker OK` summary line reporting ≥ 49 non-excluded rows and ≥ 4 excluded rows
    - Negative control — a REAL EXCLUDED row flip is caught deterministically (targets the kickbacks.ai row's Status cell by item name, not a first-occurrence-in-file match): `cp .planning/research/INTEGRATION-TRACKER.md /tmp/trk.bak; sed -i '/kickbacks\.ai/ s/|[[:space:]]*EXCLUDED[[:space:]]*|/| ADOPT |/' .planning/research/INTEGRATION-TRACKER.md; node scripts/check-integration-tracker.mjs; flip_rc=$?; cp /tmp/trk.bak .planning/research/INTEGRATION-TRACKER.md; node scripts/check-integration-tracker.mjs && [ $flip_rc -ne 0 ]` exits 0 (validator FAILS on the un-logged kickbacks.ai EXCLUDED→ADOPT flip, then passes again after the original is restored; a no-op sed would leave flip_rc=0 and fail this test, so the control is non-vacuous)
    - `node scripts/check-integration-tracker.mjs` exits 0 again after restore (integrity holds)
  </acceptance_criteria>
  <done>The validator enforces the ≥49-non-excluded + ≥4-EXCLUDED row floor, status enum (column-aware), study-card coverage (EXCLUDED exempt), excluded completeness, and the re-admission rule; it passes on the real tracker and provably fails on the un-logged kickbacks.ai EXCLUDED→ADOPT flip. No package.json is added or modified.</done>
</task>

</tasks>

<artifacts_produced>
- Retroactive study cards: `superpowers.md`, `gsd.md`, `gstack.md`, `ruflo.md`, `claude-mem.md`, `caveman.md`, `headroom.md`, `codex-plugin-cc.md`, `graphify.md`, `moneyprinterturbo.md`, `voicebox.md`
- Stub study cards for all remaining non-EXCLUDED tracker rows (generated)
- `scripts/gen-study-card-stubs.mjs` — idempotent stub generator (CLI: `node scripts/gen-study-card-stubs.mjs`)
- `scripts/check-integration-tracker.mjs` — the INTEG-01/INTEG-02 compliance check (CLI: `node scripts/check-integration-tracker.mjs`; no package.json created or modified)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracker rows → study-card files | Coverage boundary: a row without a card is an unstudied/blind-install risk |
| EXCLUDED decision → future status changes | Process-integrity boundary: silent re-admission bypasses the CEO gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Tampering / Elevation of Privilege | EXCLUDED-row re-admission | medium | mitigate | `check-integration-tracker.mjs` fails any EXCLUDED→other status change lacking a dated CEO Re-admission Log entry; enforceable because 02-04 seeds 4 real EXCLUDED main-table rows, and proven by the deterministic kickbacks.ai EXCLUDED→ADOPT negative-control acceptance test |
| T-2-06 | Tampering | study-card coverage gaps | medium | mitigate | Validator asserts a study card exists for every non-EXCLUDED row; stub generator guarantees coverage including retroactive already-installed tools (Pitfall 5) |
</threat_model>

<verification>
- Every non-EXCLUDED row has a study card; generator is idempotent (Task 1).
- Validator passes on the real tracker and fails on an un-logged re-admission (Task 2 negative control).
</verification>

<success_criteria>
The tracker is complete (a study card per non-EXCLUDED row, retroactive cards for already-installed tools) and INTEG-01/INTEG-02 are machine-enforced by a validator that provably catches an un-logged EXCLUDED re-admission. Covers INTEG-01 (coverage) and INTEG-02 (re-admission enforcement).
</success_criteria>

<output>
Create `.planning/phases/02-foundation-integration-program/02-05-SUMMARY.md` when done.
</output>
