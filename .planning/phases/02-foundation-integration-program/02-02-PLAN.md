---
phase: 02-foundation-integration-program
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md
autonomous: false
requirements: [INTEG-01]
must_haves:
  truths:
    - "A human has verified that vitest and @types/node are the genuine, established packages (not slopsquats) before any install runs"
    - "The approval decision is recorded on disk so Plan 02-03's install is auditable against a real sign-off"
  artifacts:
    - ".planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md (recorded verification outcome)"
  key_links:
    - "SUS verdicts in 02-RESEARCH Package Legitimacy Audit <-> the two packages the checkpoint gates (vitest, @types/node)"
    - "this approval record <-> Plan 02-03 pnpm install (install must not run until this is approved)"
---

<objective>
Human supply-chain gate: before the workspace's dev dependencies are installed, a human verifies that the two packages flagged SUS by the Package Legitimacy Audit — `vitest` and `@types/node` — are the legitimate, high-traffic, established packages (the "too-new" flag was assessed as a heuristic false positive), not slopsquatted look-alikes.

Purpose: The binding Package Legitimacy Gate requires a `checkpoint:human-verify` before installing any [SUS]-flagged package; these checkpoints are never auto-approvable. This isolates the supply-chain trust decision (threat T-2-SC / T-2-01) as its own auditable step ahead of the Wave 3 install.
Output: A recorded approval (or rejection) in `02-02-legitimacy-approval.md` that unblocks (or blocks) Plan 02-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-foundation-integration-program/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human legitimacy verification for vitest and @types/node</name>
  <files>.planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md</files>
  <action>
    Pause for a human supply-chain check. Present the two SUS-flagged packages (`vitest`, `@types/node`) and the verification steps below, wait for the human decision, then record the outcome (`approved`/`rejected` + evidence) in `.planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md`. Do NOT proceed to Plan 02-03's install until the recorded decision is `approved`.
  </action>
  <what-built>
    Plan 02-01 declared three dev dependencies via the pnpm catalog: `typescript` (Approved), `vitest` (SUS — "too-new" flag), and `@types/node` (SUS — "too-new" flag). No install has run yet. Per the Package Legitimacy Audit in 02-RESEARCH, the two SUS packages must be human-verified before `pnpm install` executes in Plan 02-03.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest — confirm: publisher is the `vitest-dev` team, weekly downloads are in the tens of millions, repository is `github.com/vitest-dev/vitest`, and the package has years of release history (the recent publish date is a routine patch, not a brand-new package).
    2. Open https://www.npmjs.com/package/@types/node — confirm: it is the DefinitelyTyped `@types/node` package (`github.com/DefinitelyTyped/DefinitelyTyped`), ~300M+ weekly downloads, long history.
    3. Cross-check both names character-for-character against the catalog block in `pnpm-workspace.yaml` — confirm no typosquat substitution (e.g. `vit3st`, `@types/nodejs`).
    4. Record the outcome in `.planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md`: the two package names, the verified download counts / repo URLs, the reviewer, the date, and the decision (`approved` or `rejected`).
  </how-to-verify>
  <resume-signal>Type "approved" (proceed to Plan 02-03 install) or "rejected: &lt;reason&gt;" (halt — do not install; escalate the flagged package)</resume-signal>
</task>

</tasks>

<artifacts_produced>
- `.planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md` — the recorded supply-chain verification (package names, download/repo evidence, reviewer, date, decision). Referenced by Plan 02-03 as the precondition for install.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | The moment a package name is trusted enough to run its install/postinstall scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | vitest / @types/node (SUS-flagged installs) | high | mitigate | This blocking-human checkpoint — a person verifies both packages on npmjs.com against publisher, downloads, repo, and exact spelling before any install; decision recorded on disk |
| T-2-SC | Tampering | npm/pnpm install supply chain | high | mitigate | Legitimacy gate is non-auto-approvable (`workflow.auto_advance` ignored); rejection halts the Wave 3 install |
</threat_model>

<verification>
- The checkpoint blocks until a human records an explicit `approved` or `rejected` decision in `02-02-legitimacy-approval.md`.
- On `approved`, Plan 02-03 (Wave 3) install may run. On `rejected`, the phase halts pending package escalation.
</verification>

<success_criteria>
A human has verified vitest and @types/node against npmjs.com and recorded the decision on disk. No install has run without this sign-off.
</success_criteria>

<output>
Create `.planning/phases/02-foundation-integration-program/02-02-SUMMARY.md` when done (record the decision and evidence links).
</output>
