---
phase: 02-foundation-integration-program
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - pnpm-lock.yaml
autonomous: true
requirements: [INTEG-01]
must_haves:
  truths:
    - "`pnpm install` resolves the catalog dev dependencies and produces a committed lockfile"
    - "`pnpm build` (tsc --build) compiles all nine workspace projects in dependency order with zero errors"
    - "`pnpm test` runs the vitest smoke suite green"
    - "No Phase-3+ runtime package is present in the installed tree"
  artifacts:
    - "pnpm-lock.yaml (committed dependency lockfile)"
    - "dist/ build outputs across all nine projects (git-ignored, proof-of-build only)"
  key_links:
    - "02-02 legitimacy approval <-> this install (install only runs after approved sign-off)"
    - "catalog versions <-> resolved lockfile (install proves the catalog protocol resolves)"
    - "project references graph <-> tsc --build success (proves the scaffold is dependency-honest)"
---

<objective>
Activate pnpm via Corepack, install the three catalog dev dependencies, and prove the whole scaffold installs and builds clean — satisfying the "installs and builds clean" half of Phase 2 success criterion 1. Runs only after the Plan 02-02 legitimacy checkpoint is approved.

Purpose: A monorepo skeleton is only "build-ready" if `pnpm install && pnpm build && pnpm test` all exit 0. This plan is the machine proof.
Output: Committed `pnpm-lock.yaml` + a green build/test run recorded in the summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-foundation-integration-program/02-RESEARCH.md
@.planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate pnpm and install catalog dev dependencies</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Installation" (Corepack activation commands), "Common Pitfalls" Pitfall 3 (native-module OOM risk on this low-RAM machine + NODE_OPTIONS fallback), "Environment Availability" (Node 22.23.1, Corepack 0.34.6 present; pnpm absent by design)
    - .planning/phases/02-foundation-integration-program/02-02-legitimacy-approval.md — confirm the decision is `approved` before running any install
  </read_first>
  <action>
    Confirm `02-02-legitimacy-approval.md` records `approved`; if it records `rejected` or is missing, STOP and report — do not install.
    Activate pnpm through Corepack (do NOT `npm install -g pnpm`): run `corepack enable` then `corepack prepare pnpm@11.10.0 --activate`, then confirm `pnpm --version`.
    Run `pnpm install` at the repo root. This resolves the catalog dev dependencies (`typescript`, `@types/node`, `vitest`) and the `workspace:*` links, writing `pnpm-lock.yaml`.
    If `pnpm install` fails with a JavaScript-heap-out-of-memory error (this ThinkPad measured 259MB free RAM in research), retry once with `NODE_OPTIONS=--max_old_space_size=4096 pnpm install` per Pitfall 3 — do NOT add native-addon packages to work around it.
    Commit `pnpm-lock.yaml` (it is intentionally NOT git-ignored).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && test -f pnpm-lock.yaml && echo "install OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --version` prints `11.10.0` (Corepack-pinned)
    - `pnpm install` exits 0
    - `test -f pnpm-lock.yaml` exits 0
    - `! grep -Eq '(claude-agent-sdk|pg-boss|modelcontextprotocol/sdk|supabase|next)' pnpm-lock.yaml` exits 0 (no forbidden Phase-3+ package resolved into the tree)
  </acceptance_criteria>
  <done>pnpm is Corepack-activated at 11.10.0, `pnpm install` exits 0, lockfile committed, and no Phase-3+ runtime package resolved into the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Build and smoke-test the whole workspace</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Architecture Patterns" Pattern 1 (tsc --build project references), "Validation Architecture" (sampling: `pnpm build` per task, `pnpm -r test` per wave)
  </read_first>
  <action>
    Run `pnpm build` (root script → `tsc --build`) — it must compile all nine composite projects in dependency order (`shared` first) with zero errors, emitting `dist/` in each.
    Run `pnpm test` (root script → `vitest run`) — the `tests/smoke.test.ts` suite must pass.
    Do not add or modify source beyond fixing any scaffold error surfaced by the compiler (e.g. a missing `references` entry). If a real logic change would be needed, STOP and report — Phase 2 ships no business logic.
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 (tsc --build compiles all nine projects)
    - `test -f packages/shared/dist/index.js && test -f apps/dashboard/dist/index.js` exits 0 (build output emitted)
    - `pnpm test` exits 0 (vitest smoke suite green)
    - `pnpm -r exec node -e "process.exit(0)"` exits 0 (every workspace package is resolvable)
  </acceptance_criteria>
  <done>`pnpm build` and `pnpm test` both exit 0; dist output exists for representative projects; the skeleton installs and builds clean (success criterion 1 met).</done>
</task>

</tasks>

<artifacts_produced>
- `pnpm-lock.yaml` — committed lockfile pinning the resolved dev dependency tree
- Green `pnpm build` (tsc --build across 9 projects) and `pnpm test` (vitest) runs — the machine proof of "installs and builds clean"
- `dist/` outputs per project (git-ignored; existence proves compilation)
- CLI commands now working repo-wide: `pnpm build`, `pnpm test`, `pnpm -r <cmd>`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → installed node_modules | Install/postinstall scripts of resolved packages run on the local machine |
| approval record → install action | The 02-02 sign-off authorizes this install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | pnpm install (resolved deps) | high | mitigate | Install gated by the Plan 02-02 blocking-human legitimacy checkpoint; lockfile committed for reproducibility and future diff review |
| T-2-SC | Tampering | supply chain / Corepack pnpm | high | mitigate | pnpm pinned via `packageManager` + Corepack (no floating global install); lockfile-frozen resolution |
| T-2-03 | Tampering | resolved dependency tree | medium | mitigate | Post-install grep asserts no Phase-3+ runtime package resolved into pnpm-lock.yaml |
</threat_model>

<verification>
- `pnpm install` exits 0 and commits a lockfile (Task 1).
- `pnpm build` + `pnpm test` exit 0 (Task 2).
- No forbidden Phase-3+ package appears in the resolved tree.
</verification>

<success_criteria>
The pnpm monorepo skeleton installs and builds clean: `pnpm install`, `pnpm build`, and `pnpm test` all exit 0, with no Phase-3+ runtime dependency present. This closes success criterion 1.
</success_criteria>

<output>
Create `.planning/phases/02-foundation-integration-program/02-03-SUMMARY.md` when done (paste the decisive `pnpm build` / `pnpm test` output lines as evidence).
</output>
