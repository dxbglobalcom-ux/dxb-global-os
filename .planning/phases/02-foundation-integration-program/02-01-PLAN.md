---
phase: 02-foundation-integration-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - tsconfig.json
  - package.json
  - vitest.config.ts
  - .gitignore
  - db/migrations/.gitkeep
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/dxb-mcp/package.json
  - packages/dxb-mcp/tsconfig.json
  - packages/dxb-mcp/src/index.ts
  - packages/gateway/package.json
  - packages/gateway/tsconfig.json
  - packages/gateway/src/index.ts
  - packages/kernel/package.json
  - packages/kernel/tsconfig.json
  - packages/kernel/src/index.ts
  - packages/orchestrator/package.json
  - packages/orchestrator/tsconfig.json
  - packages/orchestrator/src/index.ts
  - packages/memory-router/package.json
  - packages/memory-router/tsconfig.json
  - packages/memory-router/src/index.ts
  - packages/outbox-executor/package.json
  - packages/outbox-executor/tsconfig.json
  - packages/outbox-executor/src/index.ts
  - apps/dashboard/package.json
  - apps/dashboard/tsconfig.json
  - apps/dashboard/src/index.ts
  - apps/jarvis/package.json
  - apps/jarvis/tsconfig.json
  - apps/jarvis/src/index.ts
  - tests/smoke.test.ts
autonomous: true
requirements: [INTEG-01]
must_haves:
  truths:
    - "The repo root contains a pnpm workspace whose folder tree matches the approved architecture exactly"
    - "Every package.json and tsconfig.json parses as valid JSON and declares only catalog / workspace references — no Phase-3+ runtime dependency"
    - "The root tsconfig.json references all nine workspace projects so tsc --build has a complete dependency graph"
  artifacts:
    - "pnpm-workspace.yaml (packages globs + catalog: version block)"
    - "tsconfig.base.json (shared compiler options)"
    - "tsconfig.json (root solution file, references all 9 projects)"
    - "package.json (root: private, packageManager pin, build/test scripts, catalog devDeps)"
    - "vitest.config.ts (root smoke-test config)"
    - "packages/{shared,dxb-mcp,gateway,kernel,orchestrator,memory-router,outbox-executor}/ (package.json + tsconfig.json + src/index.ts each)"
    - "apps/{dashboard,jarvis}/ (package.json + tsconfig.json + src/index.ts each)"
    - "db/migrations/.gitkeep"
    - "tests/smoke.test.ts"
  key_links:
    - "catalog: block in pnpm-workspace.yaml <-> 'catalog:' refs in every package.json (mismatch breaks pnpm install)"
    - "root tsconfig.json references[] <-> each package composite tsconfig (mismatch breaks tsc --build)"
    - "'@dxb/shared': 'workspace:*' in consumer package.json <-> packages/shared existence (mismatch breaks resolution)"
---

<objective>
Scaffold the pnpm workspace root and all nine empty-but-buildable TypeScript projects that mirror the approved DXB Global OS architecture (`db/`, `packages/{shared,dxb-mcp,gateway,kernel,orchestrator,memory-router,outbox-executor}`, `apps/{dashboard,jarvis}`). No business logic, no Phase-3+ runtime dependency — only the scaffold that Wave 3 will install and build.

Purpose: Success criterion 1 requires a monorepo skeleton that mirrors the architecture and installs/builds clean. This plan produces the files; Plan 02-03 proves install + build after the legitimacy gate.
Output: Workspace root config + 9 package/app scaffolds + db/ folder + smoke test. No `pnpm install` runs in this plan (dependencies are declared, not installed — that is gated by Plan 02-02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-integration-program/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace root configuration</name>
  <files>pnpm-workspace.yaml, tsconfig.base.json, package.json, vitest.config.ts, .gitignore</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Code Examples" (exact pnpm-workspace.yaml catalog block + root package.json), "Standard Stack" (pinned versions: typescript 6.0.3, @types/node ^22.0.0, vitest ^3.0.0), "Don't Hand-Roll" (Corepack + catalog rationale)
    - ./.claude/CLAUDE.md — locked stack table (pnpm workspaces, Node 22 LTS) and "What NOT to Use" (no Turbo/Nx)
    - ./.gitignore — existing Phase 1 vault rules (append only; do NOT remove existing lines)
  </read_first>
  <action>
    Create `pnpm-workspace.yaml` with `packages:` globs (`packages/*`, `apps/*`) and a `catalog:` block pinning `typescript: 6.0.3`, `"@types/node": ^22.0.0`, `vitest: ^3.0.0` — copy the exact block from 02-RESEARCH "Code Examples".
    Create root `package.json`: `name: dxb-global-os`, `private: true`, `packageManager: "pnpm@11.10.0"` (Corepack pin), `type: module`, `scripts.build: "tsc --build"`, `scripts.test: "vitest run"`, and `devDependencies` referencing `typescript`, `@types/node`, `vitest` each as `"catalog:"`. Declare NO other dependencies.
    Create `vitest.config.ts` (root) with a minimal defineConfig enabling the `tests/` include glob.
    Append to the existing `.gitignore` (use Edit, do not overwrite): `dist/`, `*.tsbuildinfo`, `.pnpm-store/`. Leave all Phase 1 vault lines intact. Do NOT ignore `pnpm-lock.yaml` (it must be committed).
    Do NOT run `pnpm install` here and do NOT add any Phase-3+ runtime package (the packages enumerated as "do NOT install yet" in 02-RESEARCH "Standard Stack — Supporting"); those get study cards only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('pnpm-workspace.yaml','utf8');if(!y.includes('catalog:')||!y.includes('packages/*'))process.exit(1);const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.packageManager!=='pnpm@11.10.0'||p.scripts.build!=='tsc --build')process.exit(1);console.log('root-config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pnpm-workspace.yaml && test -f tsconfig.base.json && test -f package.json && test -f vitest.config.ts` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON)
    - `grep -q 'packageManager' package.json && grep -q 'pnpm@11.10.0' package.json` exits 0
    - `grep -Eq '"typescript":\s*"catalog:"' package.json` exits 0 (catalog protocol used, not a literal version)
    - `! grep -Eq '"(@anthropic-ai/claude-agent-sdk|pg-boss|@modelcontextprotocol/sdk|@supabase/supabase-js|next)"' package.json` exits 0 (no forbidden Phase-3+ dep)
    - `grep -q 'dist/' .gitignore && grep -q '.env' .gitignore` exits 0 (new ignore added, Phase 1 vault line preserved)
  </acceptance_criteria>
  <done>Root workspace config exists, valid, uses the catalog protocol, pins pnpm via Corepack, and declares zero forbidden runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Nine package/app scaffolds + root solution tsconfig + db folder</name>
  <files>tsconfig.base.json, tsconfig.json, db/migrations/.gitkeep, packages/*/package.json, packages/*/tsconfig.json, packages/*/src/index.ts, apps/*/package.json, apps/*/tsconfig.json, apps/*/src/index.ts, tests/smoke.test.ts</files>
  <read_first>
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Architecture Patterns" Pattern 1 (per-package composite tsconfig + references), "Recommended Project Structure" (exact folder tree), "Code Examples" (minimal package scaffold with workspace:* dep)
    - .planning/phases/02-foundation-integration-program/02-RESEARCH.md — "Architectural Responsibility Map" (which package owns which future capability — set each package description accordingly)
  </read_first>
  <action>
    Create `tsconfig.base.json` with shared strict compiler options: `strict: true`, `target: ES2022`, `module: NodeNext`, `moduleResolution: NodeNext`, `esModuleInterop: true`, `declaration: true`, `declarationMap: true`, `skipLibCheck: true`. No `paths` aliases (project references only, per 02-RESEARCH Anti-Patterns).
    Create the exact folder set: `packages/{shared,dxb-mcp,gateway,kernel,orchestrator,memory-router,outbox-executor}` and `apps/{dashboard,jarvis}` and `db/migrations/` with a `.gitkeep` in `db/migrations/`.
    For EACH package/app create three files:
      1. `package.json`: name `@dxb/<folder>` (e.g. `@dxb/shared`, `@dxb/dxb-mcp`), `version: 0.0.0`, `private: true`, `type: module`, `main: dist/index.js`, `types: dist/index.d.ts`, `devDependencies: { "typescript": "catalog:" }`. Every package EXCEPT `shared` also adds `dependencies: { "@dxb/shared": "workspace:*" }`. Declare NO Phase-3+ runtime package.
      2. `tsconfig.json`: extends `../../tsconfig.base.json`, `compilerOptions: { composite: true, outDir: dist, rootDir: src }`, `include: [src]`. Every project EXCEPT `shared` adds `references: [{ path: "../shared" }]` (apps use `../../packages/shared`).
      3. `src/index.ts`: `shared` exports `export const PACKAGE = '@dxb/shared' as const;`. Every other project imports and re-exports through shared to prove the workspace link, e.g. `import { PACKAGE } from '@dxb/shared'; export const OWNER = '<folder>' as const; export { PACKAGE };`.
    Create root `tsconfig.json` (solution file): `files: []`, `references: [...]` listing all nine projects (`packages/*` and `apps/*`) by path.
    Create `tests/smoke.test.ts`: one vitest test asserting a trivial truth (e.g. `expect(1 + 1).toBe(2)`) so the runner has at least one test — workspace wiring itself is proven by tsc --build in Plan 02-03, keep this test dependency-free.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dirs=['packages/shared','packages/dxb-mcp','packages/gateway','packages/kernel','packages/orchestrator','packages/memory-router','packages/outbox-executor','apps/dashboard','apps/jarvis'];for(const d of dirs){for(const f of ['package.json','tsconfig.json','src/index.ts']){if(!fs.existsSync(d+'/'+f)){console.error('MISSING '+d+'/'+f);process.exit(1)}}JSON.parse(fs.readFileSync(d+'/package.json','utf8'))}const root=JSON.parse(fs.readFileSync('tsconfig.json','utf8'));if((root.references||[]).length!==9){console.error('root references != 9');process.exit(1)}console.log('scaffold OK: 9 projects, root references=9')"</automated>
  </verify>
  <acceptance_criteria>
    - `for d in shared dxb-mcp gateway kernel orchestrator memory-router outbox-executor; do test -f packages/$d/package.json && test -f packages/$d/tsconfig.json && test -f packages/$d/src/index.ts || exit 1; done` exits 0
    - `test -f apps/dashboard/src/index.ts && test -f apps/jarvis/src/index.ts && test -f db/migrations/.gitkeep` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('tsconfig.json','utf8'));process.exit((r.references||[]).length===9?0:1)"` exits 0 (root references all 9 projects)
    - `for d in dxb-mcp gateway kernel orchestrator memory-router outbox-executor; do grep -q 'workspace:\*' packages/$d/package.json || exit 1; done` exits 0 (each backend package depends on @dxb/shared)
    - `! grep -REq '"(@anthropic-ai/claude-agent-sdk|pg-boss|@modelcontextprotocol/sdk|@supabase/supabase-js|next)"' packages apps` exits 0 (no blind Phase-3+ install anywhere)
  </acceptance_criteria>
  <done>All nine projects scaffolded with valid package.json + composite tsconfig + index stub; root solution tsconfig references exactly nine projects; db/ folder present; zero forbidden dependencies anywhere.</done>
</task>

</tasks>

<artifacts_produced>
Files this plan creates (consumed by later plans/phases):
- `pnpm-workspace.yaml`, `tsconfig.base.json`, `tsconfig.json`, `package.json`, `vitest.config.ts` (workspace root)
- Package names: `@dxb/shared`, `@dxb/dxb-mcp`, `@dxb/gateway`, `@dxb/kernel`, `@dxb/orchestrator`, `@dxb/memory-router`, `@dxb/outbox-executor`, `@dxb/dashboard`, `@dxb/jarvis`
- Folders: `packages/*/`, `apps/*/`, `db/migrations/`
- `tests/smoke.test.ts`
- Exported symbols: `PACKAGE` (from @dxb/shared), `OWNER` (from each other project)
- Root npm scripts: `pnpm build` (tsc --build), `pnpm test` (vitest run) — wired here, executed in Plan 02-03
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §8B / CLAUDE.md stack → package.json | Which dependencies are declared crosses from doc-mandated set into committed manifests |
| local edits → git commit | Scaffold files pass through the Phase 1 gitleaks pre-commit hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Tampering | packages/*/package.json (blind Phase-3 dep) | medium | mitigate | Forbidden-deps grep gate in acceptance criteria — no `@anthropic-ai/claude-agent-sdk`, `pg-boss`, `@modelcontextprotocol/sdk`, `@supabase/supabase-js`, `next` may appear; study→install discipline enforced by the tracker (Plans 02-04/05) |
| T-2-04 | Information Disclosure | scaffold commits | low | accept | Phase 1 gitleaks pre-commit hook + `.env` vault already active; this plan touches no secrets |
</threat_model>

<verification>
- Workspace root parses and uses the catalog protocol (Task 1 automated).
- All nine projects scaffolded; root solution tsconfig references all nine (Task 2 automated).
- No forbidden Phase-3+ dependency declared anywhere (acceptance criteria greps).
</verification>

<success_criteria>
The folder tree matches the approved architecture, every manifest is valid JSON declaring only catalog/workspace references, and no Phase-3+ runtime package is present. `pnpm install`/`pnpm build` are intentionally deferred to Plan 02-03 (post legitimacy gate).
</success_criteria>

<output>
Create `.planning/phases/02-foundation-integration-program/02-01-SUMMARY.md` when done.
</output>
