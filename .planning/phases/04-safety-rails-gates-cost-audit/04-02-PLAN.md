---
phase: 04-safety-rails-gates-cost-audit
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - db/migrations/20260707000007_budget_state.sql
  - packages/dxb-mcp/src/groups/approval.ts
  - pnpm-workspace.yaml
  - tools/dxb-cli/package.json
  - tools/dxb-cli/tsconfig.json
  - tools/dxb-cli/src/index.ts
  - tools/dxb-cli/src/approve.ts
  - tests/phase4/approval-flow.test.ts
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "approval_submit_draft births a draft approvals row with the FULL payload frozen at draft time + audit; approval_list_pending lists pending rows"
    - "approve/reject exist ONLY as CLI (`dxb approve|reject`) — no MCP tool can move an approval to approved (agents cannot see a door that isn't there)"
    - "`dxb approve <id>` → pending→approved, decided_by recorded, outbox row auto-born (trigger proven through the CLI path)"
    - "Agent-role attempt to UPDATE approvals to approved raises the trigger/RLS exception (tool-path proof)"
  artifacts:
    - "0007 budget_state migration (master-plan verbatim)"
    - "approval group FULL (submit_draft, list_pending) replacing the stub"
    - "tools/dxb-cli workspace package with approve/reject/(breaker reset placeholder)"
  key_links:
    - "approval_submit_draft <-> approvals table draft status (0003 trigger chain)"
    - "dxb approve <-> trg_outbox_enqueue (outbox birth = the ONLY execution record)"
    - "tools/* glob <-> pnpm-workspace.yaml (new package resolvable)"
---

<objective>
Make GATE-01 real at the tool layer: agents can only DRAFT; decisions live in a human-only CLI; the outbox row is born by the Phase-3 trigger, never by agent code.
Output: 0007 migration, full approval group, dxb CLI, tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-04.md
@packages/dxb-mcp/src/groups/approval.ts   (current stub — draft input shape reused)
@packages/dxb-mcp/src/groups/queue.ts      (group pattern: ok(), transactions, audit append)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 0007 budget_state migration</name>
  <files>db/migrations/20260707000007_budget_state.sql</files>
  <action>
    Master-plan §3 SQL verbatim (single-row table via boolean PK CHECK; defaults 100.00 / 2.00; seeded row). Add `ALTER TABLE budget_state ENABLE ROW LEVEL SECURITY;` (project-wide RLS rule — consistent with all 13 tables; not in the skeleton but mandated by the LOCKED "tüm tablolarda RLS ENABLE" decision, record as conscious addition). `supabase db reset` green over all 7; reseed personas.
  </action>
  <verify><automated>pnpm exec supabase db reset && docker exec supabase_db_DxB_Global_OS psql -U postgres -d postgres -tAc "SELECT monthly_cap_eur, velocity_cap_eur_per_hour FROM budget_state" | grep -q '100.00|2.00'</automated></verify>
  <acceptance_criteria>
    - reset exit 0 (7 migrations); budget_state has exactly one row with LOCKED defaults; second INSERT DEFAULT VALUES fails (PK)
  </acceptance_criteria>
  <done>Breaker state table live.</done>
</task>

<task type="auto">
  <name>Task 2: approval group stub → FULL</name>
  <files>packages/dxb-mcp/src/groups/approval.ts, tests/phase4/approval-flow.test.ts</files>
  <action>
    Replace stub: `approval_submit_draft` {task_id, action_type, payload, risk_class} → INSERT approvals status 'draft' (payload frozen as-given after redact()-style pass? NO — payload must stay execution-faithful; secrets must never be in approval payloads by design, note in code) + audit append, returns row. `approval_list_pending` {risk_class?} → SELECT status='pending' ordered created_at. Also `approval_submit_for_decision` — WAIT: master-plan flow needs draft→pending before CEO decides; the state machine allows draft→pending. DECISION (recorded): submit_draft creates 'draft'; a second tool `approval_finalize_draft` moves draft→pending when the agent's work is complete (both agent-visible; neither can reach approved/rejected). CLI decides from pending.
    Tests: submit_draft → draft row + audit; finalize → pending; direct UPDATE to approved via db as simulated agent (non-CLI path) relies on trigger: pending→approved is ALLOWED by trigger for anyone with UPDATE — enforcement layer = agents have NO tool for it + RLS deny (service_role only via dxb-mcp, which exposes no approve tool). Test asserts: dxb-mcp tools/list contains NO tool matching /approve|reject/ beyond the draft/finalize/list names.
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase4/approval-flow.test.ts</automated></verify>
  <acceptance_criteria>
    - draft born with frozen payload + audit; finalize→pending; list_pending filters
    - tools/list proof: no approve/reject-capable MCP tool exists
  </acceptance_criteria>
  <done>Agents can draft and hand over — nothing more.</done>
</task>

<task type="auto">
  <name>Task 3: tools/dxb-cli — approve/reject</name>
  <files>pnpm-workspace.yaml, tools/dxb-cli/package.json, tools/dxb-cli/tsconfig.json, tools/dxb-cli/src/index.ts, tools/dxb-cli/src/approve.ts, tests/phase4/approval-flow.test.ts</files>
  <action>
    Add `tools/*` to workspace globs. New package `@dxb/cli` (bin: `dxb`): commands `approve <id>`, `reject <id> --note <text>` — UPDATE approvals pending→approved/rejected with decided_by='ceo:cli', decided_at now(), decision_note; audit append actor_type 'ceo'. Composite tsconfig referencing shared; root solution tsconfig gains the reference. No new deps (arg parsing by hand — 2 commands; zod for input floor).
    Extend approval-flow test: full chain submit_draft→finalize→CLI approve (invoke the command module directly) → approvals approved + outbox row exists with idempotency_key `action_type:id` (trigger proven through CLI path); reject path records note; approving a non-pending id → trigger exception surfaces as clean CLI error.
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase4/approval-flow.test.ts && pnpm test</automated></verify>
  <acceptance_criteria>
    - CLI approve → approved + outbox born; reject → rejected + note; non-pending → readable error
    - whole workspace build+test green (10th project compiles)
  </acceptance_criteria>
  <done>Decision path is human-only, audited, trigger-proven.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-04 | Elevation | agent reaching approved state | high | mitigate | No MCP tool exposes decisions; tools/list asserted in tests; trigger one-way machine (Phase-3 proven) |
| T-4-05 | Tampering | payload drift between draft and execution | high | mitigate | Payload frozen at draft INSERT; executor reads the same row transactionally (04-03 TOCTOU re-check) |
| T-4-06 | Repudiation | unattributed decisions | medium | mitigate | decided_by/decided_at/decision_note columns + ceo-actor audit rows |
</threat_model>

<verification>reset 7/7 green; approval-flow suite green; whole workspace green.</verification>
<success_criteria>GATE-01 tool-layer half done: draft-first enforced, decisions CLI-only, outbox birth trigger-proven. Master-plan steps 2-3 closed.</success_criteria>
<output>Create 04-02-SUMMARY.md with decisive evidence lines.</output>
