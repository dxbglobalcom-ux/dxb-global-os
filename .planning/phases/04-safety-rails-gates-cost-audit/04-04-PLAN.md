---
phase: 04-safety-rails-gates-cost-audit
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-03]
files_modified:
  - packages/shared/src/litellm.ts
  - packages/outbox-executor/src/scheduler.ts
  - packages/outbox-executor/src/breaker.ts
  - tools/dxb-cli/src/breaker.ts
  - tools/hooks/tag-subscription-call.ts
  - .claude/settings.json
  - tests/phase4/budget-stop.test.ts
  - tests/phase4/velocity.test.ts
autonomous: false
requirements: [COST-01, COST-02, COST-03, KERN-03]
must_haves:
  truths:
    - "Every API model call routes through LiteLLM with a per-department virtual key; a real call lands in litellm spend tables with department metadata"
    - "Hard-stop proven: a key at exhausted max_budget gets 429/blocked and the event is audited"
    - "Velocity breaker (pg-boss cron 5min): injected retry-storm trips breaker_tripped=true within one cycle, blocks non-critical keys via /key/update, audits, and CEO can `dxb breaker reset` (audited)"
    - "Subscription-mode calls are tagged: a real claude -p run produces a cost_ledger row mode='subscription' via the PostToolUse/session hook"
    - "pg-boss schedules run in ONE scheduler process: outbox tick 15s, reaper 60s, breaker 5min"
  artifacts:
    - "packages/shared/src/litellm.ts (base_url + virtual key wrapper)"
    - "scheduler.ts (pg-boss wiring) + breaker.ts (60-min EUR window logic)"
    - "tools/hooks/tag-subscription-call.ts + settings.json hook registration"
    - "tests/phase4/{budget-stop,velocity}.test.ts"
  key_links:
    - "breaker SUM window <-> cost_ledger + litellm spend tables (unified view; table names from 04-01 card, not hard-coded)"
    - "critical-exception list <-> kernel L1 (constant in breaker.ts v1; moves to routing_rules in Phase 5 — note in code)"
    - "pg-boss connection <-> direct 54322 session mode (NEVER pooler)"
---

<objective>
Make the cost rails real: LiteLLM virtual keys enforce budgets, the velocity breaker catches storms in minutes, subscription calls get tagged, and all system routines run under one pg-boss scheduler.
Output: enforced-budget proofs (hard-stop + velocity), tagged subscription row, scheduler live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-04.md
@.planning/research/study-cards/litellm.md   (verified slugs + spend table names from 04-01)
@.planning/research/study-cards/pg-boss.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: CEO credential placement — OpenRouter + master key</name>
  <files>(vps/litellm/.env — CEO-placed, A8-vaulted, never read by tools)</files>
  <action>
    CEO's ONLY manual step this phase (CEO-delegation rule): place real `OPENROUTER_API_KEY` and a strong `LITELLM_MASTER_KEY` into `vps/litellm/.env` per vps/litellm/README.md. Blocking checkpoint asks confirmation "keys placed". I then restart the container and verify /health/readiness 200 and one authenticated `/key/generate` round-trip WITHOUT ever reading the .env file (A8). If OpenRouter has no funded account, CEO says so → plan continues with test-key-only scope and marks live-call criteria ⚠ UNVERIFIED (requires funded key) — no guessing, no fake evidence.
  </action>
  <verify><automated>curl -s http://127.0.0.1:4000/health/readiness | grep -qi 'healthy\|ready'</automated></verify>
  <acceptance_criteria>
    - Checkpoint answered; container healthy with CEO env; key-auth round-trip works (or UNVERIFIED scope recorded)
  </acceptance_criteria>
  <done>Real credentials live only in the vaulted env; enforcement testable.</done>
</task>

<task type="auto">
  <name>Task 2: virtual keys + shared/litellm.ts + hard-stop proof</name>
  <files>packages/shared/src/litellm.ts, tests/phase4/budget-stop.test.ts</files>
  <action>
    Generate per-department virtual keys via `/key/generate` (master key from env `LITELLM_MASTER_KEY` at runtime — process env, not file read): engineering, marketing, research + a sacrificial `test-budget` key with max_budget 0.01. Store key VALUES nowhere in repo — test keys created at test-time and printed only as prefixes.
    litellm.ts: thin OpenAI-compatible chat wrapper `llmCall({department, model, messages})` reading `LITELLM_BASE_URL` (default http://127.0.0.1:4000) + `DXB_LITELLM_KEY_<DEPT>` env; after response, also `cost.record(mode='api', source='litellm')`? NO — LOCKED: Cost Monitor reads LiteLLM's own spend tables; litellm.ts records nothing (single source, note in code).
    budget-stop.test.ts (gated on funded key; else test.skip with UNVERIFIED note): (1) one glm-5.2 call with engineering key → spend row appears in the litellm schema table (name from card) with department metadata; (2) exhaust/exceed test-budget key (0.01) → subsequent call returns 429/budget-exceeded; write an audit_log entry from the test harness capturing the block (alert plumbing note: %70 webhook → Phase 8 dashboard, recorded as follow-up).
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase4/budget-stop.test.ts</automated></verify>
  <acceptance_criteria>
    - Spend row with department metadata proven by SELECT from litellm schema
    - 429/blocked on exhausted key observed; audit row written
    - No key value committed anywhere (gitleaks + grep gate)
  </acceptance_criteria>
  <done>COST-02 enforced in practice, not config-theory.</done>
</task>

<task type="auto">
  <name>Task 3: scheduler (pg-boss) + velocity breaker + dxb breaker reset</name>
  <files>packages/outbox-executor/src/scheduler.ts, packages/outbox-executor/src/breaker.ts, tools/dxb-cli/src/breaker.ts, tests/phase4/velocity.test.ts</files>
  <action>
    scheduler.ts: one pg-boss instance (direct 54322, session mode) scheduling: `outbox-tick` every 15s → tick(); `lease-reaper` every 60s → SELECT reap_expired_leases(); `velocity-breaker` every 5min → checkVelocity(). Exported `startScheduler()`; graceful stop.
    breaker.ts checkVelocity(): unified 60-min EUR sum = cost_ledger.cost_eur + litellm spend (table name imported from a config constant module fed by the 04-01 card — NOT hard-coded literal in logic) ; if sum > budget_state.velocity_cap_eur_per_hour AND NOT breaker_tripped → set breaker_tripped=true+timestamp, block non-critical department keys via /key/update {blocked:true} (critical exception constant: ['kernel-L1'] with Phase-5 routing_rules migration note), audit append actor 'system:breaker'.
    dxb breaker reset: clears breaker_tripped, unblocks keys, REQUIRES --confirm, audits actor 'ceo:cli'.
    velocity.test.ts: inject 100 fake cost_ledger rows (~5 EUR in last hour) → run checkVelocity() directly → breaker_tripped=true + audit row + (if live LiteLLM) key blocked verified via /key/info; then CLI reset → state cleared + audit. Also negative: under-cap sum does NOT trip.
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase4/velocity.test.ts && pnpm test</automated></verify>
  <acceptance_criteria>
    - Storm injection trips within one check; non-critical keys blocked; reset works and is audited; under-cap negative test green
    - Scheduler cadences: tick 15s / reaper 60s / breaker 5min (asserted from registered schedules)
  </acceptance_criteria>
  <done>COST-03: storms die in minutes, 7/24, with an audited human override.</done>
</task>

<task type="auto">
  <name>Task 4: subscription tagging hook</name>
  <files>tools/hooks/tag-subscription-call.ts, .claude/settings.json</files>
  <action>
    Hook script (Node, no new deps): reads Claude Code hook stdin JSON (PostToolUse/SessionEnd shape per current docs — verify shape from live `claude` docs at execution, no guessing), extracts model + usage tokens, department from env DXB_DEPARTMENT (default 'engineering'), task from DXB_TASK_ID (nullable) → INSERT cost_ledger mode='subscription', source='hook' via @dxb/shared getDb() (compiled dist import). Register in .claude/settings.json hooks block (merge, don't clobber existing hooks). Proof: run `claude -p 'say ok'` with DXB_DEPARTMENT=research → cost_ledger gains a mode='subscription' row.
    KERN-03 note: API-mode rows come from LiteLLM tables; subscription rows from this hook — both queryable per dept/model/mode (COST-04 view in Phase 8).
  </action>
  <verify><automated>docker exec supabase_db_DxB_Global_OS psql -U postgres -d postgres -tAc "SELECT count(*) FROM cost_ledger WHERE mode='subscription'" | grep -qv '^0$'</automated></verify>
  <acceptance_criteria>
    - Real claude -p run produces the tagged row (model, tokens>0, department='research')
    - settings.json valid JSON, existing hooks preserved
  </acceptance_criteria>
  <done>KERN-03 tagging complete across both modes.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-11 | Info disclosure | virtual/master keys leaking to repo/logs | high | mitigate | Keys env-only; tests print prefixes only; gitleaks pre-commit; A8 on .env |
| T-4-12 | DoS (financial) | retry storm burning budget | high | mitigate | Hourly-window breaker + hard-stop budgets; storm test proves trip < 1 cycle |
| T-4-13 | Tampering | breaker silently disabled | medium | mitigate | reset requires CLI --confirm and writes ceo:cli audit; state single-row table |
| T-4-14 | Repudiation | untagged subscription spend | medium | mitigate | Hook writes mode='subscription' rows; real-run proof required |
</threat_model>

<verification>budget-stop + velocity suites green (or explicitly UNVERIFIED-scoped on unfunded key); subscription row proven; whole workspace green.</verification>
<success_criteria>COST-01/02/03 + KERN-03 enforced and demonstrated; single scheduler owns all system routines. Master-plan steps 6-9 closed.</success_criteria>
<output>Create 04-04-SUMMARY.md with decisive evidence lines (two-tier: ✓ verified vs ⚠ unverified-if-unfunded).</output>
