---
phase: 04-safety-rails-gates-cost-audit
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - packages/outbox-executor/src/index.ts
  - packages/outbox-executor/src/actions/index.ts
  - packages/outbox-executor/src/actions/test-write-file.ts
  - packages/outbox-executor/package.json
  - db/migrations/20260707000008_revoke_truncate.sql
  - tests/phase4/double-fire.test.ts
  - tests/phase4/toctou.test.ts
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "outbox-executor is the LOCKED core loop verbatim (⛔ any change needs recorded evidence+rationale) — the ONLY side-effect process"
    - "e2e: draft→finalize→dxb approve→tick → file written exactly once; audit chain ≥3 entries"
    - "double-fire: two parallel ticks on one ready row → SKIP LOCKED gives single execution, attempts=1, single file write"
    - "TOCTOU: a ready outbox row whose approval is not 'approved' at execution time goes failed, handler never runs"
    - "Phase-3 backlog closed: TRUNCATE revoked from non-postgres roles on append-only tables (⛔ decision recorded)"
  artifacts:
    - "packages/outbox-executor/src/{index.ts,actions/} (LOCKED core + registry + test.write_file)"
    - "0008 revoke-truncate migration"
    - "tests/phase4/{double-fire,toctou}.test.ts"
  key_links:
    - "handlers registry <-> approvals.action_type (executor refuses unknown types loudly)"
    - "idempotency_key <-> handler input (handler-level dedup possible)"
    - "tick() transaction <-> approvals re-read (TOCTOU closed in ONE transaction)"
---

<objective>
Build the single side-effect process: LOCKED tick() loop over outbox with execution-time re-check, a harmless v1 handler, and the two tests that make GATE-02 a fact (double-fire, TOCTOU).
Output: executor package green e2e; 0008 hardening; whole suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-04.md   (LOCKED core loop — transcribe; note import line `@dxb/shared/db` adapts to the real export `getDb()` like Phase-3 precedent, evidence+comment)
@.planning/phases/03-state-layer-dxb-mcp-core/03-02-SUMMARY.md   (TRUNCATE note)
</context>

<tasks>

<task type="auto">
  <name>Task 1: executor core (LOCKED) + actions registry + test.write_file</name>
  <files>packages/outbox-executor/src/index.ts, packages/outbox-executor/src/actions/index.ts, packages/outbox-executor/src/actions/test-write-file.ts, packages/outbox-executor/package.json</files>
  <action>
    Transcribe the LOCKED tick() verbatim with only the mandated adaptations, each commented with rationale: (a) `import { db } from "@dxb/shared/db"` → `getDb()` (real export; single-client rule), (b) audit append per branch (the LOCKED comment says "audit.append her dalda" — implement as audit_log INSERT inside the same trx, actor 'system:outbox'), (c) `executed_at: new Date()` → `sql\`now()\`` (DB-side time, LOCKED architecture decision "hep now() DB-tarafı"). Everything else byte-faithful: SKIP LOCKED join read, approval_status re-check → failed, executing→executed/failed, attempts increment.
    actions/index.ts: `handlers: Record<string, (payload: unknown, idempotencyKey: string) => Promise<unknown>>`; unknown action_type throws (loud). v1: `test.write_file` — writes `{path, content}` payload to a path CONFINED under `tmp/outbox-proof/` (path traversal guard: resolve+prefix check; refuse absolute/.. paths), append-safe: if file exists with same idempotency key marker, skip (write-once proof).
    Executor package.json: deps @dxb/shared (workspace), kysely, pg-boss (04-01), zod; keep OWNER/PACKAGE exports for the scaffold contract. pg-boss wiring (schedules) lands in 04-04 — this plan exports tick() and a `runOnce()` CLI for tests.
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase4/toctou.test.ts</automated></verify>
  <acceptance_criteria>
    - tick() diff vs master-plan block: only the three recorded adaptations
    - unknown action_type → thrown error (test); path traversal payload → refused (test)
  </acceptance_criteria>
  <done>The only side-effect process exists, harmless handler only.</done>
</task>

<task type="auto">
  <name>Task 2: e2e + double-fire + TOCTOU tests</name>
  <files>tests/phase4/double-fire.test.ts, tests/phase4/toctou.test.ts</files>
  <action>
    toctou.test.ts: (1) full e2e — queue task, approval_submit_draft + finalize via MCP client, CLI-approve module, tick() → file exists under tmp/outbox-proof, outbox executed, audit trace for the task shows draft/decision/execution entries (≥3); (2) TOCTOU — craft outbox row 'ready' whose approval row is forced back to... approvals can't leave approved (one-way); instead INSERT an outbox row directly referencing a still-'pending' approval (bypassing the trigger path, simulating corruption/attack) → tick() marks failed with the exact re-check error, handler never ran (no file).
    double-fire.test.ts: one approved outbox row; `Promise.all([tick(), tick()])` on two separate connections → exactly one execution: file written once (content marker unique), outbox.attempts=1, status executed; second tick found no ready row (SKIP LOCKED proof). Run twice for flake check.
  </action>
  <verify><automated>pnpm vitest run tests/phase4/double-fire.test.ts tests/phase4/toctou.test.ts && pnpm test</automated></verify>
  <acceptance_criteria>
    - e2e chain green with ≥3-entry audit trace; TOCTOU row failed, no side effect
    - double-fire: single write, attempts=1; green twice
  </acceptance_criteria>
  <done>GATE-02's "one row, one effect, once" is machine-proven.</done>
</task>

<task type="auto">
  <name>Task 3: 0008 — revoke TRUNCATE on append-only tables (⛔ recorded)</name>
  <files>db/migrations/20260707000008_revoke_truncate.sql</files>
  <action>
    Fable ⛔ decision (carried from 03-02 security note): `REVOKE TRUNCATE ON task_events, audit_log, cost_ledger FROM PUBLIC, anon, authenticated, service_role;` — evidence chain must be unwipeable by any API-path role. postgres superuser retains it (operational escape, audited by being superuser-only). Rationale comment inline. Reset green over 8; `\dp` shows no D for those roles; personas reseeded.
  </action>
  <verify><automated>pnpm exec supabase db reset && node --experimental-strip-types db/seed/import-personas.ts | grep -q '153' && docker exec supabase_db_DxB_Global_OS psql -U postgres -d postgres -c "\dp task_events" | grep -v 'postgres=' | grep -qv 'D' && echo "truncate revoked"</automated></verify>
  <acceptance_criteria>
    - reset 8/8 green; anon/authenticated/service_role show no TRUNCATE on the three tables
  </acceptance_criteria>
  <done>Append-only now means undeletable AND untruncatable via API roles.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-07 | Elevation | double execution of an outward action | high | mitigate | SKIP LOCKED single-claim + attempts assertion + idempotency_key to handler; double-fire test |
| T-4-08 | TOCTOU | approval flips between read and effect | high | mitigate | Same-transaction re-check (LOCKED); corruption-path test proves failed-no-effect |
| T-4-09 | Tampering | handler path traversal (test.write_file) | medium | mitigate | Confined dir + resolve/prefix guard + refusal test |
| T-4-10 | Repudiation | evidence wipe via TRUNCATE | medium | mitigate | 0008 revoke; \dp evidence |
</threat_model>

<verification>e2e + double-fire + TOCTOU green (double-fire twice); reset 8/8; whole workspace green.</verification>
<success_criteria>GATE-02 proven end-to-end with a harmless action; evidence tables hardened. Master-plan steps 4-5 closed.</success_criteria>
<output>Create 04-03-SUMMARY.md with decisive evidence lines.</output>
