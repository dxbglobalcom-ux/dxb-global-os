---
phase: 04-safety-rails-gates-cost-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/study-cards/litellm.md
  - .planning/research/INTEGRATION-TRACKER.md
  - .planning/phases/04-safety-rails-gates-cost-audit/04-01-legitimacy-approval.md
  - pnpm-workspace.yaml
  - packages/outbox-executor/package.json
  - pnpm-lock.yaml
  - vps/litellm/compose.yaml
  - vps/litellm/config.yaml
autonomous: false
requirements: [INTEG-01, COST-02]
must_haves:
  truths:
    - "litellm FULL study card exists BEFORE the image is pulled; pg-boss card (02-04) governs its install"
    - "CEO approved in ONE blocking checkpoint: pg-boss 12.x npm evidence + LiteLLM image pin + live-verified OpenRouter model slugs (no-guessing)"
    - "pg-boss installed ONLY in packages/outbox-executor (single scheduler process decision); session-mode 54322 documented"
    - "LiteLLM container healthy against the SAME Supabase Postgres in schema 'litellm'; /health/readiness 200"
    - "Tracker INSTALL flipped for pg-boss + litellm; validator exit 0"
  artifacts:
    - "study-cards/litellm.md (FULL, upgraded from stub)"
    - "04-01-legitimacy-approval.md (CEO decision record)"
    - "vps/litellm/{compose.yaml,config.yaml} (master-plan skeleton, verified slugs)"
  key_links:
    - "OpenRouter live model list <-> config.yaml slugs (verified at install day, recorded in approval)"
    - "LITELLM_DATABASE_URL <-> Supabase Postgres ?schema=litellm (one DB, LOCKED)"
---

<objective>
Open Phase 4's toolset gate: LiteLLM full card, one CEO supply-chain checkpoint (pg-boss npm + LiteLLM image + verified slugs), pg-boss into outbox-executor, LiteLLM container healthy on the shared Postgres.
Output: cards + approval record + running LiteLLM + tracker truth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-04.md   (LOCKED config skeleton + decisions)
@.planning/research/study-cards/pg-boss.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: litellm FULL study card + live slug verification raw material</name>
  <files>.planning/research/study-cards/litellm.md</files>
  <read_first>
    - existing stub .planning/research/study-cards/litellm.md (02-05 generated) — upgrade in place
    - .planning/master-plan/PHASE-04.md §3 LiteLLM config skeleton
  </read_first>
  <action>
    Upgrade the stub to a FULL card: image `ghcr.io/berriai/litellm:main-stable` (1.91.x line per CLAUDE.md), Prisma-managed schema in Postgres (`?schema=litellm` on the shared Supabase DB — LOCKED single-DB decision), virtual keys via `/key/generate` (max_budget, budget_duration, metadata.department), `/key/update blocked:true` as the breaker's lever, spend tables NOT hard-coded (names verified at install, recorded in card), /health/readiness gate, master_key+provider keys env-only (A8 vault).
    Fetch the live OpenRouter public model list and verify the master-plan slugs (glm/kimi/deepseek/qwen/minimax families) — record EXACT verified slugs in the card; any master-plan slug that does not exist live is listed with its correct replacement (no-guessing rule).
  </action>
  <verify><automated>test -f .planning/research/study-cards/litellm.md && grep -q 'main-stable' .planning/research/study-cards/litellm.md && grep -qi 'verified.*20[0-9][0-9]' .planning/research/study-cards/litellm.md</automated></verify>
  <acceptance_criteria>
    - Card FULL (purpose, pinned image, key APIs, pitfalls incl. session/schema notes, legitimacy verdict, lifecycle checklist STUDY checked)
    - Verified slug table with fetch date; mismatches flagged with replacements
  </acceptance_criteria>
  <done>LiteLLM studied before pull; slugs live-verified.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: CEO supply-chain checkpoint — pg-boss + LiteLLM image + slugs</name>
  <files>.planning/phases/04-safety-rails-gates-cost-audit/04-01-legitimacy-approval.md</files>
  <read_first>
    - .planning/phases/03-state-layer-dxb-mcp-core/03-01-legitimacy-approval.md — record format
  </read_first>
  <action>
    Fetch live npm evidence for `pg-boss` (version 12.x current, created date, maintainer, weekly downloads, exact spelling) inline. Present to CEO in ONE blocking checkpoint: (1) pg-boss npm install, (2) LiteLLM container image `ghcr.io/berriai/litellm:main-stable` pull (GitHub Container Registry, BerriAI official), (3) the verified OpenRouter slug list from Task 1. Any build-script request beyond the approved set halts. Record decision. If rejected: STOP, Tasks 3-4 do not run.
  </action>
  <verify><automated>grep -q 'Decision.*approved' ".planning/phases/04-safety-rails-gates-cost-audit/04-01-legitimacy-approval.md"</automated></verify>
  <acceptance_criteria>
    - Record on disk with evidence for all three items; blocking (no install/pull before it)
  </acceptance_criteria>
  <done>Supply-chain trust auditable; install gate open.</done>
</task>

<task type="auto">
  <name>Task 3: pg-boss into outbox-executor</name>
  <files>pnpm-workspace.yaml, packages/outbox-executor/package.json, pnpm-lock.yaml, .planning/research/INTEGRATION-TRACKER.md</files>
  <action>
    Catalog: `pg-boss` at the card-pinned 12.x version. Dependency ONLY in packages/outbox-executor (single scheduler process — tick/reaper/breaker crons all live there; recorded decision). Update the pg-boss tracker row note (was "Phase 4 install") → INSTALL flipped now.
    `pnpm install`; any unexpected build-script request STOPS the plan. `pnpm build && pnpm test` stay green. Card checklist INSTALL checked.
  </action>
  <verify><automated>pnpm -r ls pg-boss | grep -q pg-boss && ! grep -rq '"pg-boss"' packages/shared/package.json packages/dxb-mcp/package.json && pnpm build && pnpm test && node scripts/check-integration-tracker.mjs</automated></verify>
  <acceptance_criteria>
    - pg-boss resolved at pin, present ONLY in outbox-executor
    - build+test green; validator exit 0
  </acceptance_criteria>
  <done>System-routines queue installed at its owner, nowhere else.</done>
</task>

<task type="auto">
  <name>Task 4: LiteLLM container up on shared Postgres (schema litellm)</name>
  <files>vps/litellm/compose.yaml, vps/litellm/config.yaml, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - .planning/master-plan/PHASE-04.md — config.yaml skeleton (copy, slugs from Task 1's verified table)
  </read_first>
  <action>
    Write compose.yaml (image pinned `main-stable`, network host-or-mapped port 4000, env_file ./.env, healthcheck on /health/readiness) + config.yaml (skeleton verbatim with verified slugs; `database_url: os.environ/LITELLM_DATABASE_URL`).
    Document required env in vps/litellm/README.md (A8: .env itself is CEO-placed later in 04-04; for THIS task generate a throwaway local LITELLM_MASTER_KEY + LITELLM_DATABASE_URL pointing at `postgresql://postgres:postgres@<db-host>:54322/postgres?schema=litellm` — local dev defaults, not secrets; container must reach host Postgres: use host networking or `host.docker.internal`/`172.17.0.1` — pick what works on this Linux Docker 28, record it).
    `docker compose up -d` → wait → `curl /health/readiness` 200. Verify Prisma created the `litellm` schema in the shared DB (`\dn litellm`). Record spend-table names discovered (`\dt litellm.*`) in the study card (no hard-coding rule). `free -m` after start recorded.
    Tracker: litellm row INSTALL; validator green.
  </action>
  <verify><automated>docker compose -f vps/litellm/compose.yaml ps | grep -qi 'healthy\|running' && curl -s http://127.0.0.1:4000/health/readiness | grep -qi 'healthy\|ready\|"status"' && node scripts/check-integration-tracker.mjs</automated></verify>
  <acceptance_criteria>
    - /health/readiness 200-family with healthy body
    - `litellm` schema exists in the SAME Supabase Postgres; spend table names recorded in card
    - RAM headroom recorded (>300MB available)
  </acceptance_criteria>
  <done>Cost-enforcement engine alive on the single DB; ready for keys in 04-04.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering | pg-boss npm / litellm image supply chain | high | mitigate | Blocking CEO checkpoint with live evidence; pinned versions; deny-default build scripts |
| T-4-02 | Info disclosure | LiteLLM master key / provider keys | high | mitigate | env-only via A8-vaulted .env; local throwaway values until CEO places real keys (04-04); nothing keylike in config.yaml or repo |
| T-4-03 | Tampering | wrong OpenRouter slugs (guessing) | medium | mitigate | Live list verification recorded in card + approval; wrong slug = task FAIL, not guess |
</threat_model>

<verification>
- Cards precede installs (commit order); approval precedes install/pull; /health 200; validator green; build+test green.
</verification>

<success_criteria>Phase-4 toolset studied, CEO-gated, installed at its owners; LiteLLM healthy on the shared Postgres. Master-plan step 1 closed.</success_criteria>

<output>Create 04-01-SUMMARY.md with decisive output lines (health check, pnpm ls, validator).</output>
