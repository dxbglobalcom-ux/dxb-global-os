---
phase: 06-memory-router-knowledge-stores
plan: 08
type: execute
wave: 8
depends_on: ["06-06", "06-07"]
files_modified:
  - packages/outbox-executor/src/scheduler.ts
  - packages/memory-router/src/compaction.ts
  - packages/memory-router/src/index.ts
  - tests/phase6/known-facts.test.ts
  - .planning/research/INTEGRATION-TRACKER.md
  - .planning/phases/06-memory-router-knowledge-stores/06-VERIFICATION.md
autonomous: false
requirements: [MEM-01, MEM-02, MEM-03, MEM-04]
must_haves:
  truths:
    - "Expired memory is compacted on schedule, delete-free: pg-boss cron marks expires_at<now() rows superseded (self-tombstone superseded_by=id) + audit 'memory_expired' — recall already excludes them (rule 5's cron half; silme YOK)"
    - "The known-fact battery: the SAME 20 facts from the 06-02 fixture, now written THROUGH the router into their stores and recalled end-to-end — >=18/20 (90%) correct-body retrieval across the composition (master step 10)"
    - "Phase gate is verdict-complete: all four ROADMAP success criteria evidenced in 06-VERIFICATION.md, and the closure verdict is written by Fable after personally reading the artifacts (⛔ FABLE-ONLY — checker PASS is never the last word)"
    - "Tracker truth at exit: memory-stack rows advanced to their real states (EMBED where wired), each with evidence citation"
  artifacts:
    - "packages/memory-router/src/compaction.ts (compactExpired + claude-mem sync schedule hook)"
    - "tests/phase6/known-facts.test.ts (20-fact end-to-end battery)"
    - ".planning/phases/06-memory-router-knowledge-stores/06-VERIFICATION.md (criterion-by-criterion evidence + FABLE verdict)"
  key_links:
    - "known-facts.test.ts <-> tests/phase6/fixtures/known-facts.json (same fixture as the spike — diff-guarded)"
    - "compaction cron <-> outbox-executor scheduler (single pg-boss instance, session-mode 5432 rule)"
    - "06-VERIFICATION.md <-> STATE.md phase closure + ROADMAP Phase 6 checkboxes"
---

<objective>
Master step 10: phase closure. The compaction cron completes write-policy rule 5, the known-fact battery proves >=90% end-to-end retrieval across the full composition (the spike measured classification; this measures the whole pipeline), and 06-VERIFICATION.md closes the phase criterion-by-criterion with executed evidence.
autonomous: false — closure is a checkpoint: ⛔ FABLE-ONLY verdict; Fable reads the battery output, the verification doc, and the diff before any DONE/PASSED status exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-06.md   (step 10 + §1 gate criteria 1–4)
@.planning/phases/05-kernel-orchestrator-core-loop/05-VERIFICATION.md   (house verification format + FABLE verdict precedent)
@tests/phase6/fixtures/known-facts.json   (the fixture — unchanged since 06-02 by contract)
</context>

## Artifacts this phase produces

**This plan:** `compactExpired()` + scheduled jobs (memory-compaction daily, claude-mem-sync); known-fact battery; 06-VERIFICATION.md; tracker exit states.
**Phase-wide (context):** see 06-01 list — all of it now exists; this plan certifies it.

<tasks>

<task type="auto">
  <name>Task 1: compaction cron + claude-mem sync schedule</name>
  <files>packages/memory-router/src/compaction.ts, packages/memory-router/src/index.ts, packages/outbox-executor/src/scheduler.ts</files>
  <read_first>
    - packages/outbox-executor/src/scheduler.ts (pg-boss schedule registration pattern; direct-5432 session-mode rule; minute-grained cron notes)
    - packages/memory-router/src/adapters/claude-mem.ts (syncClaudeMem from 06-06)
  </read_first>
  <action>
    compaction.ts: `compactExpired(db)` — one UPDATE marking rows where expires_at < now() AND superseded_by IS NULL as superseded_by = id (self-tombstone; DESIGN NOTE in code: schema has no status column and deletion is forbidden — self-reference is the delete-free retired marker; recall's filter already excludes it) + one audit row per batch ('memory_expired', payload {count, ids}) in the same transaction; returns count. Register two pg-boss schedules in the existing scheduler (same boss instance): `memory-compaction` daily 03:00 → compactExpired; `claude-mem-sync` hourly → syncClaudeMem (pointer freshness; note on T-06-18: synced pointers get the contradiction sweep only when promoted content collides — documented residual). Handlers follow the outbox tick's error-isolation pattern (a failing job never kills the scheduler).
  </action>
  <verify><automated>pnpm --filter @dxb/memory-router build && pnpm --filter @dxb/outbox-executor build && pnpm vitest run tests/phase6/known-facts.test.ts -t compaction 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - Expired-row test: seed fact with past expires_at → compactExpired marks it, recall excludes it, audit row exists, nothing deleted (row count unchanged)
    - Idempotent: second run marks 0
    - Both schedules registered next to the existing ones (grep evidence in SUMMARY)
  </acceptance_criteria>
  <done>Rule 5 is complete: memory ages out audited and delete-free, and claude-mem pointers stay fresh unattended.</done>
</task>

<task type="auto">
  <name>Task 2: known-fact battery — >=90% end-to-end across the composition</name>
  <files>tests/phase6/known-facts.test.ts</files>
  <read_first>
    - tests/phase6/fixtures/known-facts.json (fixture contract — MUST be byte-identical to the 06-02 commit)
    - tests/phase6/adapters-roundtrip.test.ts (per-store write/read helpers + container health guard)
  </read_first>
  <action>
    Battery: (1) commit all 20 fixture facts through commitMemory (kind from fixture → store per KIND_STORE; origin 'agent'); (2) for each fixture question run the FULL production recall (memory_recall path with classifier live — this is the pipeline test; DXB_LIVE gating: classifier + embeddings live in the gate run, injectable in CI-deterministic mode with the spike's recorded classifications); (3) score a hit when the returned rows contain the fixture fact's body (id-tracked); (4) print per-question table + `BATTERY=<n>/20`; assert n>=18. Fixture-drift guard: sha256 of known-facts.json compared against the hash recorded in the spike report (T-06-04 mitigation honored). Cleanup tracked ids across all stores.
  </action>
  <verify><automated>pnpm vitest run tests/phase6/known-facts.test.ts 2>&1 | tee /tmp/battery-tail.log | tail -6 | grep -E "passed" && grep -E "BATTERY=(1[89]|20)/20" /tmp/battery-tail.log</automated></verify>
  <acceptance_criteria>
    - BATTERY >= 18/20 printed from a real run (per-question table pasted in SUMMARY)
    - Fixture hash matches the spike report's recorded hash (anti-gaming guard executed)
    - Hits verified by body content, not row presence alone
  </acceptance_criteria>
  <done>Gate criterion 3's second half holds: retrieval quality across the WHOLE composition is measured >=90%, on the same facts the spike promised.</done>
</task>

<task type="checkpoint">
  <name>Task 3: 06-VERIFICATION.md + tracker exit + FABLE closure verdict (⛔)</name>
  <files>.planning/phases/06-memory-router-knowledge-stores/06-VERIFICATION.md, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - .planning/phases/05-kernel-orchestrator-core-loop/05-VERIFICATION.md (format + verdict precedent)
    - .planning/ROADMAP.md Phase 6 success criteria (the four TRUE-statements being certified)
  </read_first>
  <action>
    Write 06-VERIFICATION.md: criterion-by-criterion table mapping each ROADMAP success criterion to its executed evidence (1 → poisoning tests + write-policy rules [06-04/06-05]; 2 → 2-tool MCP surface + obsidian/claude-mem round-trips [06-05/06-06]; 3 → spike report + adapter round-trips + BATTERY score [06-02/06-06/06-08]; 4 → context-rot log + recall-back [06-07]), each line ✓ VERIFIED with command→output or ⚠ UNVERIFIED with reason (two-tier rule; expected ⚠: none — every criterion here is machine-checkable). Advance tracker rows to exit states with citations (open-notebook INSTALL→ADOPT/EMBED as wired; graphify/claude-mem/obsidian-stack → EMBED where round-trips prove embedding). Then the checkpoint: Fable personally reads the battery table, the verification doc, and `git diff --stat` of the phase, and writes the FABLE verdict block (05-VERIFICATION precedent) — only after that verdict may status advance. Any spike-FAIL scope reduction from 06-02 must be restated in the verification doc as a ROADMAP deviation with CEO visibility.
  </action>
  <verify><manual>06-VERIFICATION.md exists with 4/4 criteria evidenced and a FABLE-verdict block; tracker exit states cited; STATE.md advance staged for the closure commit</manual></verify>
  <acceptance_criteria>
    - Every gate criterion has executed evidence (no prediction-tense claims)
    - FABLE verdict present and written in this conversation by Fable (not relayed from any checker)
    - Tracker rows at truthful exit states; deviations/adaptations listed ([ADAPT] 0009→0010 filename, spike outcome, any scope change)
  </acceptance_criteria>
  <done>Phase 6 closes the way Phase 5 did: on evidence, with a Fable-authored verdict, tracker and ROADMAP truth aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron jobs → memory lifecycle | unattended processes mutate trust/lifecycle markers |
| battery score → phase gate | a gamed test would certify a broken memory system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-23 | Tampering | battery fixture drift inflating the score | medium | mitigate | sha256 pin against the spike report; same-fixture contract asserted in test |
| T-06-24 | Repudiation | compaction retiring rows without trail | medium | mitigate | audit row per batch in the same transaction; delete-free tombstones keep history queryable |
| T-06-25 | DoS | runaway sync/compaction jobs starving the scheduler | low | mitigate | error-isolation per handler; daily/hourly cadence; batch UPDATE is single-statement |
</threat_model>

<verification>
- Compaction test green (mark/exclude/audit/idempotent); BATTERY>=18/20 from live-gated run with hash guard; 06-VERIFICATION.md 4/4 + FABLE verdict; tracker exit states cited.
</verification>

<success_criteria>Master step 10 satisfied: lifecycle automation running, >=90% known-fact retrieval proven across the composition, and Phase 6 closed with a criterion-complete verification document under a Fable-authored verdict.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-08-SUMMARY.md with the battery table, compaction evidence, tracker exit table, and the closure commit list. STATE.md advances at closure per house convention.</output>
