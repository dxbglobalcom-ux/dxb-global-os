---
phase: 06-memory-router-knowledge-stores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/study-cards/obsidian-stack.md
  - .planning/research/study-cards/graphify.md
  - .planning/research/study-cards/open-notebook.md
  - .planning/research/study-cards/headroom.md
  - .planning/research/study-cards/claude-mem.md
  - .planning/research/study-cards/litellm.md
  - .planning/research/INTEGRATION-TRACKER.md
  - vps/open-notebook/compose.local.yml
autonomous: true
requirements: [MEM-03]
must_haves:
  truths:
    - "Every Phase-6 tool (obsidian-stack pick, graphify, open-notebook, headroom, claude-mem) has a FILLED study card BEFORE any install/integration step runs (INTEG-01 'no blind installs')"
    - "open-notebook runs as a LOCAL docker service and answers its health endpoint (VPS placement stays Phase 7 per master plan §5)"
    - "The embedding model is PINNED (name + dimension + mode) in the litellm study card — 06-03's migration reads the dimension from this decision, not from a guess"
    - "The memory router has its own LiteLLM identity: virtual key alias dxb-os exists and DXB_LITELLM_KEY_OS is documented as its env carrier (cost attribution = OS overhead, not a borrowed department)"
  artifacts:
    - ".planning/research/study-cards/{obsidian-stack,graphify,open-notebook,headroom,claude-mem}.md — all TBD fields resolved"
    - "vps/open-notebook/compose.local.yml (local-only compose; RAM note for Phase 7)"
    - ".planning/research/INTEGRATION-TRACKER.md rows advanced (STUDY→INSTALL/ADOPT as evidenced)"
  key_links:
    - "litellm.md embedding section <-> 20260709000010_memory_embeddings.sql vector dimension (06-03)"
    - "study cards <-> tracker Status column (a row advances only with its card filled)"
---

<objective>
Phase toolset study→install (master plan PHASE-06 step 1, dual-role principle: tools installed at the START of the phase that uses them). Fill the five memory-stack study cards, install/verify what Phase 6 needs locally (open-notebook container; graphify already active; obsidian = repo-root vault already live; headroom/claude-mem already installed as session plugins), pin the embedding model, and mint the router's own LiteLLM virtual key. No router code in this plan.
Opus may execute per master §6; card verdicts and the embedding-model pin are read by Fable before 06-03 proceeds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-06.md   (step 1 + §5 open-notebook RAM fallback)
@.planning/research/INTEGRATION-TRACKER.md   (status legend, advance-left-to-right rule)
@.planning/research/STACK.md   (memory/RAG layer paragraph; automatic-embeddings pattern; LiteLLM embedding spend rule)
</context>

## Artifacts this phase produces

**This plan:** filled study cards ×5 + litellm.md embedding supplement; `vps/open-notebook/compose.local.yml`; tracker row advances; LiteLLM virtual key `dxb-os` (live, alias-verifiable); env var name `DXB_LITELLM_KEY_OS` (documented — value never committed).
**Phase-wide (context):** migration `memory_embeddings`; `packages/memory-router/src/{classify-read,write-policy}.ts` + `adapters/{obsidian,graphify,notebook,pgvector,claude-mem}.ts`; MCP tools `memory_commit`/`memory_recall`; CLI `dxb promote`; routing-seed rows `memory.classify`/`memory.promote`; `llmEmbed()` in shared/litellm.ts; spike report `.planning/master-plan/spikes/06-routing.md`; tests/phase6/*; compaction cron; 06-VERIFICATION.md.

<tasks>

<task type="auto">
  <name>Task 1: fill the five study cards (STUDY step, no blind installs)</name>
  <files>.planning/research/study-cards/obsidian-stack.md, .planning/research/study-cards/graphify.md, .planning/research/study-cards/open-notebook.md, .planning/research/study-cards/headroom.md, .planning/research/study-cards/claude-mem.md</files>
  <read_first>
    - .planning/research/study-cards/litellm.md (the format a FILLED card takes — 04-01 precedent)
    - .planning/research/INTEGRATION-TRACKER.md rows for the five items (Trigger/Owner constraints)
  </read_first>
  <action>
    Resolve every TBD field on each card: source repo/URL, pinned version, purpose in DXB, key API/usage notes (the EXACT invocation the Phase-6 adapter will use), known pitfalls, install command, legitimacy verdict. Specifics per card:
    - obsidian-stack: the study PICKS the final combination (tracker: "Study pass picks the final combination"). Constraint: the repo root IS the vault (quick task 260706-h26); the adapter writes plain .md files with YAML frontmatter via fs — no Obsidian plugin dependency for the write path. Card records: chosen note location `memory-store/` (router-owned subtree at vault root), frontmatter contract (memory_index id, kind, trust_tier, provenance), and which community components (if any) are ADOPTED read-side vs EXCLUDED.
    - graphify: already active (this project's own graph). Card records the exact CLI/API the graphify adapter calls to add a node + query, where the graph lives (graphify-out/ vs .planning/graphs/), and idempotency behavior.
    - open-notebook: container image, pinned tag, API surface (create doc / search / health path), RAM footprint measured locally (Phase-7 budget input), auth model. Verdict BEFORE install (34.9k stars claim re-verified).
    - headroom: session-layer compression proxy — card states explicitly what it does and does NOT cover for MEM-04 (it compresses harness sessions; the OS-level context-budget mechanism lands in 06-07). Records activation state + config.
    - claude-mem: hook architecture, where observations live (DB path/schema), the read surface the pointer-sync adapter (06-06) will use, and the LOCKED rule: claude-mem stays an autonomous hook OUTSIDE the router write path — pointer sync only.
  </action>
  <verify><automated>! grep -l "TBD" .planning/research/study-cards/{obsidian-stack,graphify,open-notebook,headroom,claude-mem}.md</automated></verify>
  <acceptance_criteria>
    - Zero TBD fields across the five cards; each has Legitimacy Verdict + pinned version
    - obsidian-stack card names the router-owned vault subtree and frontmatter contract 06-04 will implement verbatim
    - claude-mem card cites the LOCKED pointer-sync-only rule
  </acceptance_criteria>
  <done>Study pass complete — installs may proceed with eyes open.</done>
</task>

<task type="auto">
  <name>Task 2: open-notebook local install + tracker advance</name>
  <files>vps/open-notebook/compose.local.yml, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - .planning/research/study-cards/open-notebook.md (install command recorded in Task 1 — run exactly that)
    - vps/ (existing compose layout conventions)
  </read_first>
  <action>
    Write vps/open-notebook/compose.local.yml from the card (pinned tag, local-only ports, named volume, mem_limit set to the measured footprint + headroom; comment: "LOCAL ONLY this phase — VPS placement decided by Phase 7 RAM budget test, master §5"). Bring it up, hit the health endpoint from the card, record the measured RSS in the card's RAM note. Advance tracker rows with evidence: open-notebook STUDY→INSTALL, obsidian-stack STUDY→ADOPT (no install artifact — vault already live; note the picked combination), graphify INSTALL→ADOPT (retroactive card now filled), headroom STUDY→ADOPT, claude-mem STUDY→ADOPT (hook already active; pointer-sync EMBED lands 06-06). Every advance cites its card in Notes.
  </action>
  <verify><automated>docker compose -f vps/open-notebook/compose.local.yml ps --format '{{.Status}}' | grep -qi up && curl -sf "$(grep -oP 'health[^ ]*' .planning/research/study-cards/open-notebook.md | head -1 || echo http://127.0.0.1:5055/health)" >/dev/null && echo HEALTH_OK</automated></verify>
  <acceptance_criteria>
    - `docker compose ps` shows the service Up; health endpoint returns success (exact URL from the card; the grep fallback above is replaced by the card's real path at execution)
    - Tracker: the five rows advanced left-to-right only, each with card citation; no row skips a stage
  </acceptance_criteria>
  <done>Research brain runs locally; tracker reflects reality (master step-1 verification satisfied).</done>
</task>

<task type="auto">
  <name>Task 3: pin embedding model + mint dxb-os virtual key</name>
  <files>.planning/research/study-cards/litellm.md</files>
  <read_first>
    - packages/shared/src/litellm.ts (keyGenerate/keyInfo/listDxbKeys + DXB_KEY_ALIAS_PREFIX — reuse, do not reimplement)
    - .planning/research/study-cards/litellm.md (04-01 card being supplemented)
    - vps/ litellm config location (model registration pattern used for existing api-mode models)
  </read_first>
  <action>
    (a) Embedding pin: choose the cheap API embedding model (STACK.md rule: embedding via LiteLLM so spend stays in the ledger; OpenRouter-reachable; budget-fit). Register it in the LiteLLM config the same way existing api models are registered; restart/reload proxy; prove with one live /embeddings call through the proxy. Append an "Embeddings (Phase 6)" section to litellm.md: model name, LiteLLM alias, VECTOR DIMENSION (the number 06-03's migration uses), price note, and the curl proof line.
    (b) Router identity: using shared keyGenerate (master key from env), create virtual key alias `dxb-os` if `listDxbKeys()` doesn't already show it; budget-tag it like existing department keys. Document env carrier name DXB_LITELLM_KEY_OS in the card (key VALUE goes to the local vault/.env flow — NEVER into the repo; A8).
    (c) glm-5.2 reachability: the 06-02 spike and the 06-05 read classifier call glm-5.2 (api mode) through the proxy with the dxb-os key — prove it now with one live /chat/completions call (model registered in LiteLLM config if it is not already; same registration pattern as the L4 fallbacks). Paste the one-line response proof in the card next to the embeddings proof. A dead glm-5.2 path discovered at 06-02 would invalidate the spike run — fail here instead.
  </action>
  <verify><automated>grep -q "DXB_LITELLM_KEY_OS" .planning/research/study-cards/litellm.md && grep -qiE "dimension.*[0-9]{3,4}" .planning/research/study-cards/litellm.md</automated></verify>
  <acceptance_criteria>
    - Live /embeddings call through the proxy returns a vector; its length equals the documented dimension (paste both in SUMMARY)
    - Live glm-5.2 chat call through the proxy succeeds with the dxb-os key (one-line proof in card + SUMMARY)
    - `listDxbKeys()` output includes alias dxb-os (paste in SUMMARY); key value absent from git (`git grep` of any key fragment returns nothing)
    - litellm.md has the Embeddings section with model + dimension + alias
  </acceptance_criteria>
  <done>06-03 can write `vector(N)` from a decided N; router spend is attributable from day one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| study card → install command | cards are the only path from "doc says use X" to "X runs here" |
| LiteLLM master key → key mint | admin operation with env-held secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | unpinned open-notebook image drifting under us | medium | mitigate | pinned tag in compose.local.yml from the card; legitimacy verdict before first pull |
| T-06-02 | Info disclosure | dxb-os key value leaking into repo/prompts | high | mitigate | env-carrier documented by NAME only; acceptance includes git-grep-clean check; A8 vault flow for the value |
| T-06-03 | Elevation | open-notebook exposed beyond localhost | medium | mitigate | compose binds local ports only; no Caddy/route this phase; VPS exposure is Phase 7's gateway decision |
</threat_model>

<verification>
- Five cards TBD-free; container Up + health OK; tracker advanced with citations; embedding dimension documented and proven by a live call; dxb-os alias listed.
</verification>

<success_criteria>Master step 1 done with evidence: no Phase-6 tool remains unstudied, everything Phase 6 touches is installed/active locally, and the two decisions later plans consume (embedding dimension, router key) are pinned in writing.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-01-SUMMARY.md with: card-by-card verdict table, container health line, embeddings proof (model, dimension, vector length), dxb-os key listing (alias only).</output>
