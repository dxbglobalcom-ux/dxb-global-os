---
phase: 06-memory-router-knowledge-stores
plan: 07
type: execute
wave: 7
depends_on: ["06-05"]
files_modified:
  - packages/orchestrator/src/context-budget.ts
  - packages/orchestrator/src/worker-shim.ts
  - packages/orchestrator/src/index.ts
  - tests/phase6/context-rot.test.ts
autonomous: true
requirements: [MEM-04]
must_haves:
  truths:
    - "A 50-step long-running task keeps its working context inside a fixed token band via summarize + memory-offload (facts committed through memory_commit's door, NOT a side channel) — measured per step and logged (master step 9's ölçüm logu)"
    - "Without the mechanism the same 50-step task provably exceeds the band (control run) — the demo shows prevention, not absence of the problem"
    - "Offloaded facts are recoverable: a post-run recall retrieves planted mid-task facts that were evicted from the live context (offload is memory, not amnesia)"
    - "The mechanism is orchestrator-level and deterministic-testable (injectable summarizer/executor like runWorkerOnce); headroom remains the session-layer complement per its 06-01 card — MEM-04 is satisfied at the OS layer, not by pointing at a harness plugin"
  artifacts:
    - "packages/orchestrator/src/context-budget.ts (token estimate + band check + summarize-and-offload step)"
    - "tests/phase6/context-rot.test.ts (50-step demo: control vs managed, token log, recall-back proof)"
  key_links:
    - "context-budget offload <-> commitMemory (single write door — provenance origin:'agent', source:'context-offload')"
    - "worker-shim step loop <-> checkContextBudget() hook (opt-in per task; default threshold from one exported constant)"
    - "measurement log <-> 06-VERIFICATION MEM-04 evidence (06-08 cites this file)"
---

<objective>
Master step 9: context-rot prevention demonstrated. Build the OS-level mechanism — a context-budget helper the worker loop calls between steps: estimate tokens, and past the band compress (summarize) + offload durable facts through the router's single write door, keeping the live context flat. Prove it with a 50-step synthetic task: control run grows unbounded, managed run stays in band, offloaded facts recall back. MEM-04's requirement text ("compression, summaries, memory offloading keep long-running work inside clean contexts") maps 1:1 to the three moves.
Opus may execute per master §6; the band threshold default is a plan constant — changing it later is a routing/quality decision (⛔ Fable).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-06.md   (step 9 + §1 criterion 4)
@.planning/research/study-cards/headroom.md   (session-layer scope note from 06-01 — what this plan deliberately does NOT lean on)
</context>

## Artifacts this phase produces

**This plan:** `estimateTokens()`, `checkContextBudget()`, `summarizeAndOffload()` in context-budget.ts; worker-shim opt-in hook; the 50-step demo test with its measurement log.
**Phase-wide (context):** see 06-01 list.

<tasks>

<task type="auto">
  <name>Task 1: context-budget.ts — estimate, band, summarize-and-offload</name>
  <files>packages/orchestrator/src/context-budget.ts, packages/orchestrator/src/index.ts</files>
  <read_first>
    - packages/orchestrator/src/worker-shim.ts (step/context shape the helper must accept; injectable-executor seam style)
    - packages/memory-router/src/write-policy.ts (commitMemory signature — offload goes through the door)
    - packages/orchestrator/src/qa.ts (llmCall usage pattern for the summarizer's production default)
  </read_first>
  <action>
    context-budget.ts: `estimateTokens(text)` (chars/4 heuristic, documented as estimate — band sized with margin so the heuristic's error cannot mask a breach); `CONTEXT_BAND = { softLimit: 12_000, hardLimit: 16_000 }` exported constants (tokens, per-task working context); `checkContextBudget(ctx, deps)`: under soft → unchanged; over soft → `summarizeAndOffload`: (a) extract durable facts from the oldest 60% of step history (deps.extractFacts — production default: llmCall summarize-class row, injectable for tests), (b) commitMemory each fact (kind 'fact', provenance {agent: task worker id, task_id, origin:'agent', source:'context-offload'}), (c) replace that history slice with a compact summary block carrying the memory ids (recall handles), (d) return new context + measurement record {step, before, after, offloaded_count}. Over hard even after compression → loud error (band breach is a bug, never silent). Export from orchestrator index.
  </action>
  <verify><automated>pnpm --filter @dxb/orchestrator build && node -e "const o=require('./packages/orchestrator/dist/index.js'); for(const s of ['estimateTokens','checkContextBudget','CONTEXT_BAND']) if(!(s in o)) throw s+' missing'; console.log('BUDGET_OK')"</automated></verify>
  <acceptance_criteria>
    - Offload path calls commitMemory (grep: no direct adapter/DB write in context-budget.ts)
    - Summary block references committed memory ids (recall pointer, not lossy discard)
    - Hard-limit breach throws (test-asserted)
  </acceptance_criteria>
  <done>The OS has a compress+offload move any long task can invoke.</done>
</task>

<task type="auto">
  <name>Task 2: worker-shim opt-in hook</name>
  <files>packages/orchestrator/src/worker-shim.ts</files>
  <read_first>
    - packages/orchestrator/src/worker-shim.ts (event-trail discipline — hook must event when it fires)
  </read_first>
  <action>
    Between executor steps, when the task's context budget is enabled (deps flag, default ON for multi-step tasks), call checkContextBudget; when it compresses, append a task_event 'context_compressed' {before, after, offloaded_count} to the existing event trail (observability — the 10/10-gate style event chains stay contiguous). No behavior change for single-step tasks (Phase-5 slice signature untouched — assert by running the phase-5 suite).
  </action>
  <verify><automated>pnpm --filter @dxb/orchestrator build && pnpm vitest run tests/phase5/ 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - Phase-5 suite stays green (no regression on the closed gate's behavior)
    - 'context_compressed' event lands in task_events when the hook fires (asserted in Task 3)
  </acceptance_criteria>
  <done>Long-running work gets the mechanism without disturbing the certified core loop.</done>
</task>

<task type="auto">
  <name>Task 3: 50-step context-rot demo — control vs managed, with measurement log</name>
  <files>tests/phase6/context-rot.test.ts</files>
  <read_first>
    - tests/phase5/decompose-dispatch.test.ts (injectable executor e2e pattern)
    - tests/phase6/recall.test.ts (recall assertion helpers)
  </read_first>
  <action>
    Synthetic 50-step task via injectable executor: each step appends ~800 estimated tokens of synthetic work product, and steps 10/25/40 plant distinctive facts (e.g. 'the DXB spike gate threshold is 16/20'). CONTROL run (budget disabled): assert final context estimate exceeds hardLimit (the rot is real). MANAGED run (budget on, injected deterministic extractFacts/summarizer): assert (1) every per-step measurement ≤ hardLimit and post-compression points ≤ softLimit (band held), (2) ≥2 compressions fired with 'context_compressed' events in the trail, (3) planted facts recall back via recallMemory after eviction from live context (offload proof), (4) write per-step CSV log tests/phase6/context-rot.log (step,tokens,event) — the master step-9 ölçüm logu, committed. One DXB_LIVE_SDK=1 managed run with the real summarizer recorded for SUMMARY.
  </action>
  <verify><automated>pnpm vitest run tests/phase6/context-rot.test.ts 2>&1 | tail -5 | grep -E "passed" && test -s tests/phase6/context-rot.log && awk -F, 'NR>1 && $2+0 > 16000 {exit 1}' tests/phase6/context-rot.log && echo BAND_HELD</automated></verify>
  <acceptance_criteria>
    - Control exceeds band; managed never does (log-backed, awk-checked)
    - Planted-fact recall proof green; compression events present in trail
    - context-rot.log committed with 50 rows of measurements
  </acceptance_criteria>
  <done>Gate criterion 4 has executed evidence: the context-rot scenario is demonstrated AND defeated, with recoverable memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step history → summarizer → memory | task-internal text becomes durable trusted memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-20 | Tampering | offload laundering untrusted step content into trusted memory | high | mitigate | offload goes through commitMemory: provenance origin:'agent' source:'context-offload' recorded; contradiction rule 2 applies to offloaded facts like any other |
| T-06-21 | Repudiation | silent context truncation losing CEO-relevant facts | medium | mitigate | compression is evented ('context_compressed') + summary carries memory ids; hard-limit breach throws instead of silently dropping |
| T-06-22 | DoS/cost | summarizer loop burning tokens on every step | low | mitigate | soft-limit trigger only (not per-step); summarize-class L4 row; deterministic seam keeps tests free |
</threat_model>

<verification>
- Exports live; phase-5 suite green; demo green with committed measurement log; band held by awk over the log; recall-back proven.
</verification>

<success_criteria>Master step 9 satisfied: a long-running task demonstrably stays inside a clean bounded context using compression + summaries + router-door offload, with the measurement log as evidence and no regression to the certified Phase-5 loop.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-07-SUMMARY.md with control-vs-managed numbers, log excerpt (first/compression/last rows), recall-back proof line, and the live-run sample.</output>
