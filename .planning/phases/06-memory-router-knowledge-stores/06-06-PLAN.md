---
phase: 06-memory-router-knowledge-stores
plan: 06
type: execute
wave: 6
depends_on: ["06-02", "06-05"]
files_modified:
  - packages/memory-router/src/adapters/graphify.ts
  - packages/memory-router/src/adapters/notebook.ts
  - packages/memory-router/src/adapters/claude-mem.ts
  - packages/memory-router/src/write-policy.ts
  - packages/memory-router/src/index.ts
  - tests/phase6/adapters-roundtrip.test.ts
autonomous: true
requirements: [MEM-02, MEM-03]
must_haves:
  truths:
    - "Every routed store in the spike-confirmed composition has a live adapter: write→memory_index→recall round-trips for obsidian, graphify, notebook, pgvector (master step 8) — StoreNotWiredError paths from 06-04/06-05 are gone for in-scope stores"
    - "claude-mem stays an autonomous hook OUTSIDE the router write path — its adapter is pointer sync ONLY: observation pointers appear in memory_index (store='claude-mem') without claude-mem itself being re-routed (LOCKED)"
    - "If the 06-02 spike deferred a store (FAIL branch), its adapter is NOT built here — the plan shrinks to the confirmed composition and the deferral note is restated in the SUMMARY (scope truth, no zombie code)"
    - "Graphify/notebook invocations follow their 06-01 study cards exactly — no undocumented API guesswork ('no guessing' process rule)"
  artifacts:
    - "packages/memory-router/src/adapters/{graphify,notebook,claude-mem}.ts"
    - "tests/phase6/adapters-roundtrip.test.ts (per-store write→index→recall)"
  key_links:
    - "adapters registry in write-policy.ts <-> KIND_STORE map (relation→graphify, procedure→notebook now real)"
    - "claude-mem adapter syncClaudeMem() <-> memory_index pointer rows (store='claude-mem', ref=observation id)"
    - "adapter contracts <-> study cards (invocation lines quoted in code comments)"
---

<objective>
Master step 8: the remaining store adapters land behind the SAME single door — graphify (relation kinds), open-notebook (procedure kinds), and the claude-mem pointer sync. Each proves a full round-trip: commitMemory → memory_index row + physical store write → recallMemory returns the body. After this plan, the composition validated by the spike is fully wired and MEM-02/MEM-03's store integration claims hold with executed evidence.
Scope guard: this plan implements ONLY the stores the 06-02 verdict confirmed. Opus may execute per master §6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/spikes/06-routing.md   (confirmed composition — the scope authority for this plan)
@.planning/research/study-cards/graphify.md   (node add/query invocation from 06-01)
@.planning/research/study-cards/open-notebook.md   (API surface + local container from 06-01)
@.planning/research/study-cards/claude-mem.md   (observation store read surface; LOCKED pointer-sync rule)
</context>

## Artifacts this phase produces

**This plan:** graphify/notebook/claude-mem adapters; write-policy registry updated (StoreNotWiredError removed for wired stores); round-trip test suite.
**Phase-wide (context):** see 06-01 list.

<tasks>

<task type="auto">
  <name>Task 1: graphify + notebook adapters (write + readByRef)</name>
  <files>packages/memory-router/src/adapters/graphify.ts, packages/memory-router/src/adapters/notebook.ts, packages/memory-router/src/write-policy.ts, packages/memory-router/src/index.ts</files>
  <read_first>
    - .planning/research/study-cards/graphify.md + open-notebook.md (EXACT invocations recorded at 06-01 — quote them in adapter header comments)
    - packages/memory-router/src/adapters/obsidian.ts (adapter contract shape: write returns ref, readByRef returns body, loud failures)
    - packages/memory-router/src/write-policy.ts (registry entries currently throwing StoreNotWiredError)
  </read_first>
  <action>
    graphify.ts: `writeNode({ indexId, body, provenance })` adds a node via the card's documented CLI/API (graph location from card), ref = node id; `readByRef(ref)` returns the node body; both loud on nonzero exit / missing node. notebook.ts: `writeDoc({ indexId, body, provenance })` creates a doc via the local container's API (base URL env `DXB_NOTEBOOK_URL` defaulting to the compose.local.yml port), ref = doc id; `readByRef(ref)` fetches it back; container-down = typed loud error (recall stays honest when the service sleeps). Replace the two StoreNotWiredError registry entries with the real adapters. If the spike deferred one of these stores: implement only the confirmed one, leave the deferred entry throwing with a comment citing the spike report, and skip its tasks below.
  </action>
  <verify><automated>pnpm --filter @dxb/memory-router build && node -e "const w=require('./packages/memory-router/dist/index.js'); console.log('REGISTRY_OK')" && ! grep -rn "StoreNotWiredError" packages/memory-router/src/write-policy.ts | grep -v "spike-deferred" </automated></verify>
  <acceptance_criteria>
    - Both in-scope adapters implement write + readByRef with card-quoted invocations
    - Registry has no StoreNotWiredError entries except an explicitly spike-deferred store (comment cites the report)
    - notebook adapter fails typed-loud when the container is down (test-asserted in Task 3)
  </acceptance_criteria>
  <done>Relations and procedures physically land in their stores through the one door.</done>
</task>

<task type="auto">
  <name>Task 2: claude-mem pointer sync (LOCKED: sync, don't reroute)</name>
  <files>packages/memory-router/src/adapters/claude-mem.ts</files>
  <read_first>
    - .planning/research/study-cards/claude-mem.md (where observations live + read surface — filled at 06-01)
    - db/migrations/20260707000005_memory_index.sql (store CHECK includes 'claude-mem')
  </read_first>
  <action>
    claude-mem.ts: `syncClaudeMem(db, { since? })` reads observation records from claude-mem's store (per card), and for each new observation inserts a memory_index pointer row: kind='fact', store='claude-mem', ref=observation id, provenance {agent:'claude-mem-hook', task_id:null, origin:'agent', source:'claude-mem'}, trust_tier='trusted' (agent-origin per rule 1), confidence default. Idempotent by ref (existing ref skipped, count reported `{scanned, inserted, skipped}`). `readByRef(ref)` resolves the observation body for recall. NO write path INTO claude-mem exists (the hook stays autonomous — enforced by the adapter simply not exporting a write; comment cites LOCKED rule). Register readByRef in the recall registry; syncClaudeMem exported for the 06-08 cron to schedule.
  </action>
  <verify><automated>pnpm --filter @dxb/memory-router build && node -e "const m=require('./packages/memory-router/dist/index.js'); if(typeof m.syncClaudeMem!=='function') throw 'sync missing'; console.log('SYNC_OK')" && ! grep -n "writeObservation\|commitToClaudeMem" packages/memory-router/src/adapters/claude-mem.ts</automated></verify>
  <acceptance_criteria>
    - Second run inserts 0 (idempotency count evidence)
    - Pointer rows carry store='claude-mem' + observation ref; no router→claude-mem write exists (grep proof)
  </acceptance_criteria>
  <done>Cross-session memory is discoverable through recall without breaking the working hook system.</done>
</task>

<task type="auto">
  <name>Task 3: per-store round-trip suite (master step 8 verification)</name>
  <files>tests/phase6/adapters-roundtrip.test.ts</files>
  <read_first>
    - tests/phase6/recall.test.ts (recall assertion helpers from 06-05)
    - tests/phase6/pgvector.test.ts (tracked-id cleanup pattern)
  </read_first>
  <action>
    For EACH wired store: commitMemory a fixture body of that store's kind → assert memory_index row (correct store, trust, expires_at per kind) + physical artifact exists (obsidian: file on disk; graphify: node queryable; notebook: doc GET 200; pgvector: embedding row) → recallMemory with kind given returns the body. Plus: claude-mem sync round-trip (seed one observation per card's mechanism or fixture, sync, recall by ref); notebook container-down negative (stop container or point env at a dead port → typed error, memory_index write rolled back — atomicity holds cross-adapter); cleanup removes created notes/nodes/docs/rows (obsidian files under memory-store/ deleted by tracked indexId).
    Live-service note: this suite needs the open-notebook container Up (06-01) — guard with a beforeAll health check that FAILS (not skips) with a pointer to compose.local.yml when down, keeping Evidence-Before-Done honest.
  </action>
  <verify><automated>pnpm vitest run tests/phase6/adapters-roundtrip.test.ts 2>&1 | tail -5 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - One green round-trip per wired store, each asserting index row + physical artifact + recall body
    - Cross-adapter atomicity negative proven (dead notebook → zero orphan rows)
    - Suite fails loud (not skip) when the container is down
  </acceptance_criteria>
  <done>Master step 8's "her adapter round-trip testi" is executed evidence, store by store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → graphify CLI / notebook API | router shells out / HTTP-calls external store surfaces |
| claude-mem store → memory_index | hook-written content becomes recallable pointers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Tampering | shell-argument injection via memory body into graphify CLI | high | mitigate | no shell string interpolation — execFile-style arg arrays / API where card allows; body passed via stdin/file per card |
| T-06-18 | Spoofing | claude-mem pointers importing content the write policy never saw | medium | accept-partial | pointers are agent-origin trusted by rule 1; sync inserts pointer METADATA only (body stays in claude-mem); contradiction sweep over synced facts deferred to compaction cron note (06-08) |
| T-06-19 | Info disclosure | notebook API exposed beyond localhost during tests | low | mitigate | DXB_NOTEBOOK_URL defaults to 127.0.0.1 compose port; no external binding (T-06-03 carries) |
</threat_model>

<verification>
- Registry free of StoreNotWiredError (or spike-deferred-commented); syncClaudeMem idempotent counts; round-trip suite green with atomicity negative.
</verification>

<success_criteria>Master step 8 satisfied: the confirmed composition is fully wired behind the single door, claude-mem is pointer-synced without rerouting, and every store proves write→index→recall with executed evidence.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-06-SUMMARY.md with per-store round-trip evidence lines, sync counts, and (if applicable) the spike-deferral restatement.</output>
