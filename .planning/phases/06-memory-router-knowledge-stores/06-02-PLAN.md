---
phase: 06-memory-router-knowledge-stores
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/kernel/policy/routing-seed.json
  - tests/phase6/fixtures/known-facts.json
  - tests/phase6/routing-spike.mjs
  - .planning/master-plan/spikes/06-routing.md
autonomous: false
requirements: [MEM-03]
must_haves:
  truths:
    - "The read-path classifier's store choice is measured BEFORE any router code is built: 20 hand-labeled known-fact questions, classifier picks the correct store on >=16/20 (master step 2 — this is the LOW-confidence composition gate)"
    - "The classifier prototype uses EXACTLY the production design: memory-kind heuristics + cheap model from routing_rules row task_class='memory.classify' (glm-5.2, api mode) — no Fable/Opus call anywhere on the read path (LOCKED)"
    - "A <16/20 result STOPS the phase at a checkpoint: composition simplification (defer graphify OR notebook, master §5) is a ⛔ FABLE-ONLY decision surfaced to the CEO, and MEM-03 scope change is flagged as a ROADMAP deviation — never silently absorbed"
    - "The 20-question fixture is committed and reused verbatim by the 06-08 known-fact battery (same facts, end-to-end this time)"
  artifacts:
    - "tests/phase6/fixtures/known-facts.json (20 questions × {question, fact, correct_store, kind})"
    - "tests/phase6/routing-spike.mjs (runnable spike: prints per-question verdict + total)"
    - ".planning/master-plan/spikes/06-routing.md (spike report: score, misroutes, prompt version, PASS/FAIL verdict)"
    - "routing-seed.json row: memory.classify → L4 glm-5.2 api low"
  key_links:
    - "routing_rules row 'memory.classify' <-> classify-read.ts model lookup (06-05 uses the SAME row)"
    - "known-facts.json <-> 06-08 battery (>=90% end-to-end gate reads the same fixture)"
    - "spike verdict <-> 06-06 adapter scope (FAIL shrinks the wired composition)"
---

<objective>
Routing-quality spike (master step 2). The store composition (Obsidian + Graphify + open-notebook + pgvector behind one router) was rated LOW confidence at roadmap time — before building adapters and the write path, prove the cheap read-classifier can route known-fact questions to the right store at >=16/20. PASS unlocks 06-03..06-08 as planned; FAIL returns the composition decision to Fable (⛔) with the CEO informed.
autonomous: false — the verdict step is a checkpoint regardless of outcome (PASS is Fable-read evidence; FAIL is a Fable decision + CEO-visible scope flag).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-06.md   (step 2 gate; §2 LOCKED "read-path classifier cheap model"; §5 fallback)
@.planning/research/study-cards/litellm.md   (glm-5.2 reachability; dxb-os key from 06-01)
@packages/kernel/policy/routing-seed.json   (row format; priority semantics)
</context>

## Artifacts this phase produces

**This plan:** `memory.classify` routing row (seeded live); `tests/phase6/fixtures/known-facts.json`; `tests/phase6/routing-spike.mjs`; `.planning/master-plan/spikes/06-routing.md` with the gate verdict.
**Phase-wide (context):** see 06-01 list.

<tasks>

<task type="auto">
  <name>Task 1: seed memory.classify routing row + author the 20-fact fixture</name>
  <files>packages/kernel/policy/routing-seed.json, tests/phase6/fixtures/known-facts.json</files>
  <read_first>
    - packages/kernel/policy/routing-seed.json (row shape; existing L4 rows for mode/effort values)
    - db/seed/import-routing-rules.ts (idempotency contract — task_class+model+priority key)
    - .planning/master-plan/PHASE-06.md §2 (LOCKED: classifier is glm-5.2 via routing_rules, NOT hardcoded)
  </read_first>
  <action>
    (a) Append to routing-seed.json: `{ "task_class": "memory.classify", "match": {}, "model_tier": "L4", "model": "glm-5.2", "mode": "api", "effort": "low", "needs_council": false, "priority": 10 }`. Run `node --experimental-strip-types db/seed/import-routing-rules.ts` and paste the inserted-row line.
    (b) Author known-facts.json: 20 entries `{id, question, fact, kind, correct_store}` hand-distributed across the four routed stores per the composition's kind map — fact→pgvector (semantic/atomic facts), relation→graphify (who-relates-to-what), artifact→obsidian (documents/notes), procedure→notebook (research/how-to corpus). Minimum 4 per store, realistic DXB content (department facts, tool relations, persona artifacts, research procedures — NO invented company facts; use real repo/project truths). correct_store is the hand label the classifier must hit.
  </action>
  <verify><automated>node --experimental-strip-types db/seed/import-routing-rules.ts && psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -t -A -c "SELECT count(*) FROM routing_rules WHERE task_class='memory.classify' AND model='glm-5.2'" | grep -qx 1 && node -e "const f=require('./tests/phase6/fixtures/known-facts.json'); const s={}; f.forEach(x=>s[x.correct_store]=(s[x.correct_store]||0)+1); if(f.length!==20) throw 'need 20'; for (const st of ['pgvector','graphify','obsidian','notebook']) if(!s[st]||s[st]<4) throw 'min 4 per store: '+JSON.stringify(s); console.log('FIXTURE_OK', JSON.stringify(s))"</automated></verify>
  <acceptance_criteria>
    - routing_rules has exactly one enabled memory.classify row (glm-5.2, api, low)
    - Fixture: 20 entries, all four stores represented >=4×, every entry has all five fields
  </acceptance_criteria>
  <done>The gate's measuring stick exists and the classifier's model choice is data, not code.</done>
</task>

<task type="auto">
  <name>Task 2: spike runner — classify 20 questions via the production design</name>
  <files>tests/phase6/routing-spike.mjs</files>
  <read_first>
    - packages/shared/src/litellm.ts (llmCall signature; departmentKeyEnvVar — spike calls with department 'os')
    - packages/orchestrator/src/qa.ts (existing llmCall JSON-verdict pattern — reuse the parse-strictly approach)
  </read_first>
  <action>
    routing-spike.mjs: for each fixture entry, run the EXACT classification the production classify-read.ts (06-05) will ship: (1) look up routing_rules memory.classify row (model/mode/effort from DB — no literal), (2) prompt: system text states the four stores with one-line store contracts (pgvector=atomic facts semantic lookup; graphify=entity relations; obsidian=authored artifacts/notes; notebook=research corpus/procedures) + the kind hints, user text = the question; demand strict JSON `{"store":"...","kind":"..."}`, (3) llmCall via department 'os' (DXB_LITELLM_KEY_OS), temperature-free deterministic settings, (4) compare to correct_store. Print per-question line `Q<id> want=<store> got=<store> OK|MISS` and final `SCORE=<n>/20`. Exit 0 if n>=16 else exit 1. The prompt text is written ONCE here and lifted verbatim into classify-read.ts at 06-05 — note this in a header comment.
  </action>
  <verify><automated>node tests/phase6/routing-spike.mjs; test $? -le 1</automated></verify>
  <acceptance_criteria>
    - Runner reads model from routing_rules (grep: no "glm" literal in routing-spike.mjs)
    - Strict JSON parse with loud failure on malformed output (a parse failure counts as MISS, never a crash)
    - SCORE line printed; exit code encodes the gate
  </acceptance_criteria>
  <done>The gate is executable and deterministic in shape (same fixture, same prompt, same row).</done>
</task>

<task type="checkpoint">
  <name>Task 3: run the gate, write the spike report, route the verdict (⛔ FABLE-ONLY on FAIL)</name>
  <files>.planning/master-plan/spikes/06-routing.md</files>
  <read_first>
    - .planning/master-plan/PHASE-06.md §5 (fallback: defer graphify OR notebook; MEM-03 scope = ROADMAP change flag)
  </read_first>
  <action>
    Run `node tests/phase6/routing-spike.mjs | tee tests/phase6/spike-run.log`. Write .planning/master-plan/spikes/06-routing.md: date, routing row used, prompt version hash (sha256 of the prompt block), full per-question table, SCORE, misroute analysis (which store pairs confuse), and VERDICT.
    - **PASS (>=16/20):** report states composition CONFIRMED (all four routed stores stay in scope); Fable reads the report and records the verdict line in the SUMMARY; phase continues to 06-03.
    - **FAIL (<16/20):** STOP. Do not start 06-03. The report gains a "Composition decision required" section with the two master-§5 candidates (defer graphify vs defer open-notebook) and the misroute evidence for each. This decision is ⛔ FABLE-ONLY and CEO-visible: present verdict + recommendation at the checkpoint, record the chosen simplification in the report AND flag MEM-03 scope reduction in STATE.md Blockers as a pending ROADMAP change. Downstream plans 06-05/06-06/06-08 are then revised by Fable before execution (kind map + adapter scope + battery fixture shrink accordingly).
    One retry IS allowed before declaring FAIL if the failure is mechanical (auth/parse), never to shop for a better score: identical fixture+prompt, root cause of the mechanical failure documented.
  </action>
  <verify><manual>Spike report exists with SCORE + VERDICT; on PASS: score >=16 visible in spike-run.log; on FAIL: composition decision recorded + STATE.md blocker updated</manual></verify>
  <acceptance_criteria>
    - Report contains: per-question table, SCORE=n/20, prompt hash, verdict section
    - The verdict path taken matches the score (no PASS narration on <16)
    - On FAIL: STATE.md Blockers carries the MEM-03 ROADMAP-change flag and names the deferred store
  </acceptance_criteria>
  <done>Master step-2 gate executed; the LOW-confidence composition is now a measured fact with a recorded decision, whichever way it went.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture labels → gate verdict | hand labels define "correct"; a lazy fixture makes the gate meaningless |
| cheap-model output → store choice | glm-5.2 JSON is untrusted text until strictly parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | fixture edited later to make 06-08 battery trivially pass | medium | mitigate | fixture committed here; 06-08 references the same file and diffs against this commit in its verify |
| T-06-05 | Repudiation | spike score claimed without a run | high | mitigate | spike-run.log tee'd + per-question table pasted in report; Evidence-Before-Done applies |
| T-06-06 | Spoofing | malformed model JSON silently counted as a hit | medium | mitigate | strict parse; parse failure = MISS by contract |
</threat_model>

<verification>
- Routing row live (SQL count=1); fixture valid (20/4×4); spike ran with tee'd log; report has SCORE + VERDICT; FAIL path leaves a Fable decision + STATE flag, PASS path leaves a Fable-read confirmation.
</verification>

<success_criteria>Master step 2 satisfied: routing quality across the composition is measured with the production classifier design, and the phase proceeds (or reshapes) on evidence instead of LOW-confidence hope.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-02-SUMMARY.md with the SCORE line, misroute table, verdict, and — on FAIL — the composition decision and its ROADMAP flag.</output>
