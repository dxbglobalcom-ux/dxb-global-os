---
phase: 06-memory-router-knowledge-stores
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - db/migrations/20260709000010_memory_embeddings.sql
  - packages/shared/src/db-types.ts
  - packages/shared/src/litellm.ts
  - packages/memory-router/src/adapters/pgvector.ts
  - packages/memory-router/src/index.ts
  - packages/memory-router/package.json
  - tests/phase6/pgvector.test.ts
autonomous: true
requirements: [MEM-01]
must_haves:
  truths:
    - "memory_embeddings exists behind memory_index (FK index_id), with an hnsw cosine index — created by migration, surviving `supabase db reset`"
    - "The vector dimension in the migration equals the pinned dimension from the 06-01 litellm card — one source, one place to change (master §5)"
    - "All embedding calls go through shared/litellm.ts (new llmEmbed) with the dxb-os department key — no second proxy surface, spend stays in the LiteLLM ledger"
    - "The pgvector adapter can round-trip: embed body → insert row → cosine-query returns that row first (live test against local Supabase)"
  artifacts:
    - "db/migrations/20260709000010_memory_embeddings.sql (vector ext + table + hnsw index)"
    - "packages/shared/src/litellm.ts llmEmbed() (single proxy surface preserved)"
    - "packages/memory-router/src/adapters/pgvector.ts (write + cosineSearch)"
    - "tests/phase6/pgvector.test.ts (live round-trip, tracked-id cleanup)"
  key_links:
    - "memory_embeddings.index_id <-> memory_index.id (every embedding row belongs to an index row — write path enforces order)"
    - "vector(N) <-> litellm.md Embeddings section dimension"
    - "adapters/pgvector.ts cosineSearch <-> 06-04 contradiction check (cosine >=0.85 candidate lookup) and 06-05 recall"
---

<objective>
Master step 3: the pgvector store becomes real — migration, typed table, the ONE llmEmbed surface, and the pgvector adapter with a live insert+cosine proof. This is the write path's semantic backbone: 06-04's contradiction check and 06-05's recall both stand on cosineSearch built here.
[ADAPT — record in SUMMARY]: master plan names the file `0009_memory_embeddings.sql`, but slot 0009 was consumed by `20260708000009_routing_and_deps.sql` (Phase 5). Content is byte-faithful to master §3; the filename advances to timestamp slot 10. Numbering-only deviation.
[LOCKED-interpretation — record in SUMMARY, CEO-visible]: the LOCKED table cites the "Supabase automatic-embeddings kalıbı" (trigger+queue, async). Write-policy rule 2 requires the embedding BEFORE the insert decision (contradiction check gates the write), so embedding is computed synchronously inside commitMemory; async trigger-embedding would break the LOCKED rule order 1→2→4. The kalıp's intent — embedding inside existing machinery, spend in the ledger — is preserved (commitMemory runs in worker context; llmEmbed via LiteLLM). Documented adaptation, not silent deviation.
Opus may execute per master §6 (interface and SQL are verbatim below).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-06.md   (§3 SQL core; §5 embedding-model fallback)
@.planning/research/study-cards/litellm.md   (Embeddings section from 06-01: model alias + DIMENSION)
@db/migrations/20260707000005_memory_index.sql   (parent table being referenced)
</context>

## Artifacts this phase produces

**This plan:** migration `20260709000010_memory_embeddings.sql`; `MemoryEmbeddingsTable` in db-types; `llmEmbed()` + `LlmEmbedArgs/Result` in shared/litellm.ts; `packages/memory-router/src/adapters/pgvector.ts` exporting `writeEmbedding()` + `cosineSearch()`; `tests/phase6/pgvector.test.ts`.
**Phase-wide (context):** see 06-01 list.

<tasks>

<task type="auto">
  <name>Task 1: migration + db-types</name>
  <files>db/migrations/20260709000010_memory_embeddings.sql, packages/shared/src/db-types.ts</files>
  <read_first>
    - .planning/research/study-cards/litellm.md (PINNED dimension N — replaces 1536 below if different)
    - db/migrations/20260708000009_routing_and_deps.sql (house migration style: comments, grants)
    - packages/shared/src/db-types.ts (table interface + DB registration pattern, MemoryIndexTable at ~144)
  </read_first>
  <action>
    Migration content (master §3 verbatim, dimension from the card):
    ```sql
    CREATE EXTENSION IF NOT EXISTS vector;
    CREATE TABLE memory_embeddings (
      index_id  uuid PRIMARY KEY REFERENCES memory_index(id),
      body      text NOT NULL,
      embedding vector(1536)  -- dimension pinned by study-cards/litellm.md Embeddings section (06-01)
    );
    CREATE INDEX ON memory_embeddings USING hnsw (embedding vector_cosine_ops);
    ```
    Match the house style for any role grants the sibling migrations apply to app-facing tables. db-types.ts: add `MemoryEmbeddingsTable { index_id: string; body: string; embedding: string | null }` (pgvector transports as string through kysely/pg) and register `memory_embeddings` in DB. Run `supabase db reset` to prove the chain replays — then re-apply the documented seed order (db/README: import-personas, apply-persona-v2, import-routing-rules) so the memory.classify row from 06-02 is back before any later verify depends on it.
  </action>
  <verify><automated>supabase db reset --no-seed >/dev/null 2>&1 || supabase db reset >/dev/null; psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -t -A -c "SELECT count(*) FROM pg_indexes WHERE tablename='memory_embeddings' AND indexdef ILIKE '%hnsw%'" | grep -qx 1 && echo MIGRATION_OK</automated></verify>
  <acceptance_criteria>
    - Reset replays clean; memory_embeddings exists with hnsw cosine index (SQL evidence)
    - FK to memory_index enforced (insert with random uuid fails — negative proof in test file, Task 3)
    - Dimension literal equals the card's N (grep both, paste pair in SUMMARY)
  </acceptance_criteria>
  <done>Semantic store schema is live and reset-survivable.</done>
</task>

<task type="auto">
  <name>Task 2: llmEmbed in shared/litellm.ts (single proxy surface)</name>
  <files>packages/shared/src/litellm.ts</files>
  <read_first>
    - packages/shared/src/litellm.ts (proxyFetch + llmCall pattern + LOCKED header comment: no cost_ledger writes here)
  </read_first>
  <action>
    Add `LlmEmbedArgs { department: string; model: string; input: string | string[] }` and `llmEmbed(args): Promise<{ vectors: number[][]; model: string; usage: { prompt_tokens: number } }>` hitting `/embeddings` via proxyFetch with departmentKey(args.department) — same shape discipline as llmCall (defensive parse of data[].embedding, loud LiteLLMError pass-through). No cost writes (LOCKED single-source cost rule stands). Callers pass the model alias read from the litellm card via their own config — llmEmbed itself stays model-agnostic.
  </action>
  <verify><automated>pnpm --filter @dxb/shared build && node -e "const m=require('./packages/shared/dist/index.js'); if(typeof m.llmEmbed!=='function') throw 'llmEmbed missing'; console.log('EMBED_SURFACE_OK')"</automated></verify>
  <acceptance_criteria>
    - llmEmbed exported from @dxb/shared; no fetch to LiteLLM outside litellm.ts (grep: `/embeddings` appears only there)
    - No cost_ledger reference added (header rule intact)
  </acceptance_criteria>
  <done>Embedding spend flows through the one audited surface.</done>
</task>

<task type="auto">
  <name>Task 3: pgvector adapter + live round-trip test</name>
  <files>packages/memory-router/src/adapters/pgvector.ts, packages/memory-router/src/index.ts, packages/memory-router/package.json, tests/phase6/pgvector.test.ts</files>
  <read_first>
    - packages/shared/src/db.ts (getDb/closeDb — session-mode 54322 constraint)
    - tests/phase5/decompose-dispatch.test.ts (house test pattern: env default URL, tracked ids, sequential cleanup)
    - packages/outbox-executor/package.json (workspace dep declaration pattern)
  </read_first>
  <action>
    adapters/pgvector.ts: `writeEmbedding(trx, { indexId, body, vector })` inserting via kysely `sql` fragment (`embedding = ${'[v1,v2,...]'}::vector`), and `cosineSearch(db, { vector, limit, trustTier?, kind? })` returning `{ index_id, body, distance }` ordered by `embedding <=> query` joined to memory_index (filters: superseded_by IS NULL, expires_at NULL-or-future, trust_tier when given — the same WHERE contract 06-05 recall reuses; export the filter builder so it is written ONCE). memory-router package.json gains @dxb/shared workspace dep + kysely (match sibling versions). index.ts re-exports the adapter.
    tests/phase6/pgvector.test.ts (live, no LLM — hand vectors): insert memory_index row + embedding, cosine-query with a near vector → that row first; FK negative (embedding insert with unknown index_id rejects); quarantined row excluded when trustTier='trusted'. Tracked-id cleanup, env URL default 54322.
  </action>
  <verify><automated>pnpm --filter @dxb/memory-router build && pnpm vitest run tests/phase6/pgvector.test.ts 2>&1 | tail -5 | grep -E "passed" </automated></verify>
  <acceptance_criteria>
    - Round-trip test green against local Supabase (output pasted in SUMMARY)
    - FK negative and trusted-filter negative both asserted (not just happy path)
    - Filter builder exported (one WHERE contract for 06-04/06-05)
  </acceptance_criteria>
  <done>The semantic store answers queries with the exact trust/lifecycle filters the router will rely on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| body text → embedding call | memory content leaves the DB toward the proxy/provider |
| query vector → SQL | vector literal interpolation into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | SQL injection via crafted body/vector string | high | mitigate | kysely parameterization; vector literal built from Number()-validated floats only |
| T-06-08 | Info disclosure | quarantined/superseded content surfacing through cosineSearch | high | mitigate | shared filter builder defaults to trusted + live rows; tests assert exclusion |
| T-06-09 | DoS/cost | unbounded embedding batches burning budget | medium | mitigate | llmEmbed input length check; adapter embeds one body per index row; dxb-os key is budget-capped (06-01) |
</threat_model>

<verification>
- Reset-replayed migration with hnsw evidence; llmEmbed exported, `/embeddings` confined to litellm.ts; live round-trip + two negative proofs green.
</verification>

<success_criteria>Master step 3 satisfied: embedding insert + cosine query provably work on a reset-fresh schema, through the audited proxy surface, with the trust filters recall will inherit.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-03-SUMMARY.md with reset+index evidence, dimension pair (card ↔ migration), and test tail. Record the 0009→0010 filename [ADAPT].</output>
