---
phase: 06-memory-router-knowledge-stores
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - packages/memory-router/src/classify-read.ts
  - packages/memory-router/src/index.ts
  - packages/dxb-mcp/src/groups/memory.ts
  - tests/phase6/poisoning.test.ts
  - tests/phase6/recall.test.ts
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "memory.recall routes by memory_index metadata + the spike-validated cheap classifier — the router NEVER queries stores by guess (LOCKED); kind given → classifier skipped entirely"
    - "trust='trusted' is the default and quarantined rows NEVER appear in it; trust='include-quarantined' returns them ONLY with an audit row recording the request (LOCKED: quarantined content never enters gated decision context by default)"
    - "The poisoning scenario is closed end-to-end: the 06-04 poisoned row is absent from default recall output and absent from a worker-style context assembly built on recall (master step 7)"
    - "The classifier prompt is the spike's prompt verbatim (06-02 lifted, not re-invented) and its model/mode come from routing_rules memory.classify at call time — fail-closed if the row is missing"
  artifacts:
    - "packages/memory-router/src/classify-read.ts (classifyQuery + recallMemory)"
    - "memory_recall MCP tool (RecallInput verbatim)"
    - "tests/phase6/recall.test.ts + poisoning.test.ts recall half"
  key_links:
    - "recallMemory store dispatch <-> memory_index metadata filters + adapters (pgvector live; obsidian by-ref read; graphify/notebook via 06-06)"
    - "include-quarantined audit <-> audit_log action 'quarantined_recall'"
    - "classify prompt <-> tests/phase6/routing-spike.mjs header (verbatim-lift contract)"
---

<objective>
Master steps 5 + 7: the read path. classify-read.ts picks the store (metadata first, spike-validated cheap model when kind is absent), recallMemory applies the LOCKED trust filters, and memory_recall exposes it to agents. The poisoning test closes: what 06-04 quarantined provably cannot reach a gated task's context through default recall.
Opus may execute per master §6; trust-filter semantics are LOCKED — any relaxation is ⛔ FABLE-ONLY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-06.md   (§3 RecallInput verbatim; §2 LOCKED read-path + quarantine-context rules)
@.planning/master-plan/spikes/06-routing.md   (validated prompt + composition)
@tests/phase6/routing-spike.mjs   (the prompt block to lift verbatim)
</context>

## Artifacts this phase produces

**This plan:** `classifyQuery()` (routing_rules lookup + spike prompt) and `recallMemory()` (metadata filter → store adapter fan-in, trust default, limit clamp) in classify-read.ts; `memory_recall` MCP tool; recall + poisoning-recall tests.
**Phase-wide (context):** see 06-01 list.

<tasks>

<task type="auto">
  <name>Task 1: classify-read.ts — classifyQuery + recallMemory</name>
  <files>packages/memory-router/src/classify-read.ts, packages/memory-router/src/index.ts</files>
  <read_first>
    - tests/phase6/routing-spike.mjs (prompt block — lift VERBATIM per its header comment)
    - packages/memory-router/src/adapters/pgvector.ts (cosineSearch + shared filter builder from 06-03)
    - packages/memory-router/src/write-policy.ts (KIND_STORE map + RecallInput schema)
    - packages/kernel/src/policy.ts (fail-closed row-lookup discipline to mirror)
  </read_first>
  <action>
    classifyQuery(db, query): load routing_rules memory.classify row (enabled, priority order — fail CLOSED with a loud error if missing, mirroring kernel policy discipline); llmCall with the spike prompt verbatim (department 'os'); strict-JSON parse → { store, kind }; parse failure throws — no silent fallback store (misrouting is silent quality loss per LOCKED rationale).
    recallMemory(db, input: RecallInput): (1) kind given → store = KIND_STORE[kind], classifier skipped; else classifyQuery. (2) memory_index query via the 06-03 shared filter builder: store match, trust filter (trusted default; 'include-quarantined' widens to both tiers), superseded/expiry exclusion, limit clamp 1..20. (3) Body fetch per store: pgvector → cosineSearch with llmEmbed(query) vector; obsidian → read file at ref (existence-checked; missing file = loud error + audit 'memory_ref_broken'); graphify/notebook → adapter readByRef (StoreNotWiredError until 06-06 — recall over those stores stays loud, never silently empty). (4) trust='include-quarantined' → audit 'quarantined_recall' {query, returned_ids, caller} appended in the same call. Returns rows {id, kind, store, body, trust_tier, confidence, created_at}. Export both from package index.
  </action>
  <verify><automated>pnpm --filter @dxb/memory-router build && node -e "const m=require('./packages/memory-router/dist/index.js'); for(const s of ['classifyQuery','recallMemory']) if(typeof m[s]!=='function') throw s+' missing'; console.log('READ_OK')" && ! grep -qn "glm" packages/memory-router/src/classify-read.ts && echo NO_MODEL_LITERAL</automated></verify>
  <acceptance_criteria>
    - Prompt block in classify-read.ts is byte-identical to the spike's (diff proof in SUMMARY)
    - No model literal on the read path (grep clean); missing routing row throws (test-asserted)
    - Trust default excludes quarantined at the SQL layer (filter builder), not by post-filtering
  </acceptance_criteria>
  <done>Reads route on metadata + validated classification, with LOCKED trust semantics in one place.</done>
</task>

<task type="auto">
  <name>Task 2: memory_recall MCP tool — the agent read surface</name>
  <files>packages/dxb-mcp/src/groups/memory.ts</files>
  <read_first>
    - packages/dxb-mcp/src/groups/memory.ts (memory_commit registration from 06-04 — match shape)
    - packages/dxb-mcp/src/groups/queue.ts (ok() response + appendAudit conventions)
  </read_first>
  <action>
    Register `memory_recall` (inputSchema = RecallInput.shape) → recallMemory(); response ok() with rows + a `classifier_used: boolean` field (observability: did the cheap model fire). Agents now have exactly two memory tools: memory_recall + memory_commit (MEM-02's surface). Header comment: promote deliberately absent (LOCKED), stores never addressed directly by agents.
  </action>
  <verify><automated>pnpm --filter @dxb/dxb-mcp build && grep -c "registerTool" packages/dxb-mcp/src/groups/memory.ts | grep -qx 2 && echo SURFACE_2_TOOLS</automated></verify>
  <acceptance_criteria>
    - Exactly 2 registered memory tools (memory_recall, memory_commit); no store-direct or promote tool
    - RecallInput.shape used verbatim (trusted default visible in tool schema)
  </acceptance_criteria>
  <done>MEM-02's tool surface is complete: agents touch memory only through recall/commit.</done>
</task>

<task type="auto">
  <name>Task 3: recall tests + poisoning end-to-end close (master step 7)</name>
  <files>tests/phase6/recall.test.ts, tests/phase6/poisoning.test.ts</files>
  <read_first>
    - tests/phase6/poisoning.test.ts (06-04 write half — extend, keep its fixtures)
    - tests/phase5/ladder.test.ts (injection seams pattern; DXB_LIVE gating)
  </read_first>
  <action>
    recall.test.ts: (a) kind given → classifier NOT called (injected classifier spy asserts zero calls); (b) trusted default: seeded quarantined row absent, trusted row present; (c) include-quarantined: both returned + audit 'quarantined_recall' row exists with returned_ids; (d) superseded + expired rows excluded from both modes; (e) missing routing row (temporarily disabled in a transaction) → classifyQuery throws fail-closed; (f) contradiction follow-through from 06-04: after promote, recall for the fact returns "X=B" and NOT "X=A" (master step 6 retrieval clause closed).
    poisoning.test.ts recall half: default recall for a query matching the poisoned content returns zero poisoned ids; then a worker-style context assembly (mirror runWorkerOnce's prompt-build: envelope objective + recalled trusted memory concatenated) proven to NOT contain the poison marker string 'sk-FAKE-POISON'; include-quarantined path shows the row IS reachable explicitly and leaves the audit trail (the quarantine is a gate, not a black hole). Injected embedder/classifier keep it deterministic; one DXB_LIVE_SDK=1 live recall recorded for SUMMARY.
  </action>
  <verify><automated>pnpm vitest run tests/phase6/recall.test.ts tests/phase6/poisoning.test.ts 2>&1 | tail -5 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - All six recall behaviors asserted; fail-closed proven; X=B-wins retrieval proven
    - Poison marker absent from assembled worker context (string-level assertion) — gate criterion 1 read half closed
    - include-quarantined leaves 'quarantined_recall' audit evidence
  </acceptance_criteria>
  <done>Master steps 5+7 verification lines are green: trusted-by-default recall, audited quarantine access, poison provably outside gated context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recalled memory → agent prompt | memory bodies become prompt material for gated work |
| classifier output → store choice | cheap-model text steers retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14 | Info disclosure | quarantined/poisoned content leaking into default recall | critical | mitigate | SQL-layer trust filter; string-level context assembly test; audit on explicit access |
| T-06-15 | Spoofing | prompt injection inside a TRUSTED memory body steering agents | high | accept-partial | trusted tier is provenance-gated (agent/ceo origin only) — residual risk documented; Phase-10 persona rules + approval gates remain the backstop; QA judge sees provenance |
| T-06-16 | DoS | classifier outage bricking all reads | medium | mitigate | kind-given path skips classifier entirely; failures are loud and typed so callers can retry with explicit kind |
</threat_model>

<verification>
- Build + export proof; 2-tool surface; recall behaviors (a)–(f) green; poisoning read half green with context-assembly string proof.
</verification>

<success_criteria>Master steps 5+7 satisfied: metadata-routed, trust-filtered recall is the only read door, with executed evidence that poison stays out of default context and explicit quarantine access is audited.</success_criteria>

<output>Create .planning/phases/06-memory-router-knowledge-stores/06-05-SUMMARY.md with test tails, the prompt byte-diff proof, and the live recall sample.</output>
