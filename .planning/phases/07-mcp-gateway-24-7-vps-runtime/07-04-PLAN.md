---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - tests/phase7/profile-denial-runtime.test.ts
  - vps/provision/cloud-init.yaml
  - vps/provision/harden.sh
  - vps/provision/Caddyfile
  - vps/provision/README.md
autonomous: false
requirements: [MCP-02, VPS-01]
must_haves:
  truths:
    - "A session loaded with the research profile cannot CALL Stripe: the tool resolves to 'tool not found', not merely 'hidden from a menu' (Pitfall 6 'looks done' — the runtime half of MCP-02)"
    - "The VPS is provisioned with SSH key-only auth, ufw default-deny inbound except 22/80/443, and fail2ban active — password auth is OFF (hardening is not optional)"
    - "The VPS credential set is SEPARATE from the laptop's — a distinct SSH key and a Hetzner-scoped API token, never the laptop's pair (blast-radius LOCKED decision)"
    - "Caddy answers https://<domain>/health with 200 over real TLS (auto-HTTPS), proving the reverse proxy + DNS + firewall path end to end"
  artifacts:
    - "tests/phase7/profile-denial-runtime.test.ts — loads a generated profile, resolves tools, asserts denied tool absent from the resolved set"
    - "vps/provision/{cloud-init.yaml,harden.sh,Caddyfile,README.md} — reproducible provisioning, no secrets in-file"
  key_links:
    - "profile-denial-runtime <-> generate-profiles.ts output (07-03) — same .mcp.json, now proven at resolution time"
    - "harden.sh ufw/fail2ban/sshd_config <-> VPS-01 hardening; Caddyfile <-> /health 200 gate"
---

<objective>
Master steps 4–5: the runtime denial proof, then the box that will run the stack. 07-03 proved by grep that the emitted profile OMITS Stripe; here a session actually LOADS that profile and `stripe.create_charge` resolves to "tool not found" — the model can never even select it (filter-before-discovery, the study-pass v1 model; STACK "MCP Gateway pattern"). Then the EU VPS is born hardened, on its own credential set, terminating TLS on `/health`. **Checkpoint (autonomous:false):** the Hetzner API token and the DNS record are CEO credential/identity steps — Claude prepares and presents, the CEO mints and points.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§4 step 4 "tool not found"; step 5 ssh-key-only/ufw/fail2ban/Caddy; LOCKED: VPS separate credential set = blast radius)
@.planning/research/STACK.md   (Hetzner CX42/CPX31-class 8GB EU; Caddy auto-HTTPS reverse proxy — simpler than Traefik/nginx for one box)
@.planning/phases/01-security-baseline   (hardening + secret-hygiene precedent; .env / secrets never printed)
</context>

## Artifacts this plan produces

The runtime denial test; `vps/provision/{cloud-init.yaml,harden.sh,Caddyfile,README.md}` (reproducible, no secrets in-file); a live hardened Hetzner box (⚠ live-VPS evidence, not terminal-local); a DNS A record (CEO); a Hetzner API token consumed then confirmed scoped (CEO mints, Claude uses, never committed).

<tasks>

<task type="auto">
  <name>Task 1: profile-denial runtime test — "tool not found" at resolution</name>
  <files>tests/phase7/profile-denial-runtime.test.ts</files>
  <read_first>
    - packages/gateway/src/generate-profiles.ts (07-03: the emitted .mcp.json shape this test loads)
    - tests/phase7/profile-denial.test.ts (07-03 static grep assertions — this is their runtime complement)
    - packages/mcps/ (how a DXB MCP server resolves a tool call — the "tool not found" path to assert against)
  </read_first>
  <action>
    Generate a research profile to a temp dir (reuse 07-03 `generateProfiles`). Then reproduce the resolution an MCP client performs: build the selectable tool set FROM that `.mcp.json`, and attempt to resolve `stripe.create_charge`. Assert the resolver returns not-found / throws the tool-not-found error — the tool must be invisible to SELECTION (filter-before-discovery), NOT called-then-rejected downstream. Positive control: a benign allowlisted tool resolves fine (so the profile isn't empty by accident). This is the machine-checkable proxy for the master step-4 line "research-profil'li session'da Stripe çağrısı tool not found"; a fuller live proof (spawning a real Claude Code session against the profile) is recorded as ⚠ UNVERIFIED in SUMMARY if not runnable in CI.
  </action>
  <verify><automated>pnpm vitest run tests/phase7/profile-denial-runtime.test.ts 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - Denied tool resolves to not-found from the generated profile (runtime half of MCP-02)
    - Benign allowlisted tool resolves (positive control)
    - Any gap between this proxy and a full live-session test is labeled ⚠ UNVERIFIED explicitly
  </acceptance_criteria>
  <done>MCP-02 is proven both ways: absent from the profile (07-03) and unresolvable at runtime (here).</done>
</task>

<task type="checkpoint">
  <name>Task 2: CEO credential gate — Hetzner token + DNS (identity-only)</name>
  <files>vps/provision/README.md</files>
  <read_first>
    - .planning/master-plan/PHASE-07.md (LOCKED: VPS separate credential set, not the laptop's twin — blast radius)
    - MEMORY.md CEO delegation rule (CEO does ONLY credential/identity steps)
  </read_first>
  <action>
    CHECKPOINT — CEO-owned, outward-facing, real spend (~€20/mo). Claude prepares and presents, the CEO acts:
    (a) the exact Hetzner Cloud project + API-token scope to mint — read/write, PROJECT-scoped, NOT account-global;
    (b) the server type/location to create — CX42/CPX31-class 8GB, EU (Nuremberg/Helsinki);
    (c) the DNS A record the CEO points at the box's IP after creation;
    (d) a fresh SSH keypair Claude GENERATES for the VPS only — public half to Hetzner, private half to the vault/gitignored `.env`, never committed, distinct from the laptop's keys.
    CEO action: mint the token → paste it into the untracked carrier `vps/provision/.hetzner.local` (gitignored); create the DNS record. Claude never asks the CEO to run provisioning commands — only the token + DNS identity steps. The whole flow is documented in README.md (names and scopes only, no secret values).
  </action>
  <verify><manual>CEO confirms: Hetzner token minted + placed in the gitignored carrier; DNS A record created; VPS-only SSH keypair generated (public-key fingerprint recorded in README, private key in vault only)</manual></verify>
  <acceptance_criteria>
    - Token scope is project-level, not account-global (blast radius) — README states the exact scope
    - VPS SSH key is distinct from the laptop's (fingerprints differ — evidence in README)
    - No secret value appears in any tracked file (gitleaks clean)
  </acceptance_criteria>
  <done>The credential/identity prerequisites exist — CEO-minted, scoped, and separate — so provisioning can run.</done>
</task>

<task type="auto">
  <name>Task 3: provision + harden + Caddy → TLS /health 200</name>
  <files>vps/provision/cloud-init.yaml, vps/provision/harden.sh, vps/provision/Caddyfile, vps/provision/README.md</files>
  <read_first>
    - vps/provision/README.md (the checkpoint output — token carrier + VPS key + DNS now exist)
    - .planning/research/STACK.md (Caddy auto-HTTPS; 8GB CX42/CPX31-class box)
  </read_first>
  <action>
    Author reproducible provisioning:
    - cloud-init.yaml — create the box with the VPS SSH key, a non-root sudo user, unattended-upgrades.
    - harden.sh — sshd_config: `PasswordAuthentication no`, `PermitRootLogin no`, key-only; ufw default deny incoming / allow 22,80,443; fail2ban with an sshd jail.
    - Caddyfile — reverse-proxy skeleton + a `/health` route returning 200, auto-HTTPS for the CEO's domain.
    Provision via `hcloud`, sourcing the token silently so it never prints: `set -a && . vps/provision/.hetzner.local && set +a`. Run harden.sh over ssh. Verify FROM THE LAPTOP: key-only ssh login succeeds, password login is refused; `curl https://<domain>/health` → 200. The stack deploy is 07-05 — this plan stops at a hardened, TLS-terminating EMPTY box. Terminal-observable checks (ssh exit, curl status) are ✓ VERIFIED; anything only visible in the Hetzner console is ⚠ UNVERIFIED.
  </action>
  <verify><manual>From laptop: `ssh dxb@<domain> true` exits 0 (key-only); ssh with a password is refused; `curl -s -o /dev/null -w '%{http_code}' https://<domain>/health` prints 200; on the box `sshd -T | grep -i passwordauth` → `passwordauthentication no`</manual></verify>
  <acceptance_criteria>
    - Key-only SSH works; `PasswordAuthentication no` confirmed (`sshd -T | grep passwordauth` → no)
    - ufw active, default-deny inbound except 22/80/443; fail2ban sshd jail active
    - `curl https://<domain>/health` → 200 over valid TLS (✓ VERIFIED from laptop; box-internal state ⚠ UNVERIFIED where not terminal-checkable)
  </acceptance_criteria>
  <done>The EU VPS exists — hardened, on its own keys, answering TLS — ready for the stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop → Hetzner API | a token that can create/destroy paid infra |
| public internet → VPS | the box is internet-exposed from birth |
| VPS credential set → laptop credential set | must stay disjoint (blast radius) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Elevation | account-global Hetzner token leaking → all infra | high | mitigate | project-scoped token; gitignored carrier; never committed; CEO-minted |
| T-07-12 | Spoofing | password SSH brute-force on a fresh box | high | mitigate | key-only auth (PasswordAuthentication no) + fail2ban + ufw from first boot (cloud-init) |
| T-07-13 | Elevation | shared laptop/VPS key → one compromise takes both | high | mitigate | VPS-only keypair, distinct fingerprint (LOCKED separate-credentials decision) |
| T-07-14 | Info disclosure | Hetzner token printed in logs/plan output | high | mitigate | silent `set -a` source pattern; env NAMES only in tracked files; gitleaks pre-commit |
</threat_model>

<verification>
- profile-denial-runtime test green (denied tool not-found + positive control); sshd key-only confirmed; ufw + fail2ban active; `curl https://<domain>/health` → 200. Two-tier reporting: terminal-observable = ✓ VERIFIED; Hetzner-console-only = ⚠ UNVERIFIED.
</verification>

<success_criteria>Master steps 4–5 satisfied: the runtime denial is proven, and the EU VPS is provisioned, hardened, separately-credentialed, and TLS-serving `/health`.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-04-SUMMARY.md with the runtime test evidence, the hardening command outputs, the curl 200, and a two-tier ✓/⚠ split for every VPS claim.</output>
