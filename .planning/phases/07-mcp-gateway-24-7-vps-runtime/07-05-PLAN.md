---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - vps/compose.yaml
  - vps/systemd/dxb-stack.service
  - vps/.env.example
  - vps/backup/pg_dump.sh
  - vps/README.md
autonomous: false
requirements: [VPS-01]
must_haves:
  truths:
    - "The core compose profile (Postgres/Supabase, LiteLLM, dxb-mcp, workers, outbox) runs on the 8GB VPS with measured peak RAM inside the master's budget table (≤7GB peak, 1GB headroom)"
    - "All migrations 0001..0010 are applied on the VPS Postgres — the VPS DB schema equals the local one (no drift)"
    - "After `reboot`, every health check goes green within 5 minutes with NO manual intervention (systemd enable proven — the anti-baby-sitting core value at the infra layer)"
    - "A daily pg_dump + off-site vault copy exists and a restore has been test-run at least once (VPS single-point-of-failure mitigation)"
  artifacts:
    - "vps/compose.yaml — core profile (+ voice/brain profiles declared for 07 later plans, not started here)"
    - "vps/systemd/dxb-stack.service — compose up on boot, enabled"
    - "vps/backup/pg_dump.sh — daily dump + off-site copy; restore procedure in README"
  key_links:
    - "compose.yaml service list <-> master RAM budget table (docker stats measured against it)"
    - "dxb-stack.service enable <-> reboot-test green-within-5min gate"
    - "migrations 0001..0010 <-> VPS Postgres (parity with local)"
---

<objective>
Master steps 6–7: the stack the company runs on lands on the box 07-04 hardened, and proves it self-heals. The `core` compose profile — Supabase (trimmed: Storage/Imgproxy off per the RAM table), LiteLLM, dxb-mcp, workers, outbox — comes up on the 8GB VPS; measured peak RAM must fit the master budget (≤7GB, 1GB headroom); a `reboot` must bring every health check green within 5 minutes with zero manual steps (systemd enable is the anti-baby-sitting core value at the infra layer). **Checkpoint (autonomous:false):** deploy touches the paid box and places real provider secrets into the LiteLLM container env.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§3 compose.yaml spec; RAM budget table ≤7GB/1GB headroom; step 7 reboot gate; daily pg_dump fallback; fallback order 1→2→3)
@.planning/research/STACK.md   (self-hosted Supabase trimmed; pg-boss session-mode DIRECT 5432 — NOT transaction pooling; LiteLLM in the same Postgres, dedicated schema; Compose v2 + Caddy; exact-tag image pins)
@vps/provision/README.md   (the hardened box + domain from 07-04)
</context>

## Artifacts this plan produces

`vps/compose.yaml` (core profile + voice/brain profiles declared but not started); `vps/systemd/dxb-stack.service`; `vps/.env.example` (env NAMES only); `vps/backup/pg_dump.sh`; a running core stack on the VPS (⚠ live-VPS evidence); a measured `docker stats` RAM table checked against the master budget.

<tasks>

<task type="auto">
  <name>Task 1: compose.yaml core profile + systemd unit + env template</name>
  <files>vps/compose.yaml, vps/systemd/dxb-stack.service, vps/.env.example</files>
  <read_first>
    - .planning/master-plan/PHASE-07.md (§3 compose services; RAM table service list; core/voice/brain profile split)
    - .planning/research/STACK.md (Version Compatibility: pg-boss session-mode direct 5432 not transaction pooling; LiteLLM dedicated schema; Broadcast trigger helper; Installation image list)
    - vps/open-notebook/compose.local.yml (06-01 local compose precedent: exact-tag pins, no moving tags, mem_limit pattern)
    - packages/outbox-executor (the outbox + workers services that must run as containers)
  </read_first>
  <action>
    Author vps/compose.yaml with compose profiles:
    - `core` — self-hosted Supabase trimmed (Postgres + Auth + Realtime + Studio + Kong; Storage/Imgproxy DISABLED per the RAM table); LiteLLM (`ghcr.io/berriai/litellm`, pinned) pointed at a dedicated schema in the same Postgres; dxb-mcp; workers; outbox.
    - `voice` — Speaches, on-demand — declared, NOT started here.
    - `brain` — open-notebook, hermes — declared for 07 later plans.
    Pin every image to an exact tag/digest (open-notebook card precedent; moving tags banned). Set `mem_limit` per the master budget table. Postgres exposes the session-mode DIRECT port for pg-boss (NOT transaction pooling — STACK compat rule). `vps/.env.example` lists every required env NAME; real provider keys live ONLY in the LiteLLM container env, never in agent configs (COST-02). `dxb-stack.service`: `docker compose --profile core up`, `Restart=always`, `WantedBy=multi-user.target`, `systemctl enable`. No secret value goes into any tracked file.
  </action>
  <verify><automated>docker compose -f vps/compose.yaml --profile core config >/dev/null && echo COMPOSE_VALID && grep -c "mem_limit" vps/compose.yaml && ! grep -iE "sk-|password:|token:" vps/compose.yaml</automated></verify>
  <acceptance_criteria>
    - `docker compose --profile core config` validates (COMPOSE_VALID)
    - Every image pinned to an exact tag/digest (no `:latest` / moving tags — grep proof)
    - `mem_limit` present on the RAM-budgeted services; no secret literal in the file
  </acceptance_criteria>
  <done>The stack is described as one auditable, RAM-bounded, secret-free compose file with a boot unit.</done>
</task>

<task type="checkpoint">
  <name>Task 2: deploy core stack to VPS + migrations + measure RAM</name>
  <files>vps/README.md</files>
  <read_first>
    - vps/provision/README.md (ssh + domain from 07-04)
    - db/migrations/ (0001..0010 — the full set to apply on the VPS)
    - .planning/master-plan/PHASE-07.md (RAM budget table — the measurement target; fallback order)
  </read_first>
  <action>
    CHECKPOINT — deploy to the paid box. rsync compose + env (secrets placed directly on the box, sourced silently, never printed) → `docker compose --profile core up -d` → apply migrations 0001..0010 against the VPS Postgres (session-mode) → wait healthy. Measure: `docker stats --no-stream` + `free -m`, recording each service's RSS against the master budget table. If peak exceeds ≤7GB, trigger the master fallback ORDER — (1) Speaches socket-activation (already on-demand), (2) open-notebook on-demand, (3) DB tier → Supabase Cloud is ⛔ FABLE + CEO (budget impact) — do NOT execute step 3 unprompted. Every VPS-runtime fact is ⚠ live-VPS evidence; the RAM numbers and health output are pasted verbatim into SUMMARY.
  </action>
  <verify><manual>On VPS: `docker compose --profile core ps` all healthy; migrations 0001..0010 applied (`\dt` shows tool_pins + memory_index etc.); `docker stats --no-stream` peak within the master RAM table (numbers pasted into SUMMARY)</manual></verify>
  <acceptance_criteria>
    - Core services healthy on the VPS; schema parity (0010 tool_pins present on the VPS)
    - Measured peak RAM ≤7GB (table pasted; if over, fallback step 1/2 applied and re-measured — step 3 NOT taken without ⛔ Fable + CEO)
    - No secret printed in any output (silent source pattern)
  </acceptance_criteria>
  <done>The company's core stack runs on the EU VPS within budget, schema-identical to local.</done>
</task>

<task type="auto">
  <name>Task 3: reboot self-heal test + daily backup/restore</name>
  <files>vps/backup/pg_dump.sh, vps/README.md</files>
  <read_first>
    - vps/systemd/dxb-stack.service (the enable that must survive reboot)
    - .planning/master-plan/PHASE-07.md (step 7 reboot gate; daily pg_dump + off-site + tested restore)
  </read_first>
  <action>
    pg_dump.sh: a daily dump of the VPS Postgres + copy to an off-site vault target (destination from env, not hardcoded) + retention prune; wire a systemd timer OR document the cron. Then the reboot test: `sudo reboot` → poll health for ≤5 minutes → assert all core health checks green with ZERO manual steps (systemd enable is the proof). Then a restore drill: restore the latest dump into a scratch DB/namespace and verify a known row (single-point-of-failure mitigation — restore must be TESTED, not assumed). Record wall-clock-to-green and the restored-row proof. All ⚠ live-VPS evidence, pasted verbatim into SUMMARY.
  </action>
  <verify><manual>`sudo reboot`, then within 5 min `docker compose --profile core ps` all healthy (no manual intervention); pg_dump.sh produces a dump + off-site copy; the restore drill recovers a known row into a scratch target</manual></verify>
  <acceptance_criteria>
    - Post-reboot all-green within 5 minutes, zero manual steps (time-to-green recorded)
    - Daily dump + off-site copy works; the restore drill recovers a verifiable row
    - Reboot + restore evidence pasted verbatim (⚠ live-VPS)
  </acceptance_criteria>
  <done>Master step 7 holds: the box self-heals on reboot and its data is recoverable, tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secrets → LiteLLM container env | real provider keys live only here; nowhere else on the box |
| compose file → git | must never carry a secret literal |
| VPS data → off-site vault | backups leave the box; the destination must be trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-15 | Info disclosure | provider keys in compose/env in git | high | mitigate | keys only in the LiteLLM container env on the box; .env.example names-only; gitleaks |
| T-07-16 | DoS | RAM overrun OOM-killing Postgres | high | mitigate | mem_limit per budget; measured deploy; documented fallback order |
| T-07-17 | Availability | reboot leaves the stack down (no auto-start) | high | mitigate | systemd enable + Restart=always; the reboot test gates the plan |
| T-07-18 | Info disclosure | backup dump readable off-site | medium | mitigate | vault destination access-controlled; dumps not world-readable; restore drill on a scratch target only |
</threat_model>

<verification>
- COMPOSE_VALID + exact-tag proof + no-secret grep; VPS core healthy + schema parity + RAM table ≤7GB; reboot all-green ≤5min; backup + restore drill. VPS-runtime claims two-tier ⚠, local compose checks ✓.
</verification>

<success_criteria>Master steps 6–7 satisfied: the core stack runs on the EU VPS within the RAM budget, schema-identical to local, and self-heals on reboot with tested backups.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-05-SUMMARY.md with the compose validation, the measured RAM table vs budget, the reboot time-to-green, and the restore-drill evidence — VPS facts flagged ⚠ live-VPS.</output>
