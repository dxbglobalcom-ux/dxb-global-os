---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 06
type: execute
wave: 5
depends_on: [01, 05]
files_modified:
  - vps/hermes/config
  - vps/hermes/jobs/social-morning-scan.md
  - vps/systemd/hermes.service
  - vps/systemd/watchdog.service
  - vps/systemd/watchdog.timer
  - vps/watchdog/watchdog.sh
  - packages/cli/src/kill-switch.ts
  - tests/phase7/watchdog.test.ts
autonomous: false
requirements: [VPS-02]
must_haves:
  truths:
    - "Hermes NEVER runs a perpetual loop: every job is cron-triggered with a mandatory budget block {max_steps, max_tokens, max_cost_eur} and a mandatory declared artifact — a job file missing either field is REJECTED at load (Pitfall 9, LOCKED)"
    - "Hermes's GLM 5.2 brain calls route through the local LiteLLM proxy on its own department virtual key — its spend lands in the ledger like every other agent's (COST-02; no raw provider key anywhere near hermes)"
    - "The watchdog (systemd timer, 5min) kills any hermes job that is over budget OR artifactless past 2 hours, writes an audit row, and adds an anomaly line to the morning report"
    - "`dxb kill-switch on` → budget_state.hard_stopped + ALL LiteLLM virtual keys blocked + hermes systemd unit stopped — one command halts every autonomous spender"
    - "Overnight job output lands in the morning review queue — NOTHING flows outward directly (on_output: queue_review; the Phase-4 approval rails stay in front of every outward action)"
  artifacts:
    - "vps/hermes/jobs/*.md — bounded job definitions with the master's mandatory fields"
    - "vps/watchdog/watchdog.sh + systemd watchdog.{service,timer} — 5min cadence"
    - "packages/cli kill-switch subcommand — on/off with executed evidence"
  key_links:
    - "job template fields <-> master PHASE-07 §3 hermes şablonu (verbatim mandatory fields)"
    - "kill-switch <-> budget_state.hard_stopped (Phase 4 cost rails) + LiteLLM /key/block + systemctl stop hermes"
    - "queue_review output <-> Phase-4 review/approval queue tables (morning queue, step 9)"
---

<objective>
Master steps 8–9: the 24/7 resident goes live — caged. Hermes (GLM 5.2 via LiteLLM) runs ONLY cron-triggered, budget-boxed, artifact-mandatory jobs; a 5-minute watchdog kills runaways; `dxb kill-switch on` halts everything; and the first overnight job's output lands in the morning review queue with zero outward side effects. **Checkpoint (autonomous:false):** live deploy on the paid box + a new department virtual key.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§3 hermes job template verbatim fields; watchdog spec; kill-switch LOCKED decision; Pitfall 9)
@.planning/research/study-cards/hermes-agent.md   (07-01 fill: version pin, config surface, LiteLLM base-URL wiring, systemd shape)
@.planning/research/STACK.md   (hermes via LiteLLM rule — resident agent spend inside the budget envelope)
@vps/README.md   (the running core stack from 07-05)
</context>

## Artifacts this plan produces

`vps/hermes/config` (LiteLLM base-URL + virtual-key env name, version-pinned per card); `vps/hermes/jobs/social-morning-scan.md` (the master's template job, verbatim mandatory fields); `hermes.service` + `watchdog.{service,timer}` + `watchdog.sh`; `dxb kill-switch` CLI; `tests/phase7/watchdog.test.ts`; a new LiteLLM virtual key for the hermes department (alias recorded, value never committed).

<tasks>

<task type="auto">
  <name>Task 1: bounded job loader + template job + hermes config</name>
  <files>vps/hermes/config, vps/hermes/jobs/social-morning-scan.md, vps/systemd/hermes.service</files>
  <read_first>
    - .planning/research/study-cards/hermes-agent.md (job mechanism + config surface — the card is the contract)
    - .planning/master-plan/PHASE-07.md (§3 job template: job, schedule, budget{max_steps,max_tokens,max_cost_eur}, artifact, on_output — ALL mandatory)
    - packages/shared (LiteLLM env-key naming convention DXB_LITELLM_KEY_<DEPT> — hermes gets its own)
  </read_first>
  <action>
    Write the job template exactly as the master specifies: social-morning-scan.md with schedule "0 6 * * *", budget {max_steps: 30, max_tokens: 150000, max_cost_eur: 0.50}, artifact "reports/social-scan-{date}.md", on_output: queue_review. Job-load validation: a loader shim (in hermes config or a wrapper script per the card's mechanism) REJECTS any job file missing budget or artifact fields — rejection is loud (log + audit), not silent skip. hermes/config points the OpenAI-compatible endpoint at the local LiteLLM proxy with env name DXB_LITELLM_KEY_HERMES (key minted at deploy task; value never in file). hermes.service: version-pinned binary per card, After=dxb-stack.service, Restart=on-failure, EnvironmentFile the box-local env.
  </action>
  <verify><automated>grep -E "max_steps|max_tokens|max_cost_eur" vps/hermes/jobs/social-morning-scan.md && grep -c "queue_review" vps/hermes/jobs/social-morning-scan.md && ! grep -rE "sk-|Bearer " vps/hermes/</automated></verify>
  <acceptance_criteria>
    - Template job carries every mandatory field verbatim from the master
    - A fixture job missing `artifact` is rejected at load (evidence in test or loader log)
    - No secret literal under vps/hermes/ (env names only)
  </acceptance_criteria>
  <done>Hermes can only ever run jobs that are budget-boxed, artifact-mandatory, and review-routed.</done>
</task>

<task type="auto">
  <name>Task 2: watchdog + kill switch — code and tests</name>
  <files>vps/watchdog/watchdog.sh, vps/systemd/watchdog.service, vps/systemd/watchdog.timer, packages/cli/src/kill-switch.ts, tests/phase7/watchdog.test.ts</files>
  <read_first>
    - db/migrations/ Phase-4 cost-rail tables (budget_state / cost ledger — exact names for hard_stopped; NO GUESSING)
    - packages/cli/src/ (existing dxb CLI subcommand pattern — `dxb promote` precedent from 06-04)
    - .planning/master-plan/PHASE-07.md (watchdog: 5min timer; kill over-budget OR artifactless >2h; audit + morning anomaly line)
  </read_first>
  <action>
    watchdog.sh: every 5 minutes (systemd timer) inspect running hermes jobs — a job over any budget field OR running >2h without its declared artifact on disk → kill the job process, write audit row (actor 'watchdog', action 'hermes_job_killed', payload {job, reason, budget_snapshot}), append an anomaly line to the morning report artifact. `dxb kill-switch on|off|status` (kill-switch.ts, wired into the existing dxb CLI): `on` = set budget_state hard-stop flag + block every LiteLLM virtual key via the proxy admin API (loop over key aliases; admin credentials from box-local env, sourced silently) + `systemctl stop hermes`; `off` = explicit reverse with audit; both paths write audit rows. Reachability note in code comment: phone/dashboard surface arrives Phase 8 — until then SSH + CLI is the access path. tests/phase7/watchdog.test.ts (local, deterministic): budget-exceeded fixture job → kill decision + audit row; artifactless-2h fixture → kill decision; healthy job → untouched; kill-switch state transitions write audit + flip the flag (LiteLLM block + systemctl mocked via deps seam — live halves proven in Task 3).
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase7/watchdog.test.ts 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - Over-budget and artifactless-2h fixtures both produce kill decision + audit row; healthy job untouched
    - kill-switch on/off flips the hard-stop flag with audit rows both directions (deterministic halves)
    - Full suite regression green
  </acceptance_criteria>
  <done>The cage's enforcement layer exists and its decision logic is proven deterministically.</done>
</task>

<task type="checkpoint">
  <name>Task 3: live deploy — hermes key, runaway kill, kill-switch e2e, night job → morning queue</name>
  <files>vps/README.md</files>
  <read_first>
    - vps/README.md (deploy path from 07-05)
    - .planning/master-plan/PHASE-07.md (step 8 verification: fake over-budget job killed; kill-switch blocks keys + stops hermes; step 9: 06:00 output in review queue, no outward effect)
  </read_first>
  <action>
    CHECKPOINT — live on the paid box. Mint the hermes department LiteLLM virtual key (alias dxb-hermes; value into box-local env only). Deploy hermes (brain profile) + watchdog units; enable timers. Evidence run 1 — runaway: plant a fake over-budget job → watchdog kills it within one 5-min tick; audit row + anomaly line pasted. Evidence run 2 — kill switch: `dxb kill-switch on` → assert a probe call on a virtual key is REFUSED by LiteLLM, `systemctl is-active hermes` = inactive, hard-stop flag set; then `off` restores (all outputs pasted). Evidence run 3 — night job: trigger social-morning-scan (manual trigger acceptable as the schedule proof is separate) → artifact written → queue_review row in the Phase-4 review queue → assert NO outbox/outward row was created. All ⚠ live-VPS evidence, verbatim in SUMMARY; the genuinely-unattended 06:00 firing is ⚠ UNVERIFIED until the first real morning and recorded as such.
  </action>
  <verify><manual>Runaway job killed (audit row pasted); kill-switch on: key probe refused + hermes inactive + flag set, then off restores; night-job artifact + review-queue row present, outward-effect tables empty for the run</manual></verify>
  <acceptance_criteria>
    - Fake over-budget job killed by watchdog (master step-8 verification, executed)
    - Kill-switch e2e: keys blocked + hermes stopped + audited, both directions
    - Job output in morning review queue with zero outward side effects; first unattended 06:00 firing labeled ⚠ UNVERIFIED
  </acceptance_criteria>
  <done>The resident agent runs caged: budget-boxed, watchdogged, kill-switchable, and review-gated — VPS-02's mechanism live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hermes (autonomous, upstream code) → company resources | a 24/7 third-party agent with an LLM brain |
| watchdog → running jobs | unattended killer with audit duty |
| kill-switch → all spenders | the last-resort control must itself be trustworthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | DoS | hermes runaway loop burning budget overnight | high | mitigate | mandatory budget fields + watchdog kill + LiteLLM hard-stop (three independent layers) |
| T-07-20 | Elevation | hermes acting outward directly (post/send) | high | mitigate | on_output queue_review only; no outward MCP tools in its profile (07-03 denials); Phase-4 rails in front |
| T-07-21 | Info disclosure | raw provider key reachable by hermes | high | mitigate | LiteLLM virtual key only (DXB_LITELLM_KEY_HERMES); real keys never leave the LiteLLM container |
| T-07-22 | Repudiation | watchdog kills / kill-switch flips without trail | medium | mitigate | audit rows on every kill and every switch transition, both directions |
| T-07-23 | DoS | kill-switch fails half-way (keys blocked, hermes alive) | medium | mitigate | e2e test asserts all three effects; `status` subcommand reports each effect independently |
</threat_model>

<verification>
- Watchdog test green (kill decisions + audit); live runaway killed; kill-switch e2e all three effects both directions; night-job artifact + review-queue row + zero outward rows. Unattended 06:00 firing ⚠ UNVERIFIED until first real morning.
</verification>

<success_criteria>Master steps 8–9 satisfied: hermes runs bounded scheduled jobs under watchdog and kill switch, overnight output reaches the morning review queue, and nothing escapes outward — VPS-02 proven at the mechanism level.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-06-SUMMARY.md with the three live evidence runs (verbatim), the deterministic test line, the key-alias record, and the ⚠ UNVERIFIED first-morning note.</output>
