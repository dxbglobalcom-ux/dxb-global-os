---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 07
type: execute
wave: 5
depends_on: [01, 05]
files_modified:
  - tools/video-learn/src/ingest.ts
  - tools/video-learn/package.json
  - tools/video-learn/tsconfig.json
  - db/seeds/routing-seed.json
  - tests/phase7/video-ingest.test.ts
autonomous: true
requirements: [VID-01]
must_haves:
  truths:
    - "A video link becomes memory ASYNC: download → transcript (Speaches STT) → summary → commitMemory with provenance origin 'video' — and the row lands QUARANTINED (write-policy trust tiers apply; video content is untrusted external input)"
    - "The summary model is selected via a routing_rules row, NOT a code literal (house data-driven routing rule; master names a sonnet-class summarizer — the seed row records it)"
    - "Speaches runs ON-DEMAND: the voice profile comes up for the STT call and is stopped after; its under-load RAM is measured once and recorded against the master table (1–1.5GB)"
    - "The pipeline writes through the single door (commitMemory) — no direct store/adapter writes (06-04 LOCKED single-write-door)"
  artifacts:
    - "tools/video-learn/src/ingest.ts — yt-dlp → Speaches → summarize → commitMemory, each stage async + resumable-safe"
    - "routing-seed.json row `video.summarize` (sonnet-class, per master)"
    - "tests/phase7/video-ingest.test.ts — deterministic pipeline + live-gated e2e"
  key_links:
    - "ingest.ts STT call <-> speaches.md card endpoints (07-01) and compose voice profile (07-05)"
    - "commitMemory(origin 'video') <-> write-policy quarantine rules (06-04) and liveMemoryFilter trust tiers (06-05)"
    - "routing-seed `video.summarize` <-> llmCall routing_rules lookup (no model literals in code)"
---

<objective>
Master step 10: VID-01. The CEO drops a video link; the system downloads it (yt-dlp), transcribes it (Speaches STT, voice profile on-demand), summarizes it (routing_rules-selected sonnet-class model), and files it through the memory door with origin 'video' — quarantined, because video content is untrusted external input until promoted. Fully async; no CEO babysitting.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§3 ingest.ts spec: yt-dlp → Speaches STT → özet (sonnet) → memory.commit(origin='video'))
@.planning/research/study-cards/yt-dlp-video-use.md   (07-01 fill: pinned version + exact ingest invocation)
@.planning/research/study-cards/speaches.md   (07-01: STT endpoint contract, on-demand profile)
@packages/memory-router/src/write-policy.ts   (LOCKED rules — quarantine tier for non-CEO origins)
</context>

## Artifacts this plan produces

`tools/video-learn` package (ingest.ts + workspace wiring); routing-seed row `video.summarize`; `tests/phase7/video-ingest.test.ts`; one live ingest evidence run (YouTube link → quarantined memory row); Speaches under-load RAM measurement (master-table input).

<tasks>

<task type="auto">
  <name>Task 1: ingest.ts — the four-stage async pipeline through the door</name>
  <files>tools/video-learn/src/ingest.ts, tools/video-learn/package.json, tools/video-learn/tsconfig.json, db/seeds/routing-seed.json</files>
  <read_first>
    - .planning/research/study-cards/yt-dlp-video-use.md (the exact yt-dlp invocation — copy, don't improvise)
    - .planning/research/study-cards/speaches.md (STT endpoint + request shape)
    - packages/memory-router/src/write-policy.ts + commit door exports (provenance shape; how origin maps to trust tier — verify 'video' origin quarantines; if the policy enumerates origins, extend via its LOCKED-compatible path and record [ADAPT])
    - packages/shared llmCall + routing_rules usage (the summarize call pattern — 06-07 defaultExtractFacts precedent)
  </read_first>
  <action>
    `ingestVideo(url, deps)`: (1) yt-dlp audio extract (card invocation verbatim; execFile array-args, NO shell interpolation — URL is attacker-controllable input); (2) POST audio to Speaches `/v1/audio/transcriptions` (base URL env DXB_SPEACHES_URL); (3) summarize transcript via llmCall with routing_rules row `video.summarize` (seed a sonnet-class row in routing-seed.json per master; no model literal in code) under department attribution from the task; (4) commitMemory with {artifact or fact per content + provenance {origin: 'video', source: url}} — the write-policy tier must land it quarantined. Stages async with per-stage errors typed-loud (a dead Speaches container must fail the job, not fake a transcript — notebook adapter atomicity precedent). Deps seam: {download, stt, summarize, commit} injectable for deterministic tests. Package wired into workspace root build.
  </action>
  <verify><automated>pnpm build && node -e "import('./tools/video-learn/dist/ingest.js').then(m=>{if(!m.ingestVideo)throw new Error('no export');console.log('INGEST_EXPORT_OK')})" && grep -q "video.summarize" db/seeds/routing-seed.json</automated></verify>
  <acceptance_criteria>
    - ingest.ts has zero model literals (summary model from routing_rules) and zero shell-interpolated commands
    - commitMemory is the ONLY write path (grep: no adapter/store import in tools/video-learn)
    - Each stage failure is typed-loud; no silent skips
  </acceptance_criteria>
  <done>The pipeline exists: link → transcript → summary → quarantined memory, all through the door.</done>
</task>

<task type="auto">
  <name>Task 2: video-ingest tests — deterministic pipeline + live e2e</name>
  <files>tests/phase7/video-ingest.test.ts</files>
  <read_first>
    - tests/phase6/known-facts.test.ts (live-gating pattern DXB_LIVE_SDK=1; dept 'os' attribution precedent; cleanup discipline)
    - tests/phase6/adapters-roundtrip.test.ts (container health guard pattern — reuse for Speaches)
  </read_first>
  <action>
    Deterministic battery (seams injected): (1) happy path — fixture transcript in, assert commitMemory called with origin 'video' and the resulting row quarantined (recall with trusted-only mode excludes it; permissive mode finds it — 06-05 trust-tier assertions reused); (2) dead-STT — stt deps throws → ingest rejects, NO memory row (atomicity); (3) summarizer unparseable → rejects loud (anti-amnesia precedent T-06-21); (4) URL is passed as argv element, never concatenated (assert on the download deps call shape). LIVE e2e (DXB_LIVE_SDK=1, local Speaches or VPS voice profile): short real video → real yt-dlp + real STT + real routing_rules summarize → quarantined row with origin 'video'; measure Speaches RSS during STT (`docker stats --no-stream` while transcribing) and record vs the master 1–1.5GB expectation; stop the voice profile after (on-demand proof). Cleanup all rows/files.
  </action>
  <verify><automated>pnpm vitest run tests/phase7/video-ingest.test.ts 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - Deterministic: quarantine proven via both recall trust modes; atomicity + loud-failure negatives green
    - Live run (gate evidence for SUMMARY): real link → memory_index row origin 'video', quarantined; Speaches RAM measured + profile stopped after
    - Full suite regression green
  </acceptance_criteria>
  <done>Master step-10 verification executed: link → transcript → summary → quarantined memory, deterministically proven and live-demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arbitrary video URL → yt-dlp | attacker-controllable input reaching a subprocess |
| video content → memory store | untrusted third-party words entering company memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-24 | Tampering | prompt injection inside video transcript poisoning memory | high | mitigate | origin 'video' → QUARANTINED tier; gated decisions never cite quarantined rows (06-05); promotion is explicit human-path `dxb promote` |
| T-07-25 | Elevation | shell injection via crafted URL | high | mitigate | execFile array-args only; test asserts call shape; no string concatenation into commands |
| T-07-26 | DoS | huge video exhausting disk/RAM | medium | mitigate | yt-dlp audio-only extract + size cap flag per card; Speaches on-demand and stopped after; mem_limit on container |
| T-07-27 | Spoofing | fake transcript from a dead/failed STT | medium | mitigate | typed-loud stage failures; atomicity negative test (no row on failure) |
</threat_model>

<verification>
- INGEST_EXPORT_OK + seed grep; deterministic tests green (quarantine both trust modes, atomicity, loud failures, argv shape); live e2e row + Speaches RAM measured + profile stopped; regression green.
</verification>

<success_criteria>Master step 10 satisfied: a dropped video link becomes a quarantined, provenance-tagged memory through the single door, asynchronously, with the summarizer chosen by routing rules and Speaches proven on-demand within RAM expectations.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-07-SUMMARY.md with the live ingest evidence (row id, origin, tier), Speaches RAM line, and test outputs.</output>
