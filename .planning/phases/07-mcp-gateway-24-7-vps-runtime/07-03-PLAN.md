---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - packages/gateway/policy/denials.json
  - packages/gateway/src/generate-profiles.ts
  - packages/gateway/src/index.ts
  - packages/gateway/package.json
  - tests/phase7/profile-denial.test.ts
autonomous: true
requirements: [MCP-02]
must_haves:
  truths:
    - "Profiles are GENERATED from the departments/agents registry + denials.json — never hand-written (least-privilege cannot drift from the registry, LOCKED master decision)"
    - "A generated profile filters tools/list: the research profile contains NO Stripe/DocuSign; the CEO profile contains NO code/github MCP — verifiable by grep on the emitted JSON"
    - "A tool_pins row with quarantined=true is EXCLUDED from every generated profile (07-02 quarantine → 07-03 exclusion, the two halves of MCP-03 meet here)"
    - "denials.json mirrors the doc's per-department red tables exactly; widening it is a ⛔ FABLE-ONLY change"
  artifacts:
    - "packages/gateway/policy/denials.json — per-department deny map from the architecture doc"
    - "packages/gateway/src/generate-profiles.ts — registry+policy+pins → profiles/<dept>.mcp.json with a generated_at + source-hash header"
    - "profiles/<dept>.mcp.json emitted for at least research + ceo + one worker department"
  key_links:
    - "denials.json entries <-> ROADMAP MCP-02 per-department maps (worker: no Stripe/DocuSign; ceo: no code MCP)"
    - "generate-profiles quarantine filter <-> tool_pins.quarantined (07-02)"
    - "generated profile default-deny <-> 07-04 runtime 'tool not found' proof"
---

<objective>
Master step 3: least-privilege made mechanical. `denials.json` (doc's red tables) + `generate-profiles.ts` compiling per-department `.mcp.json` from the registry, policy, and the pin store — with quarantined tools excluded. This is the v1 gateway the study pass confirmed (commit b7dc785): filtering happens by what a session is even handed, before discovery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§3 file spec; LOCKED: generated not hand-written; denials widening = ⛔ Fable)
@.planning/research/study-cards/mcp-gateway-patterns.md   (v1 verdict; profile dot-notation granularity pattern adopted)
@.planning/research/STACK.md   (MCP Gateway pattern paragraph: filter tools/list before discovery, default-deny allowlist)
</context>

## Artifacts this phase produces

**This plan:** `policy/denials.json`; `generate-profiles.ts` exporting `generateProfiles(db, {denials, outDir})` returning per-dept manifests; emitted `profiles/*.mcp.json`; `tests/phase7/profile-denial.test.ts`.

<tasks>

<task type="auto">
  <name>Task 1: denials.json — the doc's per-department red tables</name>
  <files>packages/gateway/policy/denials.json</files>
  <read_first>
    - .planning/master-plan/PHASE-07.md (§3 denials.json line: "CEO: no-code-mcp; research: no-payment...")
    - .planning/bubbly-dazzling-goblet.md if present, else the architecture notes mirror in .planning/ (the authoritative per-department MCP maps — NO GUESSING: transcribe the doc's denials, do not invent)
    - db/migrations/ Phase-3 registry (canonical department slugs — denials key on real dept ids)
  </read_first>
  <action>
    Author denials.json: a map of department slug → explicit deny list (server or server.tool dot-notation, matching the study-pass-adopted granularity). Transcribe from the architecture doc's per-department MCP maps: at minimum ceo → deny code/github MCPs; research → deny payment MCPs (stripe, docusign); every worker department → deny stripe + docusign. Include a top-level schema comment field documenting that this file is the ONLY hand-edited least-privilege source and widening it is ⛔ FABLE-ONLY. If the doc's map is ambiguous for a department, record the assumption in SUMMARY as [ADAPT] for CEO confirmation — do not silently guess.
  </action>
  <verify><automated>node -e "const d=require('./packages/gateway/policy/denials.json'); if(!d.research||!d.ceo)throw new Error('missing dept'); console.log('DENIALS_OK', Object.keys(d).length)" </automated></verify>
  <acceptance_criteria>
    - denials.json parses; contains ceo (no code MCP) and research (no payment) entries at minimum
    - Every worker department entry denies stripe + docusign
    - The ⛔ FABLE-ONLY widening note is present in the file
  </acceptance_criteria>
  <done>The deny policy exists as data, transcribed from the doc, not from memory.</done>
</task>

<task type="auto">
  <name>Task 2: generate-profiles.ts — registry + policy + pins → profiles</name>
  <files>packages/gateway/src/generate-profiles.ts, packages/gateway/src/index.ts</files>
  <read_first>
    - packages/gateway/src/pin-check.ts (the inventory reader + tool_pins access this reuses)
    - db/migrations/ Phase-3 registry (departments, agents, and any per-dept tool grant tables — the allowlist source)
    - .mcp.json (repo root, if present — the emitted profile MUST be a valid .mcp.json Claude Code can load)
  </read_first>
  <action>
    `generateProfiles(db, {denials, outDir})`: for each department in the registry, start from the full known tool inventory, subtract (a) tools whose (server,tool) is denied in denials.json, (b) tools whose tool_pins.quarantined=true, (c) anything not in that department's registry allowlist (default-deny — absence = denied, not allowed). Emit `<outDir>/<dept>.mcp.json` as a valid MCP client config (mcpServers map, only surviving servers/tools) with a header block: `_generated_at`, `_source_hash` (sha256 of registry-rows + denials + pin-state that produced it — staleness detectable), `_generator: "packages/gateway/generate-profiles.ts"`. Return the per-dept manifest summary. Deterministic: same inputs → byte-identical output (sorted keys). Wire into a `dxb gateway generate` CLI entry OR an exported fn 07-04 can call; do not hand-write any profile.
  </action>
  <verify><automated>pnpm build && node -e "import('./packages/gateway/dist/index.js').then(m=>{if(!m.generateProfiles)throw new Error('no export');console.log('GENERATE_OK')})"</automated></verify>
  <acceptance_criteria>
    - generateProfiles emits valid .mcp.json per department with the generated_at + source_hash header
    - Default-deny: a tool absent from a dept's registry allowlist never appears in its profile
    - Deterministic output (re-run → identical bytes)
  </acceptance_criteria>
  <done>Per-department profiles are compiled from the registry — least-privilege that cannot drift.</done>
</task>

<task type="auto">
  <name>Task 3: profile-denial test — the doc's explicit reds proven on emitted JSON</name>
  <files>tests/phase7/profile-denial.test.ts</files>
  <read_first>
    - tests/phase7/pin-quarantine.test.ts (fixture-inventory + DB harness pattern to reuse)
  </read_first>
  <action>
    Seed a fixture registry (research dept with a payment tool granted-but-denied, ceo dept with a code MCP granted-but-denied, one worker dept) + a fixture inventory including stripe.create_charge, github.create_pr, plus benign tools. Run generateProfiles to a temp dir. Assert: (1) research.mcp.json has NO stripe/docusign server (grep-equivalent on parsed JSON); (2) ceo.mcp.json has NO github/code MCP; (3) worker profile has neither stripe nor docusign; (4) a benign allowlisted tool DOES appear (positive control — profiles aren't empty by accident); (5) quarantine cross-check: mark stripe quarantined in tool_pins → even a dept that ALLOWS it gets it excluded. Cleanup temp dir + fixture rows.
  </action>
  <verify><automated>pnpm vitest run tests/phase7/profile-denial.test.ts 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - research profile: no stripe (master step-3 verification line: "profiles/research.mcp.json'ında stripe YOK")
    - ceo profile: no github/code MCP
    - Positive control passes (allowlisted benign tool present)
    - Quarantined tool excluded even from an allowing department
  </acceptance_criteria>
  <done>MCP-02's static half is proven on the actual emitted profiles; 07-04 proves the runtime half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| denials.json (human-edited) → generated profiles | the one hand-edited least-privilege input |
| registry allowlist → session tool visibility | registry drift would widen visibility silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Elevation | a department seeing a tool it must not (Stripe to a worker) | high | mitigate | default-deny generation + denials.json + emitted-profile grep test |
| T-07-08 | Tampering | hand-edited profile drifting from the registry | high | mitigate | profiles generated-only; source_hash header makes hand-edits/staleness detectable |
| T-07-09 | Elevation | quarantined tool leaking into a profile | high | mitigate | generation subtracts quarantined pins; cross-check test |
| T-07-10 | Elevation | silent denials.json widening | medium | mitigate | ⛔ FABLE-ONLY note in-file; change shows in git diff + source_hash |
</threat_model>

<verification>
- DENIALS_OK + GENERATE_OK; profile-denial test green (research no-stripe, ceo no-code, positive control, quarantine exclusion); deterministic re-run identical; full suite regression green.
</verification>

<success_criteria>Master step 3 satisfied: per-department profiles are generated from the registry with the doc's denials applied and quarantined tools excluded, proven on the emitted JSON.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-03-SUMMARY.md with denials source citation, a sample emitted profile header, test evidence, and any [ADAPT] denial assumptions for CEO confirmation.</output>
