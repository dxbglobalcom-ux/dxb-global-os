---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/0010_tool_pins.sql
  - packages/gateway/src/pin-check.ts
  - packages/gateway/src/index.ts
  - packages/gateway/package.json
  - packages/gateway/tsconfig.json
  - packages/outbox-executor/src/scheduler.ts
  - tests/phase7/pin-quarantine.test.ts
autonomous: true
requirements: [MCP-03]
must_haves:
  truths:
    - "Every currently-known MCP tool has a row in tool_pins with schema_hash = sha256(description + inputSchema JSON, key-sorted) after the first pinAll run"
    - "A changed tool description flips quarantined=true and writes an audit row IN THE SAME TRANSACTION — no silent quarantine (anti rug-pull, MCP-03)"
    - "A quarantined tool NEVER un-quarantines automatically: re-approval is an explicit human-path update (dxb CLI or SQL by CEO decision), not a re-hash"
    - "pin-check has NO model calls and NO network egress beyond the configured MCP servers — deterministic hashing only"
  artifacts:
    - "db/migrations/0010_tool_pins.sql — verbatim from master PHASE-07 §3 (table + RLS enable)"
    - "packages/gateway/src/pin-check.ts — computeToolHash, pinAll, checkPins exported"
    - "pg-boss schedule `tool-pin-check` registered in the existing scheduler (daily 04:00, after 03:00 compaction)"
  key_links:
    - "tool_pins.quarantined <-> generate-profiles.ts exclusion filter (07-03 asserts a quarantined tool is dropped from generated profiles)"
    - "checkPins audit row <-> morning review queue anomaly surface (07-06) and Phase-8 CEO alarm UI"
---

<objective>
Master step 2: the anti-rug-pull layer. Migration `0010_tool_pins` (verbatim SQL from the master plan) + `pin-check.ts` (hash at install, re-hash and compare on cron, quarantine on drift with audit). New `packages/gateway` workspace package is born here; 07-03 adds the generator beside it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§3 verbatim SQL; LOCKED: quarantined tools never enter profiles)
@.planning/research/study-cards/mcp-gateway-patterns.md   (why hash pinning is custom — no product provides it)
</context>

## Artifacts this phase produces

**This plan:** `packages/gateway` package (tsconfig/package.json wired into workspace root build); migration `0010_tool_pins.sql`; exports `computeToolHash(tool)`, `pinAll(db, inventory)`, `checkPins(db, inventory)`; pg-boss queue `tool-pin-check` + daily schedule; `tests/phase7/pin-quarantine.test.ts`.

<tasks>

<task type="auto">
  <name>Task 1: migration 0010_tool_pins.sql (verbatim) + gateway package skeleton</name>
  <files>db/migrations/0010_tool_pins.sql, packages/gateway/package.json, packages/gateway/tsconfig.json, packages/gateway/src/index.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-07.md (§3 — the SQL is copied byte-for-byte; filename numbering follows house [ADAPT] precedent if 0010 is taken)
    - db/migrations/ (current numbering — 06-06 precedent: [ADAPT] renumber recorded if collision)
    - packages/memory-router/package.json + tsconfig.json (workspace package pattern: no per-package build script, root `pnpm build` tsc --build, references)
  </read_first>
  <action>
    Create migration with the master's verbatim CREATE TABLE tool_pins (id uuid PK, server text, tool text, schema_hash text, quarantined boolean default false, pinned_at, last_checked, UNIQUE(server,tool)) + ENABLE ROW LEVEL SECURITY; apply to local Postgres (54322, session-mode). Scaffold packages/gateway following the memory-router package shape exactly (type module, workspace deps kysely + @dxb/shared as needed, tsconfig reference added to root tsconfig).
  </action>
  <verify><automated>psql "$DXB_DB_URL" -c "\\d tool_pins" | grep -E "quarantined|schema_hash" && pnpm build</automated></verify>
  <acceptance_criteria>
    - tool_pins exists locally with UNIQUE(server, tool) and RLS enabled (\\d output quoted in SUMMARY)
    - Root `pnpm build` compiles the new package (no per-package build scripts — standing deviation since 06-03)
  </acceptance_criteria>
  <done>The pin store exists exactly as the master specifies.</done>
</task>

<task type="auto">
  <name>Task 2: pin-check.ts — hash, pin, compare, quarantine</name>
  <files>packages/gateway/src/pin-check.ts, packages/gateway/src/index.ts</files>
  <read_first>
    - packages/memory-router/src/compaction.ts (house transaction + batch-audit pattern to mirror)
    - db/migrations/ Phase-3 registry migrations (exact table/column names for the MCP server inventory — NO GUESSING: if the registry carries no server inventory, the inventory source becomes the same config the profile generator reads, recorded as [ADAPT] in SUMMARY)
    - packages/mcps/ (the in-repo DXB MCP servers — their tool declarations are the first pin corpus)
  </read_first>
  <action>
    `computeToolHash(tool: {description, inputSchema})`: canonical JSON (recursively key-sorted, no whitespace) → sha256 hex. `pinAll(db, inventory)`: for each (server, tool, hash) upsert-if-absent (never overwrite an existing hash — a pin is immutable until human re-approval); set pinned_at. `checkPins(db, inventory)`: recompute per tool; on mismatch where quarantined=false → in ONE transaction set quarantined=true + last_checked + audit row (actor 'gateway:pin-check', action 'tool_quarantined', payload {server, tool, old_hash, new_hash}); tools missing from live inventory → audit 'tool_missing' (no quarantine flip — disappearance ≠ mutation); matching tools → last_checked touch. Inventory type is injected (deps seam) so tests run without live MCP processes; the production inventory reader enumerates the in-repo DXB MCP tool declarations + any registry-listed external servers. Register pg-boss queue `tool-pin-check` (daily "0 4 * * *") in the existing scheduler beside memory-compaction, same error-isolation pattern.
  </action>
  <verify><automated>pnpm build && node -e "import('./packages/gateway/dist/index.js').then(m=>{if(!m.computeToolHash||!m.pinAll||!m.checkPins)throw new Error('missing export');console.log('GATEWAY_EXPORTS_OK')})"</automated></verify>
  <acceptance_criteria>
    - computeToolHash is deterministic under key order (same object, shuffled keys → same hash)
    - checkPins never un-quarantines and never overwrites a pin hash
    - Scheduler registers `tool-pin-check` at "0 4 * * *" (grep evidence)
  </acceptance_criteria>
  <done>Hash pinning is code, cron-wired, and audit-coupled — MCP-03's mechanism is live.</done>
</task>

<task type="auto">
  <name>Task 3: pin-quarantine test — fake rug-pull proven</name>
  <files>tests/phase7/pin-quarantine.test.ts</files>
  <read_first>
    - tests/phase6/known-facts.test.ts (DB test harness pattern: 54322, fileParallelism false, cleanup discipline)
  </read_first>
  <action>
    Test battery: (1) pinAll over a two-tool fixture inventory → 2 rows, hashes stable across re-run (idempotent, no overwrite); (2) mutate one tool's description in the fixture → checkPins → quarantined=true + audit row 'tool_quarantined' with old/new hashes, other tool untouched, last_checked updated on both; (3) run checkPins again with the mutated inventory → no duplicate audit row (already-quarantined short-circuit); (4) restore original description → STILL quarantined (no auto-unquarantine — the anti-rug-pull core assertion); (5) key-order canonicalization test on computeToolHash. Cleanup deletes fixture rows + audit rows by tracked ids.
  </action>
  <verify><automated>pnpm vitest run tests/phase7/pin-quarantine.test.ts 2>&1 | tail -4 | grep -E "passed"</automated></verify>
  <acceptance_criteria>
    - Fake description change → quarantined=true + audit (master step-2 verification, first half; profile exclusion half lands 07-03)
    - Restore-description case proves quarantine is sticky
    - Full suite still green (no regression)
  </acceptance_criteria>
  <done>A rug-pull attempt is detected, quarantined, audited, and cannot self-heal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream MCP servers → OS trust store | third parties can mutate tool descriptions/schemas at will |
| cron job → tool_pins | unattended process mutates the security posture table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | tool description rug-pull post-approval | high | mitigate | hash pin + quarantine + audit in one transaction; sticky quarantine (no auto-heal); profile exclusion at 07-03 |
| T-07-04 | Tampering | hash canonicalization bypass (semantically-equal JSON, different hash → false quarantine; or key-order tricks) | medium | mitigate | recursive key-sort canonical form + explicit key-order test |
| T-07-05 | Repudiation | quarantine flips without trail | medium | mitigate | audit row same-transaction; payload carries old+new hash |
| T-07-06 | Elevation | attacker un-quarantines via re-hash path | high | mitigate | pinAll never overwrites; un-quarantine only via explicit human action outside pin-check |
</threat_model>

<verification>
- Migration applied (\\d evidence); GATEWAY_EXPORTS_OK; pin-quarantine test green incl. sticky-quarantine case; scheduler grep shows "0 4 * * *"; full suite regression green.
</verification>

<success_criteria>Master step 2 satisfied: every known tool is hash-pinned, a live description change quarantines with audit, and the check runs unattended on the same scheduler as the memory lifecycle.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-02-SUMMARY.md with \\d output, test evidence, scheduler grep, and any [ADAPT] (migration number, inventory source).</output>
