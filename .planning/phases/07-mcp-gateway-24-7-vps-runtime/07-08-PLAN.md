---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 08
type: execute
wave: 6
depends_on: [02, 03, 04, 05, 06, 07]
files_modified:
  - .github/workflows/gitleaks.yml
  - .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-VERIFICATION.md
  - .planning/research/INTEGRATION-TRACKER.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "gitleaks-action is pinned to a full commit SHA (`@<40-hex>`), not a tag — the Phase-1 TODO is closed with evidence (supply-chain pin)"
    - "07-VERIFICATION.md maps ALL FIVE ROADMAP success criteria to executed evidence (command → decisive output), two-tier ✓/⚠, before any closure claim"
    - "The FABLE closure verdict is written in-session by Fable personally (⛔ FABLE-ONLY; Checker PASS ≠ done)"
    - "Tracker rows for hermes-agent, Speaches, yt-dlp reach truthful exit states with citations — no row advances past its executed evidence"
  artifacts:
    - ".github/workflows/gitleaks.yml — SHA-pinned action"
    - "07-VERIFICATION.md — 5/5 criteria evidenced + FABLE verdict block"
    - "ROADMAP.md Phase 7 checkboxes + STATE.md phase_complete"
  key_links:
    - "07-VERIFICATION criteria 1..5 <-> ROADMAP Phase-7 success criteria (verbatim mapping)"
    - "gitleaks SHA pin <-> Phase-1 01-01 SUMMARY TODO ('SHA-pin at remote go-live')"
---

<objective>
Master steps 11–12: phase closure. CI gitleaks-action gets its SHA pin (the Phase-1 handover item), and 07-VERIFICATION.md certifies the five ROADMAP criteria with executed evidence under a Fable-authored verdict. **Checkpoint (autonomous:false):** the ⛔ FABLE-ONLY closure verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (§1 the five acceptance criteria; §4 steps 11–12; §6 ⛔ closure = Fable-only)
@.planning/phases/06-memory-router-knowledge-stores/06-VERIFICATION.md   (format + verdict precedent)
@.planning/phases/01-security-baseline/01-01-SUMMARY.md   (the SHA-pin TODO being closed)
</context>

## Artifacts this plan produces

SHA-pinned `gitleaks.yml`; `07-VERIFICATION.md` (5/5 + FABLE verdict); tracker exits; ROADMAP/STATE closure updates; the phase-closure commit.

<tasks>

<task type="auto">
  <name>Task 1: CI gitleaks-action SHA-pin (Phase-1 handover)</name>
  <files>.github/workflows/gitleaks.yml</files>
  <read_first>
    - .github/workflows/ (current workflow state — whether gitleaks.yml exists or is created now)
    - .planning/phases/01-security-baseline/01-01-SUMMARY.md (the exact TODO wording being discharged)
  </read_first>
  <action>
    Resolve the current gitleaks-action v2 tag to its full commit SHA (`gh api` or git ls-remote on the action repo — record the resolution command + output), pin `uses: gitleaks/gitleaks-action@<full-sha> # v2.x.y` in the workflow. If the repo has a GitHub remote with Actions: push and capture `gh run` green. If NO remote exists yet: the workflow file is still authored + SHA-pinned now, and the run-green half is recorded ⚠ UNVERIFIED (needs remote go-live) — do NOT create a remote unprompted (outward action, CEO decision).
  </action>
  <verify><automated>grep -E "uses: gitleaks/gitleaks-action@[0-9a-f]{40}" .github/workflows/gitleaks.yml</automated></verify>
  <acceptance_criteria>
    - Action pinned to a full 40-hex SHA with the human-readable tag comment
    - SHA↔tag resolution evidence quoted in SUMMARY
    - `gh run` green if remote exists; otherwise ⚠ UNVERIFIED explicitly recorded
  </acceptance_criteria>
  <done>The Phase-1 supply-chain TODO is discharged with evidence.</done>
</task>

<task type="checkpoint">
  <name>Task 2: 07-VERIFICATION.md — five criteria + tracker exits + FABLE verdict (⛔)</name>
  <files>.planning/phases/07-mcp-gateway-24-7-vps-runtime/07-VERIFICATION.md, .planning/research/INTEGRATION-TRACKER.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 7 success criteria (the five TRUE-statements being certified)
    - .planning/phases/06-memory-router-knowledge-stores/06-VERIFICATION.md (two-tier format + FABLE verdict block precedent)
    - .planning/phases/07-mcp-gateway-24-7-vps-runtime/*-SUMMARY.md (the evidence pool)
  </read_first>
  <action>
    Write 07-VERIFICATION.md mapping each criterion to executed evidence: (1) profile denials → 07-03 emitted-JSON grep + 07-04 runtime not-found; (2) hash-pin quarantine on live change → 07-02 pin-quarantine test + 07-03 profile exclusion; (3) 8GB stack + reboot → 07-05 RAM table + time-to-green; (4) hermes bounded jobs + watchdog + kill switch + morning queue → 07-06 three live runs; (5) video link → quarantined memory → 07-07 live ingest row. Each line ✓ VERIFIED (command → output) or ⚠ UNVERIFIED with reason (expected ⚠ set: unattended 06:00 first firing; live-session profile proof if proxied; gh run if no remote). Deviations table: every [ADAPT] from the phase SUMMARYs restated CEO-visibly. Advance tracker exits (hermes/speaches/yt-dlp → EMBED only where round-trip evidence exists). Mark ROADMAP Phase-7 checkboxes; STATE → phase_complete. THEN the checkpoint: Fable personally reads the phase `git diff --stat`, the five evidence lines, and the ⚠ list, and writes the FABLE VERDICT block in-session — closure may not be claimed before that block exists. Suggest /compact at closure per house cadence.
  </action>
  <verify><manual>07-VERIFICATION.md exists with 5/5 criteria evidenced two-tier + deviations table + FABLE verdict block; tracker exits cited; ROADMAP [x]; STATE phase_complete staged in the closure commit</manual></verify>
  <acceptance_criteria>
    - Every criterion carries executed evidence — no prediction-tense claims (Evidence-Before-Done)
    - FABLE verdict present, written in-session by Fable (not relayed)
    - Tracker/ROADMAP/STATE truth aligned with disk truth
  </acceptance_criteria>
  <done>Phase 7 closes the way Phase 6 did: on evidence, under a Fable-authored verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closure claim → project truth | a gamed verification would certify an unsafe 24/7 runtime |
| CI action tag → supply chain | a moved tag executes third-party code on company sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-28 | Tampering | gitleaks-action tag repointed upstream | medium | mitigate | full-SHA pin + tag comment; renewal = deliberate diff |
| T-07-29 | Repudiation | closure without evidence (prediction-tense claims) | high | mitigate | Evidence-Before-Done binding; two-tier table mandatory; FABLE verdict gate |
</threat_model>

<verification>
- SHA-pin grep green; 07-VERIFICATION 5/5 two-tier + deviations + FABLE verdict; tracker/ROADMAP/STATE aligned; closure commit clean through gitleaks.
</verification>

<success_criteria>Master steps 11–12 satisfied: supply-chain pin discharged and Phase 7 certified closed on executed evidence under the Fable verdict.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-08-SUMMARY.md with the pin evidence, the five criterion lines, the ⚠ set, and the closure commit hash.</output>
