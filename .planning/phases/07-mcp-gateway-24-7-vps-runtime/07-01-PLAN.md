---
phase: 07-mcp-gateway-24-7-vps-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/study-cards/hermes-agent.md
  - .planning/research/study-cards/speaches.md
  - .planning/research/study-cards/yt-dlp-video-use.md
  - .planning/research/INTEGRATION-TRACKER.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every Phase-7 deploy target (hermes-agent, Speaches, yt-dlp) has a FILLED study card with pinned version + deploy contract BEFORE its install step runs (INTEG-01 'no blind installs')"
    - "hcloud CLI and yt-dlp are installed and version-verified LOCALLY before 07-04/07-07 depend on them (dual-role principle: tools at the START of the phase that uses them)"
    - "The gateway study pass is NOT redone here — mcp-gateway-patterns.md landed at planning (commit b7dc785, master step 1); this plan only fills the REMAINING toolset cards"
  artifacts:
    - ".planning/research/study-cards/speaches.md — NEW card (image pin, STT/TTS endpoints, RAM expectation, on-demand profile contract)"
    - ".planning/research/study-cards/hermes-agent.md + yt-dlp-video-use.md — Phase-7 deploy contracts filled (TBD fields resolved)"
    - ".planning/research/INTEGRATION-TRACKER.md rows advanced with citations"
  key_links:
    - "speaches.md image pin <-> vps/compose.yaml voice profile (07-05) and video-learn STT call (07-07)"
    - "hermes-agent.md version pin + config surface <-> vps/hermes/* and hermes.service (07-06)"
    - "yt-dlp-video-use.md invocation contract <-> tools/video-learn/src/ingest.ts (07-07)"
---

<objective>
Phase toolset study→install (master plan PHASE-07 residue of step 1; the gateway pattern study itself was executed at planning — commit b7dc785). Fill the three deploy-target cards (hermes-agent, Speaches NEW, yt-dlp/video-use), install the two local CLI tools later plans need (hcloud, yt-dlp), and advance tracker rows. No VPS, no gateway code in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-07.md   (LOCKED decisions: hermes version-lock, Speaches on-demand profile, VPS separate credentials)
@.planning/research/study-cards/mcp-gateway-patterns.md   (step-1 study pass — already done; do not repeat)
@.planning/research/STACK.md   (Speaches CPU image notes, hermes via LiteLLM rule, Hetzner class)
@.planning/research/INTEGRATION-TRACKER.md   (advance-left-to-right rule)
</context>

## Artifacts this phase produces

**This plan:** `speaches.md` study card (new); hermes-agent.md + yt-dlp-video-use.md Phase-7 contract fills; tracker row advances; local installs `hcloud` + `yt-dlp` (version-verified).
**Phase-wide (context):** migration `0010_tool_pins.sql`; `packages/gateway/src/{generate-profiles,pin-check}.ts`; `packages/gateway/policy/denials.json`; `profiles/<dept>.mcp.json` (generated); `vps/compose.yaml` + `vps/systemd/{dxb-stack,hermes,watchdog}.service`; `vps/hermes/{config,jobs/*.md}`; `packages/workers/src/vps-worker.ts`; `tools/video-learn/src/ingest.ts`; CLI `dxb kill-switch`; pg-boss schedule `tool-pin-check`; tests/phase7/{pin-quarantine,profile-denial,reboot,watchdog,video-ingest}.test.ts; 07-VERIFICATION.md.

<tasks>

<task type="auto">
  <name>Task 1: fill the three deploy-target study cards</name>
  <files>.planning/research/study-cards/hermes-agent.md, .planning/research/study-cards/speaches.md, .planning/research/study-cards/yt-dlp-video-use.md</files>
  <read_first>
    - .planning/research/study-cards/open-notebook.md (the FILLED-card format precedent: pinned image tags, RAM note, env names-only rule)
    - .planning/research/study-cards/hermes-agent.md (current state — fill, don't rewrite)
    - .planning/research/study-cards/yt-dlp-video-use.md (current state)
  </read_first>
  <action>
    Research live surfaces (GitHub releases / Docker Hub / docs), then fill: (1) hermes-agent.md — exact version/commit pin (LOCKED decision: version-lock, update = card renewal), config surface for pointing its OpenAI-compatible endpoint at the local LiteLLM proxy (GLM 5.2 brain routes through LiteLLM so spend lands in the ledger — STACK rule), systemd service shape, RAM expectation vs master table (~300–500MB), job-definition mechanism the 07-06 bounded-job template maps onto. (2) speaches.md — NEW card: exact image tag on ghcr.io/speaches-ai (moving `latest-cpu` NOT used — pin digest or version tag; open-notebook precedent), `/v1/audio/transcriptions` + `/v1/audio/speech` endpoints, whisper `small` int8 default + Kokoro-82M, measured-RAM-expectation 1–1.5GB under load, compose `--profile voice` on-demand contract (LOCKED). (3) yt-dlp-video-use.md — pinned yt-dlp version, audio-extract invocation for the 07-07 pipeline (`-x --audio-format`), legitimacy re-verify, video-use scope note (defer if not needed for VID-01 v1). Env names only, never values. Each card: Legitimacy Verdict BEFORE install (INTEG-01).
  </action>
  <verify><automated>grep -L "TBD" .planning/research/study-cards/hermes-agent.md .planning/research/study-cards/speaches.md .planning/research/study-cards/yt-dlp-video-use.md | wc -l | grep -q 3 && grep -c "Pinned Version" .planning/research/study-cards/speaches.md</automated></verify>
  <acceptance_criteria>
    - speaches.md exists with exact image pin (no moving tag), endpoint list, RAM expectation, on-demand-profile note
    - hermes-agent.md carries a version/commit pin + LiteLLM base-URL wiring note + systemd shape
    - yt-dlp-video-use.md carries a pinned version + the exact ingest invocation 07-07 will copy
    - No card contains a secret value (names only)
  </acceptance_criteria>
  <done>Every service 07-05/06/07 deploys has a studied, pinned, legitimacy-checked contract on file first.</done>
</task>

<task type="auto">
  <name>Task 2: local tool installs — hcloud CLI + yt-dlp (version-verified)</name>
  <files>.planning/research/study-cards/yt-dlp-video-use.md</files>
  <read_first>
    - .planning/phases/01-security-baseline/01-01-SUMMARY.md (sha256-verified install precedent — gitleaks)
  </read_first>
  <action>
    Install hcloud CLI (Hetzner official, GitHub releases tarball — sha256-verify against the release checksums file, install to ~/.local/bin, record version) and yt-dlp (pinned release binary or pipx — match the card's pin; sha256-verify). Both are LOCAL control-terminal tools: hcloud drives 07-04 provisioning; yt-dlp is dev-tested locally before VPS use in 07-07. Do NOT create any Hetzner resources and do NOT store any token — 07-04's checkpoint owns credentials. Append install evidence (version output lines) to the relevant card sections.
  </action>
  <verify><automated>~/.local/bin/hcloud version && yt-dlp --version</automated></verify>
  <acceptance_criteria>
    - `hcloud version` prints a version (binary sha256-verified — checksum line quoted in SUMMARY)
    - `yt-dlp --version` prints the pinned version
    - No API token created, stored, or requested in this plan
  </acceptance_criteria>
  <done>07-04 can provision and 07-07 can ingest without mid-plan tool installs.</done>
</task>

<task type="auto">
  <name>Task 3: tracker rows advanced</name>
  <files>.planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - .planning/research/INTEGRATION-TRACKER.md (status legend: advance left-to-right only, qualifiers in Notes)
  </read_first>
  <action>
    Advance with citations: hermes-agent STUDY→INSTALL only when its card is filled AND the local prep it needs is done (deploy itself is 07-06 — Notes: "install lands on VPS 07-06"); yt-dlp + video-use STUDY→INSTALL (local binary live, card filled); add NEW row for Speaches (Category: Media/content, Target Phase 7, Trigger service, card speaches.md, Status STUDY→INSTALL at 07-05 deploy — Notes the on-demand profile). Never advance a row past what executed evidence supports.
  </action>
  <verify><automated>grep -n "speaches" .planning/research/INTEGRATION-TRACKER.md</automated></verify>
  <acceptance_criteria>
    - Speaches row exists; hermes/yt-dlp rows advanced no further than evidence supports; every advance carries a dated citation in Notes
  </acceptance_criteria>
  <done>Tracker truth matches disk truth for every Phase-7 tool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release artifacts → local machine | third-party binaries (hcloud, yt-dlp) execute locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | supply-chain on hcloud/yt-dlp binaries | medium | mitigate | sha256 verification against official release checksums before first run (01-01 precedent); versions pinned in cards |
| T-07-02 | Info disclosure | secrets leaking into study cards | medium | mitigate | env NAMES only rule (open-notebook card precedent); gitleaks pre-commit already active |
</threat_model>

<verification>
- Three cards filled (no TBD), speaches.md new with exact pin; `hcloud version` + `yt-dlp --version` executed; tracker rows advanced with citations.
</verification>

<success_criteria>Master step-1 residue closed: every Phase-7 tool studied and pinned before any install/deploy step depends on it, and the two local CLIs later plans invoke are live and verified.</success_criteria>

<output>Create .planning/phases/07-mcp-gateway-24-7-vps-runtime/07-01-SUMMARY.md with card-fill evidence, install version lines, and tracker diff.</output>
