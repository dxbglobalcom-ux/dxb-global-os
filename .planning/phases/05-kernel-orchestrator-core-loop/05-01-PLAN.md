---
phase: 05-kernel-orchestrator-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/study-cards/claude-agent-sdk.md
  - .planning/phases/05-kernel-orchestrator-core-loop/05-01-legitimacy-approval.md
  - .planning/research/INTEGRATION-TRACKER.md
  - pnpm-workspace.yaml
  - packages/kernel/package.json
  - packages/orchestrator/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [KERN-01]
must_haves:
  truths:
    - "claude-agent-sdk study card is FULL and current (0.3.201 re-verified live at install day) BEFORE any install runs"
    - "CEO approved the npm install in ONE blocking checkpoint with live npm evidence (no-guessing rule)"
    - "@anthropic-ai/claude-agent-sdk installed at EXACT pin 0.3.201, dependency ONLY in packages/kernel and packages/orchestrator"
    - "Tracker INSTALL flipped for claude-agent-sdk; validator exit 0; build+test stay green"
  artifacts:
    - ".planning/research/study-cards/claude-agent-sdk.md (FULL, install-day verified)"
    - "05-01-legitimacy-approval.md (CEO decision record)"
  key_links:
    - "pnpm-workspace.yaml catalog exact pin <-> packages/kernel + packages/orchestrator dependency entries"
    - "STACK.md Version Compatibility row (Node >= 20, pre-1.0 pin-exact rule) <-> installed version"
---

<objective>
Open Phase 5's toolset gate (master-plan step 1): Agent SDK 0.3.201 studied-then-installed at its two owners (kernel, orchestrator), CEO supply-chain checkpoint passed, tracker truth updated. Tools installed at the START of the phase that uses them — nothing else in Phase 5 can call an LLM through the SDK before this closes.
Output: current FULL study card + approval record + pinned install + green build/test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§4 step 1 + §6 budget-fallback markers)
@.planning/research/STACK.md         (Version Compatibility: agent-sdk 0.3.x pre-1.0 rules — READ BEFORE INSTALL)
</context>

## Artifacts this phase produces

**This plan:** exact-pin catalog entry `@anthropic-ai/claude-agent-sdk: 0.3.201`; SDK dependency in `packages/kernel/package.json` + `packages/orchestrator/package.json`; FULL study card; 05-01-legitimacy-approval.md; tracker INSTALL row.
**Phase-wide (context):** tables `routing_rules`, `tasks.depends_on uuid[]`; SQL fn `claim_next_task` (dependency-aware replacement); kernel `ClassifiedIntent` (Zod), `classify()`, `loadPolicy()`/`route()`; orchestrator `decompose()`, `dispatch()`, `runWorkerOnce()` (worker shim), `escalate()`, `council()`, `qa()`; CLI `dxb intent`; seeds `packages/kernel/policy/routing-seed.json`, `db/seed/import-routing-rules.ts`, `db/seed/apply-persona-v2.ts`; personas `personas/product/*.md` ×5 (`v2.0-fable`); tests `tests/phase5/{routing-data,decompose-dispatch,ladder,council-judge}.test.ts`, `tests/phase5/golden/` ×10, `tests/phase5/slice-10of10.sh`; closure `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: claude-agent-sdk study card → FULL + install-day live verification</name>
  <files>.planning/research/study-cards/claude-agent-sdk.md</files>
  <read_first>
    - .planning/research/study-cards/claude-agent-sdk.md (existing STUDY-status card — upgrade in place)
    - .planning/research/STACK.md — Version Compatibility table (agent-sdk 0.3.x: Node >= 20, pin exact, review changelogs on bump — pre-1.0)
    - .planning/master-plan/PHASE-05.md §4 step 1 (pin 0.3.201)
  </read_first>
  <action>
    Upgrade the card to FULL and current: fetch live npm evidence for `@anthropic-ai/claude-agent-sdk` (current version, publish date, publisher = anthropic, weekly downloads, exact spelling). Confirm 0.3.201 is still the correct pin per master plan; if npm shows a newer 0.3.x, the pin STAYS 0.3.201 (master-plan pin wins; note the newer version in the card — bumping the pin is a recorded adaptation requiring CEO visibility, not a silent upgrade). Record in the card: headless/`query()` usage mode for kernel/orchestrator programs, subscription-mode execution (no raw provider keys — LiteLLM virtual keys only for api-mode models), Node 22 LTS requirement, pre-1.0 API-churn pitfall, and the lifecycle checklist with STUDY checked.
  </action>
  <verify><automated>grep -q '0.3.201' .planning/research/study-cards/claude-agent-sdk.md && grep -qiE 'verified.*2026' .planning/research/study-cards/claude-agent-sdk.md</automated></verify>
  <acceptance_criteria>
    - Card FULL: purpose, exact pin 0.3.201, key APIs (query/headless, subagents, hooks), pitfalls (pre-1.0 churn, Node >= 20), legitimacy verdict with live npm evidence + fetch date
    - Any live-npm divergence from the 0.3.201 pin is recorded in the card, pin unchanged
  </acceptance_criteria>
  <done>SDK studied and current before any install command exists in history.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CEO supply-chain checkpoint — Agent SDK npm install</name>
  <files>.planning/phases/05-kernel-orchestrator-core-loop/05-01-legitimacy-approval.md</files>
  <read_first>
    - .planning/phases/04-safety-rails-gates-cost-audit/04-01-legitimacy-approval.md — record format to replicate
  </read_first>
  <action>
    Present to CEO in ONE blocking checkpoint: `@anthropic-ai/claude-agent-sdk@0.3.201` install into packages/kernel + packages/orchestrator, with Task 1's live npm evidence inline (publisher anthropic, version, date, downloads). Any postinstall/build-script request beyond the approved allowBuilds set halts the plan. Record the decision in 05-01-legitimacy-approval.md. If rejected: STOP — Task 3 does not run and Phase 5 planning returns to CEO.
  </action>
  <verify><automated>grep -qi 'Decision.*approved' ".planning/phases/05-kernel-orchestrator-core-loop/05-01-legitimacy-approval.md"</automated></verify>
  <acceptance_criteria>
    - Approval record on disk with npm evidence; commit order proves record precedes install
  </acceptance_criteria>
  <resume-signal>CEO types "approved" or rejects with reason</resume-signal>
  <done>Supply-chain trust auditable; install gate open.</done>
</task>

<task type="auto">
  <name>Task 3: pinned install at owners + tracker INSTALL flip</name>
  <files>pnpm-workspace.yaml, packages/kernel/package.json, packages/orchestrator/package.json, pnpm-lock.yaml, .planning/research/INTEGRATION-TRACKER.md</files>
  <read_first>
    - pnpm-workspace.yaml (catalog pattern — exact versions, allowBuilds deny-default)
    - packages/kernel/package.json + packages/orchestrator/package.json (current scaffold dependency shape: @dxb/shared workspace:*, zod catalog:)
    - .planning/research/INTEGRATION-TRACKER.md row 58 (claude-agent-sdk, STUDY status)
  </read_first>
  <action>
    Add catalog entry `"@anthropic-ai/claude-agent-sdk": 0.3.201` (exact, no caret — pre-1.0 rule) to pnpm-workspace.yaml; add `"@anthropic-ai/claude-agent-sdk": "catalog:"` to dependencies of packages/kernel and packages/orchestrator ONLY (not shared, not dxb-mcp, not dxb-cli). `pnpm install` — any unexpected build-script prompt STOPS the plan. Flip the tracker row STUDY→INSTALL with Phase-5 note. `pnpm build && pnpm test` must stay green (45 passed | 3 skipped baseline or better).
  </action>
  <verify><automated>pnpm -r ls @anthropic-ai/claude-agent-sdk | grep -q '0.3.201' && pnpm build && pnpm test && node scripts/check-integration-tracker.mjs</automated></verify>
  <acceptance_criteria>
    - `pnpm ls @anthropic-ai/claude-agent-sdk` resolves 0.3.201 exactly, present only under packages/kernel and packages/orchestrator (master-plan step 1 verification)
    - build + full test suite green; tracker validator exit 0
  </acceptance_criteria>
  <done>SDK installed at its two owners at the LOCKED pin; master-plan step 1 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → monorepo | third-party code enters the runtime that will hold LiteLLM virtual keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | @anthropic-ai/claude-agent-sdk npm install | high | mitigate | study card with live evidence BEFORE install + blocking CEO checkpoint (Task 2) + exact pin + allowBuilds deny-default (unexpected postinstall halts plan) |
| T-05-01 | Elevation | SDK dependency sprawl into non-owner packages | low | mitigate | dependency declared only in kernel/orchestrator; Task 3 verify greps resolution scope |
</threat_model>

<verification>
- Commit order: card → approval → install (study-then-install discipline provable from git log)
- `pnpm -r ls @anthropic-ai/claude-agent-sdk` shows 0.3.201; validator + build + test green
</verification>

<success_criteria>Master-plan step 1 closed: SDK studied, CEO-gated, pinned-installed at kernel + orchestrator, tracker truthful.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-01-SUMMARY.md with decisive output lines (pnpm ls pin match, validator, test count).</output>
