---
phase: 05-kernel-orchestrator-core-loop
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - packages/orchestrator/src/decompose.ts
  - packages/orchestrator/src/dispatch.ts
  - packages/orchestrator/src/worker-shim.ts
  - packages/orchestrator/src/index.ts
  - packages/orchestrator/tsconfig.json
  - packages/orchestrator/package.json
  - tests/phase5/decompose-dispatch.test.ts
autonomous: true
requirements: [ORCH-01, ORCH-04]
must_haves:
  truths:
    - "A multi-complexity ClassifiedIntent decomposes into >= 2 TaskEnvelopes, every one Zod-valid, with a correct depends_on chain (master-plan step 4 verification)"
    - "Envelopes are self-contained: an objective referencing 'the above/previous' output is REJECTED by the lint before it reaches the queue"
    - "dispatch() writes envelopes as queued tasks rows with depends_on uuids + a 'created' task_event per task — head notification is a queue/event fact, not a chat message"
    - "One envelope runs end-to-end through the worker shim: claim → work → transitions to done with a complete task_events chain (master-plan step 5 verification)"
    - "ORCH-04 holds by construction: the worker receives ONLY the claimed task row (typed envelope fields); all inter-agent data flows through tasks/task_events — no direct agent-to-agent channel exists in the code"
  artifacts:
    - "packages/orchestrator/src/decompose.ts (LLM + Zod + self-contained lint + hop-depth guard)"
    - "packages/orchestrator/src/dispatch.ts (queue writer)"
    - "packages/orchestrator/src/worker-shim.ts (claim→execute→transition loop, injectable executor)"
    - "tests/phase5/decompose-dispatch.test.ts"
  key_links:
    - "decompose() output type <-> @dxb/shared TaskEnvelope (LOCKED schema — changes are FABLE-ONLY)"
    - "dispatch() depends_on uuids <-> claim_next_task dependency clause (05-02)"
    - "worker-shim model/tier <-> envelope.model_tier from routing data (ORCH-02 at execution time)"
---

<objective>
Master-plan steps 4+5: the orchestrator's spine. decompose.ts turns a multi ClassifiedIntent into dependent TaskEnvelopes (LLM only inside this call — control flow stays in TypeScript, LOCKED decision); dispatch.ts writes them to the queue with dependency uuids; worker-shim.ts claims and executes one task end-to-end. This is ORCH-01 + ORCH-04 with executed evidence.
Output: three orchestrator modules + e2e test with full event chain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§2 LOCKED rows 3-4, 7 (hop <= 3) + §5 risk 1 self-contained lint)
@packages/shared/src/envelope.ts     (LOCKED TaskEnvelope — the output contract)
@.planning/phases/05-kernel-orchestrator-core-loop/05-03-SUMMARY.md   (kernel exports as built)
</context>

## Artifacts this phase produces

**This plan:** `decompose(ci: ClassifiedIntent): Promise<TaskEnvelope[]>` (+ per-envelope local dependency indices), `dispatch(envelopes): Promise<{taskIds: string[]}>`, `runWorkerOnce({workerId, departments, execute?})` worker shim, orchestrator index exports, `tests/phase5/decompose-dispatch.test.ts`.
**Phase-wide (context):** migration `routing_and_deps`; kernel `ClassifiedIntent`/`classify()`/`loadPolicy()`/`route()`; `escalate()`/`council()`/`qa()`; CLI `dxb intent`; routing seed; personas/product ×5; tests routing-data/ladder/council-judge/golden/slice-10of10; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: decompose.ts — ClassifiedIntent → TaskEnvelope[] (LLM + Zod + lint)</name>
  <files>packages/orchestrator/src/decompose.ts, packages/orchestrator/src/index.ts, packages/orchestrator/tsconfig.json, packages/orchestrator/package.json</files>
  <read_first>
    - packages/shared/src/envelope.ts (LOCKED TaskEnvelope fields: department, objective min 20, output_contract min 10, model_tier, approval_class, budget, parent_task_id)
    - .planning/master-plan/PHASE-05.md §5 risk 1 (self-contained lint spec) + §2 row 7 (hop-depth <= 3 rule)
    - packages/kernel/src/policy.ts as built (route() gives tier/model per envelope's task class)
  </read_first>
  <action>
    decompose.ts: `decompose(ci)` — for complexity 'single' returns one envelope derived directly from ci (no LLM call — cheapest path). For 'multi': one Agent SDK call (model/effort from the routing 'orchestration' row — data, not constants) prompting for a JSON array of envelope drafts with a local `deps: number[]` index array per item; every item parsed with TaskEnvelope Zod schema extended locally by `{deps: number[]}` — parse failure retries once with the error text, then throws.
    Self-contained lint (code, post-parse): reject any envelope whose objective matches back-reference patterns (Turkish 'yukarıda|önceki|yukarıya' and English 'above|previous step|as mentioned') — a rejected batch triggers ONE regeneration attempt with the lint failure quoted, then throws. Hop-depth guard: reject a deps graph whose longest chain exceeds 3 (LOCKED: aktif hop <= 3; 5 only for genuinely complex work — this phase caps at 3, raising the cap later is a routing/policy decision, not a code default).
    Each envelope's model_tier comes from route() for that envelope's work class; approval_class propagates from ci (never lowered below ci's value). Add packages/orchestrator dependency on @dxb/kernel (workspace:*) + tsconfig reference; export decompose from index.ts.
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase5/decompose-dispatch.test.ts -t decompose 2>/dev/null || pnpm build</automated></verify>
  <acceptance_criteria>
    - Multi intent produces >= 2 envelopes, all TaskEnvelope.parse-valid, deps chain topologically correct (asserted in Task 3's executed test — master-plan step 4 verification)
    - Lint rejects a planted back-referencing objective (unit assertion with a crafted fixture)
    - Chain depth > 3 in a crafted deps fixture throws (hop guard assertion)
    - No fenced model name in decompose.ts — the LLM call parameters come from routing data
  </acceptance_criteria>
  <done>Intent → dependent envelopes with the LLM boxed inside one schema-validated call.</done>
</task>

<task type="auto">
  <name>Task 2: dispatch.ts — envelopes → queued tasks + events</name>
  <files>packages/orchestrator/src/dispatch.ts, packages/orchestrator/src/index.ts</files>
  <read_first>
    - db/migrations/20260707000001_operational_core.sql (tasks columns incl. status enum, task_events shape) + db/migrations/20260708000009_routing_and_deps.sql (depends_on)
    - packages/shared/src/db.ts (getDb Kysely client)
  </read_first>
  <action>
    dispatch.ts: `dispatch(envelopes: (TaskEnvelope & {deps: number[]})[])` — inserts tasks in ONE transaction: first pass inserts all rows (status 'queued', department/objective/output_contract/model_tier/approval_class/budget fields mapped 1:1, priority default), collecting generated uuids in input order; second pass UPDATEs each row's depends_on with the uuids its deps indices point at (indices only reference earlier items — forward-only, cycle-impossible by construction). One task_events row per task: event 'created', actor 'orchestrator:dispatch', payload {envelope_index, deps}. Head notification = the queue row itself + event (LOCKED: sub-agents isolated, queue + typed artifacts, no chat) — no other notification channel is built. Export from index.ts.
  </action>
  <verify><automated>pnpm build</automated></verify>
  <acceptance_criteria>
    - After dispatching a 2-envelope chain in Task 3's test: both rows status 'queued', second row's depends_on = [first row's uuid], two 'created' events exist (SQL-asserted)
    - Transaction atomicity: a forced Zod-invalid batch inserts ZERO rows (asserted with a crafted failure)
  </acceptance_criteria>
  <done>The queue is the only doorway into work; dependencies are uuids the claim function enforces.</done>
</task>

<task type="auto">
  <name>Task 3: worker-shim.ts + end-to-end test (claim → work → done, full event chain)</name>
  <files>packages/orchestrator/src/worker-shim.ts, packages/orchestrator/src/index.ts, tests/phase5/decompose-dispatch.test.ts</files>
  <read_first>
    - tests/phase3/lifecycle.test.ts (status-chain conventions: claimed→running→review→done transitions + events; DB env; cleanup)
    - .planning/master-plan/PHASE-05.md §3 escalation comment (low-confidence <0.6 counts as fail — shim must SURFACE confidence for 05-06 to consume)
  </read_first>
  <action>
    worker-shim.ts: `runWorkerOnce({workerId, departments, execute})` — calls claim_next_task via SQL (raw call through Kysely), and if a task returns: transition claimed→running (event), invoke `execute(task)` — injectable `(task) => Promise<{result: unknown, confidence: number}>`; default executor calls the Agent SDK with the task's objective + output_contract and the model resolved from routing data for the task's tier (subscription mode; api-tier models via @dxb/shared litellm client — LiteLLM virtual keys only, never raw provider keys). On success: write result jsonb + confidence into tasks.result, transition running→review (event, payload {confidence}). On throw: transition →failed (event). The shim NEVER reads or writes another agent's task and has no channel to other workers (ORCH-04 by construction).
    tests/phase5/decompose-dispatch.test.ts: (a) decompose unit group per Task 1 criteria (LLM-dependent parts env-gated like routing-data.test.ts, but run live once for the record); (b) dispatch group per Task 2 criteria with a hand-built 2-envelope chain (no LLM — deterministic); (c) e2e: dispatch one hand-built envelope (department 'engineering', injectable executor returning {result:{text:'ok'}, confidence:0.95}) → runWorkerOnce → assert task status walked queued→claimed→running→review with >= 4 task_events rows in order (master-plan step 5 verification: "task_events zinciri tam") → manual review→done transition closes the chain. Clean up created rows afterAll.
  </action>
  <verify><automated>pnpm vitest run tests/phase5/decompose-dispatch.test.ts && pnpm test</automated></verify>
  <acceptance_criteria>
    - E2E test green: single envelope reaches done; SELECT on task_events for that task shows the complete ordered chain (>= 4 events; output pasted in SUMMARY)
    - Dependency ordering observed live: with the 2-task chain queued, runWorkerOnce claims the independent task first; dependent task claimable only after its dependency is done
    - Full `pnpm test` green (Phase 3/4 suites unaffected)
  </acceptance_criteria>
  <done>Master-plan steps 4+5 closed: envelopes are real queue rows, a worker runs one to done, isolation is structural.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM decompose output → task queue | model-generated JSON becomes executable work orders |
| worker executor → tasks.result | worker output becomes downstream QA/approval input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | prompt-injection via intent text producing malicious envelopes (e.g. outward tasks disguised as internal) | high | mitigate | TaskEnvelope Zod as boundary (enums/min-lengths); approval_class can propagate UP but never below ci's value in code; outward execution still requires Phase-4 approval+outbox gates |
| T-05-11 | Elevation | envelope requesting L1/expensive tier for trivial work | medium | mitigate | model_tier assigned by route() from data, not taken from LLM output; L1 seed rows FABLE-ONLY |
| T-05-12 | DoS | runaway decompose emitting huge envelope batches | medium | mitigate | hop-depth <= 3 guard + batch size cap in decompose (reject > 10 envelopes per intent, regeneration once, then throw); budget fields default per envelope |
</threat_model>

<verification>
- decompose-dispatch suite green (live run recorded); e2e event chain pasted; dependency-order claim observed; full test suite green.
</verification>

<success_criteria>Master-plan steps 4 and 5 closed with executed evidence; ORCH-01 and ORCH-04 hold structurally and observably.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-05-SUMMARY.md with the event-chain query output and any recorded deviations.</output>
