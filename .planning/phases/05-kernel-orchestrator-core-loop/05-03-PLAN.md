---
phase: 05-kernel-orchestrator-core-loop
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - packages/kernel/src/classify.ts
  - packages/kernel/src/policy.ts
  - packages/kernel/src/index.ts
  - packages/kernel/tsconfig.json
  - tests/phase5/routing-data.test.ts
autonomous: true
requirements: [KERN-01, KERN-02, ORCH-02]
must_haves:
  truths:
    - "classify(text) returns a ClassifiedIntent that parses against the LOCKED Zod schema — the kernel NEVER makes a free-text decision"
    - "route(classified) resolves model_tier/model/mode/effort/needs_council purely from routing_rules rows (priority DESC, enabled only) — no model name is chosen in code"
    - "KERN-02 proven live: UPDATE one routing_rules row → the SAME intent resolves to a DIFFERENT model with zero code changes (test evidence)"
    - "5 sample intents classify to their expected task_class/departments (master-plan step 3 verification, executed)"
  artifacts:
    - "packages/kernel/src/classify.ts (ClassifiedIntent LOCKED schema + Agent SDK classify call)"
    - "packages/kernel/src/policy.ts (loadPolicy + route mapping engine)"
    - "tests/phase5/routing-data.test.ts"
  key_links:
    - "ClassifiedIntent.task_class <-> routing_rules.task_class (the routing seam)"
    - "classify()'s own LLM call parameters <-> routing_rules 'orchestration' row (even the kernel's model choice is data)"
---

<objective>
Master-plan step 3: the kernel — classify.ts (intent → LOCKED Zod ClassifiedIntent via Agent SDK) + policy.ts (routing_rules mapping engine). KERN-01: the CEO's text is classified and departments selected without the CEO naming a tool. KERN-02: proof that a rule UPDATE changes routing with no code touch.
Output: kernel package with two modules + executed routing-data test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§2 LOCKED decisions rows 2-3 + §3 ClassifiedIntent block — copy schema EXACTLY)
@packages/shared/src/db.ts           (getDb/closeDb Kysely client — the only DB path)
</context>

## Artifacts this phase produces

**This plan:** `ClassifiedIntent` Zod schema (LOCKED, exported), `classify(text: string): Promise<ClassifiedIntent>`, `loadPolicy()`, `route(ci: ClassifiedIntent)` returning `{model_tier, model, mode, effort, needs_council}`, kernel index exports, `tests/phase5/routing-data.test.ts`.
**Phase-wide (context):** migration `routing_and_deps` (05-02); orchestrator `decompose()`/`dispatch()`/`runWorkerOnce()`/`escalate()`/`council()`/`qa()`; CLI `dxb intent`; personas `personas/product/*.md` ×5; tests ladder/council-judge/golden/slice-10of10; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: classify.ts — LOCKED ClassifiedIntent + Agent SDK classification</name>
  <files>packages/kernel/src/classify.ts, packages/kernel/src/index.ts, packages/kernel/tsconfig.json</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 "ClassifiedIntent + escalation (arayüz — LOCKED)" — copy the Zod object EXACTLY (field names, types, enums; no additions, no renames — schema changes are FABLE-ONLY per §6)
    - packages/kernel/src/index.ts + packages/kernel/tsconfig.json (scaffold state; wire exports/references without breaking build)
    - .planning/research/study-cards/claude-agent-sdk.md (query/headless call pattern from 05-01)
    - db/seed/import-personas.ts lines 32-79 (department slugs are kebab-cased dir names — the classifier prompt must offer the REAL 11 slugs from the departments table, not invented ones)
  </read_first>
  <action>
    classify.ts: export the LOCKED `ClassifiedIntent` Zod schema verbatim, plus `classify(text: string): Promise<ClassifiedIntent>`. The function: (1) reads the live department slugs from the departments table via @dxb/shared getDb; (2) resolves its OWN call parameters (model, effort) from the routing_rules row for task_class='orchestration' via policy.ts — the kernel's model choice is itself data; (3) calls the Agent SDK (subscription mode — no raw provider keys, no LiteLLM bypass) with a prompt that enumerates the legal task_class values (the DDL comment list plus 'final-approval' and 'planning' from the seed) and the live department slugs, demanding JSON only; (4) parses the reply with ClassifiedIntent.parse — a parse failure retries once with the validation error appended, then throws (no free-text fallback, LOCKED decision "serbest metin karar YOK"). approval_class from the LLM is advisory-only for outward risk: it can raise but code clamps it — if the text matches outward markers (send/publish/pay-class verbs), classify() forces at minimum 'internal' review; the Phase-4 approval/outbox rails remain the hard outward gate regardless.
    Export ClassifiedIntent + classify from packages/kernel/src/index.ts.
  </action>
  <verify><automated>pnpm build && node -e "const s=require('/home/ghost/DxB Global OS/packages/kernel/dist/index.js'); const r=s.ClassifiedIntent.safeParse({intent_summary:'x',task_class:'code.standard',departments:['engineering'],approval_class:'none',complexity:'single'}); process.exit(r.success?0:1)"</automated></verify>
  <acceptance_criteria>
    - ClassifiedIntent block in classify.ts is textually identical to the PHASE-05 §3 schema (five fields, exact enums)
    - ClassifiedIntent.safeParse rejects an object with approval_class='urgent' and accepts the legal sample (node evidence)
    - classify() contains no hardcoded model name — its call parameters come from route()/routing_rules
  </acceptance_criteria>
  <done>Deterministic seam in place: text goes in, schema-validated structure comes out, nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: policy.ts — routing_rules mapping engine</name>
  <files>packages/kernel/src/policy.ts, packages/kernel/src/index.ts</files>
  <read_first>
    - db/migrations/20260708000009_routing_and_deps.sql (column semantics: match jsonb, priority, enabled)
    - packages/shared/src/db-types.ts (RoutingRulesTable added in 05-02)
  </read_first>
  <action>
    policy.ts: `loadPolicy(db)` selects enabled routing_rules ordered priority DESC, updated_at DESC; `route(ci: ClassifiedIntent, rules)` returns the first rule whose task_class equals ci.task_class AND whose match jsonb (when non-empty) is satisfied — supported match keys now: `dept` (string, must be in ci.departments) and `keyword` (string, case-insensitive substring of ci.intent_summary); unknown match keys make the rule NON-matching (fail-closed, so future match fields can't silently over-match). Returns `{model_tier, model, mode, effort, needs_council}`. No caching in this phase (rules read per routing decision — table is tiny; Phase 8 edits must take effect immediately). Throws a typed NoRouteError when nothing matches — never a silent default model.
    Export loadPolicy/route from index.ts.
  </action>
  <verify><automated>pnpm build && pnpm vitest run tests/phase5/routing-data.test.ts 2>/dev/null || pnpm build</automated></verify>
  <acceptance_criteria>
    - route() picks the higher-priority row when two rows share a task_class (unit-asserted in Task 3)
    - Disabled rows (enabled=false) are never returned; no-match raises NoRouteError
    - grep evidence: policy.ts selects from routing_rules and contains no literal model-name fallback
  </acceptance_criteria>
  <done>Routing decisions are table lookups; the mapping engine only orders and matches.</done>
</task>

<task type="auto">
  <name>Task 3: routing-data.test.ts — 5 intents + the KERN-02 UPDATE proof</name>
  <files>tests/phase5/routing-data.test.ts</files>
  <read_first>
    - tests/phase3/lifecycle.test.ts (DB env default, sequential-file discipline, cleanup style)
    - .planning/master-plan/PHASE-05.md §4 step 3 verification row (this test IS that verification)
  </read_first>
  <action>
    tests/phase5/routing-data.test.ts, two groups:
    (a) classification: 5 sample intents (product-listing draft → product; code fix → engineering/code.standard; strategy question → strategy/L1-class; summarize request → summarize; multi-step campaign → complexity 'multi') each asserting expected task_class and departments. These hit the live Agent SDK — guard with the repo's established env-gated skip pattern (describe.skipIf) when no live session, BUT the plan's verification requires at least one recorded live run (master-plan step 3 is an executed check, not a type check).
    (b) KERN-02 routing-as-data proof (pure DB, never skipped): resolve route for a fixed ClassifiedIntent (task_class 'code.standard') → assert seeded model; `UPDATE routing_rules SET model='sonnet-5', updated_at=now() WHERE task_class='code.standard' AND priority=(highest)` → resolve again → assert model is now 'sonnet-5' with zero code/rebuild; restore the original row value in afterAll. Also assert priority-ordering and enabled=false exclusion here.
  </action>
  <verify><automated>pnpm vitest run tests/phase5/routing-data.test.ts</automated></verify>
  <acceptance_criteria>
    - 5 example intents → expected task_class/dept assertions present and green in the live run (master-plan step 3 verification, output pasted in SUMMARY)
    - UPDATE-row test green: same intent, different model after data change, original value restored (KERN-02 kanıtı)
    - Full `pnpm test` stays green (no cross-file DB pollution: test cleans its own mutations)
  </acceptance_criteria>
  <done>KERN-01 + KERN-02 have executed evidence; master-plan step 3 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CEO/CLI text → classify() LLM prompt | untrusted natural language becomes structured routing input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | prompt-injected intent text steering classification (e.g. demanding approval_class='none' for outward work) | high | mitigate | ClassifiedIntent.parse as hard boundary (illegal enums rejected); code-side outward-marker clamp in classify(); Phase-4 approval/outbox gates remain the outer wall — classification alone can never execute outward |
| T-05-06 | Spoofing | LLM inventing departments/task_classes | medium | mitigate | prompt enumerates live slugs + legal classes; route() fail-closed NoRouteError on unknown class |
| T-05-07 | Info disclosure | intent text logged with secrets | low | mitigate | classify writes no new logs; task/audit writes inherit Phase-3 redaction filter path |
</threat_model>

<verification>
- `pnpm vitest run tests/phase5/routing-data.test.ts` green in a live run; UPDATE proof output pasted; build green.
</verification>

<success_criteria>Kernel classifies real text into the LOCKED schema and routes purely from data; master-plan step 3 closed with both verification halves executed.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-03-SUMMARY.md with the 5-intent table and the before/after model values of the UPDATE proof.</output>
