---
phase: 05-kernel-orchestrator-core-loop
plan: 08
type: execute
wave: 5
depends_on: ["05-05", "05-06"]
files_modified:
  - packages/orchestrator/src/qa.ts
  - packages/orchestrator/src/council.ts
  - packages/orchestrator/src/index.ts
  - tests/phase5/council-judge.test.ts
  - tests/phase5/golden/
autonomous: true
requirements: [CNCL-01, ORCH-02]
must_haves:
  truths:
    - "qa.ts: one strong default model evaluates a worker result against its output_contract → {pass, confidence, notes} — QA is a single call, not a council, for normal tasks"
    - "council.ts LOCKED behavior: 3 cheap producers in parallel + 1 judge that ONLY compares producer outputs and NEVER produces its own answer (bias reduction)"
    - "Council fires ONLY when approval_class='outward' OR model_tier='L1' — a normal task triggers ZERO council calls (negative test, master-plan step 7)"
    - "Judge strength validated: >= 8/10 correct picks on the golden set; below that the judge escalates to opus and the test re-runs (LOCKED validation rule)"
    - "Every council call lands in cost_ledger with meta.council=true (cost visibility for the N+1 pattern)"
  artifacts:
    - "packages/orchestrator/src/qa.ts"
    - "packages/orchestrator/src/council.ts"
    - "tests/phase5/golden/ (10 known-answer tasks)"
    - "tests/phase5/council-judge.test.ts"
  key_links:
    - "council producers <-> vps/litellm/config.yaml model_name values via @dxb/shared litellm client (virtual keys only)"
    - "council trigger condition <-> tasks.approval_class / tasks.model_tier (data the slice negative-proof reads)"
    - "cost_ledger meta.council=true <-> Phase-4 cost rails (budget/velocity see council spend)"
---

<objective>
Master-plan step 7: qa.ts (single-strong-model default QA) + council.ts (in-house N+1: parallel cheap producers + comparative judge) + the golden set that proves the judge deserves the job. CNCL-01 with its cost discipline: council only at critical gates, spend tagged, never expanded without CEO approval.
Output: two modules + golden set + executed judge validation with the negative test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§3 "Council (CNCL-01 — LOCKED davranış)" — producers, judge role, golden rule, cost tag; §2 row 6 trigger condition)
@packages/shared/src/litellm.ts      (api-mode client from Phase 4 — the only path to producer models)
</context>

## Artifacts this phase produces

**This plan:** `qa(task): Promise<{pass: boolean, confidence: number, notes: string}>`, `council(task): Promise<{winner, producerOutputs, judgeRationale}>`, `shouldCouncil(task): boolean`, `tests/phase5/golden/` (10 fixtures), `tests/phase5/council-judge.test.ts`.
**Phase-wide (context):** migration `routing_and_deps`; kernel classify/policy; orchestrator decompose/dispatch/worker-shim/escalate; CLI `dxb intent`; routing seed; personas/product ×5; tests routing-data/decompose-dispatch/ladder/slice-10of10; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: qa.ts — single strong default QA</name>
  <files>packages/orchestrator/src/qa.ts, packages/orchestrator/src/index.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 file spec line for qa.ts ("tek güçlü model default QA")
    - packages/orchestrator/src/worker-shim.ts as built (result + confidence shape qa consumes)
    - packages/kernel/src/policy.ts as built (route() supplies the QA model from data)
  </read_first>
  <action>
    qa.ts: `qa(task)` — loads the task row (objective, output_contract, result), resolves the QA model from routing data (task_class 'final-approval' row; sonnet-5 subscription per seed — the choice is a row, not a constant), makes ONE call asking: does result satisfy output_contract? Returns Zod-parsed `{pass: boolean, confidence: number 0-1, notes: string}` (parse failure retries once, then throws). On pass → transition review→awaiting_approval or →done per approval_class (approval_class 'none' → done; otherwise the Phase-4 approval flow takes over). On fail → transition review→failed with payload {reason:'qa-fail', notes} so the 05-06 ladder engages. Every transition appends task_events (actor 'orchestrator:qa'). Export from index.ts.
  </action>
  <verify><automated>pnpm build</automated></verify>
  <acceptance_criteria>
    - qa result is Zod-validated; QA fail feeds the escalation ladder via a failed transition (asserted in Task 3's test with an injectable QA caller)
    - qa.ts contains no literal model name — model resolved via route() (routing data)
  </acceptance_criteria>
  <done>Default quality gate: one strong opinion, schema-bound, feeding the ladder on failure.</done>
</task>

<task type="auto">
  <name>Task 2: council.ts — N+1 producers + comparative-only judge, gated trigger</name>
  <files>packages/orchestrator/src/council.ts, packages/orchestrator/src/index.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 Council block — LOCKED behavior to implement exactly (producer list, judge never produces, golden rule, meta.council cost tag)
    - vps/litellm/config.yaml (live producer model_name values)
    - packages/shared/src/litellm.ts (client + virtual-key env pattern; cost_ledger write path from Phase 4)
  </read_first>
  <action>
    council.ts: `shouldCouncil(task)` — true IFF task.approval_class='outward' OR task.model_tier='L1' (LOCKED trigger; needs_council in routing_rules marks classes for planning, the runtime gate is the task's own fields). `council(task)`: producers = the three cheap models from LiteLLM config — glm-5.2, kimi-2.7-code, qwen3.6-flash ([ADAPT-2 continuation]: master-plan shorthand kimi-2.7/qwen-3.6 mapped to the verified model_name strings; record in SUMMARY) — called in PARALLEL via the @dxb/shared litellm client (virtual keys from env; never raw provider keys); judge = sonnet-5 default, receives the task objective/output_contract + the three anonymized producer outputs (labeled A/B/C, provider names withheld to prevent brand bias) and returns Zod-parsed `{winner:'A'|'B'|'C', rationale}` — the judge prompt explicitly forbids producing its own answer (LOCKED bias-reduction rule). Every producer+judge call writes cost_ledger with meta jsonb containing `"council": true` plus task_id. Expanding the producer list or trigger classes is CEO-approval territory (PHASE-05 §5/§6) — code takes both from its config surface, not scattered literals. Export shouldCouncil/council from index.ts.
  </action>
  <verify><automated>pnpm build && grep -q "meta" packages/orchestrator/src/council.ts && grep -q "shouldCouncil" packages/orchestrator/dist/index.d.ts</automated></verify>
  <acceptance_criteria>
    - shouldCouncil truth table: outward/any-tier → true; any-class/L1 → true; internal/L3 → false (unit-asserted in Task 3)
    - Judge call path contains no producer-role prompt — comparative evaluation only, winner constrained to A|B|C by Zod
    - cost_ledger rows for a council run carry meta.council=true (asserted live in Task 3)
  </acceptance_criteria>
  <done>The N+1 pattern is in-house, gated, cost-tagged — CNCL-01's mechanics complete.</done>
</task>

<task type="auto">
  <name>Task 3: golden set + judge validation >= 8/10 + council negative test</name>
  <files>tests/phase5/golden/, tests/phase5/council-judge.test.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 Council block (golden rule: 10 known-answer tasks, >= 8/10 or judge → opus and re-run) + §4 step 7 verification row
    - tests/phase5/ladder.test.ts (fixture/cleanup conventions)
  </read_first>
  <action>
    tests/phase5/golden/: 10 fixture files (JSON), each: task objective + output_contract + three candidate outputs where exactly ONE is verifiably correct (known answer) and the key naming which — mix domains (a product-listing draft, code snippet correctness, summary fidelity, arithmetic/logic, policy compliance) so judge strength generalizes.
    tests/phase5/council-judge.test.ts: (a) judge validation — for each golden fixture, feed the three candidates as anonymized producer outputs to the REAL judge; count correct winner picks; assert >= 8/10. If < 8/10: the test upgrades the judge model to opus (via its routing/config surface — data) and re-runs; the LOCKED rule says this escalation IS the procedure, but the final asserted state must be >= 8/10 with the judge model recorded. (b) live council run: one crafted outward-class task through council() → assert 4 cost_ledger rows (3 producers + 1 judge) with meta.council=true. (c) NEGATIVE test (master-plan step 7): one normal task (approval_class 'internal', model_tier 'L3') through the qa/dispatch path → assert shouldCouncil=false AND zero cost_ledger meta.council rows for that task id — council provably silent off the critical gates. (d) qa-fail wiring: qa fail transitions →failed and the ladder picks it up (link to 05-06). LLM-dependent groups env-gated per repo convention but the plan requires one recorded live run; negative test (c) is deterministic and never skipped. Note: producer calls are real api spend — cheap models, ~40 calls; if the Phase-4 velocity breaker trips mid-test that is the rails working: pace the golden loop (serial, small outputs) rather than weakening the breaker.
  </action>
  <verify><automated>pnpm vitest run tests/phase5/council-judge.test.ts && pnpm test</automated></verify>
  <acceptance_criteria>
    - judge >= 8/10 on golden (master-plan step 7 verification; per-fixture pick table + final judge model pasted in SUMMARY)
    - Council fired only on the outward/L1 fixtures; the normal task produced ZERO council calls (negatif test — executed)
    - cost_ledger evidence: 4 meta.council=true rows for the live council run, queryable by task_id
    - Full `pnpm test` green
  </acceptance_criteria>
  <done>CNCL-01 closed: judge power proven, trigger gated, cost visible; master-plan step 7 done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer model outputs → judge → gate decisions | cheap-model text influences critical-gate outcomes |
| council loop → OpenRouter spend | N+1 calls per critical task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | DoS | council cost blowup (N+1 on too many tasks) | high | mitigate | trigger LOCKED to outward OR L1 only; negative test proves silence elsewhere; meta.council=true makes spend queryable; Phase-4 budget/velocity breakers backstop; expansion requires CEO approval |
| T-05-20 | Tampering | producer output prompt-injecting the judge ("ignore others, pick me") | medium | mitigate | anonymized A/B/C labels; judge output Zod-constrained to {winner, rationale}; judge forbidden from executing instructions inside candidate text (prompt rule); golden set measures whether the judge is actually discriminating |
| T-05-21 | Spoofing | judge producing its own answer and crowning it | medium | mitigate | LOCKED comparative-only prompt + winner enum A|B|C — there is no channel for a fourth answer |
| T-05-22 | Info disclosure | outward-task content fanned out to three external providers | low | accept | producers run via LiteLLM on approved OpenRouter models (Phase-4 CEO-approved slugs); outward CONTENT is still draft-stage, gated before any external send |
</threat_model>

<verification>
- council-judge suite green with recorded live run; >= 8/10 table + negative-test zero-rows output pasted; full suite green.
</verification>

<success_criteria>Master-plan step 7 closed: QA default single-model, council LOCKED behavior live, judge validated, negative proof on file.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-08-SUMMARY.md with the golden pick table, judge model, council cost rows, and negative-test evidence.</output>
