---
phase: 05-kernel-orchestrator-core-loop
plan: 07
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - tools/dxb-cli/src/intent.ts
  - tools/dxb-cli/src/index.ts
  - tools/dxb-cli/package.json
  - tools/dxb-cli/tsconfig.json
autonomous: true
requirements: [KERN-01]
must_haves:
  truths:
    - "`dxb intent \"<text>\"` is the CEO's Phase-5 surface: text in → classify → route → (multi → decompose) → dispatch → task ids printed; the CEO names NO tool, model, or department"
    - "The master-plan step 8 command executed live: `dxb intent \"tek marka X için listeleme taslağı hazırla\"` leaves a task chain on the queue (SQL evidence)"
    - "CLI input is handled defensively: argument required, length-capped, passed as data (no shell re-interpolation), exit codes meaningful (0 queued, 1 classify/route failure)"
  artifacts:
    - "tools/dxb-cli/src/intent.ts (registered `intent` subcommand)"
  key_links:
    - "dxb intent <-> kernel classify()/route() <-> orchestrator decompose()/dispatch() (the whole KERN-01 front door)"
    - "slice-10of10.sh (05-09) invokes THIS command verbatim"
---

<objective>
Master-plan step 8: the `dxb intent` CLI — the first real KERN-01 surface. The CEO states intent once; kernel + orchestrator take it to queued, dependency-ordered tasks. Dashboard command bar and JARVIS voice reuse this exact seam later (Phases 8-9).
Output: working subcommand + live queued-chain evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§4 step 8 row — the verbatim command + verification)
@tools/dxb-cli/src/index.ts          (existing subcommand registration pattern: approve, breaker)
</context>

## Artifacts this phase produces

**This plan:** `dxb intent "<text>"` subcommand (`tools/dxb-cli/src/intent.ts`), @dxb/kernel + @dxb/orchestrator wired into the CLI package.
**Phase-wide (context):** migration `routing_and_deps`; kernel `ClassifiedIntent`/`classify()`/`loadPolicy()`/`route()`; orchestrator `decompose()`/`dispatch()`/`runWorkerOnce()`/`escalate()`/`council()`/`qa()`; routing seed; personas/product ×5; tests routing-data/decompose-dispatch/ladder/council-judge/golden/slice-10of10; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: intent.ts subcommand — classify → route → decompose → dispatch</name>
  <files>tools/dxb-cli/src/intent.ts, tools/dxb-cli/src/index.ts, tools/dxb-cli/package.json, tools/dxb-cli/tsconfig.json</files>
  <read_first>
    - tools/dxb-cli/src/index.ts + tools/dxb-cli/src/approve.ts (subcommand registration, arg parsing, exit-code conventions to replicate)
    - packages/kernel/src/index.ts + packages/orchestrator/src/index.ts as built (exact export names/signatures from 05-03/05-05)
    - tools/dxb-cli/package.json + tsconfig.json (add @dxb/kernel, @dxb/orchestrator workspace deps + project references without breaking `pnpm build`)
  </read_first>
  <action>
    intent.ts: `dxb intent "<text>"` — validate: exactly one non-flag argument, non-empty, length <= 2000 chars (reject longer with a clear error; intent is a sentence, not a document); the raw string is passed to classify() as data only — never interpolated into any shell command or SQL string (Kysely parameterization only). Flow: classify(text) → route(ci) → complexity 'single' ? single-envelope path : decompose(ci) → dispatch(envelopes) → print a human-readable line per task: id, department, model_tier, depends_on. Failure paths: classify/route/decompose errors print the reason and exit 1 (nothing queued); partial dispatch is impossible (05-05 transaction). Register the subcommand in index.ts alongside approve/breaker; add workspace deps + tsconfig references.
  </action>
  <verify><automated>pnpm build && node tools/dxb-cli/dist/index.js intent 2>&1 | grep -qi 'usage\|required'</automated></verify>
  <acceptance_criteria>
    - Missing argument → usage error, exit != 0; > 2000-char input → rejected with clear message, nothing queued
    - Build green with the new package wiring (workspace deps + references)
    - intent.ts contains no template-interpolated SQL and no child_process/shell usage on the input string
  </acceptance_criteria>
  <done>The front door exists, guarded, wired to the real kernel/orchestrator exports.</done>
</task>

<task type="auto">
  <name>Task 2: live evidence — the master-plan step 8 command queues a chain</name>
  <files>tools/dxb-cli/src/intent.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §4 step 8 (verbatim command + "görev zinciri kuyruğa düştü" verification)
  </read_first>
  <action>
    Execute live: `node tools/dxb-cli/dist/index.js intent "tek marka X için listeleme taslağı hazırla"` (the master-plan step 8 text, verbatim). Then prove the chain with SQL: SELECT id, department, status, model_tier, depends_on FROM tasks WHERE created_at > (run start) ORDER BY created_at — expect >= 1 row status 'queued' and, if the classifier chose 'multi', a coherent depends_on chain. Paste both the CLI stdout and the SQL rows into the SUMMARY. If classification routes the listing draft away from the product department, record it — 05-09 needs dept determinism and its plan carries the data-side lever (routing_rules match keyword pin, a rule row not a code change; any use is a recorded adaptation). Clean up the created evidence rows afterwards (the slice runs its own fresh chains in 05-09).
  </action>
  <verify><automated>node tools/dxb-cli/dist/index.js intent "tek marka X için listeleme taslağı hazırla" && test "$(psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -t -A -c "SELECT count(*) FROM tasks WHERE status='queued' AND created_at > now() - interval '2 minutes'")" -ge 1</automated></verify>
  <acceptance_criteria>
    - CLI exit 0 and printed task line(s); SQL shows the queued chain created by this run (master-plan step 8 verification, executed output pasted)
    - Observed classification (task_class + departments) recorded in SUMMARY for 05-09's determinism decision
  </acceptance_criteria>
  <done>Master-plan step 8 closed: intent text → queued task chain, no tool named by the human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal argv → kernel/orchestrator | arbitrary user text enters the LLM + queue pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16 | Tampering | injection via intent text (shell/SQL/prompt) | high | mitigate | argv treated as data (no shell interpolation, Kysely parameters only); length cap 2000; prompt-injection bounded by ClassifiedIntent/TaskEnvelope Zod (T-05-05/T-05-10) and Phase-4 outward gates |
| T-05-17 | DoS | scripted intent spam flooding the queue with LLM-decompose cost | medium | mitigate | CLI is local-operator-only in this phase (no network surface); Phase-4 budget/velocity breakers cap spend; queue depth visible via tasks table |
| T-05-18 | Repudiation | intents with no origin trace | low | mitigate | dispatch writes 'created' events actor 'orchestrator:dispatch'; CLI prints ids; audit trail via task_events |
</threat_model>

<verification>
- Live step-8 command executed with queued-chain SQL proof; guard behaviors executed; build green.
</verification>

<success_criteria>KERN-01's first real surface works end-to-end into the queue with executed evidence; master-plan step 8 closed.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-07-SUMMARY.md with CLI stdout + SQL chain rows + observed classification note.</output>
