---
phase: 05-kernel-orchestrator-core-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/20260708000009_routing_and_deps.sql
  - packages/shared/src/db-types.ts
  - packages/kernel/policy/routing-seed.json
  - db/seed/import-routing-rules.ts
autonomous: true
requirements: [KERN-02, ORCH-01]
must_haves:
  truths:
    - "routing_rules table exists with LOCKED DDL (byte-faithful to PHASE-05 §3) and RLS enabled; tasks has depends_on uuid[] NOT NULL DEFAULT '{}'"
    - "claim_next_task is replaced: it never returns a task whose depends_on contains a non-done task (proven live with a 2-task chain)"
    - "Routing policy lives as DATA: >= 8 seeded rows from routing-seed.json land in routing_rules after a full reset+seed — zero rules in code"
    - "Migration applied to the LIVE local database before any dependent plan runs (schema push is blocking, not optional)"
  artifacts:
    - "db/migrations/20260708000009_routing_and_deps.sql (LOCKED SQL verbatim)"
    - "packages/kernel/policy/routing-seed.json (>= 8 brain-map rows)"
    - "db/seed/import-routing-rules.ts (idempotent seeder)"
    - "packages/shared/src/db-types.ts RoutingRulesTable + tasks.depends_on typing"
  key_links:
    - "routing-seed.json api-mode model strings <-> vps/litellm/config.yaml model_name values (verified names, no guessing)"
    - "claim_next_task dependency clause <-> tasks.depends_on column (same migration, atomic)"
---

<objective>
Master-plan step 2: land migration `routing_and_deps` (routing_rules + tasks.depends_on + dependency-aware claim_next_task — LOCKED SQL verbatim), render the §10 brain map into routing-seed.json, seed it idempotently, and PUSH the schema to the live local database with executed proof. KERN-02's substrate: routing policy becomes rows, never code.
Output: applied migration + >= 8 live routing rows + dependency-claim behavior proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§3 LOCKED SQL "0008_routing_and_deps.sql" + seed lines — copy SQL EXACTLY)
@db/migrations/20260707000001_operational_core.sql   (function being replaced; naming/format conventions)
</context>

## Artifacts this phase produces

**This plan:** table `routing_rules` (RLS on), column `tasks.depends_on uuid[]`, replaced fn `claim_next_task(p_worker_id, p_departments, p_lease_seconds)`, `packages/kernel/policy/routing-seed.json`, `db/seed/import-routing-rules.ts`, `RoutingRulesTable` in db-types.
**Phase-wide (context):** kernel `ClassifiedIntent`/`classify()`/`loadPolicy()`/`route()`; orchestrator `decompose()`/`dispatch()`/`runWorkerOnce()`/`escalate()`/`council()`/`qa()`; CLI `dxb intent`; `db/seed/apply-persona-v2.ts`; personas `personas/product/*.md` ×5; tests `tests/phase5/{routing-data,decompose-dispatch,ladder,council-judge}.test.ts` + `golden/` ×10 + `slice-10of10.sh`; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: migration 20260708000009_routing_and_deps.sql (LOCKED SQL verbatim) + db-types</name>
  <files>db/migrations/20260708000009_routing_and_deps.sql, packages/shared/src/db-types.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 "0008_routing_and_deps.sql (birebir — LOCKED)" — the ENTIRE SQL block is copied byte-faithful
    - db/migrations/20260707000008_revoke_truncate.sql (latest migration — confirms timestamp naming + that logical slot 0008 is occupied)
    - packages/shared/src/db-types.ts (interface style: 1:1 mirror, no codegen; TasksTable to extend)
  </read_first>
  <action>
    Create the migration with the PHASE-05 §3 SQL block verbatim (CREATE TABLE routing_rules with all columns/CHECKs/defaults, ALTER TABLE routing_rules ENABLE ROW LEVEL SECURITY, ALTER TABLE tasks ADD COLUMN depends_on, CREATE OR REPLACE FUNCTION claim_next_task with the NOT EXISTS dependency clause). Add NO policies to routing_rules (RLS default-deny for anon/authenticated; only service_role/backend writes — same posture as the existing 13 tables).
    [ADAPT-1, recorded]: master plan names this file `0008_routing_and_deps.sql`; logical slot 0008 was consumed by Phase 4's `revoke_truncate` hardening migration, and the repo uses Supabase CLI timestamp naming (03-02 precedent). Filename is `20260708000009_routing_and_deps.sql`; SQL CONTENT is unchanged. Record in SUMMARY "Deviations (recorded)".
    Update packages/shared/src/db-types.ts: add `RoutingRulesTable` (id, task_class, match, model_tier, model, mode, effort, needs_council, priority, enabled, updated_at) mirroring the DDL 1:1; add `depends_on: string[]` to TasksTable; register `routing_rules` in the `DB` interface.
  </action>
  <verify><automated>grep -q 'CREATE TABLE routing_rules' db/migrations/20260708000009_routing_and_deps.sql && grep -q 'ADD COLUMN depends_on' db/migrations/20260708000009_routing_and_deps.sql && grep -q 'NOT EXISTS (SELECT 1 FROM tasks d' db/migrations/20260708000009_routing_and_deps.sql && grep -q 'RoutingRulesTable' packages/shared/src/db-types.ts && pnpm build</automated></verify>
  <acceptance_criteria>
    - Extracted SQL region (CREATE TABLE routing_rules → `$$ LANGUAGE sql;`) diffs EMPTY against the PHASE-05 §3 block (executor runs the diff and pastes the empty result)
    - db-types compiles: RoutingRulesTable present, TasksTable has depends_on, DB map includes routing_rules; `pnpm build` exit 0
    - ADAPT-1 filename note staged for SUMMARY
  </acceptance_criteria>
  <done>Schema authority extended with the LOCKED routing/dependency migration; types mirror it 1:1.</done>
</task>

<task type="auto">
  <name>Task 2: routing-seed.json (brain map as data) + idempotent seeder</name>
  <files>packages/kernel/policy/routing-seed.json, db/seed/import-routing-rules.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 "Seed satırları" paragraph — the 8 brain-map lines to render as rows
    - vps/litellm/config.yaml — VERIFIED api-mode model_name values (glm-5.2, kimi-2.7-code, deepseek-v4-flash, qwen3.6-flash, minimax-m3); api rows MUST use these exact strings
    - db/seed/import-personas.ts — seeder conventions to replicate (node --experimental-strip-types, pg via @dxb/shared createRequire, idempotency, count output)
  </read_first>
  <action>
    Render the §3 seed lines into routing-seed.json rows (fields exactly matching the DDL: task_class, match, model_tier, model, mode, effort, needs_council, priority). One row per task_class/model pairing; where a seed line lists alternatives (code.standard codex-5.5|sonnet-5; code.bulk; summarize), emit one row per model with descending priority so fallback order is data, not code. `needs_council: true` ONLY where the seed lines mark it (the L1 strategy/architecture/final-approval group and content.outbound) — marking any other row needs_council requires CEO approval (LOCKED cost decision, PHASE-05 §5).
    [ADAPT-2, recorded]: api-mode model strings use the LiteLLM-verified names where master-plan shorthand differs (kimi-2.7 → kimi-2.7-code; qwen → qwen3.6-flash; deepseek-v4 → deepseek-v4-flash; minimax-3 → minimax-m3). [ADAPT-3, recorded]: seed line effort "medium-high" is not a legal enum value (CHECK low|medium|high|max) — the orchestration row uses effort 'high'. Both go in SUMMARY "Deviations (recorded)".
    Write db/seed/import-routing-rules.ts: reads routing-seed.json, inserts idempotently (skip when a row with same task_class+model+priority exists), prints `routing rules: inserted N / total M`. Header comment: adding L1 rows to this seed is FABLE-ONLY (PHASE-05 §6).
  </action>
  <verify><automated>node -e "const r=require('/home/ghost/DxB Global OS/packages/kernel/policy/routing-seed.json'); if(!Array.isArray(r)||r.length<8)process.exit(1); const names=['glm-5.2','kimi-2.7-code','deepseek-v4-flash','qwen3.6-flash','minimax-m3']; process.exit(r.filter(x=>x.mode==='api'&&!names.includes(x.model)).length?1:0)"</automated></verify>
  <acceptance_criteria>
    - routing-seed.json has >= 8 rows; every api-mode row's model string exists as a model_name in vps/litellm/config.yaml (node check exit 0)
    - needs_council=true appears ONLY on the L1 strategy/architecture/final-approval rows and content.outbound
    - Seeder re-run prints `inserted: 0` on second execution (idempotency evidence, executed)
  </acceptance_criteria>
  <done>The §10 brain map is repo data feeding a table — no rule strings in TypeScript.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] schema push + dependency-claim live proof</name>
  <files>db/migrations/20260708000009_routing_and_deps.sql</files>
  <read_first>
    - .planning/phases/03-state-layer-dxb-mcp-core/03-02-SUMMARY.md — reset/seed sequence precedent (reset wipes seeds; re-seed after)
  </read_first>
  <action>
    Apply the schema to the LIVE local database — this task is BLOCKING for every dependent plan; type-checks passing without it is a false-positive state. Sequence: `supabase db reset` (all 9 migrations from scratch) → re-run `db/seed/import-personas.ts` → run `db/seed/import-routing-rules.ts` → prove counts and behavior via psql on postgresql://postgres:postgres@127.0.0.1:54322/postgres.
    Dependency-claim proof (executed, then cleaned up): INSERT task A (status queued, department engineering) and task B (status queued, depends_on = {A.id}); call `SELECT id FROM claim_next_task('proof-worker','{engineering}'::text[], 5)` — must return A, never B; mark A done; second claim call now returns B; DELETE both proof tasks afterwards.
  </action>
  <verify><automated>supabase db reset && node --experimental-strip-types db/seed/import-personas.ts && node --experimental-strip-types db/seed/import-routing-rules.ts && test "$(psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -t -A -c 'SELECT count(*) FROM routing_rules')" -ge 8</automated></verify>
  <acceptance_criteria>
    - `supabase db reset` exit 0 with all nine migrations applied (master-plan step 2 verification)
    - `SELECT count(*) FROM routing_rules` >= 8 (master-plan step 2 verification, executed output pasted)
    - `\df claim_next_task` lists the function; claim on the 2-task chain returns the dependency-free task first, the dependent task only after its dependency is done (psql output pasted in SUMMARY)
    - `SELECT count(*) FROM agents` = 153 after re-seed (reset did not silently lose the persona corpus)
  </acceptance_criteria>
  <done>Live database carries the Phase-5 schema; dependency-aware claim proven with observed rows, not types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routing_rules writers → kernel routing | whoever can UPDATE routing_rules controls which model/tier runs every task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | routing_rules UPDATE authority | high | mitigate | RLS enabled with NO policies (anon/authenticated default-deny); writes only via service_role backend/psql; Phase 8 dashboard will add scoped policies — none now |
| T-05-03 | Elevation | seed rows granting L1/council to cheap task classes | medium | mitigate | needs_council + L1 rows byte-checked against §3 seed lines in acceptance; L1 seed additions FABLE-ONLY (§6) |
| T-05-04 | DoS | dependency cycles in depends_on making tasks unclaimable forever | low | accept | decompose (05-05) only emits forward chains; reaper + escalation surface stuck tasks; cycle CHECK deferred unless observed |
</threat_model>

<verification>
- Reset + seeds + count >= 8 executed live; dependency-claim proof rows observed via psql; build green.
</verification>

<success_criteria>Master-plan step 2 closed: LOCKED migration applied, brain map seeded as data, dependency-aware claim proven on the live database.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-02-SUMMARY.md with the executed psql outputs and the ADAPT-1/2/3 deviations recorded.</output>
