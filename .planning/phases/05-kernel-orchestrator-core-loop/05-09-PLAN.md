---
phase: 05-kernel-orchestrator-core-loop
plan: 09
type: execute
wave: 6
depends_on: ["05-03", "05-04", "05-05", "05-06", "05-07", "05-08"]
files_modified:
  - tests/phase5/slice-10of10.sh
  - .planning/phases/05-kernel-orchestrator-core-loop/05-VERIFICATION.md
autonomous: true
requirements: [KERN-01, KERN-02, ORCH-01, ORCH-02, ORCH-03, ORCH-04, CNCL-01]
must_haves:
  truths:
    - "The vertical slice — intent → kernel → worker → QA → approval → recorded result — passes 10 OUT OF 10 repeated runs (I9 exit gate; 9/10 is NEVER accepted, LOCKED)"
    - "The gate physically refuses to run unless every agent in the slice department carries persona_version='v2.0-fable' (criterion 6 enforced in the script, not on the honor system)"
    - "The slice is outward-facing NOWHERE: its outbox action is test.write_file (LOCKED slice definition — Outleteuro P11 precursor, no external side effect)"
    - "Each run leaves a machine-reconstructable trail: task done + complete task_events chain + written artifact file + audit trace; council stayed silent on the slice's normal tasks (negative proof inside the gate)"
    - "05-VERIFICATION.md records the 10/10 output line by line; the gate verdict is FABLE-ONLY"
  artifacts:
    - "tests/phase5/slice-10of10.sh (repeatable gate script, exit 0 only at 10/10)"
    - ".planning/phases/05-kernel-orchestrator-core-loop/05-VERIFICATION.md"
  key_links:
    - "slice script <-> `dxb intent` CLI (05-07, verbatim master-plan text) <-> worker shim (05-05) <-> qa (05-08) <-> Phase-4 approval/outbox test.write_file"
    - "persona precondition query <-> agents rows flipped by 05-04"
---

<objective>
Master-plan steps 9+10 — the phase exit gate: `tests/phase5/slice-10of10.sh` drives the full loop (CEO intent text → classify → decompose → queue → worker → QA → approval → outbox test.write_file → recorded result) ten times; all ten must pass. Then 05-VERIFICATION.md captures every criterion with executed evidence and the FABLE-ONLY verdict closes the phase. If any run fails: narrow the slice (single department, single worker — master-plan §5 fallback), find root cause via /gsd-debug — never weaken the 10/10 bar.
Output: the gate script + 10/10 executed evidence + phase verification document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§1 gate criteria 1-6, §4 steps 9-10, §5 fallback rules, §6 FABLE-ONLY verdict)
@.planning/phases/04-safety-rails-gates-cost-audit/04-VERIFICATION.md   (verification-document format + two-tier evidence discipline to replicate)
</context>

## Artifacts this phase produces

**This plan:** `tests/phase5/slice-10of10.sh`, `05-VERIFICATION.md` (gate evidence + verdict).
**Phase-wide (complete ledger):** tables `routing_rules`, `tasks.depends_on uuid[]`; SQL fn `claim_next_task` (dependency-aware replacement); kernel `ClassifiedIntent` (Zod), `classify()`, `loadPolicy()`, `route()`; orchestrator `decompose()`, `dispatch()`, `runWorkerOnce()`, `escalate()`, `failCount()`, `qa()`, `council()`, `shouldCouncil()`; CLI `dxb intent`; seeds `packages/kernel/policy/routing-seed.json`, `db/seed/import-routing-rules.ts`, `db/seed/apply-persona-v2.ts`; personas `personas/product/{product-manager,product-trend-researcher,product-feedback-synthesizer,product-sprint-prioritizer,product-behavioral-nudge-engine}.md` (all `v2.0-fable`); tests `tests/phase5/routing-data.test.ts`, `tests/phase5/decompose-dispatch.test.ts`, `tests/phase5/ladder.test.ts`, `tests/phase5/council-judge.test.ts`, `tests/phase5/golden/` ×10, `tests/phase5/slice-10of10.sh`; migration `db/migrations/20260708000009_routing_and_deps.sql`; closure `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: slice-10of10.sh — the gate as a script</name>
  <files>tests/phase5/slice-10of10.sh</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §2 row 8 (LOCKED slice: "tek marka ürün listeleme taslağı", non-outward, outbox closes via test.write_file) + §4 step 9 verification row
    - .planning/phases/05-kernel-orchestrator-core-loop/05-07-SUMMARY.md (observed classification of the slice text — determinism input)
    - tests/phase4/gate-canary.test.ts (approval→outbox→test.write_file mechanics + ceo:cli approve path to reuse)
    - tools/dxb-cli/src/approve.ts (programmatic CEO-surrogate approval surface)
  </read_first>
  <action>
    Write tests/phase5/slice-10of10.sh (bash, set -euo pipefail). Structure:
    PRECONDITIONS (abort with named reason if any fail): Supabase up; routing_rules count >= 8; persona gate — `SELECT count(*) FROM agents WHERE department='product' AND persona_version <> 'v2.0-fable'` must be 0 AND v2 count = 5 (criterion 6: the gate CANNOT run without the Fable v2 batch); LiteLLM up only if the run needs api-tier calls.
    LOOP i=1..10, each iteration: (1) `dxb intent "tek marka X için listeleme taslağı hazırla"` (verbatim step-8 text) and capture created task ids; (2) drive workers: repeatedly invoke the worker-shim runner for the slice departments until the chain's tasks leave queued/claimed/running (bounded loop with timeout — a stall is a FAIL, not a wait-forever); QA runs per 05-08 wiring; escalation (05-06) may fire — a run that completes via the ladder still counts as pass only if the final state is done; (3) approval leg: the awaiting_approval task's draft is finalized and approved via the ceo:cli surface (script is the CEO surrogate — same posture as the Phase-4 flip test), outbox action test.write_file executes; (4) ASSERT per run: every task in the chain status 'done'; task_events chain complete for each (created→claimed→running→review→…→done); artifact file exists and is non-empty; audit trace reconstructs the causal chain; ZERO cost_ledger rows with meta.council=true for the chain's task ids (council-silent negative proof — the slice is internal/non-L1); (5) cleanup per iteration (files + rows) so run N+1 starts clean.
    OUTPUT: per-run PASS/FAIL line `run i/10: PASS (chain=N tasks, events=M, artifact=path)`; exit 0 IFF 10/10. Any FAIL → exit 1 immediately with the failing assertion named. Determinism lever (recorded if used): if classification wanders departments across runs, pin via a routing_rules match keyword row (data change — [ADAPT-4] in SUMMARY) or narrow the slice per §5 — the 10/10 bar itself NEVER changes.
  </action>
  <verify><automated>bash -n tests/phase5/slice-10of10.sh && test -x tests/phase5/slice-10of10.sh</automated></verify>
  <acceptance_criteria>
    - Script aborts (non-zero, named reason) when the persona precondition query finds any non-v2 product agent — proven once by temporarily flipping one row back and restoring it
    - Per-run assertions cover: done status, full event chain, artifact file, audit trace, zero council rows
    - Exit code contract: 0 only at 10/10; first failure exits 1 with the run number and assertion
  </acceptance_criteria>
  <done>The exit gate is executable, honest, and refuses to run without its LOCKED preconditions.</done>
</task>

<task type="auto">
  <name>Task 2: run the gate 10/10 + 05-VERIFICATION.md + FABLE-ONLY verdict</name>
  <files>.planning/phases/05-kernel-orchestrator-core-loop/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-safety-rails-gates-cost-audit/04-VERIFICATION.md (format: criterion → executed command → decisive output, two-tier ✓/⚠ discipline)
    - .planning/master-plan/PHASE-05.md §1 (all six gate criteria to evidence one by one)
  </read_first>
  <action>
    Execute `bash tests/phase5/slice-10of10.sh` for the record. If it exits 1: STOP, root-cause via /gsd-debug, fix in the owning plan's domain, re-run from run 1 — partial credit does not exist (LOCKED: never "9/10 yeterli"). When exit 0: write 05-VERIFICATION.md with all six §1 criteria, each with its executed command and decisive output line: (1) KERN-01 — dxb intent run, CEO named no tool (CLI stdout); (2) KERN-02 — routing-data UPDATE proof (05-03 test output); (3) ORCH-01/ORCH-04 — envelope chain + isolation evidence (05-05 event chains); (4) ORCH-02/ORCH-03/CNCL-01 — tier map spot-check, ladder proof (05-06), judge >= 8/10 + council negative test (05-08); (5) the 10/10 slice output pasted LINE BY LINE (master-plan step 10: "10/10 çıktısı VERIFICATION.md'de satır satır"); (6) persona precondition query output (5 × v2.0-fable). Anything outside terminal observability is labeled ⚠ UNVERIFIED explicitly. Close the file with the ⛔ FABLE-ONLY gate verdict block (the verdict itself is Fable's, per §6 and governance v4). After verification passes, follow phase-completion protocol: /gsd-graphify build refresh is the established closure step (project rule), and STATE/ROADMAP advancement happens in phase closure, not in this plan.
  </action>
  <verify><automated>bash tests/phase5/slice-10of10.sh && grep -c 'PASS' .planning/phases/05-kernel-orchestrator-core-loop/05-VERIFICATION.md | awk '{exit ($1>=10)?0:1}'</automated></verify>
  <acceptance_criteria>
    - `tests/phase5/slice-10of10.sh` exit 0 with ten `run i/10: PASS` lines (master-plan step 9 verification, executed)
    - 05-VERIFICATION.md contains the ten run lines verbatim + evidence for all six §1 criteria, two-tier labeled
    - Verdict block present and authored by Fable (⛔ FABLE-ONLY)
  </acceptance_criteria>
  <done>I9 exit gate passed and documented; Phase 5 is closable on evidence, not intention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script → phase closure decision | the script's honesty decides whether an autonomous loop is declared safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-23 | Repudiation | gate theater: script asserting less than the gate means (e.g. counting queued as done) | high | mitigate | per-run assertions enumerate done+events+artifact+audit+council-silence explicitly; VERIFICATION pastes raw output; verdict FABLE-ONLY |
| T-05-24 | Tampering | slice accidentally gaining outward reach | high | mitigate | LOCKED slice definition: outbox action test.write_file only; Phase-4 gate-canary suite still required in `pnpm test`; approval leg goes through the real one-way state machine |
| T-05-25 | DoS | 10× loop cost (LLM calls per run) | low | accept | slice is small, mostly subscription-mode; Phase-4 breakers backstop; cost visible in cost_ledger per run |
</threat_model>

<verification>
- slice-10of10.sh exit 0 (executed); VERIFICATION contains ten PASS lines + six-criteria evidence; persona gate proven blocking once.
</verification>

<success_criteria>Master-plan steps 9-10 closed: 10/10 vertical slice with line-by-line evidence, persona precondition enforced, FABLE verdict recorded. Phase 5 exit gate (I9) passed.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-09-SUMMARY.md with the gate output, any [ADAPT-4] determinism lever use, and the closure handoff note.</output>
