---
phase: 05-kernel-orchestrator-core-loop
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - packages/orchestrator/src/escalate.ts
  - packages/orchestrator/src/index.ts
  - tests/phase5/ladder.test.ts
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "Escalation is CODE, not an LLM decision: fail_count derives ONLY from task_events (event='transition' AND to_status='failed') — no separate counter table exists (LOCKED)"
    - "The LOCKED ladder runs exactly: 0→first-assignment tier | 1→same-tier retry | 2→specialist (one tier up) | 3→head review (L2) | 4→Fable final (L1) | beyond→hard stop with a 'blocked' report to the CEO — never a silent drop"
    - "A worker result with confidence < 0.6 counts as a fail (LOCKED low-confidence rule)"
    - "Proven live: worker fails 2× → the THIRD claim serves the task at a higher tier, with task_events as the evidence (master-plan step 6 verification)"
  artifacts:
    - "packages/orchestrator/src/escalate.ts"
    - "tests/phase5/ladder.test.ts"
  key_links:
    - "fail_count query <-> task_events append-only log (single source of truth)"
    - "tier bump <-> tasks.model_tier + requeue (status failed→queued) that claim_next_task serves"
---

<objective>
Master-plan step 6: escalate.ts — the ladder as deterministic TypeScript over the event log. ORCH-03: worker fails 2× or reports low confidence → specialist retry → head review → Fable final → blocked report. Infinite-retry cost abuse is structurally impossible: fail_count is monotonic and the ladder hard-stops after 4.
Output: escalate module + executed ladder test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-05.md   (§3 "escalate.ts kuralı" comment block — the LOCKED ladder; §5 risk 2 hard stop)
@.planning/phases/05-kernel-orchestrator-core-loop/05-05-SUMMARY.md   (worker-shim fail/confidence surface as built)
</context>

## Artifacts this phase produces

**This plan:** `escalate(taskId)` — computes fail_count from task_events, applies the LOCKED ladder step, requeues at the new tier or emits the blocked report; `failCount(taskId)` helper; `tests/phase5/ladder.test.ts`.
**Phase-wide (context):** migration `routing_and_deps`; kernel classify/policy; orchestrator decompose/dispatch/worker-shim/council/qa; CLI `dxb intent`; routing seed; personas/product ×5; tests routing-data/decompose-dispatch/council-judge/golden/slice-10of10; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: escalate.ts — the LOCKED ladder over task_events</name>
  <files>packages/orchestrator/src/escalate.ts, packages/orchestrator/src/index.ts</files>
  <read_first>
    - .planning/master-plan/PHASE-05.md §3 escalation comment block — implement the mapping EXACTLY (rule changes are FABLE-ONLY per §6)
    - packages/orchestrator/src/worker-shim.ts as built (how failure transitions and confidence payloads are written)
    - db/migrations/20260707000001_operational_core.sql (task_events columns; tasks.feedback for the blocked report path)
  </read_first>
  <action>
    escalate.ts: `failCount(db, taskId)` = COUNT of task_events rows WHERE task_id=$1 AND event='transition' AND to_status='failed'. Low-confidence rule: worker-shim (05-05) writes review transitions with payload {confidence}; escalate treats a review event with confidence < 0.6 as a fail by transitioning that task →failed with payload {reason:'low-confidence'} BEFORE counting — so the count stays derived from events only (no shadow state).
    `escalate(db, taskId)`: n = failCount → n=1: requeue same tier (status failed→queued, event payload {ladder:'retry-same-tier'}); n=2: bump model_tier one level (L4→L3→L2), requeue, payload {ladder:'specialist'}; n=3: set tier L2, requeue for head review, payload {ladder:'head-review'}; n=4: set tier L1, requeue, payload {ladder:'fable-final'}; n>=5: HARD STOP — no requeue; tasks.feedback = human-readable blocked report (task id, objective, 5 failure summaries), one audit_log row actor 'orchestrator:escalate' action 'task.blocked' — the CEO-visible "never a silent drop" artifact. All transitions append task_events (actor 'orchestrator:escalate'). Pure functions where possible: ladder step decision is a pure `(n) => LadderAction` map, unit-testable without DB. Export escalate/failCount from index.ts. No new tables, no new migration (LOCKED: counters derive from events).
  </action>
  <verify><automated>pnpm build && test "$(ls db/migrations | wc -l)" -eq 9</automated></verify>
  <acceptance_criteria>
    - The `(n) => action` map matches the §3 comment line-for-line (0/1/2/3/4/beyond) — reviewer can diff against PHASE-05
    - Migration count is still 9 (no counter table was created)
    - Low-confidence path converts confidence<0.6 review into a failed transition with reason payload
  </acceptance_criteria>
  <done>Ladder is auditable code over the append-only log — nothing for an LLM to decide.</done>
</task>

<task type="auto">
  <name>Task 2: ladder.test.ts — 2× fail → higher-tier third claim + hard stop</name>
  <files>tests/phase5/ladder.test.ts</files>
  <read_first>
    - tests/phase5/decompose-dispatch.test.ts (injectable-executor pattern, cleanup conventions)
    - .planning/master-plan/PHASE-05.md §4 step 6 verification row (this test IS that verification)
  </read_first>
  <action>
    tests/phase5/ladder.test.ts (pure DB + injectable failing executors — deterministic, no LLM, never skipped):
    (a) master-plan proof: dispatch one L4 envelope; runWorkerOnce with an executor that throws → escalate; repeat (2 fails total) → escalate → assert the task is queued at a HIGHER tier and the third claim_next_task returns it with the bumped model_tier; assert the full ladder story is readable from task_events alone (events output pasted in SUMMARY).
    (b) low-confidence: executor returns {confidence: 0.4} → assert escalate converts it to failed(reason low-confidence) and the retry ladder engages.
    (c) hard stop: drive the same task to 5 fails → assert NO queued row remains for it, tasks.feedback contains the blocked report, audit_log has the task.blocked row, and a further escalate() call is a no-op (monotonic, no infinite loop — §5 risk 2).
    Clean up rows afterAll.
  </action>
  <verify><automated>pnpm vitest run tests/phase5/ladder.test.ts && pnpm test</automated></verify>
  <acceptance_criteria>
    - Worker 2× fail → third claim serves the task at the higher tier, proven by task_events (master-plan step 6 verification, executed)
    - Hard stop after fail 5: zero requeue, blocked report present in tasks.feedback + audit_log (executed)
    - Full `pnpm test` stays green
  </acceptance_criteria>
  <done>ORCH-03 has executed evidence including the abuse-stop; master-plan step 6 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failing tasks → escalating model cost | each ladder step buys a more expensive model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | DoS | escalation-ladder cost abuse: crafted always-failing tasks climbing to L1 repeatedly | high | mitigate | fail_count monotonic from append-only events; hard stop at 5 (no requeue ever after); Phase-4 budget/velocity breakers cap spend independently; blocked report makes the pattern CEO-visible |
| T-05-14 | Tampering | forging events to reset fail_count | medium | mitigate | task_events is append-only (UPDATE/DELETE revoked, Phase 3) — count can rise, never fall |
| T-05-15 | Repudiation | silent task death (dropped without trace) | medium | mitigate | every ladder action is an event; terminal path REQUIRES feedback report + audit row (asserted in test c) |
</threat_model>

<verification>
- ladder.test.ts green (deterministic, executed); events narrative pasted; migration count unchanged; full suite green.
</verification>

<success_criteria>ORCH-03 closed with the LOCKED ladder proven live, including hard stop and low-confidence conversion.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-06-SUMMARY.md with the ladder event-chain output.</output>
