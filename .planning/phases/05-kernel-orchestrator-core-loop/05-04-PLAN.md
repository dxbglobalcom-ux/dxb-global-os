---
phase: 05-kernel-orchestrator-core-loop
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - personas/product/product-manager.md
  - personas/product/product-trend-researcher.md
  - personas/product/product-feedback-synthesizer.md
  - personas/product/product-sprint-prioritizer.md
  - personas/product/product-behavioral-nudge-engine.md
  - db/seed/apply-persona-v2.ts
autonomous: true
requirements: [KERN-01]
must_haves:
  truths:
    - "The slice department (product) has a COMPLETE Fable-authored v2 persona set: all 5 product agents have persona files under personas/product/ (criterion 6 precondition — the 10/10 gate cannot run without this)"
    - "Registry truth updated: all 5 product rows in agents carry persona_version='v2.0-fable' and persona_path pointing at personas/product/<slug>.md"
    - "The v2 flip survives `supabase db reset` — apply-persona-v2.ts is a re-runnable seed-stage script, not a one-off UPDATE"
    - "Legacy agency-agents/product/ files are untouched (READ-ONLY reference per the Persona v2 Programı)"
  artifacts:
    - "personas/product/{product-manager,product-trend-researcher,product-feedback-synthesizer,product-sprint-prioritizer,product-behavioral-nudge-engine}.md"
    - "db/seed/apply-persona-v2.ts (idempotent registry updater)"
  key_links:
    - "agents.persona_path <-> personas/product/<slug>.md files on disk (registry never points at a missing file)"
    - "slice-10of10.sh precondition check (05-09) <-> persona_version='v2.0-fable' rows written here"
---

<objective>
Persona v2 first batch (PHASE-05 criterion 6, CEO 2026-07-07): the vertical slice runs in the product department, so its 5 personas are reborn as Fable-authored v2 files under personas/product/, and the registry is flipped to v2.0-fable via a reset-survivable script. Program definition: PHASE-03 "Persona v2 Programı". ⛔ FABLE-ONLY: the final persona text is authored by Fable personally — this plan may not be delegated to a cheaper executor.
Output: 5 v2 persona files + registry rows proven flipped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-03.md   ("Persona v2 Programı" section — file location, version tag, legacy read-only rule)
@.planning/master-plan/PHASE-05.md   (§1 criterion 6 — precondition wording)
</context>

## Artifacts this phase produces

**This plan:** `personas/product/` directory (Fable-owned) with 5 v2 persona files; `db/seed/apply-persona-v2.ts`; registry rows `agents.persona_version='v2.0-fable'` + updated `persona_path` for department='product'.
**Phase-wide (context):** migration `routing_and_deps`; kernel `ClassifiedIntent`/`classify()`/`loadPolicy()`/`route()`; orchestrator `decompose()`/`dispatch()`/`runWorkerOnce()`/`escalate()`/`council()`/`qa()`; CLI `dxb intent`; routing seed + seeder; tests routing-data/decompose-dispatch/ladder/council-judge/golden/slice-10of10; `05-VERIFICATION.md`.

<tasks>

<task type="auto">
  <name>Task 1: author 5 product v2 personas (⛔ FABLE-ONLY text)</name>
  <files>personas/product/product-manager.md, personas/product/product-trend-researcher.md, personas/product/product-feedback-synthesizer.md, personas/product/product-sprint-prioritizer.md, personas/product/product-behavioral-nudge-engine.md</files>
  <read_first>
    - agency-agents/product/*.md (all 5 legacy files — raw material and role intent; READ-ONLY, do not edit)
    - .planning/master-plan/PHASE-03.md "Persona v2 Programı" (v2 rules: personas/<dept>/<slug>.md, persona_version tag, legacy stays)
    - packages/shared/src/envelope.ts (TaskEnvelope fields — personas must speak the envelope contract: objective in, output_contract honored, no free-form chat)
  </read_first>
  <action>
    Write 5 v2 personas at personas/product/<slug>.md, slugs identical to the legacy filenames (so registry slug join is 1:1). Each file: YAML frontmatter with `name`, `slug`, `department: product`, `role` (head for product-manager; worker/specialist for the rest per legacy role), `persona_version: v2.0-fable`, `model_tier_default` — then the v2 body: mission, operating rules (works ONLY from a TaskEnvelope objective; produces ONLY the output_contract artifact; reports confidence 0-1 with every result; confidence < 0.6 self-reports as low-confidence per the escalation rule; never contacts another agent directly — queue + typed artifacts only, ORCH-04), department-specific craft (product listing structure, trend evidence standards, prioritization frames), and escalation posture (what it hands to its head vs. solves). Quality bar: Fable v2 — each persona must be materially richer and more operational than its legacy counterpart, not a reformat. Do NOT modify anything under agency-agents/.
  </action>
  <verify><automated>test "$(ls personas/product/*.md | wc -l)" -eq 5 && grep -l "persona_version: v2.0-fable" personas/product/*.md | wc -l | grep -qx 5 && git diff --quiet -- agency-agents/</automated></verify>
  <acceptance_criteria>
    - 5 files exist, each with frontmatter keys name/slug/department/role/persona_version exactly, persona_version value `v2.0-fable`
    - Every persona body states the envelope-only operating rule and the confidence self-report rule (grep: `confidence` present in all 5)
    - agency-agents/ has zero diff (read-only honored)
  </acceptance_criteria>
  <done>The slice department's personas exist at Fable v2 quality — criterion 6's substance.</done>
</task>

<task type="auto">
  <name>Task 2: apply-persona-v2.ts — reset-survivable registry flip + proof</name>
  <files>db/seed/apply-persona-v2.ts</files>
  <read_first>
    - db/seed/import-personas.ts (seeder conventions: strip-types run, pg client via @dxb/shared, idempotency, count prints)
    - db/migrations/20260707000002_registry.sql (agents columns: slug, department, persona_path, persona_version)
  </read_first>
  <action>
    db/seed/apply-persona-v2.ts: scans personas/*/ for .md files with `persona_version: v2.0-fable` frontmatter; for each, UPDATE agents SET persona_path=<repo-relative path>, persona_version='v2.0-fable', matching on slug + department; prints `v2 applied: N updated / M files; missing-in-registry: [..]` (a v2 file with no registry row is reported, never silently skipped). Idempotent: second run reports 0 changed. Document in db/README.md seed order: import-personas.ts THEN apply-persona-v2.ts after every reset (05-09's slice script depends on this order).
    Execute against the live DB and paste proof: `SELECT slug, persona_version FROM agents WHERE department='product' ORDER BY slug` → 5 rows, all v2.0-fable.
  </action>
  <verify><automated>node --experimental-strip-types db/seed/apply-persona-v2.ts && test "$(psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -t -A -c "SELECT count(*) FROM agents WHERE department='product' AND persona_version='v2.0-fable'")" -eq 5</automated></verify>
  <acceptance_criteria>
    - Live query returns exactly 5 product rows with persona_version='v2.0-fable' (executed output in SUMMARY)
    - Every updated persona_path resolves to an existing file (script asserts fs.existsSync before UPDATE)
    - Re-run prints 0 updated (idempotency evidence); non-product departments remain 'v1.0-legacy' (count check: 148 legacy rows untouched)
  </acceptance_criteria>
  <done>Registry truth matches disk truth; the 10/10 gate's persona precondition is machine-checkable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persona files → agent behavior | persona text becomes system-prompt material for workers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | persona file edits changing agent behavior silently | medium | mitigate | personas/ is git-tracked (any change = diff); registry persona_path/version flip only via reviewed seed script; v2 authorship FABLE-ONLY |
| T-05-09 | Spoofing | v2 file for a slug that has no registry row (ghost agent) | low | mitigate | script reports missing-in-registry explicitly and never INSERTs — registry birth stays with import-personas.ts |
</threat_model>

<verification>
- 5 files on disk with correct frontmatter; live registry query = 5 × v2.0-fable; idempotent re-run; agency-agents/ diff-clean.
</verification>

<success_criteria>PHASE-05 criterion 6 precondition satisfied for the slice department with executed registry evidence; first v2 batch born.</success_criteria>

<output>Create .planning/phases/05-kernel-orchestrator-core-loop/05-04-SUMMARY.md with the 5-row registry query output and Fable's authorship note.</output>
