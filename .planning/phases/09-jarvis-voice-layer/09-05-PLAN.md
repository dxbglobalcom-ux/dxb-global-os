---
phase: 09-jarvis-voice-layer
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - apps/jarvis/src/wakeword.ts
  - apps/jarvis/src/index.ts
  - tools/wakeword/listener.py
  - tools/wakeword/README.md
  - tests/phase9/gate-negative.test.ts
  - tests/phase9/wakeword-config.test.ts
  - .planning/phases/09-jarvis-voice-layer/09-VERIFICATION.md
autonomous: true
requirements: [VOICE-01, VOICE-02]
must_haves:
  truths:
    - "Wake word comes ONLY from config (default 'Selamünaleyküm ya Hamza'); changing the config value + restart activates the new phrase and the old one stops triggering — hard-coding anywhere is a suite failure (CEO B2)"
    - "Voice can NEVER execute an outward/gated action: spoken 'ödemeyi onayla' leaves approvals untouched (still pending) and at most references/highlights the inbox item — negative test proves it (master plan step 5)"
    - "If the custom TR wake-word model is not production-grade this phase, the fallback chain is EXPLICIT and CEO-visible (pretrained interim model or PTT-only), never a silent quality drop"
    - "Phase closes with 09-VERIFICATION.md two-tier evidence + ⛔ Fable verdict; VPS voice deploy is a gated step with exact commands, never run around the classifier"
  artifacts:
    - "wakeword.ts + tools/wakeword/listener.py — openWakeWord sidecar (stdout event protocol), model path + phrase from jarvis.config.json"
    - "tests/phase9/{wakeword-config,gate-negative}.test.ts"
    - "09-VERIFICATION.md — criteria table + CEO checklist (kulak testi, mic feel, VPS deploy one-liner)"
  key_links:
    - "wakeword.ts <-> jarvis.config.json wakeword block (09-03 Task 1 surface)"
    - "gate-negative test <-> approvals double-confirm rails (Phase 4 GATE-01 + Phase 8 inbox)"
---

<objective>
Master plan steps 4b + 5 + 6: the personal wake word as pure config, the hard proof that voice cannot spend money, and honest phase closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-09.md   (4b wake word config test, 5 gated negative, 6 closure)
@apps/jarvis/src/config.ts + index.ts   (09-03/04 outputs)
@.planning/phases/08-ceo-dashboard-crm/08-VERIFICATION.md   (two-tier verification format precedent)
@packages/kernel/src/classify.ts   (OUTWARD_MARKERS clamp — the layer that must catch 'onayla/öde')
openWakeWord docs (github dscripka/openWakeWord) — pretrained models, custom model training path, ONNX runtime CPU
</context>

## Artifacts this phase produces

**This plan:** wakeword sidecar (py listener + ts bridge), wake word config test, gate-negative test, 09-VERIFICATION.md.

<tasks>

<task type="auto">
  <name>Task 1: openWakeWord sidecar + config binding</name>
  <files>tools/wakeword/listener.py, tools/wakeword/README.md, apps/jarvis/src/wakeword.ts, apps/jarvis/src/index.ts</files>
  <read_first>openWakeWord README (model loading, threshold, 16kHz frame contract, custom-model training notebook path); uv/python availability on machine; study-card discipline: capture findings in tools/wakeword/README.md BEFORE coding</read_first>
  <action>
  listener.py (small, uv-run, pinned deps): loads ONNX model(s) named by CLI arg --model, listens on mic frames, prints one JSON line {"wake": true, "phrase": "...", "score": n} on detection over threshold; nothing else on stdout. wakeword.ts: spawns sidecar when config wakeword.enabled=true with modelPath from config, on wake event triggers the SAME capture flow PTT uses (index.ts refactor point: one shared handleUtterance()). Custom phrase reality (recorded, CEO-visible): openWakeWord custom phrases need a trained model — Task includes a bounded training attempt for "Selamünaleyküm ya Hamza" via the documented synthetic-sample pipeline (local GPU available); if the trained model's live false-accept/miss rate is unusable within the phase window, config ships wakeword.enabled=false + PTT primary and README records the exact resume path + interim options (pretrained 'hey jarvis' as loaner phrase ONLY if CEO opts in — default stays his phrase or nothing). Hard rule enforced in code: phrase/model NEVER hard-coded; wakeword.ts reads config only.
  </action>
  <verify>sidecar starts with a model file + prints wake JSON on a played sample (TTS-generated positive sample); kill sidecar → jarvis PTT still fully functional (wake word is an optional layer, never a dependency)</verify>
  <acceptance_criteria>Config-only binding; sidecar isolation (its crash never breaks PTT); training attempt outcome recorded honestly either way</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: wake word config-swap test (master plan 4b)</name>
  <files>tests/phase9/wakeword-config.test.ts</files>
  <read_first>config.ts loader; wakeword.ts spawn arg construction</read_first>
  <action>
  Unit-level proof of the B2 contract without audio: (a) config phrase/model A → wakeword.ts constructs sidecar args for model A; switch config to model B + "restart" (re-instantiate) → args now model B and NO reference to A remains (old phrase dead); (b) default config asserts phrase == "Selamünaleyküm ya Hamza"; (c) structural grep test: no source file under apps/jarvis/src or tools/wakeword contains the phrase literal outside jarvis.config.json default + tests (hard-code ban machine-enforced). If a live dual-model audio A/B is feasible with generated samples, add it as a skippable integration case (skipIf no models present) — audio-level swap then ⚠ CEO-ear only if models absent.
  </action>
  <verify>pnpm vitest run tests/phase9/wakeword-config.test.ts → pass incl. grep gate</verify>
  <acceptance_criteria>Config swap provably changes the active phrase binding and kills the old one; ban on hard-coding is a failing test away</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: gated negative test (master plan 5) — voice cannot spend</name>
  <files>tests/phase9/gate-negative.test.ts</files>
  <read_first>classify.ts clampOutward + approval rails (Phase 4 outbox/approvals; Phase 8 inbox double-confirm); command-bar-intent test seeds</read_first>
  <action>
  Seed a pending high-risk approval. Submit via the VOICE path (jarvis client, STT-injected text) the Turkish command "ödemeyi onayla" (+ variants "onayla", "approve the payment"). Drive intake. Assert: (a) approvals row STILL status='pending' — untouched; (b) NO outbox/outward action row was created or executed; (c) whatever the chain produced is at most an internal task / inbox reference (approval_class never 'none' on outward-marked text — clampOutward evidence); (d) same for the typed path (parity of the gate). This is the phase's non-negotiable safety criterion (ROADMAP success criterion 3): voice alone can never execute an outward action.
  </action>
  <verify>pnpm vitest run tests/phase9/gate-negative.test.ts → pass; deliberately weaken assertion target in scratch (mark approval approved) → test FAILS (sensitivity), revert</verify>
  <acceptance_criteria>Negative proof executed, sensitive, and covering both voice and typed paths</acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 4: phase closure — 09-VERIFICATION.md + gated VPS voice deploy</name>
  <files>.planning/phases/09-jarvis-voice-layer/09-VERIFICATION.md</files>
  <read_first>08-VERIFICATION.md format (two-tier, criteria table, CEO checklist); all phase-9 SUMMARYs</read_first>
  <action>
  Write 09-VERIFICATION.md: 3 ROADMAP criteria + VOICE-01/02 evidence table (executed commands + decisive lines); ⚠ UNVERIFIED tier listing honestly: briefing SES KALİTESİ (CEO kulağı), mic-in-hand feel, wake-word live comfort, VPS voice deploy. CEO checklist: (1) sabah 07:00 brifingini dinle / `node apps/jarvis/dist/... --briefing-now` ile anında dinle, (2) PTT ile TR komut ver → cockpit'te zinciri gör, (3) sesli "ödemeyi onayla" dene → onayın pending kaldığını ve inbox highlight'ını gör, (4) wake word dene (model durumuna göre), (5) VPS voice deploy tek-cümle izinle: `git archive HEAD | ssh dxb@46.225.89.249 'tar -x -C /opt/dxb'` sonra `ssh dxb@46.225.89.249 'cd /opt/dxb && docker compose -f vps/compose.yaml --profile voice up -d speaches && docker compose -f vps/compose.yaml build outbox && docker compose -f vps/compose.yaml up -d outbox'` (outbox rebuild da bekliyor — Phase 8 residue ile birleşik). ⛔ Fable verdict: MACHINE-COMPLETE vs CLOSED ayrımı net.
  </action>
  <verify>File exists with two-tier table + verdict; every ✓ row cites an executed command; nothing outward ran without approval</verify>
  <acceptance_criteria>Honest closure doc; VPS steps prepared-but-gated; phase status set by evidence, not optimism</acceptance_criteria>
</task>

</tasks>

<verification>
1. wakeword-config + gate-negative suites green (incl. sensitivity proofs)
2. Hard-code ban grep gate green
3. 09-VERIFICATION.md two-tier + ⛔ verdict; VPS deploy commands staged for CEO one-liner
</verification>
