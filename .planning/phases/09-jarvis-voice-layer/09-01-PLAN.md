---
phase: 09-jarvis-voice-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/study-cards/speaches.md
  - vps/compose.yaml
  - /var/tmp/dxb/speaches (local runtime state, not committed)
autonomous: true
requirements: [VOICE-01, VOICE-02]
must_haves:
  truths:
    - "Speaches runs LOCALLY (same image family the VPS will use) and answers both `/v1/audio/transcriptions` and `/v1/audio/speech` with Turkish content"
    - "STACK.md Speaches notes + official docs studied BEFORE install (study card with ⛔ Fable verdict exists) — dual-role principle: tool installed at the start of the phase that uses it"
    - "vps/compose.yaml speaches service is fully specified under the `voice` profile (image pin, mem_limit, healthcheck) — deploy itself is a later gated step"
  artifacts:
    - ".planning/study-cards/speaches.md — endpoints, model matrix (whisper small int8 / Kokoro-82M / Piper), RAM measurements, TR quality notes, pitfalls"
    - "vps/compose.yaml — speaches service block (voice profile) updated from live-verified local settings"
  key_links:
    - "speaches local base URL <-> apps/jarvis client env (SPEACHES_BASE_URL, default http://127.0.0.1:8969)"
---

<objective>
Master plan step 1: Speaches study → local install → TR smoke proof. Voice infra exists and is measured before any voice code is written. VPS deploy is explicitly deferred to the gated step in 09-05 (classifier-gated prod SSH).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-09.md   (LOCKED: single container, both endpoints, CPU-only)
@.planning/research/STACK.md   (Speaches row: ghcr.io/speaches-ai/speaches:latest-cpu, faster-whisper 1.2.1 + Kokoro-82M + Piper bundle — MANDATORY read before install)
@.planning/phases/09-jarvis-voice-layer/09-CONTEXT.md
@vps/compose.yaml   (voice profile stub from Phase 7 — speaches service joins here)
@.planning/study-cards/design-bundle.md   (study-card format precedent)
</context>

## Artifacts this phase produces

**This plan:** study-cards/speaches.md, running local Speaches container, updated vps/compose.yaml speaches service, measured RAM + latency numbers.

<tasks>

<task type="auto">
  <name>Task 1: Speaches study card (before install — no guessing)</name>
  <files>.planning/study-cards/speaches.md</files>
  <read_first>STACK.md Speaches row + RAM budget section; official speaches docs (speaches.ai) — API surface, model management endpoints, env vars; openai-audio compatibility notes</read_first>
  <action>
  Study pass BEFORE install: exact image tag strategy (latest-cpu vs pinned digest — pin what is pulled), how models are downloaded/cached (HF hub cache volume), STT endpoint contract (multipart file field, model param `Systran/faster-whisper-small`, language hint tr), TTS endpoint contract (model `speaches-ai/Kokoro-82M-v1.0-ONNX`, voice list, response format wav), health endpoint, env vars, RAM expectations per model, Piper fallback invocation. Record 5+ pitfalls (e.g. first-call model download latency, ONNX thread count, wav vs mp3 default). Close with ⛔ Fable verdict line.
  </action>
  <verify>study card exists with endpoint matrix + pitfalls + verdict; every claim carries a doc/source pointer</verify>
  <acceptance_criteria>No install command ran before this card was written; card is execution-ready (curl examples verbatim)</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Local Speaches up + model pull + TR smoke</name>
  <files>/var/tmp/dxb/speaches-run.md (evidence log; runtime only)</files>
  <read_first>study card Task 1; laptop RAM headroom (free -m before/after)</read_first>
  <action>
  Run Speaches locally via docker (same image the VPS profile pins): named container `dxb_speaches_local`, port 8969, HF cache volume for model persistence, restart unless-stopped (survives reboots like the rest of the local stack). Pull/warm both models by exercising the endpoints. Smoke: (a) TTS — Turkish sentence "Günaydın. Gece üç görev tamamlandı, altı onay bekliyor." → wav file, playable (file size > 20KB, `file` reports RIFF WAVE, duration 3-8s via ffprobe); (b) STT — feed that same wav back → transcript contains "onay" (case-insensitive) — full loop proof without a microphone. Measure: container RSS after both calls (docker stats --no-stream), TTS latency, STT latency. Record numbers in evidence log AND in the study card's measurement section.
  </action>
  <verify>curl -s http://127.0.0.1:8969/v1/audio/speech ... → wav; curl STT on that wav → JSON text containing 'onay'; docker stats line captured</verify>
  <acceptance_criteria>Both endpoints proven with TURKISH content end-to-end (TTS→STT round trip); RAM number recorded for the VPS budget check</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: vps/compose.yaml speaches service (spec only, no deploy)</name>
  <files>vps/compose.yaml</files>
  <read_first>existing speaches stub in compose (Phase 7 declared voice profile); measured RAM from Task 2; STACK RAM budget table</read_first>
  <action>
  Update/complete the speaches service under `profiles: [voice]`: pinned image (digest from what Task 2 actually pulled), port binding 127.0.0.1 only (Caddy/Tailscale fronting later), hf_hub_cache volume (already declared), mem_limit set from measured RSS + safety margin (respect 8GB total budget — if measured RSS pushes the box over budget, mem_limit documents the constraint and study card records the fallback: whisper small stays, no medium upgrade on VPS), healthcheck on the health endpoint, restart unless-stopped. `docker compose -f vps/compose.yaml config` must validate. NO ssh, NO deploy — deploy is 09-05's gated step.
  </action>
  <verify>docker compose -f vps/compose.yaml config → exits 0 and rendered speaches block shows pin + mem_limit + healthcheck; git diff shows only speaches service touched</verify>
  <acceptance_criteria>Service block is deploy-ready such that the gated step is a pure `compose up -d` with zero editing</acceptance_criteria>
</task>

</tasks>

<verification>
1. Study card with ⛔ verdict precedes any docker pull (git/file timestamps + SUMMARY narrative)
2. Local TTS→STT Turkish round trip evidence (commands + decisive output lines)
3. compose config validates; RAM number vs STACK budget stated explicitly
</verification>
