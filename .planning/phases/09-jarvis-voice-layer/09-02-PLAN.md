---
phase: 09-jarvis-voice-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260710000018_briefing_view.sql
  - tests/phase9/briefing-view.test.ts
autonomous: true
requirements: [VOICE-01]
must_haves:
  truths:
    - "v_morning_briefing returns exactly three blocks — overnight task_events summary, pending approvals, 24h cost — from ONE query surface (LOCKED: brifing tek SQL görünümden; ajan 'brifing yazmaz')"
    - "View is read-only projection: SECURITY INVOKER, no new write path, no new table"
    - "Overnight window and cost window are deterministic and timezone-explicit (Europe/Berlin day boundaries), so the 07:00 briefing always describes 'since yesterday evening' correctly"
  artifacts:
    - "supabase/migrations/20260710000018_briefing_view.sql — v_morning_briefing"
    - "tests/phase9/briefing-view.test.ts — seeded-data assertions per block + empty-state shape"
  key_links:
    - "v_morning_briefing <-> briefing.ts (09-03 reads ONLY this view for content)"
    - "cost block <-> litellm.LiteLLM_SpendLogs via the same constants packages/shared/src/litellm.ts exports (COST-04 parity)"
---

<objective>
Master plan step 2: the single source of truth for the morning briefing. One SQL view; the LLM later rewords rows, never invents content. Migration renumbered 0012→0018 (recorded adaptation — 0012-0017 taken).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-09.md   (LOCKED: view blocks = gece task_events özeti + pending approvals + 24h maliyet)
@supabase/migrations/20260710000013_broadcast_triggers.sql   (task_events shape)
@apps/dashboard/src/lib (cost view query used by COST-04 — reuse aggregation semantics, do not fork definitions)
@packages/shared/src/litellm.ts   (LITELLM_SCHEMA/SPEND_TABLE constants — never hard-code litellm literals)
@tests/phase8/cost-view.test.ts   (SQL-equality test pattern)
</context>

## Artifacts this phase produces

**This plan:** v_morning_briefing view (migration 0018), briefing-view test file.

<tasks>

<task type="auto">
  <name>Task 1: 0018 v_morning_briefing migration</name>
  <files>supabase/migrations/20260710000018_briefing_view.sql</files>
  <read_first>task_events + approvals + tasks schemas (\d in psql); litellm spend table columns; timezone handling in existing views/queries</read_first>
  <action>
  One view, three labeled blocks via UNION ALL of jsonb rows (block, payload, sort) OR three-column lateral aggregate — pick the shape easiest for briefing.ts to consume deterministically (ordered, fixed keys). Block 1 overnight work: task_events since (now() at Europe/Berlin previous day 19:00) grouped by to_status with counts + up to 5 most recent completed task objectives (truncated 80 chars). Block 2 approvals: pending approvals count + per-risk-class counts + up to 5 oldest pending titles. Block 3 cost: last 24h spend EUR total + top 3 departments by spend, sourced through the SAME aggregation semantics as the COST-04 view/query (byte-equal constants from packages/shared litellm.ts — schema/table names in comments reference the constants, values copied verbatim). SECURITY INVOKER; grant SELECT to authenticated + service role used by scheduler. Apply migration to local DB (supabase migration up or psql -f as the repo convention dictates — follow how 0014-0017 were applied).
  </action>
  <verify>psql: SELECT * FROM v_morning_briefing → exactly 3 blocks with expected keys; \d+ v_morning_briefing shows security_invoker=true</verify>
  <acceptance_criteria>Three blocks, deterministic order, tz-explicit windows, no new write surface</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: briefing-view tests</name>
  <files>tests/phase9/briefing-view.test.ts</files>
  <read_first>tests/phase8/cost-view.test.ts seed/rollback pattern; vitest config include globs (tests/phase9 must be picked up)</read_first>
  <action>
  Seeded tests inside a rolled-back transaction (phase-8 pattern): (a) seed overnight task_events (one before window edge, one inside) → block 1 counts only the inside row (window edge proof); (b) seed 2 pending + 1 approved approval → block 2 count=2 and approved absent; (c) seed spend rows across 2 departments → block 3 total equals SUM to the cent and top-department order correct; (d) empty DB state → all 3 blocks still present with zero/empty payloads (07:00 cron must never crash on a quiet night). Ensure vitest picks tests/phase9 (extend include glob if needed — record as deviation if config edit required).
  </action>
  <verify>pnpm vitest run tests/phase9/briefing-view.test.ts → all pass; deliberately corrupt one expected count in a scratch run → test FAILS (sensitivity proof, then revert)</verify>
  <acceptance_criteria>Window edges, per-block correctness, and empty-state shape all pinned by executed tests</acceptance_criteria>
</task>

</tasks>

<verification>
1. Migration applied locally; SELECT returns 3 ordered blocks
2. briefing-view tests green + sensitivity (corrupt→FAIL→revert) demonstrated
3. grep migration for hard-coded 'LiteLLM' literals mismatching shared constants → aligned
</verification>
