---
phase: 09-jarvis-voice-layer
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - apps/jarvis/src/briefing.ts
  - apps/jarvis/src/speak.ts
  - apps/jarvis/src/config.ts
  - apps/jarvis/package.json
  - packages/kernel/policy/routing-seed.json
  - packages/outbox-executor/src/scheduler.ts
  - tests/phase9/briefing-pipeline.test.ts
autonomous: true
requirements: [VOICE-01]
must_haves:
  truths:
    - "07:00 Europe/Berlin cron produces a briefing artifact (TR text + wav) whose EVERY fact traces to a v_morning_briefing row — LLM rewords, never adds (content-parity test)"
    - "Briefing LLM call parameters come from a routing_rules row (task_class 'briefing'), never hard-coded — kernel translation-layer discipline holds"
    - "Scheduler keeps its re-arm/error-isolation discipline: a failing briefing job can never kill the intent-intake or outbox chains"
    - "speak.ts playback works on the laptop (paplay/aplay class) — the CEO can hear the artifact"
  artifacts:
    - "apps/jarvis/src/briefing.ts — view rows → TR script (routing_rules-routed LLM reword) → Speaches TTS → artifact on disk + briefing_runs evidence row-free design (artifact + log only; NO new tables)"
    - "packages/outbox-executor scheduler: briefingCron '0 7 * * *' tz Europe/Berlin queue + worker"
    - "tests/phase9/briefing-pipeline.test.ts — content parity with injected fake LLM/TTS"
  key_links:
    - "briefing.ts <-> v_morning_briefing (ONLY content source)"
    - "briefing.ts <-> routing_rules 'briefing' row (model/effort as data)"
    - "scheduler QUEUES/CADENCES pattern <-> new briefing queue (mirror pinCheck/compaction cron style)"
---

<objective>
Master plan step 3: the CEO hears the company every morning. View rows become a spoken Turkish briefing via Speaches TTS, scheduled by the same resident scheduler that owns every other routine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-09.md   (step 3 + LOCKED briefing rules)
@packages/outbox-executor/src/scheduler.ts   (QUEUES/CADENCES/boss.work patterns — extend, don't fork)
@packages/kernel/src/classify.ts   (SDK subscription-mode call pattern + routing_rules resolution via route())
@packages/kernel/policy/routing-seed.json   (seed row format)
@.planning/study-cards/speaches.md   (TTS contract from 09-01)
@apps/jarvis/src/index.ts   (scaffold to extend)
</context>

## Artifacts this phase produces

**This plan:** briefing.ts, speak.ts, config.ts (jarvis runtime config loader), routing_rules 'briefing' seed, scheduler briefing cron, briefing-pipeline test.

<tasks>

<task type="auto">
  <name>Task 1: routing_rules 'briefing' row + jarvis config loader</name>
  <files>packages/kernel/policy/routing-seed.json, apps/jarvis/src/config.ts</files>
  <read_first>routing-seed.json existing rows + how seeds reach the DB (Phase 5 seed mechanism); kernel route() lookup contract</read_first>
  <action>
  Add task_class 'briefing' row (model sonnet-5 per master plan "sonnet-5 toparlama", effort medium, subscription mode — same legality rules as orchestration row). Reseed/upsert into local DB the same way existing seeds landed. config.ts: typed loader for jarvis runtime config file (JSON at apps/jarvis/jarvis.config.json, committed with defaults): speachesBaseUrl, ttsVoice, sttModel, briefingOutDir (/var/tmp/dxb/briefings), playbackCmd, wakeword {enabled:false, phrase:"Selamünaleyküm ya Hamza", modelPath:null} — wake word fields land NOW as config surface (B2: runtime-configurable, hard-code FORBIDDEN); engine wiring is 09-05.
  </action>
  <verify>psql: select * from routing_rules where task_class='briefing' → 1 enabled row; node -e config load prints defaults; grep -rn "Selamünaleyküm" apps/jarvis/src → ONLY as config default value, never in logic</verify>
  <acceptance_criteria>Model choice is data; config file is the single wake-word/voice knob surface</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: briefing.ts + speak.ts</name>
  <files>apps/jarvis/src/briefing.ts, apps/jarvis/src/speak.ts, apps/jarvis/package.json</files>
  <read_first>classify.ts runQuery pattern (SDK call, outputFormat json_schema); Speaches TTS curl contract from study card; getDb usage in packages (jarvis must NOT open its own kysely surface — briefing generation runs inside the scheduler process which already has db; briefing.ts exports a pure function taking rows in)</read_first>
  <action>
  briefing.ts: exported `composeBriefing(rows, deps)` — takes v_morning_briefing rows + injected {llm, tts} deps. LLM step: strict prompt "reword ONLY these facts into a 6-10 sentence spoken Turkish executive briefing; no additions" with zod schema {script: string}; model/effort resolved via kernel route('briefing') (SDK subscription path, SDK_MODEL_IDS map). TTS step: script → Speaches /v1/audio/speech (voice from config) → wav bytes → write ${briefingOutDir}/briefing-YYYYMMDD-HHmm.wav + sibling .txt with the script + .json with the source rows (traceability trio). Returns artifact paths. speak.ts: `playWav(path, config)` via child_process (paplay default, configurable) + `speakText(text)` (TTS→temp wav→play) for 09-04 confirmations. Keep jarvis thin: NO db import in briefing.ts (rows injected), NO provider SDK keys (subscription SDK only, same legality as kernel).
  </action>
  <verify>tsc --build green; unit-run composeBriefing with fixture rows + fake deps → script mentions every seeded fact class; real one-shot run (live LLM + local Speaches) produces playable wav + txt + json trio (ffprobe duration > 5s)</verify>
  <acceptance_criteria>Traceability trio on disk; deps injected (testable); zero own-DB/zero own-keys in jarvis</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: scheduler briefing cron + pipeline test</name>
  <files>packages/outbox-executor/src/scheduler.ts, tests/phase9/briefing-pipeline.test.ts</files>
  <read_first>scheduler QUEUES/CADENCES/boss.schedule + boss.work blocks (mirror exactly, incl. per-job error containment comment); pg-boss v12 schedule tz option (verify against installed pg-boss docs — no guessing)</read_first>
  <action>
  Add QUEUES.briefing='morning-briefing', CADENCES.briefingCron='0 7 * * *' with tz Europe/Berlin (pg-boss schedule options; if installed pg-boss version lacks tz support, compute UTC cron '0 5 * * *' and record deviation with DST caveat). Worker: query v_morning_briefing via getDb, call composeBriefing with real deps, log artifact path; throw containment identical to compaction/memSync jobs (scheduler survives). Test (briefing-pipeline): fake llm/tts deps → (a) every numeric fact in blocks appears in the script input handed to LLM (content-parity at the boundary we control); (b) artifact trio written; (c) LLM told 'no additions' — assert prompt contains the guard clause; (d) worker error does not unschedule other queues (unit-level: worker throws → enqueue state of other chains untouched — mirror how existing scheduler behavior is tested, or assert via pg-boss job rows in a live-DB test).
  </action>
  <verify>pnpm vitest run tests/phase9/briefing-pipeline.test.ts → pass; restart local resident scheduler → pgboss.schedule row for morning-briefing exists with correct cron+tz (psql evidence); manual `boss.send` of briefing job → artifact trio appears + scheduler stays up</verify>
  <acceptance_criteria>Cron registered with explicit tz semantics; live manual fire produced audible artifact; failure isolation proven</acceptance_criteria>
</task>

</tasks>

<verification>
1. Live one-shot briefing artifact (wav+txt+json) exists; wav playable — machine-verifiable part; ⚠ ses KALİTESİ CEO kulağı (explicitly unverifiable by machine)
2. pgboss schedule row (cron 0 7 * * *, tz Europe/Berlin or recorded UTC fallback)
3. Pipeline tests green incl. content-parity guard
4. routing_rules-driven model choice (psql row + no hard-coded model literal in jarvis: grep)
</verification>
