---
phase: 09-jarvis-voice-layer
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-03"]
files_modified:
  - apps/jarvis/src/listen.ts
  - apps/jarvis/src/client.ts
  - apps/jarvis/src/auth.ts
  - apps/jarvis/src/index.ts
  - tests/phase9/parity.test.ts
autonomous: true
requirements: [VOICE-02]
must_haves:
  truths:
    - "A spoken Turkish command travels mic → STT → THE SAME /api/intent seam the dashboard uses → kernel chain; jarvis adds ZERO interpretation and has ZERO own DB access (thin second client, CEO B2)"
    - "Parity is proven by test: identical intent text via dashboard route vs via voice path → equivalent chains (same department, model_tier, approval_class; different ids)"
    - "jarvis authenticates AS the CEO through the normal Supabase session flow (one-time interactive login, refresh token persisted 0600 outside the repo) — no service backdoor, AAL2 wall respected"
    - "STT test needs no microphone: TTS-generated wav fixtures close the loop (09-01 round-trip pattern)"
  artifacts:
    - "listen.ts (capture → Speaches STT → text), client.ts (POST /api/intent + session refresh), auth.ts (login/store/refresh), index.ts PTT CLI loop"
    - "tests/phase9/parity.test.ts — typed-vs-spoken chain equivalence"
  key_links:
    - "client.ts <-> apps/dashboard/src/app/api/intent/route.ts (SAME contract, zod-matching body {text, lang})"
    - "listen.ts <-> Speaches /v1/audio/transcriptions (study-card contract, language hint tr)"
    - "index.ts PTT <-> speak.ts confirmations (09-03) — spoken ack of what was heard + intent id"
---

<objective>
Master plan step 4: the CEO speaks and the company acts — voice becomes the second full command channel, byte-equal to typing, with the kernel owning all understanding (B2 verbatim clause binding).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-09.md   (step 4 + B2 directive)
@apps/dashboard/src/app/api/intent/route.ts   (the seam: auth check + body contract + intents insert — jarvis calls THIS, never re-implements)
@tests/phase8/command-bar-intent.test.ts   (chain-assertion pattern to mirror)
@.planning/study-cards/speaches.md   (STT contract)
@apps/jarvis/src/config.ts + speak.ts   (09-03 outputs consumed here)
</context>

## Artifacts this phase produces

**This plan:** listen.ts, client.ts, auth.ts, PTT CLI loop in index.ts, parity test.

<tasks>

<task type="auto">
  <name>Task 1: auth.ts + client.ts (CEO session, same seam)</name>
  <files>apps/jarvis/src/auth.ts, apps/jarvis/src/client.ts</files>
  <read_first>dashboard login flow (supabase password + TOTP AAL2) and /api/intent auth expectations; @supabase/supabase-js session persistence options; where dashboard route reads the session (cookie names) — decide cookie-header vs Authorization strategy from what route.ts ACTUALLY accepts, no guessing</read_first>
  <action>
  auth.ts: interactive `jarvis login` (email+password prompt + TOTP code prompt → supabase-js signInWithPassword + mfa challenge/verify) → persist session JSON at ~/.config/dxb-jarvis/session.json chmod 600 (OUTSIDE repo); silent refresh on expiry; never logs tokens. client.ts: `submitIntent(text, lang)` → POST http://localhost:3100/api/intent with the session attached in whatever form route.ts verifies (match reality discovered in read_first; if route is cookie-only, construct the sb-* cookie header from the stored session exactly as @supabase/ssr serializes it). Returns {intentId}. Failure modes surfaced (401 → "run jarvis login", server down → clear message). ZERO direct DB usage.
  </action>
  <verify>jarvis login (interactive, CEO absent → use e2e test user + its TOTP secret from phase-8 e2e setup) → session file 600; node one-shot submitIntent('test niyeti', 'tr') → 200 {intentId} + intents row exists (psql); rm session file → clean 401 guidance</verify>
  <acceptance_criteria>Same route, same auth wall as the browser; token file 600 outside repo; no secrets in output/transcript</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: listen.ts + PTT loop</name>
  <files>apps/jarvis/src/listen.ts, apps/jarvis/src/index.ts</files>
  <read_first>study card STT contract (multipart field names, model id, language param); available capture binary on this machine (arecord -l / pactl) — pick what exists, make it config</read_first>
  <action>
  listen.ts: `captureOnce(config)` — spawn capture binary (default arecord: 16kHz mono wav, stops on keyup/Enter for PTT v1 CLI) → temp wav → POST to Speaches transcriptions (model from config, language 'tr' hint honoring config locale) → {text, durationMs}. index.ts PTT loop: hold/press Enter → record → STT → echo transcript → speak.ts ack "Anlaşıldı: <transcript kısaltması>. Gönderiyorum." → client.submitIntent → speak intent-queued confirmation with short id; 'q' quits; wakeword.enabled=false path only (engine lands in 09-05, config surface already present). Errors spoken AND printed (Speaches down, 401, empty transcript → "duyamadım" retry prompt). No transcript audio retained beyond temp (privacy default; config keepRecordings=false).
  </action>
  <verify>Scripted (no-mic) run: feed a TTS-generated wav through listen.ts STT path → transcript echoes source text ≥ fuzzy match; live PTT smoke IF mic present (⚠ else recorded as CEO-hand check); full loop test with e2e user → intents row created from spoken-path text</verify>
  <acceptance_criteria>PTT loop runs as CLI daemon; every failure mode has a spoken+printed message; nothing bypasses /api/intent</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: parity test (typed vs spoken = same result)</name>
  <files>tests/phase9/parity.test.ts</files>
  <read_first>tests/phase8/command-bar-intent.test.ts (chain assertions + how it injects/waits intake); scheduler test hooks used there</read_first>
  <action>
  Test: same Turkish intent text submitted (a) via the dashboard route path and (b) via jarvis client path (bypassing audio: inject the text where STT would return it — the audio leg is already covered by the 09-01/Task-2 round-trip). Drive both through intake (drainIntents with injected classifyFn OR live path consistent with phase-8 test conventions). Assert chain equivalence: resulting tasks match on department, model_tier, approval_class, objective-derivation; intents rows both 'dispatched'; broadcast events present for both. Negative structural assert: jarvis source contains no kysely/pg import (grep-level test, mirrors purity-gate style) — thin client enforced by suite.
  </action>
  <verify>pnpm vitest run tests/phase9/parity.test.ts → pass incl. the no-DB-import structural gate</verify>
  <acceptance_criteria>Master-plan acceptance 2 proven: voice path result ≡ command-bar result; thinness machine-enforced</acceptance_criteria>
</task>

</tasks>

<verification>
1. Parity test green (chain equivalence + structural thinness gate)
2. Live spoken-path intent row evidence (or TTS-fixture path if no mic; mic feel ⚠ CEO-hand)
3. Session file 0600 outside repo; grep jarvis for token logging → none
</verification>
