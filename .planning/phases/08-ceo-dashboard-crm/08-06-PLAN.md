---
phase: 08-ceo-dashboard-crm
plan: 06
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/dashboard/src/app/(cockpit)/crm/layout.tsx
  - apps/dashboard/src/app/(cockpit)/crm/clients/page.tsx
  - apps/dashboard/src/app/(cockpit)/crm/requests/page.tsx
  - apps/dashboard/src/app/(cockpit)/crm/contacts/page.tsx
  - apps/dashboard/src/app/(cockpit)/crm/deals/page.tsx
  - apps/dashboard/src/components/crm/entity-table.tsx
  - apps/dashboard/src/components/crm/entity-form.tsx
  - apps/dashboard/src/app/(cockpit)/crm/actions.ts
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - tests/phase8/crm-views.test.ts
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "All four CRM entities (clients/requests/contacts/deals) render as cockpit views over the Phase-3 CRM tables — no new CRM schema is invented here"
    - "CEO edits are limited to RLS-permitted fields; non-permitted fields render disabled in the form (UI mirrors policy, never widens it)"
    - "Agent-written rows (via crm.* MCP tools) appear in the same views — provenance chip distinguishes agent vs CEO writes"
    - "CRM pages run at DENSITY 5: table-first, no card-grid gallery (UI-SPEC §8)"
  artifacts:
    - "crm/* pages (4 entities) + EntityTable (sortable, filterable, mono ids/amounts) + EntityForm (permitted-field editing)"
    - "actions.ts CEO-write server actions (per-entity, field-whitelisted) + purity allowlist extension"
    - "tests/phase8/crm-views.test.ts — CRUD on permitted fields, denial on non-permitted, agent-row visibility"
  key_links:
    - "Phase 3 crm schema (db/migrations 0003-class files + shared db-types) <-> entity field maps"
    - "crm.* dxb-mcp tools (agent write path) <-> provenance chip source field"
    - "field whitelist in actions.ts <-> RLS policies (must match — test asserts both layers deny)"
---

<objective>
Thin CRM inside the cockpit (DASH-04; master plan step 9): four entity views over the existing Phase-3 tables, CEO field-scoped editing, agent-write visibility. Projection-first, policy-mirrored.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-08.md   (step 9 + 'CEO yazımı yalnız izinli alanlarda')
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (§8 crm row: DENSITY 5, table-first, form disable pattern; §4 inputs/focus)
@packages/shared/src/db-types.ts + db/migrations (crm tables from Phase 3 — clients, requests, contacts, deals shapes)
@packages/dxb-mcp   (crm.* tool surface — what agents can write; provenance fields)
</context>

## Artifacts this phase produces

**This plan:** CRM layout + 4 entity pages, EntityTable/EntityForm, field-whitelisted CEO actions, CRM test suite, purity allowlist extension (4th path: crm CEO-writes).

<tasks>

<task type="auto">
  <name>Task 1: EntityTable/EntityForm + four entity pages</name>
  <files>apps/dashboard/src/app/(cockpit)/crm/**, src/components/crm/{entity-table,entity-form}.tsx, messages/*</files>
  <read_first>Phase 3 crm migration files (exact columns, enums, FKs); UI-SPEC §8 crm + §4 input rules (label above, error below, no placeholder-as-label); db-types</read_first>
  <action>
  Generic EntityTable: server-fetched rows, column defs per entity (ids/amounts mono; dates '10 Tem 14:32' TR format; status chips icon+text), sort + simple filter, row → side-panel detail (not modal for browse flow), pagination 50. Provenance chip per row: 'ajan' (crm.* MCP writer) vs 'CEO' based on writer fields. EntityForm in side panel: permitted fields editable (whitelist per entity mirroring RLS), non-permitted rendered disabled with lock hint; validation zod-shared with actions. Pages: clients, requests, contacts, deals — each a thin config over the generic pair; crm/layout adds sub-nav (rail-consistent). Mobile: table → stacked row-cards per UI-SPEC responsive rule. Strings localized.
  </action>
  <verify>Playwright: each entity lists seeded rows (incl. one agent-written row with provenance chip), edit permitted field persists, non-permitted input disabled; mobile 375px stacked view screenshot; build green</verify>
  <acceptance_criteria>4/4 entities browsable + editable within whitelist; provenance visible; DENSITY 5 table-first (no card gallery); forms follow §4</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: field-whitelisted actions + RLS parity test</name>
  <files>apps/dashboard/src/app/(cockpit)/crm/actions.ts, tests/phase8/crm-views.test.ts, tests/phase8/live-projection.test.ts (allowlist edit)</files>
  <read_first>RLS policies on crm tables (Phase 3/4 migrations); 08-03/08-05 allowlist pattern</read_first>
  <action>
  Per-entity update actions: zod-validate ONLY whitelisted fields (strip unknown), CEO session required, audit append on every CRM write (actor='ceo'). Test: (a) permitted-field update succeeds + audit row; (b) crafted payload with non-permitted field → stripped/rejected at action AND direct supabase update as authenticated user on that column → RLS denies (parity proof); (c) agent-written seed row visible with provenance. Extend purity allowlist with the crm action write set (4th path), assert total enumerated paths = 4.
  </action>
  <verify>`pnpm vitest run tests/phase8/crm-views.test.ts` PASS incl. RLS-parity denial; purity gate 4-path PASS</verify>
  <acceptance_criteria>UI whitelist and RLS provably agree; every CEO CRM write audited; write surface enumerated</acceptance_criteria>
</task>

</tasks>

<verification>
1. crm-views.test.ts PASS (CRUD-whitelist + RLS parity + provenance)
2. Playwright per-entity screenshots (desktop + 375px stacked)
3. Purity gate: exactly 4 enumerated write paths
4. Audit rows for CEO CRM edits evidenced (psql count)
</verification>
