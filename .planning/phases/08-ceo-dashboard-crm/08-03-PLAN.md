---
phase: 08-ceo-dashboard-crm
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/dashboard/src/app/(cockpit)/approvals/page.tsx
  - apps/dashboard/src/components/approvals/approval-card.tsx
  - apps/dashboard/src/components/approvals/batch-bar.tsx
  - apps/dashboard/src/app/(cockpit)/approvals/actions.ts
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - tests/phase8/approval-inbox.test.ts
autonomous: true
requirements: [GATE-03, DASH-01]
must_haves:
  truths:
    - "Inbox is grouped by approvals.risk_class: high = one-by-one with FULL payload visible + double-confirm; medium = grouped list; low = single 'approve all (n)' — per-action popups DO NOT EXIST anywhere"
    - "Batch approve runs as ONE transaction: selected pending→approved, one audit row per item; ANY failure → NOTHING approved (rollback) + error panel"
    - "B7b asymmetry visible in UI: money-OUT drafts sit in the approval queue with a permanent badge; money-IN never enters the inbox (feed-only visibility)"
    - "Empty inbox renders the green 'Kapı temiz' state; 50+ pending shows the gate-fatigue warning band"
    - "CEO decisions are the ONLY dashboard write here — server action writes approval decision + audit rows, added to the 08-02 purity allowlist explicitly"
  artifacts:
    - "approvals/page.tsx — risk-grouped inbox, live via dxb:approvals channel"
    - "ApprovalCard (high/medium/low variants per UI-SPEC §4) + BatchBar"
    - "actions.ts server action: single-tx batch approve/reject with per-item audit append"
    - "tests/phase8/approval-inbox.test.ts — seed 6 drafts (2/2/2), grouped render logic, batch-low one-tap, rollback on injected failure, audit rows count"
  key_links:
    - "approvals.risk_class enum (packages/shared db-types) <-> group order high→medium→low"
    - "actions.ts allowlist entry <-> tests/phase8/live-projection.test.ts purity gate (UPDATE the allowlist in the same commit)"
    - "dxb:approvals channel (0013 trigger INSERT OR UPDATE) <-> inbox live refresh + Horizon Line pending counter"
---

<objective>
Risk-grouped batch approval inbox (GATE-03; master plan step 5): the CEO's decision surface. High-risk singular + double-confirm, low-risk one-tap batch in one transaction, B7b money-direction asymmetry, gate-fatigue guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-08.md   (Onay inbox davranışı LOCKED block — group behaviors, rollback, empty/fatigue states)
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (§4 ApprovalCard visual language: no side-stripe, top badge strip, danger accents; §7 bans)
@.planning/phases/08-ceo-dashboard-crm/08-CONTEXT.md   (B7b: money-OUT gated / money-IN autonomous+visible)
@packages/shared/src/db-types.ts   (approvals, audit tables; risk_class values)
@tests/phase4/   (approval gate + audit conventions from Phase 4 — reuse semantics, do not re-model)
</context>

## Artifacts this phase produces

**This plan:** approvals page (grouped live inbox), ApprovalCard high/medium/low, BatchBar, single-tx server action + audit append, inbox test suite, purity-allowlist update.

<tasks>

<task type="auto">
  <name>Task 1: ApprovalCard variants + grouped inbox page (live)</name>
  <files>apps/dashboard/src/app/(cockpit)/approvals/page.tsx, src/components/approvals/approval-card.tsx, src/components/approvals/batch-bar.tsx, messages/tr.json, messages/en.json</files>
  <read_first>Master plan LOCKED inbox block (verbatim behaviors); UI-SPEC §4 ApprovalCard (badge strip not side-stripe; double-confirm = second click armed after 600ms); §5 z-scale for confirm layer</read_first>
  <action>
  Page: server-fetch pending approvals grouped high→medium→low; subscribe dxb:approvals for live insert/update. high: one card per item, full payload (pretty-printed JSON disclosure, dept/agent/cost fields), top strip --danger badge + money-OUT badge when applicable, approve button double-confirm (arm→confirm, disarms on blur/5s). medium: grouped list rows, expandable payload. low: dense rows + BatchBar 'Tümünü onayla (n)' accent pill. Reject path on every tier (reason optional textarea). Empty state: 'Kapı temiz' --ok scene per UI-SPEC. 50+ pending: sticky warning band (gate-fatigue, Pitfall UX). Money-IN records NEVER queried here (WHERE direction != 'in' guard mirrors B7b; they surface in 08-02 changed-feed with --ok mark). All strings via messages/.
  </action>
  <verify>Seed 2 high/2 medium/2 low (money-OUT among high) → Playwright: groups render in order, high shows full payload + double-confirm arming, low shows single batch button; empty-seed run shows Kapı temiz</verify>
  <acceptance_criteria>No per-action popup exists (decision inline in cards); side-stripe absent (grep border-l); money-OUT badge visible; strings localized</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: single-transaction batch action + audit + purity allowlist</name>
  <files>apps/dashboard/src/app/(cockpit)/approvals/actions.ts, tests/phase8/live-projection.test.ts (allowlist edit)</files>
  <read_first>Phase 4 approval/audit table semantics (audit.append conventions), master plan rollback rule, 08-02 purity-gate implementation</read_first>
  <action>
  Server action `decideApprovals(ids[], decision, reason?)`: one DB transaction — for each id: assert still pending, set approved/rejected + decided_at + decided_by='ceo', INSERT audit row (actor, action, approval id, payload hash); any assertion/DB failure → full rollback, return typed error consumed by an inline error panel (NOT a toast — critical per UI-SPEC). RLS check: action runs with CEO session; anon call must fail (test). Update the purity allowlist in live-projection.test.ts to exactly this action's write set (approvals.update + audit.insert) — nothing broader.
  </action>
  <verify>`pnpm vitest run tests/phase8/approval-inbox.test.ts`: batch-low approves n rows + n audit rows atomically; injected failure on item 2 of 3 → 0 approved, 0 audit, error surfaced; purity gate still PASS with new allowlist</verify>
  <acceptance_criteria>Atomicity proven by test (partial failure = zero effect); audit rows 1:1 with decisions; anon rejected; allowlist tightened not widened</acceptance_criteria>
</task>

</tasks>

<verification>
1. approval-inbox.test.ts PASS (grouping, batch, rollback, audit counts, anon-deny)
2. Playwright: 6-draft grouped render + one-tap batch-low → approved states live-update (dxb:approvals) without reload
3. Kapı temiz + gate-fatigue band evidenced (seeded states, screenshots)
4. Grep evidence: no dialog/confirm() popups, no border-l stripes
</verification>
