---
phase: 08-ceo-dashboard-crm
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/dashboard/src/lib/realtime.ts
  - apps/dashboard/src/app/(cockpit)/page.tsx
  - apps/dashboard/src/components/panel.tsx
  - apps/dashboard/src/components/freshness-stamp.tsx
  - apps/dashboard/src/components/task-board.tsx
  - apps/dashboard/src/components/agent-roster.tsx
  - apps/dashboard/src/components/shell/horizon-line.tsx
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - tests/phase8/live-projection.test.ts
autonomous: true
requirements: [DASH-01, DASH-05]
must_haves:
  truths:
    - "Cockpit main view is exception-first: 'waiting on me' + 'just changed' — an all-agents wall does NOT exist (LOCKED UX rule)"
    - "A task state transition in the DB updates the board within 2s WITHOUT page reload (Broadcast, not polling, not postgres_changes)"
    - "Every live panel carries a FreshnessStamp; on channel disconnect it degrades to 'canlı değil — son: HH:MM' with --warn state"
    - "Dashboard reads operational tables only — zero dashboard-owned state tables, zero agent-facing write paths (DASH-05 pure projection)"
    - "All numeric values render in Geist Mono (UI-SPEC §3 data rule)"
  artifacts:
    - "lib/realtime.ts — single channel-subscription helper (dxb:task_events; reused for approvals/costs in later plans; reconnect + status callback)"
    - "Panel component (Double-Bezel per UI-SPEC §4) + FreshnessStamp + TaskBoard + AgentRoster"
    - "Horizon Line live counters wired: active tasks · pending approvals · today's cost · liveness"
    - "tests/phase8/live-projection.test.ts — DB write → broadcast payload shape assertion"
  key_links:
    - "lib/realtime.ts status callback <-> FreshnessStamp degrade state"
    - "Panel component <-> 08-03 inbox + 08-04 cost/audit + 08-06 CRM (single surface language)"
    - "task_events/agents/registry tables (packages/shared db-types) <-> board/roster queries"
---

<objective>
Live cockpit heart: task board + agent roster as pure projections over Broadcast (master plan step 4), Panel/FreshnessStamp primitives per UI-SPEC, Horizon Line live counters. Exception-first composition per LOCKED rules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-08.md   (step 4 + exception-first + freshness LOCKED rows)
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (§4 Panel/DataTile/FreshnessStamp, §5 cockpit grid 7-8/4-5 split, §6 value-flip pulse)
@apps/dashboard/src/app/globals.css   (tokens from 08-01 — consume, never redefine)
@packages/shared/src/db-types.ts   (task_events, agents/registry shapes)
@.planning/research/STACK.md   (Broadcast v2 channel API supabase-js 2.110)
</context>

## Artifacts this phase produces

**This plan:** lib/realtime.ts, Panel, FreshnessStamp, TaskBoard ("Beni bekleyenler" + "Az önce değişti" feed), AgentRoster (right rail), Horizon Line counters live, live-projection test.

<tasks>

<task type="auto">
  <name>Task 1: realtime helper + Panel/FreshnessStamp primitives</name>
  <files>apps/dashboard/src/lib/realtime.ts, src/components/panel.tsx, src/components/freshness-stamp.tsx</files>
  <read_first>08-UI-SPEC.md §4 (exact Double-Bezel radii/padding/edge-light recipe; stamp format '14:32 itibarıyla'), §7 bans (no nested cards, no spinners)</read_first>
  <action>
  lib/realtime.ts: `subscribeDxb(channel: 'task_events'|'approvals'|'cost_ledger', onMessage, onStatus)` wrapping supabase-js Broadcast v2 (private channel `dxb:{name}`, auth required per 0013 RLS); exposes connection status (live/reconnecting/stale + last-message timestamp); single client instance, cleanup on unmount. Panel: outer shell --surface rounded-[1.25rem] p-1.5 ring-1 --line + tinted ambient shadow; inner core --surface-2 concentric radius + --edge-light inset top highlight; header row = title (15px/600) + slot + FreshnessStamp. FreshnessStamp: mono 11.5px --ink-2, live tick each broadcast; onStatus stale → --warn dot + 'canlı değil — son: {HH:MM}'. Client Components isolated ('use client' leaves only); shell stays RSC.
  </action>
  <verify>`pnpm --filter @dxb/dashboard build`; unit render test (vitest+testing-library) — Panel renders both themes, stamp switches to stale state when status callback fires</verify>
  <acceptance_criteria>Helper reused-able for all three channels (typed); Panel matches §4 byte-recipe (radii/padding checked against spec values); stamp degrade behavior proven in test</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TaskBoard + AgentRoster + exception-first cockpit composition</name>
  <files>apps/dashboard/src/components/task-board.tsx, src/components/agent-roster.tsx, src/app/(cockpit)/page.tsx, messages/tr.json, messages/en.json</files>
  <read_first>08-UI-SPEC.md §5 (12-col: left 7-8 waiting+changed, right 4-5 rail), §8 cockpit row; db-types for task/agent status enums</read_first>
  <action>
  TaskBoard: two stacked sections in the left column — 'Beni bekleyenler' (tasks in states requiring CEO: approval-pending count summary linking to /approvals, blocked, failed) and 'Az önce değişti' (last N transitions feed via dxb:task_events; value-flip + 600ms accent pulse on arrival per §6; each row: mono stamp + task title + state chip [color+icon+label — never color alone] + link to /tasks/[id]). AgentRoster right rail: registry-backed list grouped by department, status dot+label, active-task count mono; NO all-agents wall — collapsed by default to active/errored agents, full list behind disclosure. Cockpit page composes grid per §5 with Panel primitives; server-fetch initial state (RSC) + client hydrate live layer. Horizon Line counters: subscribe once at shell level (active tasks, pending approvals count, today's cost placeholder until 08-04 view exists — wire from cost_ledger sum query, liveness dot). All strings → messages/.
  </action>
  <verify>Seed script inserts a task transition → Playwright observes board update <2s without reload (timestamped screenshots before/after); build green; grep: no 'grid-cols-3' equal-card pattern in cockpit page</verify>
  <acceptance_criteria>Exception-first composition (no firehose, no all-agents default); live update <2s proven; state chips carry icon+text; roster grouped and collapsed per spec</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: projection proof test + purity audit</name>
  <files>tests/phase8/live-projection.test.ts</files>
  <read_first>tests/phase8/broadcast-triggers.test.ts (08-01 pattern), master plan I8 rule</read_first>
  <action>
  Test: (a) INSERT task_events row → assert broadcast payload shape consumed by lib/realtime.ts contract (topic, event fields the UI reads); (b) purity audit as code: scan apps/dashboard/src for any Supabase .insert/.update/.upsert calls — allowed ONLY in the intent/approval paths list (empty until 08-03/08-05); fail test if any other write path exists. This encodes DASH-05 as a regression gate.
  </action>
  <verify>`pnpm vitest run tests/phase8/live-projection.test.ts` PASS; purity scan lists zero non-allowlisted writes</verify>
  <acceptance_criteria>Projection purity is machine-enforced from this plan onward (08-03/05 extend the allowlist explicitly)</acceptance_criteria>
</task>

</tasks>

<verification>
1. Transition→board <2s Playwright evidence (⚠ visual smoothness → CEO eye at close)
2. live-projection.test.ts PASS incl. purity gate
3. Stale-degrade demonstrated (kill realtime container/socket in test env → stamp flips)
4. Build + zero hard-coded strings/colors greps
</verification>
