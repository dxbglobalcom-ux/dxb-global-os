---
phase: 08-ceo-dashboard-crm
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/dashboard/src/app/(cockpit)/costs/page.tsx
  - apps/dashboard/src/components/costs/cost-breakdown.tsx
  - apps/dashboard/src/components/costs/data-tile.tsx
  - apps/dashboard/src/app/(cockpit)/tasks/[id]/page.tsx
  - apps/dashboard/src/components/audit-timeline.tsx
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - tests/phase8/cost-view.test.ts
autonomous: true
requirements: [COST-04, DASH-03]
must_haves:
  truths:
    - "Cost view unifies cost_ledger + LiteLLM tagged-call data with dept/model/mode breakdown toggles; every displayed total EQUALS the SQL aggregate byte-for-byte (test-enforced)"
    - "All cost numbers render Geist Mono tabular; no hero-metric template, no pie charts (UI-SPEC §8 costs row)"
    - "Task drill-down renders ONLY that task's audit.trace chronology — no cross-task firehose exists on the page (DASH-03)"
    - "Costs panel updates live via dxb:cost_ledger; freshness stamp present"
  artifacts:
    - "costs/page.tsx + CostBreakdown (dept/model/mode toggle, per-dimension bar composition per dataviz discipline) + DataTile (plain-layout metric per UI-SPEC §4)"
    - "tasks/[id]/page.tsx + AuditTimeline (vertical chronology: mono stamp + actor + action + payload disclosure)"
    - "tests/phase8/cost-view.test.ts — UI aggregation vs SQL SUM equality across all three dimensions"
  key_links:
    - "cost_ledger schema + litellm virtual-key tags (packages/shared litellm.ts) <-> breakdown dimensions"
    - "DataTile <-> Horizon Line 'today's cost' counter (same query, single source)"
    - "audit.trace shape (Phase 4 conventions) <-> AuditTimeline renderer"
---

<objective>
Money and truth surfaces: COST-04 cost meter (dept/model/mode) with SQL-equal totals live over Broadcast, and the task drill-down audit chronology (master plan steps 6+8).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-08.md   (steps 6+8; firehose ban; SQL-equality verification row)
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (§4 DataTile plain-layout; §8 costs: bar/line only, single-hue + status exception; audit timeline row format)
@packages/shared/src/litellm.ts + db-types.ts   (cost_ledger fields, tag dimensions dept/model/mode)
@~/.claude/skills — dataviz skill triggers on chart work: READ IT before writing the first chart line (single-system palette, axis/legend rules)
</context>

## Artifacts this phase produces

**This plan:** costs page (live breakdown + DataTiles), reusable AuditTimeline + task drill-down page, cost equality test.

<tasks>

<task type="auto">
  <name>Task 1: DataTile + CostBreakdown + costs page (live, SQL-equal)</name>
  <files>apps/dashboard/src/app/(cockpit)/costs/page.tsx, src/components/costs/{data-tile,cost-breakdown}.tsx, messages/*</files>
  <read_first>dataviz skill (BEFORE chart code); UI-SPEC §4 DataTile recipe + §8 costs row; litellm.ts tag fields; STACK.md (ai SDK ban — charts are plain React/SVG or a tiny chart lib already vetted, no new heavy dep without STACK check)</read_first>
  <action>
  DataTile: plain layout inside Panel — mono value 20px/650 + 12px label + optional mini trend sparkline (single hue); hairline-grouped at DENSITY 7. CostBreakdown: dimension toggle (departman/model/mod) → horizontal bar list (value-sorted, mono values, single accent-neutral hue; status colors ONLY for budget-threshold marks 70%/100% per Cost Monitor rules); period selector (bugün/7g/30g). Server component computes aggregates via SQL (GROUP BY dimension); client layer subscribes dxb:cost_ledger → invalidate/refresh. Today's-cost Horizon counter switches to this same query module (single source). Empty/loading: skeleton shaped like final layout.
  </action>
  <verify>`pnpm vitest run tests/phase8/cost-view.test.ts` (written in Task 2) green after wiring; Playwright: toggle all three dimensions, values visibly mono + sorted; skeleton screenshot</verify>
  <acceptance_criteria>No pie; single-hue bars + status-threshold exception only; period + dimension toggles work; live refresh on seeded cost insert</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SQL-equality test (COST-04 gate)</name>
  <files>tests/phase8/cost-view.test.ts</files>
  <read_first>cost_ledger seed conventions in tests/phase4; the aggregate module from Task 1</read_first>
  <action>
  Seed a deterministic cost_ledger set spanning ≥3 depts × ≥2 models × 2 modes with awkward decimals. Test: for each dimension, call the SAME aggregate module the page uses and compare against an independent raw SQL SUM (string-compared at the rendered precision). Any drift (rounding, filter mismatch, tag miss) fails. Include a period-boundary case (yesterday-23:59 vs today-00:01).
  </action>
  <verify>vitest PASS; intentionally corrupt one tag in a scratch run → test fails (prove sensitivity), revert</verify>
  <acceptance_criteria>Equality enforced across dept/model/mode + period edge; sensitivity demonstrated in output</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: AuditTimeline + task drill-down page</name>
  <files>apps/dashboard/src/app/(cockpit)/tasks/[id]/page.tsx, src/components/audit-timeline.tsx, messages/*</files>
  <read_first>Phase 4 audit.trace shape; UI-SPEC §5 drill-down (vertical time line, mono stamps) + §8 tasks row; DASH-03 no-firehose rule</read_first>
  <action>
  tasks/[id]: server-fetch single task + its full audit trace chronology (ORDER BY ts). AuditTimeline: vertical line, per-row mono timestamp + actor chip (agent/dept/CEO) + action label + payload disclosure (collapsed JSON, lazy). Page header: task title, current state chip, cost-so-far (mono), link back. NO subscriptions to other tasks; live updates only for THIS task id (filter on dxb:task_events payload). 404 state for unknown id.
  </action>
  <verify>Playwright: navigate from 08-02 changed-feed row → drill-down renders full chronology of that task only; seeded second task's events absent from page (grep DOM); build green</verify>
  <acceptance_criteria>Chronology complete for target task; zero cross-task rows; payload disclosure lazy; state chip icon+text</acceptance_criteria>
</task>

</tasks>

<verification>
1. cost-view.test.ts PASS + sensitivity proof line
2. Playwright: three-dimension toggle screenshots; drill-down single-task DOM proof
3. Live cost insert → panel refresh <2s evidence
4. dataviz discipline check: single-hue + threshold-only status colors (screenshot + grep)
</verification>
