---
phase: 08-ceo-dashboard-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/20260710000013_broadcast_triggers.sql
  - apps/dashboard/package.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/src/app/layout.tsx
  - apps/dashboard/src/app/globals.css
  - apps/dashboard/src/app/(auth)/login/page.tsx
  - apps/dashboard/src/app/(cockpit)/layout.tsx
  - apps/dashboard/src/app/(cockpit)/page.tsx
  - apps/dashboard/src/lib/supabase.ts
  - apps/dashboard/src/lib/i18n.ts
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - apps/dashboard/src/components/shell/app-shell.tsx
  - apps/dashboard/src/components/shell/horizon-line.tsx
  - apps/dashboard/PRODUCT.md
  - apps/dashboard/DESIGN.md
  - tests/phase8/broadcast-triggers.test.ts
autonomous: true
requirements: [DASH-07]
must_haves:
  truths:
    - "realtime.broadcast_changes helper EXISTS in the local Supabase image (verified via \\df) BEFORE the trigger migration is written — polling fallback is FORBIDDEN (master plan risk rule)"
    - "task_events INSERT produces a row in realtime.messages on channel dxb:task_events (psql evidence)"
    - "Anonymous request to any (cockpit) route returns 401/redirect — auth wall is real, not layout-deep"
    - "Design tokens in globals.css are byte-derived from 08-UI-SPEC.md §2 OKLCH table — no ad-hoc colors anywhere"
    - "impeccable context.mjs finds PRODUCT.md + DESIGN.md (no NO_PRODUCT_MD) so every later UI step runs under the discipline"
  artifacts:
    - "db/migrations/20260710000013_broadcast_triggers.sql — triggers for task_events/approvals/cost_ledger + realtime.messages RLS (channel join = authenticated CEO only)"
    - "apps/dashboard converted to Next.js 16.2.x App Router + @supabase/ssr auth + Tailwind v4 theme tokens (dark primary + light)"
    - "AppShell (rail + topbar + Horizon Line) + login scene per UI-SPEC §8"
    - "tests/phase8/broadcast-triggers.test.ts — INSERT→realtime.messages proof for all three channels"
  key_links:
    - "dxb:{task_events,approvals,cost_ledger} channel names <-> lib/realtime.ts subscriptions (08-02/03/04)"
    - "globals.css @theme tokens <-> every component plan 08-02..08-07"
    - "messages/tr.json keys <-> all UI strings (hard-coded string ban, DASH-06)"
deviations:
  - "Migration number 0013 (not master plan's literal '0011'): 0011/0012 already consumed by tool_pins + registry_ceo_research. Name broadcast_triggers preserved. Mandatory-adaptation record per master-plan-fidelity."
---

<objective>
Foundation: Broadcast-from-DB triggers + channel RLS, Next.js 16 App Router conversion of apps/dashboard with @supabase/ssr auth (CEO single user, 2FA), Tailwind v4 token system implementing UI-SPEC §2-§3, AppShell + Horizon Line + login scene, i18n skeleton (tr primary). Master plan steps 2-3 + the design-foundation half of step 1 (study closed at planning — commit 2f19966; UI-SPEC approved ⛔).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-08.md   (LOCKED: broadcast_changes pattern block, channel naming, auth rules, ai-SDK lint ban)
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (binding design contract — tokens §2, type §3, shell §5, login §8)
@.planning/phases/08-ceo-dashboard-crm/08-CONTEXT.md
@.planning/research/STACK.md   (Version Compatibility: next 16.2.x + @supabase/ssr, NOT auth-helpers; Tailwind v4; broadcast_changes helper presence check)
@.planning/research/study-cards/design-bundle.md   (impeccable setup chain: context.mjs → init if NO_PRODUCT_MD)
</context>

## Artifacts this phase produces

**This plan:** migration 0013 (broadcast triggers + RLS), Next.js dashboard app shell (layout.tsx, globals.css @theme tokens, AppShell, HorizonLine, login page, cockpit empty shell), lib/supabase.ts (ssr client pair), lib/i18n.ts + messages/{tr,en}.json, PRODUCT.md + DESIGN.md (impeccable init output, derived from UI-SPEC), tests/phase8/broadcast-triggers.test.ts.
**Phase-wide (context):** task board/roster (08-02), approval inbox (08-03), costs+drilldown (08-04), command bar (08-05), CRM (08-06), i18n-complete+responsive+closure (08-07).

<tasks>

<task type="auto">
  <name>Task 1: broadcast triggers migration + channel RLS + proof test</name>
  <files>db/migrations/20260710000013_broadcast_triggers.sql, tests/phase8/broadcast-triggers.test.ts</files>
  <read_first>.planning/master-plan/PHASE-08.md (SQL kalıp block), db/migrations/20260708000009_routing_and_deps.sql (style/idempotency conventions), packages/shared/src/db.ts</read_first>
  <action>
  FIRST verify helper exists: `psql "$DXB_DATABASE_URL" -c "\df realtime.broadcast_changes"` → must list the function. If absent: STOP, record blocker (image upgrade required), do NOT write polling fallback.
  Write migration 0013 following the master plan kalıp exactly: `broadcast_task_events()` SECURITY DEFINER function + AFTER INSERT trigger on task_events; same pattern for approvals (AFTER INSERT OR UPDATE) and cost_ledger (AFTER INSERT); channels 'dxb:task_events'/'dxb:approvals'/'dxb:cost_ledger'. Add realtime.messages RLS policy: only authenticated role may SELECT (channel join) — write policy scoped to the three dxb:% topics. Apply with existing migration runner. Test (vitest, tests/phase8/): INSERT a row into each of the three tables → assert a realtime.messages row with matching topic exists (and clean up).
  </action>
  <verify>supabase migration applied cleanly; `pnpm vitest run tests/phase8/broadcast-triggers.test.ts` → 3 channel assertions PASS; psql \df evidence captured in output</verify>
  <acceptance_criteria>All three tables broadcast on write; channel RLS denies anon; test file green; no postgres_changes anywhere</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Next.js 16 conversion + @supabase/ssr auth wall + token system</name>
  <files>apps/dashboard/package.json, next.config.ts, src/app/layout.tsx, src/app/globals.css, src/lib/supabase.ts, src/middleware.ts</files>
  <read_first>.planning/research/STACK.md Version Compatibility table, 08-UI-SPEC.md §2 (full OKLCH tables) + §3 + §6 motion tokens, pnpm-workspace.yaml (allowBuilds conventions)</read_first>
  <action>
  Convert apps/dashboard to Next.js: add next@16.2.x react@19 react-dom @supabase/supabase-js @supabase/ssr; NO `ai` SDK yet (arrives 08-05 render-only); add eslint no-restricted-imports rule banning @anthropic-ai/*, openai, ai/rsc provider imports (master plan lint rule). App Router structure: root layout (Geist via next/font self-host, lang from i18n, dark class strategy `data-theme`), globals.css with Tailwind v4 `@theme` block containing EVERY token from UI-SPEC §2 dark+light tables verbatim (OKLCH strings) + type scale §3 + motion tokens §6 + semantic z-scale. middleware.ts + lib/supabase.ts per @supabase/ssr App Router pattern (createServerClient/createBrowserClient, cookie handling); (cockpit) route group requires session → redirect to /login. Supabase Auth: email+password CEO user seeded via env-driven script (no plaintext in repo), MFA/TOTP enrollment enforced at login flow (Supabase MFA API).
  </action>
  <verify>`pnpm --filter @dxb/dashboard build` succeeds; `curl -s -o /dev/null -w '%{http_code}' localhost:3000/` (cockpit root, no cookie) → 307→/login or 401; grep confirms zero hard-coded hex colors in src/app (tokens only)</verify>
  <acceptance_criteria>Build green; anon blocked; tokens byte-match UI-SPEC; lint rule rejects provider SDK import (prove with a temp file compile fail, then delete)</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: impeccable init derivation + AppShell + Horizon Line + login scene + i18n skeleton</name>
  <files>apps/dashboard/PRODUCT.md, DESIGN.md, src/components/shell/app-shell.tsx, src/components/shell/horizon-line.tsx, src/app/(auth)/login/page.tsx, src/app/(cockpit)/layout.tsx, src/app/(cockpit)/page.tsx, src/lib/i18n.ts, messages/tr.json, messages/en.json</files>
  <read_first>08-UI-SPEC.md §4 (Panel/Double-Bezel, buttons, focus), §5 (AppShell + Horizon Line spec), §6 (door-opening transition), §8 (login + cockpit page rules); run `node .claude/skills/impeccable/scripts/context.mjs` first</read_first>
  <action>
  Run impeccable context.mjs; on NO_PRODUCT_MD write PRODUCT.md (register: product; audience: non-technical CEO; job: run the company from one cockpit) and DESIGN.md derived FROM 08-UI-SPEC (do not invent divergent tokens — DESIGN.md mirrors the spec for the skill's hooks). Build AppShell: left icon rail (64px, expand 220px on hover, Phosphor icons, backdrop-blur allowed — fixed chrome), topbar with Horizon Line (1px accent gradient + pulse dot 2.4s loop + placeholder slots for live counters wired in 08-02), content grid max-w-[1600px]. Login page per §8: dark scene, single ambient gold light (one radial, no mesh), DXB monogram (simple inline SVG geometric mark), Supabase Auth form (email+password+TOTP), door-opening clip-path transition to cockpit (700ms, once per session, reduced-motion: instant). Cockpit page.tsx = empty-state shell ("Kapı temiz" style placeholder) — real panels arrive 08-02. i18n: minimal dictionary loader (lib/i18n.ts, server-side, tr default + en), ALL strings introduced so far live in messages/tr.json+en.json.
  </action>
  <verify>Playwright (MCP or `pnpm exec playwright`): screenshot login (375px+1440px, dark) + cockpit shell after seeded login; axe/contrast spot-check on login form; grep src for literal Turkish strings outside messages/ → 0</verify>
  <acceptance_criteria>context.mjs reports PRODUCT.md found; shell renders both themes without color literals; login→cockpit transition works and respects prefers-reduced-motion; every visible string comes from messages/</acceptance_criteria>
</task>

</tasks>

<verification>
1. `psql \df realtime.broadcast_changes` → exists (evidence line in SUMMARY)
2. `pnpm vitest run tests/phase8/broadcast-triggers.test.ts` → PASS ×3 channels
3. `pnpm --filter @dxb/dashboard build` → success
4. anon → (cockpit) HTTP 3xx/401 evidence
5. Playwright screenshots (login/shell, 2 viewports × dark) attached; visual "Burj feel" flagged ⚠ UNVERIFIED (CEO eye at phase close)
</verification>
