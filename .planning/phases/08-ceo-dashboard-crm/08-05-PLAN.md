---
phase: 08-ceo-dashboard-crm
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/dashboard/src/components/command-bar.tsx
  - apps/dashboard/src/app/api/intent/route.ts
  - apps/dashboard/src/components/intent-strip.tsx
  - apps/dashboard/package.json
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - tests/phase8/command-bar-intent.test.ts
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "CEO types a Turkish intent → it reaches the kernel end-to-end through the dxb-mcp/HTTP seam and a task chain lands in the queue; result state appears on the board (dashboard = FIRST kernel client)"
    - "The dashboard performs ZERO LLM calls — no provider SDK anywhere; `ai` package (if added) used strictly for render/stream helpers; lint rule from 08-01 still passing"
    - "Intent submission is one of exactly two dashboard write paths (intents insert) — purity allowlist extended precisely, nothing broader"
    - "Command bar reachable via ⌘K + persistent topbar affordance on desktop AND bottom fixed strip on mobile (UI-SPEC §4)"
  artifacts:
    - "CommandBar (modal layer, TR/EN placeholder rotation, recent intents) + IntentStrip (submitted-intent progress chip row wired to task_events)"
    - "api/intent/route.ts — server route: auth check → kernel seam call (packages/shared envelope conventions) → intent row + queue task chain"
    - "tests/phase8/command-bar-intent.test.ts — TR intent fixture → queue rows exist → transition events visible to board query"
  key_links:
    - "kernel client seam (packages/kernel or dxb-mcp HTTP surface — follow Phase 5 client conventions in packages/shared/envelope.ts) <-> route.ts"
    - "IntentStrip <-> dxb:task_events (08-02 helper reuse)"
    - "purity allowlist (live-projection.test.ts) <-> intents write path"
---

<objective>
Command bar → kernel (master plan step 7): the anti-baby-sitting core made tangible — CEO states intent once in Turkish, kernel executes, cockpit shows the chain. Dashboard stays a pure client (no LLM in-process).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/master-plan/PHASE-08.md   (step 7 + LOCKED 'command bar → kernel seam; no LLM in dashboard')
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (§4 CommandBar: modal layer blur allowed on fixed overlay; intent tracked in strip not toast)
@packages/shared/src/envelope.ts + packages/kernel   (Phase 5 kernel intake contract — the seam the dashboard calls; DO NOT invent a second intake shape)
@.planning/phases/05-kernel-orchestrator-core-loop/*SUMMARY.md   (how intents become task chains — consume, don't re-model)
@tests/phase8/live-projection.test.ts   (purity allowlist to extend)
</context>

## Artifacts this phase produces

**This plan:** CommandBar + IntentStrip, /api/intent kernel-seam route, intent E2E test, purity allowlist extension.

<tasks>

<task type="auto">
  <name>Task 1: /api/intent kernel seam route</name>
  <files>apps/dashboard/src/app/api/intent/route.ts</files>
  <read_first>packages/shared/src/envelope.ts (envelope shape), Phase 5 SUMMARY intake section, master plan seam rule; 08-01 middleware auth pattern</read_first>
  <action>
  POST route: verify CEO session (supabase ssr server client); validate body {text, lang} (zod, 1..500 chars); write intents row (status=received, source='dashboard'); call the kernel intake exactly as Phase 5 defines (existing client util if present in packages/shared — else the documented dxb-mcp/HTTP seam; envelope with intent id, actor='ceo', lang); return {intentId}. Errors: kernel unreachable → intent row status='failed_dispatch' + typed error (UI shows inline panel, retry affordance). NO model call, no interpretation — raw text passes through; kernel owns understanding.
  </action>
  <verify>curl with session cookie + TR body → 200 {intentId}; intents row + kernel-side queue rows exist (psql); anon curl → 401; grep route for provider SDK imports → none</verify>
  <acceptance_criteria>Seam matches Phase 5 contract (no new intake shape); auth enforced; failure path recorded and surfaced</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CommandBar + IntentStrip UI</name>
  <files>apps/dashboard/src/components/command-bar.tsx, src/components/intent-strip.tsx, package.json, messages/*</files>
  <read_first>UI-SPEC §4 CommandBar row (⌘K, mobile bottom strip, modal z-scale, backdrop-blur fixed-overlay-only), §6 motion tokens; 08-02 realtime helper</read_first>
  <action>
  CommandBar: ⌘K listener + topbar affordance + mobile fixed bottom strip; opens modal layer (z modal, scrim 0.55, blur on the fixed overlay allowed); input with TR/EN rotating placeholder ('Outleteuro için hafta sonu kampanya taslağı hazırla…'); submit → POST /api/intent → optimistic chip in IntentStrip; recent 5 intents listed (from intents table). IntentStrip: chip row under Horizon Line showing in-flight intents (received→queued→running→done/failed via dxb:task_events correlation on intent id); click → drill-down of the chain's root task. If `ai` SDK streaming helpers are needed for future talk-panel, DO NOT add now — plain fetch suffices (STACK: ai only-when-render-streaming; defer to the panel that needs it). Reduced-motion: modal fade only.
  </action>
  <verify>Playwright: ⌘K opens bar, TR intent submits, chip appears and transitions as seeded kernel processes; mobile viewport shows bottom strip; ESC/scrim closes; build + lint green</verify>
  <acceptance_criteria>Both entry points (⌘K + mobile strip) work; chip lifecycle mirrors task chain; no toast-based intent status; strings localized</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E intent test + purity allowlist extension</name>
  <files>tests/phase8/command-bar-intent.test.ts, tests/phase8/live-projection.test.ts (allowlist edit)</files>
  <read_first>08-03 allowlist edit pattern; kernel test seeds from tests/phase5</read_first>
  <action>
  Test: authenticated POST with Turkish fixture → assert (a) intents row, (b) kernel queue task-chain rows exist and reference the intent, (c) subsequent transition event is the shape the board consumes. Extend purity allowlist with exactly `intents.insert` (route path) — assert allowlist length is now 3 write-paths total (approvals decision, audit append, intents insert) and nothing else in src writes.
  </action>
  <verify>`pnpm vitest run tests/phase8/command-bar-intent.test.ts` PASS; purity gate PASS with 3-path allowlist</verify>
  <acceptance_criteria>TR intent E2E proven at DB level; write-surface still minimal and enumerated</acceptance_criteria>
</task>

</tasks>

<verification>
1. command-bar-intent.test.ts PASS (intent→chain→event)
2. Playwright TR intent journey (desktop ⌘K + mobile strip) screenshots
3. Purity allowlist = exactly 3 paths, gate green
4. Zero provider-SDK imports (lint + grep evidence)
</verification>
