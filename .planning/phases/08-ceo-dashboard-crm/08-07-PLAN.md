---
phase: 08-ceo-dashboard-crm
plan: 07
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04", "08-05", "08-06"]
files_modified:
  - apps/dashboard/messages/tr.json
  - apps/dashboard/messages/en.json
  - apps/dashboard/src/app/(cockpit)/**/*.tsx (responsive/a11y fixes)
  - scripts/contrast-audit.mjs
  - .planning/phases/08-ceo-dashboard-crm/08-VERIFICATION.md
autonomous: false
requirements: [DASH-06, DASH-07]
must_haves:
  truths:
    - "messages/tr.json is COMPLETE: mechanical scan finds zero hard-coded user-facing strings in src; tr is primary, en secondary; labels Turkish-friendly for a non-technical CEO"
    - "Cockpit usable on a phone: 375px pass on every page (no horizontal overflow, touch ≥44px, safe-areas); Lighthouse mobile accessibility ≥ 90 (measured, number recorded)"
    - "UI-SPEC §9 verification battery executed IN FULL: contrast table, anti-pattern grep set (0 hits), Playwright viewport×theme×reduced-motion matrix, LCP < 2.5s cockpit"
    - "Phase closes ONLY with Fable-authored verdict in 08-VERIFICATION.md; visual 'Burj feel' items listed ⚠ UNVERIFIED for CEO eye — never self-declared VERIFIED"
  artifacts:
    - "scripts/contrast-audit.mjs — OKLCH→sRGB WCAG ratio table for every UI-SPEC §9.1 pair (reusable in CI)"
    - "Complete tr/en message catalogs"
    - "08-VERIFICATION.md — two-tier evidence table (✓ VERIFIED with command→output / ⚠ UNVERIFIED with reason) covering all 5 ROADMAP success criteria + 9 requirement IDs"
  key_links:
    - "UI-SPEC §9 battery <-> VERIFICATION evidence rows (1:1)"
    - "ROADMAP Phase 8 success criteria 1-5 <-> VERIFICATION criterion table"
    - "Lighthouse + contrast outputs <-> DASH-06 acceptance"
---

<objective>
Phase closure: i18n completeness, phone-grade responsive + accessibility hardening, the full UI-SPEC §9 verification battery, and the Fable closure verdict (master plan steps 10-11). `autonomous: false` — final visual acceptance requires CEO eye; everything machine-checkable is machine-checked first.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ceo-dashboard-crm/08-UI-SPEC.md   (§9 verification battery — the checklist this plan executes; §7 grep set)
@.planning/ROADMAP.md Phase 8 success criteria (5 items)
@.planning/phases/07-mcp-gateway-24-7-vps-runtime/07-VERIFICATION.md   (two-tier evidence format precedent)
@.planning/master-plan/PHASE-08.md   (steps 10-11; ⛔ FABLE-ONLY closure)
</context>

## Artifacts this phase produces

**This plan:** contrast-audit script + results, completed message catalogs, responsive/a11y fix pass, 08-VERIFICATION.md with Fable verdict.

<tasks>

<task type="auto">
  <name>Task 1: i18n completeness + hard-coded string sweep</name>
  <files>apps/dashboard/messages/tr.json, messages/en.json, src fixes</files>
  <read_first>All (cockpit) pages built in 08-01..06; DASH-06 wording (Turkish-friendly labels, non-technical CEO)</read_first>
  <action>
  Mechanical sweep: AST/grep scan for JSX text nodes + aria-labels + title/alt attributes outside messages/; migrate every hit. Review tr labels for non-technical clarity (CEO dili: 'Onay bekleyenler' not 'Pending approvals queue'; no engineer jargon). en catalog mirrors keys 1:1 (missing-key check script). Date/number formatting via Intl with tr-TR default.
  </action>
  <verify>Scan script output: 0 hard-coded user-facing strings; key-parity check tr↔en: 0 missing; Playwright spot-check: cockpit renders fully in tr, toggle to en renders (no raw keys on screen)</verify>
  <acceptance_criteria>Zero-hit sweep evidence in SUMMARY; both catalogs complete; CEO-register Turkish reviewed</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: responsive + a11y hardening pass (phone-grade)</name>
  <files>apps/dashboard/src/** (fixes only), scripts/contrast-audit.mjs</files>
  <read_first>UI-SPEC §5 responsive rules + §9.1 contrast pairs + §9.2 grep set; impeccable `audit`/`adapt` references fire here (run the skill commands)</read_first>
  <action>
  Run impeccable `audit` then `adapt` scoped to apps/dashboard (skill discipline, product register). Fix pass: 375px/768px/1440px every page — no horizontal overflow, tables→stacked cards, touch targets ≥44px, safe-area insets, focus order + visible rings, aria on live regions (board updates announced politely), reduced-motion full matrix. Write scripts/contrast-audit.mjs (culori: OKLCH→sRGB, WCAG ratio) printing the §9.1 table with PASS/FAIL per pair in BOTH themes; fix any FAIL by adjusting tokens (record token change as UI-SPEC amendment note if needed). Run anti-pattern grep set (§7 list as a script or documented one-liner battery) → must be 0 hits.
  </action>
  <verify>contrast-audit table all PASS (output pasted); grep battery 0 hits; Playwright matrix (3 viewports × 2 themes × reduced-motion on/off) screenshot set saved; axe scan on cockpit+approvals: no critical violations</verify>
  <acceptance_criteria>All §9.1 pairs pass in both themes; §9.2 battery clean; matrix evidence archived under phase dir</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lighthouse + LCP measurement</name>
  <files>(measurements; fixes if score <90)</files>
  <read_first>UI-SPEC §9.4 targets; STACK.md perf notes (next/image, font swap)</read_first>
  <action>
  Run Lighthouse (mobile emulation) against the served production build (`next build && next start`): cockpit, approvals, costs, crm/clients, login. Record accessibility + performance scores and LCP per page. If accessibility <90 anywhere: fix and re-run until ≥90 (loop belongs to this task). LCP cockpit target <2.5s local-VPS-class conditions; record number even if environment-noisy (note conditions).
  </action>
  <verify>Lighthouse JSON/score lines pasted per page; accessibility ≥90 all pages; LCP values recorded</verify>
  <acceptance_criteria>Measured numbers in SUMMARY (not 'should pass'); a11y ≥90 verified on every page</acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 4: 08-VERIFICATION.md + Fable closure verdict + CEO eye handoff</name>
  <files>.planning/phases/08-ceo-dashboard-crm/08-VERIFICATION.md</files>
  <read_first>07-VERIFICATION.md format; ROADMAP Phase 8 criteria 1-5; all phase-8 SUMMARYs</read_first>
  <action>
  Compose two-tier evidence table: every ROADMAP criterion + every requirement ID (DASH-01..07, GATE-03, COST-04) → ✓ VERIFIED (command → decisive output line) or ⚠ UNVERIFIED (reason + what CEO must eyeball). ⚠ list will include: 'Burj Al Arab feel' aesthetic acceptance, phone-in-hand fluidity, non-technical usability walkthrough. Fable reads the full diff set + evidence PERSONALLY and writes the verdict block (⛔ FABLE-ONLY). CEO usage-test items presented as a short checklist (in Turkish, step-by-step per ceo-communication-style). Phase status flips only per verdict; deviations table included (e.g. migration 0013 numbering).
  </action>
  <verify>08-VERIFICATION.md exists with two-tier table covering 5 criteria + 9 req IDs; verdict block authored by Fable inline</verify>
  <acceptance_criteria>No mixed-tier claims; every ✓ row carries executed evidence; CEO checklist ready; verdict present</acceptance_criteria>
</task>

</tasks>

<verification>
1. i18n sweep + parity: 0/0 evidence
2. contrast-audit: all pairs PASS both themes (table output)
3. Anti-pattern battery: 0 hits (command list + outputs)
4. Lighthouse mobile a11y ≥90 × 5 pages + LCP numbers
5. 08-VERIFICATION.md two-tier complete; ⚠ items enumerated for CEO eye; ⛔ Fable verdict written
</verification>
