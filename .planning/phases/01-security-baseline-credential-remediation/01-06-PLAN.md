---
phase: 01-security-baseline-credential-remediation
plan: 06
type: execute
wave: 5
depends_on: [01-02, 01-04, 01-05]
files_modified:
  - .planning/phases/01-security-baseline-credential-remediation/evidence/trufflehog-verified-sweep.md
  - .planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]
must_haves:
  truths:
    - "Zero verified live secrets among trufflehog-supported detectors anywhere in full history (verified sweep, exit 0) — plain-password dead-proofs (Google, Microsoft, Namecheap, hostloom, WordPress, sudo) come from the 21 per-item probes in ROTATION-EVIDENCE.md, which trufflehog cannot cover (cross-AI review MED-3)"
    - "Every hard-gate criterion has a machine-generated pass row in the gate report: evidence completeness via the column-aware DEAD?-column parser (cross-AI review HIGH-5) plus the unique CRED-01..21 ID-set gate and the exactly-one-CRED-17-row grace parse (cycle 3 MED-6), vault presence, scanner active + history clean, sanitized doc tracked + original absent"
    - "The SEC-02 row treats the A8 deny check as blocking AND machine-readable: it parses the single A8-RESULT line from plan 01-02's committed evidence/a8-deny-check.md — PASSED passes; FAILED+COMPENSATED passes only with the compensating-control block live; a missing artifact, malformed line, or bare FAILED renders the report BLOCKED (cross-AI review HIGH-1)"
    - "The CEO has confirmed 2FA on Google, Cloudflare, Namecheap, Microsoft x2, Private Email x2 and hosting (or a recorded risk-acceptance for hosting), backup codes stored, and password uniqueness — and has approved the phase exit"
    - "Phase 2 is unblocked only by this plan's completion — nothing before it"
  artifacts:
    - ".planning/phases/01-security-baseline-credential-remediation/evidence/trufflehog-verified-sweep.md"
    - ".planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md"
  key_links:
    - "trufflehog --only-verified complements the 21 per-item 401 proofs with a detector-scoped whole-repo statement: zero verified live secrets among supported detectors; password dead-proofs remain the per-item probes (MED-3)"
    - "gate report -> CEO sign-off checkpoint (the report is the evidence the CEO approves against; a blocked report stops the checkpoint)"
---

<objective>
Close the CEO-locked hard gate: run the trufflehog verified sweep as the semantic whole-repo proof, machine-generate the gate report re-running every SEC-01..04 check live, and obtain the CEO's final 2FA + uniqueness confirmation and phase-exit approval.

Purpose: ROADMAP locked decision — Phase 2 and any build work MUST NOT start until old values are verifiably dead, 2FA confirmed, ODT sanitized, vault in place, and the secret scan passes clean. This plan is the gate itself.
Output: two evidence artifacts + recorded CEO approval; Phase 1 exit.
</objective>

## Artifacts this phase produces

Full Phase 1 artifact set (bold = owned by this plan):

- `.gitleaks.toml`, `scripts/hooks/pre-commit`, `.github/workflows/secret-scan.yml`, `evidence/hook-selftest.md`, `evidence/gitleaks-history-report.json` (plan 01-01)
- `.gitignore` vault rules, `.env.example`, `.claude/settings.json` deny rules (plan 01-02)
- `CREDENTIAL-ROTATION-CHECKLIST.md`, `evidence/ROTATION-EVIDENCE.md` template, `ODT-SANITIZATION-PROCEDURE.md` (plan 01-03)
- Filled rotation evidence (plan 01-04)
- `docs/source-architecture-notes-sanitized.md`, `evidence/ODT-SANITIZATION-EVIDENCE.md` (plan 01-05)
- **`evidence/trufflehog-verified-sweep.md`** (this plan)
- **`evidence/PHASE-1-GATE-REPORT.md`** (this plan)

All `evidence/` paths are under `.planning/phases/01-security-baseline-credential-remediation/evidence/`.

Binaries installed outside repo by this plan: `~/.local/bin/trufflehog`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md
@.planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md
@.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trufflehog verified sweep over full history</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/trufflehog-verified-sweep.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md — "Post-rotation verified sweep" section and Package Legitimacy Audit (trufflehog: Approved, checksum-verified binary)
  </read_first>
  <action>
    Download the trufflehog v3 linux amd64 binary from github.com/trufflesecurity/trufflehog/releases; verify its sha256 against the release checksums file before extracting; install to `~/.local/bin/trufflehog`.

    Run `trufflehog git file://. --only-verified --fail --json > /tmp/th-sweep.json 2>/tmp/th-sweep.log` from repo root. Output goes to scratch files OUTSIDE the repo because trufflehog can print candidate secret material on findings — the scratch files are never committed and are deleted at the end of this task. Interpretation (scoped precisely — cross-AI review MED-3): exit 0 = zero verified findings among trufflehog-supported detectors (API keys/tokens it can live-test against providers); this does NOT prove dead-status for plain passwords (Gmail, Hotmail, Namecheap, hostloom, WordPress, sudo) — those proofs are the 21 per-item probes in ROTATION-EVIDENCE.md; exit 183 = a verified LIVE secret exists somewhere in history — STOP, extract only DetectorName and file path from the JSON (never the raw value), report those to the CEO: a rotation was missed or incomplete, and the phase gate cannot pass. This tool makes live network calls to providers — run it once, do not loop it.

    Write `evidence/trufflehog-verified-sweep.md`: UTC timestamp, trufflehog version, the exact command, the exit code, the verified-findings count (must be 0), and the scope statement: "zero verified live secrets among trufflehog-supported detectors; plain-password dead-proofs come from the 21 per-item probes in ROTATION-EVIDENCE.md". Delete /tmp/th-sweep.json and /tmp/th-sweep.log. Commit the evidence file.
  </action>
  <verify>
    <automated>~/.local/bin/trufflehog git file://. --only-verified --fail >/dev/null 2>&1 && test -f '.planning/phases/01-security-baseline-credential-remediation/evidence/trufflehog-verified-sweep.md'</automated>
  </verify>
  <acceptance_criteria>
    - `trufflehog git file://. --only-verified --fail` exits 0 (re-runnable proof)
    - evidence file exists recording exit code 0, findings count 0, UTC timestamp, tool version
    - evidence file states the sweep's scope: `grep -c 'trufflehog-supported detectors' evidence/trufflehog-verified-sweep.md` outputs at least 1 (MED-3 — no overclaim)
    - sha256 verification of the binary recorded in the task summary; both scratch files deleted (review LOW-1): `test ! -f /tmp/th-sweep.json && test ! -f /tmp/th-sweep.log` exits 0
  </acceptance_criteria>
  <done>Detector-scoped whole-repo proof recorded: zero verified live secrets among trufflehog-supported detectors in full history; plain-password dead-proofs remain the 21 per-item probes.</done>
</task>

<task type="auto">
  <name>Task 2: Machine-generate the phase gate report (all SEC checks, live re-run)</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 1 success criteria (the five hard-gate criteria this report proves)
    - .planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md (verify-only)
    - .planning/phases/01-security-baseline-credential-remediation/evidence/a8-deny-check.md — the committed A8-RESULT line the SEC-02 gate row parses (review HIGH-1)
    - .planning/phases/01-security-baseline-credential-remediation/01-VALIDATION.md — per-task verification map
  </read_first>
  <!-- planner-discipline-allow: .env -->
  <action>
    Execute every hard-gate check LIVE (re-run commands now — do not copy results from earlier summaries) and write `evidence/PHASE-1-GATE-REPORT.md` as a table: requirement ID, check description, exact command, observed output, status. The status column uses exactly two symbols: the green check mark (U+2705) for pass and the red cross mark (U+274C) for fail — no other words or symbols in that column, so the report is machine-gateable.

    Checks: SEC-01 — evidence completeness via the column-aware DEAD?-column parser from plan 01-04 Task 3, never a whole-row reverse filter (cross-AI review HIGH-2/HIGH-5 — a row-level filter false-passes when N/A sits in an unrelated column such as 2FA): `awk -F'|' '$2 ~ /CRED-/ { n++; v=$11; gsub(/[ \t]/,"",v); if (v != "✅" && v !~ /^N\/A\(.+\)/) bad++ } END { print n"-rows "bad+0"-unresolved" }' evidence/ROTATION-EVIDENCE.md` must print `21-rows 0-unresolved`; unique ID-set gate (cycle 3 MED-6 — the row count alone cannot prove the ID set; a duplicate CRED-17 row keeps the count at 21 while masking a pending grace decision): `[ "$(awk -F'|' '$2 ~ /CRED-/ { id=$2; gsub(/[ \t]/,"",id); print id }' evidence/ROTATION-EVIDENCE.md | sort)" = "$(awk 'BEGIN{for(i=1;i<=21;i++) printf "CRED-%02d\n", i}')" ] && echo ID-SET-OK || echo ID-SET-BAD` must print ID-SET-OK (the ID column is exactly CRED-01..CRED-21, no duplicates, no extras); every dead row timestamped AND with a non-empty Observed cell (review MED-4 — same parse, Probe (UTC) field $9 and Observed field $10): `awk -F'|' '$2 ~ /CRED-/ { v=$11; gsub(/[ \t]/,"",v); if (v == "✅" && ($9 !~ /[0-9]{4}-[0-9]{2}-[0-9]{2}|[0-9]{2}:[0-9]{2}Z/ || $10 !~ /[^ \t]/)) bad++ } END { print bad+0 }' evidence/ROTATION-EVIDENCE.md` prints 0; attestation table complete (HIGH-3): `awk -F'|' '$2 ~ /ATT-/ { n++; s=$4; gsub(/[ \t]/,"",s); if (s != "YES" && s != "DONE") bad++ } END { print (n==5 && bad==0) ? "ATT-OK" : "ATT-INCOMPLETE" }' evidence/ROTATION-EVIDENCE.md` prints ATT-OK with ATT-01 Status = YES; Apify structured grace literal parsed from CRED-17's Action column (MED-2 — never a whole-row grep): `awk -F'|' '$2 ~ /CRED-17/ { n++; g=$5 } END { if (n == 1 && (g ~ /grace=declined/ || g ~ /grace=accepted; probe_after_24h=yes/) && g !~ /grace=__PENDING__/) print "GRACE-OK"; else print "GRACE-BAD" }' evidence/ROTATION-EVIDENCE.md` prints GRACE-OK — the `n == 1` guard fails the parse when zero or multiple CRED-17 rows exist, so a duplicate row can never win by being parsed last (cycle 3 MED-6). SEC-02 — `git check-ignore -q .env` exits 0; `.env.example` tracked; `git ls-files | grep -cxF '.env'` = 0; `.claude/settings.json` deny rules parse and contain the three Read entries; A8 gate (HIGH-1 — blocking, machine-parsed from the committed artifact, not from any task summary): `grep -cE '^A8-RESULT: (PASSED|FAILED\+COMPENSATED)$' evidence/a8-deny-check.md` must output exactly 1 — if the file is missing, the count is 0, or the count of lines starting `A8-RESULT:` differs from 1, the row FAILS (unrecorded is not pass); both counts are machine-run as the gate row's commands, mirroring plan 01-02's automated verify: the valid-line grep above = 1 AND `grep -c '^A8-RESULT:' evidence/a8-deny-check.md` = 1 (cycle 3 LOW-2 — a PASSED line plus a stray bare-FAILED line gives valid-count 1 but prefix-count 2 and fails the row); if the line is `A8-RESULT: PASSED` the row passes; if it is `A8-RESULT: FAILED+COMPENSATED` the row passes only with the compensating control live (`grep -c 'OUT-OF-WORKSPACE VAULT' .env.example` outputs at least 1 and `test ! -f .env` at the workspace root — the real vault then lives at ~/.dxb-vault/.env, outside workspace tooling and git scope though not shell-proof until Phase 2's least-privilege profiles, per review MED-5); any other content is a failing row and the report header goes BLOCKED. SEC-03 — `git config core.hooksPath` = scripts/hooks; `test -x scripts/hooks/pre-commit` passes; hook-selftest evidence exists; `gitleaks git --redact` full-history re-run exits 0 NOW; CI workflow file present with full-history checkout. SEC-04 — `docs/source-architecture-notes-sanitized.md` tracked; key-shape grep on it = 0; `git log --all --name-only --format= | grep -ci '\.odt$'` = 0; ODT sanitization evidence exists with CEO approval recorded. Cross-cutting — trufflehog sweep exit 0 (Task 1 evidence).

    Append an operational-notes section: fresh clones must run `git config core.hooksPath scripts/hooks` once; the CI workflow arms on first push to a GitHub remote (repo currently has none); pin gitleaks-action to a commit SHA at that point; the A8-RESULT line quoted from evidence/a8-deny-check.md and — if FAILED+COMPENSATED — the out-of-workspace vault compensating control now in force (this note records the outcome; the pass/fail decision itself lives in the SEC-02 gate row above, which parses that artifact line, per review HIGH-1). In the SEC-03 CI row, state the status explicitly as "wired, arms on first push" — never an unqualified pass. In the SEC-02 section, add one line recording that the runtime `.env` loader is deferred to Phase 2 by documented sequencing choice (no code exists to load into this phase) so the CEO signs off on the deferral explicitly.

    If ANY check fails: mark the report header BLOCKED, list the failing requirement IDs, and STOP — do not proceed to Task 3; the fix loops back through the owning plan.
  </action>
  <verify>
    <automated>test -f '.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md' && test "$(grep -c '❌' '.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md')" = "0" && grep -q 'SEC-04' '.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md' && grep -q '21-rows 0-unresolved' '.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md' && test "$(awk -F'|' '$2 ~ /CRED-/ { id=$2; gsub(/[ \t]/,"",id); print id }' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md' | sort)" = "$(awk 'BEGIN{for(i=1;i<=21;i++) printf "CRED-%02d\n", i}')" && grep -q 'ID-SET-OK' '.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md'</automated>
  </verify>
  <acceptance_criteria>
    - report exists and contains at least one check row for each of SEC-01, SEC-02, SEC-03, SEC-04
    - SEC-01 rows generated by the column-aware parsers (HIGH-5): the report records `21-rows 0-unresolved`, ID-SET-OK from the unique CRED-01..21 ID-set gate (cycle 3 MED-6 — additionally re-run live by the automated verify against ROTATION-EVIDENCE.md itself, not only read from the report), the MED-4 timestamp+Observed check printing 0, ATT-OK, and GRACE-OK from the exactly-one-CRED-17-row Action-column parse (MED-2, cycle 3 MED-6)
    - SEC-02 A8 row applies the blocking rule from the committed artifact (HIGH-1): exactly one `A8-RESULT:` line in evidence/a8-deny-check.md — PASSED, or FAILED+COMPENSATED with the compensating control live; a missing/malformed line or bare FAILED yields a failing row and a BLOCKED header; both grep counts (valid-line and `^A8-RESULT:` prefix) run as the row's commands (cycle 3 LOW-2)
    - `grep -c '❌' evidence/PHASE-1-GATE-REPORT.md` outputs 0 (zero failing rows; the cross mark appears only in failing rows by construction)
    - full-history gitleaks re-run at report time exited 0 (recorded in the SEC-03 row)
    - operational-notes section present (clone bootstrap + CI arming + A8 outcome)
  </acceptance_criteria>
  <done>Every hard-gate criterion has a live, machine-generated pass row; the report is the single document the CEO signs off against.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CEO confirms 2FA and uniqueness, approves the phase exit</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/evidence/PHASE-1-GATE-REPORT.md (the report being approved)
  </read_first>
  <action>
    Present the gate report to the CEO for the final human confirmations that no machine can check: (1) 2FA is enrolled and WORKING (a fresh login prompts for the code) on: Google, Microsoft Hotmail x2, Namecheap dashboard, Private Email x2, Cloudflare — and hostloom if the host offers it; if hostloom offers no 2FA, the risk-acceptance line in the evidence stands as the CEO's recorded decision (ROADMAP minimum includes hosting); (2) every backup-code set is stored in the password manager; (3) every new value is unique and password-manager generated (re-affirming the attestation); (4) the CEO has read the gate report and accepts it. On approval, record the approval line with UTC timestamp in the gate report and update the report header from pending to approved. Phase 2 is unblocked by this approval and nothing else.
  </action>
  <how-to-verify>
    1. Log out and back in on one or two of the critical services — the 2FA prompt must appear
    2. Open the password manager — backup codes present for every enrolled service
    3. Read PHASE-1-GATE-REPORT.md — every row passes
  </how-to-verify>
  <acceptance_criteria>
    - CEO approval line with UTC timestamp appended to PHASE-1-GATE-REPORT.md and committed
    - any "no" answer blocks: the gap goes back to the owning plan (rotation → 01-04, vault → 01-02, scan → 01-01, doc → 01-05) before this checkpoint can be re-presented
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the Phase 1 hard gate — or state which confirmation fails.</resume-signal>
  <done>The hard gate is closed with recorded CEO approval; Phase 2 (Foundation & Integration Program) may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo history → external providers | trufflehog live-tests candidate strings against provider APIs; its raw output may contain candidate material and must stay out of the repo |
| gate report → Phase 2 start | the report is the authorization artifact for all downstream build work; a false pass poisons everything after it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23 | Spoofing | phase gate passes while a live leaked secret still exists somewhere in history | critical | mitigate | trufflehog --only-verified semantic sweep + live gitleaks full-history re-run at report time + evidence completeness re-check incl. the unique CRED ID-set gate and single-CRED-17-row grace parse (cycle 3 MED-6) |
| T-01-24 | Tampering | trufflehog binary supply chain | high | mitigate | sha256 checksum against release checksums file before install |
| T-01-25 | Information Disclosure | trufflehog output prints candidate secret material | high | mitigate | output redirected to /tmp scratch outside the repo, never committed, deleted after; evidence records counts/exit codes/detector names only |
| T-01-26 | Repudiation | gate "approved" without evidence backing (tick-without-proof at phase level) | high | mitigate | report rows are machine-generated from live command output; CEO approval is a recorded, timestamped checkpoint against that report |
</threat_model>

<verification>
- trufflehog verified sweep exits 0; evidence recorded without any candidate material and with the detector-scoped statement (MED-3)
- Gate report: zero failing rows across SEC-01..04, generated from live re-runs using the column-aware SEC-01 parsers incl. Observed-cell and Action-column grace checks (HIGH-5, MED-4, MED-2), the unique CRED ID-set gate and single-CRED-17-row grace parse (cycle 3 MED-6), and the blocking A8 rule parsed from evidence/a8-deny-check.md with both grep counts machine-run (HIGH-1, cycle 3 LOW-2)
- CEO approval line timestamped and committed
- Cycle-3 gate simulation: a 21-row evidence file with two CRED-17 entries (one pending, one resolved) and no CRED-18 — the cycle-2 report rows printed `21-rows 0-unresolved` and GRACE-OK (last match won); the hardened rows print ID-SET-BAD (duplicate + gap in the sorted ID list) and GRACE-BAD (n=2), each rendering a ❌ row and a BLOCKED header; an a8-deny-check.md holding a valid PASSED line plus a stray line beginning `A8-RESULT: FAILED` gives valid-count 1 but prefix-count 2 → the SEC-02 row fails (LOW-2)
</verification>

<success_criteria>
The Phase 1 hard gate is verifiably closed: old values dead (21 per-item proofs + detector-scoped verified sweep), 2FA CEO-confirmed, vault in place with the A8 outcome resolved machine-readably (A8-RESULT: PASSED, or FAILED+COMPENSATED with the control live), scan clean on staged + full history, ODT sanitized — and the CEO's approval is recorded. Phase 2 is unblocked.
</success_criteria>

<output>
Create `.planning/phases/01-security-baseline-credential-remediation/01-06-SUMMARY.md` when done.
</output>
