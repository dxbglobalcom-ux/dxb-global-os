---
phase: 01-security-baseline-credential-remediation
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-03, 01-04]
files_modified:
  - docs/source-architecture-notes-sanitized.md
  - .planning/phases/01-security-baseline-credential-remediation/evidence/ODT-SANITIZATION-EVIDENCE.md
autonomous: false
requirements: [SEC-04]
must_haves:
  truths:
    - "A sanitized, credentials-free copy of the source document lives in docs/ as tracked markdown (not .odt, which the ignore rule would silently drop)"
    - "The sanitized content passes the secret scanner and key-shape gates at both the markdown and ODT-container level"
    - "The original .odt has never been committed, remains gitignored, and its synced/trash copies are purged with the sweep recorded"
    - "The executor never opens, prints, or greps-with-output the original document — it contains the leaked credentials"
    - "Word-match line content never enters git: the CEO reviews full match text from a /tmp scratch file; committed evidence holds line numbers + word categories only, and nothing is committed before CEO approval (cross-AI review HIGH-4)"
  artifacts:
    - "docs/source-architecture-notes-sanitized.md (tracked)"
    - ".planning/phases/01-security-baseline-credential-remediation/evidence/ODT-SANITIZATION-EVIDENCE.md"
  key_links:
    - "CEO's LibreOffice Save-As (full zip rewrite) -> container verification (deleted text must not survive in tracked changes/versions)"
    - "markdown export (not .odt) -> git tracking (RESEARCH Pitfall 6: an .odt copy in docs/ would be silently ignored and SEC-04 would fail invisibly)"
    - "CEO word-match approval (Task 3) -> commit (both the sanitized markdown and the evidence file are committed only after the human review passes — review HIGH-4)"
---

<objective>
The CEO sanitizes the source .odt per ODT-SANITIZATION-PROCEDURE.md and exports a credentials-free markdown copy; the builder verifies it at markdown and container level, records evidence, and commits (SEC-04). This runs AFTER rotation (Wave 2) — live credentials outrank document hygiene, and the single human does one thing at a time.

Purpose: the source document is the original leak vector (its content was pasted into third-party AI tools). The credentials section must be stripped, the sanitized copy must live in project docs, and the original must stay out of git and cloud sync — a CEO-locked hard-gate criterion.
Output: tracked sanitized doc + verification evidence.
</objective>

## Artifacts this phase produces

Full Phase 1 artifact set (bold = owned by this plan):

- `.gitleaks.toml`, `scripts/hooks/pre-commit`, `.github/workflows/secret-scan.yml`, `evidence/hook-selftest.md`, `evidence/gitleaks-history-report.json` (plan 01-01)
- `.gitignore` vault rules, `.env.example`, `.claude/settings.json` deny rules (plan 01-02)
- `CREDENTIAL-ROTATION-CHECKLIST.md`, `evidence/ROTATION-EVIDENCE.md` template, `ODT-SANITIZATION-PROCEDURE.md` (plan 01-03)
- Filled rotation evidence (plan 01-04)
- **`docs/source-architecture-notes-sanitized.md`** (CEO-produced, builder-verified, this plan)
- **`evidence/ODT-SANITIZATION-EVIDENCE.md`** (this plan)
- `evidence/trufflehog-verified-sweep.md`, `evidence/PHASE-1-GATE-REPORT.md` (plan 01-06)

All `evidence/` paths are under `.planning/phases/01-security-baseline-credential-remediation/evidence/`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md
@.planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: CEO sanitizes the source document and runs the copy sweep</name>
  <files>docs/source-architecture-notes-sanitized.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md (the CEO's script for this task — bilingual)
  </read_first>
  <action>
    CEO-only work — the agent presents this checkpoint and waits. Following the procedure: (1) copy the original to /tmp/sanitize-work.odt (never edit the original in place); (2) in LibreOffice: delete the entire credentials section; Edit > Track Changes > Manage — accept all, recording off; File > Versions — delete stored versions; check headers/footers/comments; (3) File > Save As to a NEW file (full zip rewrite — no deleted-text remnants); (4) export the sanitized content as markdown to docs/source-architecture-notes-sanitized.md (create the docs/ directory if needed) — markdown, NOT .odt; (5) run the guided copy sweep: Downloads, cloud-sync folders, ~/.local/share/Trash, email attachments, external backups — note each location checked and the action taken (deleted / moved to the single non-synced location where the original will live); keep the sweep notes for the builder to record in evidence; (6) tell the builder the path of the sanitized working copy (default /tmp/sanitize-work.odt) so container-level verification can run.
  </action>
  <how-to-verify>
    1. docs/source-architecture-notes-sanitized.md exists and contains the architecture notes WITHOUT any credentials section
    2. The sanitized working .odt copy still exists at the path you report (needed for Task 2's container check)
    3. Your sweep notes list every location checked
  </how-to-verify>
  <acceptance_criteria>
    - sanitized markdown exists in docs/ with the credentials section absent (builder gates in Task 2)
    - sanitized working .odt path reported; sweep-locations list provided in the checkpoint response
  </acceptance_criteria>
  <resume-signal>Type "sanitized copy exported, sweep done" plus the sanitized .odt working path and your sweep-locations list — or describe any blocker.</resume-signal>
  <done>Credentials-free markdown exists in docs/; container copy available for verification; copy sweep performed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify sanitized artifacts at both levels, stage evidence — commit deferred to post-review</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/ODT-SANITIZATION-EVIDENCE.md, docs/source-architecture-notes-sanitized.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md — "Document Sanitization (SEC-04)" verification commands (source of truth)
    - docs/source-architecture-notes-sanitized.md (the file being verified — but see the hard rule below before touching anything else)
  </read_first>
  <action>
    HARD RULE first: never open, unzip, print, cat, or grep-with-content-output the ORIGINAL .odt at repo root — it contains the leaked credentials; only filename-level git checks are permitted against it. All content-level checks run against the SANITIZED artifacts only, and every grep against them uses count-only or filename-only flags where the target is the ODT container.

    Gates: (1) sanitized markdown exists in docs/; (2) key-shape gate on the markdown: `grep -cE 'sk-[A-Za-z0-9]{20,}|sk-or-v1-[A-Za-z0-9]|nvapi-[A-Za-z0-9_-]{20,}|apify_api_[A-Za-z0-9]{20,}' docs/source-architecture-notes-sanitized.md` must output 0; (3) `gitleaks dir docs --redact` exits 0; (4) container-level check on the SANITIZED working copy at the path the CEO reported (default /tmp/sanitize-work.odt): unzip it into /tmp/odt-check, then run `gitleaks dir /tmp/odt-check --redact` (must exit 0) and the same key-shape grep in count mode over the extracted files (must output 0); clean up /tmp/odt-check afterwards; (5) original exclusion: `git check-ignore -q` on the original .odt filename exits 0; `git log --all --name-only --format= | grep -ci '\.odt$'` outputs 0 (never committed); `git ls-files | grep -ci '\.odt$'` outputs 0 (not tracked now); (6) word-level review aid — NOT a zero-gate, because architecture prose legitimately says token/API key/secret: collect line-numbered matches of credential-adjacent words (password, api key, token, secret) from the sanitized markdown and write the FULL matched lines only to `/tmp/odt-wordmatch-review.md` — a scratch file outside the repo, never committed, deleted after Task 3 (cross-AI review HIGH-4: committing match content before human review could preserve a residual fragment in permanent history).

    Write `evidence/ODT-SANITIZATION-EVIDENCE.md`: UTC timestamp, each gate's command and observed result (status/counts only), the CEO's sweep-locations list from the Task 1 checkpoint, and a word-match summary section whose entries each occupy one line of the exact form `- line 42 — token` (a dash, the word "line", the line number, an em dash, then one category out of password / api key / token / secret) — line numbers and categories ONLY, never the matched line text; write the entries only, do not restate this format specification inside the file. Do NOT commit anything in this task: both the sanitized markdown and the evidence file are committed in Task 3, only after the CEO approves the word-match review.
  </action>
  <verify>
    <automated>test -f docs/source-architecture-notes-sanitized.md && test "$(grep -cE 'sk-[A-Za-z0-9]{20,}|sk-or-v1-[A-Za-z0-9]|nvapi-[A-Za-z0-9_-]{20,}|apify_api_[A-Za-z0-9]{20,}' docs/source-architecture-notes-sanitized.md)" = "0" && gitleaks dir docs --redact && test "$(git log --all --name-only --format= | grep -ci '\.odt$')" = "0" && test -f /tmp/odt-wordmatch-review.md && test "$(git ls-files | grep -c 'docs/source-architecture-notes-sanitized.md')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/source-architecture-notes-sanitized.md` exists and is NOT yet tracked: `git ls-files | grep -c 'docs/source-architecture-notes-sanitized.md'` outputs 0 (tracking happens in Task 3 after CEO approval — review HIGH-4; the Pitfall 6 tracked-markdown gate moves there)
    - key-shape grep on the markdown outputs 0; `gitleaks dir docs --redact` exits 0
    - container check ran on the sanitized working copy: gitleaks over the extracted container exits 0, key-shape count 0 — both recorded in evidence
    - `git log --all --name-only --format= | grep -ci '\.odt$'` outputs 0 and `git ls-files | grep -ci '\.odt$'` outputs 0
    - `evidence/ODT-SANITIZATION-EVIDENCE.md` exists with UTC timestamp, all gate results, sweep-locations list, and the word-match summary — format-gated: `grep -E '^- line ' evidence/ODT-SANITIZATION-EVIDENCE.md | grep -vcE '^- line [0-9]+ — (password|api key|token|secret)$'` outputs 0 (no matched line text in the to-be-committed evidence)
    - `/tmp/odt-wordmatch-review.md` exists for the CEO's Task 3 review, and nothing from this task is committed yet
  </acceptance_criteria>
  <done>Sanitized doc proven clean at two levels; original proven absent from all history; evidence staged with a line-numbers+categories-only word-match summary; full match text waits in /tmp for the CEO — nothing committed yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CEO reviews residual word matches and confirms the original is contained</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/ODT-SANITIZATION-EVIDENCE.md</files>
  <read_first>
    - /tmp/odt-wordmatch-review.md — full word-match line content (scratch, never committed — review HIGH-4)
    - .planning/phases/01-security-baseline-credential-remediation/evidence/ODT-SANITIZATION-EVIDENCE.md — gate results + word-match summary (line numbers + categories only)
  </read_first>
  <action>
    Present to the CEO: (1) the full word-match content from /tmp/odt-wordmatch-review.md (review HIGH-4: full line text lives only in this local scratch file, never in git) — every line in the sanitized markdown mentioning password/api key/token/secret; the CEO confirms each is architectural prose (e.g., token discipline, API key management design), not a residual value or a value-adjacent fragment (a username, a key name paired with a hint); (2) confirmation that the original .odt now exists in exactly one non-synced location and every other copy found in the sweep was deleted.

    After approval, the builder finishes: append the CEO approval line with UTC timestamp to evidence/ODT-SANITIZATION-EVIDENCE.md, delete /tmp/odt-wordmatch-review.md, and commit the sanitized markdown and the evidence file together (the plan 01-01 pre-commit hook scans both on the way in). If the CEO flags any match, delete the scratch file, commit nothing, and return to Task 1 for re-sanitization.
  </action>
  <how-to-verify>
    1. Read /tmp/odt-wordmatch-review.md — each matched line is harmless prose
    2. Confirm: original in one non-synced location; sweep copies deleted (Downloads, cloud, trash, mail)
  </how-to-verify>
  <acceptance_criteria>
    - CEO approval line with UTC timestamp in evidence/ODT-SANITIZATION-EVIDENCE.md; if the CEO flags any match, the flow returns to Task 1 for re-sanitization before this plan can complete
    - post-approval commit done: `git ls-files | grep -c 'docs/source-architecture-notes-sanitized.md'` outputs 1 (Pitfall 6 defeated) and `git log --oneline -- '.planning/phases/01-security-baseline-credential-remediation/evidence/ODT-SANITIZATION-EVIDENCE.md' | wc -l` outputs at least 1
    - scratch removed: `test ! -f /tmp/odt-wordmatch-review.md` exits 0
  </acceptance_criteria>
  <resume-signal>Type "approved" if every match is harmless and the original is contained — or list the line numbers that need another sanitization pass.</resume-signal>
  <done>Human review confirms the machine gates BEFORE anything is committed: sanitized copy genuinely credentials-free, original contained, artifacts committed post-approval, /tmp scratch deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| original .odt → executor context | the original holds the leaked credentials; content must never cross into agent context, logs, or evidence |
| sanitized copy → git history | once committed it is permanent and clonable; must be proven clean at markdown and container level first |
| local disk → cloud sync/trash | copies of the original outside the repo are an invisible re-leak vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Information Disclosure | residual secret in exported markdown or ODT container internals (tracked changes, versions, headers) | critical | mitigate | LibreOffice purge steps + Save-As zip rewrite + two-level gates (gitleaks + key-shape on markdown AND extracted container) + CEO word-match review from /tmp scratch BEFORE any commit; committed evidence carries line numbers + categories only (review HIGH-4) |
| T-01-20 | Information Disclosure | executor echoes original ODT content into context or logs | critical | mitigate | hard rule in Task 2: filename-level checks only on the original; count-only flags on container greps; --redact everywhere |
| T-01-21 | Information Disclosure | synced/trash/backup copies of the original persist after sanitization | high | mitigate | guided sweep in Task 1 + CEO containment confirmation in Task 3; locations recorded in evidence |
| T-01-22 | Repudiation | sanitized copy silently not in git because it was exported as .odt (*.odt ignore rule) | medium | mitigate | markdown export mandated by procedure; tracked-file gate in Task 2 acceptance |
</threat_model>

<verification>
- Sanitized markdown tracked in docs/, clean by gitleaks + key-shape gates at both levels
- Original .odt: ignored, never committed, contained to one non-synced location
- Evidence file records every gate result and the sweep list; word-match summary is line-numbers+categories only; CEO approval on residual matches precedes every commit
</verification>

<success_criteria>
SEC-04 satisfied: credentials section stripped, sanitized copy lives in project docs as tracked markdown, original stays out of git and cloud sync — all machine-verified and CEO-confirmed.
</success_criteria>

<output>
Create `.planning/phases/01-security-baseline-credential-remediation/01-05-SUMMARY.md` when done.
</output>
