---
phase: 01-security-baseline-credential-remediation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md
  - .planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md
  - .planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md
autonomous: true
requirements: [SEC-01, SEC-04]
must_haves:
  truths:
    - "Every credential category in the CONTEXT inventory has exactly one pre-assigned evidence row (21 rows), and the ID set is machine-proven to be exactly CRED-01..CRED-21 with no duplicates (cycle 3 MED-6) — ticking without probing is structurally impossible"
    - "The CEO can execute every rotation mechanically from the checklist: rotate path, 2FA enrollment, sign-out, probe command, evidence pointer — with Turkish annotations at every ambiguous or trapdoor step"
    - "All three research trapdoors are encoded as warnings the CEO cannot miss: Apify grace declined, Google old app password captured BEFORE the account password change, 9Router treated as a local config sweep"
    - "The ODT sanitization procedure produces a markdown copy in docs/ (never an .odt, which the ignore rule would silently drop), includes container-level verification, and keeps the original-copy and sanitized-copy scratch paths distinct (/tmp/odt-original-work.odt vs /tmp/odt-sanitized-check.odt — cross-AI review HIGH-6)"
  artifacts:
    - ".planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md (21 CRED rows + 5 machine-parseable ATT attestation rows, no-values header rules)"
    - ".planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md (bilingual, risk-first, 12 sections)"
    - ".planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md"
  key_links:
    - "checklist item -> evidence row ID (every checklist section names its CRED-NN rows so nothing is probed without being recorded)"
    - "probe commands copied verbatim from 01-RESEARCH.md Per-Service Recipes (the source of truth for endpoints — a wrong endpoint gives a false PASS, RESEARCH Pitfall 2); machine-checked both directions in Task 2 acceptance: endpoint presence loop + full probe-command/EXPECT verbatim loop scoped to probe-tagged fenced blocks, so Section 11 sweep commands cannot false-block (cross-AI review MED-1; scope per cycle 3 MED-7)"
---

<objective>
Produce the three CEO-facing deliverables: the bilingual credential-rotation checklist (SEC-01), the no-secrets evidence template that makes unverified ticking structurally impossible (SEC-01), and the ODT sanitization procedure (SEC-04 preparation).

Purpose: CEO-locked division of labor — the CEO personally performs all rotations; the builder prepares the checklist, verification procedures, and evidence structure. These artifacts are the entire interface between builder and CEO for Waves 2-3.
Output: three markdown deliverables in the phase directory, containing credential NAMES and CATEGORIES only — never values.
</objective>

## Artifacts this phase produces

Full Phase 1 artifact set (bold = owned by this plan):

- `.gitleaks.toml`, `scripts/hooks/pre-commit`, `.github/workflows/secret-scan.yml`, `evidence/hook-selftest.md`, `evidence/gitleaks-history-report.json` (plan 01-01)
- `.gitignore` vault rules, `.env.example`, `.claude/settings.json` deny rules (plan 01-02)
- **`CREDENTIAL-ROTATION-CHECKLIST.md`** (this plan; every probe fenced block opens with the `probe` fence-info tag — the marker the MED-7-scoped verbatim gate keys on, cycle 3 MED-7)
- **`evidence/ROTATION-EVIDENCE.md` template** (this plan; filled by CEO in plan 01-04)
- **`ODT-SANITIZATION-PROCEDURE.md`** (this plan; executed by CEO in plan 01-05)
- Filled rotation evidence (plan 01-04)
- `docs/source-architecture-notes-sanitized.md`, `evidence/ODT-SANITIZATION-EVIDENCE.md` (plan 01-05)
- `evidence/trufflehog-verified-sweep.md`, `evidence/PHASE-1-GATE-REPORT.md` (plan 01-06)

All `evidence/` paths are under `.planning/phases/01-security-baseline-credential-remediation/evidence/`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md
@.planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rotation evidence template (21 pre-assigned rows, no-values schema)</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md — credential inventory (the authoritative category list)
    - .planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md — "Evidence-Record Schema (proposal)" section (source of truth for columns and rules)
  </read_first>
  <action>
    Create the evidence directory (`mkdir -p`) and write `ROTATION-EVIDENCE.md` implementing the RESEARCH schema. Header rules block, stated plainly at the top: this file records status codes, error phrases, and UTC timestamps ONLY — no secret values, old or new, ever; the Observed column may hold only a status/error code or the literal phrase "login rejected"; a row's DEAD? column may be marked complete only when the probe timestamp and Observed cells are filled; rows that genuinely do not apply put the literal `N/A (reason)` — e.g. `N/A (local software)` — in the DEAD? column itself, with any fuller explanation in the Action column; plan 01-04's completeness gate parses the DEAD? column specifically (cross-AI review HIGH-2), so an N/A anywhere else does not resolve a row.

    Table columns per RESEARCH: ID | Category | Service / account | Action | Rotated (UTC) | 2FA | Dead-value probe | Probe (UTC) | Observed | DEAD?. Pre-fill exactly 21 rows with IDs (the ID column must be exactly CRED-01..CRED-21, each ID appearing exactly once — plans 01-04 and 01-06 re-prove this exact set with a sorted-ID-set gate, because the 21-row count alone cannot detect a duplicated row masking a missing one, cycle 3 MED-6), categories, service names, and probe-method names — leaving Rotated/Probe/Observed/DEAD? cells empty for the CEO: CRED-01 Google account + app password (probe: SMTP AUTH smtps://smtp.gmail.com:465 + incognito login), CRED-02 Hotmail account 1, CRED-03 Hotmail account 2 (probe: login.live.com incognito login), CRED-04 Namecheap dashboard (login probe), CRED-05 Private Email support@ mailbox, CRED-06 Private Email sales@ mailbox (probe: IMAP mail.privateemail.com:993 or webmail login), CRED-07 Cloudflare account/agent token (probe: accounts tokens/verify), CRED-08 Cloudflare legacy user token (probe: user/tokens/verify), CRED-09 Cloudflare Global API Key — probe only if it appeared in the leaked doc, else `N/A (reason)` in the DEAD? column, CRED-10 hostloom client-area password (login probe), CRED-11 hostloom cPanel password (:2083 login probe), CRED-12 WordPress admin password (wp-login.php probe), CRED-13 WordPress application passwords (wp-json users/me probe, if old value available), CRED-14 OpenAI API key (GET /v1/models), CRED-15 OpenRouter API key (GET /api/v1/key), CRED-16 NVIDIA Build API key (chat/completions probe), CRED-17 Apify token (GET /v2/users/me — the row's Action cell is pre-filled with exactly `grace=__PENDING__`; the CEO replaces that placeholder with exactly `grace=declined` or `grace=accepted; probe_after_24h=yes`; plan 01-04's gate parses the Action column of the CRED-17 row specifically — column-aware like the DEAD? gate, never a whole-row grep — and blocks while `grace=__PENDING__` remains; all instructional prose about the grace field lives ONLY in the header rules block, never inside the row, so an untouched template cannot satisfy the gate — cross-AI review MED-2), CRED-18 Ollama cloud key (api/generate probe), CRED-19 OpenCode Zen key (zen chat/completions probe), CRED-20 9Router local config sweep (local software: the DEAD? cell gets `N/A (local software)`, evidence = swept + providers re-authed), CRED-21 Linux sudo password (sudo -K old-password rejection).

    After the table, a second machine-parseable attestation table (cross-AI review HIGH-3 — free-form checkbox lines cannot be gated), columns ID | Attestation | Status | Notes (paths only), pre-filled with exactly 5 rows and empty Status/Notes cells: ATT-01 all new values unique, generated by password manager (the password-reuse hazard is the root cause per CONTEXT — CEO fills Status with exactly YES); ATT-02 browser/mail-client saved logins updated; ATT-03 tool configs re-authed and prefix-swept (~/.config, ~/.local/share, ~/.claude*); ATT-04 shell rc/history scrubbed; ATT-05 stray .env files found and resolved. ATT-02..ATT-05 come from the RESEARCH Runtime State Inventory; the CEO fills their Status cells with exactly DONE and the Notes cells with file paths only — list only, no values. Plan 01-04's gate parses the Status column of every ATT row and blocks on any blank.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| CRED-[0-9]+' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md')" = "21" && test "$(grep -cE '^\| ATT-0[1-5] ' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md')" = "5"</automated>
  </verify>
  <acceptance_criteria>
    - file exists and `grep -cE '^\| CRED-[0-9]+' evidence/ROTATION-EVIDENCE.md` outputs exactly 21
    - unique ID set (cycle 3 MED-6 — the row count alone cannot prove uniqueness): `test "$(awk -F'|' '$2 ~ /CRED-/ { id=$2; gsub(/[ \t]/,"",id); print id }' evidence/ROTATION-EVIDENCE.md | sort)" = "$(awk 'BEGIN{for(i=1;i<=21;i++) printf "CRED-%02d\n", i}')"` exits 0 — the ID column contains exactly the set CRED-01..CRED-21, no duplicates, no extras
    - header rules state the no-values rule and the probe-before-tick rule (grep -ci 'no secret values' outputs at least 1)
    - uniqueness attestation line present: `grep -ci 'unique' evidence/ROTATION-EVIDENCE.md` outputs at least 1
    - attestation table pre-filled (review HIGH-3): `grep -cE '^\| ATT-0[1-5] ' evidence/ROTATION-EVIDENCE.md` outputs exactly 5
    - CRED-17 grace placeholder pre-filled in the Action column (review MED-2): `awk -F'|' '$2 ~ /CRED-17/ { print $5 }' evidence/ROTATION-EVIDENCE.md | grep -c 'grace=__PENDING__'` outputs 1
    - no key-shaped strings: `grep -cE 'sk-[A-Za-z0-9]{20,}|sk-or-v1-[A-Za-z0-9]|nvapi-[A-Za-z0-9_-]{20,}|apify_api_[A-Za-z0-9]{20,}' evidence/ROTATION-EVIDENCE.md` outputs 0
  </acceptance_criteria>
  <done>Evidence template committed: 21 pre-assigned CRED rows + 5 machine-parseable ATT attestation rows; a row cannot read as complete without probe data, and attestations cannot silently stay blank.</done>
</task>

<task type="auto">
  <name>Task 2: Bilingual CEO rotation checklist (risk-first, trapdoors encoded)</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md — "Per-Service Recipes" 1-14, "2FA Enrollment (TOTP-first)" table, "Checklist ordering", "Common Pitfalls" 1-8, "Runtime State Inventory" (sole source of truth for every rotation path and probe endpoint — do not invent or substitute endpoints)
    - .planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md — division-of-labor and no-values locked decisions
  </read_first>
  <action>
    Write `CREDENTIAL-ROTATION-CHECKLIST.md` — the document the CEO follows mechanically. Language rule (CEO reads Turkish): every step in simple English; add a Turkish annotation line starting with `TR:` directly under any step that is a trapdoor, an ordering constraint, an irreversible action, or otherwise ambiguous — minimum 12 TR: lines across the document, mandatory on every trapdoor.

    Structure, with numbered `##` section headings 0 through 11:

    Section 0 — Before you start (preamble): use a password manager for every new value, one unique generated password/key per service, never reuse (the reuse hazard is the root cause being fixed); pick one TOTP app for all 2FA enrollments and store every backup-code set in the password manager; universal probe hygiene copied from the RESEARCH preamble — verify `echo $HISTCONTROL` shows ignoreboth, capture each old value with a LEADING-SPACE export in your own terminal, run the probe, record only the status code and UTC time into the evidence row, then unset the variable and close the terminal; the old value must never be typed into a file, a chat, or an agent conversation; the acceptance rule — any documented auth rejection counts (401, 403, SMTP 535, "login rejected") — record what you actually observe.

    Sections 1-10 in the RESEARCH recovery-vector risk-first order: 1 Google (recovery hub first), 2 Microsoft Hotmail x2, 3 Namecheap dashboard, 4 Namecheap Private Email x2, 5 Cloudflare (agent token, legacy user token, Global API Key if leaked), 6 Hosting hostloom (client area + cPanel; discover and note panel URLs — no credentials — and if the host offers no 2FA, write that in the evidence row as a risk-acceptance line), 7 WordPress outleteuro (password + Log Out Everywhere Else + revoke application passwords + rotate salts + audit user list), 8 API keys — OpenAI, OpenRouter, NVIDIA Build, Apify, Ollama cloud, OpenCode Zen, 9 9Router local sweep, 10 Linux sudo password.

    Each service section contains, in order: numbered rotate steps (dashboard paths from the RESEARCH recipe), 2FA/TOTP enrollment step with backup-codes reminder (per the RESEARCH 2FA table), sign-out-everywhere / session-kill step, the probe command copied VERBATIM from the corresponding RESEARCH recipe as a fenced code block with its EXPECT line, the target evidence row IDs (CRED-NN), and an "Also check" line where the recipe has one (unknown keys, forwarding rules, rogue admins). Every probe fenced block MUST open with the `probe` fence-info tag: the opening fence line is the three-backtick marker immediately followed by the word probe (no space), and the block closes with a bare three-backtick line (cycle 3 MED-7 — the tag marks the block as a byte-identical RESEARCH copy for the verbatim gate; every OTHER fenced block in this document, such as the Section 11 sweep commands or any illustrative snippet, uses a plain or differently-tagged fence and is exempt from that gate). Copy each probe block byte-identically — including continuation lines (`-H` headers, `-d` bodies, backslashes) and the `# EXPECT:` comment line. The acceptance criteria below machine-check this two ways (cross-AI review MED-1): (a) an endpoint presence loop — every RESEARCH endpoint must appear in this checklist character-for-character; (b) a full-verbatim loop in the reverse direction, scoped to probe-tagged fenced blocks only (cycle 3 MED-7 — Section 11's runtime sweep commands legitimately do not appear in RESEARCH's probe recipes and must not false-block the gate): every non-empty line inside a probe-tagged fenced block must exist verbatim somewhere in 01-RESEARCH.md, so an invented or edited probe command cannot survive. If any probe cannot be copied verbatim, stop and flag rather than substituting an endpoint.

    Trapdoor callouts rendered as unmissable warning blocks, each with a TR: annotation: (a) Apify — when rotating, DECLINE the 24-hour grace window (choose immediate invalidation); if grace was accepted, the old token stays alive for 24h and the probe must wait until T+24h — a 200 response during grace is expected, do not panic-tick; either way REPLACE the `grace=__PENDING__` placeholder in the CRED-17 row's Action cell with exactly `grace=declined`, or `grace=accepted; probe_after_24h=yes` once the delayed probe has run (plan 01-04's gate parses that cell by column and blocks while the placeholder remains — cross-AI review MED-2); (b) Google — copy the OLD app password into your probe terminal BEFORE changing the account password, because the password change automatically revokes all app passwords (the probe becomes impossible to prepare afterwards); (c) 9Router — it is local software on this machine holding OTHER providers' keys; there is nothing to revoke in a cloud dashboard — rotate the upstream providers, then re-enter credentials in the 9Router dashboard and sweep its config directory for stale key strings; (d) OpenRouter — never probe the public models endpoint (it returns 200 without auth); use the key-info endpoint from the recipe.

    Section 11 — Runtime-state sweep (after all rotations): from RESEARCH Runtime State Inventory — update browser/mail-client saved logins; re-auth 9Router, opencode CLI, Claude/MCP tool configs; prefix-grep sweep commands over ~/.config, ~/.local/share, ~/.claude* (the greps list FILE PATHS only); scrub shell rc files and truncate history if an old value was ever typed without a leading space; find stray .env files under home. Each sweep item points at its attestation row ID (ATT-02..ATT-05) in ROTATION-EVIDENCE.md and tells the CEO to write exactly DONE in that row's Status cell (Notes: file paths only). The sweep-command fenced blocks in this section are plain-fenced, never probe-tagged — they are runtime sweep commands, not RESEARCH probe recipes, and the MED-7-scoped verbatim gate skips them by design (cycle 3 MED-7).
  </action>
  <verify>
    <automated>test "$(grep -cE '^## [0-9]+\.' '.planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md')" = "12" && test "$(grep -c '^ *TR:' '.planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md')" -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - 12 numbered section headings present: `grep -cE '^## [0-9]+\.' CREDENTIAL-ROTATION-CHECKLIST.md` outputs 12
    - Turkish annotations: `grep -c '^ *TR:' CREDENTIAL-ROTATION-CHECKLIST.md` outputs at least 12
    - Apify grace trapdoor present: `grep -ci 'grace' CREDENTIAL-ROTATION-CHECKLIST.md` outputs at least 2
    - Google ordering trapdoor present: section 1 contains the capture-before-change warning (`grep -ci 'before changing' CREDENTIAL-ROTATION-CHECKLIST.md` outputs at least 1)
    - probe commands present: `grep -c 'curl -sS' CREDENTIAL-ROTATION-CHECKLIST.md` outputs at least 8
    - every RESEARCH probe endpoint present verbatim (cross-AI review MED-1): `for u in 'https://api.openai.com/v1/models' 'https://openrouter.ai/api/v1/key' 'https://integrate.api.nvidia.com/v1/chat/completions' 'https://api.apify.com/v2/users/me' 'https://api.cloudflare.com/client/v4/accounts/${ACCOUNT_ID}/tokens/verify' 'https://api.cloudflare.com/client/v4/user/tokens/verify' 'X-Auth-Key' 'https://ollama.com/api/generate' 'https://opencode.ai/zen/v1/chat/completions' 'smtps://smtp.gmail.com:465' 'imaps://mail.privateemail.com:993' 'login.live.com' 'wp-login.php' 'wp-json/wp/v2/users/me' ':2083' 'grace=declined'; do grep -qF "$u" CREDENTIAL-ROTATION-CHECKLIST.md || echo "MISSING: $u"; done` prints nothing
    - full probe-command/EXPECT verbatim gate, scoped to probe-tagged fenced blocks (cross-AI review MED-1; scope per cycle 3 MED-7): `FENCE=$(printf '\140\140\140'); awk -v fs="$FENCE" 'index($0,fs)==1 { if (inblk) { inblk=0; probe=0 } else { inblk=1; probe=($0 == fs "probe") } next } inblk && probe && NF' CREDENTIAL-ROTATION-CHECKLIST.md | while IFS= read -r line; do grep -qF -- "$line" 01-RESEARCH.md || echo "NOT-VERBATIM: $line"; done` prints nothing — every non-empty line inside a probe-tagged fenced block exists character-for-character in 01-RESEARCH.md, while plain-fenced blocks (Section 11 sweep commands, illustrative snippets) are exempt (FENCE is the three-backtick fence marker, built via printf so this criterion needs no literal backticks; a probe block's opening fence is the marker immediately followed by probe, and the awk state machine only emits lines from blocks opened that way)
    - probe blocks actually tagged — guards the MED-7 scope against a vacuous pass where no block carries the tag and the verbatim loop scans nothing (cycle 3 MED-7): `FENCE=$(printf '\140\140\140'); grep -c "^${FENCE}probe$" CREDENTIAL-ROTATION-CHECKLIST.md` outputs at least 8
    - evidence linkage: `grep -cE 'CRED-[0-9]+' CREDENTIAL-ROTATION-CHECKLIST.md` outputs at least 15
    - no key-shaped strings: `grep -cE 'sk-[A-Za-z0-9]{20,}|sk-or-v1-[A-Za-z0-9]|nvapi-[A-Za-z0-9_-]{20,}|apify_api_[A-Za-z0-9]{20,}' CREDENTIAL-ROTATION-CHECKLIST.md` outputs 0
  </acceptance_criteria>
  <done>The CEO can execute the entire rotation mechanically: every service has rotate/2FA/sign-out/probe/record steps, every trapdoor carries an unmissable bilingual warning.</done>
</task>

<task type="auto">
  <name>Task 3: ODT sanitization procedure document</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md — "Document Sanitization (SEC-04)" section and Pitfall 6 (source of truth for the container-level procedure)
    - .planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md — Specific Ideas (odt location, gitignore status, assumed third-party exposure)
  </read_first>
  <action>
    Write `ODT-SANITIZATION-PROCEDURE.md` for the CEO, following the RESEARCH SEC-04 procedure step by step BUT with the two-path naming below superseding RESEARCH's single `/tmp/sanitize-work.odt` path (cross-AI review HIGH-6: one path playing both roles can leave the credential-bearing copy verified or left behind), with TR: annotation lines under every LibreOffice UI step and every irreversible step: (1) copy the original to `/tmp/odt-original-work.odt` — this path holds the UNSANITIZED credential-bearing copy; never edit the original in place; (2) open the copy in LibreOffice: delete the entire credentials section; Edit > Track Changes > Manage — accept all and ensure recording is off (deleted text can survive inside tracked changes); File > Versions — delete stored versions; check headers, footers, and comments for stray values; (3) File > Save As to the distinct NEW path `/tmp/odt-sanitized-check.odt` so LibreOffice rewrites the whole zip container and no deleted-text remnants survive — the sanitized copy NEVER reuses the original-copy path, and only `/tmp/odt-sanitized-check.odt` may ever be reported to the builder as the sanitized working copy; (4) export the sanitized content as Markdown to `docs/source-architecture-notes-sanitized.md` — explicitly NOT as .odt, because the *.odt ignore rule would silently keep an .odt copy out of git and SEC-04 would fail invisibly (RESEARCH Pitfall 6); note that LibreOffice may need Save-As-txt or copy-paste-to-editor as the practical markdown route — content fidelity matters, formatting does not; (5) verification commands the builder will run in plan 01-05 (listed here so the CEO knows what happens next): unzip of `/tmp/odt-sanitized-check.odt` into /tmp/odt-check, key-shape greps in count-only mode, and a gitleaks directory scan; both /tmp ODT copies are deleted by the builder after the plan 01-05 CEO approval; (6) guided cloud-sync and backup sweep with a concrete checklist of locations: Downloads, any cloud-sync folders (Drive/Dropbox/OneDrive), ~/.local/share/Trash, email attachments (sent and received), external backups — for each: found/not found and action taken (deleted, or moved to the single non-synced location where the original will live); note that copies already pasted into third-party AI tools are unrecoverable, which is exactly why every listed credential is treated as compromised and rotated.

    End with the handoff line: when steps 1-4 and 6 are done, resume plan 01-05 — the builder then runs verification and commits the sanitized copy plus evidence.
  </action>
  <verify>
    <automated>test -f '.planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md' && grep -q 'docs/source-architecture-notes-sanitized.md' '.planning/phases/01-security-baseline-credential-remediation/ODT-SANITIZATION-PROCEDURE.md'</automated>
  </verify>
  <acceptance_criteria>
    - file exists; markdown export destination named: `grep -c 'docs/source-architecture-notes-sanitized.md' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1
    - two distinct scratch paths, ambiguous path retired (review HIGH-6): `grep -c '/tmp/odt-original-work.odt' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1, `grep -c '/tmp/odt-sanitized-check.odt' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1, and `grep -c '/tmp/sanitize-work.odt' ODT-SANITIZATION-PROCEDURE.md` outputs 0
    - container hygiene steps present: `grep -ci 'track changes' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1 and `grep -ci 'versions' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1
    - verification step present: `grep -c 'unzip' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1
    - cloud-sweep checklist present: `grep -ci 'trash' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1 and `grep -ci 'cloud' ODT-SANITIZATION-PROCEDURE.md` outputs at least 1
    - Turkish annotations: `grep -c '^ *TR:' ODT-SANITIZATION-PROCEDURE.md` outputs at least 4
    - no key-shaped strings: `grep -cE 'sk-[A-Za-z0-9]{20,}|sk-or-v1-[A-Za-z0-9]|nvapi-[A-Za-z0-9_-]{20,}|apify_api_[A-Za-z0-9]{20,}' ODT-SANITIZATION-PROCEDURE.md` outputs 0
  </acceptance_criteria>
  <done>The CEO has a complete, bilingual, container-aware sanitization procedure ending in a markdown export and a guided copy sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CEO's terminal (old values) → repo files | probes handle live-until-rotated secrets; only status codes and timestamps may cross into evidence |
| checklist text → CEO behavior | a wrong endpoint or missed trapdoor converts a live credential into a false PASS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Information Disclosure | checklist/evidence/procedure text embedding a secret value | critical | mitigate | by-name/category references only (CEO-locked rule); key-shape grep gates on all three files; gitleaks hook from plan 01-01 scans them at commit |
| T-01-10 | Repudiation | checklist item ticked without a real probe | high | mitigate | evidence schema requires filled probe timestamp + Observed cell before a row reads complete; plan 01-04 runs an automated completeness gate |
| T-01-11 | Spoofing | probe against an unauthenticated endpoint yields false PASS | high | mitigate | probe endpoints copied verbatim from RESEARCH recipes; OpenRouter public-endpoint ban encoded as a trapdoor callout (Pitfall 2); verbatim gate scoped to probe-tagged fenced blocks with a ≥8 tag-count floor so sweep commands cannot false-block and untagged docs cannot vacuously pass (cycle 3 MED-7) |
| T-01-12 | Information Disclosure | old value leaks during probing (shell history, files, chat) | high | mitigate | preamble hygiene: HISTCONTROL check, leading-space export, unset + close terminal, status-code-only recording (Pitfall 4) |
| T-01-13 | Spoofing | Apify 24h grace window makes a live token look "pending-dead" | high | mitigate | unmissable decline-grace trapdoor block with TR: annotation; CRED-17 Action cell pre-filled grace=__PENDING__, replaced by the CEO with grace=declined / grace=accepted; probe_after_24h=yes — parsed by column in plan 01-04, placeholder blocks (MED-2); the gate requires exactly one CRED-17 row and the exact CRED-01..21 ID set, so a duplicate row cannot mask a pending grace decision (cycle 3 MED-6) |
</threat_model>

<verification>
- Evidence template has exactly 21 CRED rows whose ID set is proven exactly CRED-01..CRED-21 by the sorted-set compare (cycle 3 MED-6) + 5 machine-parseable ATT attestation rows; no key-shaped strings in any deliverable
- Checklist has 12 sections, 12+ TR: annotations, verbatim probe commands (endpoint presence loop AND probe-scoped full-verbatim loop both pass, with at least 8 probe-tagged fenced blocks — MED-1, cycle 3 MED-7), and all four trapdoor callouts
- ODT procedure names the markdown export path, includes container hygiene + cloud sweep, and uses the two distinct scratch paths with /tmp/sanitize-work.odt fully retired (HIGH-6)
- Cycle-3 gate simulation: (MED-6) a template with a duplicate CRED-17 and a missing CRED-18 still counts 21 rows, but its sorted ID list carries a duplicate and a gap, so the set-compare against the generated CRED-01..CRED-21 sequence fails; (MED-7) a plain-fenced sweep block containing a sudo command is never emitted by the awk state machine — no false NOT-VERBATIM — while an edited line inside a probe-tagged block prints NOT-VERBATIM and fails; a checklist with zero probe-tagged blocks fails the ≥8 tag-count floor instead of passing vacuously
</verification>

<success_criteria>
SEC-01 preparation complete (checklist + evidence structure) and SEC-04 preparation complete (procedure): the CEO can start Wave 2 with zero ambiguity, and no deliverable contains anything a secret scanner would ever flag.
</success_criteria>

<output>
Create `.planning/phases/01-security-baseline-credential-remediation/01-03-SUMMARY.md` when done.
</output>