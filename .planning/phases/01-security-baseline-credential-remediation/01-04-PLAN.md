---
phase: 01-security-baseline-credential-remediation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - .planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md
autonomous: false
requirements: [SEC-01]
must_haves:
  truths:
    - "Every one of the 21 credential rows is resolved in the DEAD? column specifically: probed dead with a recorded status code + UTC timestamp AND a non-empty Observed cell, or N/A (reason) — zero pending rows, verified by a column-aware parse, never a whole-row grep (cross-AI review HIGH-2, MED-4) — and the CRED ID set is machine-proven to be exactly CRED-01..CRED-21 with no duplicates, so a duplicated row cannot hide behind the 21-row count (cycle 3 MED-6)"
    - "The Apify row's Action cell carries the structured literal grace=declined, or grace=accepted; probe_after_24h=yes — parsed from that column specifically, with the template's grace=__PENDING__ placeholder blocking (cross-AI review MED-2); the parser fails unless exactly one CRED-17 row exists (cycle 3 MED-6)"
    - "The uniqueness attestation ATT-01 reads YES and every runtime-sweep attestation ATT-02..ATT-05 reads DONE — machine-verified, blank attestations block the commit (cross-AI review HIGH-3)"
    - "The evidence file still contains zero secret values after the CEO filled it (machine-verified before commit)"
  artifacts:
    - ".planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md (filled, gated, committed)"
  key_links:
    - "CEO checklist execution -> evidence rows (each checklist section names its CRED-NN targets)"
    - "Task 3 completeness gate -> checkpoint feedback loop (failing row IDs go back to the CEO, nothing is committed until clean)"
---

<objective>
The CEO personally executes every rotation, 2FA enrollment, dead-value probe, and runtime-state sweep following CREDENTIAL-ROTATION-CHECKLIST.md, filling ROTATION-EVIDENCE.md as he goes; the builder then machine-verifies evidence completeness and commits it (SEC-01).

Purpose: this is the heart of the CEO-locked hard gate — old values verifiably dead, per-item evidence, not ticks. The builder cannot do this work (dashboards require interactive auth; old values must transit only the CEO's terminal) and must not see any value.
Output: a complete, verified, committed evidence record for all 21 credentials.
</objective>

## Artifacts this phase produces

Full Phase 1 artifact set (bold = owned by this plan):

- `.gitleaks.toml`, `scripts/hooks/pre-commit`, `.github/workflows/secret-scan.yml`, `evidence/hook-selftest.md`, `evidence/gitleaks-history-report.json` (plan 01-01)
- `.gitignore` vault rules, `.env.example`, `.claude/settings.json` deny rules (plan 01-02)
- `CREDENTIAL-ROTATION-CHECKLIST.md`, `evidence/ROTATION-EVIDENCE.md` template, `ODT-SANITIZATION-PROCEDURE.md` (plan 01-03)
- **`evidence/ROTATION-EVIDENCE.md` — filled and committed** (this plan)
- `docs/source-architecture-notes-sanitized.md`, `evidence/ODT-SANITIZATION-EVIDENCE.md` (plan 01-05)
- `evidence/trufflehog-verified-sweep.md`, `evidence/PHASE-1-GATE-REPORT.md` (plan 01-06)

All `evidence/` paths are under `.planning/phases/01-security-baseline-credential-remediation/evidence/`.

Side effects owned by the CEO during this plan (outside the repo): all credentials rotated at providers; TOTP 2FA enrolled (Google, Microsoft x2, Namecheap, Private Email x2, Cloudflare, hostloom if offered); real `.env` optionally created by the CEO from the `.env.example` names — at repo root (gitignored; agents deny-ruled), or at `~/.dxb-vault/.env` if plan 01-02's committed `evidence/a8-deny-check.md` reads `A8-RESULT: FAILED+COMPENSATED` (compensating control, review HIGH-1).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-baseline-credential-remediation/01-CONTEXT.md
@.planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md
@.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: CEO rotates accounts and infrastructure (checklist sections 0-7, rows CRED-01..CRED-13)</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md — sections 0 through 7 (the CEO's script for this task)
    - .planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md — rows CRED-01..CRED-13
  </read_first>
  <action>
    CEO-only work — the agent presents this checkpoint and waits. Following checklist sections in order: 0 Before-you-start hygiene (password manager ready, TOTP app chosen, HISTCONTROL verified), 1 Google (TRAPDOOR: copy the old app password into the probe terminal BEFORE changing the account password — the change auto-revokes app passwords), 2 Microsoft Hotmail x2, 3 Namecheap dashboard, 4 Private Email support@ and sales@, 5 Cloudflare (agent token roll, legacy user token roll, Global API Key if it was in the leaked doc), 6 hostloom client area + cPanel (note panel URLs, no credentials; record 2FA unavailability as risk-acceptance if the host offers none), 7 WordPress (password + Log Out Everywhere Else + revoke application passwords + rotate salts + audit admin list). For each: rotate, enroll 2FA with backup codes, sign out everywhere, run the old-value probe in your own terminal, write ONLY the status/error code and UTC timestamp into rows CRED-01 through CRED-13. Never paste any value — old or new — into the evidence file, this chat, or any file.
  </action>
  <how-to-verify>
    1. Open evidence/ROTATION-EVIDENCE.md
    2. Rows CRED-01..CRED-13: each has Rotated (UTC), 2FA, Probe (UTC), Observed filled — or N/A with a reason (CRED-09 Global API Key may be N/A if it never appeared in the leaked doc)
    3. Every Observed cell holds only a status code or "login rejected"
  </how-to-verify>
  <acceptance_criteria>
    - rows CRED-01..CRED-13 each resolved in the DEAD? column (probe data + dead marker, or N/A (reason)) — machine-checked in Task 3
    - 2FA column filled for Google, Microsoft x2, Namecheap, Private Email x2, Cloudflare; hostloom row carries 2FA status or a risk-acceptance note
  </acceptance_criteria>
  <resume-signal>Type "rows 1-13 filled" when checklist sections 0-7 are complete and the evidence rows are written — or describe any blocker (e.g., a probe that did not reject).</resume-signal>
  <done>All account/infrastructure credentials rotated with 2FA, probed dead, and recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CEO rotates API keys, local machine, and runtime state (checklist sections 8-11, rows CRED-14..CRED-21 + attestations)</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/CREDENTIAL-ROTATION-CHECKLIST.md — sections 8 through 11
    - .planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md — rows CRED-14..CRED-21 and attestation rows
  </read_first>
  <action>
    CEO-only work — the agent presents this checkpoint and waits. Checklist section 8, per provider — create replacement key first (into the password manager, and into the vault .env if you are creating it now per .env.example names — at repo root, or at ~/.dxb-vault/.env if plan 01-02's evidence/a8-deny-check.md reads A8-RESULT: FAILED+COMPENSATED), then revoke the leaked key, then probe: OpenAI (CRED-14), OpenRouter (CRED-15 — use the key-info endpoint from the checklist, never the public models endpoint), NVIDIA Build (CRED-16), Apify (CRED-17 — TRAPDOOR: DECLINE the 24-hour grace window; if grace was accepted, probe at T+24h before marking dead; either way REPLACE the grace=__PENDING__ placeholder in the row's Action cell with exactly grace=declined, or grace=accepted; probe_after_24h=yes after the delayed probe — Task 3 parses that cell by column and blocks while the placeholder remains, review MED-2), Ollama cloud (CRED-18), OpenCode Zen (CRED-19). Section 9: 9Router local sweep (CRED-20) — nothing to revoke in a cloud; re-enter new provider credentials in its dashboard and sweep its config directory for stale key strings, recording file paths only; the row's DEAD? cell reads N/A (local software). Section 10: Linux sudo password (CRED-21) — passwd, then the sudo -K old-password rejection probe. Section 11: runtime-state sweep — browser/mail saved logins, tool configs (~/.config, ~/.local/share, ~/.claude*), shell rc/history scrub, stray .env files — fill the Status cell of each attestation row ATT-02..ATT-05 with exactly DONE (Notes: file paths only). Fill ATT-01 with exactly YES: all new values unique and password-manager generated.
  </action>
  <how-to-verify>
    1. Open evidence/ROTATION-EVIDENCE.md
    2. Rows CRED-14..CRED-21: probe status + UTC timestamp each (CRED-17's Action cell carries its grace= structured field with the __PENDING__ placeholder gone; CRED-20 shows swept + re-authed with N/A (local software) in DEAD?)
    3. ATT-01 Status is exactly YES; ATT-02..ATT-05 Status is exactly DONE with path-only Notes
  </how-to-verify>
  <acceptance_criteria>
    - rows CRED-14..CRED-21 each resolved in the DEAD? column — machine-checked in Task 3
    - CRED-17's Action cell contains the literal `grace=declined` or `grace=accepted; probe_after_24h=yes` and no longer contains `grace=__PENDING__` — column-parsed and machine-checked in Task 3 (MED-2)
    - ATT-01 Status = YES; ATT-02..ATT-05 Status = DONE with path-only Notes — machine-checked in Task 3 (HIGH-3)
  </acceptance_criteria>
  <resume-signal>Type "rows 14-21 and attestations filled" — or describe any blocker (e.g., Apify grace accepted, probe deferred to T+24h).</resume-signal>
  <done>All API keys rotated and probed dead, local machine password rotated, runtime state swept, attestations complete.</done>
</task>

<task type="auto">
  <name>Task 3: Machine-verify evidence completeness and commit</name>
  <files>.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md (the filled file — verify, never edit row content: rows are CEO-owned)
    - .planning/phases/01-security-baseline-credential-remediation/01-RESEARCH.md — "Evidence-Record Schema" rules
  </read_first>
  <action>
    Run the completeness gates against the CEO-filled evidence file — verify only, never modify row content. Row gates parse the DEAD? column specifically (10th table column; with awk -F'|' and the leading pipe that is field $11) — never a whole-row grep, which false-passes whenever N/A legitimately appears in another column such as 2FA or Action (cross-AI review HIGH-2). Gates: (1) zero unresolved rows and exactly 21 rows — for every table line whose ID field matches CRED-, the whitespace-stripped DEAD? field must be exactly the green check mark (U+2705) or match `N/A (reason)`: `awk -F'|' '$2 ~ /CRED-/ { n++; v=$11; gsub(/[ \t]/,"",v); if (v != "✅" && v !~ /^N\/A\(.+\)/) bad++ } END { print n"-rows "bad+0"-unresolved" }' file` must print `21-rows 0-unresolved`; (1b) unique ID set (cycle 3 MED-6 — the row count alone cannot prove uniqueness: a duplicated CRED-17 row, one pending and one resolved, keeps the count at 21 while masking the pending grace decision): `test "$(awk -F'|' '$2 ~ /CRED-/ { id=$2; gsub(/[ \t]/,"",id); print id }' file | sort)" = "$(awk 'BEGIN{for(i=1;i<=21;i++) printf "CRED-%02d\n", i}')"` must exit 0 — the ID column is exactly the set CRED-01..CRED-21, no duplicates, no extras; (2) every dead-marked row has a probe timestamp AND a non-empty Observed cell (review MED-4 — the completeness definition in the template requires both): `awk -F'|' '$2 ~ /CRED-/ { v=$11; gsub(/[ \t]/,"",v); if (v == "✅" && ($9 !~ /[0-9]{4}-[0-9]{2}-[0-9]{2}|[0-9]{2}:[0-9]{2}Z/ || $10 !~ /[^ \t]/)) bad++ } END { print bad+0 }' file` must print 0; (3) the Apify row (CRED-17) carries the structured grace literal in its Action column — column-parsed, never a whole-row grep, which prose in another cell could satisfy (review MED-2): `awk -F'|' '$2 ~ /CRED-17/ { n++; g=$5 } END { if (n == 1 && (g ~ /grace=declined/ || g ~ /grace=accepted; probe_after_24h=yes/) && g !~ /grace=__PENDING__/) print "GRACE-OK"; else print "GRACE-BAD" }' file` must print GRACE-OK — the `n == 1` guard makes the parser fail when zero or multiple CRED-17 rows exist, so a duplicate row can never win by being parsed last (cycle 3 MED-6); if the cell instead records grace accepted with the probe still pending (or still holds grace=__PENDING__), STOP and return to the CEO checkpoint with that row ID — the phase gate cannot pass with a pending probe; (4) attestation completeness (review HIGH-3): `awk -F'|' '$2 ~ /ATT-/ { n++; s=$4; gsub(/[ \t]/,"",s); if (s != "YES" && s != "DONE") bad++ } END { print (n==5 && bad==0) ? "ATT-OK" : "ATT-INCOMPLETE" }' file` must print ATT-OK, and the ATT-01 Status cell specifically must be exactly YES; (5) no key-shaped strings anywhere in the file: `grep -cE 'sk-[A-Za-z0-9]{20,}|sk-or-v1-[A-Za-z0-9]|nvapi-[A-Za-z0-9_-]{20,}|apify_api_[A-Za-z0-9]{20,}'` outputs 0; (6) `gitleaks dir ".planning/phases/01-security-baseline-credential-remediation/evidence" --redact` exits 0.

    If ANY gate fails: do not commit; list the failing gate numbers and row IDs (never cell contents beyond status codes) and return to the corresponding checkpoint for the CEO to fix. When all gates pass: commit the evidence file (the plan 01-01 pre-commit hook scans it again on the way in).
  </action>
  <verify>
    <automated>test "$(awk -F'|' '$2 ~ /CRED-/ { n++; v=$11; gsub(/[ \t]/,"",v); if (v != "✅" && v !~ /^N\/A\(.+\)/) bad++ } END { print n"-rows "bad+0"-unresolved" }' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md')" = "21-rows 0-unresolved" && test "$(awk -F'|' '$2 ~ /CRED-/ { id=$2; gsub(/[ \t]/,"",id); print id }' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md' | sort)" = "$(awk 'BEGIN{for(i=1;i<=21;i++) printf "CRED-%02d\n", i}')" && test "$(awk -F'|' '$2 ~ /CRED-/ { v=$11; gsub(/[ \t]/,"",v); if (v == "✅" && ($9 !~ /[0-9]{4}-[0-9]{2}-[0-9]{2}|[0-9]{2}:[0-9]{2}Z/ || $10 !~ /[^ \t]/)) bad++ } END { print bad+0 }' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md')" = "0" && test "$(awk -F'|' '$2 ~ /ATT-/ { n++; s=$4; gsub(/[ \t]/,"",s); if (s != "YES" && s != "DONE") bad++ } END { print (n==5 && bad==0) ? "ATT-OK" : "ATT-INCOMPLETE" }' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md')" = "ATT-OK" && test "$(awk -F'|' '$2 ~ /CRED-17/ { n++; g=$5 } END { if (n == 1 && (g ~ /grace=declined/ || g ~ /grace=accepted; probe_after_24h=yes/) && g !~ /grace=__PENDING__/) print "GRACE-OK"; else print "GRACE-BAD" }' '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md')" = "GRACE-OK" && gitleaks dir '.planning/phases/01-security-baseline-credential-remediation/evidence' --redact</automated>
  </verify>
  <acceptance_criteria>
    - column-aware unresolved gate (HIGH-2): `awk -F'|' '$2 ~ /CRED-/ { n++; v=$11; gsub(/[ \t]/,"",v); if (v != "✅" && v !~ /^N\/A\(.+\)/) bad++ } END { print n"-rows "bad+0"-unresolved" }' evidence/ROTATION-EVIDENCE.md` prints `21-rows 0-unresolved`
    - unique ID-set gate (cycle 3 MED-6): `test "$(awk -F'|' '$2 ~ /CRED-/ { id=$2; gsub(/[ \t]/,"",id); print id }' evidence/ROTATION-EVIDENCE.md | sort)" = "$(awk 'BEGIN{for(i=1;i<=21;i++) printf "CRED-%02d\n", i}')"` exits 0 — the CRED ID set is exactly CRED-01..CRED-21 with no duplicates, so a duplicated row (e.g. two CRED-17 entries, one pending and one resolved) cannot satisfy the row-count gate while masking a pending decision
    - every dead-marked row carries a probe timestamp AND a non-empty Observed cell (MED-4): `awk -F'|' '$2 ~ /CRED-/ { v=$11; gsub(/[ \t]/,"",v); if (v == "✅" && ($9 !~ /[0-9]{4}-[0-9]{2}-[0-9]{2}|[0-9]{2}:[0-9]{2}Z/ || $10 !~ /[^ \t]/)) bad++ } END { print bad+0 }' evidence/ROTATION-EVIDENCE.md` prints 0
    - attestation gate (HIGH-3): `awk -F'|' '$2 ~ /ATT-/ { n++; s=$4; gsub(/[ \t]/,"",s); if (s != "YES" && s != "DONE") bad++ } END { print (n==5 && bad==0) ? "ATT-OK" : "ATT-INCOMPLETE" }' evidence/ROTATION-EVIDENCE.md` prints ATT-OK, and `awk -F'|' '$2 ~ /ATT-01/ { s=$4; gsub(/[ \t]/,"",s); print s }' evidence/ROTATION-EVIDENCE.md` prints YES
    - Apify structured grace gate, Action-column-parsed from exactly one CRED-17 row (MED-2, cycle 3 MED-6): `awk -F'|' '$2 ~ /CRED-17/ { n++; g=$5 } END { if (n == 1 && (g ~ /grace=declined/ || g ~ /grace=accepted; probe_after_24h=yes/) && g !~ /grace=__PENDING__/) print "GRACE-OK"; else print "GRACE-BAD" }' evidence/ROTATION-EVIDENCE.md` prints GRACE-OK — zero or multiple CRED-17 rows print GRACE-BAD
    - key-shape grep outputs 0; `gitleaks dir` on the evidence directory exits 0
    - evidence file committed: `git log --oneline -- '.planning/phases/01-security-baseline-credential-remediation/evidence/ROTATION-EVIDENCE.md' | wc -l` outputs at least 1
  </acceptance_criteria>
  <done>Evidence is complete, secret-free, and committed: every credential in the inventory is verifiably dead or explicitly N/A with reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CEO terminal (old values) → evidence file | live-until-rotated secrets are handled during probes; only status codes/timestamps may cross |
| provider dashboards → CEO session | rotation actions happen in authenticated browser sessions the agent never touches |
| filled evidence → git history | the file is committed and permanent; it must be machine-proven value-free first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Spoofing | attacker retains access via an unrotated or unprobed credential | critical | mitigate | 21-row completeness gate parsing the DEAD? column specifically (column-aware awk, review HIGH-2) + unique CRED-01..21 ID-set gate so a duplicated row cannot hide a missing one (cycle 3 MED-6) + 5-row attestation gate (review HIGH-3); failing row IDs loop back to the CEO checkpoint |
| T-01-15 | Information Disclosure | CEO pastes a secret into evidence or chat during checkpoint | critical | mitigate | checklist hygiene preamble; key-shape grep + gitleaks dir gate BEFORE commit (Task 3); pre-commit hook as final layer |
| T-01-16 | Spoofing | Apify grace window: old token alive up to 24h after "rotation" | high | mitigate | decline-grace trapdoor in checklist; CRED-17 structured grace literal parsed from the Action column in Task 3, grace=__PENDING__ placeholder blocks (grace=declined / grace=accepted; probe_after_24h=yes — review MED-2); parser requires exactly one CRED-17 row so a duplicate cannot mask a pending decision (cycle 3 MED-6) |
| T-01-17 | Spoofing | session/app-password persistence after password change | high | mitigate | checklist pairs every password change with sign-out-everywhere + app-password revocation + WordPress salt rotation |
| T-01-18 | Elevation of Privilege | attacker persistence via forwarding rules, rogue admins, unknown API keys | high | mitigate | "Also check" steps in checklist sections (mail forwarding, WP admin audit, unknown-key deletion) |
</threat_model>

<verification>
- All 21 CRED rows resolved via the DEAD?-column parse, with the ID set proven exactly CRED-01..CRED-21 by the sorted-set compare (cycle 3 MED-6); every ✅ row carries timestamp + non-empty Observed (MED-4); Apify grace literal parsed from the Action column of the exactly-one CRED-17 row (MED-2, cycle 3 MED-6); 5 ATT attestations filled (ATT-01 = YES, ATT-02..05 = DONE)
- Evidence file secret-free by grep + gitleaks; committed to history
- Cycle-3 gate simulation (MED-6): a 21-row file containing two CRED-17 entries (one grace=__PENDING__, one grace=declined) and no CRED-18 — the cycle-2 parsers printed `21-rows 0-unresolved` and GRACE-OK because the last CRED-17 match won; the hardened gates now fail twice: the sorted ID list carries a duplicate and a gap so the set-compare exits non-zero, and the grace awk counts n=2 and prints GRACE-BAD; a clean file with a single CRED-17 row still prints GRACE-OK and the set-compare exits 0
</verification>

<success_criteria>
SEC-01 satisfied: every credential in the source-doc inventory rotated by the CEO, old value verifiably dead with per-item evidence (status + UTC timestamp), 2FA enrolled, runtime state swept — and the record is committed without a single secret value in it.
</success_criteria>

<output>
Create `.planning/phases/01-security-baseline-credential-remediation/01-04-SUMMARY.md` when done.
</output>
