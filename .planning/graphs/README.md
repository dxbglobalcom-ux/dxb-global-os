# This knowledge graph is STALE. Do not trust it.

<!-- HISTORY -->

**Measured 2026-07-30:** `graph.json` and `graph.html` were built on **2026-07-09 23:39**, and
**368 commits** have landed since. Every standing instruction that says *"query the knowledge
graph before any broad repo reading"* was, in practice, pointing sessions at a three-week-old
picture of a project that changes several times a day.

That is why the always-on core no longer carries a graph-first rule. The rule was not wrong — a
graph really is cheaper than scanning source — but a rule that points at a stale artefact is worse
than no rule, because it is confidently misleading.

**Until it is rebuilt, this directory answers nothing.** Read the board, the state photograph and
the spec that owns your work instead. Board row **B23** carries the decision: rebuild it, or
retire it and delete the rule that depends on it.

The files are kept, not deleted: rebuilding is cheaper from an existing cache, and deleting a
measurement because it aged is how history gets lost.
