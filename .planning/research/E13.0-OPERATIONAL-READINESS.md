# E13.0 Operational Readiness — drill report (2026-07-18 ~10:10)

Every verdict below is a measurement taken this session (RULE #0-A) or an
explicit cross-reference to recorded drill evidence. Author: Fable 5 in person.

## Leg-by-leg status

| E13.0 leg | Verdict | Evidence |
|---|---|---|
| Backup **restore drill** (RPO/RTO proof) | ✓ done 2026-07-17 | `scripts/restore-db.sh` — real live pg_dump → pristine container → ledger 87 / agents 220 / RTO 5s / inventory diff 0 + smoke 36/36 (recorded in E13.0 row + R2.5) |
| **Migration rollback drill** | ✓ done 2026-07-18 ~05:50 | `scripts/test/db-suite.sh` — ROLLBACK BLOCKS 46/46 declared + 0021x settings-family block EXECUTED LIVE on ephemeral (3 tables gone, stale CASCADE fixed in-file) — E13.1 machine-tier wave |
| **Off-site dump pipeline + cadence** | ◐ VPS ✓ / laptop local ✓ / laptop off-site CEO-gated | VPS: daily cron 02:30 + Storage Box `u629578-sub1` LIVE since 2026-07-09, OFFSITE_OK drill byte-identical (vps/README.md "Backups"). Laptop (current production host): `scripts/backup/laptop-pg-dump.sh` BORN, first dump EXECUTED (`BACKUP_OK 2026-07-18 10237807 bytes`, 0600, retention 14d), cron INSTALLED (`crontab -l` → 30 2 * * *). Laptop→StorageBox key install = CEO-owned credential step (new SSH key on subaccount needs renewed hcloud token) — recorded boundary; `BACKUP_DEST` hook ready |
| **Accessibility** (keyboard/focus/contrast/reduced-motion) | ◐ machine legs ✓ / authed-surface legs wait storageState | Contrast: `node scripts/contrast-audit.mjs` exit 0 (WCAG pairs PASS both themes, OKLCH-native). Keyboard focus order + accessible names + prefers-reduced-motion (§6 CSS rule asserted IN-BROWSER): `tests/e2e/public.spec.ts` accessibility battery 3/3 PASS (L5 public 8/8). Authed cockpit keyboard sweep joins the authed tier (storageState-gated, recorded reason) |
| **Failure-path tests** (timeout/duplicate/retry/partial/idempotency) | ✓ all five named classes have EXECUTED coverage | timeout → `tests/phase3/crash.test.ts` (kill -9 lease reap, same task re-handed) · duplicate → `tests/phase4/double-fire.test.ts` (2 parallel ticks, 1 execution, attempts=1) · retry → `tests/r24/staging-outbox.test.ts` (re-fired handler returns existing id, replayed:true, at-most-once) + `packages/outbox-executor/src/breaker.ts` retry-storm breaker · partial failure → `tests/phase4/toctou.test.ts` (unapproved ready row → failed, handler never runs) + HR 7-step atomicity (demo step 10, settings row parity) · idempotency → db-suite IDEMPOTENT OK (2nd push applies 0) + r24 message tags. Battery green tonight: 63 files 463/0 |
| **Pagination/perf 100+/10k** | ✓ PERF DRILL PASS 7/7 | `scripts/test/perf-drill.sh` BORN — ephemeral container, full 98-file chain push (fresh-parity re-proven incl. tonight's 4 migrations), synthetic 10k tasks + 10k audit + 1k approvals, EXPLAIN ANALYZE on the dashboard's REAL bounded queries (limits measured in source: approvals 200/50, tasks 50, library 30): all < 19ms, threshold 250ms. Empty-measurement = FAIL gate built in (a query error can never fake a PASS) |
| **Session E2E** | CEO-gated | authed L5 tier 9 scenarios ready, skip-with-reason until `node scripts/test/e2e-login.mjs` mints storageState |

## Remaining to close the row

1. Laptop→StorageBox off-site key (CEO credential step) → then `BACKUP_DEST` env and the same cron goes off-site; real-RPO statement follows.
2. Authed a11y/session-E2E legs → unlock with CEO storageState (2-min headed login).
3. CEO eye on baselines (E13.2 session).
