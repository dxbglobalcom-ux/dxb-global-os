# E12.5 — Workforce Completeness Gate: measured baseline (pre-activation)

**Date:** 2026-07-18 ~03:05 · **Author:** Fable 5 · **Status:** BASELINE ONLY — the
activation waves and the promised-ADD set-diff are NOT started; this report is the
row's "capability coverage" reporting leg's foundation, measured live.

## Measured state (all SQL against the live registry, 2026-07-18 03:00)

| Measure | Value | Meaning for the gate |
|---|---|---|
| agents total | 221 | roster after E5.x waves + R1.8 |
| employment_status | active **1** (finance) · dormant **143** · draft **55** · archived **22** | the holding's working hands = ONE employee |
| heads (role_level=director) | **21/21 departments** | "head'siz aktif dept 0" holds STRUCTURALLY — but every head is dormant |
| chain-ready dormant (persona_id + quality_gate=passed + mcp_profile) | **143/143** | activation is a status flip + hook/model/budget binding, not missing artifacts |
| chain-ready draft | **55/55** (persona bound + gate passed) | drafts are one step behind dormant, artifacts complete |
| stale persona_path (`agency-agents/%`) | **21** — ALL archived | gate demands 0; sweep = repoint/annotate archived history rows (mechanical) |
| persona_version column drift | **127** rows: file at `personas/%` (v2 tree) but column says `v1.0-legacy` | metadata drift — column correction belongs to the activation wave's seam, not a blanket UPDATE |
| DEPUTY-FAILOVER-MAP.md | exists, 49 table rows | SPOF-coverage check joins the activation wave |

## What remains for the gate (named, ordered)

1. **Promised-ADD set-diff → 0**: parse WORKFORCE-GAP-MATRIX §3 + WORKFORCE-MUST-
   EXPANSION-PLAN §4 ADD rows, diff against live agents; every missing role = a
   Fable-personal persona (CEO K2 — no factory, no delegation).
2. **Activation waves**: dormant→active per department with the full chain
   (registry + model routing + skill/MCP grants + permission + budget + dashboard
   + audit). ⚠ BLOCKED-CLASS DEPENDENCY: the MUSTS-Talep audit (2026-07-16) left
   CEO decision questions OPEN (worker objective contract, text-only worker scope).
   Mass activation before those answers = spend + blast-radius without a contract.
3. **153-legacy dispositions**: keep/merge/rewrite/retire per role, CEO-visible
   (no deletion without CEO approval).
4. **Stale-path sweep** (21 archived) + **persona_version drift** (127) — both
   mechanical, both belong inside the activation wave's seam discipline.
5. **Coverage report** against MUST-EXPANSION-PLAN §3 (capability, not headcount)
   + DEPUTY-FAILOVER-MAP SPOF verification.

## Why this matters beyond E12.5

R4.3's recorded boundary (arsenal row) waits on exactly this gate: the first
staffed worker task exercising the new external hands (git/context7/playwright/
scrapling) needs an ACTIVE engineering employee — activation here unlocks that
proof. The R3.1-era ladder debris ("only finance answers") is the same root.

## Verification commands (re-run any time)

```sql
SELECT employment_status, count(*) FROM agents GROUP BY 1;
SELECT count(*) FROM agents WHERE persona_path LIKE 'agency-agents/%';
SELECT count(*) FROM agents a JOIN personas p ON p.id=a.persona_id
 WHERE a.employment_status='dormant' AND p.quality_gate='passed' AND a.mcp_profile IS NOT NULL;
```
