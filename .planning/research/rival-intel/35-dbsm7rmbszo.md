# 35 — the same operator, filmed under the floor: paperclip, a Hermes gateway, a model roster, and an agent that reports its own unfinished work

> **READ THIS WITH 30 AND 33 — SAME OPERATOR, THIRD FILM.** Bennett Spooner / BENNETT OS. Reports
> `30-dbcpo0dmq3d.md` and `33-dbeny9artri.md` already carry the radial company graph, the SOP behind
> every node, the `1 runnable skill file` distribution move, the `FULLY AUTONOMOUS` ceiling, the two
> themes and the docked conductor. **None of that is repeated here.** 30 and 33 filmed the cockpit;
> **35 films the machinery under it** — the runtime that makes the cockpit move, named part by part.
> It is also the film that joins this queue to itself: the thing doing the delegating is
> **paperclip**, which is our own source 08.

## WHY IT MATTERS TO THIS HOLDING

This is the closest thing on the whole queue to DXB's own blueprint, and it is running. One operator,
a conductor that acts as CEO, department heads under it, a shared brain every agent reads, a gateway
that holds the tools, and all of it on one machine in his room reachable from anywhere.

And in one frame it does the thing this holding's own first rule demands of every session. At 00:60
his agent opens with:

> *"Nothing is blocked on me right now. **Three things are waiting on you, in order of payoff** … And
> two loose ends I flagged but you haven't ruled on … **What do you want to pick up?**"*

That is `.claude/CLAUDE.md` §0 — what is finished, what is next, what is blocked on him — written by a
rival's product and delivered to its owner unprompted. We wrote the rule. He shipped it.

---

## 1. Source identity

| | |
|---|---|
| Address | https://www.instagram.com/reels/DbsM7RMBSzo/ |
| Operator | Bennett Spooner — BENNETT OS |

---

## 2. The record — what the screen showed, second by second

72.19 s · 1080×1920 · 30 fps · AAC audio kept · watched start to end at native resolution with the
transcript beside it. Every row below is a design, mechanism or money observation, never a retelling
of the film.

| Time | On screen / in the audio | What it is |
|---|---|---|
| 00:00 | Title card **"How I run a 37+ AI Agent Team"** over a wall-mounted monitor showing a department tree | The claim the film has to pay for |
| 00:04 | Operator card: **Bennett Spooner · OPERATOR**, `admin@operatoros.ai · @bennettx.ai · 72,500 reach`, businesses listed as *Merydian Agency · Accelerants · Personal Brand · FounderOS / OperatorOS Personal* | One human, four businesses, on the surface itself |
| 00:04 | **`CONDUCTOR (SUPER AGENT)` ● IDLE · `claude-fable-5`**; under it `AI HEAD → CONDUCTOR`, *"super agent · builtin runtime until the Mac mini lands"*; a chat box `Chat with Conductor — reaches …` with `Send`; `AGENT TOOLS: broadcast · openelaw · tmux`; a counter reading **`21/30 ok`** | The CEO seat is an agent, and it carries its own health count |
| 00:04 | Flanking the conductor: **`G-Brain` — "markdown + pgvector knowledge store"**, and **`Comms Feed` — Gmail · WhatsApp · Slack, unified** | Memory and inbox are the conductor's two hands |
| 00:04 | Department cards, each with its own model and state pill: **Marketing/Growth ● IDLE `claude-opus-4-6` · TECH ● ERROR `gpt-5.4` · Finances ● IDLE `claude-fable-5` · Communications ● IDLE `claude-haiku-4-6`**, plus Sales. Each card holds a crew (Social Agent, Data Agent, Stack Monitor, Payments Pulse, Comms Agent …) with `BUILTIN` badges and an `AGENT TOOLS` row | **A department is a model + a crew + a toolbox**, and the model is printed on the card |
| 00:07 | Second surface: breadcrumb `bennett-os / agents`, heading `// RUNTIME` **`REAL AGENTS`**, tabs **ROSTER · HERMES WORKERS** | His own OS, separate from paperclip |
| 00:07 | Conductor described as *"the real CEO — claude-fable-5 on the company board · delegates, creates tasks, reads your data"*; input placeholder **`Message the CEO — it can delegate … pull real data`** | The owner talks to a CEO, not to a tool |
| 00:07 | Board messages from the conductor: *"Recovery check-in (BEN-25): standing cockpit thread — no new messages from Bennett since the last reply at 20:38 UTC … Will respond again on next Bennett message."* · *"Cockpit liveness fixed. Root cause of the recovery loop: this standing thread sat in_progress with no machine-visible continuation path … I have now registered a persisted issue monitor (daily check, next 2026-08-06 ~20:48 UTC)"* · *"Watchdog review BEN-26 closed out clean: subtree healthy, no restore needed"* | **The system files its own incident reports, with a root cause and a next-wake time** |
| 00:10 | Stat tiles: **TOTAL 30 · ACTIVE 15 · OPEN TASKS 7 · CRON JOBS 0** | Four numbers, no chart |
| 00:10 | `ACTIVITY` feed, one `CAST` row per worker, each carrying a real reading: *Gmail Worker — OperatorOS 1295 unread · Personal 59402 · Agency Accelerant 476 · Merydian 510 · **total 61683 unread*** · *Payments Pulse — Stripe: **3229.23 USD available** · 5 recent charges* · *Client Roster — Attio live: **100 deals on the roster** · funnel backup holds 7 clients* · *Attio CRM — **50+ deals on record*** · *WhatsApp Worker **FAIL** — ChatStorage.sqlite found but the read timed out. Likely permissions: grant Full Disk Access* · *Social Agent — Zernio LIVE · **Arcads DOWN** · 1 post queued* · *Zernio Publisher — 0 platforms, 0 total followers* | **Every line is a measured value, and the failures are printed beside the successes with the fix named** |
| 00:13 | paperclip's cockpit: sidebar `Bennett OS ▾` · New Task · **Dashboard ● 1 live** · **Inbox 5** · WORK: Tasks, Routines, Artifacts, Skills, Projects · AGENTS: **Conductor ● 1 live**, See all agents · COMPANY: **Org · Timeline · Costs · Activity · Audit · Settings** | The nineteen control areas of a holding, in one rail |
| 00:13 | Dashboard: a live agent card — **`Conductor` `RUNNING`**, task `BEN-25 ~ Bennett OS Cockpit`, its own reasoning printed in the card (*"Bennett wants six micro-tasks fanned out in parallel, one per department agent. First I'll look up the agent roster to get their IDs, then create all six child issues in one parallel batch."*), and under it **`Working` for 9 seconds · called 2 tools · startup step: workspace.resolve (0ms)** | **The card shows the plan, the elapsed time, the tool count and the step — while it runs** |
| 00:13 | Second card: `Conductor · Finished 7h ago · BEN-30 — DELEGATION TASK 1 · worked for 2 minutes` | Finished work keeps its duration |
| 00:13 | Tiles: **10 Agents Enabled** (1 running, 2 paused, 1 errors) · **1 Tasks in Progress** (6 open, 1 blocked). Charts: **Run Activity, last 14 days** (Succeeded / Recovered / Failed / Other) and **Tasks by Priority, last 14 days** (Critical / High / Medium / Low) | A **Recovered** class exists beside Succeeded and Failed |
| 00:13 | `RECENT ACTIVITY`: *Conductor **environment lease acquired*** … *Conductor **environment lease released** · 2h ago* | An agent takes and returns a lease on its workspace |
| 00:16 | AGENTS page: tabs `All · Active · Paused · Error`, **"10 agents"**, tree under **Conductor CEO** — Communications (Communications Lead) · **Finances CFO** · **Forge Engineer – Founding Engineer** · Hermes Workers · **Marketing/Growth CMO** · **Reflection Coach** · Sales Lead · **Summarizer** · **TECH CTO** | Job titles, not agent ids — and two of them exist only to think about the work: **Reflection Coach** and **Summarizer** |
| 00:16 | Status bar of the browser: **`larps-mac-mini.tail090dce.ts.net:3100/BEN/agents/sales`** | One Mac mini, Tailscale, port 3100 — the whole company's address |
| 00:26 | Model roster, one row per agent: `claude-fable-5 / Claude Code` ● **Live · running** · `claude-haiku-4-6` idle · `claude-fable-5` idle · `claude-fable-5` 1d ago idle · **`— / Hermes Gateway` 3d ago idle** · `claude-opus-4-8` idle · `— / Claude Code` **paused** · `claude-sonnet-4-6` idle · `claude-haiku-4-5` paused · **`gpt-5.4 / Codex` 9h ago `error`** | **Six model tiers and two vendors on one page, each with last-activity and a state pill** |
| 00:30 | `HERMES WORKERS` tab opens a full console inside the page: rail **CHAT · SESSIONS · FILES · MODELS · LOGS · CRON · SKILLS · PLUGINS · MCP · CHANNELS · WEBHOOKS · PAIRING · PROFILES · CONFIG**; System block reads **Gateway Status: Running · Active Sessions: 1**, with `Restart Gateway` and `Update Hermes` | The tool layer has its own operating console, not a settings page |
| 00:30 | Hermes banner: *Nous Research — Messenger of the Digital Gods* · **Hermes Agent v0.19.1 (2026-7-31)** · Available Tools — files: `patch, read_file, search_files, write_file`; other: `tool_call, tool_describe, tool_search`; terminal: `process, terminal` · **Available Skills (78) in 35 categories · 9 tools · 78 skills** · model **`glm-5.2`** · `Session: 5c42f835` · status line **`ready | glm 5.2 | 4s | voice off | 2 sessions`** | **Nine tools, seventy-eight skills, one gateway — and the status line prints the model, the latency and the session count** |
| 00:34 | The brain, live: a green path lights up **Pava → PAVA Financing → "Quote financing op…" → Finances** while the rest of the map stays dim | **A traversal is drawn as it happens** |
| 00:38 | The brain zoomed out: department nodes on dashed spokes — Finances (green) · Clients (teal) · Sales (red) · Marketing/Growth (orange) · Communications (blue) · TECH (purple) — around a dense red core, ringed by ~40 person icons and ~40 tool icons | The company as one picture |
| 00:47 | `// KNOWLEDGE CORE` **`G-BRAIN`**, input **`dump into the brain… or drop documents`** · `text · voice · drag or upload` · view toggle **`Radial | Neural`**; side panel counts **6 FOLDERS · 1 CLUSTERS** with a bar list `35 · 32 · 30 · 12 · 6 · 5` and rows `CLAUDE ARCHIVE 32↔ · ORG 29↔`. The core's sub-clusters are labelled **tools · people · org · agents · sops · Claude Archive · Service active clients** | **The brain has one input for everything and two ways to look at itself** |
| 00:52 | Department drill-down, `COMMUNICATIONS`: four tool nodes (`Comms Feed · Slack · Imap · Whatsapp`) → five workers (`Comms Agent · Slack Worker · Mia Torres · WhatsApp Worker · Gmail Worker`) → five work items (*"Compose the unifie…", "Digest Slack chann…", "Handle escalations…", "Monitor WhatsApp c…", "Triage the four Gm…"*) → all dotted lines converge on the **Communications** node. Pager `‹ Communications ›` | **Four layers — tool, worker, work, department — and a human name sits in the worker row beside the agents** |
| 00:56 | Same shape for `FINANCES`: tools → seven workers (incl. **Dana Whitfield**, `Fanbasis`, `Processor`) → *"Close the books mo…", "Confirm payments a…", "Quote fin…"* → Finances | The pattern holds across departments |
| 00:60 | A coding session reports to him: *"**Nothing is blocked on me right now. Three things are waiting on you, in order of payoff:** 1. Taildrop a snippet with the **9 missing API keys** — Attio, Zernio, ManyChat, Miro, Arcads, WebinarJam, Trakyo, Meta Ads, Notion (that one's present but empty). **Takes 11 → 20 of 22.** 2. **G-Brain** — send `~/.gbrain/config.json` and the `brain-store/` corpus and `/brain` stops being a dead page here. 3. Say go on the brew installs — ollama, tmux, whisper would lift local-stack from **2/9 to ~5/9**. **I haven't touched your machine without asking.** … neither service survives a reboot yet (both are LaunchAgents, auto-login is off — the Hermes gateway has the same gap). **What do you want to pick up?**"* · `Worked for 12s` | **The position, the price of each item, the blocker, and the thing it refused to do without permission** |
| 00:60 | The other pane: *"**None of this is live yet.** :4100 is still the production build — it needs `npm run build` … I won't restart it without your say-so."* · *"**Nothing is committed** — 12 files across … still on `main` against your working tree."* · *"25 new tests across the three modules, typecheck clean, **972/973 pass**"* · *"I also put each connector probe on a 2.5[s timeout]; a slow vendor now degrades to an honest error instead of hanging the whole page."* · `Baked for 10m 19s` · `/compact` → `Compacting conversation… (31s)` | **It states what is NOT done before what is** |
| 00:60 | Its own workspace rail: `Workspaces · Tasks · Pull requests`, projects **FounderOS… `+4 ↑2 +10 −29`** and **Bennett-OS `+4`**, and a row of agent tabs: **claude · amp · codex · gemini · copilot · vibe · kimi · grok** | Eight assistants side by side in one window |
| 00:68 | Skill card inside G-Brain: **"Confirm payments across pro…"** · `Finance · Payment Verification` · **`FULLY AUTONOMOUS`** with a toggle · rule: *"No deal marked paid without an [invoice] from the claimed processor."* · **`⤓ 1 runnable skill file — yours to download`** · **BREAKS INTO** `claim-receiver · api-verifier · audit-writer` · **BUILDS ON** `Processor Registry` · **$ WHAT IT REPLACES** *"Taking a salesperson's word that … was paid."* · **THE LADDER** `HUMAN-LED → HUMAN-ASSISTED → …` | A capability card that names its **sub-skills, its dependency, the human cost it removes, and the rung it stands on** |
| 00:72 | Closing card **"COMMENT 'BUILD' to see the stack"**; audio: *"if you want the free repo to try out for yourself, just come and build and I'll send it over"* | The money mechanism, stated |

---

## 3. Capabilities — the mechanism 35 adds

**The runtime is three named parts, and the film points at each one.** A **conductor** — Claude Code
run headless so his subscription is used instead of the API, carrying `claude-fable-5` — takes what
the owner pastes in and delegates. **paperclip** (our source 08) is the app the delegation travels
through: it holds the tasks, the roster, the dashboard and the audit trail, and it is reachable at
`larps-mac-mini.tail090dce.ts.net:3100`. **Hermes** is the action layer between intent and execution,
running `glm-5.2`, carrying **9 tools and 78 skills in 35 categories**, with a gateway that reports
`Running`, a session count, and its own `Restart Gateway` / `Update Hermes` buttons. Above all three
sits **G-Brain**, *"markdown + pgvector knowledge store"*, which every agent reads so, in his words,
*"none of the agents hallucinate."*

**The model is a property of the department, printed where the department lives.** Marketing runs
`claude-opus-4-6`, Finances `claude-fable-5`, Communications `claude-haiku-4-6`, TECH `gpt-5.4` on
Codex — and TECH is the one showing `ERROR`. The roster screen puts model, last activity and state on
one line for all ten. **Six model tiers, two vendors, one page, no configuration file.**

**The brain is walkable, and it is the same picture at every depth.** One input takes everything —
`text · voice · drag or upload`. Two views, `Radial` and `Neural`. From the whole company you step
into a department and get the same grammar four layers deep: **tool → worker → work item →
department**, with the dotted edges of the work converging on the department node. Human names sit in
the worker layer beside the agents (`Mia Torres` in Communications, `Dana Whitfield` in Finances), so
the map is of the COMPANY and not of the software.

**Every capability is a card with a rung on a ladder.** `FULLY AUTONOMOUS` with a toggle, the rule it
enforces, the sub-skills it breaks into, what it builds on, **what it replaces in human terms**, and
the ladder from `HUMAN-LED` through `HUMAN-ASSISTED`. Autonomy is presented as a position that moves,
not a switch that is on.

**And the machine argues its own case in the owner's units.** Three asks ordered *"by payoff",* each
with the number it moves — API keys `11 → 20 of 22`, brew installs `2/9 → ~5/9` — the two loose ends
he has not ruled on, and the sentence that decides whether a system can be left alone: *"I haven't
touched your machine without asking."* It reports `972/973 pass`, that **nothing is committed**, that
**none of it is live yet**, and that **neither service survives a reboot**. It closes by asking what
he wants to pick up.

### Aliveness — how 35 shows this system is built to live

**1. What runs on its own clock, and by what machinery.** A **daily persisted issue monitor** with its
next wake printed on the board — *"next 2026-08-06 ~20:48 UTC"* — created by the conductor itself
after it diagnosed a recovery loop. A **watchdog review** (`BEN-26`) that closed clean without being
asked. The **CRON** rail inside the Hermes console, and a `CRON JOBS 0` tile that says plainly how
many are set. **Environment leases** taken and released around agent runs. The whole thing on a Mac
mini behind Tailscale, so *"I can go anywhere in the world and it's always gonna be running."* And
against all of it, the machine's own admission that **neither service survives a reboot yet** —
LaunchAgents with auto-login off. The clock runs; the clock is not yet armed.

**2. What makes the surface breathe, and the screen's own movement, MEASURED.** The film is handheld,
so the screen was measured against a static control region using the queue's own instrument
(`scripts/rival-intel/motion.py 35 --screen 100,300,1000,1300 --control 100,60,900,240`). Of 71
consecutive frame-pairs the camera held still on only **5** — `t013, t028, t038, t056, t069`. On those
five pairs: **screen 22.645 against a control of 1.970 — a ratio of 11.5×**, with a per-pair range of
**7.365 … 40.085**. **The surface never rests while the camera does.** The sample is small and is
declared: five pairs, not seventy-one.

The timed figures the screen prints about itself are firmer than any pixel measurement, and they are
all elapsed-work counters rather than decoration: **`Working for 9 seconds · called 2 tools`**,
`startup step: workspace.resolve (0ms)`, **`worked for 2 minutes`**, **`Worked for 12s`**,
**`Baked for 10m 19s`**, `Compacting conversation… (31s)`, a connector probe timeout set to **2.5 s**
so *"a slow vendor degrades to an honest error instead of hanging the whole page"*, and a Hermes
status line carrying **`4s`** as its live latency. **Time on this surface always means work, never
animation.**

**3. How it answers the human.** Three doors, all of them a sentence rather than a form: the conductor
box on the hierarchy screen (`Chat with Conductor — reaches …`), the CEO line on the runtime page
(`Message the CEO — it can delegate … pull real data`), and G-Brain's single input that takes text,
voice, a drag or an upload. Hermes adds a fourth from the terminal side, with `voice off` shown as a
state that can be turned on. **A latency figure for the answer is not published in this film —
UNREADABLE from the source**; what is published is the elapsed time of the WORK, which is the number
this operator chose to show.

**4. What DXB takes.** In §5, and only what 30 and 33 did not already hand over.

---

## 4. What DXB has today — measured 2026-08-19

| | DXB, measured this session | 35, measured on screen |
|---|---|---|
| Agents | **205** written | `TOTAL 30 · ACTIVE 15`, roster page `10 agents`, title card `37+` |
| Agents running now | **0** — `dxb-jarvis` and `dxb-scheduler` are `inactive` and `disabled` by CEO order | 1 running · 2 paused · 1 error, the rest idle with a last-activity time each |
| Model shown per department | not on any surface | printed on every department card and every roster row, six tiers across two vendors |
| Shared brain | `memory_index` **13,919** rows, **0 relations**, `memory-store/relation/` **0 files** | G-Brain: 6 folders, 1 cluster, a walkable four-layer map per department |
| Open tasks | **0** | `OPEN TASKS 7` · `1 in progress, 6 open, 1 blocked` |
| Scheduled work | 15 schedules existed when the services last ran; **0 running now** | `CRON JOBS 0` printed, plus one persisted daily monitor with its next wake time |
| Money on the surface | lifetime revenue **€0.00** | `Stripe: 3229.23 USD available · 5 recent charges`; `Attio live: 100 deals on the roster`; `50+ deals on record`; `funnel backup holds 7 clients` |
| The first reply to the owner | a written rule in `.claude/CLAUDE.md` §0 | a shipped behaviour: three items ordered by payoff, two open rulings, one question |

---

## 5. The build project

**Read §5 alone if you read nothing else.** Six parts, none of them repeated from 30 or 33. They are
filed against what this holding already owns; nothing here opens a new plan.

**1. THE POSITION REPORT IS A PRODUCT SURFACE, NOT A SESSION HABIT.** We wrote *"your first reply
tells him where the work stands"* as a rule for the author. He built it as a **thing the system emits**:
its own state (clear), then the owner's items **ordered by payoff, each with the number it
moves** (`11 → 20 of 22`), what it refused to do without permission, and a closing question. Our
equivalent belongs on the CEO's own surface, generated by the machine, not by whoever happens to be
in the session. **This is the single highest-value part in the film and it needs no install and no
money.**

**2. THE MODEL BELONGS ON THE CARD.** Every department and every roster row prints its model, its last
activity and its state pill, and the one department that is broken (`TECH · gpt-5.4 · ERROR`) is
broken in public. We route 205 agents across tiers and show the CEO none of it. Putting model +
last-activity + state on the employee and department surfaces is a display change over data we
already hold.

**3. `Recovered` IS A THIRD OUTCOME, AND IT BELONGS IN OUR CHART.** His run-activity chart has four
classes — **Succeeded · Recovered · Failed · Other**. A system that repairs itself and says so is
telling the owner something a green/red bar cannot. Our task and run records already carry retries;
the class is missing from the reading, not from the data.

**4. THE BRAIN IS WALKED, NOT QUERIED — FOUR LAYERS, ONE GRAMMAR.** `tool → worker → work item →
department`, the same shape at every depth, with human names in the worker layer beside the agents.
This is the **display half** of the memory decision the board already approved
(`v2-memory-brain-2026-08-01`); source 34 supplied the mechanism half. Our measured gap is unchanged —
13,919 facts, 0 relations — and this film shows exactly what the edges are for once they exist.

**5. THE MACHINE STATES WHAT IS NOT DONE, FIRST.** *"None of this is live yet."* *"Nothing is
committed — 12 files."* *"Neither service survives a reboot."* *"972/973 pass."* *"I haven't touched
your machine without asking."* We have this as governance prose — evidence before done, measure never
guess, the approval gate. **He has it as sentences the product says out loud.** Our completion
reporting should read like that: the failure, the count, the thing not touched, then the result.

**6. THE CAPABILITY CARD CARRIES ITS OWN LADDER.** 30 already recorded the downloadable skill file and
the `FULLY AUTONOMOUS` ceiling. What 35 adds is the rest of the card: **BREAKS INTO** (its sub-skills),
**BUILDS ON** (its dependency), **WHAT IT REPLACES** (the human act it removes, in one plain
sentence), and **THE LADDER** from `HUMAN-LED` through `HUMAN-ASSISTED` to autonomous. Our 19 control
areas each need that fourth line — *what human act does this remove* — before any of them can be
called finished.

### What must NOT be copied, and why

- **Not the stack.** paperclip, Hermes/`glm-5.2`, Tailscale, LaunchAgents, a Mac mini — every one is a
  second runtime beside the one we already run. `STACK.md` governs, and the answer is no.
- **Not `FULLY AUTONOMOUS` on money.** His payment-verification skill sits at fully autonomous. Ours
  stops at the CEO: money out, contracts, e-mail, ad spend and identity. The ladder is worth copying;
  its top rung is not ours to reach on those acts.
- **Not eight assistants in one window.** `claude · amp · codex · gemini · copilot · vibe · kimi ·
  grok` side by side is a workshop, not a company. One author per session is a law here.
- **Not the unread counter as a status.** `61,683 unread` printed on a company surface is a number
  that reports the owner's backlog, not the system's work.

---

## 6. Verdict

**What this source PRODUCES, measured on its own screen.** A live company on one machine:
**Stripe — 3,229.23 USD available with 5 recent charges**; **Attio — 100 deals on the roster and 50+
deals on record**, with a funnel backup holding **7 clients**; **11 of 22 connectors working** and the
other nine blocked only on API keys the owner has to send; **972 of 973 tests passing**; four
businesses named on the operator card (Merydian Agency, Accelerants, Personal Brand, FounderOS /
OperatorOS) and **72,500 reach** behind them. The system files its own incident reports, sets its own
next wake time, and closes its own watchdog reviews. It is also honest about what it is not: nothing
committed, nothing live on the production port, neither service surviving a reboot, one department in
`ERROR`, `Arcads DOWN`, `WhatsApp Worker FAIL`, `Zernio Publisher 0 followers`.

**How it earns.** Two mechanisms, both on screen. The businesses are the revenue and the OS runs them —
the Stripe and Attio readings are the proof. The OS itself is the **lead magnet**: *"if you want the
free repo to try out for yourself, just come and build and I'll send it over"*, over a closing card
reading **`COMMENT "BUILD" to see the stack`**. He gives the repository away and keeps the audience;
paperclip, the piece doing the actual delegating, is somebody else's open-source project (our source
08). **He sells nothing in this film and monetises the attention the film creates.**

**Against DXB, on the only measure that decides.** He runs 10 agents on a Mac mini and takes payments
through them. We hold 205 written agents with **0 running today** and **€0.00 lifetime revenue**. On
does-it-work-and-does-it-earn we are behind him, and the distance is not design — 30, 33 and this film
show a surface we can match. **The distance is that his is switched on.**

**What 35 changes for us that 30 and 33 did not.** Those two showed the finished cockpit and left the
question of what runs underneath. This one names every part, prints the model on every card, shows the
brain being walked department by department, and — the part worth the whole film — shows a machine
opening its report with what is blocked, what is waiting, and what it refused to do without
permission. Ours lives as prose in a rule file; his product performs it on screen.

---

## What was read

`media/35-DbsM7RMBSzo.mp4` watched start to end with its audio — 72.19 s, 1080×1920, 30 fps, AAC ·
`transcripts/35.json`, all 36 segments, 294 words, read beside the picture · all 72 native-resolution
frames `frames/35/t001.jpg … t072.jpg` (1080×1920, never downscaled) in order · screen motion measured
with `scripts/rival-intel/motion.py` against a static control region. Cross-checked against
`30-dbcpo0dmq3d.md`, `33-dbeny9artri.md` (same operator) and `08-paperclip-repo.md` (the app filmed
here) so nothing already reported is repeated. Own side measured live against the company database,
SELECT only.

**Fingerprint of what was studied:** `media/35-DbsM7RMBSzo.mp4`, 24,254,492 bytes, duration
72.191995 s, fetched `2026-08-07T22:24:38Z`, sha256 `f89c91cb7083afad0edab66802bc1ac4fd541e8a0e4fc09ce734565f88a69757`.
