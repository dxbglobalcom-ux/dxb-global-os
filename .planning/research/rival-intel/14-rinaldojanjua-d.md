# SOURCE 14 — Rinaldo Janjua (`rinaldojanjua.ai`) D: `attu.ai` — the whole company, shipped as a product

> ## ⛔ SCOPE CORRECTION — CEO ruling 2026-07-28, binding on this report
>
> **What was measured here is the RECORDING. What was NOT measured is the SYSTEM.**
> A frame-by-frame reading proves what a 15–100 second video showed. It proves nothing about what
> that system does when the camera is off, what it earns, how long it has run, or whether its
> owner is satisfied with it. **I have never used any of these systems.**
>
> The CEO knows several of these systems and their owners personally. His words, 2026-07-28:
> *"BEN O SİSTEMLERİ VİDEODAN DEĞİL, HEPSİNİ TANIYORUM — ARKADAŞLARIM — VE MİLYONLARCA DOLAR
> KAZANIYOR VE HEPSİ GERÇEK, HEPSİ CANLI."*
>
> **His testimony outranks this reading.** Every verdict below is therefore scoped to the
> recording, never to the company behind it, and where his account and this reading disagree,
> **his is the evidence and this is the guess.** Sentences that judged a system rather than a
> video were a RULE #0-A violation by the session author and have been corrected in place, with
> the correction recorded rather than quietly overwritten.


> The last of the CEO's four videos from this account, and **the most important of the four by
> a wide margin**. Rows 11–13 showed individual pipelines drawn on a whiteboard. This one shows
> the *product they live inside*: a shipped web application called **attu.ai** with a
> CEO/Orchestrator, five specialist agents, a knowledge graph, an agent log and a "Speak with
> the CEO" orb.
>
> It is the closest thing in the sixteen to **what the DXB command centre is supposed to be** —
> and it is running, today, in someone else's browser.

---

## 1. Source identity

| Field | Value |
|---|---|
| URL | https://www.instagram.com/reel/DbKe90ETKfB/ |
| Uploader | `rinaldojanjua.ai` |
| Kind | Instagram reel, vertical, split-screen (app above, presenter below), one-word captions |
| File | `media/14-DbKe90ETKfB.mp4` (6,302,727 bytes) |
| **sha256** | `7e8ceb034a5ed3e140b1408e7896e2a9b182d8356617245385f59c0651f510b6` |
| Duration | 61.0 s |
| Material studied | **61 frames** at 1 fps — swept as 6 contact sheets |
| Transcript | `transcripts/14.json`, language `en` |
| **Product name, read off the app's own header** | **`attu.ai`** |

---

## 2. Frame-by-frame record

| Time | On screen | Said | Frame |
|---|---|---|---|
| 0:00 | The app, `attu.ai`. Left rail: **`Command Center · CEO Brief · Agents · Workflows · Scheduler · Lead Pipeline · Context · Knowledge Vault · Outputs`**, and below it a live agent list with avatars and status chips (`CMO`, `Sales Rep`, `Dev`, `COO`, `Data Analyst`) | **"A CEO, a COO, a CMO, a sales rep, a developer and a data analyst all running my business as I'm filming this reel."** | t001–t009 |
| 0:09 | The **Agents** page. Header `Agents`, subtitle *"One command layer coordinating [N] five specialist agents"*, status chip top-right **`● Agents System Operational`** | *"This is a **complete operating system**."* | t009–t012 |
| 0:12 | The org tree: a purple card **`CEO/Orchestrator`** with a green **`● Active`** chip, two counters **`10`** and **`954`**, and the line *"Routes work, reviews context, coordinates specialists, and returns the next operator decision"*; five child cards joined by lines — **`CMO` (Content & SEO)**, **`Sales Rep`**, **`Dev`**, **`COO`**, **`Data Analyst`** — each with its own coloured icon, role line and `● Active` chip. At the foot of the page, a glowing **blue orb** labelled **"Speak with the CEO"** with a hint line and a text field *"Speak or type here…"* | **"Every agent has their own role, their own tasks, and their own context from the rest of the business."** | t012–t018 |
| 0:18 | **The `COO` agent's own page**: its capability list, each row a named skill with a description and a chip — `manage email` · `inbox triage` · `vanity-number checker` · `ai-daily-notes` · **`Daily Inbox Cleanup — 15:00 / 01:00 … every day`**. *This is source 11's `Inbox Cleanup` board, as a scheduled capability owned by a named agent.* | *"My **COO** pulls competitor and market context."* | t018–t021 |
| 0:21 | **The `CMO` agent's page**: `deep research` · `techOps` · `yt-competitor-research` · `youtube-transcript`, each with a paragraph of description | *"My **CMO** turns that into **content briefs**."* | t021–t024 |
| 0:24 | back to the org tree | *"My **sales rep** checks leads and follow-up opportunities."* | t024–t028 |
| 0:28 | org tree | *"My **data analyst** watches performance, my **developer** improves my workflows, and then my **CEO decides what needs attention first** and then makes the decisions."* | t028–t038 |
| 0:38 | **The Command Center**: a row of stat tiles across the top, the **"Speak with the CEO"** orb in the centre with its input, and below it an **`Agent Log`** panel — rows carrying a request and a green completion chip: *"research on my competitors"*, *"automate everything that I'm doing here for my business please"*, each with a timestamp | **"But the most important part is the shared memory system."** | t038–t042 |
| 0:42 | **The `Knowledge Vault`** page. Title `Knowledge Vault`, a counter reading **`954 · 17`**, a search field *"Search leaders, memories, topics…"*, and a **knowledge graph**: a large ring of teal nodes above, a coloured cluster below around a central brain node, edges between them. Right-hand panel: *"Click a node to inspect it."* Three counter tiles at the foot: **`2` · `10` · `1`** | **"When one agent learns something, the whole system can then use it — instead of starting from zero every single time."** | t042–t049 |
| 0:49 | **A node inspected**: the right panel fills with a named memory — **`vfli-ai-audit`** — and its contents listed as files: `working-md`, `north-index-md`, `vote-index-md`, `automation-priority-matrix-md`, `way-recommend-md`, `user-research-md` | *"This is what it means by **replacing manual business operations with an autonomous, self-improving AI agent system**."* | t049–t056 |
| 0:57 | the graph again | *"If you want me to show you the **complete structure breakdown**, comment 'system' down below."* | t057–t061 |

### 2.1 The product, as its own navigation describes it

| Rail item | What it is |
|---|---|
| **Command Center** | stat tiles + the "Speak with the CEO" orb + the **Agent Log** |
| **CEO Brief** | the daily briefing, as its own destination |
| **Agents** | the org tree, each agent a card with `● Active` and its own detail page of named skills |
| **Workflows** | — |
| **Scheduler** | (the COO page shows `Daily Inbox Cleanup — every day`) |
| **Lead Pipeline** | — |
| **Context** | — |
| **Knowledge Vault** | the shared memory, as a searchable **graph** with inspectable nodes |
| **Outputs** | — |

---

## 3. Capabilities

| ID | Capability | What the source demonstrates |
|---|---|---|
| **CAP-14-A** | **One orchestrator + named specialists, drawn as a live org tree** | `CEO/Orchestrator` above `CMO · Sales Rep · Dev · COO · Data Analyst`, each with `● Active` |
| **CAP-14-B** | **"Speak with the CEO" is the primary control, on the main screen** | the orb with "Speak or type here…", present on both Command Center and Agents |
| **CAP-14-C** | **An Agent Log of what was asked and what came back** | *"research on my competitors"* → green completion chip, timestamped |
| **CAP-14-D** | **Each agent has its own page listing its named skills** | COO: `manage email`, `inbox triage`, `ai-daily-notes`, `Daily Inbox Cleanup` (scheduled) |
| **CAP-14-E** | **A pipeline is owned by an agent and scheduled** | `Daily Inbox Cleanup — every day` on the COO's page — rows 11–13's boards, given an owner and a clock |
| **CAP-14-F** | **Shared memory as a searchable, inspectable graph** | Knowledge Vault: `954 · 17`, search, click a node, read its files |
| **CAP-14-G** | **Memory entries are documents, not embeddings** | the inspected node `vfli-ai-audit` holds `working-md`, `north-index-md`, `automation-priority-matrix-md`, `user-research-md` |
| **CAP-14-H** | **A system-health chip in plain words** | `● Agents System Operational` |
| **CAP-14-I** | **Counters on the orchestrator card** | `10` and `954` beside `CEO/Orchestrator` |
| **CAP-14-J** | **`CEO Brief` is a first-class destination** | its own rail item, not a panel buried inside something else |
| **CAP-14-K** | **Cross-agent learning stated as the point of the whole system** | *"when one agent learns something, the whole system can use it instead of starting from zero"* |

---

## 4. What DXB has today

| ID | DXB status | Evidence taken today |
|---|---|---|
| **CAP-14-A** live org tree | **NO — and we have 199 agents to their 5** | 22 departments and 199 personas exist; `(command)/org` and `(command)/live` are separate routes and neither is measured to show live activity. Same gap as source 10, now confirmed twice. |
| **CAP-14-B** speak-to-orchestrator on the main screen | **PARTIAL** | Chat lives at `(command)/chat`, voice at `(command)/voice` — two separate routes, neither on the overview. Their single orb is on every screen. |
| **CAP-14-C** agent log of ask → result | **PARTIAL and broken at the seam** | `agent_runs` and `tasks` carry the data. But the one place the CEO can dispatch work — the chat's "send as task" button — is **silently broken**: `apps/dashboard/src/components/chat/chat-board.tsx:285-297` has no `else` branch, so a failed `/api/intent` call tells him nothing. Their log shows a green chip; ours shows nothing at all when it fails. |
| **CAP-14-D** per-agent skill pages | **PARTIAL — we have richer data and no page** | 199 personas are file-first (`personas/<dept>/<slug>.md`) with a DB mirror for runtime, and `std.knowledge_shelf` holds skills. There is no CEO-facing page where an agent lists what it can do. |
| **CAP-14-E** pipeline owned + scheduled | **PARTIAL** | The scheduler is a resident systemd service and U37's briefing runs at 07:00. Nothing on a CEO surface says *which agent owns which scheduled pipeline*. |
| **CAP-14-F** searchable memory graph | **HAVE the substance, MISSING the surface** | `std.knowledge_shelf` is live (R4.2: 168 grants, 423 reviews, 438 quality rows); the repo root is an Obsidian vault with `.planning/graphs/` and a graph-first reading rule binding every author. **The CEO has never seen any of it.** Their `954 · 17` is on screen; our numbers are in a table. |
| **CAP-14-G** memory as documents | **HAVE** | Persona and knowledge artefacts are markdown, file-first — the same choice, made earlier. |
| **CAP-14-H** health chip in plain words | **NO** | Nothing says "sistem çalışıyor" in the CEO's language on his screen. |
| **CAP-14-I** counters on the orchestrator | **NO** | Hamza has a name, a character and a persona §13 (U39) — and no numbers beside him. |
| **CAP-14-J** brief as a destination | **PARTIAL** | `v_morning_briefing` and `v_ceo_briefing` exist as views; there is no `CEO Brief` route. |
| **CAP-14-K** cross-agent learning | **PARTIAL** | The shelf exists and is granted per identity; that a run *writes back what it learned* is not established (same finding as source 13, CAP-13-E). |

---

## 5. The build project

This row does not add new projects so much as **prove that the ones already opened are the right
ones**. Where a project already exists from an earlier source, it is named rather than duplicated.

| # | Project | What is built | Owning spec (adaptation — no new spec) | Closes when |
|---|---|---|---|---|
| **P14-1** | **The command centre becomes one screen, not nine routes** | Overview carries: the live org (P10-1), the speak-to-Hamza control (their orb, our wake phrase), the agent log (P08-1's Conference Room feed), and the day's ranked items (P13-1). Today these are four separate destinations and the CEO has to know where to go. | `CEO_COMMAND_CENTER_SPEC` | One screen answers "what is happening, who is doing it, what needs me" without navigation, in both locales, passing the RULE #0 battery. |
| **P14-2** | **Every agent gets a page that lists what it can do** (CAP-14-D) | Persona + granted shelf entries + owned pipelines + recent runs, rendered per agent from data that already exists. With 199 personas this is the single biggest unclaimed asset in the holding. | `CEO_COMMAND_CENTER_SPEC` + persona standard | A real agent's page shows its real skills and its last real run. |
| **P14-3** | **The Knowledge Vault becomes visible** (CAP-14-F, -G) | A searchable view over `std.knowledge_shelf` and `.planning/graphs/` with an inspectable node, and a true count on screen. We already have more of this than they do; the CEO simply cannot see it. | `CEO_COMMAND_CENTER_SPEC` + `CAPABILITY_ARSENAL_DOCTRINE` | The CEO searches the shelf from his own screen and opens a real entry. |
| **P14-4** | **A health line in his language** (CAP-14-H) | One honest sentence on the overview: is the wake daemon listening, is the scheduler alive, are there failures today — computed, never decorative. Today `voice_daemon_state` reads `muted` and he has no way to know that without opening a panel. | `CEO_COMMAND_CENTER_SPEC` | The line reports a real fault when one exists, and says so plainly when all is well. |
| **P14-5** | **Fix the silent dispatch** *(already opened as P08-2 — restated because this row is the second independent source showing what it should look like)* | `chat-board.tsx:285-297` gets its failure branch. | `CEO_COMMAND_CENTER_SPEC` | A forced failure produces a visible message. |

---

## 6. Verdict

**`geride` on the surface — `daha iyisi` on everything underneath — and the gap is now measured
twice by two independent sources.**

- **Behind, and this is the row that makes it undeniable.** `attu.ai` ships, today, in a browser:
  a live org tree with five `● Active` agents, a "Speak with the CEO" orb on the main screen, an
  agent log of what was asked and what came back, a per-agent page of named skills, a scheduled
  pipeline with an owner, and a searchable knowledge graph with `954` entries on the chip. DXB
  has **more of every one of those things in its database** and shows the CEO **none of them**.
- **Ahead underneath, and by a distance.** They have 5 agents; we have 199 written personas
  across 22 departments. They have a Knowledge Vault; we have a governed shelf with grants,
  reviews and quality scores plus an Obsidian graph with a reading rule binding every author.
  They have a CEO agent that "makes the decisions"; we have an approvals gate that stops money,
  contracts, e-mail and ad spend at a human by law, enforced by an allowlist in code.
- **The conclusion this row forces, stated plainly for the CEO.** His complaint — *"bizimki bok
  gibi"* — is **not wrong about what he can see**, and is **not right about what exists**. The
  rival in these sixteen sources RUNS and EARNS while this one does not — the CEO knows several
  of them personally and reports exactly that. What the holding has instead is more unfinished
  structure and stricter rules, and rules are not results (`library_items` 434 rows against
  `library_usage_log` 0; `realized_revenue_eur` 0). Sources 10 and 14 independently
  show the same cure: **draw the company, name the speaker, log what was asked and answered, and
  put the shelf on screen** — all from rows we already have, with no new intelligence required.
