# Source 38 — `ericsocal`, "5 AI Tools That Will Make You a Millionaire in 2026"

> **WHAT THIS READING IS, AND WHAT IT IS NOT — his live order, 2026-08-19.**
> *"bunu izle SS ile. dinlemene gerek yok."* The film was opened **frame by frame, all 21 of the 21
> frames on disk**, and **its audio was never read** — the `.wav` was never transcribed to text for
> this report and no sentence below rests on anything spoken. This is **the mirror image of row 25**,
> which was listened to and never opened, and like that one it is **an exception granted to this one
> source, not a change to ledger law 4.**
>
> **The brief is an integration question, not a ranking** — *"Holdingte kullanabilir miyiz acaba diye?
> integraration research bölümünde. yani bunlar paralıysa zaten biraz zor önce sen bu programları
> araştırıp çalışmanı istiyorum."* So the weight of this report is **§3**: each of the five programs
> was opened at its own source and measured for **licence, price, free tier and whether it has an
> API** — because a program with no API is a tool a person uses, not a part an operating system can
> hold.

## WHY IT MATTERS TO THIS HOLDING

This source is read against **W-C42-4** — *"Dış dünya gelsin"*, the connector row, whose two hardest
cells are a **revenue aggregator** and **ads with ROAS per creative** — and against the holding's one
real storefront asset. Four measurements taken this session, before any judgement below:

| Our gap | Measured, 2026-08-19 |
|---|---|
| Storefront platform we actually own | **WooCommerce, not Shopify.** `outleteuro-ai-os/README.md`, in its own words: *"Project-scoped engineering workspace for Outleteuro **WooCommerce** rescue operations."* No file in `outleteuro-*` names Shopify at all (grep, 0 hits) |
| Store connectors in DXB code | **None.** `grep -ril "shopify\|woocommerce\|tiktok\|dropship" apps packages` returns 6 paths and **not one is a connector**: a revenue **entry form** the human types into (`revenue-entry-form.tsx`), its API route, the two locale files that carry its labels, an integrations **page**, and a build cache |
| Who is written to do this work | **63 employees** — `personas/commerce` 10 · `marketing` 33 · `social-media` 13 · `paid-media` 7, out of 200 written personas. `commerce-integration-engineer.md` and `marketing-cross-border-ecommerce.md` exist by name |
| These five in our tracker | **0 rows.** `grep -ci "pagepilot\|kalodata\|rytr\|sellthetrend"` on `INTEGRATION-TRACKER.md` = 0 before this session |

So the shape of the question is exact: **63 written commerce employees, one live WooCommerce store, and
zero wires between them.** The reel names five programs that each own one of those wires.

## 1. Source identity

| Field | Value |
|---|---|
| URL | https://www.instagram.com/reel/Da4dEQvlOCG/ |
| Account | **`ericsocal`** — profile name *"Eric | AI & E-commerce Skills"*, numeric id `53427588532` (measured with `yt-dlp --simulate`) |
| **Same author as row 37** | Yes — `37-dcat9zpd0cn.md` is the same account. This is the second `ericsocal` source on the queue, and like 37 it is **a list of tools, not a system** |
| Uploaded | **2026-07-17** |
| Duration | **20.944 s** |
| Resolution · codec | **1080×1920**, h264, 30/1 fps, 628 frames |
| Audio | **aac present on the file — deliberately not read**, on his order |
| sha256 | `f9133a13f3a015fbf7c5feb82c085d742566382be8456ca94b2b0032b3043a6e` |
| Frames on disk | **21** (`frames/38/t001…t021.jpg`, 1 fps, native, never downscaled — law 5) |
| Frames opened | **21 of 21** |
| Dense pass | `zoom/38/dense_001…020.jpg` — **10 fps over 4.0-6.0 s**, native, cut from the video (law 8's second clause) |
| Engagement | like count **4** at the minute it was probed; view and comment counts are not exposed to the prober |

**What the film is, in one line:** a phone held in the hand, filming a monitor, walking through **five
browser tabs** — five marketing pages, opened one after the other. The five products' own interiors
never appear; every screen in the film is a **public landing page**, which is exactly why §3 is
research at the source rather than a reading of the film.

## 2. The frame-by-frame record — 21 of 21 frames opened

No summarising. Each row is what that frame carries; where the pixels run out the row says **UNREADABLE**.

| t | Frame | What is on the screen |
|---|---|---|
| 00:00 | t001 | Man to camera in a room: white wall, light switch, black pegboard with white over-ear headphones on a hook, white/black **Secretlab** chair, black tee reading `TSUKIJI YONEMOTO · Since 1960`. Red caption block, white type, three lines: **`5 AI Tools That Will Make You a Millionaire in 2026`** |
| 00:01 | t002 | Same framing, speaking, caption unchanged |
| 00:02 | t003 | **Open hand raised, five fingers spread** — the count is given with the body, not with a graphic |
| 00:03 | t004 | Camera turns to the monitor. Deep-blue page, tilted ~12° in frame. A **blue marquee strip** across the top: `Try PagePilot.ai 100% FREE! No credit card required`. Headline, partly out of frame: `…antly Boost Your Sales / …ith High-Converting / …duct Pages in Seconds`. Sub-line `…y Test Multiple Products and Outpace Your Competition with …ePilot's AI. Save Time, Cut Costs, and Maximize Profits.` Button `Start Your Free Trial Now`. Bottom line `…Shopify Store? | Create one using our AI Store Builder in less than 10 minutes` |
| 00:04 | t005 | Steadier: rocket app-mark in a rounded blue tile + wordmark **`PAGEPILOT`**; **five orange stars** and `"Excellent"`; two Safari tabs legible — `…- The Best Tool…` and **`Create Shopify AI Produc…`** |
| 00:05 | t006 | Same page, closest reading of the sub-line: `Effortlessly Test Multiple Pro… / PagePilot's AI. Save Ti…` |
| 00:06 | t007 | Fast camera swing — motion-blurred. A pale-grey page with a centred line `What can I help with?` and a row of five coloured chips along the bottom. Individual chip labels **UNREADABLE** at this blur |
| 00:07 | t008 | Resolves: compose icon + **`ChatGPT ⌄`** at top left of an empty thread; tab **`Kalodata - The Best Tool…`** at left, a second tab `Create Shopif…`; input box `Ask anything` |
| 00:08 | t009 | Same screen held steady — the thread is **empty**; nothing is typed, no answer is requested. ChatGPT appears in the film as a name, not as a demonstration |
| 00:09 | t010 | White page: `…e **Best Tool** for …k Shop Analytics / Insights` (the words `Best Tool` in cyan). Counter row: **`200M+ Creator Data`** · **`300M+ Video & Livestream Data`** · `5(0…` cut off. Nav `Product · Video & Ad · Livestream`, `Log in` pill. Body line `…influencers collaboration, video inspiration, livestream design, advertising optimization, … We help you seize every opportunity in TikTok!` Button **`Start Free 7-day Trial`**, under it `No credit card needed`. Customer strip beginning `whitelab`, `Herborist`. **Five tabs are open now** — `…data.com`, `ChatGPT`, **`Dropshipping Software`**, **`Free AI Writer, Content G…`** |
| 00:10 | t011 | Kalodata nav across the top: `Kalodata · Explore · Category · Shop · Creator · Pro…`; headline `The … / TikTok …`; counters **`100M+ Product Data`**, `200M…` |
| 00:11 | t012 | Closest reading of the mark: the **`K` glyph in two blues** + wordmark `Kalodata`, `Explore` |
| 00:12 | t013 | Full-red page: `…our AI writing **assistant** fo… / …tent, accessible **(anywhere)**` — `assistant` underlined and `anywhere` circled in a hand-drawn stroke. Button **`Get Rytr It's Free`**. Below the fold: **`8,000,000+ happy copywriters, marketers & entrepreneurs`** and **`4.9/5`** |
| 00:13 | t014 | Wordmark **`Rytr`** top-left; headline `Rytr is your AI … / form content, ac…`; a three-stroke "spark" mark sits on the first word |
| 00:14 | t015 | Same, closer — the red ground fills the frame; tab `Kalodata - The Best Tool…` still first |
| 00:15 | t016 | Magenta page. URL bar reads **`landing.sellthetrend.com`**. Headline `…ropshipping Software`; four ticked lines, blurred but readable in the next frames; `…40,000+ merchants`; two five-star rows, one against a red mark and one against `App Store`; bottom `…ck guarantee` |
| 00:16 | t017 | Wordmark **`SellTheTrend`** with a rising-bars glyph. Headline **`#1 AI Dropshi…`**. Four green ticks: `Get your store built in minutes with …` · `Find and add winning products to yo…` · `Automate your Dropshipping Store a…` · `1-to-1 live chat support`. Dark pill **`Try for free`**, beside it `30 day mone…` |
| 00:17 | t018 | Same, closest — the four ticks fully legible |
| 00:18 | t019 | Back to the room. Caption has changed to **`Follow for More Videos on How to Put This Together`**; index finger raised |
| 00:19 | t020 | Same |
| 00:20 | t021 | Same, closing frame |

**Count check, written into the report as law 4 requires:** 21 frames on disk, **21 opened**, plus 20
dense frames at 10 fps over 4.0-6.0 s. The five programs, in the order the film gives them:
**PagePilot → ChatGPT → Kalodata → Rytr → Sell The Trend.**

### 2.2 Design and appearance

Measured from native pixels of the film's own rendering of those screens (a phone camera pointed at a
monitor — the values are the film's, and it is the *relationships* between them that survive a camera).

| Screen | Ground, sampled median-cut | The role the colour plays |
|---|---|---|
| PagePilot | `#26295D` 32% · `#0C0F4B` 25% · `#1A1A51` 22% | Near-black navy as the page ground, with the CTA strip a **pure electric blue `#0402F4`** — the only pure hue on the page, and it is spent on the offer |
| ChatGPT | `#4F606E` · `#6E8092` · `#607282` | No brand hue at all. The page is grey; the colour that exists sits in five small chips at the bottom edge |
| Kalodata | `#C5CCE5` · `#C0C6DD` · `#CED6ED` | White ground; the brand cyan appears **only on the two words `Best Tool`** inside the headline and on the trial button |
| Rytr | `#DC0001` 41% · `#D60002` · `#DA0001` | **The ground itself is the brand** — a single saturated red fills the viewport, type reversed out in white |
| Sell The Trend | `#CC00D1` 37% · `#BF00CE` · `#D600D3` | Same move in magenta: one saturated ground, white type, and the one **dark** object on the page is the `Try for free` pill |

**The pattern, and it is the part worth keeping:** three of the five (`Rytr`, `Sell The Trend`,
`PagePilot`) let **one saturated colour own the whole ground** and reverse the type out of it, so the
single dark or light object on the page *is* the call to action. Two of the five (`ChatGPT`,
`Kalodata`) do the opposite — a neutral ground where colour is rationed to two or three words. Both
are disciplined; what none of them does is spread a palette evenly.

**Type:** every one of the five sets its headline in a heavy geometric or grotesque sans at a size
several times the body, and every one of the five puts a **number** in the top third — `4.8/5`,
`200M+`, `4.9/5`, `8,000,000+`, `40,000+`, `#1`. **The figure is the argument.**

**What DXB takes into the design package (P38-4, below):** the one-ground rule for a page whose job is
a single decision, and the discipline of putting the *measured figure* where the eye lands first —
against our own surfaces, where a number is currently something the CEO must go and find.

### Aliveness (2.3) — the mechanism, and the honest limit of this film

**What runs on its own clock:** one thing is visible and it belongs to the browser, not to the
products — PagePilot's top strip is a **marquee**, and its text sits at a different horizontal
position in every frame of the dense pass, so the strip is running continuously without anyone
touching the page.

**Timed, against a static control in the same frames** — 20 frames at **10 fps** (100 ms apart) over
4.0-6.0 s, the marquee strip measured against the **browser tab bar** (screen content that cannot
move, filmed by the same camera at the same instant):

| Region | Mean change per 100 ms | Max |
|---|---|---|
| Marquee strip | **13.25** grey levels | 18.25 |
| Tab bar (static control) | **13.86** grey levels | 20.80 |
| Ratio | **0.96×** | — |

**Read honestly: the camera swamps it.** The hand holding the phone moves the whole frame by as much
per 100 ms as the marquee moves inside it, so **the marquee's own speed is UNVERIFIED from this file.**
What would be needed to settle it costs nothing and touches no account: open `pagepilot.ai` in the
Playwright MCP already pinned here (24 tools, granted to engineering) and time the strip against a
fixed viewport. That is a measurement note, not a conclusion about the rival.

**What the film chooses to show:** five **public landing pages**, in five saturated grounds, each
carrying a figure and one button. The five products' logged-in interiors are outside this film's
frame — that is what this 21-second advertisement is for, and it is a limit of the film rather than a
statement about the programs, four of which sit behind a login wall that this session deliberately did
not open (no account, no card — the Phase-1 discipline).

**How it answers the human:** the ChatGPT tab is opened at 00:07 and held **empty for two full seconds**
(t008, t009) — the name is shown, no prompt is typed and no answer is requested. In an
**e-commerce** list, the general assistant is the one entry with no vertical product behind it, and
the film treats it accordingly.

**What DXB takes from this source's mechanism:** not a screen — a **selection rule**. Every one of the
five is a **closed, per-seat SaaS subscription**, and four of the five are gated behind a login before
a price can even be read. An operating system that is meant to run itself cannot be assembled out of
parts a human has to log into: the buildable mechanism this source hands us is the **API-first
admission test** written as **P38-1** in §5, under which a tool enters this holding's tracker only with an answer
to *"can an agent drive it without a person at a browser?"* — and its first application, **P38-2**, the
one connector on this list that our own store can actually accept.

## 3. Capabilities — the five programs, researched at their own sources

**This is what he asked for**: *"önce bu programları araştırıp çalışmanı istiyorum"*, with the
condition that decides everything — *"bunlar paralıysa zaten biraz zor"*. Every figure below was
fetched this session; where a price could not be reached at the vendor's own page, the row says so and
names what would be needed.

### 3.1 PagePilot — `pagepilot.ai`

| | |
|---|---|
| What it is | An **AI product-page builder for Shopify**. Generates and publishes converting product pages, plus AI images, a template builder, 30+ languages, SEO fields |
| Licence | **Closed, proprietary SaaS.** No source, no self-host |
| Price (vendor's own pricing page) | **Lite $39/mo** (10 pages/mo, 50 AI images, 1 store) · **Starter $59/mo** (unlimited pages, 200 images, 3 stores) · **Scaler $99/mo** (unlimited, 5 stores). Yearly billing gives **"2 Free Months"** |
| Free tier | **A trial only** — *"Try PagePilot.ai 100% FREE! No credit card required"* is the film's own marquee, and it is a trial, not a free plan. Every plan on the pricing page is paid |
| API | **None published.** Its pricing page and landing page name no API and no integration other than Shopify |
| Platform | **Shopify only**, in its own words: *"Built for Shopify"*, *"Works with any Shopify theme"*. **WooCommerce is not supported** |
| **Fit for this holding** | **EXCLUDED.** Two independent stoppers, either alone sufficient: our storefront is **WooCommerce**, and there is **no API** for an agent to drive. Buying it would mean buying a Shopify store to put it on |

### 3.2 ChatGPT — `chatgpt.com` / OpenAI

| | |
|---|---|
| What it is | The general assistant. In this reel it is the copy/idea step between the other four |
| Licence | Closed, proprietary SaaS |
| Price (OpenAI's own pages) | **Go $8/mo** · **Plus $20/mo** · **Pro** (price not on the reachable page — see below) · **Business $25 per user/mo monthly, $20 per user/mo annual**; Business **Premium seats $125 per user/mo monthly, $100 annual** · **Enterprise** by sales contact. The pricing page renders its figures client-side and the fetch returned the six plan **names** but **no `$` string at all** (0 hits), so the seat prices above come from OpenAI's own help-centre articles rather than from the pricing page's rendered DOM |
| Free tier | Yes — a free ChatGPT plan exists |
| API | Yes, the OpenAI API — **separate product, separate billing** from these seats |
| **Fit for this holding** | **ALREADY ANSWERED, and the answer is no new purchase.** This holding does not buy per-seat chat subscriptions: model access runs through the **LiteLLM proxy** (EMBED, pinned `ghcr.io/berriai/litellm:main-stable`, virtual keys per department, hard-stop budget enforcement) with **OpenRouter primary**, and `STACK.md` forbids scattering raw provider keys into agent configs. If a model from this vendor is ever wanted, it is added **behind LiteLLM as a route**, never as a seat |

### 3.3 Kalodata — `kalodata.com`

| | |
|---|---|
| What it is | **TikTok Shop analytics** — product, creator, video and livestream data for finding what is selling. Operated by Kalowave Technology Co., Limited; **not affiliated with TikTok or ByteDance** |
| Licence | Closed, proprietary SaaS |
| Price | **Could not be measured at the vendor.** `kalodata.com/pricing`, `/en/pricing` and `/price` were all fetched this session with a stealth browser: each returns **HTTP 200 and renders the login/signup wall**, not a price table. **The price is behind an account.** Third-party trade sources of 2026 put **Starter ≈ $45.90/mo** (≈ $38.30 annualised) and **Professional ≈ $99.99 → $109.99/mo**, with **Enterprise negotiated** — recorded as secondary, uncorroborated at the vendor. What would settle it: creating a free account, which is a step this session did not take because it opens an outside account |
| Free tier | A **7-day free trial**, advertised on the film's own frame — `Start Free 7-day Trial · No credit card needed` |
| API | **Not published for the standard plans.** API access is described as part of the **Enterprise** tier, priced by sales contact |
| Live-vs-film delta, measured | The film (2026-07-17) shows `100M+ Product · 200M+ Creator · 300M+ Video & Livestream`. **The live site today reads `200M+ Product · 250M+ Creator · 400M+ Video & Livestream · 1000 Days Historical Data`** — the product's own counters have grown on every axis in the month since the film |
| **Fit for this holding** | **STUDY, and the interesting part is not the vendor.** The data it sells is TikTok Shop's, and **TikTok publishes its own Shop APIs** (`partner.tiktokshop.com`) plus a Research API for shop info. For a holding that must run itself, the buildable answer is the **official API behind our own gate**, not a per-seat analytics subscription whose price we cannot even read without an account. It also touches nothing we own today: DXB has **no TikTok Shop** |

### 3.4 Rytr — `rytr.me`

| | |
|---|---|
| What it is | An **AI writing assistant** — short-form copy, tone matching, plagiarism checks, 35+ languages. Its own landing page claims **8,000,000+** users and **4.9/5** |
| Licence | Closed, proprietary SaaS |
| Price (vendor's own pricing page) | **Free $0/mo** — 10K characters per month, 1 language, no tone matching · **Unlimited $7.50/mo** — unlimited generations, 1 tone match, 50 plagiarism checks, 1 language · **Premium $24.16/mo** — unlimited, multiple tone matching, 100 checks, 35+ languages. Annual billing gives 2 months free |
| Free tier | **A real free plan**, not just a trial: 10K characters/month, no card |
| **API — and this is the one difference that matters** | **Yes, published and documented**, `github.com/rytr-me/documentation`. Base URL `https://app.rytr.me/v1/`, **Bearer token** auth, key generated at Account → API. **Usage-priced, not per-seat:** `0-10k characters free` · `$0.75/10k` for 10k-1M · `$0.70/10k` for 1M-10M · `$0.65/10k` for 10M-100M · `$0.60/10k` above 100M. Billing note in their own docs: *"Your card will be billed for usage worth of every $25."* Rate limit reported at 60 req/min standard |
| **Fit for this holding** | **STUDY, and it is the only one of the five an agent could drive today — but it is still not recommended.** The reason is not price, it is duplication: this holding already routes every generation through **LiteLLM with per-department virtual keys and a hard budget stop**, which is exactly what `$0.75/10k characters` would sit outside of. Adding Rytr adds a **second, unmetered text-generation path** and a second provider key, against `STACK.md`'s own named failure mode. What is worth taking from it is not the endpoint — it is the **product shape**: tone-match as a first-class control, which our `humanizer` row (STUDY, Phase 10, 33 patterns, mandatory before the outbound approval gate) already owns |

### 3.5 Sell The Trend — `sellthetrend.com`

| | |
|---|---|
| What it is | **AI dropshipping software** — NEXUS AI product discovery, daily handpicked winners, Facebook and TikTok ad finders, 1-click product push to a store, automated order fulfilment, a bundled storefront ("SellShop"), live chat support. Its landing page claims **40,000+ merchants** |
| Licence | Closed, proprietary SaaS |
| Price (vendor's own pricing page) | **Monthly:** Lite **$29.97** (reg. $59.97) · Essential **$49.97** (reg. $99.97) · Pro **$99.97** (reg. $249.97). **Yearly (−33 %):** Lite **$19.97/mo** = $237.64/yr · Essential **$32.97/mo** = $395.64/yr · Pro **$66.64/mo** = $799.68/yr |
| Free tier | **Trial only** — *"100 percent free trial"*, plus a 30-day money-back line on the landing page. No free plan |
| API | **None published.** The integration is an app-level push into the store, not a developer API |
| Platform | **The only one of the four verticals that fits our store.** Its own integrations page names **Shopify, WooCommerce, eBay, TikTok Shop, Wix and Amazon**, all with *"Full Automation"* |
| **Fit for this holding** | **STUDY — conditional, and the condition is his, not mine.** It is the single program on this list whose platform list contains **WooCommerce**, which is what Outleteuro actually runs. But it is **money out and an outside account connected to a live store**, which is the approval gate by name, and it is a **dropshipping supplier-arbitrage business model** — a commercial direction, not a tool purchase. Nothing here is bought or connected on an author's judgement |

### 3.6 The five, in one table

| Program | Licence | Cheapest real cost | Free plan | **API for an agent** | Runs on **our** WooCommerce | Verdict |
|---|---|---|---|---|---|---|
| **PagePilot** | Closed SaaS | **$39/mo** | Trial only | **No** | **No — Shopify only** | **EXCLUDED** |
| **ChatGPT** | Closed SaaS | $0 free / $20 Plus / $25 seat | Yes | Yes (separate API product) | n/a | **No new purchase — LiteLLM already owns this seam** |
| **Kalodata** | Closed SaaS | **Price behind a login wall**; ≈$45.90/mo secondary | 7-day trial | **Enterprise tier only** | n/a (TikTok Shop) | **STUDY — prefer TikTok's own APIs** |
| **Rytr** | Closed SaaS | **$0 (10K chars/mo)**, $7.50/mo unlimited | **Yes** | **Yes — documented, usage-priced** | n/a | **STUDY — duplicates LiteLLM; take the tone-match idea, not the key** |
| **Sell The Trend** | Closed SaaS | **$19.97/mo yearly** | Trial only | No | **Yes** | **STUDY — CEO decision, business direction + money out** |

**The direct answer to his condition** — *"bunlar paralıysa zaten biraz zor"*: **four of the five are
paid with no usable free plan**, and the fifth is a chat seat this holding has already decided not to
buy. The total cost of running the reel's own stack as advertised is **$39 + $20 + ~$45.90 + $7.50 +
$29.97 = ~$142.37 per month, ≈ $1,708 per year** at the cheapest tiers that actually deliver what the
film shows — for **one** operator, on **one** Shopify store this holding does not own.

## 4. What DXB has today

Measured this session, not recalled:

- **The store is WooCommerce.** `outleteuro-ai-os/README.md` says so in its own first line, and no file under `outleteuro-*` mentions Shopify. Three of the five programs (PagePilot, and the Shopify half of the other two's value) are built for a platform we do not run.
- **There is no connector of any kind.** The six code paths matching `shopify|woocommerce|tiktok|dropship` in `apps` and `packages` are a manual **revenue entry form** (`apps/dashboard/src/components/command/revenue-entry-form.tsx`), its API route, two locale files, an integrations **page**, and a build cache. Revenue enters this holding **by a human typing it in**.
- **The workforce for this work is written and idle.** 63 personas across commerce (10), marketing (33), social-media (13) and paid-media (7), including `commerce-integration-engineer` and `marketing-cross-border-ecommerce`, out of 200 written personas.
- **The model seam is already solved and guarded.** LiteLLM is **EMBED** (pinned, virtual keys per department, hard budget stop, imported by the dxb-mcp cost group), OpenRouter is primary, and `STACK.md` names *"scattering raw provider API keys into agent configs"* as a failure mode to be prevented, not a shortcut.
- **The scraping seam is already solved.** **Scrapling** is **ADOPT** with its MCP bridge catalogued (10 tools pinned) and **Camoufox** live — this session used exactly that stack to reach Kalodata's wall. **Playwright MCP** is **ADOPT**, 24 tools pinned, granted to engineering. Whatever public commerce data is wanted, the holding can already go and take it.
- **The tracker had zero rows for any of these five** before this session.

**The gap this source actually exposes** is not a missing subscription. It is that **the one storefront
this holding owns and the 63 employees written to run it are joined by nothing at all** — the same
finding W-C42-4 already carries, now with a platform name attached to it.

## 5. The build project

Numbered as this queue numbers them. **None of these installs anything, none costs money, and none
touches an outside account.**

**P38-1 — The API-first admission test, written into the tracker's own rules.**
Every row in `INTEGRATION-TRACKER.md` gains an answer to one question before it may leave STUDY:
*can an agent drive this without a human at a browser?* A closed SaaS with no API is admissible only
as a **human tool for a named person**, never as a part of the operating system — and the row must say
which it is. This source is the reason: **four of its five programs cannot be driven by anything this
holding builds**, and until now the tracker had no column that would have caught it. Needs no install.

**P38-2 — One wire into the store we actually own.**
A read-only **WooCommerce connector** for Outleteuro under the existing gate: orders, products and
revenue read on a schedule into `v_objective_progress`, so **`realized_revenue_eur` stops being a
number a human types into a form**. This is W-C42-4's *revenue aggregator* cell, aimed at the platform
we have rather than the one the reel assumes. Read-only first; every outward action keeps the
approvals gate. Needs no purchase — WooCommerce's REST API is part of the store we already run.
**Blocked on the CEO for one thing only: whether the live Outleteuro store may be connected** (that is
W-C42-4's own CEO cell).

**P38-3 — The one-decision page.**
From §2.2, into the visual design package (blocked, like every design item, on his approval of the
package): where a DXB surface asks for **one** decision, one ground owns it and the measured figure
sits where the eye lands first. The five landing pages are the reference, not the aesthetic.

**P38-4 — Tone-match as a control, not a prompt.**
Rytr's product shape — *tone* as a first-class control the operator sets, rather than a sentence
buried in a prompt — folded into the existing **`humanizer`** row (STUDY, Phase 10, 33 patterns,
mandatory before the outbound approval gate). No new provider, no second key: the control is ours, the
generation stays behind LiteLLM.

**P38-5 — Read TikTok Shop from its own source, if it is ever wanted.**
Should the holding ever open a TikTok Shop seat, the data path is **TikTok's own published APIs**
(`partner.tiktokshop.com`, plus the Research API for shop info) behind our gate — not a per-seat
analytics subscription whose price cannot be read without an account. Recorded now so the decision is
not re-litigated later. Needs no install today.

**What is NOT taken, and why, so no later session re-opens it:** PagePilot (no API, wrong platform) ·
a ChatGPT seat (LiteLLM already owns the model seam; `STACK.md` forbids the second key path) · a Rytr
API key (a second, unmetered generation path outside the budget stop) · the dropshipping business model
itself, which is **his call and not an author's**.

## 6. Verdict

**What this source produces, measured:** it is a **21-second promotional list** by `ericsocal`, the
same account as row 37, with **4 likes** at the minute it was probed. It sells nothing itself — the
closing frame asks for a follow (*"Follow for More Videos on How to Put This Together"*), which is the
account's product. **The five programs it names, however, each run and earn:** Rytr's own page claims
**8,000,000+** users at **4.9/5**, Sell The Trend's claims **40,000+ merchants**, Kalodata's counters
grew from `100M/200M/300M` in the film to **`200M/250M/400M`** on the live site in one month, and
PagePilot sells at **$39-$99/month** against a 4.8/5 rating. These are live businesses with paying
customers. **DXB has not run end to end and enters revenue by hand**; on the measure that decides, they
are ahead.

**But the reel's answer is not this holding's answer, and the difference is structural.** Those five
products are sold to **one operator with one Shopify store and a browser**. This holding is an
operating system that must run with **one human in it who approves and does not operate**. Four of the
five cannot be driven by an agent at all, and the fifth duplicates a seam that is already built, metered
and budget-stopped here. Buying the reel's stack would cost **~$142/month for one seat on a platform we
do not own**, and would hand the holding five more logins for a person to sit behind.

**So the answer to his question — *"Holdingte kullanabilir miyiz acaba?"* — is: one of the five, and
only as a business decision that is his.**

- **PagePilot — no.** Shopify-only, no API. Two stoppers.
- **ChatGPT — nothing to buy.** The seam is LiteLLM's, and it is already EMBED.
- **Kalodata — no, take the source instead.** Its price is behind a login wall and its API is Enterprise-only; TikTok publishes the data itself.
- **Rytr — no key, take the idea.** The only agent-drivable one on the list, and the one that most clearly duplicates what we already run.
- **Sell The Trend — the only real candidate, and it is a decision, not an install.** It is the one program whose platform list contains **WooCommerce**, which is what Outleteuro runs. It costs **$19.97/mo yearly at the floor**, it needs an outside account connected to a live store, and it carries a **business model** — dropshipping — with it. That is the approval gate, and it is his.

**The finding worth more than any of the five:** this reel is what a commerce stack looks like when it
is assembled out of subscriptions, and reading it measured the thing it was read against — **the one
storefront this holding owns is joined to its 63 written commerce employees by nothing**, and revenue
reaches the CEO's screen because a person typed it. **P38-2 closes that with the store's own API, at
no cost and with no new vendor**, and **P38-1** makes sure the next list of five is filtered before it
reaches a tracker row rather than after.
