---
phase: quick-260706-h26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .obsidian/app.json
  - .obsidian/appearance.json
  - .obsidian/core-plugins.json
  - .obsidian/graph.json
  - .gitignore
  - .planning/graphs/
  - .claude/CLAUDE.md
autonomous: true
requirements: [QUICK-H26-second-brain]
must_haves:
  truths:
    - "Repo root is a valid Obsidian vault: .obsidian/ sits at repo root and the nested 'DXB GLOBAL OS/' folder no longer exists"
    - "git status is clean of the 26 research-cache JSON files and the accidental vault folder"
    - "The existing SEC-02 vault/.env deny rules in .gitignore remain byte-intact and correctly ordered"
    - "A knowledge graph exists under .planning/graphs/ OR the build failure is documented in SUMMARY (graph build never blocks the other tasks)"
    - "CLAUDE.md instructs every agent to consult .planning/graphs/ and .planning/ docs before broad repo reading"
  artifacts:
    - .obsidian/app.json
    - .obsidian/appearance.json
    - .obsidian/core-plugins.json
    - .obsidian/graph.json
    - .gitignore
    - .planning/graphs/
    - .claude/CLAUDE.md
  key_links:
    - "The .gitignore edit MUST be append-only — the SEC-02 vault deny block is the critical link; any regression re-exposes secrets"
    - ".obsidian/workspace.json + workspace-mobile.json must be ignored while app/appearance/core-plugins/graph.json stay tracked"
---

<objective>
Turn the repo root into a proper Obsidian second-brain vault, clean up git hygiene, build the project knowledge graph, and codify agent session-efficiency rules — without touching the mid-execution security phase.

Purpose: Give the OS a persistent, human-navigable knowledge substrate (Obsidian vault + knowledge graph) and teach all agents to read the graph/`.planning` docs before scanning the repo, cutting token waste. CEO-approved scope; decisions are locked — do not re-litigate.

Output: repo-root `.obsidian/` vault, hardened `.gitignore`, `.planning/graphs/` knowledge graph (best-effort), and a "Knowledge Graph & Session Efficiency" section in `.claude/CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.gitignore

# Facts already verified during planning (do not re-investigate):
# - "DXB GLOBAL OS/" folder is UNTRACKED in git (git status shows "?? DXB GLOBAL OS/") → plain `mv` is a clean move, no `git mv` / no cached removal needed.
# - "DXB GLOBAL OS/.obsidian/" holds 5 files: app.json, appearance.json, core-plugins.json, graph.json (shareable) + workspace.json (personal). Plus disposable Welcome.md (203 bytes) in the folder.
# - No .obsidian/ exists at repo root yet → `mv` will not nest.
# - .gitignore currently ends at line 22 (`!.env.example`) with the SEC-02 vault deny block above it — APPEND ONLY below it.
# - The 26 files under .planning/research/.cache/ are UNTRACKED → gitignoring removes them from status with no `git rm --cached`.
# - .planning/graphs/ does not exist yet.
</context>

<constraints>
DO NOT modify, reorder, or rewrite any existing .gitignore line — append only, at EOF.
DO NOT touch .planning/phases/, .env.example, .claude/settings.json, .gitleaks.toml, or any Phase 01 evidence files (security phase is mid-execution).
QUOTE every shell path that contains the space: "DXB GLOBAL OS".
Commit atomically per task (three separate commits).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Repurpose the accidental Obsidian vault to repo root + gitignore hygiene</name>
  <files>.obsidian/app.json, .obsidian/appearance.json, .obsidian/core-plugins.json, .obsidian/graph.json, .gitignore</files>
  <action>
Move the accidental vault config up to repo root and delete the leftover folder: run `mv "DXB GLOBAL OS/.obsidian" .obsidian` then `rm -rf "DXB GLOBAL OS"` (both paths quoted for the space; the folder and its disposable Welcome.md are untracked, so this is a clean filesystem move — no git mv). Repo root itself is now the Obsidian vault.

Then harden .gitignore by APPENDING a single new block at end-of-file, BELOW the existing `!.env.example` line. Do not alter, reorder, or delete any existing line (the SEC-02 vault deny block above must stay byte-identical). The appended block adds an Obsidian comment header followed by two ignore lines for personal/volatile UI state — `.obsidian/workspace.json` and `.obsidian/workspace-mobile.json` — and then a research-cache comment header followed by one line, `.planning/research/.cache/`. Leave the shareable configs (app.json, appearance.json, core-plugins.json, graph.json) un-ignored so they stay tracked.

Stage the four shareable Obsidian configs and the .gitignore change: `git add .obsidian/app.json .obsidian/appearance.json .obsidian/core-plugins.json .obsidian/graph.json .gitignore`. Confirm workspace.json was NOT staged (it is now ignored). Commit atomically: `chore(quick-h26): repurpose Obsidian vault to repo root + gitignore hygiene`.
  </action>
  <verify>
    <automated>cd "/home/ghost/DxB Global OS" && test -f .obsidian/app.json && test ! -d "DXB GLOBAL OS" && git check-ignore -q .obsidian/workspace.json && git check-ignore -q .obsidian/workspace-mobile.json && git check-ignore -q .planning/research/.cache/probe.json && grep -qx '!.env.example' .gitignore && grep -qx 'agency-agents/' .gitignore && test "$(git status --short | grep -c '.planning/research/.cache/')" = "0" && test "$(git ls-files .obsidian/ | grep -c 'workspace.json')" = "0" && echo PASS</automated>
  </verify>
  <done>.obsidian/ exists at repo root with the 4 shareable configs tracked; workspace.json/workspace-mobile.json and .planning/research/.cache/ are git-ignored; the nested "DXB GLOBAL OS/" folder is gone; the SEC-02 deny lines (`!.env.example`, `agency-agents/`) are still present and unchanged; commit made.</done>
</task>

<task type="auto">
  <name>Task 2: Build the project knowledge graph (best-effort, non-blocking)</name>
  <files>.planning/graphs/</files>
  <action>
Invoke the knowledge-graph builder via the Skill tool: skill `gsd-graphify`, args `build`. This should produce graph artifacts under `.planning/graphs/`.

If the skill succeeds, stage and commit whatever it wrote: `git add .planning/graphs/` then `chore(quick-h26): build project knowledge graph`.

If the skill is unavailable, errors, or produces no output, DO NOT block: record a short note in the SUMMARY under a "Knowledge graph build" heading (what was attempted, the exact failure/return message, and that the remaining tasks proceeded) and continue to Task 3. Per the locked scope, graph build must never block the rest of the plan.
  </action>
  <verify>
    <automated>cd "/home/ghost/DxB Global OS" && { { test -d .planning/graphs && test -n "$(ls -A .planning/graphs 2>/dev/null)"; } || grep -qi 'knowledge graph build' .planning/quick/260706-h26-set-up-second-brain-infrastructure-obsid/*SUMMARY*.md 2>/dev/null; } && echo PASS</automated>
  </verify>
  <done>Either .planning/graphs/ exists and is non-empty (committed), OR the SUMMARY documents the graph-build failure and execution continued. No task was blocked by this step.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Knowledge Graph & Session Efficiency" rules to project CLAUDE.md</name>
  <files>.claude/CLAUDE.md</files>
  <action>
Insert a new top-level section titled "Knowledge Graph & Session Efficiency" into `.claude/CLAUDE.md`. Place it OUTSIDE any GSD-managed comment block: insert it immediately after the `<!-- GSD:skills-end -->` line and before the `<!-- GSD:workflow-start source:GSD defaults -->` line (do not move or edit those marker comments). Use Edit with a scoped, unique anchor — do not Write the whole file.

The section states four rules for all agents and subagents: (a) before any broad repo reading, query `.planning/graphs/` and the `.planning/` docs (STATE.md, phase SUMMARYs) first; (b) the graph is refreshed at phase completion by running `/gsd-graphify build` after each phase's verify step; (c) the repo root is an Obsidian vault, so human-facing docs may use `[[wiki-links]]`; (d) stale-graph rule — if graph metadata is older than the current HEAD by more than one phase, rebuild before trusting it. Keep it concise (a short intro line plus a four-item list).

Commit atomically: `docs(quick-h26): add knowledge graph + session efficiency rules to CLAUDE.md`.
  </action>
  <verify>
    <automated>cd "/home/ghost/DxB Global OS" && grep -q 'Knowledge Graph & Session Efficiency' .claude/CLAUDE.md && grep -q '\[\[wiki-links\]\]' .claude/CLAUDE.md && grep -q 'gsd-graphify build' .claude/CLAUDE.md && grep -q '<!-- GSD:workflow-start' .claude/CLAUDE.md && grep -q '<!-- GSD:skills-end -->' .claude/CLAUDE.md && echo PASS</automated>
  </verify>
  <done>CLAUDE.md contains a "Knowledge Graph & Session Efficiency" section with all four rules (graph-first reads, phase-completion refresh, Obsidian wiki-links, stale-graph rebuild); the GSD marker comments are intact; commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | .gitignore governs what secret-adjacent content can ever be committed; editing it during a mid-execution security phase is the sensitive operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h26-01 | Tampering | .gitignore SEC-02 vault deny block | high | mitigate | Append-only edit at EOF; Task 1 verify asserts `!.env.example` and `agency-agents/` still present via `grep -qx` before the commit is accepted |
| T-h26-02 | Information Disclosure | .planning/research/.cache/*.json (fetch cache) | medium | mitigate | Add `.planning/research/.cache/` to .gitignore so transient fetch payloads never enter history; files are currently untracked so no prior leak to purge |
| T-h26-03 | Information Disclosure | .obsidian/workspace.json (local UI state) | low | mitigate | Ignore workspace.json / workspace-mobile.json; keep only sanitized shareable configs tracked |
| T-h26-04 | Tampering | Phase 01 security artifacts (.env.example, settings.json, phases/) | high | mitigate | Hard constraint: no task reads or writes any Phase 01 evidence path; scope is limited to root vault, .gitignore append, graphs/, CLAUDE.md |
</threat_model>

<verification>
- `git check-ignore` confirms workspace.json, workspace-mobile.json, and .planning/research/.cache/ are ignored.
- Existing SEC-02 deny lines (`!.env.example`, `agency-agents/`, `.env`, `.env.*`) remain present and unchanged.
- `git status --short` no longer lists the research-cache files or the "DXB GLOBAL OS/" folder.
- .obsidian/ resides at repo root; the four shareable configs are tracked, workspace.json is not.
- .planning/graphs/ is non-empty OR SUMMARY documents the build failure.
- CLAUDE.md carries the new efficiency section with GSD marker comments intact.
- Three atomic commits exist (vault+gitignore, graph, CLAUDE.md) — graph commit optional if the build failed.
</verification>

<success_criteria>
Repo root is a functioning Obsidian vault with clean git hygiene, the research-cache noise is gone from `git status`, the knowledge graph is built (or its failure is documented), and every agent now has a written rule to consult the graph and `.planning` docs before broad repo reads — all achieved without touching the in-flight security phase.
</success_criteria>

<output>
Create `.planning/quick/260706-h26-set-up-second-brain-infrastructure-obsid/260706-h26-SUMMARY.md` when done.
</output>
