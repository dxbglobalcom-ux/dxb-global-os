# SUMMARY — C5 trigger leg + workforce truth + suite-residue sweep (2026-07-24 late night)

**Trigger:** CEO screenshots — (1) overview pulse "Awaiting approval 6" vs Approval Center "Pending 0"; (2) Employees "205 / Active 0 / Dormant 205 / Active departments 0" vs overview 199/198/1.

| Item | What was done | Evidence |
|---|---|---|
| C5 trigger leg | `decide_approvals` now moves the linked task out of `awaiting_approval` (approved→done, rejected→returned; a task another lane already moved is untouched). Migration `20260724006000`. | TDD red→green `tests/c5/decide-approvals-task-sync.test.ts` (red: 2 failed "expected returned, got awaiting_approval"; green 3/3). LIVE proof: rejecting 3 fixture gates moved all 3 tasks to `returned`. |
| Orphan repair | The 6 stuck orch-qa tasks (approvals decided 2026-07-19 per audit, then purged) repaired by replaying their audited decisions (2 done, 4 returned; audit `tasks.repair`), then purged via `control_records_purge`. | `awaiting=0`, `pending=0` measured after; audit rows on record. |
| Root cause #2 (why orphans existed) | `tests/phase5/council-judge.test.ts` afterAll never deleted the bridge-created approvals → task delete died on FK → residue stayed silently (2026-07-19 AND tonight, same mechanism). Sweep now clears outbox/approvals before tasks. | Suite re-run green 6/6, water-bottle rows after run = 0. |
| Workforce truth | `/org/employees` + `/org/directors` re-keyed from dead `agents.status` (measured: all 205 dormant) to `employment_status`; archived off every figure (C8); active departments = departments with active employees (`departments.status` also measured all-dormant). | Battery 8/8 (EN+TR × 1366/1920): scrollOK, 0 ellipsis, 0 clipped, KPIs 199/198/1/21 present, stale 205 absent; screenshots eyeballed. |
| Suite-residue sweep | Tonight's full-suite run against the live DB leaked: 57 run-less hook_violations + 173 machine decision rows + 3 fixture hook alerts + 1 zombie run + 16 probe tasks — swept via audited doors/repairs; 3 stale zombie-run alerts (2026-07-19 class) resolved with close-out note. e9 sweep fixed (worker task_events FK). | Post-sweep: unresolved alerts 0, hv-tonight 0, running runs 0, probe tasks 0. Audit `records.purge`/`tasks.repair` rows. One audit row initially overclaimed (3 vs 0) — corrected in place with a `correction` field. |
| C9 rollout measurement (CEO order: measure first) | FilterBar exists on exactly 2 pages (fin/costs, fin/tokens). Missing on 11 DataGrid pages (ai/memory, fin/pnl, fin/providers, gov/permissions, gov/policies, gov/risks, ops/automations, org/departments, org/directors, org/employees, org/hr) + non-DataGrid lists (approvals, alerts, gov/decisions, ai/library, ops/workflows, live). Machine Room routes exempt (A4). | grep inventory 2026-07-24; rollout = next queued work. |

**Boundary:** other live-DB suites (e8 log-decision, e10 escalation class) still write terminal probe rows visible on the ticker during/after a full-suite run — post-run sweep required until their afterAll hygiene lands (recorded in ledger audit table).
