# WORK ORDER — the remaining studio legs: B08 step (0), W9, then the handoff's W-order (2026-09-15, 13:30)

**From the CEO, relayed by the Fable 5.1 checker session (dxb-global-os-2a).** His words: *"yeni session'da kalan
işlerden devam edelim. Sen talimatları ver. İşi bitsin. Sonra sen denetlemeye devam et."*
You are a NEW Opus 5 session, the BUILDER (author opus-5, U30). Branch `studio/b43-ferrari-implementation-20260915`,
HEAD `38381faa`, tree clean, 18 commits on the branch. The checker re-measures every commit with its own commands
and reports to him; you report to him too, in his shape.

**Delivery of messages:** cross-session messages DO arrive — but session names rotate (measured 2026-09-15: the
builder was `dxb-global-os-51`, ten minutes later `dxb-global-os-e7`); a send to a stale name silently goes nowhere.
Run `ListAgents` immediately before every `SendMessage` and use the current name with its `[ref]`. The delivery test
of 13:23 ("RECEIVED-2A") proved the channel. Still: every hash you produce goes into your reply to the CEO as well.

## The method you work under (his rulings of today, registered)

- **The ruler rule** (`dxb-verify` door, `<!-- CEO-OK: audit-ruler-is-a-runnable-script-2026-09-15 -->`): every
  audit's ruler is a runnable script, handed to the builder before the work; the order is that script's PASS. For
  code the ruler IS the battery; for persona files it is `scripts/persona-ruler.sh`; for records it is
  `verify:ledger` + records parity. Paste each ruler's decisive line into the evidence.
- Measure before you state; command → decisive line for every claim; a count you did not run is a lead.
- One session, one author: you write every line inline; subagents may audit read-only, never write.
- No `cd` in a compound Bash command (the prompt gate blocks it — absolute paths, `git -C`); no recursive scan of
  `/` or `$HOME` (the cost gate blocks it).
- LAW B: nothing is accepted until his eye; every approval claim carries a `<!-- CEO-OK: id -->` naming an entry in
  `scripts/governance/ceo-approvals.json` with HIS words. Nothing becomes a law or standing rule without his
  explicit word.
- A fix names its blast radius before the change and re-measures the dependants after (his order 2026-08-17).
- Two sessions share one tree: commit only the paths you touched (`git add <paths>`, never `git add -A`).

## Your FIRST reply to him (before any code) — the understanding report, four lines

1. What waits for his eye: nothing from W5–W6c (accepted 13:0x, `studio-seats-w5-to-w6c-accepted-2026-09-15`).
2. What is next: Part A needs his one sentence; Part B needs his "onay" on the W9 plan; then Part C.
3. What is blocked on him: exactly those two words. Quote them so he can copy them.
4. Then wait for his answer. Build nothing until he answers. If he answers only one, do that one.

## PART A — B08 step (0): the activation gate aligned with U30, then the bind (only on his sentence)

His sentence, verbatim, must appear in his message: **"kapıyı U30'a hizala, sonra bağla"** (or an unmistakable
equivalent — if in doubt, ask him in one line). Register it in `ceo-approvals.json` (id
`activation-gate-aligned-with-u30-2026-09-15`, his words verbatim) in the same commit.

Measured facts (checker, today): `enforce_persona_gate_on_activation()` md5 `b21b62c7a5f74a6163dd8eab96f3d592`
accepts `fable-5 | hr-factory` only (line 13); it fires on `UPDATE OF persona_id` and on activation; all 16 studio
seats are bound to their older `fable-5` versions while their newest `passed` versions are `opus-5` (e.g.
creative-director bound v16, newest passed v21; storyboard v11 → v18; identity v11 → v17). U30 (his ruling of
2026-07-26) made Opus 5 and Fable 5 equally authorized authors; `personas_author_check` already accepts
`opus-5 | fable-5 | hr-factory`.

Build, in this order:
1. **Blast radius named first** in the evidence: the trigger function, every code path that binds a persona
   (`grep -rn "persona_id" scripts src apps --include=*.ts --include=*.sh` — print the hits), the HR activation
   chain, Hamza's record (B08 says "then re-bind Hamza's persona record v2 → v6 — his file = v6, VERIFY PASS"),
   the tests that assert the gate (`grep -rln "activation denied\|enforce_persona_gate" tests` — print).
2. **One migration**, `db/migrations/<next>_activation_gate_author_list_u30.sql`, that replaces the author check
   in `enforce_persona_gate_on_activation()` with the SAME list `personas_author_check` uses
   (`opus-5 | fable-5 | hr-factory`) — nothing else changes in the function; the error text keeps its shape.
   Rollback SQL beside it (the previous function body, verbatim from `pg_get_functiondef` taken BEFORE the change,
   pasted into the evidence).
3. **Both engines**: the construction engine first (`tests/construction-engine.ts`, port 54422) through the
   canonical chain (`scripts/bootstrap-db.sh`), the tests that touch the gate green; then the company engine
   (`127.0.0.1:54322`) through the same chain. Print `pg_get_functiondef` on both after the change.
4. **The bind**: for each of the 16 seats, `agents.persona_id` → the newest `quality_gate='passed'` version
   through the door the HR flow uses (find it: `grep -rn "persona_id" scripts/sync-personas-to-db.sh scripts/*.sh`;
   if the flow binds by a `fn_`/`control_` door, use it; a raw UPDATE only if no door exists, and say so). Then
   Hamza v2 → v6 if his file is v6 and `--verify` says PASS for it (measure; if not, leave Hamza and report).
5. **Re-measure the dependants**: `sync --verify` 213/0 PASS; `SELECT slug, bound version, newest passed` →
   16/16 equal; `tests/r31/persona-delivery.test.ts` 4/4; `tests/b43/road-consistency.test.ts` 16/16;
   `scripts/persona-ruler.sh` 16/16; the scheduler and the worker still start (`systemctl --user status` of the
   `dxb-*` units, no restart unless in-flight = 0 and a restart is needed — say which); `pnpm typecheck`;
   `pnpm exec vitest run tests/b43 tests/r31 tests/b39`; `verify:ledger`; i18n; gitleaks.
6. Records: DATA_MODEL / the spec that owns the gate gets the adaptation registered where U30 lives; B08's
   step (0) marked built on the board with the evidence; STATE live block; `EVIDENCE-B08-step0-2026-09-15.md`.
   Commit: `db(B08 step 0): …`.

## PART B — W9: the two assigned seats can take a studio task (only on his "onay")

The plan he saw (EVIDENCE-W4b, § "W9 — the assigned seats, the plan put to him") is the contract; do not widen it:
1. **The assignment becomes a record**: `agent_assignments` (seat, studio department, seat title, since
   2026-09-03, the ledger id) — one migration on both engines, registered in DATA_MODEL as an adaptation.
2. **The drawer grant per employee**: `mcp/dxb-mcp/media` granted `grantee_kind='employee'` to
   `design-image-prompt-engineer` and `marketing-short-video-editing-coach`; the profile compiler emits a
   per-employee profile = the home department's kit + the media group; `agents.mcp_profile` of the two rows
   points at it; the two home profiles (`design.mcp.json`, `marketing.mcp.json`) unchanged for everyone else.
3. **The dispatcher accepts an assigned seat**: `queue_create_task` and `queue_dispatch` (`queue.ts:87`, `:213`)
   accept a seat whose department differs from the author's when an assignment row binds it to the author's
   department; a stranger stays refused. The existing refusal test keeps passing; two new tests: a sheet naming
   the Editor for a studio task is accepted; a sheet naming a marketing seat with no assignment is refused.
4. **Records**: A19 registered adaptation (one task per named seat of the author's department OR a seat assigned
   to it), CAPABILITY_ARSENAL §9 media row, B43's BUILT block and F033 closed on the board with the evidence,
   STATE. Blast radius as W4b names it: `queue.ts` both doors, the profile compiler and the two profile files,
   `agents.mcp_profile` for two rows, the worker's staffing (unchanged), A19. Rollback written in the evidence.
5. **Proof through the road**, not a unit test alone: one studio sheet that names the Editor, dispatched on the
   construction engine, the task row printed with the seat's slug; then the same on the company engine ONLY if
   he says so (it creates a real task) — otherwise the company engine is SELECT-only in this part.
6. The battery (§ "The method" above) + `verify:ledger` + i18n + gitleaks + tree clean. Register his "onay" in
   `ceo-approvals.json` (id `w9-assigned-seats-plan-approved-2026-09-15`, his words). Commit: `studio(W9): …`.

## PART C — the handoff's order, one W at a time, each put to him first

`STUDIO_AUDIT_HANDOFF.md` fixes the order W5 → W6 → W7–W10 (each with his word) → W11–W12 → W13 → W14. W5, W6
(and W5b, W6b, W6c) are done and accepted. Read the handoff's definition of each remaining W (`grep -n "^## W\|^### W"`),
and for EACH: a four-line understanding report to him (what it is · what it touches · what it costs · what it
needs from him), his word, then build with the same discipline, the same battery, its own evidence file and its
own commit. Never two W's in one commit. Stop and give him the four-line position whenever he closes the session.

## What you do NOT do

- No persona text changes (the ruler is green; the seats are accepted — do not reopen them).
- No new spec or plan file; adaptations are registered inside the spec that owns the contract.
- No money out, no outward message, no identity step — the CEO gate.
- No merge to master (it stays at `a6fdfdde` until he says merge).
- No standing rule written anywhere without his explicit word.

## Report shape (every reply)

Turkish, `dxb-ceo-report`: the position in four lines first (his eye · next · blocked on him · answer), then the
answer, a picture from his world, the numbers in one table, what it means for him. `⚠ UNVERIFIED` for anything a
terminal cannot observe, listed apart. Never "ne yapmamı istersiniz". Each commit hash in the reply.

---

## FOR THE CHECKER SESSION (Fable 5.1 or whichever brain he opens as checker) — read this, then act

You are the CHECKER, not the builder. You write no repo line except records (evidence, STATE, the board) and
the ruler scripts you hand over. The builder is the Opus 5 session in the other pane. His rule of 2026-09-15:
the checker's ruler is a runnable script, handed before the work; builder and checker run the same script.

**Your loop, per commit the builder reports (he relays the hash by hand):**
1. `git -C "/home/dxb/DxB Global OS" log --oneline -3` and `git status --short | wc -l` — the hash exists, tree clean.
2. Run the ruler for that part yourself and paste its output into your reply to him:
   `bash "/home/dxb/DxB Global OS/.planning/quick/20260903-media-studio-founding/CHECK-W9-B08-2026-09-15.sh" A` (or `B`, or `battery`).
   Do not trust the builder's paste; run it. Same script, your hands.
3. Read the diff of the commit against the order (`git show --stat`, then the files that matter): what the
   order asked and the diff does not contain is "left out" — name it. Scope gaps are the checker's second job;
   the ruler cannot see them.
4. Blast radius: the order names the dependants per part; check the builder re-measured them (evidence file)
   and re-measure the ones that cost the most yourself (bindings, gate md5, migrations count, sync --verify).
5. Report to the CEO in Turkish, `dxb-ceo-report` shape: answer first (true / overstated / false, per claim), a
   picture from his world, one table of measured numbers, what it means for him. Anything a terminal cannot
   observe: ⚠ UNVERIFIED, listed apart. Never "ne yapmamı istersiniz".
6. If a claim is overstated, tell the builder in ONE line (finding + the exact line/command) — through the CEO
   or by typing into the builder's box; and when a further W needs a ruler, write it BEFORE the work, as a
   script beside this one, red on today's tree by design.

**Lessons this checker paid for today (do not repay them):**
- Cross-session messages: session NAMES ROTATE (the builder was `dxb-global-os-51`, ten minutes later
  `dxb-global-os-e7`). Run `ListAgents` immediately before every `SendMessage` and use the current name with
  its `[ref]`; otherwise the send silently goes nowhere. A session that has not yet had its first turn processes
  nothing — its first message must be typed (operator: click the box, shot, verify focus, paste, Return, shot).
- "Sentence" must be defined in the ruler (period-based); a builder given prose thresholds will split lines
  instead of sentences and report 118 → 0 in good faith.
- Counting by grep is a lead; the finding is the line read in context. Two W5 counts were overstated that way.
- The evidence-file paste that matched the dossier cut 1796 words from a seat; every replacement is guarded to
  the body (`# PERSONA —` … `## 12.`).
- No `cd` in a compound Bash command; no recursive scan of `/` or `$HOME` (the gates block both).
- The ruler is red on today's tree by design; a ruler that passes before the work is a wrong ruler.

**Where things stand at the checker's close (2026-09-15, 13:30):** W5 → W6c accepted by his eye
(`studio-seats-w5-to-w6c-accepted-2026-09-15`); the persona ruler is in the battery and in the sync gate; the
ruler rule and its holding-wide table are in the `dxb-verify` door with his markers; the builder waits for his two
words (`kapıyı U30'a hizala, sonra bağla` · `onay`); master stays at `a6fdfdde` until he says merge.
