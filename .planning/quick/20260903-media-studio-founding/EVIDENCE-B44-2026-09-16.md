# EVIDENCE — board row B44, 2026-09-16

Tests that pass alone and time out inside the whole suite.
Ruler: `CHECK-B44-2026-09-16.sh`, md5 **f1d3cb39463a55881f16cdb8a4296541**, dictated by the checker
session `dxb-global-os-c7` (Fable 5.1) and committed ALONE as `61d80dd8` before the first line of
work (the audit law, `c7570071`).
His word that opened the row, 12:48:15 local: *"tamam düzelt o zaman lakin para zaten benim iznim
olmadan asla harcanamaz"* — registered as `b44-fix-ordered-money-never-without-permission-2026-09-16`.

---

## 1. THE MEASUREMENT IS EXCLUSIVE, AND THE EARLIER ONE WAS NOT

The checker measured, 12:51–12:55, that its own whole-suite run overlapped this session's on the ONE
construction engine, and that the overlap produced a red never seen before (`tests/e125/workforce-gate.test.ts`,
7/7 alone). **Every run quoted below had the machine to itself** — the ruler's `run` mode refuses to
start while another `vitest` process is alive, and the hunt scripts check the same thing before each run.
The morning's overlapped runs are marked CONFOUNDED and kept, not deleted.

Exclusive whole-suite runs, 2026-09-16 12:56 → 13:27, nine in all:

| run | clock | result | velocity |
|---|---|---|---|
| A | 12:56:39–12:58:38 | 6 failed / 123 passed (129) · timeouts 0 | GREEN 6154 ms |
| B | 12:59:00–13:01:00 | 7 failed / 122 passed (129) · timeouts 2 | **RED 30082 ms** |
| C | 13:12:30–13:14:31 | 6 failed / 123 passed (129) · timeouts 0 | GREEN |
| D | 13:14:50–13:16:54 | 7 failed / 122 passed (129) · timeouts 2 | **RED 30085 ms** |
| E–I | 13:17:19–13:27:14 | 6 failed / 123 passed (129) · timeouts 0 (×5) | GREEN ×5 |

**The rotation is real with the machine to itself: 2 red in 9 exclusive runs.**

## 2. MEASURED CAUSE: the case's assertions cost 132 ms; the 30 s is its TEARDOWN

`tests/phase4/velocity.test.ts` was instrumented UNCOMMITTED with a `[B44-PROBE]` mark around each
phase, the whole suite was run exclusively until it went red, and the probe was reverted before any
commit (`grep -c B44-PROBE tests/phase4/velocity.test.ts` = 0 in the committed tree).

GREEN run (C), the case's own marks:

```
[B44-PROBE] before startScheduler      @ 0 ms
[B44-PROBE] after  startScheduler      @ 96 ms
[B44-PROBE] after  getSchedules        @ 97 ms
[B44-PROBE] after  the tick-chain count@ 98 ms
[B44-PROBE] before stopScheduler       @ 98 ms
[B44-PROBE] after  stopScheduler       @ 6082 ms
```

RED run (D), the same marks — the last one never printed:

```
[B44-PROBE] before startScheduler      @ 0 ms
[B44-PROBE] after  startScheduler      @ 88 ms
[B44-PROBE] after  getSchedules        @ 89 ms
[B44-PROBE] after  the tick-chain count@ 132 ms
[B44-PROBE] before stopScheduler       @ 132 ms
        (no "after stopScheduler" — the 30 000 ms limit expired inside it)
× scheduler (KERN: one process owns all system routines) 30085 ms
```

The teardown's duration measured in every green run of the day: **6082 · 6072 · 6069 · 6074 · 6068 ·
6072 ms**. It is a floor, not a fluctuation.

**MEASURED CAUSE:** the case asserts REGISTRATION (tick 15 s / reaper 60 s / breaker 5 min) and that
costs 132 ms. Stopping the scheduler again costs everything else, and TWO numbers own it, neither of
them this test's:

1. **~6 s, the lanes' uninterruptible rest.** `stopScheduler` (`packages/outbox-executor/src/scheduler.ts:725-737`)
   awaits `activeLanes.stop()` and then `activeMediaLanes.stop()`. `TaskLanes.runLane`
   (`task-lanes.ts:92-107`) re-tests the `stopping` flag only at the top of its loop and an idle lane
   `await sleep(restMs)` FIRST — so a resting lane cannot hear `stop()` until its rest ends.
   `DXB_LANE_REST_SECONDS` is 3 s, two lane sets are awaited one after the other, and the measured
   floor is 6.07 s with nothing whatever to do.
2. **up to 30 000 ms, pg-boss's own graceful window.** `boss.stop({ graceful: true })` in
   **pg-boss 12.25.1** (`dist/index.js:150` and `:174`) reads `timeout = 30000` by default and then
   loops `while (Date.now() - stoppingOn < timeout && manager.hasPendingCleanups()) await delay(500)`.
   `startScheduler` bootstraps EIGHT self-chained queues at delay 0 immediately before the test stops
   it, so whether a worker is still in flight at that instant is a coin toss — **and that coin toss IS
   the rotation.** When it comes up heads the teardown needs 6 s + up to 30 s = up to 36 s against a
   30 000 ms stopwatch: the case was given the same number its own library waits, so it could never win.

**Nothing is broken in the company.** The brake is live and answered every five minutes throughout
(company database, SELECT only: `velocity-breaker` completed 12:45:09 · 12:40:39 · 12:35:09;
`budget_state` = monthly cap 100.00 EUR, hourly 2.00 EUR, `breaker_tripped` false).

**What was NOT the cause, measured and discarded rather than left standing:**
* *"the suite runs its files in parallel"* — the board's own recorded cause. `vitest.config.ts:96`
  carries `fileParallelism: false` and there is no workspace file: the files run ONE AT A TIME.
  Deleted from the row under LAW A and replaced by the sentence above.
* *pg-boss table bloat* — the `pgboss` schema on the construction engine totals 1232 kB
  (`queue` 23, `job_common` 1561, `schedule` 14).
* *the number of hands* — my own first reading, and it was wrong: run A started **8 hands**
  (`[scheduler] the company is working with 8 hands (first tick)`) and finished in 6154 ms, run B
  started 8 hands and timed out. Hand count does not separate red from green.
* *lanes draining leftover work* — during the whole 30 s of run D the construction engine's waiting
  task count stayed flat at **12** and its queued media jobs at **0** (2-second sampler). No lane was
  mid-drain. The wait was pg-boss's, not a task's.

### `tests/r31/voice-line.test.ts` — the row's second named member
NOT reproduced. Green in every exclusive run of the day (8125 ms in run D, the red one). It stays
recorded as ONE builder measurement from 2026-09-16 that neither the checker nor this session has
ever seen again, and nothing here claims it fixed.

## 3. THE FIX — the teardown leaves the assertion's stopwatch, which is not raised

`tests/phase4/velocity.test.ts` only. The scheduler the last case boots is handed to `afterAll`, which
already existed for the sweep, and is stopped there before `closeDb()`. The case keeps its own
`30_000` literal, unchanged, for the ~130 ms of work it actually does.

**TIMEOUT CHANGED:** one literal is added — `afterAll(..., 45_000)`. It is a teardown budget, not a
widened stopwatch on an assertion, and it is sized by the two measured numbers above: 6 s (the lanes'
rest floor, measured 6068–6082 ms) + 30 s (pg-boss 12.25.1's own default graceful window) + 9 s margin.
The 2026-07-26 move that raised this case's own limit from 5 s to 30 s is NOT repeated: that number
stays exactly where it was, and what changed is that it no longer has to cover a library's 30-second wait.

Effect on the case itself, measured alone: **the KERN case went from 6154 ms to 74 ms.**

## 4. NEGATIVE PROOF: the brake's guard still has teeth

The brake was broken in the working tree (uncommitted): `packages/outbox-executor/src/breaker.ts:91`,
`if (windowEur <= capEur ...)` → `if (windowEur <= capEur * 1000 ...)`, so a retry storm can no longer
trip it. Rebuilt, then `pnpm exec vitest run tests/phase4/velocity.test.ts`:

```
 ✓ velocity breaker (COST-03) > under-cap hour does NOT trip the breaker 13ms
 × velocity breaker (COST-03) > injected retry storm (100 rows, ~5 EUR/hour) trips within one check; keys blocked; audited
 × velocity breaker (COST-03) > second check while tripped is a no-op (no duplicate audit)
 × velocity breaker (COST-03) > reset without --confirm is refused
 × velocity breaker (COST-03) > dxb breaker reset --confirm clears state, unblocks keys, audits actor ceo:cli
 ✓ scheduler (KERN: one process owns all system routines) > registers tick 15s / reaper 60s / breaker 5min 74ms
 Test Files  1 failed (1) · Tests  4 failed | 2 passed (6)
```

**Four of the six cases go red the moment the brake stops braking**, so his sentence — *"para zaten
benim iznim olmadan asla harcanamaz"* — is still guarded by this file. The mutation was reverted and
rebuilt; `git status` showed only `tests/phase4/velocity.test.ts` afterwards.

## 5. THE SOURCE FIX THAT IS NOT B44's TO MAKE — one line for him

The 6-second floor is runtime behaviour: an idle lane sleeps its full rest before it can hear `stop()`.
It could be made interruptible (a lane that wakes on stop), and that would remove the floor for the
company as well as for this test — but it lives under `packages/outbox-executor/` which is outside this
row's allow-list, and it is NOT covered by the company rule it looks like ("a task mid-run is never
cut": nothing is running while a lane rests). **It goes to him as a question, not as a change**, and if
he wants it, it becomes its own row.

## 6. THE THREE CLOSING RUNS (exclusive, after the fix)

Run with the ruler's own `run` mode, which refuses to start while another `vitest` process is alive.
Each line is the ruler's own printed summary; each run's reds are exactly the five constant reds
(B23 · B36 ×3 · C42), and `velocity` and `voice-line` are green in all three.

```
RUN 1: Test Files  5 failed | 124 passed (129) · timed out: 0 · 2026-09-16 13:39   CHECK B44 [run]: PASS
RUN 2: Test Files  5 failed | 124 passed (129) · timed out: 0 · 2026-09-16 13:41   CHECK B44 [run]: PASS
RUN 3: Test Files  5 failed | 124 passed (129) · timed out: 0 · 2026-09-16 13:43   CHECK B44 [run]: PASS
```

## 7. A RED THIS SESSION CAUSED ITSELF, FOUND AND SWEPT

Between 12:51 and 13:37 every whole-suite run carried a SIXTH red that no row owned —
`tests/e125/workforce-gate.test.ts`, which passed 7/7 alone. It was not a member of this class and
not a defect: killing a suite mid-run at 12:55 (to take the machine back for an exclusive measurement)
robbed `tests/b43/media-hands.test.ts` of its own `afterAll`, and its fixtures stayed behind on the
CONSTRUCTION engine — marker `b43t-6f1d23`, created 10:55:52Z. The gate counts live rows, so it saw
two headless departments and two unpromised agents and said so, correctly.

Swept on the construction engine only (`127.0.0.1:54422`), by the same marker the owning file uses:
2 departments · 2 agents · 9 tasks · 84 task_events · 1 routing rule. The company database
(`127.0.0.1:54322`) was read with SELECT and never written to at any point in this row's work.

```
$ pnpm exec vitest run tests/e125/workforce-gate.test.ts
 Test Files  1 passed (1) · Tests  7 passed (7)
```

The three closing runs above were all taken AFTER the sweep, which is why their reds are five and not six.

## 8. BLAST RADIUS

Changed: `tests/phase4/velocity.test.ts` (one file), plus records — this evidence, board row B44,
`.planning/STATE.md`, `scripts/governance/ceo-approvals.json`, and the ruler itself (committed alone,
earlier). Untouched, and checked by the ruler's `guard` mode: `packages/` · `apps/` · `db/` ·
`.claude/` · `scripts/` other than the ledger · and the five constant reds' own files
(`tests/b23`, `tests/b36`, `tests/c42`) — other rows' work, not absorbed here.

What stands on the changed file: nothing imports a test. The runtime `stopScheduler` it calls is
unchanged, and `pnpm typecheck` is green. The 123 files that were green before this change are green
after it in all three closing runs.

### The checker's own runs, on `231e3803`, exclusive

`dxb-global-os-c7` (Fable 5.1) re-measured the whole row with its own hands rather than reading
this file: `b44` 16/16 · `guard 1263c24a..HEAD` 5/5 · four whole-suite runs plus the door's whole
battery.

```
checker RUN 13:45  clean · timed out: 0
checker RUN 13:47  158 s · timed out: 0 · ONE red outside the five, name not captured
                   ⚠ UNVERIFIED — the checker filtered its own output and lost the name; it
                   records the slip as its own. Nothing else was running tests at the time.
checker RUN 13:50  clean · timed out: 0
checker RUN 13:53  clean · timed out: 0
checker BATTERY    the dxb-verify door's list, WHOLE — ALL PASS, its own whole-suite run clean
```

It also confirmed from source rather than from this file: pg-boss 12.25.1 `dist/index.js:150`
(`timeout = 30000`) and `:174` (the `hasPendingCleanups()` loop); that the committed diff equals the
patch it had read before it was applied; `B44-PROBE` residue 0; the `30_000` literal unchanged; the
construction sweep complete (`b43t-6f1d23` rows 0 in `agents`, `departments`, `tasks`); the company
snapshot unchanged; and that Codex/Astra ran no test and no database command against this row.

**Status: FINISHED 2026-09-16 on the checker's side · NOT ACCEPTED — his eye is what is left (LAW B).**

### The checker's *tamam*, round 2 (2026-09-16)

Round 1 (`231e3803`) passed every gate and was still refused its tamam for THREE records defects —
a STATE passage still saying in the present tense that the two files ARE a rotating class, a board row
that recorded the pre-fix hunt instead of the closing runs its own acceptance column demands, and an
evidence file carrying only the builder's runs. Each was measured true in the file before it was
applied, and the checker then re-measured all three IN THE FILES on round 2 (`ca23c345`) before giving
its word:

```
STATE   "Test timed out in 30000ms" 0 · "rotates between runs" 0
        replacement reads: FIXED in 231e3803 · timeouts 0 in every exclusive run after the fix
        (three builder, five checker) · FINISHED, waits only for his eye · voice-line never
        reproduced · the pre-fix record labelled HISTORY with the CONFOUNDED runs named
BOARD   heading "FIXED 2026-09-16, WAITS FOR HIS EYE" 1
        "STATUS: FINISHED 2026-09-16 · NOT ACCEPTED BY HIS EYE (LAW B)" 1
        RUN 1-3 lines carrying "timed out: 0" 3
        the checker's own run-2 slip written as ⚠ UNVERIFIED, in its name
GATES   b44 PASS 16/16 · guard 1263c24a..HEAD PASS · ledger truth OK (304 claims)
        STATE branch count 73 = git 73 · tree clean
```

Its verdict, in its words: *"complete per the ruler, correct, nothing left out — Ferrari in its own
scope."* **B44 is closed on the checker's side. It is NOT accepted: his eye is what is left (LAW B).**
