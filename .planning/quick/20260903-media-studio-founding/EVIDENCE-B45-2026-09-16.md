# EVIDENCE — board row B45, 2026-09-16

A resting hand could not hear "stop". Now it wakes at once.
Ruler: `CHECK-B45-2026-09-16.sh`, md5 **18a1ddca59b49b852671504fe88ed1d5**, dictated by the checker
session `dxb-global-os-c7` (Fable 5.1) and committed ALONE as `110a487c` before the first line of work.
Authority: his delegation of 2026-09-16, 14:24 — *"denetçiye danış ok verirse yap vermezse yapma"* —
registered as `b45-interruptible-rest-on-the-checkers-yes-2026-09-16`. He decided WHO decides; the
checker decided YES and bounded it.

---

## 1. WHAT WAS WRONG, measured while closing B44

`TaskLanes.runLane` re-tested its `stopping` flag only at the top of its loop, and an idle lane
`await this.deps.sleep(this.restMs)` FIRST. So a lane that had just found an empty queue was asleep
for `DXB_LANE_REST_SECONDS` (3 s) and could not hear `stop()` until it woke. `stopScheduler` awaits
the task lanes and then the media lanes one after the other, so the floor was two rests. `MediaLanes`
wraps `TaskLanes` (one `deps.sleep`, one place to fix).

Measured floor, six whole-suite runs of 2026-09-16 with nothing whatever for the lanes to do:

```
6082 · 6072 · 6069 · 6074 · 6068 · 6072 ms
```

Nothing was broken by it. It was paid at every shutdown of the company's hands, and at every test
that boots the scheduler.

## 2. THE FIX — one place, and a working lane is still never cut

`packages/outbox-executor/src/task-lanes.ts` only. The idle rest is now a race between the sleep and
a wake handle that `stop()` resolves; the set of currently-resting lanes is held so `stop()` can end
each rest. The lane still rests through `deps.sleep`, still on `DXB_LANE_REST_SECONDS`, and never
spins. `media-lanes.ts` needed no change — it wraps `TaskLanes` and inherited it.

What did NOT change, and is asserted: `stop()` still waits for the drain a lane is inside. The
existing case `"stop() waits for the current drains and takes no new work"` keeps its title and
passes. A task mid-run is still never cut — nothing is running while a lane rests, which is why this
was never covered by that rule.

**NO STOPWATCH TEST:** the acceptance reads no clock. Both new cases inject a sleep that never
resolves on its own, so a `stop()` that still had to wait a rest out would hang for ever; `stop()`
resolving IS the proof. No elapsed-time assertion, and no timeout literal was added anywhere in
`tests/`.

```
$ CHECK-B45-2026-09-16.sh unit
PASS  other vitest processes before the run                              0
PASS  vitest exit code                                                   0
      Test Files  2 passed (2) · Tests  17 passed (17)
PASS  new task-lanes case ran and passed                                 1
PASS  new media-lanes case ran and passed                                1
PASS  'stop() waits for the current drains' still passes                 1
CHECK B45 [unit]: PASS
```

15 existing cases + 2 new = 17. Every old title is unchanged.

## 3. THE NUMBER HE READS — the resident scheduler's restart

**DIST REBUILT:** `pnpm build` → `packages/outbox-executor/dist/task-lanes.js` rebuilt 2026-09-16
14:31:12, after the source; `dxb-scheduler` was then restarted onto it (ExecMainStart 14:32:47,
14:33:28, 14:34:08). The daemon runs compiled code, so the rebuild is part of the fix, not a detail.

**IN FLIGHT AT RESTART: 0** — every restart below was taken with the company idle: running media jobs
0 and running tasks 0, measured on the company database by the ruler's own `restart` mode, which
refuses to restart otherwise.

**RESTART BEFORE:** 2.67 s (`systemctl restart` → active), of which the stop half was **2.659 s**
(journal 14:30:31.383 → 14:30:34.042), measured on a scheduler that had been up 5 h 33 min, so its
lanes were resting. Two further pre-change restarts were taken seconds apart on a service that had
only just booted — 0.757 s and 0.0004 s — and are recorded here rather than used, because a lane that
has not begun resting cannot demonstrate the defect.

**RESTART AFTER:** 0.03 s each (`systemctl restart` → active), three settled samples 40 s apart, of
which the stop halves were **0.020 s · 0.019 s · 0.021 s**:

```
sample 1  14:32:47.953 Stopping → 14:32:47.973 Stopped   (0.020 s)
sample 2  14:33:28.052 Stopping → 14:33:28.071 Stopped   (0.019 s)
sample 3  14:34:08.149 Stopping → 14:34:08.170 Stopped   (0.021 s)
```

The seconds are printed, never judged — they are a number he reads, not a stopwatch verdict.

**LIKE FOR LIKE, the cleanest comparison of all:** the file whose teardown B44 measured at
**6147 ms** with the old rest, run alone on the same machine with the new one:

```
$ pnpm exec vitest run tests/phase4/velocity.test.ts
 ✓ tests/phase4/velocity.test.ts (6 tests) 175ms
```

6147 ms → 175 ms. The floor is gone, and nothing about the case changed between the two runs.

## 4. BLAST RADIUS

Changed: `packages/outbox-executor/src/task-lanes.ts`, `tests/b43/task-lanes.test.ts`,
`tests/b43/media-lanes.test.ts`, and records. Untouched and checked by the ruler's `guard` mode:
`scheduler.ts` · the breaker · `db/` · `apps/` · `.claude/` · `scripts/` other than the ledger.

What stands on the changed file: `TaskLanes` is used by the scheduler's task lanes and, through the
wrapper, by the studio's media lanes. Both are covered by the 17 cases above; the whole battery was
run after the change and the company was restarted onto the rebuilt code and answered.

**Status: FINISHED 2026-09-16 · NOT ACCEPTED BY HIS EYE (LAW B).**

## 5. ONE RED I SAW ONCE AND COULD NOT NAME — ⚠ UNVERIFIED

In the FIRST battery run after the change (2026-09-16 ~14:36, tree still dirty),
`tests/phase4/velocity.test.ts` appeared in the battery's red list. **I could not say why:** the
W15 battery deletes its own scratch directory, so the vitest log was gone before it could be read —
the same mistake the checker made on B44 and recorded against itself, made here by the builder.

What I did instead of guessing: re-ran the whole suite twice on the committed tree.

```
run A  Test Files  5 failed | 124 passed (129) · Tests 6 failed | 942 passed | 15 skipped (963)
run B (inside the battery)  Test Files  5 failed | 124 passed (129)
       reds: b23 · b36 ×3 · c42 — the five constant reds, velocity GREEN in both
```

It has not recurred. It is written here as **one observation whose reason was not captured**, not as
a fixed thing and not as a fluke — and if it returns, this paragraph is where it starts.

## 6. THE GATES, on the committed tree

```
CHECK B45 [baseline]: PASS   (18/18)
CHECK B45 [unit]:     PASS   (17/17, both new cases green, the drain-waiting case green)
CHECK B45 [guard]:    PASS   (110a487c..HEAD — allow-list, no timeout literal, ruler not self-certified)
CHECK B45 [restart]:  PASS   (in-flight 0, dist newer than src, service active after)
CHECK battery:        ALL PASS   (the shared W9-B08 battery)
CHECK W15 [battery]:  the door's list, whole — typecheck · pnpm test (every red named in STATE)
                      · verify:ledger · i18n · gitleaks whole history · vitrin gate · tree clean
```
