# EVIDENCE — W15, governance hygiene (the last row of the studio plan)

**Ruler:** `CHECK-W15-2026-09-16.sh`, md5 `1cb8ad929eefd1aa1d854a0b59813e61`, dictated by the checker
session `dxb-global-os-c7` (Fable 5.1) and committed ALONE as `9239bada` before the first line of work.
**His word:** *"cetvel gelince başla. bitincede biliyorsun süreci zaten. aynı w14te gibi."*
(`w15-ordered-to-start-on-the-rulers-arrival-2026-09-16`).
**Baseline before the work:** RED on 12 lines by design.
**ACCEPTED BY HIS EYE, 2026-09-16** — *"onaylıorm göz tmmdır."* <!-- CEO-OK: w14-and-w15-accepted-by-his-eye-2026-09-16 --> The checker had closed W15 on its side the same day; his own eye is what made it accepted (LAW B).

---

## The scope ruling this row was built on

The checker measured, before dictating the ruler, that four of the nine findings were already
SETTLED by the CEO's own registered words of 2026-09-15, so W15 executes their CONSEQUENCES and does
not re-ask him a settled question — which would itself break his ruling of 2026-09-13 that the
rulings are read before a plan is written.

| Finding | Settled by | What W15 therefore did |
|---|---|---|
| **F014** LAW C cited everywhere, registered nowhere | `law-c-not-a-ceo-law-source-unverified-2026-09-15` | measured only: every mention on an active surface already carries its not-a-law label → **0** unlabelled |
| **F018** eight board sentences promoted to rules | `board-rule-like-sentences-are-not-ceo-laws-2026-09-15` | measured only: the heading is gone, **0** ledger keys mint one of the eight |
| **F015** session-only remarks in standing files | his prohibition of 2026-08-13 | measured only: **0** live `.planning` files and **0** `.claude` files carry them |
| **F016 · F017 · F054** handover prompts | `handover-prompt-only-on-his-session-request-2026-09-15` | real work — see below |

## F016 / F054 — the three handover prompts: the false voice removed, the files kept

**THE LIE WAS REMOVED FIRST; THEN HE ORDERED THE FILES DELETED AND THEY ARE GONE.** <!-- CEO-OK: ceo-delete-the-three-handover-prompt-files-2026-09-16 -->
The ruler accepted either deletion or a kept banner with every CEO-voice line removed. The second was
taken first, for one reason: deleting is his call, not the author's, and his standing lesson of
2026-09-05 is that a deletion goes in front of him — list and sizes — before it happens. He was shown
the three names, their sizes and the eight places in the corpus that name them, and he answered:
*"o 3 dosyayı sil"* (2026-09-16), registered as `ceo-delete-the-three-handover-prompt-files-2026-09-16`.

**What he was shown before he decided:**

```
NEXT-SESSION-PROMPT-2026-09-13.md    1916 bytes   20 lines
NEXT-SESSION-PROMPT-2026-09-13b.md   1916 bytes   20 lines
NEXT-SESSION-PROMPT-2026-09-14.md    2407 bytes   22 lines
TOTAL                                6239 bytes
```

The eight places that name them — STATE.md, STATE-ARCHIVE.md, the W2 / audit / W15 evidence, the
handoff, the ledger and the ruler — are every one a RECORD of what existed, not a pointer that needed
the file; the ruler's own F016 line accepts the deleted state, which is why it stayed green after the
deletion. Removed with `git rm`, so every line stays addressable in history at 9b529bc6 and before.
Ruler on the deleted tree: **F016/F054 PASS · F017 PASS**.

| File | Before | After | First-person lines |
|---|---|---|---|
| `NEXT-SESSION-PROMPT-2026-09-13.md` | 37 lines, 8 442 B | 21 lines, then **deleted on his order** | 3 → **0** |
| `NEXT-SESSION-PROMPT-2026-09-13b.md` | 28 lines, 7 179 B | 21 lines, then **deleted on his order** | 3 → **0** |
| `NEXT-SESSION-PROMPT-2026-09-14.md` | 33 lines, 8 542 B | 23 lines, then **deleted on his order** | 3 → **0** |

What each file now carries: its original banner at line 1, and an author-voice note saying that the
body was removed under LAW A on 2026-09-16, what stood there, that git keeps every removed line
addressable at that path, that his own orders are in the ledger with his verbatim, and that a
handover prompt is written only when he asks for one in that session. **LAW A's reason, in one line:**
a banner saying "these are not his words" on top of a page still written in his voice IS the
"superseded but retained" form the law names.

`git show 9239bada^:…` recovers any removed line. Live surfaces telling a session to paste one of
these: **0**.

## F017 — no record orders a handover prompt; the door carries the ruling

- STATE sentences ordering a handover prompt (write / goes to / Phase 5, not "only when he asks"): **0**
- `.claude/skills/dxb-start/SKILL.md` Phase 5 now carries `<!-- CEO-OK: handover-prompt-only-on-his-session-request-2026-09-15 -->`
  with the ruling in the author's own English voice: no handover prompt is written unless he asks in
  that session; the next session starts from `.planning/STATE.md`, which Phase 5 has just made true.

## F048 — the battery is the door's list, WHOLE

The defect was not wording. `dxb-verify` lists `pnpm test` and `gitleaks detect`; STATE named a
three-folder subset and called it "the battery", and a 2026-09-14 record even claimed "the battery
green" on that subset. Both are corrected: the subset sentence is replaced by the door's whole list,
and the historical claim now records what actually ran, naming what the door requires and nobody ran.

**The whole suite, run on this tree 2026-09-16 (`pnpm test`):**

```
Test Files  5 failed | 124 passed (129)
     Tests  6 failed | 940 passed | 15 skipped (961)
```

**The six known reds, each named in STATE.md with the board row that owns it** — fixing one is that row's
work, never a by-product of this one (the guard blocks `tests/` outside `tests/personas` and
`tests/governance`):

| Red | Row | Why it is red, measured |
|---|---|---|
| `tests/b23/graph-is-not-stale.test.ts` | **B23** | the knowledge graph is 165 commits behind its limit of 150; fix is `pnpm graph:refresh` |
| `tests/b36/wall-question.test.ts` (2 tests) | **B36** | red BY DESIGN under a plain `pnpm test` — the file asserts it runs inside the construction sandbox (`uid=997`) and says so: *"start it with `pnpm construction:battery`"* |
| `tests/b36/live-drills.host.test.ts` | **B36** | a privilege class is open on the company |
| `tests/b36/seed-is-fiction.test.ts` | **B36** | one project row on the construction engine is not what the seed generates for it |
| `tests/c42/rival-intel-ledger.test.ts` | **C42** | the NEXT pointer names a row that is neither reported nor skipped |
| `tests/phase4/velocity.test.ts` | **B44** (opened by W15) | passes 3/3 alone, fails under the whole suite — load-sensitive; guards COST-03 and KERN |
| `tests/r31/voice-line.test.ts` | **B44** (the row widened the same day) | red in ONE run, the builder's; the checker did not reproduce it; passes 2/2 alone — same class, same message |

`gitleaks detect` over the **whole history**: no leaks found.

**The sixth red, and the correction of my own first reading.** The checker named **six** failing
files; my first whole-suite run showed five, with `tests/phase4/velocity.test.ts` passing
(`✓ … (6 tests) 6140ms`), and I reported that difference. The battery run on the committed tree then
**failed** on it, so the checker's count was right and mine was the lucky run. Measured properly:
run on its own the file passes **3 out of 3** (6 tests, ~6 s); run inside the whole suite it fails.
It is **load-sensitive, not broken** — and a test that only tells the truth when the machine is idle
cannot guard what this one guards: COST-03, the velocity breaker that stops the holding spending
faster than it is allowed to, and KERN, one process owning all system routines.

It was also the only red in the whole suite that **no board row owned**, and board law 1 says open
work has a row. **W15 opened `B44`** for it and named it in STATE with the others. W15 did NOT fix
it: the guard forbids this row touching `tests/`, and fixing a red is its own row's work, never a
by-product of another.

## F055 — the four quotations, each resolved by his own typed words

None was left as UNVERIFIED: all four were found in his own typed records and **copied**, never
retyped. Each is now a ledger entry carrying his verbatim.

| Quotation | His record | Registered as |
|---|---|---|
| **"sadece bu ikisi"** — the two standing-order texts must stay short | typed 2026-09-13T22:37:52.605Z = **2026-09-14 00:37 local** | `ceo-two-standing-orders-must-be-short-2026-09-14` |
| **"devir promtu yazma"** — no handover prompt; commit, measure, expect to be audited | typed 2026-09-14T15:13:47.158Z | `ceo-no-handover-prompt-audit-what-you-did-2026-09-14` |
| **Turkish summary** decision — *"türkçe özete gerek yok orası için ceo kararı"* | typed 2026-09-13T22:32:44.531Z = **2026-09-14 00:32 local** | `ceo-no-turkish-summary-on-the-b43-card-2026-09-14` |
| migration **`20260913001000`** header, *(CEO 2026-09-13, "düzelt")* | typed 2026-09-13T18:52:25.117Z | `ceo-qa-receipt-fix-ordered-2026-09-13` |

**The gate learned the shape of the claim.** `scripts/governance/ledger-truth.mjs` previously saw only
assertions like "CEO-approved"; a line saying "done on his word" asserted his authority just as hard
and passed unseen — the hole its own comment named. It now rings on `on his word` / `on the CEO's
order` **when the line asserts the work happened** (a completion verb before it, or his quoted words
after it), and stays silent on the far commoner rule form ("waits on his word", "gate: his word").

Measured while tuning it, and reported because it shaped the result: the first pattern rang on **51**
lines, almost all rules; tightened to the asserting form it rang on **25**; of those, **21** were
dated records under `.planning/research/` (rival-intel reports, study cards) and **4** were on
binding surfaces. The check is scoped to exclude `.planning/research/`, for the reason the file
already excludes `_ARCHIVE` from its other scope — annotating a dated record of what was read that
day is rewriting history — and because F055's own words name "standing files, hooks and migrations".

The four binding-surface claims were resolved, not silenced:

- `.planning/PROJECT.md:94` — claimed the GSD commands were deleted "on his order of 2026-08-01".
  **The date was wrong.** `git log -S` puts that very sentence in commit `e56dcc68` of **2026-08-27**,
  the commit that executed `methodology-plugins-out-codex-hook-off-2026-08-27`. Corrected and marked.
- `.planning/STATE.md` — `dxb-jarvis.service` DISABLED on his order 2026-09-14 → marked with
  `jarvis-silent-listening-voice-04-okayed-2026-09-14`.
- `HOLDING-OS-MASTER-PLAN/00-INDEX.md:153` — decision U44 → marked with the W13 and W14 entries.
- `00-NOTE-CEO-COMPLAINT-LEDGER-2026-07-19.md:4` — the renumbering on *"şikayet tahtasını düzelt"* →
  marked with `flux-avatars-retired-and-complaint-numbering-2026-09-13`, which already held his words.

`verify:ledger` with the new check: **ledger truth OK**.

## F056 — personas are bound through a door

**Confirmed before building:** `SELECT proname FROM pg_proc WHERE prosrc ~* 'update\s+agents\s+set[^;]*persona_id'`
returned **nothing** — no function in the company set `agents.persona_id`. All 213 bindings had been
written by raw `UPDATE`s from shell scripts: no gate of its own, no audit row, free to drift from
`agents.persona_version`.

**One migration, `20260916010000_w15_persona_bind_door.sql`**, creating `fn_persona_bind(agent, persona, actor)`:
refuses a persona whose `quality_gate` is not `passed` or whose author is not a v2 author (the
activation trigger's rule, applied at BIND time instead of afterwards); refuses a persona belonging to
another employee, which a raw UPDATE could not see; copies `persona_version` across in the same
statement; writes one `audit_log` row per bind and per unbind; is idempotent; and refuses to unbind an
ACTIVE employee.

Applied through the canonical chain, construction first, then the company:

```
[bootstrap] applying 20260916010000_w15_persona_bind_door.sql
[bootstrap] done — applied 1, skipped 169, ledger total 170     (construction)
[bootstrap] done — applied 1, skipped 169, ledger total 170     (company)
```

**The snapshot before and after — not one existing binding changed:**

```
before  213/219/1e2b5552947189574efcc386b8815358
after   213/219/1e2b5552947189574efcc386b8815358
```

**Every bind path now routes through the door.** Raw `UPDATE agents … persona_id` in `scripts/`,
`packages/`, `apps/`: **2 files → 0**. `scripts/hr-demo-hire.sh` (3 sites) and
`scripts/bench/drain-throughput.mjs` (1 bulk site) call `fn_persona_bind`;
`scripts/sync-personas-to-db.sh` gained a `--bind` pass that binds only through the door, with
`--dry-run`, and is deliberately NOT part of `--verify` or of a submit run because it changes live
bindings. Its dry run on this tree: *"nothing to bind — every employee already carries its newest
passed persona"*.

**The door proved on the CONSTRUCTION engine (never on the company):**

```
TEST 1 PASS — refused: persona bind denied: no agent 00000000-…-0001
TEST 2 PASS — refused: persona bind denied: no persona 00000000-…-0002
TEST 3 PASS — idempotent, audit_log unchanged (40207)
TEST 4 PASS — refused: persona unbind denied: agent engineering-minimal-change-engineer is active
```

**A defect of my own, found and reported rather than hidden:** the first version of the `--bind` pass
sat AFTER the file loop, so `--bind --dry-run` ran the submit loop first and attempted persona
submits. The database refused every one (`personas_author_check`, author empty) and **nothing was
written** — measured immediately: personas **595**, rows created in the last 20 minutes **0**,
snapshot unchanged. The pass was moved before the loop and re-run clean.

## Round 2 — the checker's three corrections

It passed every gate on round 1 and withheld its tamam for three things. All three were real.

### 1. The door's HAPPY PATH, proven — it had only ever been proven to REFUSE

The checker measured what my own evidence had not: construction `audit_log` held **persona_bind 0 /
persona_unbind 0**. My four proofs were three refusals and one idempotence — nothing had ever been
bound THROUGH the door. A door proven only to stay shut is not proven to open.

`scripts/hr-demo-hire.sh` is the end-to-end proof of the hire chain, but it was hardcoded to the
company's engine — and a proof that can only be run against the live company is a proof nobody dares
run. It now takes `DXB_DB_CONTAINER`, defaulting to the company exactly as before, and the whole
chain was run against the CONSTRUCTION engine:

```
== E5.4b demo:hire — probe slug: hr-demo-probe ==
created: 15d73ed4-f986-4a05-b85d-e922ded2b16e
blocked as expected: draft->probation refused (missing e5 + persona)
persona submitted+passed+bound: c92c5f2c-cfbe-4a71-9f36-a9f45f40092b
equipment 7/7: true all_ok=true
evaluate 0.5 -> probation · evaluate 0.9 -> active · employment_status: active
```

**The bind, as the database shows it:**

```
slug | employment_status | persona_id                           | persona_version
hr-demo-probe | active | c92c5f2c-cfbe-4a71-9f36-a9f45f40092b | v1
personas: c92c5f2c-… | v1 | passed | fable-5
audit_log: persona_bind | demo-hire |
  {"slug":"hr-demo-probe","agent_id":"15d73ed4-…","to_persona":"c92c5f2c-…",
   "from_persona":null,"persona_version":"v1"}
```

`persona_version` reads `v1` and the persona's own `version` is `1` — the door wrote it, not a human.
Exactly **one** `persona_bind` row.

**The unbind, and the refusal before it:**

```
NOTICE: REFUSAL PASS — persona unbind denied: agent hr-demo-probe (15d73ed4-…) is active —
        an active employee without a persona is what the activation gate forbids
UPDATE 1                      (stood down first, as the door demands)
unbound -> NULL
audit_log: persona_unbind | w15-proof |
  {"slug":"hr-demo-probe","agent_id":"15d73ed4-…","from_persona":"c92c5f2c-…"}
agents: hr-demo-probe | draft | persona_id=-
```

Exactly **one** `persona_unbind` row. The probe was then removed whole (tasks 2, personas 1,
settings 3, records 1, agent 1 → probe rows left **0**), and the construction roster is back to
**agents=219 bound=213**.

**The company was never touched by any of it:** `213/219/1e2b5552947189574efcc386b8815358` before and
after, and company `audit_log` rows for `persona_bind`/`persona_unbind`: **0**.

The run ended on a construction-specific red — *"unequipped active employees exist: 213"* — which is
the construction engine's generated workforce, not the door: those 213 were never equipped through
the hire chain because the seed writes them directly. It is named here rather than hidden.

### 2. Two ledger dates were UTC, not the holding's local day

He typed them at `2026-09-13T22:37:52.605Z` and `2026-09-13T22:32:44.531Z` — **2026-09-14 00:37 and
00:32 in the holding's own time.** Every other entry, and his own session records, use the local day;
the one-day UTC/local disagreement is audit finding F108. Both keys were renamed to `…-2026-09-14`,
both `date` fields set to `2026-09-14`, and each entry now states the UTC timestamp so the message can
still be found in the transcript. Every reference in this document follows.

### 3. A Turkish banner in an English artefact

`NEXT-SESSION-PROMPT-2026-09-14.md` line 3 was still Turkish — F016's third complaint. Rewritten in
English, with his own quoted words left in his language and marked, because **a quotation is his and
not the artefact's**: *"devir promtu yazma"*.

## The red list is a CLASS, and it rotates — measured, not assumed

After his deletion order the whole battery was re-run, and a red appeared that had never been seen:
`tests/r31/voice-line.test.ts`. The first question was whether the deletion had broken it. It had not —
the file names the deleted prompts **0** times and passes on its own **2 out of 2**. The next run of the
whole suite put velocity back in the red list and voice-line back in the green.

Four consecutive whole-suite runs on 2026-09-16 (the builder's): **5, 5, 6, 6** reds. The checker ran
four of its own: **6, 5, 6, 5**. **velocity's rotation both sessions have seen with their own hands;
voice-line's red appeared in ONE run, the builder's, and the checker did NOT reproduce it** (11/11 alone
on its machine, 0 mentions of the deleted files). It is named as the builder's single measurement and
labelled as such wherever it appears — a record that hides who saw what is how a finding becomes folklore. The five constant members are
B23, B36 ×3 and C42. The rotating members are velocity and voice-line, and the message is the same one
every time — **`Test timed out in 30000ms`**. That is contention, not logic: the suite runs its files in
parallel and these wait on wall-clock.

So **B44 was widened the same day from one file to the class**, and STATE names both members, so the
battery line tells the truth whichever of them goes red on a given run. The row now closes on three
consecutive whole-suite runs with no timeout, or on the wall-clock limits being replaced.

## Gates, on the committed tree

| Mode | Result |
|---|---|
| `w15` | PASS |
| `db` | PASS — 8/8 |
| `guard <range>` | PASS |
| `battery` (the door's list, whole) | PASS with the five known reds named in STATE |

