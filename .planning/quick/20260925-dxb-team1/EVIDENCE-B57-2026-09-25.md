# EVIDENCE — B57, the dxb-team1 door (2026-09-25 night)

Chief engineer: this Fable 5.1 session (`claude-fable-5-1`), the lead seat of the door it built.
Writer for code: `builder` (Opus 5.5 · max) — the gate branch and its tests. Instruction and
record text: the chief engineer. Plan he approved by word: `~/.claude/plans/imdi-unu-yapaca-z-dxb-team1-compiled-hearth.md`
(*"tmmdır dxb-team1 yapılsın"*, ~23:05).

## Acceptance items (written before the work, in the plan's engineer section) — measured

| # | Item | Command → decisive output | Result |
|---|---|---|---|
| 1 | SKILL.md ≤ 150 lines; description ≤ 400 bytes | `wc -l .claude/skills/dxb-team1/SKILL.md` → see §A · opening ruler R2: project skills 3,996 bytes ≤ 5,000 | see §A |
| 2 | three seats: model `claude-opus-5-5`; effort medium · high · max; verifier and arbiter read-only; no `Agent` tool | `grep -H "^effort:\|^model:\|^disallowedTools:\|^tools:" ~/.claude/agents/{builder-medium,verifier,arbiter}.md` → see §A | see §A |
| 3 | gate tests: old 34 pass + ≥ 4 new | `pnpm vitest run tests/hooks/code-gate.test.ts` → see §B | see §B |
| 4 | gate live: medium/builder-medium non-guarded → allowed; guarded → deny; plain builder at medium → deny | three stdin runs → see §B | see §B |
| 5 | dependants intact: four files sha256 before = after; `verify:ledger`; opening ruler; codex mirror `--check` | see §C | see §C |
| 6 | the door is visible to a session | the harness listed `dxb-team1` in this session's skill list right after the file was written (mid-session, no restart) — printed by the tool runtime, not by me | MEASURED (this session); a fresh-session check was not run tonight because it would open a terminal on his screen while he was reading |
| 7 | first team job with its three measurements | not run — owed (door §6) | OWED |

## A. The door and the seats

```
$ wc -l .claude/skills/dxb-team1/SKILL.md            → 123 lines   (≤ 150)
description bytes (frontmatter, utf-8)              → 396 bytes   (≤ 400; opening ruler R2 3,996 / 5,000)
$ grep -H "^model:|^effort:|^disallowedTools:|^tools:" ~/.claude/agents/{builder-medium,verifier,arbiter}.md
builder-medium.md: tools: Read, Write, Edit, Grep, Glob, Bash · model: claude-opus-5-5 · effort: medium
verifier.md:       tools: Read, Grep, Glob, Bash · disallowedTools: Write, Edit · model: claude-opus-5-5 · effort: high
arbiter.md:        tools: Read, Grep, Glob, Bash · disallowedTools: Write, Edit · model: claude-opus-5-5 · effort: max
```
No seat has the `Agent` tool. The tool runtime listed all three as new agent types in THIS session
minutes after the files were written (*"New agent types are now available for the Agent tool:
arbiter, builder-medium, verifier"*), and listed the `dxb-team1` skill the same way — measured
live, no restart; this contradicts the plugin lesson of 2026-09-19 (a plugin enabled mid-session
stays unknown), which stays true for plugins only.

## B. The gate — the builder's report, then the chief engineer's own re-measurement

Builder (Opus 5.5 · max, 134,466 tokens, 14.6 min): `gate_medium_write` (no envelope; every code
target of a Bash command checked against `GUARDED_PATTERNS` through a new `guarded_pattern()` that
`gate_simple_write` now shares), the NORMAL · medium · `builder-medium` branch in `gate_code_write`,
the third option in the deny text, `route_builder` recognising the third name, a docstring
paragraph. Its own count of the old tests was 28, not the 34 the plan wrote (the plan's grep counted
`describe(` lines too) — 28 + 6 new = 34. Backup of the old gate: `~/.claude/backups/2026-09-25-dxb-team1/`.

Chief engineer, own hands (23:35):

```
$ pnpm vitest run tests/hooks/code-gate.test.ts
 ✓ tests/hooks/code-gate.test.ts (34 tests) 3329ms · Test Files 1 passed · Tests 34 passed (34)
$ python3 -m py_compile ~/.claude/hooks/dxb-code-gate.py            → no output (OK)
$ sha256sum (after)   dxb-crew/SKILL.md a6245a0d… · builder.md 75ba093a… · builder-lean.md be684f8c… · refuter.md 91908b8e…  = BEFORE
                      dxb-code-gate.py 9d985ef9… → 6827695a…   (diff: 70 lines; tests: +59)
hand runs, stdin JSON, XDG_RUNTIME_DIR and DXB_CODE_GATE_LOG_DIR pointed at a scratch dir (no quota record → NORMAL):
  medium · builder-medium · Write /tmp/x/a.ts                 → ALLOWED (no output)
  medium · builder-medium · Write /tmp/x/db/a.ts              → deny — "… is a guarded path … subagent_type \"builder\" (max)"
  medium · builder        · Write /tmp/x/a.ts                 → deny — "Code is written only by a builder subagent at Opus 5.5 · max …" (old behaviour)
  medium · builder-medium · Write ~/.claude/hooks/y.py        → deny — guarded (hooks)
  medium · builder-medium · Bash "printf x > /tmp/x/src/y.py; printf y | tee /tmp/x/db/z.ts" → deny — the second target is guarded
LEAN (a scratch quota record at 85 %):
  Agent builder          → allow, rerouted to builder-lean        (2026-09-24 contract intact)
  Agent builder-medium   → allow, stays builder-medium, model key stripped
```

Builder's notes taken as B and repaired in the same pass: `builder-medium.md` said the gate refuses
guarded paths in both modes; in LEAN medium is allowed to everyone by the 2026-09-24 order, so the
agent file now says "NORMAL kipte kanca reddeder". Older than this work (C, not touched): the gate
fires only on code files (an `.md` or `.json` is writable at any effort — by design, the crew rule,
not the hook); `cp` / `mv` / `python -c` writes are not recognised; `dxb-cost-gate` matches
`echo yes || …`; two test titles and the docstring's item 2 still say "builder or builder-lean".

## C. Dependants — before → after

Before (taken at 23:07, before any file was touched):

```
a6245a0d5d45cee7471377d94dba89310cd94cc93fa2663f97b921bcc14b136f  .claude/skills/dxb-crew/SKILL.md
75ba093ad2b7f40ffe78336668367cc5f816941c4310faea758a9edb12f9c903  ~/.claude/agents/builder.md
be684f8ce1ad944a890778ea82a00be7db966bc5ac1046a5f65bfc594f2d5d65  ~/.claude/agents/builder-lean.md
91908b8e30fa34eb70c820a11f3df37d7e45b17b7da9b4d3704292f1e176cef1  ~/.claude/agents/refuter.md
9d985ef97b23e95a26510722041a019e3ed890af63d15b38edb0331a3c34a8f2  ~/.claude/hooks/dxb-code-gate.py  (the gate BEFORE the builder's change)
quota record: seven_day 75 % (written 21:05Z) → NORMAL mode; the builder call was not rerouted
```

Opening ruler, after the door and the doors row: core 12,995 · project skills 3,996 · position block
7,535 · total 24,526 (bytes) — 6/6 PASS. **The core was 12,998 bytes at HEAD**, two bytes under its
13,000 budget, so the new doors row could not fit as a row of its own: the `dxb-crew` row and the
`dxb-team1` row were merged into one (*"running a job on its own — alone (crew) or as a team (Fable
lead, parallel writers, verifier) | `dxb-crew` · `dxb-team1`"*) and the doors footer lost 12 bytes of
punctuation; no sentence of his was touched.

Ledger: `pnpm verify:ledger` → *ledger truth OK: 11 state claims re-measured, 164 open markers
resolved against 87 board rows … 429 CEO approval claims each backed by a registered approval* (after
the three ids were registered; the first run failed on the STATE block lacking an `OPEN: B57` marker —
fixed).

Codex mirror: `sync-codex-mirror.sh` → *SYNC_OK AGENTS.md + 13 doors + 3 hooks regenerated*; `--check`
→ *SYNC_OK mirror matches source*. **Found on the way (older than this work):** the mirror had not been
regenerated since commit `e12549fe` (B53) — 36 files drifted, among them the mirror copy of
`agent-reach`, whose repo original he had deleted in `35ddc3d8` (*"one research door"*). The
regeneration is committed apart from the door so the two do not mix. `--check` is not wired into the
pre-commit; that is why it drifted.

## D. The first fresh-verifier run — and the same diff at `high` and at `xhigh` (his question, measured)

Both seats were given the same brief on the same diff (the gate + its tests) with the same six
done-list items, in parallel, at 23:38; neither saw the builder's report.

| | `verifier` — Opus 5.5 · **high** | `refuter` — Opus 5.5 · **xhigh** |
|---|---|---|
| time | 229.6 s (3.8 min) | 282.1 s (4.7 min) |
| tokens | 49,574 | 69,210 |
| tool uses | 12 | 17 |
| done-list | 6/6 run and matched | 6/6 run and matched (noted that item 6 needs `--cached` once the file is staged) |
| **A** | **1 — a Bash write target with a shell variable (`D=/work/db; printf x > $D/a.ts`) passes the guarded check; the old gate refused it; born with this work** | **the same A**, measured in five variants (`>`, `>>`, `tee`, `sed -i`, `${D}`), with the repair spelled out |
| B | 2 — a stray `~/.claude/hooks/__pycache__/*.pyc` from 23:19 (removed by the chief engineer); LEAN lets `builder-medium` write a guarded path (the lead's call: kept, it is the 2026-09-24 LEAN policy for everyone, written into the agent file) | 0 (measured the LEAN case, judged it in-design and out of scope) |
| C | 1 — `cp` / `mv` / `install` / interpreter one-liners are not seen as writes (older than B57) → board row B58 | the same C, with `python3 -c` and a backtick target measured |

Reading: both found the one real defect; `xhigh` cost 40 % more tokens and 23 % more time for the
same A and one fewer B. One data point, on a 145-line diff — the door's §6 still owes the sweep on a
real team job before the second look's level is judged. The A was handed back to the `builder`
(max) in the same pass — see §E.

## E. The A-1 repair (builder, max) and the chief engineer's re-measurement

Builder, round 2 (213,654 tokens, 12.2 min): `bash_unreadable_write(command)` — for `builder-medium`
a Bash write whose target holds `$` or a backtick or begins with `~`, or a writing command whose raw
text holds `$(` or a backtick, is refused before the guarded check ("… cannot be measured …
Write or Edit …"); a descriptor copy (`2>&1`) and `/dev/null` are not writes; the lexer untouched;
four `it.each` cases. Its own health list: 8/8 legitimate commands still pass, 13/13 bypass shapes
refused, 84/84 runs identical for every other writer. It also corrected one claim of the verifiers:
the backtick shape had passed the ORIGINAL gate too for every writer (the lexer drops a redirect
whose target starts with a backtick) — so that one is older than B57 for the other seats (B58).
The stray `~/.claude/hooks/__pycache__` was its comparison scripts' doing (`importlib`); removed.

Chief engineer, own hands (23:58):

```
$ pnpm vitest run tests/hooks/code-gate.test.ts     → ✓ (38 tests) · Tests 38 passed (38)
$ python3 -m py_compile … (PYTHONPYCACHEPREFIX=scratch)  → no output (OK)
$ sha256sum (after round 2)  the four untouched files = BEFORE · dxb-code-gate.py 6827695a… → a9c62711…
hand runs (NORMAL, medium, builder-medium, Bash):
  D=/work/db; printf x > "$D/x.ts"          → deny  "… cannot be measured …"
  printf x | tee "${D}/x.ts"                → deny
  printf x > `echo /work/db`/x.ts           → deny
  printf x > ~/.claude/hooks/y.py           → deny
  D=/work/db; sed -i s/a/b/ "$D/x.ts"       → deny
  printf x > $(echo /work/db)/x.ts          → deny
  printf x > /tmp/x/src/y.py                → ALLOWED (literal, not guarded)
  pnpm vitest run … 2>&1 | tail -3          → ALLOWED (a descriptor copy is not a write)
  printf x > /tmp/x/db/z.ts                 → deny  "… is a guarded path …"
$ git diff --numstat -- tests/hooks/code-gate.test.ts   → 12 0 (round 2, on top of the staged 59 0)
```

Verifier round 2 (A list only): see §F.

## F. Verifier round 2 — A list only

`verifier` (Opus 5.5 · high), 79.3 s, 34,461 tokens, 9 tool uses, no new hunt: **A-1 CLOSED.** 38/38;
the seven bypass shapes refused with "cannot be measured" (and, checked against the round-1 copy, five
of them had passed there); the three legitimate commands still pass; the literal guarded path still
refused as guarded; `builder` at medium still gets the old general refusal; `builder-lean`'s Bash
refusal unchanged. Seven further variants (`>"$D"/x.ts`, `1>`, `&>`, `>|`, `tee -a`, `sudo tee`,
`perl -pi -e`) refused too. Its C note (`cp`, `dd` are not writes for any seat) is B58.

## G. What was NOT done, and what waits on him

- A fresh-session check of the door by `operator` — not run tonight (it would have opened a terminal
  on his screen while he was reading); the runtime's own live listing of the skill and the three
  agents is the measurement on record. `⚠ UNVERIFIED — a fresh session` until one opens.
- The first team job and the door's three §6 measurements — owed; they begin on his *"başla"*.
- His eye on the door (LAW B).
- B58 (the gate blind to `cp` / `mv` / `install` / `dd` / interpreter writes) — older than this work,
  on the board, not touched.

