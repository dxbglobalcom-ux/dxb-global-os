# HANDOVER — 2026-09-26 ~00:30, from the Fable 5.1 chief engineer session at 48 % context

**The job's place in the whole (read this line first):** the holding OS is the Ferrari — one CEO
states intent and the company runs itself, quality-gated. The construction that builds it now runs
through a door called `dxb-team1` (built tonight, his default door since 00:05); that door's own
rule says a session hands over at 40 % context, and this session broke that rule (48 %). Your first
job is to make that rule a machine (B59), so no session can break it again; after it, the first
real team job through the door, on his "başla". Nothing you do tonight is "a small part of a
wheel hub" — it is the discipline the whole car is built with.

## Seats

You are the chief engineer / lead: a Fable 5.1 session (`claude-fable-5-1`, effort high), the lead
seat of `dxb-team1` (`.claude/skills/dxb-team1/SKILL.md`). Code is written by subagents only:
`builder` (Opus 5.5 · max) for anything hard or on a guarded path (hooks are guarded);
`builder-medium` (Opus 5.5 · medium) for a lane you judge not hard; `verifier` (Opus 5.5 · high,
fresh) checks a diff against the done-list you write BEFORE the work; `refuter` (xhigh) only on an
A finding; `arbiter` (max) only when those two disagree. You write record and instruction text
yourself. You never write a code file (the gate refuses it).

## The CEO's words tonight, verbatim, in order (all in the ledger `scripts/governance/ceo-approvals.json`)

1. *"şimdi şunu yapacağız. dxb-team1 şeklinde bir skill açıp dxb-crew e ikinci bir arkestratör skill kursak olur mu? dxb-crew üzerinde oynama yapma o kalsın bazen onu kullanırız…"* + his diagram → `dxb-team1-ordered-2026-09-25`
2. *"yani işin zorluk seviyesine göre ya max'li opus 5.5'e yada mediumlu opus 5.5 e ver göre."* → `dxb-team1-effort-by-difficulty-2026-09-25`
3. *"çürütücü neden opus 5.5 xhigh? lütfen önce opus 5.5'in resmi sitesine bakıp karar verir misin"* → answered from Anthropic's effort page: first look high, second xhigh, last max (door §3)
4. *"bana sorduğun her konuda senin önerini de söyle zira ben CEO'yum mühendis değil"* — obeyed; NOT a law (he was asked once whether to make it permanent, no answer)
5. *"tmmdır dxb-team1 yapılsın"* → `dxb-team1-build-order-2026-09-25`
6. *"önerini kabul ediorum varsayılan kapı dxb-team1 olsun."* → `dxb-team1-default-door-2026-09-26`
7. *"peki neden %48 e gelmene rağmen yeni session açmadın?"* … *"NEDEN DİYE SORDUM. bu dxb-team1 içinde yazılı talimat. neden uymadın bu büyük sıkıntı"* … *"tmm önerini uygula."* → `context-gate-hook-ordered-2026-09-26` (B59)

Session-only, not law: he wants to TALK through a plan before approving (the plan-mode approve
button was refused four times until the plan was one page in Turkish and the questions were answered
in text); every question to him carries your recommendation; engineer checklists never go in front of
him. Memory: `plan-in-his-language-talk-first-2026-09-25`.

## What stands (commits, all on `studio/b43-ferrari-implementation-20260915`)

- `6ee8526d` — the door, three seats (`~/.claude/agents/{builder-medium,verifier,arbiter}.md`, outside the repo), the gate widened (`~/.claude/hooks/dxb-code-gate.py` 9d985ef9 → a9c62711, outside the repo, backup `~/.claude/backups/2026-09-25-dxb-team1/`), `tests/hooks/code-gate.test.ts` 28 → 38, records (ledger ×3, board B57 + B58, STATE, MODEL_ROUTING_SPEC A-2026-09-25, model-routing-hierarchy v15, doors row, Kadro), evidence `EVIDENCE-B57-2026-09-25.md` (this folder).
- `4e0b7547` — the Codex mirror regenerated (36 files had drifted since e12549fe).
- `7231ab5a` — the default-door word registered where it is read.
- This handover's commit — ledger `context-gate-hook-ordered-2026-09-26`, board B59, STATE, this note.
- `dxb-crew` untouched: sha256 before = after (SKILL.md a6245a0d…, builder.md 75ba093a…, builder-lean.md be684f8c…, refuter.md 91908b8e…).

## Measured tonight (the numbers you may cite; re-measure before you build on them)

- Opening ruler `node --no-warnings tests/hooks/opening-budget.ts` → 6/6, core 12,991 bytes of 13,000 (a new row in `.claude/CLAUDE.md` does NOT fit; the crew/team1 rows were merged into one).
- `pnpm verify:ledger` → OK. `bash scripts/governance/sync-codex-mirror.sh --check` → SYNC_OK.
- The gate: 38/38; hand-run recipe in EVIDENCE §B/§E (stdin JSON, `XDG_RUNTIME_DIR` + `DXB_CODE_GATE_LOG_DIR` to a scratch dir → NORMAL; a scratch `claude-ctx/rate-limits.json` at 85 % → LEAN).
- The door's first live run on its own gate change: `verifier` (high) 3.8 min / 49,574 tokens found one real A (a shell variable in a Bash target bypassed the guarded check); `refuter` (xhigh) 4.7 min / 69,210 tokens found the same and nothing more; `builder` (max) closed it; round 2 79 s. One data point for high vs xhigh.
- Quota at 23:05 was 75 % (seven-day) → NORMAL mode; check `/run/user/1000/claude-ctx/rate-limits.json` before spawning `builder` (≥ 80 % reroutes it to `builder-lean`, which cannot write hooks).

## YOUR FIRST JOB — B59, the context gate as a hook (his order, "tmm önerini uygula.")

Spec, as he accepted it: a global hook in `~/.claude/hooks/` (wired in `~/.claude/settings.json`
like `dxb-code-gate.py`; UserPromptSubmit for the red line, PreToolUse on `Agent|Task` for the
refusal) that reads the context number the status line already writes (`~/.claude/hooks/dxb-statusline.js`
writes it; `dxb-ctx --pct` reads it — find the file it reads and read the same one, per session).
At ≥ 40 %: inject one red line into the session on every prompt: `CONTEXT N% — hand over at the
first clean break (dxb-team1 §5 / dxb-crew §3)`. At ≥ 45 %: deny the `Agent` / `Task` tool with a
reason naming the handover, until the session is new. Stdlib only; on any failure exit 0 with no
output (the code gate's contract); decisions logged one JSON line each; cases in `tests/hooks/`
(the code-gate test file shows the harness). Numbers 40 / 45 are HIS (2026-09-21) — one place,
named. Write the done-list first (command + expected output), then `builder` (max) writes it
(hooks are guarded → never `builder-medium`), then `verifier` (high) on the diff, then you
re-measure with your own hands, commit, report in the `dxb-ceo-report` shape, position first.
Blast radius to name and re-measure: `dxb-statusline.js` (must keep writing the number),
`dxb-code-gate.py` (unchanged), every existing hook test still green, the opening budget (a
settings.json change is outside the repo; the ruler is unaffected).

## Then

On his *"başla"*: the first real team job through `dxb-team1` — pick a small board row that splits
into two disjoint lanes (one medium, one max), name it to him in one line before starting, and pay
the door's three measurements (§6): the same diff verified at high and at xhigh; the same lane
written by a subagent and by a separate session; cost and time beside `dxb-crew`'s last phase.

## Traps met tonight

- `pnpm -C "<path with a space>"` → EACCES; the Bash cwd is already the repo root — run `pnpm vitest run …` plainly. Never `cd` inside a compound command (dxb-prompt-gate blocks it).
- `git add` of a whole file while another session has a hunk in it mixes their work into your commit; stage `HEAD + your insertions` via `git hash-object -w` + `git update-index --cacheinfo`, or `git commit --only -- <path>`. Tonight's peer session (dxb-global-os-20) has closed; the tree is clean.
- A `__pycache__` appears in `~/.claude/hooks/` when a script imports the hook; use `PYTHONPYCACHEPREFIX` / `sys.dont_write_bytecode` and check the dir is gone before you finish.
- New skill and agent files are seen by the running session at once (measured tonight); plugins are not (2026-09-19).
- The verifier needs a diff: the hook is not in git — copy it to a backup BEFORE the builder writes, and diff against that.
- Read the bar: `dxb-ctx --pct` after every phase and before starting one; open every report with `CONTEXT: N%`. This session did not, and he caught it at 48 %.

## The first message you send him

The position, four lines, in Turkish (CLAUDE.md §0; empty lines dropped): (1) waiting his eye —
the `dxb-team1` door (B57, LAW B); (2) next — B59, the context-gate hook, starting now, then the
first team job on his "başla"; (3) blocked on him — his eye on B57 and "başla"; then, since he
opened nothing, one line: this session is the successor, opened at the predecessor's 48 %.
Open with `CONTEXT: N%` from `dxb-ctx --pct`.
