# EVIDENCE — B54: code is written by the builder subagent at max, and a hook holds it (2026-09-24)

Session fa4d10f2-131c-4369-9391-8762a60c0b5c (chief engineer, Opus 5.5 · xhigh), taking over from
1ee0bc86 (Phase A). His words: `code-by-builder-at-max-hook-2026-09-24`, `code-gate-plan-click-2026-09-24`,
`simple-code-at-medium-rule-2026-09-24` in `scripts/governance/ceo-approvals.json`. Waits for his eye (LAW B).

## Phase A (session 1ee0bc86, scratch folder only) — 7/7 probes passed
Subagent effort is carried by its own file; hook input carries `effort.level` and `agent_type`; a write at the
wrong effort is refused and the main session delegates by itself; `updatedInput` must never carry `model`
(the Agent schema takes aliases only); the status line — not the hook — receives `rate_limits`.

## T8 — medium against max on this repository's own past fixes
Each task: a clean copy at the fix's parent, the fix's own test laid in (measured RED at the parent, GREEN
with the shipped fix), the same task text, run once as `claude -p --agent builder|builder-lean` at `max` and at
`medium` (transcripts: every record `effort: max` / `effort: medium`). The four ran in parallel.

| Task | Effort | Time | Cost (API-equivalent) | Output tokens | Test (chief's own run) | Refuter |
|---|---|---|---|---|---|---|
| bb3cc10e — a resting lane hears stop() (1 file) | max | 354 s | $1.51 | 34,597 | 7/7 | equal (high confidence; 10 hidden-behaviour probes identical) |
| | medium | 63 s | $0.27 | 4,740 | 7/7 | equal |
| aa1a3afa — no closure without his word (2 files) | max | 593 s | $3.04 | 63,057 | 7/7 | better (medium confidence): the closed-row rule defined once |
| | medium | 84 s | $0.53 | 8,506 | 7/7 | two B findings: rule rebuilt in two places; only the first marker read |

Both guard versions convicted the same six rows (B19, B20, B23, B24, B38, C36) as the shipped fix; both beat
the shipped fix on a prototype-key hole (`CEO-OK: constructor`). Limits: one run per arm; both judges saw the
labels through the symlink names (the chief's harness error) — the verdicts rest on measured behaviour.

## Phase B — what was written
- `~/.claude/hooks/dxb-code-gate.py` (builder, max; 4 rounds) — registered in `~/.claude/settings.json`
  PreToolUse `Write|Edit|MultiEdit|NotebookEdit|Agent|Task|Bash`; the two older gates untouched.
- `~/.claude/hooks/dxb-statusline.js` — persists `rate_limits` to `$XDG_RUNTIME_DIR/claude-ctx/rate-limits.json`;
  rendered line byte-identical to the backup's (sha256, and the refuter's `cmp`).
- `tests/hooks/code-gate.test.ts` — spawns the real hook and the real status line.
- `~/.claude/agents/builder.md` (description, no-commit line), new `builder-lean.md` (medium).
- LAW A text: `.claude/CLAUDE.md` §2, `dxb-crew` §1–2 and §4, `~/.claude/CLAUDE.md` crew lines, memories
  audit-twin-rule / model-routing-hierarchy (v14) / opus-5-construction-governance and their mirror, STATE, B53/B54.
- Backups: `~/.claude/backups/2026-09-24-code-gate/`.

## Live proof (fresh sessions in the repository, flagless = Opus 5.5 · xhigh, bypass mode)
- L1: main `Write z.ts` → refused by the hook (log: xhigh, NORMAL, deny) → delegated to `builder-lean` (simple)
  → written; subagent transcript `claude-opus-5-5` · `medium`. The SessionStart position block arrived.
- L2: `builder-lean` wrote `s1.ts`; `s2.ts` refused, "This is not simple work (…second code file…)"; not on disk.
- L3: `builder` wrote `m.ts`; subagent transcript `claude-opus-5-5` · `max`; no refusal.
- Live quota file written by this session's status line: seven_day 49 %, five_hour 8 %.

## Dependants re-measured
dxb-prompt-gate: compound `cd` → BLOCKED, rc 2. dxb-cost-gate: blocked a live command of this session
(unbounded-loop shape). Status line renders; `dxb-ctx --pct` prints. Seven rulers, final run before the commit: opening-budget 6/6 · session-start-fits 4/4 · records-truth 4/4 · ledger truth OK (156 open markers, 84 board rows) · startup-budget GREEN (basket 10,903 ≤ 11,000; it went RED at 11,002 once mid-phase and the global crew line was shortened) · research-ruler 16/16 · research-ruler test 26/26. Refuter round 2: A list empty (A1, A2 PASS).

## Refuter (A / B / C) and the fixes
Round 1: A1 the guarded paths missed the repository's own money / approval / database / security code
(`bootstrap-db.sh`, `shared/src/db.ts`, `dxb-cli/src/approve.ts`, `reset-ceo-password.ts`, `critical-gate.ts`,
`fin/pnl`) → patterns added (builder, round 4); A2 a global crew line still sent one-line code to `builder` →
rewritten. B1 extensionless `#!` files and `.mts`/`.cts` → counted as code; B2 builder-lean's own test rule
contradicted the one-file envelope → it writes no new test file; B3 "refuses any other writer" was wrong (the
hook judges effort, not identity) → reworded; B4 a swallowed line break in model-routing-hierarchy → restored;
B5 two-seat leftovers in dxb-crew → rewritten; B6 the governance subagent sentence now names code writing.
Gate suite after round 4: 28/28 (the chief's own run). Round 2 checks the A list only.

Honest note: the builder edited the live, registered hook in two steps; for about one second at 02:46:26 a
call hit a half-written file (`NameError: is_code_file`, `~/.claude/logs/dxb-code-gate.err`) and the hook
failed open as designed — nothing was blocked, nothing written. Next time a registered hook is replaced
whole (copy, then rename).

## Known limits (C — recorded, not blockers)
Bash writing an extensionless `#!` file (`cat > pre-commit <<EOF`) is not seen (Bash detection reads the path).
Indirect writes are not seen (cp, mv, install, `python -c open()`, git apply, patch, `npx prettier --write`,
dd, truncate, find -exec sed -i, `$(…)` inside double quotes); MultiEdit `replace_all` counted once;
`sudo --user x tee`; perl options containing "i" may over-deny; a heredoc body with an odd number of `"`
inside `$(…)` may over-deny (use `git commit -F`); status line crashes on a null input (older);
`sync-codex-mirror.sh` rewrites `~/.claude/hooks/` paths to `.codex/hooks/` (older; the crew door now names
the hook without a path) and its `--check` was already red on `agent-reach` (older).
