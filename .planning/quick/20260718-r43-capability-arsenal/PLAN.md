---
task: R4.3 Capability Arsenal Expansion
spec_pointer: HOLDING-OS-MASTER-PLAN/IMPLEMENTATION_ROADMAP.md row R4.3 (CEO order 2026-07-18 00:20 + paid-bench ruling 00:30)
started: 2026-07-18T00:50:00+02:00
---

# PLAN — R4.3 Capability Arsenal Expansion (execution ticket)

Execution ticket ONLY — zero new design decisions. The design lives in roadmap
row R4.3 (deliverables a–f, c2) and the CEO rulings quoted there.

## Scope (from the row)

- (a) Arsenal doctrine doc in corpus: per-department target MCP/tool surface
  (sources: R4.2 need-matrix, 75 study cards, audit F-07 list; free-first D1;
  least-privilege classes).
- (b) FREE credential-less tranche installed + pinned + granted, live tool-call
  proof per install: playwright-class browser automation, git local, scrapling
  MCP bridge (ADOPT-step camoufox fix), context7-class docs lane.
- (c) Credential tranche NAMED (github PAT etc. — CEO hands secret, vault-only).
- (c2) PAID tools = PROPOSAL BENCH (studied + registered with price/trigger,
  never installed/spent).
- (d) Money-touching tranche stays Phase-11 LOCKED (→ R6.1).
- (e) dxb-mcp internal growth list (crm beyond 1 tool).
- (f) Every install = library rows + tool_pins (schema-hash) + grant chain
  (E9.5) + SkillSpector gate for third-party code.

## Evidence contract (gate)

1. Doctrine doc exists in HOLDING-OS-MASTER-PLAN/ + registered in 00-INDEX.
2. Free-tranche servers in grants.json catalog + tool_pins rows (schema-hash)
   + library items + library grants; recompiled profiles emit them.
3. ≥1 REAL tool_call per installed server (MCP tools/call round-trip, output
   quoted) + ≥1 end-to-end worker task landing tool_calls rows via the runtime
   chain.
4. Ghost-server count 8 → named dispositions table (installed / credential /
   Phase-11 locked); kurulumsuz-blind = 0 (every pendingInstall named).
5. Battery: pnpm build exit 0; vitest regression green; RULE #0 spot pass on
   /ai/library (new rows render, clipped 0, EN+TR).
6. Commits atomic, gitleaks clean each.
