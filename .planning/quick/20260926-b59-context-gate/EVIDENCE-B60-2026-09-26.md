# EVIDENCE — B60, the 80 % quota rule repaired in two places (2026-09-26)

Lead: the Fable 5.1 chief engineer session. His word ~00:45: *"önerini yap."*
(`lean-rule-repaired-2026-09-26`). Writer: the same `builder` instance that wrote B59 (opened at max
before the quota crossed 80 %; the gate's own text lets a writer mid-job finish). Fresh check:
`verifier` (high). Record text: the lead.

## The spec (fixed before the work)

In `~/.claude/hooks/dxb-code-gate.py` (backup taken first):
1. `LEAN_MIN_HOURS_TO_RESET = 24` beside his numbers, one place, with the comment idiom ("the
   engineer's number, his word 2026-09-26"). `current_mode()` returns LEAN only when
   `used ≥ LEAN_WEEKLY_PCT` AND the record is fresh AND `resets_at − now > LEAN_MIN_HOURS_TO_RESET × 3600`.
   Everything else NORMAL. The docstring says why (the level alone fired at 80 % with 9.5 h to the reset).
2. In LEAN, a medium write whose target matches `GUARDED_PATTERNS` is denied (every code path of a
   Bash command checked; an unreadable Bash target refused, as `gate_medium_write` already does),
   with a reason naming the guard and `subagent_type "builder"` at max. Max stays allowed everywhere.
   The docstring's "with no limits" for LEAN medium becomes "no envelope, but never a guarded path".
3. `tests/hooks/code-gate.test.ts`: the `quota()` helper's default `resetsInH` (48) already sits
   past 24 h, so the existing LEAN cases stay as they are. New cases: (a) 80 % with `resetsInH: 9`
   → NORMAL (a main session at xhigh is denied with the NORMAL reason; Agent(builder) not rerouted);
   (b) 80 % with `resetsInH: 25` → LEAN; `resetsInH: 23` → NORMAL; (c) LEAN, medium, `builder-lean`
   Write on `/home/x/.claude/hooks/x.py` → deny naming the guard; same for `builder-medium` and for a
   plain medium session; a non-guarded `/work/src/x.ts` at medium in LEAN still allowed; (d) LEAN,
   medium, Bash `echo x > /work/db/x.sql` → deny; (e) max on a guarded path in LEAN → allowed.

## Acceptance items — measured after the work

| # | Item | Command → expected | Result |
|---|---|---|---|
| 1 | old 38 gate cases green + the new ones | `pnpm vitest run tests/hooks` → all files pass | MEASURED — 74/74 (code-gate 38 → 46, context-gate 14, opening-budget 9, session-start-fits 5) |
| 2 | hand run: 80 %, reset in 9 h → NORMAL; reset in 48 h → LEAN | two stdin runs, a main-session xhigh Write on `/work/src/x.ts` → deny reason names max (NORMAL) / names medium (LEAN) | MEASURED — `reset in 9h → max` (NORMAL) · `reset in 48h → medium` (LEAN); log lines `"mode": "NORMAL"` / `"LEAN"` |
| 3 | hand run: LEAN, medium, builder-lean, Write `~/.claude/hooks/x.py` → deny naming the guard | stdin run | MEASURED — deny: `/home/dxb/.claude/hooks/x.py is a guarded path: it matches '(?:^|/)\\.claude/hooks/'` |
| 4 | the numbers in one place | `grep -n "^LEAN_WEEKLY_PCT = 80\|^LEAN_MIN_HOURS_TO_RESET = 24"` → 2 lines | MEASURED — lines 77 · 83 |
| 5 | dependants intact | context-gate tests 14 green · `dxb-statusline.js` sha unchanged · no `__pycache__` · opening ruler 6/6 · `verify:ledger` OK | MEASURED — context-gate 14 green; statusline sha cc09496e… unchanged; no `__pycache__`; ruler 6/6; ledger OK |
| 6 | the live machine tonight | `rate-limits.json` at 80 % with reset 10:00 → gate mode NORMAL (a real Agent(builder) call is not rerouted; decision log shows no reroute) | MEASURED — real record 81 % · reset in 9.0 h (23:00:34Z) → `Agent(builder)` through the real gate: empty stdout, nothing logged = NORMAL, not rerouted |

## A. The writer, the dead end, the decision

The same `builder` instance as B59 (opened at max at 00:15, before the quota crossed 80 % at
22:35Z; the gate's own text lets a writer mid-job finish — and B60 is the repair of that very
rule). It wrote both repairs and five cases (a)–(e), proved the cases fail on the backup hook
(4 of 5; (e) pins that max on a guarded path stays allowed), and proved NORMAL unchanged on 8
inputs old vs new (`SAME` ×8). **It named a dead end the spec had not seen:** in LEAN,
`route_builder` still turns `Agent(builder)` into `builder-lean`, so after the new deny ("hand it to
builder at max") no fresh spawn could reach max while LEAN held — and with repair (1) LEAN now
means the reset is more than a day away, so "wait" could mean days. **The lead's decision:** a
guarded lane is DECLARED by the lead — an `Agent(builder)` call whose `description` starts with
`guarded:` is kept at max in LEAN and logged `keep-max-guarded`; anything else is rerouted as
before; the deny reason names that exit. Cases (f)–(h). Also found and fixed by the writer on the
way: the OLD hook let `builder-medium` write a guarded path in LEAN (medium returned early before
its guard check) — measured with a Write to `/work/db/x.ts` at quota 90: old = allowed, new = deny.

## B. The lead's own measurements (01:00 → 01:10)

```
$ PYTHONDONTWRITEBYTECODE=1 pnpm vitest run tests/hooks → 46 · 14 · 9 · 5 = 74 passed (74)
$ grep -n "^LEAN_WEEKLY_PCT = 80$\|^LEAN_MIN_HOURS_TO_RESET = 24$" dxb-code-gate.py → 77: · 83:
80 % + reset 9 h  → "Opus 5.5 · max effort"    mode NORMAL
80 % + reset 48 h → "Opus 5.5 · medium effort" mode LEAN
LEAN, builder-lean @ medium, Write ~/.claude/hooks/x.py → deny "is a guarded path: it matches '(?:^|/)\.claude/hooks/'"
LEAN, Agent(builder) description "guarded: hooks" model opus → allow, updatedInput without model; log decision keep-max-guarded
real record 81 % · reset 9.0 h → Agent(builder) → empty stdout (NORMAL, not rerouted)
```

## C. A `__pycache__` that was not ours

The writer found `~/.claude/hooks/__pycache__/dxb-code-gate.cpython-314.pyc` written 00:53:17 by
session `e4dad022…` (the peer session running B56), which loaded the gate with `importlib` without
`PYTHONDONTWRITEBYTECODE`; listed, then deleted; the battery re-run 74/74. Told to the peer.

## D. The fresh verifier (Opus 5.5 · high, 79,964 tokens, 5.6 min) on B60 + B61

No A. **B60 PASS, B61 PASS.** Two B, both text: the LEAN deny reasons for an unmeasurable Bash target
and for a context not at an allowed effort still said "hand it to builder" without the `guarded:`
prefix, so a chief following them would be rerouted once more before reaching max (a dead end new
with B60) — closed by the writer in the same pass (§E). One C: writes through `cp`, `mv` or an
interpreter one-liner are not seen as writes, so in LEAN a guarded path can still be written that
way at medium — the same gap board row B58 already carries; it now matters in LEAN too (noted on
B58, not fixed here). Its own measurements: the new B60 cases fail 5 of 46 on the backup hook; old
vs new in NORMAL identical on 18 inputs × 6 quota states (108 comparisons, 0 diffs); the boundaries
24.00 h → NORMAL / 24.02 h → LEAN, 79.9 → NORMAL / 80 → LEAN, record age 5.98 h → LEAN / 6.02 h →
NORMAL; `tee a.ts b.sql` names `b.sql`; `guarded:` on builder-lean / builder-medium changes nothing;
a non-string `description` reroutes without error; no `.err`, no stderr, no leak into the live
records (31 = 31). It could not run the B61 test file against a 40/45 hook under vitest because the
live code gate refused its scratch `.py` copy (a verifier at high may not write code — the gate
working as built); it measured the same by running the hook source with the constants swapped
through a process substitution: 49 / 50 / 54 all fail there, pass on the new hook.

## E. The last round and the lead's final measurement (01:20)

The writer closed B1 and B2 as a per-mode suffix (empty in NORMAL): the LEAN reasons now say `with a
description starting "guarded:"`; NORMAL text proved byte-identical on both inputs against today's
hook and against the `.before-b60` backup (`SAME` ×4); three cases (i)–(k). The lead: battery
**77/77** (code-gate 49 · context-gate 14 · 9 · 5); LEAN at 90 %, a plain xhigh Write on
`/work/db/x.sql` → the reason carries `starting "guarded:"`; no `__pycache__`; final shas
code gate `c7c080ab…`, context gate `4356efd3…`, status line and settings.json unchanged.
Writer totals for the night (one `builder` instance, five rounds, B59 + B60 + B61): 305,537 tokens.

## F. The third repair — the pace rule, on his word ~01:30 (`quota-pace-rule-2026-09-26`)

**Spec (fixed before the work).** `dxb-statusline.js`: beside `rate-limits.json`, append one line to
`claude-ctx/rate-limits-history.jsonl` — `{ts, seven_day:{used_percentage, resets_at}, five_hour:{…}}` —
at most once per 60 s (skip when the last line is younger and the numbers are unchanged), pruned to
24 h when the file exceeds 2,000 lines; best effort like the record. `dxb-code-gate.py`: constants
`PACE_SAFETY = 1.5`, `PACE_WINDOW_HOURS = 3`, `PACE_MIN_SPAN_MINUTES = 15`, one place; `current_mode()`
returns LEAN when the record is fresh AND (his level rule as repaired: used ≥ 80 with > 24 h to the
reset) OR the pace rule for either window: rate = (used_now − used_oldest) / hours between them, over
history lines from the last PACE_WINDOW_HOURS that carry the SAME `resets_at` as the current record
(a reset breaks the series), needing ≥ 2 lines ≥ PACE_MIN_SPAN_MINUTES apart and a rate > 0; LEAN when
`100 − used < hours_to_reset × rate × PACE_SAFETY`. No history, too short, corrupt, or a different
reset → no pace decision (the level rule alone). The decision log's `mode` field becomes `LEAN` with a
new `why` field (`level` · `pace-7d` · `pace-5h`; absent in NORMAL). NORMAL byte-identical otherwise.

| # | Acceptance | Command → expected | Result |
|---|---|---|---|
| 1 | history written and throttled | statusline test: two renders 1 s apart, same numbers → 1 line; changed numbers → 2 lines; a 2,100-line file with 30 h-old lines → pruned to the 24 h lines | MEASURED — statusline cases green; live: 3 renders (47, 47, 48) → 2 lines |
| 2 | tonight's shape → LEAN(pace-7d) | history 75 → 82 over 2.3 h, used 82, reset 8.6 h → `mode LEAN`, `why pace-7d`; a main-session xhigh Write deny names medium | MEASURED — case green (LEAN, why pace-7d) |
| 3 | boundary | same history, reset 4.0 h → 4×3×1.5 = 18 = remaining → NORMAL (strict `<`); reset 4.1 h → LEAN | MEASURED — the boundary re-cut by the writer: 76 → 82 over exactly 2 h (3.0/h) so 4 × 3 × 1.5 = 18.0 = remaining → NORMAL; 4.1 h → LEAN (the spec's own 2.3 h series gave 3.04/h and 18.26 > 18, LEAN — the spec's arithmetic was wrong, the rule was not) |
| 4 | no pace decision | one line only; two lines 10 min apart; lines with another `resets_at`; corrupt file; missing file → NORMAL at used 60 | MEASURED — green; three guards each proved live by loosening a copy |
| 5 | five-hour window | 5h used 70, reset 1.5 h, history 40 → 70 over 1 h → LEAN(pace-5h) | MEASURED — green |
| 6 | level rule still there | used 80, reset 48 h, no history → LEAN(level) | MEASURED — green |
| 7 | guarded lane unchanged | LEAN(pace), `Agent(builder)` `guarded:` → keep-max-guarded | MEASURED — green |
| 8 | NORMAL identical | old (`c7c080ab…` backup) vs new on the B60 verifier's input set, NORMAL states → 0 diffs | MEASURED — old vs new SAME 16/16 on three NORMAL states |
| 9 | battery | `pnpm vitest run tests/hooks` all green | MEASURED by the lead — 85/85 (code-gate 57 · context-gate 14 · 9 · 5); the bar's stdout old vs new SAME; no `__pycache__`; context gate and settings.json shas unchanged |
| 10 | live | the real `rate-limits.json` + real history after a few renders → `python3 -c` calling `current_mode()` prints LEAN (tonight's pace) — and a real `Agent(builder)` call is rerouted | MEASURED by the lead at 01:48: real record 84 %, reset 8.3 h, history 6 lines over a few minutes → `pace=` blank, `gate=NORMAL` — **then at 01:51, with 16 min of real history: `7d used=84% reset_in=8.1h pace=3.8/h lasts=4.2h | 5h used=44% reset_in=1.1h pace=19.0/h lasts=3.0h | gate=LEAN(pace-7d)`, and a real `Agent(builder)` call through the real gate → rerouted to `builder-lean`, log `"why": "pace-7d"`.** His worry, measured and held: at that pace the week ran out 4 h before the reset, so every NEW writer from 01:51 opened at medium; the running ones finished at max |

**`dxb-quota`** (his word ~01:40, `quota-read-per-lane-2026-09-26`): `~/.local/bin/dxb-quota`, stdlib,
imports the real gate with `sys.dont_write_bytecode = True` and reports `current_mode()` + `MODE_WHY`
as the verdict; `--json`, `--mode`; exit 3 without a record. The lead's live run 01:48:
`7d used=84% reset_in=8.3h pace= lasts= | 5h used=41% reset_in=1.3h pace= lasts= | gate=NORMAL`.
The door's QUOTA step (`dd8e4990`) named it before it existed — the other session caught that and
read the numbers by hand meanwhile; the command landed at 01:47.

## G. The fresh verifier on the pace rule (Opus 5.5 · high, 82,440 + 85,830 tokens, 7.1 + 0.4 min)

Round 1: statusline PASS, tests PASS, `dxb-quota` PASS, gate **FAIL — one A**: `pace_fires` measured
hours-to-reset from `written_at` and never looked at the real clock, so a window ALREADY reset (record
still ≤ 6 h old) fired LEAN (measured: 5h window reset 1 h ago → `LEAN pace-5h`; 7d the same →
`LEAN pace-7d`). The lead reproduced it by hand (`reset_in=-1.0h … gate=LEAN(pace-5h)`), which stood in
for the door's xhigh refuter step on that A — the finding was unambiguous and measured twice, and the
quota was the night's constraint; the deviation is named here. One C: a `written_at` from the future
counted as fresh (old and new alike). Its own sweep: 220 old-vs-new comparisons (22 record shapes ×
2 histories × 5 calls) — NORMAL 190/190 identical, LEAN(level) 30/30 identical but for the new `why`;
`why` never in a NORMAL line; corrupt / huge / future-ts / foreign-reset histories all sane; prune
kept 1,002 of 2,102 lines, no `.tmp`; an unwritable history dir never fails the bar; `dxb-quota` exits
3 / 2 / 4 as specified. Writer's fix: `resets_at > time.time()` in `pace_fires`; fresh = `0 ≤ now −
written_at ≤ 6 h`; two cases (gate 57 → 59). Round 2 (A list only): A closed (four scenarios, each
with its control), C closed, battery **87/87**. The lead: 87/87, reset −1 h → NORMAL, +1 h → LEAN.
Display-only leftover, not fixed: `dxb-quota` still prints `pace`/`lasts` for a window past its
reset while the verdict beside them is right.
