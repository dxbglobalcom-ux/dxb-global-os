Coverage gap first: I read 43 of the 106 persona bodies in full. The other 63 (product, project-management, quality, revops, risk-audit, sales, 7 security, social-media, strategy) were checked by full-text grep, not read line by line. The brief asked for all 106; details are in the last section.

**Summary.** Part 1 has 12 findings: G3 1 · 1f 1 · 1c 3 · 1b 1 · 1a 1 · 1d 1 · G2 3 flags. Four of them are his-words. Part 2 covers the 8 `query(` call sites and has 13 findings: G4 6 · 1d 2 · 1f/anti-format 2 · 1a 1 · 1b 1 · 1c 1.

- **Highest impact.** Six call sites omit `settingSources`: chat, voice, classify, decompose, the workflow executor and council. The SDK then loads every settings source, so the construction CLAUDE.md files and session hooks reach Hamza's own answers to the CEO. That includes the global "Respond terse like smart caveman… Fragments OK". The repo fixed this for seats and QA only (`sdk-isolation.ts`).
- The decompose prompt still says "longest chain <= 3", but the code allows 5 by the CEO's order of 2026-08-27.
- The workflow executor runs seats with no effort and no persona.
- In chat and voice, an `xhigh` routing row runs at `low` (I confirmed the whitelist).
- In the personas, 91 of 106 tell the seat to use `notify_broadcast` and 33 tell it to use `WebSearch/WebFetch`. No seat holds any of these tools.

Paths below use `P=/home/dxb/DxB Global OS/personas` and `R=/home/dxb/DxB Global OS`.

## PART 1 — personas 108–213

| # | Location | Evidence | Pattern | Why obsolete for Opus 5.5 | Conf | Action | Replacement | Bucket |
|---|---|---|---|---|---|---|---|---|
| 1 | 91 files `notify_broadcast`, e.g. P/marketing/marketing-social-media-strategist.md:113, P/quality/testing-test-results-analyzer.md:114, P/strategy/venture-builder.md:117. 33 files `WebSearch/WebFetch`, e.g. P/marketing/marketing-social-media-strategist.md:111. 3 files "web fetch and search". | "notify_broadcast ('dxb:live' work events): campaign/publication states visible in the task stream." | G3: tool names in the prompt shadow the real tool list | Seats run with built-in tools off (`R/packages/orchestrator/src/worker-shim.ts:376` sets `tools: []`). No profile in `R/packages/gateway/profiles/*.mcp.json` grants these names. `notify_broadcast` is a SQL function called by code (`R/packages/gateway/src/library-profiles.ts:243`). The model follows §9 literally, so it looks for missing tools or claims checks it cannot run. Also live on Opus 5. For `P/product/product-trend-researcher.md:109` ("the primary instruments") this is a missing capability, not a text problem. | High | remove / rewrite | Delete the `notify_broadcast` line. Research line, EN: "Research: this seat holds no web tools — when an outside source is needed, it opens a research task through the queue." TR: "Araştırma: bu koltukta web aracı yoktur — dış kaynak gerektiğinde kuyruğa araştırma görevi açar." | runtime |
| 2 | 106 files (escalation line) and 88 files (cadence line) in §8, e.g. P/security/ciso.md:106, P/media-studio/media-film-director.md:149, P/platform/sre.md:106 | "Escalation language: one sentence — which account/surface, what happened, visibility scale, response underway, decision needed." | 1f numeric ceiling | Forces 5–6 items into one sentence. That is the telegraph style the CEO rejected, and the standing layer that follows forbids it (`R/packages/voice/src/prompt-core.ts:82-97`: conclusion first, whole sentences). Also live on Opus 5. | Medium | rewrite | EN: "Escalation language: conclusion first — what happened, where, and the decision that is his — then the evidence and the exposure, in plain whole sentences." TR: "Eskalasyon dili: önce sonuç — ne oldu, nerede, kendisine düşen karar — sonra kanıt ve etki; sade, tam cümlelerle." Cadence line: "immediate single line" → "an immediate short alert"; "ANINDA tek satır" → "ANINDA kısa uyarı". | runtime |
| 3 | 55 files "Language: English (project artifact…", e.g. P/marketing/marketing-tiktok-strategist.md:106. 36 files "Dil: rapor Türkçe", e.g. P/security/ciso.md:107, P/platform/sre.md:107 | "Dil: rapor Türkçe; güvenlik terimleri İngilizce aynen (least-privilege, containment, rotation)" | 1c: duplicated rules that disagree (keep-list 8 exception) | Contradicts the lane line that comes after it. Task lane: "every deliverable and every record in English" (prompt-core.ts:134). Voice lane: "Answer the CEO in Turkish" (:138). The language law also requires a short explanation in brackets (:85-86). The 14 media-studio seats already use a consistent form. | Medium | rewrite | EN: "Language: Turkish to the CEO, English in every artifact (CEO directive 2026-07-12); technical terms verbatim, each explained once in plain words." TR: "Dil: CEO'ya Türkçe, her artefakt İngilizce (CEO direktifi 2026-07-12); teknik terimler aynen, her biri bir kez sade sözle açıklanır." | runtime |
| 4 | 60 files §2, e.g. P/security/ciso.md:60, P/paid-media/paid-media-ppc-strategist.md:60, P/quality/testing-reality-checker.md:60 | "Fixed reasoning order for every engagement: (1) business goal → audience map … (5) measurement contract" | 1c step script for judgment work; 1b "plan before acting" | Thinking is always on and the model plans by itself. A mandated fixed order over-constrains judgment. The listed considerations are context and stay. Keep any "X first, because…" step that states its reason. Also live on Opus 5. | Medium | rewrite (lead-in only) | EN: "Weighs every engagement on:" TR: "Her işte şunları tartar:" (items unchanged) | runtime |
| 5 | Code: R/packages/voice/src/persona.ts:50. Text: 212 comment lines, 2 per file, e.g. P/security/ciso.md:50,131 | "<!-- Constitutional section — … Uniform by design (G8); persona gate FAILs without it. -->" | 1c grader wording; G2 history (author tag `fable-5`) | The runtime sends author-facing metadata to the model. Stripping comments at load time leaves the files, the gate and the sync untouched. | Medium | rewrite (code) | `return text.slice(start).replace(/<!--[\s\S]*?-->\n?/g, "").trim();` | runtime (the §12 comment stays in the file) |
| 6 | 106 files §11 (99 files say pre-task/post-task), e.g. P/marketing/marketing-social-media-strategist.md:124-125, P/security/ciso.md:126-127 | "…drafts without named-person approval references are rejected post-task … On violation: the run halts fail-closed, writes to hook_violations, alerts the CMO." | 1c grader wording | Describes the checking machinery instead of the requirement, which pushes effort toward being watched. The gate still enforces the rule either way. | Medium | rewrite | "X without Y references are rejected post-task" → "every X carries its Y reference". Delete the "On violation / İhlalde davranış" line. Keep `hook_version: vN` (the gate reads it: `R/packages/hr/src/template.ts:55`) and the CEO-exception line. | runtime |
| 7 | 106 files §12, e.g. P/security/ciso.md:135-136 | "Plan before execution: understand → plan → execute → verify → report … Self-review before handoff: output is re-checked against §6" | 1b plan-before-acting; Opus 5 self-check / over-verification re-test | Anthropic's Opus 5 guidance: telling the model to verify or re-check causes over-verification, and removing it costs no capability. The evidence-before-done law stays. Also live on Opus 5. | Medium | rewrite — for HIM | "- Evidence before done: "done" exists only with executed evidence (Evidence-Before-Done); a check that was not run is never reported as run." / "- Complete handoffs: a handoff carries complete context and open risks — silent gaps are defects." | his-words (D5+D6: R/HOLDING-OS-MASTER-PLAN/00-CEO-DIRECTIVE-REVENUE-FIRST.md:29-30; canonical text EMPLOYEE_PERSONA_STANDARD.md:63-64) |
| 8 | 106 files §12, e.g. P/security/ciso.md:138 | "No lazy proposals: … mainstream-by-default without research is a violation." | 1a laziness booster | Current models act on their own by default; "lazy / violation" makes the model more cautious. The D4 content stays. | Medium | rewrite — for HIM | "- Researched proposals: every recommendation rests on researched alternatives with strong tooling (ruling D4)." | his-words (ruling D4; standard §4.1) |
| 9 | P/media-studio/media-advertising-director.md:89, media-character-identity.md:57,124, media-creative-director.md:71,73, media-film-director.md:63,68,69,75, media-product-brand-consistency.md:122, media-storyboard-previz.md:118, media-ai-video-engineer.md:74 | "The general default is text-to-video, and image-to-video is not forbidden (CEO 2026-09-04 22:35)." / "Never assumes that text-to-video is forbidden" | 1d phrasing relative to an older rule; 1c a prohibition that names the failure | Refers to a rule the model never saw and implies there is another one. Stating the current rule alone is enough. | Medium | rewrite — for HIM | "The general default is text-to-video; both roads are open, and the brief names which (CEO 2026-09-04 22:35)." Delete media-film-director.md:75. "(the Flux faces are out of use, 2026-09-13)" → "(no face is drawn outside the engine, 2026-09-13)". Lines :71/:63 → "on this station a one-take talking film of 15 s held (EYW-002C, 003, 005); beyond the measured hold the engine invents." | his-words (dated rulings; tests/b43/road-consistency.test.ts:72 accepts "both roads") |
| 10 | P/security/ciso.md:102; P/social-media/social-inbox-agent.md:101 | "**MUST-B amendment (D7-D, 2026-07-12 — [[WORKFORCE-MUST-EXPANSION-PLAN]] §5, Fable in person):**" | G2 history (incident id, wiki link, author) | The seat cannot resolve the link or the id; the rule's authority is its content. | Low | flag | — | runtime |
| 11 | 106 files §12, e.g. P/security/ciso.md:132,142 | "the discipline of Fable 5 and Solo 5.6 Ultra" / "Inheritance: every future persona is created with this section verbatim" | G2 pinned model names; 1c text that does not apply to the seat | Names retired models, and gives an authoring instruction to seats that never write personas. | Low | flag | — | his-words (quoted Talep §5.12) |
| 12 | P/people-hr/persona-workforce-architect.md:55,64,79; P/media-studio/media-character-identity.md:96,131 | "tüm yazarlığı Fable 5'tedir … (bu dosya Fable yazımıdır)" / "a board rule, not a registered law" | G2 pinned model name; construction-governance wording | The author seat is now Opus 5.5. The "law vs board rule" distinction is construction governance the seat cannot act on. | Low | flag | — | his-words (K2; the CEO's rule on what becomes a law) |

Re-verify, with `S=/tmp/claude-1000/-home-dxb-DxB-Global-OS/6176c228-1529-4280-b4b4-74440cbfa8f3/scratchpad/audit/slice-c.txt`. Each command is `xargs -d '\n' grep -n -E '<pattern>' < "$S"`:
1. `notify_broadcast|WebSearch/WebFetch|web fetch and search`
2. `^(Escalation language: one sentence|Eskalasyon dili: tek cümle)|^(Cadence|Sıklık):.*(single line|tek satır|one line)`
3. `^(Language: English \(project artifact|Dil: rapor Türkçe)`
4. `Muhakeme sırası sabittir|Fixed reasoning order`
5. `<!--`
6. `pre-task|post-task|^(On violation|İhlalde davranış):`
7. `^- (Plan before execution|Self-review before handoff)`
8. `No lazy proposals`
9. `is not forbidden|is forbidden, or|out of use|were superseded`

## PART 2 — runtime prompt and request code

| # | Location | Evidence | Pattern | Why obsolete for Opus 5.5 | Conf | Action | Replacement | Bucket |
|---|---|---|---|---|---|---|---|---|
| 1 | R/packages/orchestrator/src/chat-drain.ts:112-126; R/packages/voice/src/answer.ts:108-128; R/packages/kernel/src/classify.ts:56-68; R/packages/orchestrator/src/decompose.ts:115-124; R/packages/kernel/src/workflow/executor.ts:103-119; R/packages/orchestrator/src/council.ts:90-99 | No `settingSources` or cwd. Only worker-shim.ts:371 and qa.ts:85 spread `workerIsolation()`. | G4 architecture; 1d instruction re-inserted on every prompt (hooks) | SDK doc: "When omitted, all sources are loaded … Must include 'project' to load CLAUDE.md" (sdk.d.ts:2061-2069). The scheduler runs in the repo root as user `dxb` (`~/.config/systemd/user/dxb-scheduler.service:11`). So these calls load ~/.claude/CLAUDE.md (the caveman rule), the project CLAUDE.md, the spec-bootstrap.sh session hook, no-laziness.sh + ceo-language.sh on every prompt, and `effortLevel: medium`. The repo already recorded this leak for seats (sdk-isolation.ts:4-10). Also live on Opus 5. I did not run a model call, so the exact injected text is unverified. | High | add | Add `settingSources: [], cwd: process.env.DXB_REPO_ROOT ?? process.cwd(),` to each options object. Move `workerIsolation` to @dxb/shared: voice and kernel cannot import orchestrator without a cycle. | runtime |
| 2 | chat-drain.ts:116-120; answer.ts:117-121 | `["low", "medium", "high", "max"].includes(r.effort ?? "") ? r.effort : "low"` | G4 request config | `xhigh` has been legal in routing_rules since R/db/migrations/20260903190000_b43_media_hands.sql:25. A chat or voice row set to xhigh silently runs at `low`. Current rows are max / medium / low (20260726003000_chat_legs.sql:27-28), so nothing is wrong yet — it triggers the day the CEO sets xhigh. On Opus 5.5 effort is the only control on thinking. Also live on Opus 5. | High | rewrite | `const EFFORTS = ["low","medium","high","xhigh","max"] as const;` then `effort: (EFFORTS as readonly string[]).includes(r.effort ?? "") ? (r.effort as (typeof EFFORTS)[number]) : "medium",` | runtime |
| 3 | executor.ts:103-118 | Options set `model`, `tools`, `maxTurns`, `outputFormat` — no `effort`. | G4: effort omitted | On Opus 5.5 an omitted effort runs at the API default `medium`. Because settingSources is also omitted, the CLI may instead take `effortLevel` from ~/.claude/settings.json. That breaks U21: effort must come from the routing row (answer.ts:112). Which default actually applies is unverified. | High | add | Carry the route's effort on AgentWork and pass `effort: work.effort` (same cast as worker-shim.ts:369). | runtime |
| 4 | executor.ts:74; worker-shim.ts:442-447 | "You are ${work.employeeSlug}, a DXB Global OS employee agent (${work.department})." | 1d an identity line standing in for context | Workflow steps run a named seat with no persona, no laws and no approval line. worker-shim delivers these as `systemPrompt` (:363-371, B43 plan ②). worker-shim's LiteLLM path also sends no seat prompt. | High | rewrite | The runner passes `seatStandingPrompt(employee)` on AgentWork; the executor spreads `systemPrompt` plus isolation; the API paths prepend it as a system message. | runtime |
| 5 | decompose.ts:158 (vs :38) | "Keep the longest chain <= 3 envelopes." while `MAX_HOP_DEPTH = 5` | 1d text that outlived its rule | The CEO raised the cap to 5 on 2026-08-27. The model follows the prompt literally, so it drafts ≤3 and quietly brings back the old limit. Also live on Opus 5. | High | rewrite | `` `  (0-based, forward references forbidden). Keep the longest chain <= ${MAX_HOP_DEPTH} envelopes.` `` | runtime (carries out his order) |
| 6 | chat-drain.ts:105 | "No markdown headers. Keep it under 8 sentences unless the CEO asked for depth." | 1d anti-formatting rule; 1f numeric cap | The chat board shows raw text and does not render markdown (R/apps/dashboard/src/components/chat/chat-board.tsx:352, `whitespace-pre-wrap`). So the real rule is "no markdown symbols at all", and it needs its reason. The 8-sentence cap fights PLAN MODE and the explain shape of the language law. | Medium | rewrite | "His chat board shows plain text and does not render markdown: write plain prose, and when you list steps or options use plain numbered lines without #, ** or other markdown symbols. Answer at the length the question needs." | runtime |
| 7 | chat-drain.ts:102 | "PLAN MODE is ON: … DO NOT start any work" | 1a pressure wording | The call has `tools: []`, so it cannot dispatch anything anyway. The capitals add register, not information. | Medium | rewrite | "Plan mode is on: think the CEO's topic through with him — propose a concrete plan (goal, steps, who does what, rough cost) and ask what to adjust. Work starts only when he dispatches it himself." | runtime |
| 8 | answer.ts:99 | "This is a VOICE call: answer in 2-4 short spoken sentences, no markdown, no lists." | 1f numeric cap (the no-markdown part is a real format need — keep it) | Keep the format need, state its reason (text-to-speech), drop the number. Also live on Opus 5. | Medium | rewrite | "This is a VOICE call and the answer is read aloud by text-to-speech: answer in a few short spoken sentences — only what the question needs — with no markdown or list symbols, which the voice would read out." | runtime |
| 9 | chat-drain.ts:92-113; answer.ts:91-109; prompt-core.ts:194-203 | `${sys}\n\nConversation so far:…\n\nCEO says: ${q.message}\n\nHamza replies:` — no `systemPrompt` (the SDK then sends an empty one: sdk.mjs `if(s===void 0)p=""`). `memoryBlock` is 3rd, above five fixed lines. | G4 cache-hostile ordering; 1b completion cue in the user turn (no 400) | The standing layer — Hamza's persona alone is 19,459 bytes — is re-sent without caching on every answer, and per-turn memory sits above fixed text. On Opus 5.5 cache reads cost 0.05× of normal input, so each miss costs relatively more. Chat drains every 3 s (outbox-executor/src/scheduler.ts:171). The real savings are unverified. | Medium | move | `systemPrompt: [identity, persona, language, law, honesty, gate, noRefusal, ...laneLines, SYSTEM_PROMPT_DYNAMIC_BOUNDARY, memory, legInstruction]`; prompt = history + "CEO says: …"; put `memoryBlock` last in `standingPrompt`; drop "Hamza replies:". | runtime |
| 10 | classify.ts:126-127, 75-81, 144-153; decompose.ts:146-147, 129-135; qa.ts:70-72, 47-51, 172-179; council.ts:71; worker-shim.ts:314-318; executor.ts:75-78 | "Output JSON ONLY — no prose." next to `outputFormat: {type:"json_schema"}` | 1b JSON-forcing text beside structured outputs | The schema already carries the shape, and the prose copy can drift from the zod schema. The prose is only needed on the LiteLLM paths (qa.ts:116-121, worker-shim.ts:442-447, executor.ts:156-161), where no schema is sent. The fence-unwrap and retry fallbacks were written for opus-4.8 (classify.ts:62-64); re-test them on 5.5 before removing. | Medium | replace-with-API-feature | On the subscription path, delete the JSON-only lines. Build `task_class: z.enum(taskClasses)` per call. Send the prose schema only when `mode !== "subscription"`. | runtime |
| 11 | worker-shim.ts:266-272 (+ :333-336, :327-329) | "verify with a real tool call before you answer … a task you moved yourself is judged empty." | Opus 5 over-verification re-test; 1c grader wording | The verification requirement belongs once, in the block that is only sent when the seat has tools. TASK_LANE_LINE sends it to tool-less seats too. "Judged empty" and "rejected by the gate" describe the grader. Also live on Opus 5. | Medium | rewrite | "You are working ONE task from the company's queue, as this seat and nobody else. The task's objective and output contract are the whole job: deliver exactly that with the tools you were given, and never claim a check you did not run. Your final answer IS the delivery — the road moves your task the moment you answer — so never move your own task's status (no queue_transition, no queue_return on it) and never claim tasks (no queue_claim)." Line :329 → "When the contract names a file ref, the file entry carries that ref exactly." | runtime |
| 12 | All 8 sites, e.g. chat-drain.ts:130, 228-231 | No `fallbackModel`. An empty `msg.result` is stored as Hamza's reply. | G4 migration checklist [BLOCKS]: refusal handling and fallback | Opus 5.5 adds `bio` and `reasoning_extraction` classifiers, and their declines arrive as normal results. Only matters if B51 moves the runtime to Opus 5.5. | Medium | add | `fallbackModel: SDK_MODEL_IDS["opus-5"]` on calls routed to 5.5; treat an empty successful result as a failure. | runtime |
| 13 | classify.ts:32-41; council.ts:26-31 | `"opus-4.8": "claude-opus-5"`; no Opus 5.5 slug; `judgeEscalation: "opus-4.8"`; judge effort hard-coded `"medium"` | G4 pinned model IDs | No slug maps to `claude-opus-5-5`; that is B51, his decision. council.ts is dead in production — its only callers are orchestrator/src/index.ts:65 and tests/phase5/council-judge.test.ts. It is the LOCKED CNCL-01 part, so removal is his call. | Low | flag | — | runtime |

Outside prompt scope: `executor.ts:26-30` WorkerEvidence has no `ref` field. That is the same zod-strip bug already fixed at worker-shim.ts:129-136.

Deterministic-step check: all 8 call sites do adaptive work (intent, decomposition, QA judgement, the task itself, council judgement, chat and voice answers). None should be replaced by plain code. `classifyLeg` is already plain code.

## Keep-list decisions
1. `R/packages/orchestrator/src/chat-legs.ts:180-188` "ONLY these numbers… KUSUR DEĞİLDİR": the CEO corrected this failure twice (keep-list 5), and it is pinned by tests/c9/chat-legs.test.ts:114, 121-122.
2. In prompt-core: `noRefusalLaw`, `honestyLine`, `ceoLanguageLaw` are his orders (2026-08-20, RULE #0-A, standing order 14). `identityLine` "HE IS TALKING TO YOU" guards a failure that happened (Hamza said Hamza was unavailable) (keep-list 5).
3. The voice TOPIC line (answer.ts:103, "MUST be exactly") is a format the code parses at :275 (keep-list 3/7).
4. The persona lines "Never assumes:" / "Asla varsaymaz:" (106 files) are role context (keep-list 1).
5. "NEVER records:" / "ASLA kaydetmez:" (106 files) is one capitalised label per file carrying data-protection rules. Too sparse for 1a (keep-list 5).
6. The §11 heading "Fable 5 hook binding" and `hook_version` are gate keys (template.ts:33, 55) (keep-list 4).
7. §12 Islamic conduct, Evidence before claim, Accountability: constitutional, his.
8. The road block repeated word for word in 10 media-studio files, and the no-TTS rule repeated in media-sound-music.md: the copies agree and are test-pinned (keep-list 8).
9. media-vfx-post "100 % zoom" checks and the identity/product meters run on crops: these are measurement tools for video defects, not vision crutches (the 1d visual row explicitly keeps tools).
10. The 17 "uydurma / hallucinate" prohibitions are role-specific and carry reasons. Removing them would be low-confidence (1c).

## Coverage and tests to change with any hunk
- **Read in full (43):** 9 marketing, 14 media-studio, 7 paid-media, 6 people-hr, 6 platform, and security/ciso.md.
- **Not read line by line (63):** product, project-management, quality, revops, risk-audit, sales, security (7), social-media, strategy. For these I did three things: checked that the template lines are identical across all 106 (`uniq -c`), used hits.tsv, and grepped the full text for model names, dates, wiki links, amendment headers, numeric caps, self-check wording, markdown/anti-format rules and tool names.
- **Why not all 106:** reading the 63 in full would have taken this session to about 900K of its 1M context. A follow-up reader is needed if the brief's "all 106" must hold.
- **Tests that pin text proposed above:**
  - tests/b43/dispatch-book.test.ts:201-216 (seat prompt text and `workerIsolation` shape)
  - tests/b21/agent-context.test.ts:43-126 (standing layer; re-run after the reorder)
  - tests/c9/chat-legs.test.ts:114, 121-122
  - tests/u15r2/voice-round2.test.ts:155 (TOPIC)
  - tests/r31/persona-delivery.test.ts:28-58 (persona loader)
  - tests/b43/road-consistency.test.ts:72, 121
  - tests/phase6/poisoning.test.ts:196-205 — a hand copy of the worker prompt, not an import; it will drift silently
  - tests/phase5/council-judge.test.ts
  - packages/hr/tests/gate.test.ts and tests/personas/persona-ruler.test.ts:62 (for §12, together with EMPLOYEE_PERSONA_STANDARD.md §4.1)
- **Not pinned by any test:** "No markdown headers", "under 8 sentences", "Hamza replies:", "2-4 short spoken", "Output JSON ONLY", "<= 3 envelopes", "VERIFY your work".